# DK-048: LLD: диспетчер пачки задач, параллельное исполнение независимых задач бэклога

Задачи берутся с доски по одной, и на пачке однотипной мелочи это выходит
дорого. В хвосте бэклога сейчас лежат DK-024, DK-027, DK-028, DK-031, DK-035:
пять багов ценой S, независимых друг от друга, каждый на один коммит с тестом.
Диспетчер заходит за ними пять раз подряд, хотя разработка у них не связана
ничем.

Механика под параллельную работу почти вся уже есть. `shipctl start` разводит
задачи по отдельным worktree, так что исполнители не топчутся в одном
чекауте. `agentctl pick` считает исполнителя и ревьювера без участия глаза.
Поезд выката (`merge --train` плюс `ship`) описывает, как пачка едет на прод
одним выкатом и проверяется позадачными сценариями. Не хватает слоя сверху,
который отберёт пачку и разложит её по субагентам.

Что спроектировать: отбор кандидатов командой в taskctl и скилл-диспетчер,
который гоняет пачку по конвейеру. Отбор именно командой, а не рассуждением
агента: критерии для поезда уже сформулированы (одно приложение, задачи
независимые, цена S или M со снятой неопределённостью, готов сценарий
проверки, не больше 3-5 задач), и правило прямо требует не решать такое на
глаз. Команда отдаёт список кандидатов и причину отказа для остальных, а это
уже проверяется тестами. Процедуру сборки пачки держать скиллом, а не в
правилах: `RULES.board.md` резидентный, и DK-029 как раз про его вес.

Открытые развилки:

- доска одна на всех. Если каждый исполнитель сам зовёт `taskctl move` и
  пушит, пачка передерётся и за `docs/TASKS.md`, и за индекс основного
  чекаута, тем более что доску положено пушить сразу. Либо доску трогает
  только диспетчер и исполнителям это запрещено явно, либо в taskctl
  появляется блокировка на время правки;
- квоты. `agentctl pick` правит вердикт по остатку недельных лимитов, но
  считает его на одну задачу. Пять параллельных исполнителей плюс пять
  ревьюверов расходуют совсем не то, что те же десять запусков вразбивку, так
  что размер пачки надо выводить из остатка, а не только из потолка «3-5
  задач»;
- порядок обхода бэклога. Верхушка сейчас занята китом DK-033, где почти всё
  висит на `[после ...]`, и первые N строк сверху в пачку не годятся. Отбор
  должен ходить по всему бэклогу с учётом зависимостей;
- где кончается параллельность. Разработка и ревью идут одновременно, слияние
  с выкатом остаются строго последовательными, и очередь выката это уже
  держит. Проверить, что предусловия `merge --train` не ломаются, когда
  задачи приезжают готовыми вперемешку.

Долгоживущая дока: раздел про поезд выката в `RULES.board.md` (п. 9) и
README той утилиты, куда сядет отбор.

## Ход работы

- Исполнение: субагент opus/xhigh по вердикту pick, 2026-08-01.
- Дизайн написан: [lld/DK-048-batch-dispatcher.md](../lld/DK-048-batch-dispatcher.md),
  все четыре развилки постановки сняты решениями, разбивка на пять
  исполнительских задач в конце документа.

## Ревью

- agentctl budget: вывод «до fable пачка не доходит по построению» неверен, корректор поднимает opus -> fable как раз на свежем профиците, то есть в том самом состоянии, где потолок пачки 5; пять исполнителей M тогда жгут week_fable, который budget сознательно не смотрит: исправлено, потолок считается через correctModel на opus, подъём на fable режет пачку до 3
- пример вывода taskctl batch расходится с объявленным порядком проверок: DK-043, DK-044, DK-045 и DK-046 показаны в группе незакрытых зависимостей, хотя неопределённость у них 2 и падают они на четвёртом критерии, раньше пятого (DK-040 в том же примере разложен как раз по первой непрошедшей): исправлено, пример переписан по первой непрошедшей проверке, DK-043..DK-046 ушли в группу неопределённости
- шаг 5 процедуры падает на живой доске: taskctl file у задачи, где уже есть и файл, и ссылка, отдаёт ошибку и код 1 (taskctl/view.go:58), а в примерной пачке такие DK-018 и DK-021; либо сказать в скилле, что этот исход штатный, либо сделать команду идемпотентной: исправлено, taskctl file делается идемпотентной, правка в задаче рубежа доски
- рубеж доски не говорит, относительно чего считается линкованность дерева: если по cwd запуска, отказ прилетит штатному shipctl merge из дерева задачи, он зовёт taskctl move с -C на основной чекаут; привязать проверку к найденному корню и закрыть это тестом: исправлено, линкованность считается по найденному корню, а не по cwd, и закрыта тестом на -C из worktree
- замок .devkit/ship.lock не покрывает shipctl start, а это единственная команда конвейера, которая пишет и коммитит доску в основном дереве; чужой start посреди merge или ship бьёт ровно в тот зазор, ради которого замок и заводится: исправлено, замок берут все четыре команды shipctl, включая start
- не разобрана задача пачки, чья ветка правит только docs/: merge --train сольёт её, но в поезд она не попадёт (codeCommits считает кодом только правки вне docs/), и после ship она останется в In progress вместо Check: исправлено, merge --train переводит бескодовую ветку в Check сам, признак берётся у состава поезда
- про замок не сказано, что с ним после аварийного завершения: снимает ли его ядро (flock) или остаётся файл, и как снять руками; иначе один SIGKILL посреди merge закрывает слияния насовсем: исправлено, flock снимает ядро при завершении процесса, пустой файл замка руками снимать нечего
- правки devkitctl под ключ test (пустой test в болванке deploy.local и находка doctor) не отнесены ни к одной задаче разбивки; уедут в задачу про скилл, и две задачи серии сойдутся на devkitctl.py, то есть ровно тот случай, который дизайн сам называет непригодным для пачки: исправлено, все правки devkitctl.py собраны в последнюю задачу серии
- не сказано, читает ли команду тестов из конфига shipctl revert: сейчас без --test он тесты не гоняет вовсе, и ключ test молча поменял бы поведение отката: исправлено, ключ test читает только merge, поведение revert не меняется
