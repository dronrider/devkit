# DK-070: mergedShas читает файл задачи построчно и не знает про ограждённые блоки: процитированный раздел «Выкат» становится настоящей записью

Вылезло сразу на DK-065, при первом же прогоне её сценария после выката. В файл
задачи вкладывается реальный вывод (RULES.board.md, «Трекинг задач» п. 6), а в
выводе стоял заголовок `## Выкат` и строка записи из синтетического
репозитория. `mergedShas` (`shipctl/record.go`) идёт по строкам файла и про
ограждённые блоки не знает, поэтому процитированное принял бы за свою запись:
пришлось сдвигать вывод на два пробела и оговаривать это прозой.

Сам по себе чужой sha безвреден, он просто не найдётся в логе. Опасен обратный
случай: файл задачи цитирует чужую запись (разбор соседней задачи, кусок
истории), sha из неё в логе есть, и очередь выката посчитает выкаченной не ту
задачу. Того же поля `openReviewNotes` в `shipctl/board.go`: он тоже ищет
`## Ревью` построчно, и процитированное замечание без исхода отобьёт слияние.

Что сделать: разбирая файл задачи, пропускать содержимое ограждённых блоков
(``` и ~~~), общим хелпером для обоих читателей. Тест на файл, где раздел
процитирован внутри блока.

## Ход работы

- Исполнение: субагент opus/medium по вердикту pick (маппинг sonnet, корректор: профицит week_all), 2026-08-02.
- Общий хелпер лёг в `shipctl/taskdoc.go`: `fenceMask` помечает строки внутри
  ограждённого блока вместе с самими ограждениями, `sectionLines` отдаёт
  строки раздела мимо блоков. Через него пошли `mergedShas` и
  `openReviewNotes`, а `appendToSection` ищет по маске начало и конец своего
  раздела: иначе строка записи уехала бы внутрь процитированного вывода.
- Тот же построчный разбор живёт в `taskctl/review.go` (`loadReview`) и в
  `agentctl/pick.go` (`recordExecution`). Там он опаснее, чем читатели
  shipctl: `review resolve N` перепишет строку внутри блока, а запись
  исполнителя встанет в процитированный «Ход работы». Хелпер лежит в модуле
  shipctl, на два соседних модуля его так просто не растянуть, поэтому эта
  часть уехала отдельной задачей DK-076.
- Регрессия закрыта regcheck на тестах `shipctl/record_test.go`:

  ```
  regcheck --base main -- go test -C shipctl ./ -run 'TestMergedShasSkipsFenced|TestAppendToSectionSkipsFenced|TestMergeIgnoresFencedReviewNote'
  тест краснеет на main и проходит на текущем коде, регрессия закрыта (тесты: shipctl/record_test.go)
  ```
- Ревью: субагент sonnet/high по вердикту pick, 2026-08-02.
- Второй круг по замечанию ревью: тем же хелпером пошёл третий читатель
  модуля, `scenarioWarning` в `shipctl/advice.go` (`hasHeading` в
  `shipctl/taskdoc.go`, заголовок любого уровня вне блоков). Тест
  `TestScenarioWarningSkipsFenced` до правки краснел прямо на ветке.
  Повторный проход по модулю четвёртого читателя файла задачи не нашёл:
  остальные построчные разборы shipctl читают доску (`loadBoard`) и журнал
  `.devkit/log` (`regcheckLogged`), ограждённых блоков там не бывает.
- Третий круг, обратная сторона разбора. `fenceMask` вторым значением отдаёт
  строку незакрытого ограждения, и обрыв файла задачи виден вслух: `status`
  называет такой файл строкой по каждой задаче доски, а `merge` по ней
  отказывает до ребейза. Отказ, а не одна подсказка, потому что предупреждение
  печатается уже после того, как merge слил ветку, дописал запись «Выкат» в
  оборванный хвост и перевёл задачу в Check: сказать про потерю после потери
  это не «видно снаружи», а извещение о случившемся. На отказе не сделано
  ничего, и чинится он закрытием ограждения. Угадывать намерение автора
  (считать незакрытый блок закрытым) не стал: тогда цитата с одним ограждением
  снова начнёт читаться как запись, то есть вернётся исходный баг.

## Сценарий проверки

Агентский, скрипт `docs/tasks/DK-070-check.sh` (синтетический репозиторий во
временной директории, живая доска не трогается). После выката:

```
sh docs/tasks/DK-070-check.sh
```

Шаг 1 воспроизводит исходный баг: XR-002 стоит в Check без своей записи
выката, зато её файл цитирует в ограждённом блоке чужую запись с настоящим sha
из лога. Старый shipctl считает XR-002 выкаченной и держит очередь, новый
видит очередь свободной. Шаг 2 обрывает ограждение в файле XR-003 и ждёт, что
про это скажут дважды: строкой в `status` и отказом `merge` с номером
оборванной строки. Шаг 3 возвращает файл в порядок и показывает, что настоящий
раздел «Ревью» работает как работал: замечание без исхода слияние отбивает.
Шаг 4 закрывает это замечание и берёт XR-003 в поезд: настоящего раздела
«Сценарий проверки» у неё нет, есть только процитированный, и подсказка обязана
прозвучать. Шаг 5 сливает XR-001, чей файл цитирует чужое замечание без исхода,
и проверяет, что запись встала в настоящий раздел «Выкат», подсказки про
сценарий у неё нет, а очередь после выката поезда занята.

Прогон на собранном из ветки shipctl (шаги проверяются `grep`, любой
несовпавший обрывает скрипт по `set -e`):

```
--- 1. процитированная чужая запись выката не держит очередь
ветка: main
In progress: XR-001 Первая; XR-003 Третья
Check: XR-002 Вторая
Blocked: пусто
Backlog: 0 задач(и)
очередь свободна: в Check только задачи без выкаченного кода, подтверждение за пользователем, но выкат они не держат
выкат: за пользователем (autonomous=false), команда есть в .devkit/deploy.local
--- 2. оборванный ограждённый блок в файле задачи виден вслух
предупреждение: в docs/tasks/XR-003.md не закрыт ограждённый блок (строка 17), всё после него читается как цитата: разделы «Выкат», «Ревью» и «Сценарий проверки» оттуда не видны; очередь и состав поезда считаются без записи этой задачи, merge по ней откажет
ошибка: в docs/tasks/XR-003.md не закрыт ограждённый блок (строка 17), всё после него читается как цитата: разделы «Выкат», «Ревью» и «Сценарий проверки» оттуда не видны; закрыть ограждение и повторить, иначе запись слияния уйдёт туда же и очередь её не увидит
--- 3. настоящее замечание без исхода слияние по-прежнему отбивает
ошибка: в ревью XR-003 замечания без исхода, сначала закрыть их в docs/tasks/XR-003.md:
--- 4. процитированный заголовок за раздел «Сценарий проверки» не сходит
XR-003: замечание 1 исправлено, коммит 8a0bfe9
предупреждение: в docs/tasks/XR-003.md нет раздела «Сценарий проверки», а ship переведёт задачу в Check разом со всем поездом: проверять выкат будет нечем (RULES.board.md, «Трекинг задач» п. 6)
--- 5. процитированное замечание слияние пропускает, запись встаёт в свой раздел
XR-001 слита в main fast-forward, ветка xr-001-fix удалена
коммиты задачи записаны в docs/tasks/XR-001.md, коммит a93d7a9
в поезде: XR-001; выкат поезда: shipctl ship
## Выкат

- 2026-08-02 слито: 4268989
доска: XR-001 в Check, коммит 658d6c9
после проверки каждая задача закрывается своим taskctl close
очередь занята: XR-001 в Check, сначала проверка и taskctl close
--- готово, дерево: /var/folders/3r/.../tmp.DsjlPNFs8F
```

Тот же скрипт на shipctl, собранном из main, обрывается на первом шаге (код
выхода 1):

```
--- 1. процитированная чужая запись выката не держит очередь
...
очередь занята: XR-002 в Check, сначала проверка и taskctl close
выкат: за пользователем (autonomous=false), команда есть в .devkit/deploy.local
ПРОВАЛ: очередь держит цитата чужой записи
```

На сборке ветки после второго круга (коммит 96affda) первый шаг проходит, а
второй обрывается сразу: про оборванный файл задачи никто не говорит, grep не
находит строки и скрипт выходит с кодом 1.

```
--- 2. оборванный ограждённый блок в файле задачи виден вслух
```

## Ревью

- scenarioWarning (shipctl/advice.go) читает файл задачи на тот же построчный манер, ищет заголовок с текстом «Сценарий проверки» без учёта ограждённых блоков; процитированный в примере заголовок глушит предупреждение о реально отсутствующем разделе. Третий читатель в том же модуле shipctl, не тронут и не назван: исправлено, scenarioWarning пошёл через тот же хелпер (hasHeading в shipctl/taskdoc.go), тест TestScenarioWarningSkipsFenced краснел на ветке до правки; повторный проход по модулю четвёртого читателя файла задачи не нашёл, остальные построчные разборы читают доску и журнал .devkit/log
- найденный в этой же задаче баг того же класса в taskctl/review.go (loadReview) и agentctl/pick.go (recordExecution) не заведён на доску ни строкой, ни черновиком taskctl draft, только упомянут прозой в файле задачи; RULES.board.md п.5 требует заводить найденные баги сразу: исправлено, баг того же класса в taskctl/review.go и agentctl/pick.go заведён строкой DK-076, доску не трогал
- hasHeading (shipctl/taskdoc.go): мутация 'уровень заголовка любой -> только «## »' не ловится ни одним тестом пакета (прогнал весь go test ./... с этой мутацией: зелёный). TestScenarioWarningSkipsFenced проверяет только уровень 2, заявленное 'заголовок любого уровня' не покрыто: исправлено, добран TestScenarioWarningHeadingLevel на уровни 1 и 3; мутацию '#' -> '## ' в hasHeading прогнал, тест краснеет
- обратная сторона: необрубленный ограждённый блок где угодно раньше по файлу (частый обрыв вставленного вывода) проглатывает весь остаток файла по спецификации markdown, а с ним и настоящие непроцитированные разделы «Выкат»/«Ревью»/«Сценарий проверки» для всех четырёх читателей разом (mergedShas и hasHeading проверил живыми тестами). Не новое в этом круге (унаследовано от fenceMask круга 1), но круг явно просил проверить обратную сторону, и на этом входе она реальна: подсказка про сценарий срабатывает ложно, а mergedShas/openReviewNotes теряют настоящую запись: исправлено, обрыв теперь виден вслух: fenceMask отдаёт строку незакрытого ограждения, status называет такой файл по каждой задаче доски, merge по ней отказывает до ребейза (предупреждения мало: молча слить и записать выкат в оборванный хвост уже поздно); TestUnterminatedFenceSpeaksUp, README и шаг 2 сценария
