# DK-071: doctor проверяет полноту доки: компонент без описания в карте архитектуры это находка

В xr-proxy закрыто 109 задач, номера дошли до XR-244, и документация там живая:
`docs/ARCHITECTURE.md` на 866 строк правился 78 коммитами, README 502 строки,
37 LLD, модульные `//!` в 62 rust-файлах из 91. При этом раздел «Компоненты»
описывает семь крейтов из десяти. Своего подраздела нет у xr-hub (5814 строк,
control plane), xr-share (7370, агент файлообмена) и xr-setup (2900,
установщик). Это больше трети rust-кода репозитория, и всё, что про него
сказано, это по строке в таблице состава.

## Разбор корня

Правило «Документация изменений» в `RULES.md` (появилось 12 июля 2026) устроено
как дифф-контроль: оно спрашивает, что правка изменила в доке. Все контролёры
смотрят туда же, в дифф ветки. Вопрос «а всё ли в проекте описано» не задаёт
никто и никогда, поэтому компонент, проехавший мимо доки при рождении, не
описывается уже никогда: последующие правки в нём мелкие, порога «заметное
поведение» не берут, а ревьювер за пределы диффа не смотрит. Показательно, что
xr-setup появился 24 июля, то есть уже под правилом, и раздела всё равно не
получил, а xr-relay, родившийся 11 июля, получил.

Само правило работает: доля кодовых задач, которые тронули долгоживущую доку,
выросла с 10 из 38 до его появления до 18 из 36 после, и ревьюверы про доку
пишут (30 замечаний из 222 в файлах задач xr-proxy). Но все тридцать про
расхождение уже описанного с кодом, ни одного про отсутствующее описание.
Второе из диффа и не видно.

Механической проверки нет вовсе. `devkitctl doctor` смотрит хуки, определения
агентов, снимок квоты, `deploy.local`, битые ссылки в правилах; `taskctl lint`
доску; хуки символы, чувствительное и текст коммита. Тесты держит regcheck и
отдельное правило про краснеющий прогон, а доку не держит ничего, кроме одной
фразы в промпте ревьювера. Ревью вдобавок гоняется только по фичеветкам:
мелочь, идущая в main напрямую, контролёра не проходит совсем.

## Что чинить

Находка doctor на компонент, у которого нет описания. Компонент считается
описанным, если про него есть заголовок в карте архитектуры проекта, либо
`<компонент>/README.md`, либо модульная дока в корневом файле. Достаточно
любого из трёх: в devkit покомпонентная дока живёт как README рядом с кодом
(шесть утилит, шесть README), в xr-proxy как разделы одного ARCHITECTURE.md,
и навязывать один стиль поверх второго незачем.

Проверка не только для новых компонентов, а сразу для всех. Долг, накопленный
до правила, иначе так и останется: процесса, который заводил бы на него
задачу, в devkit нет.

Правило в `RULES.md` тем же заходом: новый компонент получает описание в том же
коммите, что и первый его код. И пункт ревьюверу в `agents/review-*.md` про
появление новых каталогов и членов сборки, а не только про изменённые строки.

## Развилки

1. Откуда doctor берёт список компонентов. Разбирать манифесты сборки (члены
   Cargo workspace, `settings.gradle`, `go.work`) точнее, но привязывает
   проверку к экосистемам; каталоги верхнего уровня с кодом работают везде, но
   шумят. Похоже, что манифест там, где он есть, каталоги как запасной путь.
2. Куда писать исключения (генерённое, вендоренное, каталог сборки). Скорее
   всего в `.devkit/`, рядом с `deploy.local`, но проектный конфиг там сейчас
   один и под выкат.
3. Считать ли компонентом Android-модуль и прочее, что живёт вне манифеста
   основной сборки.

## Сценарий проверки

Агентский, обкатывается на xr-proxy: `devkitctl doctor` в чистом чекауте обязан
назвать ровно xr-hub, xr-share и xr-setup и не назвать xr-relay, у которого
раздел 4.7 в карте есть. Плюс прогон на самом devkit, где описаны все шесть
утилит и находок быть не должно.

## Ход работы

Развилки решены так.

1. Список компонентов читается из манифеста в корне: `Cargo.toml` workspace,
   `go.work`, `settings.gradle` (запасной порядок тот же, что в задаче:
   манифест там, где он есть). Без манифеста запасной путь по каталогам
   верхнего уровня с кодом, и он включается только в репозитории: корень без
   манифеста и не под git молчит, иначе проверка шумела бы по чужому дереву.
2. Исключения живут в коммитимом `.devkit/describe.ignore`, строка
   «имя: причина». В отличие от deploy.local в гитигнор его не прятать:
   в вычеркнутом имени секрета нет, а ревью видит, кто и зачем вычеркнул
   компонент из описания.
3. Вне-манифестные модули (Android и прочее) закрываются тем же
   describe.ignore: отдельный механизм под редкий случай был бы вторым
   списком с той же работой.

Компонент описан, если есть любое из трёх: заголовок в карте архитектуры
(`docs/ARCHITECTURE.md` или `ARCHITECTURE.md` в корне), README.md рядом
с кодом, модульная дока в корневом файле (`//!` у main.rs/lib.rs,
`// Package` у doc.go, docstring у `__init__.py`). Заголовок узнаётся и по
ссылке в каталог, и по имени компонента в тексте, а таблица состава разделом
не считается: она называет всех подряд, включая тех, у кого раздела нет.
Модульная дока считается живой, когда начинается с имени компонента: список
`mod`-строк имени не содержит. Каталоги без языка (skills, шаблоны)
прикрыты своими маркерами: SKILL.md, AGENTS.project.md.

Реализация: `tools/devkitctl/describe.py`, включён в doctor рядом с проверкой
битых ссылок (находка «нет описания компонента; <как описать>»). Тесты в
`describe_test.py`, 15 штук: члены из трёх манифестов и запасной путь,
все три способа описания, таблица против раздела, исключения, молчание вне
репозитория, находка из самого doctor и его же зелёность после записи README.
Тест doctor-находки проверен на регресс: со спрятанной интеграцией в
`devkitctl.py` он краснеет. Полный прогон devkitctl, 477 тестов, зелёный;
фикстура веса резидента получила README-болванки, чтобы её ожидание rc=0
не тонуло в новых находках.

Сценарий из постановки расходится с критерием самой задачи, и расхождение
решено в пользу критерия «любого из трёх»: xr-share несёт README.md с рождения
(25 июня), xr-setup модульную доку `//! xr-setup:` с рождения (24 июля),
так что реальная находка на xr-proxy одна. Живой прогон, чистый чекаут:

    $ devkitctl doctor
    xr-hub: нет описания компонента; заголовок со ссылкой на него в карте
    архитектуры (docs/ARCHITECTURE.md или ARCHITECTURE.md), README.md рядом
    с кодом либо модульная дока в корневом файле компонента
    находок: 1

xr-relay не назван: раздел 4.7 в карте есть. На самом devkit проверка
зелёная («все компоненты описаны»): README утилит были, добавлены описания
у internal и cmdout, чей код до задачи не имел ни одного из трёх.

Дока тем же заходом: пункт о новой проверке в `tools/devkitctl/README.md`,
правило про описание нового компонента в `RULES.md` («Документация
изменений»), пункт ревьюверу про новые каталоги и члены сборки во всех
четырёх `kit/agents/review-*.md`.

## Ревью

- tools/devkitctl/README.md:180 опечатка «стиль dokы» (латиница вперемешку с кириллицей) в долгоживущей доке: исправлено
- describe.py main(): сообщение об ошибке запуска «опрос: python3 describe.py...» не совпадает ни с лексикой репозитория, ни с самим случаем (речь о вызове без аргумента, а не об опросе); свериться с соседними модулями devkitctl и переформулировать: исправлено
- describe.py map_dirs(): заголовок карты по имени без ссылки гасит проверку и для члена из вложенного каталога (crates/hub описан заголовком «### hub: ...»): сам вызов check() уже держит отдельную ветку «имя названо заголовком, а раздела не нашлось» ровно про этот случай, а documented() смотрит в map_dirs раньше неё и находку съедает; сравнивать путь каталога из ссылки либо требовать у имени заголовка совпадения с последней компонентой пути члена: исправлено
- describe.py main(): вывод «все компоненты описаны» уходит в stdout без префикса утилиты (grep нашёл только stderr-строки «находок:»), и caller не отличает его от чужого вывода; свериться с тем, как это делают соседние команды doctor: исправлено
- tools/devkitctl/README.md:184-185, фикс-коммит a4054a30: при правке опечатки задвоена строка «раздела нет. Генерённое и вендоренное вычёркивается строкой в» (два раза подряд, вторая копия отрезает фразу от продолжения «.devkit/describe.ignore»); убрать дубль: исправлено

## Выкат

- 2026-08-14 слито: 970caab8, c048e62b, 1db6c0ee, aa2f59bf, db0dcd2c, fef9b73a, 3c44f49a, 86558a1b, fd372ecf
