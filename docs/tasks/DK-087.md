# DK-087: shipctl: ветка по ключу тикета, отказ слияния в корп-контуре

Задача серии корп-контура по принятому LLD
[DK-074](../lld/DK-074-corp-contour.md), раздел «Конвейер: что остаётся от
shipctl» (строки 411-435). Идёт после закрытых DK-081 и DK-082: признак контура
и привязка с шаблоном ветки уже на проде.

## Что делать

В корп-контуре слияние делает MR-флоу компании, и конвейер обязан честно
остановиться на этой границе, а не изображать её.

1. `start` остаётся точкой взятия в работу и делает то же самое с двумя
   поправками: имя ветки строится по шаблону `branch` из привязки
   `.devkit/tracker.local` (ключ тикета вместо локального ID), а после перевода
   строки в In progress зовётся `trackctl take <KEY>`, тем же порядком, каким
   `merge` зовёт `taskctl`. Привязку читает плоский разбор `key = value`,
   который у shipctl уже есть, парсер TOML сюда не тащить.
2. `trackctl` не в PATH или привязки нет, значит `start` проходит, но говорит
   об этом вслух: доска не должна вставать из-за трекера.
3. `merge`, `ship` и `revert` в корп-контуре отказывают честной строкой.
   Подсказка называет ручные шаги: `trackctl submit`, пуш ветки, MR, дальше по
   процессу контура. Команду «подготовь MR» дизайн строить отказывается: пуш в
   корп-origin и создание MR это ровно та граница, где по `RULES.md`
   останавливается агент и начинается пользователь.
4. Локальная часть предусловий merge при этом не пропадает: проверку
   «замечания ревью закрыты, тесты зелёные» берёт на себя локальное ревью по
   обычным правилам доски, оно от контура не зависит.
5. Очередь выката в корп-контуре не считается, и `status` говорит почему, а не
   молчит. Check здесь означает «мяч на чужой стороне»: MR открыт, тикет в
   ревью или тестировании, и двигает строку туда pull синхронизации (DK-084), а
   не выкат, которого нет.
6. Починить найденное DK-081: `status` спрашивает git по корню, а корень в
   корп-контуре это боковая директория со своим репозиторием, поэтому он
   называет веткой ветку доски и падает на `git rev-parse HEAD`, пока в боковой
   директории нет ни одного коммита. Рабочую ветку надо спрашивать у клона.

## Дока

- `shipctl/README.md`: `start` в корп-контуре строит ветку по шаблону тикета и
  зовёт `trackctl take`; `merge`, `ship`, `revert` отказывают с подсказкой про
  MR-флоу; очередь выката в корп-контуре не считается.
- `RULES.board.md`: шапка сегодня отсекает корп-контур целиком («в
  корпоративном контуре файл не применяется»), и это противоречит дизайну, где
  доска, локальное ревью и worktree живут. Формулировка меняется на то, что в
  корп-контуре файл применяется, кроме раздела про слияние и выкат: их делает
  MR-флоу компании, а `merge`, `ship`, `revert` и поезд не работают. Тем же
  абзацем называется, где лежит доска (боковая директория) и что ID задачи в
  subject корп-коммита заменяет ключ тикета. Больше нескольких фраз не нужно,
  раздел резидентный.

## Сценарий проверки

Агентский.

Юниты: ветка строится по шаблону привязки с ключом тикета; `start` зовёт
`trackctl take`; отсутствие `trackctl` или привязки не валит `start`, но видно
в выводе; `merge`, `ship` и `revert` в корп-контуре отказывают и называют
ручные шаги; `status` не считает очередь выката и говорит почему; `status`
берёт рабочую ветку у клона, а не у боковой директории, и не падает, когда в
боковой директории нет коммитов. Мутации, которые тесты обязаны ловить: ветка
собрана по локальному ID вместо ключа, отсутствие trackctl уронило `start`,
`merge` в корп-контуре прошёл, `status` спросил ветку у корня, корп-поведение
включилось в домашнем проекте.

Живая проверка на временном репозитории вне рабочих деревьев: корп-клон с
редиректом и привязкой, `shipctl start` по зеркальной строке, отказ `merge` с
подсказкой на подложном адаптере, `status` в клоне без коммитов в боковой
директории. В файл задачи вкладывается реальный вывод.

Прогон общего тест-набора из `.devkit/deploy.local` (ключ `test`) с кодом 0.

## Найденное по ходу серии

- DK-081: `shipctl status` в корп-контуре спрашивает git по корню, а корень
  теперь боковая директория со своим репозиторием. Из-за этого он называет
  веткой ветку доски, а не рабочую ветку клона, и падает на `git rev-parse
  HEAD`, пока в боковой директории нет ни одного коммита. Рабочую ветку надо
  спрашивать у клона, а не у корня.

## Ход работы

- Исполнение: субагент sonnet/high по вердикту pick (квота: week_all 21%, снимок 2м назад, сдвига нет), 2026-08-05.

### Реализация

- `shipctl/corp.go`: `loadTrackerBinding` читает `.devkit/tracker.local`
  плоским разбором `key = value` (тот же `unquote`, что у `deploy.local`),
  `corpTrackerRepo` переложен на него без смены поведения. `corpActive`
  это местный признак корп-контура для команд конвейера: наличие файла
  привязки в уже отрезолвленном root, домашний проект такого файла не несёт.
  `corpBranchName` разворачивает шаблон `branch` ({key}, {slug}), пустой
  шаблон берёт вид `{key}-{slug}`. `corpWorkBranch` чинит DK-081: без
  привязки или без ключа `repo` спрашивает ветку у `root`, как раньше, а с
  ключом `repo` спрашивает у клона, на который он указывает. `corpRefused`
  формулирует общий отказ merge/ship/revert.
- `shipctl/worktree.go` (`cmdStart`): в зеркальной строке ID и есть ключ
  тикета (DK-074, «Граница доски и тикета»), поэтому подстановка `{key}`
  берёт `p.ID`, а не поле `key` привязки (то общепроектный префикс, не
  тикета). Привязка без ключа `key` не валит `start`, ветка остаётся
  домашней, но находка идёт строкой в отчёт. После успешного создания
  worktree зовётся `trackctl -C root take <ID>` тем же порядком, что
  `taskMove` выше зовёт `taskctl`; ни отсутствие `trackctl` в PATH, ни его
  отказ `start` не валят, оба случая видно в отчёте.
- `shipctl/ops.go`: `cmdMerge`, `cmdShip`, `cmdRevert` отказывают
  `corpRefused` сразу после `primaryRoot`, до захвата замка и до любых
  git-операций. `cmdStatus` берёт ветку через `corpWorkBranch` и, если
  `corpActive`, не считает очередь и поезд вовсе (весь блок `trainTasks`,
  `checkQueue`, `returned`, `resolveDeploy` пропускается), а печатает одну
  объясняющую строку про MR-флоу и `trackctl sync`.
- Место создания worktree (`cmdStart`) не менялось: LLD DK-074 прямым
  текстом говорит, что в корп-контуре worktree остаётся тем же самым
  механизмом (раздел «Рамка»), поэтому он и остаётся сиблингом `root`, как и
  раньше.

### Живая проверка

Временное окружение вне рабочих деревьев (`/tmp`, дальше уже стёрто):
клон-стенд `proj` с редиректом `devkit.local`, боковая директория
`proj-local` с приявязкой и доской (задача `ABC-42` в Backlog), подложный
`trackctl` (шелл-стаб: живого адаптера в сборке нет, DK-083) и настоящие
собранные `shipctl`/`taskctl`.

```
$ cd proj && shipctl start ABC-42 --slug add-x
ветка feature/ABC-42-add-x в worktree .../proj-local-abc-42
доска: ABC-42: backlog -> in-progress, коммит 11c3fa0
работать в .../proj-local-abc-42, по готовности: shipctl merge ABC-42 (оттуда же или из основного чекаута)
трекер: тикет ABC-42: «Open» -> «In Progress»
исполнитель: тестовый пользователь контура

$ cat trackctl-calls.log
trackctl -C .../proj-local take ABC-42

$ cd proj-local-abc-42 && shipctl merge ABC-42 --test true
ошибка: корп-контур: merge здесь не работает, слияние и выкат ведёт MR-флоу компании; дальше руками: trackctl submit, пуш ветки, MR, дальше по процессу контура

$ shipctl ship
ошибка: корп-контур: ship здесь не работает, слияние и выкат ведёт MR-флоу компании; дальше руками: trackctl submit, пуш ветки, MR, дальше по процессу контура

$ shipctl revert ABC-42
ошибка: корп-контур: revert здесь не работает, слияние и выкат ведёт MR-флоу компании; дальше руками: trackctl submit, пуш ветки, MR, дальше по процессу контура

$ cd proj && shipctl status
ветка: main
worktree: feature/ABC-42-add-x в .../proj-local-abc-42
In progress: ABC-42 Тестовый тикет
Check: пусто
Blocked: пусто
Backlog: 0 задач(и)
корп-контур: слияние и выкат ведёт MR-флоу компании, shipctl очередь и поезд не считает; Check означает «мяч на чужой стороне» (MR открыт, тикет в ревью или тестировании), строку двигает trackctl sync
```

Отдельным стендом (второй клон `proj2` на ветке `dk-099-feature`, боковая
директория `proj2-local` заведена git-репозиторием без единого коммита)
проверено репро DK-081 и подтверждение починки:

```
$ git -C proj2-local log
fatal: your current branch 'main' does not have any commits yet

$ cd proj2 && shipctl status
ветка: dk-099-feature
In progress: пусто
Check: пусто
Blocked: пусто
Backlog: 0 задач(и)
корп-контур: слияние и выкат ведёт MR-флоу компании, shipctl очередь и поезд не считает; Check означает «мяч на чужой стороне» (MR открыт, тикет в ревью или тестировании), строку двигает trackctl sync
```

`status` не падает и называет веткой `dk-099-feature` (клон), а не `main`
(боковая директория без кода): до правки на этом же стенде `git rev-parse
--abbrev-ref HEAD` в root падал ровно так, как описано в «Найденном».

### Мутации

Каждая заводилась в код руками (юнит из `shipctl/corp_pipeline_test.go`
краснел), код возвращался.

| Мутация | Кто ловит |
|---|---|
| ветка собрана по локальному ID вместо ключа тикета (шаблон не применён) | TestCorpStartBranchTemplate |
| отсутствие `trackctl` в PATH уронило `start` или отказ проглочен молча | TestCorpStartTrackctlMissing |
| отказ `trackctl take` проглочен молча | TestCorpStartTrackctlFails |
| `merge`/`ship`/`revert` в корп-контуре прошли (или упали по другой причине) | TestCorpMergeShipRevertRefuse |
| `status` спросил ветку у корня (боковой директории), а не у клона | TestCorpStatusBranchFromClone |
| `status` падает без коммитов в боковой директории (DK-081) | TestCorpStatusNoCommitsInLocal |
| корп-поведение включилось в домашнем проекте | TestCorpHomeUnaffected |

### Прогон тест-набора

`.devkit/deploy.local` (ключ `test`, берётся из основного чекаута): go test
по taskctl/shipctl/agentctl/trackctl/regcheck/obeycheck, `hooks/test.sh`,
`devkitctl/test.sh`, `skills/goal-loop/test.sh`. Код выхода 0, все части в
порядке (включая полный пакет shipctl с новыми тестами корп-конвейера).
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 21%, снимок 36м назад, сдвига нет), 2026-08-05.
- Ревью: замечаний нет. Домашнее поведение перепроверено живым прогоном на временном репозитории: start, worktree, merge, status показывают то же самое, что и раньше (очередь считается, поезд собирается, ни одной корп-строки в выводе). Отказ merge, ship и revert проверен и чтением, и живым прогоном: primaryRoot только читает git worktree list и git rev-parse, замка и git-операций до corpActive не происходит, а после отказа merge в фикстуре ни .devkit/ship.lock не тронут, ни worktree не появился, ни доска не сдвинулась. Подстановку {key} значением p.ID, а не полем key привязки, сверил с trackctl/cmd.go: там ticketKey строит полный ключ тикета из короткого проектного префикса b.Key и номера, то есть поле key привязки это другая сущность, а зеркальная строка доски уже несёт готовый ключ тикета своим ID (в живом прогоне start ветка вышла feature/ABC-42-add-x). Решение соответствует границе «Доска и тикет» из LLD, а не пропускает использование поля. corpWorkBranch без привязки или без repo ведёт себя как раньше, а с repo берёт ветку у клона: воспроизвёл баг DK-081 вручную (боковая директория без единого коммита) и убедился, что status больше не падает и называет ветку клона. Две мутации прогнал лично: возврат corpWorkBranch к старому git(root, rev-parse) роняет TestCorpStatusBranchFromClone и TestCorpStatusNoCommitsInLocal, снятие corpActive-отказа в cmdMerge роняет TestCorpMergeShipRevertRefuse, обе ловятся ровно так, как обещает таблица мутаций постановки. Тесты детерминированы: t.TempDir, PATH-стабы, ни сети, ни машинных путей. Дока (shipctl/README.md и шапка RULES.board.md) пересказывает поведение, а не код, формулировка RULES.board.md короткая, обе сверены построчно с текстом постановки. Границы серии соблюдены: дифф не трогает trackctl/, devkitctl/ и hooks/. Отдельно, вне этой задачи: worktree cmdStart по-прежнему заводит в самой боковой директории, а не в клоне из repo, и рабочее дерево задачи не несёт кода проекта; это унаследовано от DK-081 и DK-082, диффом не тронуто, а постановка прямо говорит, что место создания worktree не меняется (LLD «Рамка»). Для DK-087 не замечание, но стоит держать в поле зрения для серии.

## Выкат

- 2026-08-05 слито: 5e09702, 9d3c2c1, a973b7a, d3b18d5

## Прогон сценария после выката: не сошёлся

Диспетчер, 2026-08-05, на выкаченном коде. Корп-проект поднимался не фикстурой
теста, а по-настоящему: временный bare-origin, клон, `devkitctl corp` (DK-085),
зеркальная строка на доске боковой директории.

Что сошлось:

```text
=== status в корп-контуре ===
ветка: master
корп-контур: слияние и выкат ведёт MR-флоу компании, shipctl очередь и поезд не считает; Check означает «мяч на чужой стороне» (MR открыт, тикет в ревью или тестировании), строку двигает trackctl sync
=== merge, ship, revert ===
ошибка: корп-контур: merge здесь не работает, слияние и выкат ведёт MR-флоу компании; дальше руками: trackctl submit, пуш ветки, MR, дальше по процессу контура
```

Ветку `status` берёт у клона («master»), а не у боковой директории, очередь не
считает и объясняет почему, все три команды отказывают честной строкой и замка
после себя не оставляют. Баг из «Найденного» починен.

Не сошёлся пункт 1 постановки, и это видно только на подключённом проекте:

```text
=== start сразу после devkitctl corp ===
ошибка: не нашёл ветку main или master
=== start после первого коммита в боковой директории ===
ветка ac-001 в worktree .../corpproj-local-ac-001
работать в .../corpproj-local-ac-001, по готовности: shipctl merge AC-001
привязка .devkit/tracker.local без ключа key, ветка по локальному ID, trackctl take не зовём
=== что лежит в дереве задачи ===
AGENTS.md  CLAUDE.md  docs
```

Четыре находки:

1. Дерево задачи заводится в боковой директории и кода проекта не содержит,
   там одна доска. Работать в таком дереве нельзя, а LLD в «Рамке» обещает «те
   же worktree». Дерево задачи должно заводиться в корп-клоне, путь к нему
   лежит в ключе `repo` привязки.
2. Ветка собралась по локальному ID, шаблон `branch` не применился: он ждёт
   ключ `key`, а подключение `devkitctl corp` вписывает в привязку только
   `repo`. Ни одна задача серии ключ `key` не заводит, и пункт про ветку по
   ключу тикета из-за этого не работает вовсе.
3. `trackctl take` по той же причине не зовётся.
4. Пока в боковой директории нет ни одного коммита, `start` падает на поиске
   `main` или `master`. Сразу после `devkitctl corp` это ровно то состояние, в
   котором пользователь и оказывается.

Прод при этом жив: домашний конвейер проверен отдельно (`shipctl status` и
`taskctl list` в самом devkit работают как раньше), отката не требуется.
Задача возвращается в работу форвард-фиксом.

## Форвард-фикс: разбор и починка

Все четыре находки лечатся на стороне `shipctl`, без правки `devkitctl`:
подключение уже кладёт всё, что нужно, находки были в том, как `start` этим
пользовался.

**Находка 2 (ключ `key`), она же корень проблемы.** Гейт `if tb.Key == ""` в
старом `cmdStart` требовал ключ `key`, которого сама подстановка не
использует: и до, и после правки `{key}` шаблона `branch` заполняется
`p.ID`, а не полем `key` привязки (комментарий в коде это прямо объяснял,
но гейт стоял поверх объяснения). `key` привязки это отдельная сущность,
общепроектный префикс тикетов для команд самого `trackctl`
(`trackctl/README.md`: «Ключ пишется целиком... либо одним номером, префикс
тогда берётся из привязки»), а не ключ конкретного тикета: тот несёт сам ID
зеркальной строки доски по границе LLD DK-074 «Граница доски и тикета». Ни
одна задача серии этот `key` не заводит, потому что заводить в нём и правда
нечего для нужд `start`. Из трёх решений, разрешённых постановкой, взято
«шаблон обходится без него»: гейт снят, шаблон `branch` применяется при любой
найденной привязке (`shipctl/worktree.go`, `cmdStart`), независимо от
`tb.Key`. `devkitctl corp` править не пришлось.

**Находка 3 (`trackctl take` не звался)** была прямым следствием находки 2:
вызов `trackctl take` стоял в той же ветке `if`, что гейтилась на `tb.Key`.
После снятия гейта оба чинятся одной правкой.

**Находка 1 (дерево задачи без кода) и находка 4 (`start` падает на
`main`/`master`)** чинятся общей переменной `codeRoot`. Раньше все
git-операции `start` (`mainBranch`, `git branch --list`, `git worktree add`,
путь дерева) шли от `root`, то есть от боковой директории, где после
`devkitctl corp` нет ни кода, ни единого коммита (`corp_connect` заводит
боковую директорию git-репозиторием, но ничего в неё не коммитит: доска,
привязка и `.devkit` остаются untracked до первого коммита, который делает
уже `taskctl move` изнутри `start`). Привязка несёт путь до клона ключом
`repo`; `corpCloneDir` (`shipctl/corp.go`, новая функция, её же переиспользует
переписанный `corpWorkBranch`) разворачивает его в абсолютный путь, и
`cmdStart` считает `codeRoot` этим клоном, когда `repo` в привязке есть.
Дальше `mainBranch`, проверка существующей ветки, путь `wtPath` (сиблинг
`codeRoot`, а не `root`) и сам `git worktree add` идут от `codeRoot`: дерево
задачи заводится в клоне и несёт код проекта, а `main`/`master` ищется там
же, где он есть. Доска (`loadBoard`, `taskctl move`) как и раньше живёт в
`root`: перевод строки в In progress работает с боковой директорией
независимо от того, где лежит код. Привязка без `repo` (испорченная руками,
`devkitctl corp` так не оставляет) не валит `start`: `codeRoot` остаётся
`root`, дерево заводится в боковой директории без кода, и об этом сказано
строкой в отчёте, а `trackctl take` всё равно зовётся: он завязан на ключ
тикета, а не на местоположение клона.

`.devkit` в новом дереве задачи создаётся, только когда `codeRoot == root`
(домашний случай): в корп-клоне `.devkit` уже есть в боковой директории
(`devkitctl corp` его заводит), и дерево задачи, будучи сиблингом клона,
находит его тем же редиректом `devkit.local`, что и сам клон (общий для всех
worktree одного репозитория, LLD DK-074, «боковая директория»).

### Тесты

Фикстура `corp_pipeline_test.go` переписана: старая заводила боковую
директорию с коммитом и с ключом `key` в привязке, то есть с тем, чего
`devkitctl corp` не кладёт. Новая (`corpPipeline`) повторяет ровно то, что
показал живой прогон: привязка несёт только `repo` с тем же комментарием
заголовка, что пишет `ensure_binding`, а боковая директория остаётся без
единого коммита. Изменения:

- `TestCorpStartBranchTemplate` (переписан): worktree теперь ожидается
  сиблингом клона, а не боковой директории, и добавлена проверка
  `README.md` внутри дерева задачи, ловит находку 1 (дерево без кода).
- `TestCorpStartNoCommitsInLocal` (новый): фикстура без единого коммита в
  боковой директории, `start` не должен падать, ловит находку 4 дословно.
- `TestCorpStartBindingWithoutRepo` (заменил `TestCorpStartBindingWithoutKey`,
  чья предпосылка перестала существовать): привязка без `repo`, ветка всё
  равно строится по шаблону, `trackctl take` всё равно зовётся, а находка
  уходит строкой в отчёт про отсутствие кода в дереве задачи.
- `TestCorpStatusNoCommitsInLocal`: привязка урезана до одного `repo`, как у
  остальных, вместо `key`+`contour`+`repo`.
- `TestCorpStartTrackctlMissing`, `TestCorpStartTrackctlFails`,
  `TestCorpMergeShipRevertRefuse`, `TestCorpStatusBranchFromClone`,
  `TestCorpHomeUnaffected`: без изменений в теле, кроме общей фикстуры.

Мутации, прогнанные руками (мутация вносится, `go test -run TestCorp -v .`
краснеет, откатывается):

| Мутация | Кто ловит |
|---|---|
| `mainBranch(codeRoot)` возвращён к `mainBranch(root)` (находка 4 не починена) | `TestCorpStartBranchTemplate`, `TestCorpStartTrackctlMissing`, `TestCorpStartTrackctlFails`, `TestCorpStartNoCommitsInLocal` |
| гейт `tb.Key != ""` возвращён поверх шаблона и `trackctl take` (находки 2 и 3 не починены) | все пять тестов `TestCorpStart*` |
| `wtPath` возвращён к сиблингу `root` вместо `codeRoot` (находка 1 не починена) | `TestCorpStartBranchTemplate` (проверка `README.md` в дереве задачи) |

Домашний путь (`TestCorpHomeUnaffected`) и весь пакет `shipctl`
(`GOWORK=off go test ./...`) зелёные до и после каждой мутации, кроме самой
внесённой строки: регрессии в сторону дома правка не даёт.

### Живой прогон

Вне рабочих деревьев (`/private/tmp`, окружение стёрто после прогона):
временный bare-origin, клон `corpproj` с историей и файлами (`README.md`,
`src/main.py`), `devkitctl corp --prefix AC` из этой ветки worktree,
зеркальная строка `AC-001` через `taskctl add --link`, `shipctl start`,
собранные из этой же ветки `shipctl`/`taskctl`/`trackctl`.

```text
$ python3 devkitctl/devkitctl.py corp --prefix AC
боковая директория .../corpproj-local заведена
git-репозиторий боковой директории заведён: ...
...
.devkit/tracker.local: вписан repo = ../corpproj (по нему обвязка клона находится после переклонирования)
...

$ cat corpproj-local/.devkit/tracker.local
# Привязка проекта к трекеру (trackctl/README.md). Ключ repo кладёт
# подключение: по нему обвязка клона находится после переклонирования.
repo = ../corpproj

$ git -C corpproj-local log
fatal: your current branch 'master' does not have any commits yet

$ taskctl add --id AC-001 --title "Тестовый тикет" --type task --rank "0+5+0+0+5" --cost S --link "https://tracker.example/browse/AC-001"
AC-001 заведена в backlog: P3, R=10

$ cd corpproj && shipctl start AC-001 --slug add-x
ветка AC-001-add-x в worktree .../corpproj-ac-001
доска: AC-001: backlog -> in-progress, коммит 49ced15
работать в .../corpproj-ac-001, по готовности: shipctl merge AC-001 (оттуда же или из основного чекаута)
trackctl take не прошёл: exit status 1 (ошибка: .../corpproj-local/.devkit/tracker.local: не задан contour, а по нему ищется ~/.devkit/tracker/<имя>.local); доска не встаёт из-за трекера, довести руками

$ ls corpproj-ac-001
README.md  src
$ git -C corpproj-ac-001 rev-parse --abbrev-ref HEAD
AC-001-add-x
$ git -C corpproj worktree list
.../corpproj          1ba1b88 [master]
.../corpproj-ac-001   1ba1b88 [AC-001-add-x]

$ shipctl status
ветка: master
In progress: AC-001 Тестовый тикет
Check: пусто
Blocked: пусто
Backlog: 0 задач(и)
корп-контур: слияние и выкат ведёт MR-флоу компании, shipctl очередь и поезд не считает; Check означает «мяч на чужой стороне» (MR открыт, тикет в ревью или тестировании), строку двигает trackctl sync
```

Все четыре находки закрыты: дерево задачи (`corpproj-ac-001`) несёт код
проекта (`README.md`, `src`), ветка собрана по ключу тикета (`AC-001-add-x`,
не локальный ID строчными), `trackctl take` позван (отказ ожидаем: на этой
машине не настроен контур компании, `trackctl` про это и говорит честно, тем
же порядком, что ловит `TestCorpStartTrackctlFails`), `start` не упал на
поиске `main`/`master`, хотя в боковой директории ни одного коммита. `merge`,
`ship`, `revert` и `status` в этом форвард-фиксе не менялись, их поведение
уже подтверждено предыдущим прогоном («Прогон сценария после выката»).

### Прогон тест-набора

`.devkit/deploy.local` (ключ `test`, основной чекаут): `go test ./...` по
taskctl/shipctl/agentctl/trackctl/regcheck/obeycheck, `hooks/test.sh`,
`devkitctl/test.sh`, `skills/goal-loop/test.sh`. Код выхода 0.

## Ревью

- shipctl/README.md:368-370 расходится с кодом: там написано, что неполная привязка (файл есть, а ключа repo в ней нет) start не валит, доска не должна вставать из-за трекера, ветка и дерево задачи остаются домашними. На деле домашним остаётся только дерево задачи (codeRoot), а ветку cmdStart в этом случае всё равно строит corpBranchName(tb.Branch, p.ID, p.Slug) по шаблону, раз привязка вообще найдена (гейт снят полностью, а не только для repo). Это подтверждает и сам тест: TestCorpStartBindingWithoutRepo ждёт ветку «ABC-1-x» (шаблон, регистр ID сохранён), а не домашнюю «abc-1-x». Поправить формулировку README: домашним в этом случае остаётся дерево задачи, потому что кода клона нет, а ветка всё равно уходит по шаблону тикета.
