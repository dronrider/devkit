# DK-1041: Поиск с телефона гаснет вместе с клавиатурой при наборе и стирании

## Что происходит

Замечено 17.09.2026 в мобильной версии дашборда: человек открывает поиск
лупой, набирает пару символов, делает небольшую паузу, и клавиатура уходит, а
экран выдачи пропадает целиком.

Причина видна в коде. Набор в поле поиска едет в адрес с задержкой 250 мс
(`findType` -> `findGo` в `tools/dashboard/static/app.js`). Запрос короче
двух знаков (`FIND_MIN`) при открытом экране выдачи считается отказом от
поиска, и `findGo` уводит на доску через `goKeepingChat(project)`. Правило
пришло с DK-397 и писалось под ноутбук, где поле стоит в шапке и переход на
доску его не трогает. На телефоне поле шапки спрятано стилями (`.hfind`),
а набор идёт в поле самого экрана выдачи (`.fqbar`), которое живёт внутри
`#groups` и уходит вместе с экраном. Между первой и второй буквой на
телефоне проходит больше 250 мс, таймер срабатывает на одном знаке, доска
сносит экран выдачи с полем, и фокус с клавиатурой теряются.

Стенд `testdata/screen_keep.mjs` набор проверяет только запросом из двух
знаков сразу, паузу после первого знака он не воспроизводит.

Поиском с телефона пользоваться нельзя: успеть набрать два знака быстрее
250 мс на экранной клавиатуре обычно не выходит, и каждая попытка кончается
возвратом на доску. Открыть лупу и начать набор приходится заново.

## Чего хотим

На телефоне поле экрана выдачи живёт до явного ухода. Набор любого числа
знаков с любыми паузами держит поле и клавиатуру, короткий запрос показывает
подсказку сервера про два знака. Стирание знаков, в том числе до пустого поля,
поле и клавиатуру тоже держит: пустое поле это ожидание нового запроса, а не
отказ от поиска. Крестик стирает запрос и оставляет курсор в поле. Уходят с
выдачи крошкой «Доска» и кнопкой «назад» браузера. Правило DK-397 «пустое поле
возвращает доску» остаётся у поля шапки на ноутбуке: оно живёт вне списка и
переход переживает.

## Развилки

Куда деть правило «короткий запрос это отказ»: не уходить с экрана выдачи,
пока в поле есть хоть один знак (уход только по пустому полю, крестику и
Escape), или не уходить, пока поле под фокусом. Первое проще и не зависит
от устройства.

- «правило»: куда деть правило «короткий запрос это отказ от поиска»?
  - решает: исполнитель
  - источник: код tools/dashboard/static/app.js, коммит d785a0c9 (DK-397), tools/dashboard/search.go
  - рекомендация: уход на доску по содержимому поля остаётся только у поля шапки на ноутбуке (пустое поле, крестик, Escape, правило DK-397), а поле экрана выдачи с экрана не уводит ни при каком запросе: пустое и короткое показывают подсказку сервера, крестик стирает запрос и оставляет курсор в поле; уход с выдачи на телефоне только крошкой «Доска» и кнопкой «назад». Вариант «пока поле под фокусом» ломается кликом по строке выдачи, он снимает фокус раньше перехода
- «кейсы»: какие пользовательские кейсы у DK-1041? рекомендую узкий: баг одного пути, а кейсы (3) и (4) широкого закрываются той же правкой условия и отдельной работы не требуют
  - решает: человек
  - рекомендация: узкий состав, 2 кейса: (1) набор с паузой после первой буквы на телефоне держит экран выдачи и клавиатуру, под полем стоит подсказка «Ждём двух символов»; (2) пустое поле, крестик и Escape по-прежнему уводят с выдачи на доску
  - вариант: сбалансированный состав, 3 кейса: (1) набор с паузой после первой буквы на телефоне держит экран выдачи и клавиатуру, под полем стоит подсказка «Ждём двух символов»; (2) пустое поле, крестик и Escape по-прежнему уводят с выдачи на доску; (3) стирание запроса до одного знака клавишей назад тоже держит экран выдачи
  - вариант: широкий состав, 4 кейса: (1) набор с паузой после первой буквы на телефоне держит экран выдачи и клавиатуру, под полем стоит подсказка «Ждём двух символов»; (2) пустое поле, крестик и Escape по-прежнему уводят с выдачи на доску; (3) стирание запроса до одного знака клавишей назад тоже держит экран выдачи; (4) на ноутбуке медленный набор в поле шапки не мигает доской между буквами
  - решено человеком 2026-09-17: свой состав, 4 кейса: (1) кнопка поиска на телефоне открывает поле с клавиатурой, как сейчас; (2) набор любого числа знаков с любыми паузами держит поле и клавиатуру, короткий запрос показывает подсказку сервера, а не уходит с экрана; (3) стирание знаков, в том числе до пустого поля, держит поле и клавиатуру: пустое поле это ожидание нового запроса, а не отказ от поиска; (4) уходят с экрана выдачи на телефоне только явным действием: крошка «Доска», кнопка «назад» браузера. Довод: содержимое поля не должно решать за человека, что он закончил искать
- «предмет»: верно ли переформулирован предмет и конец работы по вашему ответу про кейсы?
  - решает: человек
  - рекомендация: предмет шире первой буквы: на телефоне поле выдачи живёт до явного ухода, и правило DK-397 «пустое поле возвращает доску» действует только для поля шапки на ноутбуке, где поле переход переживает; крестик на телефоне стирает запрос и оставляет курсор в поле; DoD: стенд гоняет набор одного знака с паузой, стирание до пустого и крестик на поле выдачи, экран и фокус на месте, а пустое поле шапки на ноутбуке по-прежнему возвращает доску
  - решено человеком 2026-09-17: предмет шире первой буквы: на телефоне поле выдачи живёт до явного ухода, и правило DK-397 «пустое поле возвращает доску» действует только для поля шапки на ноутбуке, где поле переход переживает; крестик на телефоне стирает запрос и оставляет курсор в поле; DoD: стенд гоняет набор одного знака с паузой, стирание до пустого и крестик на поле выдачи, экран и фокус на месте, а пустое поле шапки на ноутбуке по-прежнему возвращает доску

## DoD

- На экране выдачи набор одного знака с паузой дольше срока набора, стирание
  до пустого поля и крестик не уводят с экрана: узел поля тот же, фокус в нём,
  под полем подсказка сервера про два знака.
- Пустое поле шапки на ноутбуке по-прежнему возвращает доску (DK-397), таб
  сессий и раздел LLD набор не меняют.
- Стенд `testdata/screen_keep.mjs` гоняет все три случая на поле выдачи и
  случай поля шапки, тест падает на старом коде.

## Ранг

`50+5+1+5+0 = 61`, P1.

- Серьёзность 50: поиском с телефона пользоваться нельзя, каждая попытка
  кончается возвратом на доску; обход есть, открыть дашборд на ноутбуке, и он
  болезненный.
- Ценность 5: ощутимое улучшение, о котором попросил пользователь.
- Неопределённость 1: подход выбран развилкой «правило», правка живёт в одном
  файле клиента.
- Поправка на баг 5: поиск с телефона работал до DK-397.
- Рычаг 0: на другую работу не влияет.
- Цена S: условие в `findGo` плюс сценарии стенда.
- Условий, посильных только человеку, не нашёл: искал в коде клиента и
  сервера, на доске и в накопителе, наружу правка не выходит.

## Ход работы

- Из черновика DK-1041 «поиск с телефона гаснет вместе с клавиатурой после первой буквы»: записан 2026-09-17, оформлен 2026-09-17.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 38%, снимок 10м назад, сдвига нет), 2026-09-17 16:57-16:57.

## Сценарий проверки

Агентская часть, из корня devkit после слияния. Каждый ```sh``` блок гонится
построчно своим прогоном (`taskctl rehearse`, DK-820). Вычисление базовой
ревизии, откат файла на неё и запуск стенда поэтому лежат одной строкой.

```sh
base=$(git merge-base HEAD main) && git show "${base}:tools/dashboard/static/app.js" > tools/dashboard/static/app-old.js && ! node tools/dashboard/testdata/screen_keep.mjs tools/dashboard/static/app-old.js
```

Ожидание: шаг зелёный. `!` переворачивает код возврата. В выводе шага строка
«один знак на экране выдачи увёл на доску». До DK-1041 короткий запрос на
поле экрана выдачи читался отказом от поиска. Пауза после первой буквы
сносила экран вместе с клавиатурой.

```sh
node tools/dashboard/testdata/screen_keep.mjs tools/dashboard/static/app.js
```

Ожидание: шаг зелёный. Вывод заканчивается сводной строкой стенда про доску,
черновики, поиск, подписки и перетаскивание, без слов об ошибке. Набор,
стирание до пустого и крестик на поле выдачи держат экран и фокус. Пустое
поле шапки по-прежнему возвращает доску (DK-397).

```sh
GOWORK=off go -C tools/dashboard test ./... -run TestScreenKeepsPlaceOnRefresh
```

Ожидание: шаг зелёный, вывод `ok`. Тот же стенд гоняется тестом
`board_test.go`.

## Проверка


- Обкатка: 2026-09-17 17:36, свежее дерево 254ab013282d, сценарий 527f6dde, временный HOME, утилит дерева 11, шагов 3, все зелёные.

Шаг 1 (зелёный):

```console
$ base=$(git merge-base HEAD main) && git show "${base}:tools/dashboard/static/app.js" > tools/dashboard/static/app-old.js && ! node tools/dashboard/testdata/screen_keep.mjs tools/dashboard/static/app-old.js
один знак на экране выдачи увёл на доску: demo
```

Шаг 2 (зелёный):

```console
$ node tools/dashboard/testdata/screen_keep.mjs tools/dashboard/static/app.js
частичная перерисовка: доска, черновики и лента панели держат место и фокус, панель разговора открывается хвостом адреса и закрывается крестиком назад, старые адреса ведут в неё же, поле ввода живое, а работу продолжает своя кнопка, экран задачи держит журнал витка, этап и стоп, ответ на нажатие не двигает раскладку; поиск: выдача своим экраном, набор одним запросом, поле держит курсор, косая черта и лупа ведут в поиск; подписки: широкая часть кнопки идёт на подписку по умолчанию, строка списка на свою, без выбора стрелки нет, вид кнопки и списка по макету; кнопки действий: стоп красный, подтверждение удаления полноразмерное своей строкой; экран черновика: пометка идущего груминга, кнопка разбора, дорога в его чат, исход разбора словами сервера и вопрос грумера с ответом; перетаскивание: коридор со щелями на живом списке, долгое нажатие против пролистывания, правка одной ценностью и откат с ожидаемой разбивкой
```

Шаг 3 (зелёный):

```console
$ GOWORK=off go -C tools/dashboard test ./... -run TestScreenKeepsPlaceOnRefresh
ok  	github.com/dronrider/devkit/tools/dashboard	1.368s
```
