# DK-1054: Экран задачи получает дату движения строки, поля строки сверяет тест

## Что происходит

2026-09-18, ревью DK-650, уточнено обходом 2026-09-19. Экран задачи получает строку доски типизированной `boardRow` (tools/dashboard/tasks.go), список получает сырой ответ `taskctl list --json`. Из полей `jsonRow` (tools/taskctl/json.go) в типе нет одного, `Moved`. `Armed` и `Arm` до экрана доезжают другим каналом: `taskDeps` зовёт `taskctl dep list --json`, и `handleTask` кладёт их в ответ, это записано в README дашборда и держится тестом chain_test.go. `static/app.js` уже рисует чип даты правки из `row.moved`, а сервер поля не отдаёт, и чип молча не появляется. Тесты проверяют только, что строка `row.moved` есть в тексте app.js.

Чип даты правки на экране задачи не работает с момента появления. Класс поломки тот же, что у ранга в DK-650: новое поле ответа taskctl видно в списке и пропадает на экране задачи, и сторожа на это нет.

## Чего хотим

Экран задачи показывает дату движения строки, а поле ответа taskctl, которого нет в типе дашборда, находит тест.

## Границы

Перевод экрана задачи на сырой ответ не берётся: `boardRow` читает десяток мест сервера (chat.go, checkrun.go, lld.go, goal.go, pulse.go, runs.go, round.go, sessions.go, wake.go). Условий, посильных только человеку, обход не нашёл: искал в README и тестах дашборда.

## Развилки

- «путь»: как поле доезжает до экрана задачи?
  - решает: исполнитель
  - рекомендация: досказать поле в тип и поставить тест-сторож на равенство перечней: у boardRow десяток читателей на сервере
  - вариант: везти экрану задачи сырой ответ, как списку
  - источник: код tools/dashboard/tasks.go, доска DK-650
  - решено агентом 2026-09-19: досказать поле Moved в boardRow и завести тест-сторож TestBoardRowCarriesEveryJSONField, сверяющий перечень полей ответа taskctl list --json с типом дашборда рефлексией; сырой ответ экрану задачи не переводим, границы задачи это уже исключают из-за десятка читателей boardRow на сервере

## DoD

- Ручка задачи отдаёт `moved`, и чип даты правки виден на экране задачи. Тест проверяет ответ сервера, а не текст app.js.
- Тест-сторож сверяет перечни json-полей `jsonRow` и `boardRow` и называет поле, которого нет в типе дашборда. Осознанные исключения перечислены в тесте с причиной.
- Регрессионный тест краснеет на старом коде, это проверено прогоном regcheck.
- README дашборда называет `moved` в ручке задачи.

## Ранг

`25+4+1+5+2 = 37`, P2.

`25+4+1+5+2 = 37`.

- Серьёзность 25: не рисуется один чип, работе с задачей это не мешает.
- Ценность 4: мёртвый код в app.js оживает, а класс поломки DK-650 получает сторожа.
- Неопределённость 1: рекомендация одна, досказать поле и поставить тест.
- Поправка на баг 5: чип написан и не работает.
- Рычаг 2: один экран.

Цена S: поле, два теста, строка README.

## Ход работы

- Из черновика DK-1054 «boardRow дашборда теряет взвод и дату движения строки из ответа taskctl»: записан 2026-09-18, оформлен 2026-09-19.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 27%, снимок 25м назад, сдвига нет), 2026-09-19 19:54-19:54.

## Ревью

Уровень 2 до 8f35124: неопределённость 1, триаж критичности: DoD-путь не сломан, доступов-данных-денег нет, горячего пути нагрузки нет; тронут только tools/dashboard, не критичный путь

- Вердикт: без замечаний. путь ответ ручки задачи -> boardRow.Moved -> detail.row.moved -> stateChips -> tailChips -> formPage (рядом с типом и ценой) прослежен чтением кода; TestBoardRowCarriesEveryJSONField и TestTaskRowCarriesMovedDate прогнаны точечно и в полном пакете (go -C tools/dashboard test ./... - ok, 189s); честность сторожа проверена инъекцией нового omitempty-поля check_dk1054 в изолированной копии jsonRow - тест поймал его без правки ручного чек-листа; per-line git blame (age.go) не даёт ложного триггера taskSeen на правку соседней строки, коммит доски без правки строки и смену дня; README и код формы не тронуты вне заявленного

## Сценарий проверки

Агентский. Шаги гоняются из корня дерева.

```sh
regcheck -C tools/dashboard --base 518aecf5aa1e4c8be6d6935149a02fb0e0940550 -- go test -run TestBoardRowCarriesEveryJSONField ./...
regcheck -C tools/dashboard --base 518aecf5aa1e4c8be6d6935149a02fb0e0940550 -- go test -run TestTaskRowCarriesMovedDate ./...
```

Ожидание. Оба прогона regcheck заканчиваются кодом 0 и печатают, что тест
красный на базовом коммите и зелёный в рабочем дереве: до правки ни один тип
дашборда не нёс поле `moved`, и оба теста ловили это на старом коде.

```sh
go -C tools/dashboard test ./...
go -C tools/taskctl test ./...
```

Ожидание. Оба пакета проходят полностью, `ok` без единого `FAIL`.

## Выкат

- 2026-09-19 слито: 4d7a25fb, eabce500, 65ce308b, 9812c76b

## Проверка

- Обкатка: 2026-09-19 20:43, свежее дерево fb4964066e36, сценарий 41a7a4a5, временный HOME, утилит дерева 11, шагов 4, все зелёные.

Шаг 1 (зелёный):

```console
$ regcheck -C tools/dashboard --base 518aecf5aa1e4c8be6d6935149a02fb0e0940550 -- go test -run TestBoardRowCarriesEveryJSONField ./...
тест краснеет на 518aecf5aa1e4c8be6d6935149a02fb0e0940550 и проходит на текущем коде, регрессия закрыта (тесты: tools/dashboard/rowfields_test.go)
```

Шаг 2 (зелёный):

```console
$ regcheck -C tools/dashboard --base 518aecf5aa1e4c8be6d6935149a02fb0e0940550 -- go test -run TestTaskRowCarriesMovedDate ./...
тест краснеет на 518aecf5aa1e4c8be6d6935149a02fb0e0940550 и проходит на текущем коде, регрессия закрыта (тесты: tools/dashboard/rowfields_test.go)
```

Шаг 3 (зелёный):

```console
$ go -C tools/dashboard test ./...
ok  	github.com/dronrider/devkit/tools/dashboard	193.673s
```

Шаг 4 (зелёный):

```console
$ go -C tools/taskctl test ./...
ok  	github.com/dronrider/devkit/tools/taskctl	182.195s
```
