# DK-1057: Фикстурный проект стенда проходит taskctl lint

## Что происходит

2026-09-18, DK-449. `taskctl lint` на tools/obeycheck/testdata/project даёт три находки на main: ячейка ранга OB-001 и OB-002 не учитывает бонус цены S+2, у OB-002 в работе нет файла задачи. Сценарии 53 и 54 хотели проверять ответ агента голым `taskctl lint` и не смогли.

Сценарий стенда не может взять зелёный lint признаком хорошего ответа и вынужден фильтровать три известные строки через grep -v. Фильтр хрупкий: четвёртая находка фикстуры покрасит оба сценария без вины агента, а поправленная формулировка отказа lint спрячет настоящую находку.

## Чего хотим

Фикстурный проект стенда проходит `taskctl lint` без находок, сценарии проверяют ответ агента голой командой.

## Границы

Бонус цены S+2 ввела DK-429 уже после появления фикстуры (DK-102). Починка задевает сценарии, которые опираются на нынешний вид фикстуры. `23-phase-junction.md` строит литерал строки OB-001 со старым рангом и правит его заменой. `31-wait-mark.md` копирует формат без бонуса. `34-park-neighbor.md` сам заводит `docs/tasks/OB-002.md` и коммитит, и с файлом в фикстуре его подготовка упадёт на пустом коммите. Правятся они той же задачей. Условий, посильных только человеку, обход не нашёл: искал в сценариях и тестах obeycheck.

## Развилки

- «ячейки»: чем чинить ячейки ранга фикстуры?
  - решает: исполнитель
  - рекомендация: пересчитать ячейки командой taskctl sort на фикстуре и завести файл OB-002
  - вариант: править ячейки руками
  - источник: код tools/obeycheck/testdata/project, дока RANKING.md
  - решено агентом 2026-09-20: taskctl sort в самом дереве задачи отказывает (boardGuard: линкованный worktree), поэтому пересчитала ячейки в копии фикстуры вне git-дерева (scratchpad) командами taskctl sort и taskctl file OB-002, перенесла результат в фикстуру руками: OB-002 35 (25+4+1+0+3, S+2) с файлом задачи, OB-001 42 (25+5+1+5+4, S+2). taskctl lint на фикстуре молчит

## DoD

- `taskctl lint` на `tools/obeycheck/testdata/project` молчит.
- Сценарии 53 и 54 проверяют ответ агента голым `taskctl lint`, фильтр трёх известных строк снят.
- Сценарии 23, 31 и 34 приведены к новой фикстуре, `obeycheck --list` принимает все сценарии, тесты obeycheck зелёные.
- Тест держит фикстуру зелёной по lint.
- Правка сценариев это правка проверок стенда: порог повода и прогон решаются по скиллу prompt-test.

## Ранг

`25+3+1+0+2 = 31`, P2.

`25+3+1+0+2 = 31`.

- Серьёзность 25: обход дешёвый, фильтр трёх строк.
- Ценность 3: фильтр хрупкий, четвёртая находка фикстуры покрасит сценарии без вины агента.
- Неопределённость 1: находки названы, стыки со сценариями 23, 31 и 34 найдены обходом.
- Поправка на баг 0.
- Рычаг 2: два сценария и фикстура.

Цена S: три ячейки, файл OB-002 и правка четырёх сценариев.

## Ход работы

- Из черновика DK-1057 «фикстурный проект стенда сам не проходит taskctl lint»: записан 2026-09-19, оформлен 2026-09-19.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 34%, снимок 19м назад, сдвига нет), 2026-09-20 00:24-00:24.
- Развилка «ячейки» решена. `taskctl sort` в дереве задачи отказал (`boardGuard`, линкованный worktree). Пересчёт ячеек сделан в копии фикстуры вне git-дерева и перенесён руками.
- Фикстура `tools/obeycheck/testdata/project` приведена к ячейкам с бонусом S+2 и файлу `docs/tasks/OB-002.md`, `taskctl lint` на ней не даёт находок. Сценарии 23, 31 и 34 приведены к новой фикстуре, у 53 и 54 снят фильтр трёх известных строк. Разведка нашла ещё два сценария с рангом фикстуры (35, 44). Оба берут строки анкором или поиском по префиксу, а не литералом старого ранга, и правки не потребовали. Тест `TestFixtureProjectPassesLint` в `tools/obeycheck/env_test.go` проверяет, что фикстура остаётся зелёной по lint.
- Исключение: стенд. Правка касается фикстуры и проверок стенда obeycheck, а не текста, который читает агент. Скиллы, определения субагентов и страницы-контракты не менялись, шаг агента не меняется.
- Вычитка: 1 файл, 11 правок, 0 пометок, 2026-09-20.
- Сторож прозы: docs/tasks/DK-1057.md, хвост «..., а не Y» 6,7 при пороге 5, оставлено: оба хвоста называют настоящую альтернативу (литерал старого ранга, текст, который читает агент), а не пустое отрицание, и пункт 4 скилла proofread такие хвосты не трогает.
- Замечание 1 ревью отработано: тест-сторож собирает taskctl из дерева задачи (go build -C tools/taskctl), а не берёт бинарь из PATH. Проверено подложенной поломкой в tools/taskctl/rank.go (бонус S+2 временно заменён на S+3): собранный из дерева бинарь дал находку по обеим ячейкам ранга, установленный в PATH остался зелёным. Правка отменена, tools/taskctl чист.
- Сторож прозы: docs/tasks/DK-1057.md, строка записи `taskctl review resolve` по замечанию 1 (средняя длина предложения 45,8 при пороге 23, предложений длиннее 25 слов 100,0 при пороге 30, довод в той же фразе 50,0 при пороге 48, хвост «..., а не Y» 10,9 при пороге 5), оставлено: строка склеивает находку ревьювера с моим ответом через двоеточие в формате самой утилиты, переписать можно только начало строки, которое утилита не отдаёт правке.

## Ревью

Уровень 2 до bd16246: до bd162465: неопределённость 1, триаж да: правка держит стенд obeycheck, от которого зависит DoD других задач через prompt-test

- [возврат: реализация, 2026-09-20] блокирует: TestFixtureProjectPassesLint (tools/obeycheck/env_test.go:60) зовёт "taskctl" из PATH, а не бинарь, собранный из этого дерева, в отличие от соседних TestTaskRefusesStaleLayoutOnTheBinary и TestTaskFileWritesToDevkitTreeNotCwd в tasknote_bin_test.go (go build -o bin . перед exec.Command(bin, ...)). Поменяй кто-то tools/taskctl/lint.go в этом же дереве и не переустанови бинарь в PATH, go test ./... даст зелёный ответ на старом бинаре, хотя фикстура и текущий код разошлись, и DoD «тест держит фикстуру зелёной по lint» не гарантирован. Проверить: собрать taskctl во временный каталог тем же способом, что и в tasknote_bin_test.go, и звать собранный путь вместо литерала taskctl: исправлено, TestFixtureProjectPassesLint в env_test.go теперь собирает taskctl из дерева задачи (go build -C tools/taskctl -o <temp> ., по образцу tasknote_bin_test.go) и зовёт собранный бинарь, а не taskctl из PATH. Проверено подложенной поломкой: costBonus("S", 2) временно заменён на costBonus("S", 3) в tools/taskctl/rank.go, тест на бинаре из дерева покраснел находками по обеим ячейкам ранга, а установленный в PATH taskctl остался зелёным на той же фикстуре; правка отменена, tools/taskctl чист

## Сценарий проверки

Агентский. Шаги гоняются из корня дерева.

```sh
taskctl -C tools/obeycheck/testdata/project lint
sh -c 'grep -q "grep -v" tools/obeycheck/scenarios/53-draft-outcome-title.md tools/obeycheck/scenarios/54-draft-groom-no-solution.md; test $? -eq 1'
obeycheck --list | grep -cE "^(23-phase-junction|31-wait-mark|34-park-neighbor|53-draft-outcome-title|54-draft-groom-no-solution)"
go -C tools/obeycheck test ./...
```

Ожидание. Первый шаг не даёт находок и кончается кодом 0 («доска и архив в
порядке»). Второй шаг молчит и кончается кодом 0: фильтр трёх известных строк
не находится ни в одном из двух сценариев. Третий шаг печатает «5». Все пять
задетых сценариев разбираются стендом без ошибок парсинга. Четвёртый шаг
зелёный без FAIL, включая тест `TestFixtureProjectPassesLint`.

## Проверка


- Обкатка: 2026-09-20 01:14, свежее дерево 551b1b7f1575, сценарий 715a3d03, временный HOME, утилит дерева 11, шагов 4, все зелёные.

Шаг 1 (зелёный):

```console
$ taskctl -C tools/obeycheck/testdata/project lint
доска и архив в порядке
```

Шаг 2 (зелёный):

```console
$ sh -c 'grep -q "grep -v" tools/obeycheck/scenarios/53-draft-outcome-title.md tools/obeycheck/scenarios/54-draft-groom-no-solution.md; test $? -eq 1'

```

Шаг 3 (зелёный):

```console
$ obeycheck --list | grep -cE "^(23-phase-junction|31-wait-mark|34-park-neighbor|53-draft-outcome-title|54-draft-groom-no-solution)"
5
```

Шаг 4 (зелёный):

```console
$ go -C tools/obeycheck test ./...
ok  	github.com/dronrider/devkit/tools/obeycheck	82.633s
```
