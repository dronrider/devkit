# DK-1064: TestStaticPulseRing и TestChatOutboxQueueSendsItself скрыты зависанием, настоящие баги

## Что происходит

После фиксов зависания (DK-1059, DK-1062, DK-1063) пакет `tools/dashboard`
перестал упираться в таймаут раньше времени и стал доходить до конца прогона.
Дальше вскрылись два реальных, ранее ни разу не сработавших теста.

`TestStaticPulseRing` (`testdata/poc_ring.mjs`) искал в `style.css` keyframe
`ringrun` в формате `stroke-dashoffset`. Анимация кольца давно переехала на
`transform: rotate(0deg)`/`rotate(360deg)` (композитное свойство, DK-986), тест
не обновили вместе с кодом. Regex не находил совпадение, `spin` выходил
`undefined`.

`TestChatOutboxQueueSendsItself` (`testdata/outbox_offline.mjs`) проверяет
рост паузы между повторами отправки через игрушечную очередь таймеров.
Верхний уровень `static/app.js` заводит свой опрос ожиданий,
`pollEvery(WAIT_POLL, refreshWaits, true)`, и `WAIT_POLL` совпадает по
значению с `OUTBOX_POLL` (оба 15000). Оба таймера ложатся в одну и ту же
игрушечную очередь стенда, `tick()` слепо брал первый в очереди и иногда
срабатывал чужому опросу вместо повтора отправки.

## Чего хотим

Оба теста проверяют то, что заявлено, и проходят стабильно.

## DoD

- `poc_ring.mjs` проверяет актуальный механизм анимации (`transform: rotate`),
  ловит регресс при поломанном обороте (проверено вручную, откат на другое
  значение градуса красит тест).
- `outbox_offline.mjs` снимает шум верхнеуровневого `WAIT_POLL` из игрушечной
  очереди таймеров один раз при старте, дальше он не пересобирает себя сам.
- `go test -run 'TestStaticPulseRing|TestChatOutboxQueueSendsItself'` в
  `tools/dashboard` проходит.
- Полный `go test ./...` в `tools/dashboard` зелёный целиком.

## Ранг

`50+5+1+5+3 = 64`, P1.

- Серьёзность 50. Оба теста были красными на main задолго до этой находки,
  просто ни разу не доходили до выполнения из-за более раннего зависания.
- Ценность 5. Возвращает реальное покрытие двух живых механизмов: анимации
  кольца и повторной отправки офлайн-сообщений.
- Неопределённость 1. Причина обеих находок понятна и подтверждена вручную,
  сама починка теста требует понимания чужого кода (не однострочный патч).
- Поправка на баг 5. Оба теста были сломаны, не флаки.
- Рычаг 3. Снимает последнюю пару находок из цепочки DK-1059/1062/1063.

## Ход работы

- Найдена по ходу DK-881: после трёх фиксов зависания `go:dashboard` стал
  доходить до конца, и там же стабильно упал на `TestStaticPulseRing` и
  `TestChatOutboxQueueSendsItself`. Воспроизвёл оба на чистом main, без ветки
  DK-881.
- Разработка: `poc_ring.mjs` переписан под `transform: rotate`, regex ищет
  разницу углов и требует ровно 360 градусов; ручная проверка отката на
  `rotate(300deg)` красит тест обратно. `outbox_offline.mjs` получил одну
  строку `timers.length = 0` сразу после загрузки статики, снимающую шум
  `WAIT_POLL`. Полный `go test ./...` пакета `tools/dashboard` зелёный, 497
  секунд.
- Сторож прозы: docs/tasks/DK-1064.md, средняя длина предложения и довод в
  той же фразе выше порога, оставлено: это вывод команды `poc_ring.mjs` в
  блоке `console`, одна строка без переносов, не проза.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 53%, снимок 29м назад, сдвига нет), 2026-09-19 23:12-23:32.
- Прогон сценария: sonnet, 2026-09-19 23:34, `agentctl stage DK-1064 проверка
  --by sonnet`, все три команды сценария зелёные с корня репозитория.
- Сторож прозы: docs/tasks/DK-1064.md, средняя длина предложения 218,0 при
  пороге 23, предложений длиннее 25 слов 100,0 при пороге 30, довод в той же
  фразе 100,0 при пороге 48, оставлено: это буквальный вывод трёх команд
  сценария проверки в блоках `console`, не проза.

## Ревью

Уровень 2 до 1f982dd: два отдельных расследования (анимация кольца через transform:rotate и коллизия таймеров WAIT_POLL/OUTBOX_POLL), тронут живой прод-код через тест-стенды, не однострочная правка

- Вердикт: без замечаний. прочитаны оба .mjs целиком и app.js/style.css в указанных местах; прогнаны poc_ring.mjs и outbox_offline.mjs напрямую (exit 0 оба); go test -run на двух целевых тестах PASS; полный go test ./... пакета dashboard зелёный, 498.591с; эмпирически подтверждено (отладочная копия outbox_offline.mjs), что перед timers.length=0 в очереди ровно один таймер WAIT_POLL=15000 и больше top-level pollEvery/pollOnce/setTimeout в app.js нет, широта очистки не грозит регрессией; регрессия poc_ring.mjs подтверждена руками (rotate 300deg красит тест понятным сообщением, style.css возвращён и diff чист)

## Сценарий проверки

Оба теста проверяют актуальный код и держат регрессию.

```sh
timeout 15 node tools/dashboard/testdata/poc_ring.mjs tools/dashboard/static/app.js
timeout 20 node tools/dashboard/testdata/outbox_offline.mjs tools/dashboard/static/app.js
cd tools/dashboard && GOWORK=off go test -run 'TestStaticPulseRing|TestChatOutboxQueueSendsItself' -timeout=25s -v ./...
```

## Проверка

- Обкатка: 2026-09-19 23:32, свежее дерево 64cd2643528f, сценарий 741f7596, временный HOME, утилит дерева 11, шагов 3, все зелёные.

Шаг 1 (зелёный):

```console
$ timeout 15 node tools/dashboard/testdata/poc_ring.mjs tools/dashboard/static/app.js
кольцо агентов: место в шапке, четыре состояния, деления по плану сессии, простой с планом и контур без плана, список агентов с дорогой в разговор, давность словами, список закрывается вторым нажатием, кликом мимо и Escape, как и выбор подписки
```

Шаг 2 (зелёный):

```console
$ timeout 20 node tools/dashboard/testdata/outbox_offline.mjs tools/dashboard/static/app.js
очередь исходящих: обрыв связи держится, сеть вернулась и сообщение ушло само, перезагрузка очередь не потеряла, уход с экрана цикл гасит, во «Входящих» по одной строке на сообщение
```

Шаг 3 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test -run 'TestStaticPulseRing|TestChatOutboxQueueSendsItself' -timeout=25s -v ./...
=== RUN   TestChatOutboxQueueSendsItself
    messages_test.go:744: очередь исходящих: обрыв связи держится, сеть вернулась и сообщение ушло само, перезагрузка очередь не потеряла, уход с экрана цикл гасит, во «Входящих» по одной строке на сообщение
--- PASS: TestChatOutboxQueueSendsItself (0.08s)
=== RUN   TestStaticPulseRing
    pulse_test.go:531: кольцо агентов: место в шапке, четыре состояния, деления по плану сессии, простой с планом и контур без плана, список агентов с дорогой в разговор, давность словами, список закрывается вторым нажатием, кликом мимо и Escape, как и выбор подписки
--- PASS: TestStaticPulseRing (0.08s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	0.965s
```

- Прогон проверки (агент): 2026-09-19 23:34, sonnet, три шага сценария с корня
  репозитория, все зелёные (`agentctl stage DK-1064 проверка --by sonnet`).

Шаг 1 (зелёный):

```console
$ timeout 15 node tools/dashboard/testdata/poc_ring.mjs tools/dashboard/static/app.js
кольцо агентов: место в шапке, четыре состояния, деления по плану сессии, простой с планом и контур без плана, список агентов с дорогой в разговор, давность словами, список закрывается вторым нажатием, кликом мимо и Escape, как и выбор подписки
```

Шаг 2 (зелёный):

```console
$ timeout 20 node tools/dashboard/testdata/outbox_offline.mjs tools/dashboard/static/app.js
очередь исходящих: обрыв связи держится, сеть вернулась и сообщение ушло само, перезагрузка очередь не потеряла, уход с экрана цикл гасит, во «Входящих» по одной строке на сообщение
```

Шаг 3 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test -run 'TestStaticPulseRing|TestChatOutboxQueueSendsItself' -timeout=25s -v ./...
=== RUN   TestChatOutboxQueueSendsItself
    messages_test.go:744: очередь исходящих: обрыв связи держится, сеть вернулась и сообщение ушло само, перезагрузка очередь не потеряла, уход с экрана цикл гасит, во «Входящих» по одной строке на сообщение
--- PASS: TestChatOutboxQueueSendsItself (0.09s)
=== RUN   TestStaticPulseRing
    pulse_test.go:531: кольцо агентов: место в шапке, четыре состояния, деления по плану сессии, простой с планом и контур без плана, список агентов с дорогой в разговор, давность словами, список закрывается вторым нажатием, кликом мимо и Escape, как и выбор подписки
--- PASS: TestStaticPulseRing (0.08s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	0.960s
```
