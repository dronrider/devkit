# DK-1084: Цель: прогон слияния не валит чужие слияния и не давит машину

## Что происходит

Ключ `test` в `.devkit/deploy.local` это `python3 tools/devkitctl/parallel.py`, и его гонит `shipctl merge` перед каждым слиянием. Раннер поднимает восемь компонентов разом (`-j 8` по умолчанию, `tools/devkitctl/parallel.py:192`). Внутри компонента devkitctl идёт своя сюита `suite.py` с потолком `MAX_WORKERS = min(12, cpu_count)` (`tools/devkitctl/suite.py:30`), а go-компоненты гонят пакеты своим параллелизмом `go test`. Потолки перемножаются, общего бюджета у них нет, и приоритет у процессов прогона такой же, как у дашборда и терминала.

2026-09-20 в 18:53 сессия цели DK-909 запустила `shipctl merge DK-1081`. На десяти ядрах (восемь производительных и два экономичных) шли семь-восемь процессов python unittest по 95-100% CPU каждый, load average 16, прогон длился больше двенадцати минут. В эти минуты дашборд открывал задачу по тридцать секунд. Журнал `~/.devkit/dashboard.log` с 18:55 пишет «tmux ls: exec: WaitDelay expired» и «taskctl list --json ... не ответил за 30s и снят по сроку» по деревьям devkit-glm и devkit-dk-974, дальше каждый круг опроса досок пропущен. На свободной машине `taskctl list --json` идёт 0,3-1,9 с.

2026-09-19 около 02:00 то же самое при `shipctl merge DK-881 --train`. Load average вырос с 8 до 78, ручка дашборда `/api/projects` отвечала 50 с, доска и список чатов не уложились в 60 с. После конца прогона те же ручки отвечали за 0,1-1,2 с. Память тут ни при чём. После уборки чатов и перезапуска fseventsd свободно 88% памяти при свопе 2 ГБ, а картина под прогоном та же.

Семейство строк про это уже набралось, и каждая про свой тест.

- DK-764 (P1, задача): тесты со стенным потолком времени валят чужие слияния; приём выбран человеком 2026-09-12, мерить факт вместо времени; в семейство вписаны браузерные стенды доски, TestPulseRemembersJournalSteps и замок стенда taskctl, к строке приписан черновик DK-956.
- DK-746 (P1, задача): TestFeedTailSkipsWholeJournals красит под load average 214, отношение времён как замена отвергнуто.
- DK-999 (P1, задача): тот же TestFeedTailSkipsWholeJournals, дубль DK-746 с другой формой порога.
- DK-315 (P2, задача): quota_refresh краснеет под нагрузкой и роняет merge, направление названо, код не написан.
- DK-537 (черновик): go:dashboard упирается в десятиминутный потолок go test под `-j 8`, с `-j 5` прогон зелёный.
- DK-380 (черновик): go:dashboard держит стену прогона, 605 с под четырьмя исполнителями, вместе с ним упало слияние DK-366.
- DK-669 (черновик): браузерные тесты доски не поднимают хром за 90 с под нагрузкой.
- DK-845 (черновик): TestAskpassChainWithFakeSudo мигает при пороге опроса 3 с, пакет шёл 420 с против 176 с в одиночку.
- DK-883 (черновик): TestQuotaStaleConfigured краснеет, когда между записью снимка и проверкой набегает больше пяти минут.
- DK-926 (черновик): компонент hooks под `-j 8` валится разными тестами от захода к заходу, с `-j 4` зелёный.
- DK-983 (черновик): полная пачка красная разными компонентами от прогона к прогону, порознь все зелёные.
- DK-1000 (черновик): хук квоты и цикл цели красны на двух прогонах `shipctl merge DK-996` подряд, слияние прошло на `-j 3`.
- DK-1038 (черновик): замер цены холостого хода хука chat_in трижды отбил чужие слияния на разнице в сотые доли секунды.

Соседи другой природы, бюджет машины их не вылечит. DK-767 (черновик) про тест freshtree, которому в окружении прогона слияния не хватает CARGO_HOME. DK-368 (черновик) про общие ресурсы временных стендов suite.py, где классы портят друг другу файлы стенда.

Закрытое повторять не надо. DK-635 убрала секундомер из тестов parallel и проверяет факт смерти компонента файлом. DK-733 привязала фикстуры группировки чатов к полудню вместо момента запуска. Цель DK-166 собрала параллельный раннер, потолок восьми воркеров пришёл оттуда и мерился на свободной машине.

### Проверено грумингом 2026-09-21

Актуальность на месте. Потолок `-j 8` стоит умолчанием в `tools/devkitctl/parallel.py:192`. Потолок сюиты `MAX_WORKERS = min(12, os.cpu_count() or 4)` стоит в `tools/devkitctl/suite.py:30`. Ключ `test` в `.devkit/deploy.local` зовёт `parallel.py` без `-j`. Комментарий над потолком сюиты мерил стену на свободной машине. Плато лежит между восемью и шестнадцатью воркерами, откат начинается к двадцати четырём. Соседний прогон и дашборд в том замере не участвовали.

Семейство шире списка выше, к нему добавляются три соседа. DK-908 (P2, задача на доске) это тот же TestQuotaStaleConfigured, что и черновик DK-883, и DK-883 выходит дублем строки DK-908. DK-944 (черновик) это те же браузерные тесты доски под нагрузкой, что DK-669. Там хром убит пределом 90 с при load average около 20. DK-811 (черновик) это тот же go:dashboard под `-j 8`, что DK-537 и DK-380. Слияние DK-801 отбито трижды и прошло на `-j 3`. Соседи про сторож прогонов runsguard (DK-878, DK-1071, DK-1083) и про гонку fingerprint (DK-1016) к цели не относятся. У них причина в соседней сессии и в git maintenance.

Дубля самой цели на доске нет. Открытых целей семь, ни одна из них не про прогон тестов. DK-166 закрыта 2026-08-16. Условия старта цели: `autonomous = true` стоит. Снимок квоты на 08:39 показывает week_all 81% и week_max 80% при сбросе обоих бакетов сегодня в 15:00. Старт цикла ложится на свежую неделю.

Обход кода и доски нашёл сверх записанного. Множителей потолка три. Компонент go зовёт go test без -p и без GOMAXPROCS, и пакеты идут по числу ядер. Сюита suite.py стартует отдельным подпроцессом. Семафор внутри процесса между раннером и сюитой при этом невозможен, доли раздаёт верхний раннер или файловый замок. Потолок 30 с у дашборда стоит в tools/dashboard/board.go:60 для подпроцессов tmux и taskctl. Порог 120 с цели DK-166 снят решением пользователя 2026-08-16. Стена 322 с под -j 8 из DK-347 мерилась при load 3.5-15. Журнала слияний, отбитых чужой краснотой, нет. Запись shipctl хранит принадлежность коммитов, итогов прогона в ней нет. Число из гипотезы сегодня считается только ручным разбором файлов задач. Замеров стенного времени одного зелёного прогона на разных -j в файлах семейства нет, есть только пары «уронило или нет».

Корень один, а бед две. Первая беда в том, что тесты, меряющие стенное время, краснеют под нагрузкой собственного прогона и валят чужие слияния. В DK-764 записаны четыре отбитых слияния за сутки, в DK-1038 три за одну пачку, в DK-1000 два лишних прогона по пять минут на одно слияние. Исполнитель идёт разбирать компонент, которого не трогал, а потом сливает с `-j 3` или `-j 5`, в обход штатной команды. Вторая беда в том, что всё интерактивное на машине голодает десять-двадцать минут на каждое слияние. Дашборд не открывает задачу, круги опроса досок пропускаются, `taskctl` снимается по сроку, и человек в это время не видит ни доски, ни чатов. При двух-трёх слияниях за вечер машина отнята у человека на час.

Строки семейства чинят по одному тесту, и каждая новая пачка задач приносит новый черновик про новый тест. Число красных слияний по чужой красноте при этом не падает, а замера этого числа нет вовсе.

## Чего хотим

Слияние идёт с бюджетом машины и чужие слияния не роняет. Снаружи это видно тремя вещами. Доска дашборда и `taskctl list --json` под идущим прогоном слияния отвечают в пределах своего потолка, и проверяется это прогоном под нагрузкой, поднятой нарочно. Тесты времени меряют факт или свой процессорный бюджет, и полный прогон на живом коде зелёный три захода подряд под соседним прогоном. Число слияний, отбитых чужой краснотой, считается по журналу и падает до нуля за неделю после выката.

## Границы

Вне цели строки другой природы: DK-315 (гонка `awaited` в тесте хука квоты), DK-767 (CARGO_HOME в окружении прогона), DK-368 (общие ресурсы стендов сюиты), сторож runsguard (DK-878, DK-1071, DK-1083) и гонка fingerprint (DK-1016). Порог 120 с из DK-166 не возвращается.

## Развилки

Черновик ставил три вопроса. Первый про рычаг: снижать общий параллелизм прогона, отдавать приоритет пониженным nice всему дереву процессов или то и другое. Второй про судьбу семейства строк после цели. Третий про длительность прогона слияния и её потолок. Все три разобраны развилками ниже, подробности кандидатов и замеров лежат в разделе «Что происходит».

- «кейсы»: какие пользовательские кейсы у цели DK-1084? рекомендую широкий: кейсы (5) и (6) пришли из обхода и осложнения, без них цель это одна задача про потолок, а не конвейер без чужой красноты
  - решает: человек
  - рекомендация: широкий состав, 6 кейсов: (1) исполнитель сливает задачу, прогон валит компонент, которого он не трогал, и он перепрогоняет с -j 3 руками; (2) человек открывает доску и чаты дашборда во время прогона слияния и ждёт по 30-60 с, круги опроса досок пропускаются; (3) два слияния или поезд идут одновременно, и прогоны накладываются друг на друга; (4) виток цели сливает ночью без человека и тратит лишние прогоны по пять минут на одно слияние; (5) исполнитель разбирает красноту и не знает, своя она или чужая, потому что журнала итогов прогона нет; (6) новый тест со стенным порогом пишется по старому образцу, и семейство растёт черновиками
  - вариант: сбалансированный состав, 4 кейса: (1) исполнитель сливает задачу, прогон валит чужой компонент, перепрогон с -j 3 руками; (2) человек ждёт доску и чаты дашборда по 30-60 с во время прогона; (3) виток цели ночью тратит лишние прогоны на одно слияние; (4) исполнитель не различает свою и чужую красноту без журнала
  - вариант: узкий состав, 2 кейса: (1) исполнитель сливает задачу, прогон валит чужой компонент, перепрогон с -j 3 руками; (2) человек ждёт доску и чаты дашборда по 30-60 с во время прогона
  - решено человеком 2026-09-21: широкий состав, 6 кейсов: (1) исполнитель сливает задачу, прогон валит компонент, которого он не трогал, и он перепрогоняет с -j 3 руками; (2) человек открывает доску и чаты дашборда во время прогона слияния и ждёт по 30-60 с, круги опроса досок пропускаются; (3) два слияния или поезд идут одновременно, и прогоны накладываются друг на друга; (4) виток цели сливает ночью без человека и тратит лишние прогоны по пять минут на одно слияние; (5) исполнитель разбирает красноту и не знает, своя она или чужая, потому что журнала итогов прогона нет; (6) новый тест со стенным порогом пишется по старому образцу, и семейство растёт черновиками
- «длительность»: где потолок длительности прогона слияния и входит ли он в DoD?
  - решает: человек
  - рекомендация: длительность в DoD не входит; первой задачей цели A/B замер стены одного зелёного прогона на -j 3, 4, 5 и 8 на той же машине, потолок ставится по замеру, ориентир не дольше стены DK-347 (322 с под -j 8) с половиной запаса, восемь минут; порог 120 с из DK-166 снят человеком 2026-08-16 и не возвращается
  - вариант: жёсткий потолок восемь минут в DoD без замера, прогон дольше это провал цели
  - вариант: длительность не ограничивать, меры успеха только краснота и живой дашборд
  - источник: дока docs/tasks/archive/2026/DK-166.md, раздел «Итог», порог 120 с снят решением пользователя; docs/tasks/archive/2026/DK-347.md:102, стена 322 с под -j 8 при load 3.5-15
  - решено человеком 2026-09-21: длительность в DoD не входит; первой задачей цели A/B замер стены одного зелёного прогона на -j 3, 4, 5 и 8 на той же машине, потолок ставится по замеру, ориентир не дольше стены DK-347 (322 с под -j 8) с половиной запаса, восемь минут; порог 120 с из DK-166 снят человеком 2026-08-16 и не возвращается
- «бюджет»: какой бюджет у цели в пунктах бакетов и нужен ли потолок яруса?
  - решает: человек
  - рекомендация: week_all <= 30, week_max <= 12, без потолка яруса, как у DK-1069 при том же ранге 65 и составе из десятка задач; оба бакета сбрасываются сегодня в 15:00, цикл стартует на свежей неделе
  - вариант: week_all <= 20, week_max <= 10, ярус pro: механика бюджета это одна-две задачи, а семейство добирается порознь
  - вариант: week_all <= 40, week_max <= 15: замеров под нагрузкой много, витков будет больше десятка
  - решено человеком 2026-09-21: week_all <= 30, week_max <= 12, без потолка яруса, как у DK-1069 при том же ранге 65 и составе из десятка задач; оба бакета сбрасываются сегодня в 15:00, цикл стартует на свежей неделе
- «рычаг»: чем снимать перемножение потолков: общим потолком, nice или тем и другим?
  - решает: исполнитель
  - рекомендация: то и другое: один потолок параллельности от числа ядер в parallel.py, раздаваемый компонентам внутрь (suite.py аргументом, go test флагом -p и GOMAXPROCS через окружение), плюс nice на дерево процессов прогона; семафор через границу процессов не нужен, доли раздаёт верхний раннер; множителей три, а не два: go test без -p берёт число ядер
  - источник: код tools/devkitctl/parallel.py, components(): go test без -p и без GOMAXPROCS, suite.py отдельным подпроцессом; nice и taskpolicy в tools/ и hooks/ не встречаются
- «семейство»: что делать со строками семейства после цели?
  - решает: человек
  - рекомендация: нагрузочные строки закрываются замером после выката бюджета машины: DK-537, DK-380, DK-811, DK-926, DK-983, DK-1000; тесты со стенным временем остаются строками под целью: DK-764, DK-746, DK-908, DK-845, DK-1038, DK-669; дубли приписываются к строкам: DK-999 к DK-746, DK-883 к DK-908, DK-944 к DK-669; вне цели DK-315, где гонка awaited по первому непустому файлу и бюджет её не чинит, а также DK-767 и DK-368
  - вариант: все шестнадцать строк в задачи цели, каждая закрывается своим прогоном под целью
  - вариант: цель только про бюджет машины, семейство не трогать, строки живут своей жизнью
  - источник: доска DK-315 (hooks/quota_refresh_test.py:24, awaited по первому непустому файлу), DK-908, DK-944, DK-811
  - решено человеком 2026-09-21: нагрузочные строки закрываются замером после выката бюджета машины: DK-537, DK-380, DK-811, DK-926, DK-983, DK-1000; тесты со стенным временем остаются строками под целью: DK-764, DK-746, DK-908, DK-845, DK-1038, DK-669; дубли приписываются к строкам: DK-999 к DK-746, DK-883 к DK-908, DK-944 к DK-669; вне цели DK-315, где гонка awaited по первому непустому файлу и бюджет её не чинит, а также DK-767 и DK-368
- «DoD»: чем цель считается достигнутой, так чтобы последний виток проверил это командой?
  - решает: человек
  - рекомендация: три строки, все агентские: (а) под нарочно поднятой нагрузкой, вторым полным прогоном parallel.py рядом, taskctl list --json и ручка дашборда /api/projects отвечают не дольше 5 с в десяти замерах подряд, потолок 30 с из board.go не задет ни разу; (б) полный прогон parallel.py на живом коде зелёный три захода подряд, каждый под соседним полным прогоном, на -j умолчания; (в) shipctl пишет журнал итогов прогона test по компонентам, команда печатает число слияний, отбитых компонентом вне диффа задачи, и с момента выката это число ноль; недельное наблюдение идёт в раздел «Итог», ворота держат команда и ноль по журналу
  - вариант: две первые строки, журнал и счётчик отбитых слияний отдельной задачей после цели
  - вариант: пользовательский DoD: человек сливает за вечер две задачи и не замечает прогона по дашборду, цикл останавливается на согласии
  - решено человеком 2026-09-21: три строки, все агентские: (а) под нарочно поднятой нагрузкой, вторым полным прогоном parallel.py рядом, taskctl list --json и ручка дашборда /api/projects отвечают не дольше 5 с в десяти замерах подряд, потолок 30 с из board.go не задет ни разу; (б) полный прогон parallel.py на живом коде зелёный три захода подряд, каждый под соседним полным прогоном, на -j умолчания; (в) shipctl пишет журнал итогов прогона test по компонентам, команда печатает число слияний, отбитых компонентом вне диффа задачи, и с момента выката это число ноль; недельное наблюдение идёт в раздел «Итог», ворота держат команда и ноль по журналу

## DoD

Три строки, все агентские, выбраны человеком 2026-09-21.

- Под нарочно поднятой нагрузкой, вторым полным прогоном `parallel.py` рядом, `taskctl list --json` и ручка дашборда `/api/projects` отвечают не дольше 5 с в десяти замерах подряд. Потолок 30 с из `tools/dashboard/board.go` не задет ни разу.
- Полный прогон `parallel.py` на живом коде зелёный три захода подряд, каждый под соседним полным прогоном, на `-j` умолчания.
- `shipctl` пишет журнал итогов прогона `test` по компонентам. Команда печатает число слияний, отбитых компонентом вне диффа задачи, и с момента выката это число ноль. Недельное наблюдение идёт в раздел «Итог», ворота держат команда и ноль по журналу.

Длительность прогона в DoD не входит. Первая задача цели меряет стену одного зелёного прогона на `-j 3`, `4`, `5` и `8` на одной машине, потолок ставится по замеру. Ориентир восемь минут, стена 322 с из DK-347 с половиной запаса.

## Ранг

`50+8+3+0+4 = 65`, P1.

- Серьёзность 50. Основной сценарий слияния ломается чужой краснотой, обход есть и болезненный: перепрогон с `-j 3` руками и разбор чужого компонента. Человек на час за вечер остаётся без доски и чатов.
- Ценность 8. Конвейер идёт без чужой красноты, а машина во время слияния остаётся у человека. Это прямой вклад в непрерывность конвейера.
- Неопределённость 3. Подход виден, развилки в стыке: три множителя потолка в двух языках и трёх процессах, nice на macOS без прецедента в репозитории.
- Поправка на баг 0. Цель это серия, поправку несут строки семейства.
- Рычаг 4. Зелёный прогон делает каждое следующее слияние дешевле, лишние прогоны по пять минут уходят.

Итого 65, P1. Цена XL, цель это серия задач.

## Цель

Прогон тестов перед слиянием идёт с бюджетом машины. Он не роняет чужие слияния краснотой тестов со стенным временем и не отнимает у человека доску и чаты дашборда. DoD стоит разделом «DoD» выше, все три строки агентские, кейсы широкого состава решены человеком 2026-09-21 в разделе «Развилки». Условий, посильных только человеку, не нашёл: замеры под нагрузкой, правка раннера и журнал shipctl делаются с машины агента. Согласие человека понадобилось на судьбу семейства строк, оно получено развилкой «семейство».

## Бюджет

бюджет: week_all <= 30
бюджет: week_max <= 12

Потолка яруса нет, решено человеком развилкой «бюджет». Бакеты общие на машину, число это верхняя граница трат машины за время цикла.

## Задачи цели

Нарезка 2026-09-22, двенадцать строк на доске. Судьба строк семейства решена развилкой «семейство».

- DK-1122 (task, S). Стена прогона померена на `-j 3`, `4`, `5` и `8`, потолок длительности стоит по замеру. Первая по решению человека развилкой «длительность».
- DK-1123 (task, M). Потолок параллельности один на прогон и раздан компонентам внутрь. После DK-1122, развилка «рычаг» перенесена в строку.
- DK-1124 (task, M). Прогон слияния идёт пониженным приоритетом, дашборд и терминал не голодают. После DK-1123, держит первую строку DoD.
- DK-1125 (task, M). Итоги прогона по компонентам ложатся в журнал, слияния от чужой красноты считаются командой. Держит третью строку DoD.
- DK-1126 (task, M). Новый тест меряет факт вместо стенного времени, образец стоит в стандарте, а сторож смотрит дифф. Кейс (6) состава кейсов.
- DK-1127 (task, S). Нагрузочные черновики закрываются замером после выката бюджета машины: DK-537, DK-380, DK-811, DK-926, DK-983, DK-1000. После DK-1124.
- DK-764 (bug, M). Тесты со стенным потолком времени валят чужие слияния под нагрузкой.
- DK-746 (bug, S). Замер ленты дашборда валит слияние на загруженной машине. Дубль DK-999 приписан и закрыт нарезкой.
- DK-908 (bug, S). Тест свежести снимка квоты краснеет под нагрузкой параллельного прогона. Дубль DK-883 приписан грумингом.
- DK-845 (bug, S). Тест цепочки askpass мигает под нагрузкой полного прогона.
- DK-1038 (bug, S). Тест цены холостого хода хука chat_in валит слияния под нагрузкой.
- DK-669 (bug, M). Браузерные тесты дашборда мигают под параллельной нагрузкой машины. Дубль DK-944 приписан нарезкой.
- DK-1135 (bug, S). Замер тёплого опроса кольца валит слияние на загруженной машине. Дорезка 2026-09-23 по замечанию ревью DK-764: замер кольца стоял в DoD DK-764 одним списком с браузерными стендами, а хозяина среди строк цели не имел. DoD DK-764 той же правкой сужен до стенда подъёма taskctl.

оценка: 12 строк (5 M, 7 S), week_all 8.5-22 из 30, week_max 11-22 из 12; по week_max остаток попал внутрь вилки, цель едет с этим расхождением

## Журнал

- снимок 2026-09-22T21:20: week_all 5%, week_max 7%
- 2026-09-22 21:20-21:50, нарезка: 12 строк цели на доске. Новые DK-1122 (замер стены прогона), DK-1123 (общий потолок параллельности), DK-1124 (пониженный приоритет), DK-1125 (журнал итогов прогона), DK-1126 (стандарт и сторож стенного времени), DK-1127 (уборка нагрузочных черновиков). Строки семейства привязаны к цели: DK-764, DK-746, DK-908, DK-845, DK-1038, DK-669; последние три оформлены из черновиков. Дубли приписаны: DK-944 к DK-669, DK-999 к DK-746, сама DK-999 закрыта. Развилка «рычаг» перенесена в DK-1123 и DK-1124. Условий, посильных только человеку, не нашлось. Оценка 12 строк (5 M, 7 S): week_all 8.5-22 из 30 влезает, week_max 11-22 против остатка 12, остаток попал внутрь вилки, цель едет с этим расхождением; continue
- снимок 2026-09-22T22:06: week_all 7%, week_max 8%
- 2026-09-22 22:06-23:43, замер DK-1122 раунд 1: стена -j 3 535 с, -j 4 569 с, -j 5 564 с, -j 8 308 с, все четыре прогона красные. Снижение -j стену не сокращает, а удваивает: длинный полюс это компонент go:dashboard, и раннер встаёт на первом же провале. Находка вперёд замера: main красный с 766c5b400 (DK-1120), вырезка mdSource в messages_test.go не знает inlineCodeSpan, юниты рендера падают под node, и слияний в репозитории нет вовсе. Завёл строкой под цель DK-1132 (bug, P0, R=81), исполнитель починил вырезку и игрушечный DOM, закрыл разрыв сторожем имён, пакет tools/dashboard зелёный 502 с, два коммита в ветке dk-1132, ждёт ревью; continue
- снимок 2026-09-22T23:41: week_all 10%, week_max 10%
- 2026-09-22 23:41-2026-09-23 00:25, DK-1132 слита в main (fast-forward, полный прогон в свежем дереве зелёный), поезд держит её и чужую DK-1120, выкат не звал. Ворота merge отбивали ветку четырежды: исход замечания ревью, раздел «Сценарий проверки», зелёный regcheck в журнале ветки и утечка DEVKIT_GOAL_SHELL из оболочки цикла в TestWatchRegisterNamesSessionAndCarrier. Замер DK-1122 надо перегнать: раунд 1 мерил время до первого провала, а не стену зелёного прогона, и числа -j 3/4/5 к длительности прогона отношения не имеют. Второе препятствие devkit: субагент не может позвать go test и regcheck, префикс GOWORK=off требует одобрения, исполнитель и ревьювер обходили обёртками; continue
- снимок 2026-09-23T00:26: week_all 10%, week_max 10%
- 2026-09-23 00:26-00:40, DK-1130 оказалась дублем DK-1132 из соседней сессии, закрыта припиской и запушена. Сценарий DK-1132 прогнан проверяющим haiku из основного чекаута: точечный прогон 0,9 с, полный пакет 494 с, оба ok, отметка проверки стоит. Выкат поезда отбит воротами перевода в Check: чужая DK-1120 идёт с видом приёмки mixed и не имеет ни одной строки обхода, а состав общий, так что DK-1132 стоит слитой и незакрытой. Отметка ожидания на 45 минут, работа взята дальше. Замер DK-1122 раунда 2 запущен на зелёном коде: -j 3, 4, 5 и 8 по три захода, дерево подтянуто на main; continue
- снимок 2026-09-23T00:26: week_all 10%, week_max 10%
- 2026-09-23 00:26-02:59, 2026-09-23 00:42-03:05, DK-1122 доведена до Check и слита в поезд (4edf8bad7). Замер раунда 2 на зелёном main: -j 3 1276 с ok, -j 4 1184 с ok, -j 5 557 с FAIL на уборке TempDir в TestChatListGluesWindowPasses, -j 8 1044 с ok. Стена от -j почти не зависит, а понижение с 8 до 3 удлиняет её на 22 процента: весь полюс это пакет go:dashboard, 494-556 с в одиночку против 110-147 с в замере DK-380 полтора месяца назад. Потолок длительности прогона слияния поставлен 1560 с, ориентир восемь минут из постановки замером снят. Ревью уровня 2 прошло без замечаний. Обкатка сценария загашена исключением: сценарий это полный прогон проекта и вдвое длиннее предела обкатки в 10 минут. Сценарий прогнан проверяющим haiku и оказался красным, упал компонент hooks тестом quota_refresh (гонка DK-315) при load average 4-10 от соседней сессии, wall 246 с до провала. Это ровно беда цели: чужая краснота валит прогон под нагрузкой, а DK-315 стоит в границах цели как строка другой природы. Отметки проверки нет, повтор на свободной машине. Выкат поезда по-прежнему держит чужая DK-1120 с приёмкой mixed, вместе с ней невыкаченными стоят DK-1132 и DK-1122; continue
- снимок 2026-09-23T02:55: week_all 10%, week_max 10%
- снимок 2026-09-23T02:56: week_all 10%, week_max 10%
- 2026-09-23 02:56-03:42, 2026-09-23 03:26-03:45, DK-1122 закрыта: повторный прогон сценария проверяющим opus на разгруженной машине зелёный, стена 1066 с, все 18 компонентов ok, потолок 1560 с прогоном подтверждён. Первый заход этого прогона упал на TestAskpassChainWithFakeSudo при load average 30.5, случай дописан вторым в файл DK-845. Отметка проверки стоит, ребро снято у DK-1123. Разлив поезда (DK-1120, DK-1132) и пуш упираются в незакоммиченные правки соседних сессий в основном чекауте: черновик DK-1131, ссылка в LLD DK-933 и .gitignore; continue
- снимок 2026-09-23T03:42: week_all 11%, week_max 10%
- 2026-09-23 03:42-04:53, Исполнители витка 5 погибли фоновыми субагентами через 10 минут после конца хода, правки остались в деревьях незакоммиченными; подняты заново переднем планом, и это порядок на весь цикл. DK-908 оформлена и прошла ревью уровня 2 без замечаний: тест свежести снимка квоты проверяет факт вместо стенной строки «возраст 5м», краснота показана подменой снимка на родительском коммите, ворот regcheck загашен исключением (правка целиком в тесте). DK-1123 оформлена по развилке «рычаг» (один бюджет от числа ядер, доли компонентам через -p, -j и GOMAXPROCS), ревью уровня 3 вернуло блокирующее замечание: доля считается один раз на пачку и держится статичной, так что полюс go:dashboard получает GOMAXPROCS=1 и в хвосте прогона, где девять долей простаивают, а замер DK-1122 показал, что понижение параллельности этому пакету стену удлиняет. Слияние DK-908 отбито дважды: сперва утечкой DEVKIT_GOAL_SHELL из оболочки цикла в TestWatchRegisterNamesSessionAndCarrier (второй раз за цель), потом на пороге 2 с в стенде pickStand у TestRunTakesModelFromPick. Второе это известный случай DK-764, новой строки не требует, и он держит все слияния цикла, поэтому DK-764 берётся вперёд остального. Разлив поезда (DK-1120, DK-1132) по-прежнему упирается в чужую DK-1120 без строк обхода при приёмке mixed; continue
- снимок 2026-09-23T04:28: week_all 11%, week_max 10%
- 2026-09-23 04:28-06:34, DK-764 закрыта кодом и слита в main (поезд DK-1120, DK-1132, DK-764). Красноту в стенде подъёма taskctl давал не порог как таковой: заглушка python3 стенда pickStand никогда не передавала замок, лестница ждала стенные две секунды DEVKIT_RUN_ADOPT_WAIT и по сроку убивала заглушку, а под нагрузкой заглушка не получала процессора и умирала до первой своей строки. Теперь заглушка играет голову целиком и отдаёт замок, ступень headless берётся фактом передачи, своего потолка секунд стенд не ставит. Остаток стенного срока назван: передачу замка лестница ждёт боевым потолком DefaultAdopt, тридцать секунд, и решение человека от 2026-09-12 его не трогает. Строка tmux не поднял окно к красноте отношения не имеет, заглушка tmux в стенде отказывает нарочно. Ревью уровня 3 приняло с неблокирующим замечанием на постановку: DoD DK-764 брал семейство одним списком, и два его члена чинятся не здесь. DoD сужен до стенда подъёма taskctl, браузерные стенды названы строкой DK-669 с дублем DK-944, а замер тёплого опроса кольца TestPulseRemembersJournalSteps хозяина среди строк цели не имел и заведён дорезкой строкой DK-1135 (bug, S, R 67), тринадцатой задачей цели. Заведён черновик DK-1134: решение DK-460.1 ставит ворота готовности командой shipctl ready, а команды в утилите нет, отбилось дважды за сессию. Слияние DK-908 идёт повторным заходом, первый отбило ровно той краснотой, которую сняла DK-764; continue
- снимок 2026-09-23T06:01: week_all 12%, week_max 10%
- 2026-09-23 06:01-08:05, DK-908 слита в main вторым заходом: первый отбило ровно той краснотой стенда подъёма taskctl, которую сняла DK-764, и это прямое подтверждение беды цели. Поезд собрал четыре строки (DK-1120, DK-1132, DK-908, DK-764), разлив третий раз упёрся в чужую DK-1120: при приёмке mixed у барьера «глаза» три обхода, а строк исхода в «Приёмка» ноль, и пишет их владелец строки. Отметка ожидания 90 мин, чужую очередь цикл не толкает. DK-1123 прошла два круга ревью и уходит на третий. Первый круг вернул статичную долю бюджета, и доработка сделала долю живой: класс _LiveShare считает её по числу воркеров, ещё не нашедших очередь пустой, а к хвосту прогона доля новых компонентов растёт. Предел способа исполнитель назвал сам: -p и GOMAXPROCS домешать после старта процесса нельзя, и компонент первой волны держит статичную долю до конца, а go:dashboard в первую волну попадает всегда. Второй круг на этом и вернул. Живой замер трёх прогонов дал стену 508, 510 и 514 с при потолке 1560 с, но все три прогона красные, и секунды считались до провала, а не по концу всех компонентов. Это ровно ошибка раунда 1 замера DK-1122, и с потолком такая стена несравнима. Зелёного полного прогона на новом коде нет ни одного. Красноту всем трём заходам дала гонка TempDir в go:dashboard (TestChatListGluesWindowPasses, TestChatListShowsQuotaWait), исполнитель завёл её черновиком DK-1136 и назвал сторонней, а ревьювер заметил расхождение: сам черновик предполагает, что систематической гонку делает как раз GOMAXPROCS=1 (три из трёх против одного из четырёх в замере DK-1122). Третий круг отвечает на это и добывает зелёный прогон. Неблокирующее замечание первого круга закрыто тестом склейки args.jobs в долю; continue
- снимок 2026-09-23T07:31: week_all 12%, week_max 10%
- 2026-09-23 07:31-14:32, Закрыты четыре задачи цели. DK-1132, DK-908 и DK-764 выкачены поездом соседней сессии, сценарии прогнаны проверяющим sonnet и все три зелёные: юниты рендера ленты 0,6 с и полный пакет дашборда 536 с, подмена снимка квоты краснеет на родителе и зелена на фиксе, стенд подъёма taskctl зелёный под задержкой 25 с и три захода подряд под двенадцатью счётчиками нагрузки. DK-1123 прошла четыре круга ревью и слита. Третий круг снял главный вопрос замером: гонка уборки TempDir в дашборде оказалась не посторонней, её систематической делает душение полюса, пять красных полных прогонов из пяти под GOMAXPROCS=1 и шестой под GOMAXPROCS=2 против зелёных без переменной. Полюсом оказалась не связка go, а сюита devkitctl, 1161 с при своих десяти воркерах против 526-586 с у дашборда, и прежняя равная доля давала ей единицу, то есть последовательный прогон ста пятидесяти семи классов. Раздача переделана: половина бюджета раннеру, лайны из остатка, раннер первым в очереди, GOMAXPROCS снят вовсе с доводом в файле задачи. Зелёный полный прогон нового кода взял 1295 с при потолке 1560, в пике шесть процессов при бюджете десять. Четвёртый круг закрыл щель между снятием компонента с очереди и счётом его доли: шаги сведены в один под замком, инвариант держится при любом порядке. Заведены два черновика. DK-1136 про гонку уборки TempDir в стенде дашборда, там же вторая находка про живой claude в стенде. DK-1137 про рубеж свежести отметки: onlyTaskDocSince рвёт любой чужой файл между меткой и HEAD, а на живой доске чужой файл ложится каждые пару минут, и за сутки это трижды отбило выкат, плюс осложнение, где fast-forward упал на чужом index.lock после двадцати пяти минут зелёного прогона. Выкат поезда с DK-1123 держит чужая DK-1120 без отметки smoke, отметка ожидания 90 минут; continue

## Итог

## Ход работы

- Из черновика DK-1084 «Цель: прогон слияния не валит чужие слияния и не давит машину»: записан 2026-09-20, оформлен 2026-09-21.
- Постановка: разбор черновика, сессия startup, 2026-09-21 08:40-21:29.
- Вычитка: 6 файлов, 2 правки, 0 пометок, 2026-09-22.
