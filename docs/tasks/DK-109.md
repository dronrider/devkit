# DK-109: Цель: тонкий резидент по DK-100, минус 65% контекста правил

## Цель

Правила devkit лежат в контексте каждой сессии целиком и стоят ~14,3 тысячи
токенов в каждом запросе, это треть старта. Дизайн
[DK-100](../lld/DK-100-context-tree.md) режет их деревом: тонкое ядро
резидентно, процедуры уезжают скиллами по вызову, стилевые запреты держат хуки.
Цель это довести серию дизайна до конца, чтобы экономия была не в документе, а
на машине.

Фронт один, контекстный. Подписочная сторона (DK-089 про лестницу трат, DK-090
про вторую подписку) в цель не входит и живёт своими строками.

DoD, проверяется командами:

1. `devkitctl doctor` в чекауте devkit печатает вес резидента по карманам и не
   даёт находок по порогам: ядро <= 5 500 символов, ядро доски <= 1 500
   символов, общий потолок резидента devkit 6 500 токенов.
2. Контрольный замер тем же способом, каким мерил дизайн: headless-прогон
   `claude -p` в пустой директории против прогона в devkit, и разница по тексту
   devkit не больше 6 500 токенов. Сегодня она 14 290.

## Бюджет

бюджет: week_all <= 30
бюджет: week_max <= 15
ярус: pro

Числа это верхняя граница трат машины за время цикла, а не счётчик самой цели:
бакеты общие на машину, и параллельная сессия тратит те же проценты. Потолок
яруса режет вердикт `agentctl pick` последним шагом, кроме нарезки и разбивки,
их ниже pro не опускает ничто.

## Задачи цели

Ядро состава пришло с доски разбивкой дизайна (DK-102..DK-108), нарезка сверила
его с DoD и дорезала одну строку: живой замер [DK-110](archive/2026/DK-110.md), без которого
второй пункт DoD не проверить ничем, кроме повторения ручных прогонов дизайна.
Порядок такой: сначала цифра, которой меряется вся серия, потом фундамент,
дальше разрез и его следствия. Раньше фундамента ценность тут не вытащить, и
это надо сказать прямо: и разрез ядра, и хуки дизайн пускает только после
стенда послушания, иначе вынос принимается на глаз.

- DK-110, живой замер резидента headless-прогоном и сверка с расчётом doctor.
  Второй пункт DoD и базовая цифра, по которой видно вклад каждого шага серии.
  Он же ловит несвежую раскладку машины: выкат devkit пересобирает бинари, а
  `agents/`, скиллы и глобальную точку кладёт `doctor --fix`. Backlog,
  зависимостей нет, берётся первой;
- DK-102, `obeycheck`: стенд послушания. Фундамент серии, без него разрез
  принимается на глаз. Самая дорогая задача цели: полный проход это ~200
  headless-сессий, и бюджет цели рискует именно здесь. Backlog, зависимостей
  нет;
- DK-103, ось скиллов в профиле харнеса и глубина правил в генераторе. Backlog,
  зависимостей нет;
- DK-104, разрез ядра: `RULES.core.md` и `RULES.board.core.md`. Тут и появляется
  экономия; той же задачей едет строка про чтение контекста в определения
  `agents/exec-*` и `agents/review-*`. Backlog, после DK-102 и DK-103;
- DK-105, правила скиллами: `board-task`, `board-ship`, `test-standard`.
  Backlog, после DK-104;
- DK-106, хуки вместо прозы: разбор в текстах находок, тесты в pre-commit,
  pre-push на запрет пуша. Backlog, после DK-102;
- DK-107, тонкая глобальная точка правил в doctor и setup. Backlog, после
  DK-104;
- DK-029, порог резидента в doctor. Первый пункт DoD. Развилки задачи закрыл
  дизайн, ранг нарезка пересчитала (8 -> 33): без порога набранное возвращается
  и платить за резидент придётся снова, а это та же серьёзность, что у всей
  серии. Backlog, после DK-105 и DK-107;
- DK-108, `devkitctl stats --context`. DoD не держит и идёт хвостом: эффект
  серии меряет DK-110, а эта задача отвечает на другой вопрос, куда ушёл объём
  сессии. Backlog, зависимостей нет, берётся, если после DK-029 бюджет цели
  остался.

## Журнал

- снимок 2026-08-04T00:43: week_all 6%, week_max 4%
- 2026-08-04, нарезка: состав DK-110, DK-102..DK-107, DK-029, хвостом DK-108;
  дорезана DK-110 (живой замер, второй пункт DoD), DK-029 дополнена порогами
  дизайна и рангом; трактовка замера DoD зафиксирована в DK-110 (разница под
  слепком `~/.claude`, а не пустая директория); бюджет цели рискует на стенде
  DK-102, ~200 headless-сессий на проход; continue
- 2026-08-04, рабочая фаза: DK-110, DK-102, DK-103 закрыты по агентским
  сценариям, выкаты проверены. DK-110 пережила два провала проверки с
  форвард-фиксами (авторизация замера на macOS через связку ключей; импорт
  правил наружу клиент не разворачивает, файлы правил теперь копируются в
  директорию замера). Базовая цифра снята живьём: резидент devkit 15 446
  токенов замером, 14 301 расчётом, коэффициент 2,27. Готовы к взятию DK-104
  (после DK-102, DK-103) и DK-106 (после DK-102); continue
- 2026-08-04, разрез ядра: DK-104 закрыта, выкат проверен. Резидент упал с
  15 446 токенов до 8 353 замером, минус 46%; остаток держит полный
  `RULES.md`, который тянет глобальная точка, её режет DK-107. Стенд прошёл
  полным составом: восемь сценариев из десяти держатся, два красные на обеих
  раскладках (тесты и дока), они закрываются хуками DK-106, а «посмотреть,
  чего ждёт задача» ядро отыграло у полного текста 2/3 против 0/3 на новом
  правиле чтения контекста. Стенд поймал две настоящие дыры разреза
  (ослабленный запрет пуша с отсылкой в отсутствующий файл; выпавшая строка
  про вердикт pick, скилла под неё ещё нет), обе починены в ядре. Живая цена
  прохода: около 3 пунктов week_all на 100 сессий яруса mini, отсюда политика
  k=3 первичным отсевом и выборочный субагентский конец, записана в отличиях
  LLD. Готовы к взятию DK-105, DK-106, DK-107; continue
- 2026-08-04, глобальная точка: DK-107 закрыта по агентскому сценарию, выкат
  проверен. Пачка не набралась, из готовых задач цели проверки ручного отбора
  прошла одна DK-107 (DK-106 неопределённость 2, DK-105 цена L), поэтому она
  ехала одиночкой без поезда. Резидент devkit упал с 8 353 токенов до 2 591
  замером, минус 69%, и впервые ушёл под потолок 6 500: второй пункт DoD цели
  сошёлся ещё до DK-029 и DK-105. Шаг 1 сценария провалился на живой машине,
  находка о рукописной глобальной точке звала команду, которая такой файл не
  трогает; форвард-фикс дал в находке точный `mv` и повтор `--fix`, тесты на
  оба состояния файла дополнены. Прежний рукописный `~/.claude/CLAUDE.md`
  сохранён на машине как `CLAUDE.md.pre-dk107`. Остались DK-105, DK-106,
  DK-029 (после DK-105), хвостом DK-108; continue
- 2026-08-04, виток не начался: headless-сессия упёрлась в разрешения харнеса.
  Ни одна утилита доски не отработала (`taskctl`, `shipctl`, `agentctl`, в том
  числе гейт бюджета), правка файлов и уведомление тоже закрыты, поэтому
  строку витка виток записать не смог и её дописал диспетчер. Тем же упирается
  и оболочка `goal-run.sh`: витки она зовёт голым `claude -p` без флагов
  разрешений. Дальше цикл идёт либо витками из чата, где одобрение даётся по
  ходу, либо после того, как пользователь откроет headless-виткам права;
  wait-human
- снимок 2026-08-04T00:43: week_all 6%, week_max 4%
- снимок 2026-08-04T08:49: week_all 7%, week_max 5%
- снимок 2026-08-04T10:50: week_all 9%, week_max 6%
- снимок 2026-08-04T13:41: week_all 12%, week_max 8%
- снимок 2026-08-04T15:21: week_all 14%, week_max 8%
- снимок 2026-08-04T16:26: week_all 15%, week_max 9%
- снимок 2026-08-04T18:21: week_all 16%, week_max 10%
- 2026-08-04: проверка прав машинного контура по DK-113, headless-сессия
  отработала утилиты доски и запись в журнал сама
- снимок 2026-08-04T18:21: week_all 16%, week_max 10%

## Итог

Пишет последний виток.

## Ход работы

- Грумминг: субагент opus/xhigh по вердикту pick (потолок цели: pro, вердикт и так не выше; квота: week_all 6%, снимок 41м назад, вердикт грумминговый, корректор его не двигает), 2026-08-04.
