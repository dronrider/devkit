# DK-1116: Ярус проверяющего берётся одним кодом, модель для повтора стоит в отказе close

## Что происходит

DK-881: первый прогон сценария поднят на sonnet, разработку тоже вёл sonnet, `taskctl close` отбил, второй субагент поднят на opus. DK-909 (цена XL): прогон DoD на sonnet отбит тем же правилом, повтор на haiku. За неделю два файла задач несут строку «прогон того же яруса закрытие не приняло», а 13 из 77 задач имеют два и больше прогонов сценария.

Пересчитал накопившееся 25 сентября. Записей прогона проверки несут 325 файлов
задач, повторный прогон стоит у 27 из них. Дословный след отказа лежит в
DK-881, DK-1081, DK-1113 и DK-642, а свежий случай это DK-1157: два блока
«проверка после выката» в один день, между ними отказ закрытия. Остальные
повторы след не оставили. Диспетчер поднимает второго субагента и
про отказ не пишет.

Родня записи: DK-741 устраняет тот же отказ с другого конца, сверкой захода вместо
модели, и DK-1165 приносит третий случай вместе с обходом, который живёт
словами в скилле board-task (строка 292, «при совпадении с моделью разработки
ступенью выше»). По коду проверено: ролей у pick две, exec и review
(`tools/agentctl/pick.go`, `validRoles`), а совпадение моделей на роли review
это замысел, `reviewShift` пишет «спуска на ступень нет». Отказ стоит в
`closeVerifyGate` (`tools/taskctl/gate.go`), и модели для повтора в его тексте
нет.

Субагент отрабатывает впустую, приёмку XL закрывает самая дешёвая модель, а её зелёный вердикт через шесть минут разошёлся с чужим проверяющим.

## Чего хотим

Ярус и модель проверяющего определяет один код на оба входа, автономный выкат
по тику и ручной спавн диспетчером. В `internal/checkrun.Choose` ступень уже
поднимается вверх при совпадении, и брать её надо оттуда, а не заводить
третью роль в `pick.go`. Отказ `taskctl close` при совпадении моделей
печатает готовую команду с моделью, взятой той же лестницей. Словесный обход
из скилла board-task снимается. Правило переезжает в код.

## Развилки

- «место правки»: где живёт расчёт яруса проверяющего: новая роль в agentctl pick или общая библиотека checkrun
  - решает: исполнитель
  - рекомендация: переиспользовать internal/checkrun.Choose общей библиотекой, а не заводить третью роль в pick.go: ступень вверх при совпадении там уже лежит, AskTier зовёт pick --role review подпроцессом и досчитывает ступень снаружи, а вторая реализация лестницы разъедется при следующей правке маппинга подписки
  - источник: код, internal/checkrun/checkrun.go:163-191 (Choose) и :348-361 (AskTier), pick.go пакет checkrun не импортирует
- «кейсы»: какие пользовательские кейсы у DK-1116? рекомендую сбалансированный: узкий отклоняю, он оставляет отказ close без подсказки (4) и не отвечает на вопрос записи про XL (3); широкий отклоняю, кейс (2) это ось сверки по заходу, а её человек отклонил решением DK-947 от 12 сентября, и тянуть её сюда значит переоткрывать решённое
  - решает: человек
  - рекомендация: сбалансированный состав, 4 кейса: (1) диспетчер поднимает проверяющего руками после выката и узнаёт ярус до спавна, а не ловит отказ закрытия; (3) приёмка цены XL не закрывается самой дешёвой моделью ради несовпадения имён; (4) проверяющий, поймавший отказ close, читает готовую модель прямо в тексте отказа; (5) автономный выкат по тику и диспетчер руками считают ярус одним и тем же кодом
  - вариант: узкий состав, 2 кейса: (1) диспетчер поднимает проверяющего руками после выката и узнаёт ярус до спавна; (5) автономный выкат по тику и диспетчер руками считают ярус одним и тем же кодом
  - вариант: широкий состав, 5 кейсов: (1) диспетчер поднимает проверяющего руками после выката и узнаёт ярус до спавна; (2) прогон диспетчера после субагента той же модели закрытию не мешает, потому что заходы разные; (3) приёмка цены XL не закрывается самой дешёвой моделью ради несовпадения имён; (4) проверяющий, поймавший отказ close, читает готовую модель прямо в тексте отказа; (5) автономный выкат по тику и диспетчер руками считают ярус одним и тем же кодом
  - решено человеком 2026-09-25: сбалансированный состав, 4 кейса: (1) диспетчер поднимает проверяющего руками после выката и узнаёт ярус до спавна, а не ловит отказ закрытия; (3) приёмка цены XL не закрывается самой дешёвой моделью ради несовпадения имён; (4) проверяющий, поймавший отказ close, читает готовую модель прямо в тексте отказа; (5) автономный выкат по тику и диспетчер руками считают ярус одним и тем же кодом
- «ось сверки»: сверять модель прогона или заход
  - решает: исполнитель
  - рекомендация: ворота закрытия продолжают сверять модель, ось захода в эту запись не едет: кейс (2) снят выбором состава, DK-947 отклонила вариант «отдельной сессией той же модели» 12 сентября, а признак захода до файла задачи не доживает, Lines() печатает модель и время
  - источник: доска, docs/tasks/archive/2026/DK-947.md:70-76 (развилка «независимость»), плюс код internal/stage/stage.go:699-724 (Lines)
- «ярус на дорогой цене»: остаётся ли требование к ярусу проверки при цене L и XL
  - решает: исполнитель
  - рекомендация: отдельного требования по цене L и XL не заводить: Choose ищет ступень только вверх и при исчерпании лестницы отказывает, так что проверяющий дешевле разработки не бывает по построению, а случай DK-909 с повтором на haiku пришёл из ручного спавна мимо Choose
  - источник: код, internal/checkrun/checkrun.go:163-191, ветка Refusal при исчерпании лестницы

## DoD

Ярус и модель проверяющего определяет один код на оба входа. Автономный выкат
по тику и ручной спавн проверяющего диспетчером берут его оттуда же. Отказ
`taskctl close` при совпадении моделей печатает готовую команду с моделью,
взятой той же лестницей подписки. Словесный обход из
`kit/skills/board-task/SKILL.md` снят. Правило переехало в код.

Проверяется четырьмя прогонами на подготовленных записях этапов: расчёт яруса
на задаче, где разработку вёл sonnet, печатает ярус с другой моделью; тот же
расчёт при исчерпанной лестнице печатает отказ; `taskctl close` на совпавших
именах отбивает прогон и несёт в тексте готовую модель; стенд `obeycheck` на
правленом скилле board-task зелёный.

## Ранг

`25+5+1+0+3 = 34`, P2.

- серьёзность 25: проверяющий субагент отрабатывает впустую, диспетчер поднимает
  второго, а обход дешёвый и стоит словами в скилле board-task (строка 292).
  У автономного конвейера этой проблемы нет, ярус там считает checkrun.Choose.
  До полосы 50 «конвейер встаёт на человека там, где обещана автономия»
  запись не дотягивает.
- ценность 5: прогон проверки идёт у каждой задачи, повторный стоит у 27 файлов
  из 325. Это регулярная операция, а не каждый виток. Середина полосы
  4-7, а не 8-10.
- неопределённость 1: подход выбран разбором, место правки названо
  (`internal/checkrun.Choose`), развилки закрыты, границ языка и процесса
  правка не пересекает.
- поправка на баг 0: ворота закрытия работают как задумано, а роли проверки у
  расчёта яруса не было никогда, то есть это нехватка, а не регресс.
- рычаг 3: тема одна, расчёт яруса проверяющего, но стоит она на пути закрытия
  каждой строки, и это больше, чем «своя польза по одной теме» (2).

Условий, посильных только человеку, не нашёл. Искал доступы и секреты, шаг
наружу и согласие на пересмотр правил. Правка живёт в утилитах репозитория,
согласие на ось «ступенью выше» человек дал решением DK-947 от 12 сентября, а
прогон проверяется на подготовленных записях этапов без живого выката.

## Ход работы

- Из черновика DK-1116 «Прогон проверки повторяется ради ворот «другая модель»»: записан 2026-09-21, оформлен 2026-09-25.
- Вычитка: 1 файл, 18 правок, 0 пометок, 2026-09-25.

## Из черновика DK-1165 «Вердикт pick на проверку повторяет модель разработки, close отказывает» (записан 2026-09-25)

### Черновик

#### Ситуация

Проверку сценария после выката прогоняет не автор правки, и ворота закрытия
сверяют модель прогона с моделью последнего этапа кода. Вердикт `agentctl
pick <ID>` для проверяющего этого не учитывает. На DK-1157 он выдал sonnet,
тот же ярус, что вёл разработку, и первый `taskctl close` завершился с
ошибкой.

Обход есть в скилле board-task, раздел «Сценарий проверки». При совпадении
ярус берётся ступенью выше. Проверяющий субагент 25 сентября сделал это
руками, поднял opus и закрыл строку. Правило живёт словами в скилле, а в
вердикте оно не применяется, и каждый проверяющий спотыкается об один и тот
же отказ.

Просится либо ключ роли проверки у pick с учётом модели разработки, либо
подсказка в самом отказе close с готовой моделью.

Проверил по коду сегодня, предмет на месте. Ролей у `pick` две, exec и review
(`tools/agentctl/pick.go`, `validRoles`), роли проверки нет вовсе. Ярус роли
review на цене M и выше нарочно остаётся ярусом исполнителя, `reviewShift` так
и пишет: «спуска на ступень нет», то есть вердикт ревьювера и вердикт
разработки сходятся моделью закономерно, а не случайно. Отказ закрытия стоит в
`closeVerifyGate` (`tools/taskctl/gate.go`), и модели для повтора он не
печатает, только команду `agentctl stage <ID> проверка --by`.

#### Осложнение

#### Вопрос

#### Гипотеза

### Грумминг

- 2026-09-25, отложен: дубль DK-1116 по предмету: роли проверки у pick нет, ворот close отбивает повтор той же моделью. Рядом DK-741 устраняет тот же отказ сверкой захода вместо модели, и что из трёх записей станет задачей, решается вопросом самого DK-1116 про ярус на цене L и XL, а он с прошлого разбора без ответа. Вернуться, когда DK-1116 станет строкой доски. Тогда текст отсюда приписывается к ней командой draft attach
