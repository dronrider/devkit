# DK-112: Цель: дашборд управления агентской разработкой с любого устройства

## Цель

Веб-дашборд на машине, где идёт агентская разработка (ноут, мак мини), с
заходом с любого устройства пользователя. И наблюдение, и полное управление
процессом:

- доска: задачи и цели со статусами, видно, над чем агенты работают прямо
  сейчас;
- запуск задачи или цели в работу и остановка. Пауза на деле это стоп сессии,
  возобновление это новый виток, читающий состояние с диска, и интерфейс
  честно называет её стопом;
- живой статус конкретного агента: журналы витков (.devkit/goal-*.log),
  транскрипты сессий (~/.claude/projects/*.jsonl), tmux-сессии;
- чат с живыми сессиями агентов: читать ответы и писать в идущую сессию.
  Собственные сессии дашборда через Agent SDK в цель не входят;
- правка задачи и цели: заголовок, слагаемые ранга, цена, файл задачи;
- приоритеты бэклога правятся через слагаемые ранга по RANKING.md, порядок
  строк выводится из ранга, произвольного перетаскивания мимо ранга нет;
- зависимости задачи и цели видны наглядно, в обе стороны;
- важные уведомления: стоп агента, wait-human, завершение задачи. Живая лента
  в самом дашборде, канал наружу в пределах выбранного доступа.
- поддержка нескольких проектов на одном дашборде

Решения постановки. Код это компонент devkit: Go-сервер с вшитой статикой,
источник правды это доска и утилиты taskctl/agentctl/shipctl, своей базы у
дашборда нет. Доступ извне через Tailscale: дашборд слушает порт на машине,
HTTPS даёт tailscale serve, сетевой разработки в цели нет. Браузерный вход
через xr-proxy (HTTPS-вход на VPS поверх готового relay LLD-23) отложен
отдельной целью на доске XR, черновик там заведён. Дизайн UI прорабатывается
в Claude Design до кода, UX под телефон и ноутбук.

DoD. Агентская часть: smoke-сценарий через API дашборда на машине: получить
доску со статусами и текущими работами, запустить задачу и остановить её,
отправить сообщение в живую сессию агента и получить ответ, поймать
уведомление о стопе; прогон печатает строку «dashboard smoke: ok».
Пользовательская часть: зайти с телефона через Tailscale, пройти экраны
доски, живого статуса, чата и уведомлений. На пользовательской проверке цикл
штатно останавливается и зовёт человека.

## Бюджет

бюджет: week_all <= 40
бюджет: week_max <= 15

Потолка яруса нет, вердикт pick работает как обычно.

## Задачи цели

Заводит нарезка первым витком.

## Журнал

Строки витков и снимков квоты допишет цикл.

## Итог

Пишется последним витком.

## Ход работы

- 2026-08-04: постановка диалогом. Бюджет 40/15 пп без потолка яруса, код
  компонентом devkit, канал Tailscale (браузерный вход через xr-proxy отложен
  черновиком на доске XR), чат по живым сессиям агентов без собственных
  SDK-сессий.
