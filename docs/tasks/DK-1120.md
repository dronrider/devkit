# DK-1120: Команда и ссылка агента в ленте копируются кнопкой и открываются тапом

## Что происходит

2026-09-22, чат без привязки в дашборде devkit, вход в Claude Design с
телефона. Агент отдал ссылку авторизации блоком кода, как велит скилл chat
для команд. Лента рисует такой блок голым `pre` (`static/app.js`,
`mdRender`). Кнопки копирования у него нет, а адрес внутри остаётся текстом,
автоссылка
`MD_INLINE` в блок не заходит. Кнопка копирования стоит только у пузыря
целиком, у свёрнутых блоков и у команды из tool-вызова. Скопировать один
адрес с телефона не вышло, выделение уползает по всей ленте. Помогло лишь
второе сообщение, где кроме адреса ничего не было, и кнопка пузыря взяла его
одного. Инлайн-код в обратных кавычках копируется так же, никак.

Всё, что агент даёт человеку для рук (команда, адрес, код), на телефоне
не берётся. Вход в сервис с телефона встаёт на первом же шаге, а команду для
`!` приходится перепечатывать. Агент подстраивается под ленту и шлёт адрес
отдельным пустым сообщением, и разговор становится на одну реплику длиннее
на каждую ссылку.

### Что снял разбор

Буфер обмена в том самом заходе работал. Кнопка пузыря берёт текст тем же
`navigator.clipboard.writeText`. Она сработала. Защищённого контекста правке
хватает. Голый http по адресу машины в локальной сети останется без буфера.
Беда эта стара и общая всем кнопкам ленты.

Кнопку в ленте на узком экране ничто не прячет. Правило скрытия до наведения
стоит только у подписи пузыря. Разметка реплики кладётся рядом с подписью.

Ссылка авторизации Claude Design из первого абзаца уезжает на DK-920. Там
вход делается плашкой дашборда. Команды для `!`, произвольные адреса и код в
обычных репликах остаются здесь. Одна строка другую не отменяет.

## Чего хотим

У блока кода в пузыре стоит своя кнопка копирования с тем же откликом, что у
команды tool-вызова. У строчного кода кнопка стоит у команды и у текста
промта, то есть там, где фрагмент уносят в буфер целиком. Короткое упоминание
(путь, флаг, имя функции) кнопки не носит и копируется кликом по себе. Адрес
http и https
внутри блока и внутри обратных кавычек открывается тапом в соседней вкладке,
как в обычном тексте.

## Границы

Построчного взятия из блока кода задача не делает. Кнопка одна на весь блок.

Вход в Claude Design задача не чинит. Ссылка авторизации уезжает на DK-920.

Буфер обмена по голому http остаётся сломанным. Беда эта общая всем кнопкам
ленты, и чинится она своей строкой.

Условий, посильных только человеку, не нашёл. Доступа, секрета и невозвратного
шага правка не просит. Событие ей тоже не нужно.

## Развилки

- «автоссылка»: как адрес внутри кода становится ссылкой?
  - решает: исполнитель
  - рекомендация: узкий проход по https?:// внутри кода по образцу исключения для пути .md, mdInline на содержимое кода не расширять
  - источник: код, tools/dashboard/static/app.js:5437 и стенд tools/dashboard/testdata/poc_mdlinks.mjs:88
- «стенд»: чем правка доказывается?
  - решает: исполнитель
  - рекомендация: кейсы в poc_mdlinks.mjs и poc_panel.mjs, там уже каркас про неприкосновенность кода и мок буфера
  - источник: код, tools/dashboard/testdata/poc_mdlinks.mjs:88 и tools/dashboard/testdata/poc_panel.mjs:1363
- «вёрстка»: где стоит кнопка копирования у блока кода?
  - решает: исполнитель
  - рекомендация: ряд с кнопкой рядом с блоком, как у команды tool-вызова, а не подпись пузыря: подпись прячется до наведения, а на тач наведения нет
  - источник: код, tools/dashboard/static/app.js:6607 и tools/dashboard/static/style.css:2019
- «кейсы»: какие пользовательские кейсы у DK-1120? рекомендую сбалансированный: узкий отрезает (3) и (4), хотя запись прямо говорит, что инлайн-код не копируется так же, как блок, а широкий тянет (5), у которого в коде прецедента нет и который грозит шумом из кнопок
  - решает: человек
  - рекомендация: сбалансированный состав, 4 кейса: (1) агент дал ссылку в блоке кода, человек с телефона открывает её тапом; (2) агент дал команду для знака ! в блоке кода, человек берёт её кнопкой в буфер; (3) агент назвал путь или флаг обратными кавычками, человек берёт его кнопкой; (4) агент дал адрес обратными кавычками, человек открывает его тапом
  - вариант: узкий состав, 2 кейса: (1) агент дал ссылку в блоке кода, человек с телефона открывает её тапом; (2) агент дал команду для знака ! в блоке кода, человек берёт её кнопкой в буфер
  - вариант: широкий состав, 5 кейсов: (1) агент дал ссылку в блоке кода, человек с телефона открывает её тапом; (2) агент дал команду для знака ! в блоке кода, человек берёт её кнопкой в буфер; (3) агент назвал путь или флаг обратными кавычками, человек берёт его кнопкой; (4) агент дал адрес обратными кавычками, человек открывает его тапом; (5) в блоке кода несколько команд, человек берёт одну строку, а не весь блок
  - решено человеком 2026-09-22: сбалансированный состав, 4 кейса: (1) агент дал ссылку в блоке кода, человек с телефона открывает её тапом; (2) агент дал команду для знака ! в блоке кода, человек берёт её кнопкой в буфер; (3) агент назвал путь или флаг обратными кавычками, человек берёт его кнопкой; (4) агент дал адрес обратными кавычками, человек открывает его тапом
- «кнопка блока»: сколько кнопок у блока кода?
  - решает: исполнитель
  - рекомендация: одна кнопка на весь блок кода, построчного взятия нет
  - источник: доска, ответ по развилке «кейсы» от 2026-09-22
- «взятие инлайна»: чем человек берёт инлайн-код в обратных кавычках? рекомендую тап: кейс (3) просит взять путь или флаг одним движением, а кейс (4) уже отдаёт тап адресу, и содержимое кавычек само разводит эти два поведения
  - решает: человек
  - рекомендация: тап по самому инлайн-коду копирует его, отдельной кнопки рядом нет: поток абзаца не рвётся, частокола кнопок в тексте с пятью упоминаниями пути не будет
  - источник: код, tools/dashboard/static/app.js:5443, инлайн-код стоит внутри потока абзаца
  - вариант: кнопка рядом с каждым инлайн-кодом, как у блока: взять можно видно глазом, но абзац с несколькими упоминаниями зарастает кнопками
  - вариант: кнопка только у длинного инлайн-кода, короткий берётся тапом: шума меньше, но порог длины придётся угадывать
  - решено человеком 2026-09-22: кнопка рядом с каждым инлайн-кодом, как у блока: взять можно видно глазом, но абзац с несколькими упоминаниями зарастает кнопками
- «рехёрсал»: poc_panel.mjs красный на чужом блоке (DK-1128), rehearse DK-1120 не проходит: чинить DK-1128 сейчас или ждать?
  - решает: человек
  - рекомендация: починить DK-1128 отдельным заходом и потом повторить rehearse DK-1120, дёшево и расчищает stend для всех задач дашборда
  - вариант: подождать, пока DK-1128 разберут своим чередом, DK-1120 стоит в In progress без отметки обкатки
  - вариант: другое: сузить или подменить сценарий DK-1120, чтобы не звать весь poc_panel.mjs
  - решено человеком 2026-09-24: по рекомендации: сначала починить DK-1128 отдельным заходом, потом повторить rehearse DK-1120
- «разводка инлайна»: чем разводятся фрагменты инлайн-кода с кнопкой и без неё? развилка пересматривает «взятие инлайна» по замечанию приёмки 2026-09-25
  - решает: исполнитель
  - рекомендация: кнопка у фрагмента из нескольких слов и у строки для знака «!», остальное берётся кликом по себе
  - решено исполнителем 2026-09-25: кнопку носит фрагмент, который человек уносит целиком: несколько слов через пробел (команда, текст промта) или строка со знаком «!» впереди. Одно слово (путь, флаг, имя функции) это короткое упоминание, и оно копируется кликом по себе. Разводит форма фрагмента, а не длина: порог длины пришлось бы угадывать, а форма у команды и у упоминания разная всегда

## DoD

- у блока кода в ленте стоит одна кнопка копирования. Отклик у неё тот же, что
  у команды tool-вызова.
- у строчного кода в обратных кавычках кнопка стоит у команды и у текста
  промта. Короткое упоминание кнопки не носит, оно копируется кликом по себе.
- адрес http и https открывается тапом в соседней вкладке. Это работает и в
  блоке кода, и в обратных кавычках. Вид кода остаётся прежним.
- прочее содержимое кода разбор не трогает. Это проверяют кейсы стенда
  `poc_mdlinks.mjs`.
- кнопка и ссылка проверены стендом `poc_panel.mjs` с моком буфера.
- человек взял команду и открыл ссылку с телефона.

## Ранг

`25+6+1+0+2 = 34`, P2.

- серьёзность 25. Трение заметное. Обход есть и он дешёвый: агент шлёт адрес
  отдельной репликой, и кнопка пузыря берёт его одного.
- ценность 6. С телефона не берётся ничего из того, что агент даёт для рук.
  Улучшения ждут на каждом заходе.
- неопределённость 1. Подход выбран. Прецеденты стоят в коде: ряд с кнопкой у
  команды tool-вызова, исключение для пути `.md`.
- поправка на баг 0. Задача добавляет новое поведение ленты.
- рычаг 2. Польза своя и небольшая. Каждый следующий разговор с телефона
  дешевле.

## Приёмка

- вид: mixed
- барьер «глаза»: вид кнопки и попадание пальцем в ленте на телефоне. Наличие
  кнопки и адрес ссылки снимает стенд на DOM, остальное смотрит человек.
  - утверждённый макет: не годится, макета заранее не было, кнопка встроена в
    уже существующую ленту, а не в новый экран;
  - headless-браузер с замером: годится частично, наличие кнопки и адрес
    ссылки снимает стенд на DOM, а живое попадание пальцем он не проверяет;
  - тест по разметке и стилям: годится для отступов и размера кнопки по CSS,
    но не для удобства касания на живом устройстве.

## Ход работы

- Из черновика DK-1120 «Команда и ссылка агента в ленте копируются кнопкой и открываются тапом»: записан 2026-09-22, оформлен 2026-09-22.
- Вычитка: sonnet по скиллу proofread, 2026-09-22.
- Постановка: разбор черновика, сессия startup, 2026-09-22 13:12-21:28.
- Ревью: субагент sonnet/high по определению review-high, работа a4c229ba28b380013, минут 8, 2026-09-22 13:14-13:21.
- Вычитка: субагент sonnet по определению proofread, работа a22a64afc8cd2e367, минут 3, 2026-09-22 16:02-16:05.
- Вычитка: 2 файла, 2 правки, 0 пометок, 2026-09-22.
- Ворота выката (сессия DK-879, поезд DK-879+DK-1120+DK-1132+DK-908+DK-764):
  перебор трёх обходов барьера «глаза» в разделе «Приёмка» отсутствовал,
  раскладка по уже написанной мысли строки барьера, 2026-09-23.
- Разработка: субагент sonnet/high по определению exec-high, работа a472a1d92e0e30c0a, 2026-09-22 21:30-21:51.
- Разработка: субагент sonnet/high по определению exec-high, работа a54f448f3a394f2d3, минут 1, 2026-09-22 21:44-21:45.
- Ждёт человека: вопрос: «рехёрсал»: poc_panel.mjs красный на чужом блоке (DK-1128), rehearse DK-1120 не проходит: чинить DK-1128 сейчас или ждать?, 2026-09-22 21:51-21:51.
- Ждёт человека: блок: вопрос: «рехёрсал»: poc_panel.mjs красный на чужом блоке (DK-1128), rehearse DK-1120 не проходит: чинить DK-1128..., 2026-09-22 21:51-22:07.
- Ждёт очереди: shipctl ship ждёт замок конвейера, 2026-09-22 22:12-13:00.
- Выкат: shipctl ship, поезд: DK-879, DK-1120, DK-1132, DK-908, DK-764, 2026-09-23 13:00-13:00.
- Ждёт человека: проверка после выката, 2026-09-23 13:00-12:13.
- Ждёт человека: вопрос: «рехёрсал»: poc_panel.mjs красный на чужом блоке (DK-1128), rehearse DK-1120 не проходит: чинить DK-1128 сейчас или ждать?, 2026-09-24 12:17-12:17.
- Ждёт человека: блок: вопрос: «рехёрсал»: poc_panel.mjs красный на чужом блоке (DK-1128), rehearse DK-1120 не проходит: чинить DK-1128..., 2026-09-24 12:17-12:18.
- Разработка: субагент sonnet/high по определению exec-high, работа a0b15a5294cdca8f2, 2026-09-24 13:04-13:37.
- Разработка: субагент sonnet/high по определению exec-high, работа abe4aff17f6c44a54, ходов 55, минут 12, 2026-09-24 13:13-13:25.
- Слияние: shipctl merge, 2026-09-24 13:35.
- Слияние: shipctl merge, 2026-09-24 13:36-13:36.
- Слияние: shipctl merge, 2026-09-24 13:36-13:36.
- Ждёт очереди: shipctl merge ждёт замок конвейера, 2026-09-24 13:37-13:50.
- Слияние: shipctl merge, 2026-09-25 13:22-13:35.
- Выкат: shipctl ship, поезд: DK-1121, DK-1120, DK-1160, DK-1158, DK-1157, 2026-09-25 13:50-13:50.
- Ждёт человека: проверка после выката, 2026-09-25 13:50-13:54.
- Проверка: сценарий прогнал opus, 2026-09-25 13:54-16:57.
- Разработка: субагент opus/high по определению exec-high, работа aec47ad52908a79d9, минут 6, 2026-09-25 16:59-17:06.
- Ревью: субагент sonnet/high по определению review-high, работа a72f80dbdacf6c199, ходов 15, минут 4, 2026-09-25 17:06-17:10.
- Слияние: shipctl merge, 2026-09-25 17:10.
- Слияние: shipctl merge, 2026-09-25 17:10-17:17.
- Слияние: shipctl merge, 2026-09-25 17:22-17:33.
- Выкат: shipctl ship, поезд: DK-1120, 2026-09-25 17:34-17:34.

## Ревью

Уровень 3 до 35e2256: круг разработки 2, правка чинит визуальный путь DoD (позиция кнопки блока, центровка строчного кода, общая рамка), которую ранее уже завернула приёмка; горячего пути и critical_paths дифф не касается

- [возврат: правила, 2026-09-24] не блокирует: новые проверки poc_panel.mjs (замечания 2 и 5, строчный код) сверяют только parentNode кнопки и кода внутри .mdicode, а это условие уже выполнялось до правки, поскольку JSDOM не считает layout. Саму правку (background/border ушли с code на группу, vertical-align:-4px сменился на middle) тест не ловит: откат этих двух строк CSS стенд не покраснит. По образцу проверки .svc.fold (poc_panel.mjs:694-699, чтение правила из style.css регуляркой) стоит добавить такую же проверку на правило .md .mdicode: исправлено, poc_panel.mjs читает правило .md .mdicode из style.css регуляркой (по образцу .svc.fold): vertical-align:middle, фон и рамка на группе, у code внутри неё сброшены; кнопка блока проверена на position:absolute
- Вердикт: без замечаний до 18372a4. круг 2 до 18372a47e: дифф f229979cf..HEAD (app.js, style.css, poc_mdlinks.mjs, poc_panel.mjs, файл задачи); замечание приёмки о лишних кнопках у короткого инлайн-кода закрыто (inlineCodeTaken разводит по форме - пробел или знак «!», короткое упоминание берётся кликом через copyInto, клик по ссылке внутри кавычек копирование не запускает); стенды poc_mdlinks.mjs и poc_panel.mjs зелёные, новый тест poc_panel.mjs сверен на красноту против app.js из f229979cf (падает на старом коде); DoD, развилки и сценарий проверки в файле задачи обновлены в согласии с кодом

## Сценарий проверки

Агентская часть, оба стенда разметки ленты:

```sh
cd tools/dashboard && node testdata/poc_mdlinks.mjs static/app.js && node testdata/poc_panel.mjs static/app.js
```

Ожидание: оба стенда заканчиваются без падения. Среди строк вывода второго
стенда стоит «блок кода: кнопка копирования, буфер без разметки ссылок» и
«инлайн-код: кнопка у команды и промта, короткое упоминание берётся кликом».
Наличие кнопки, адрес ссылки и то, что автоссылка не задевает прочий код (ID
задачи, путь `.md`, флаг), проверяет стенд на DOM. Тем же стендом проверено
размещение. Кнопка блока лежит внутри `.mdcode` рядом с `pre`. Отдельной
панели над блоком нет. Кнопка команды лежит внутри той же рамки, что и код. У
короткого упоминания кнопки нет, и клик по нему кладёт фрагмент в буфер, а
клик по ссылке внутри кавычек в буфер не кладёт ничего.

Пользовательская часть. Человек открывает панель дашборда с телефона и
находит в ленте реплику с блоком кода и реплику со строчным кодом в обратных
кавычках. У блока кода кнопка копирования видна без наведения. Кнопка стоит
внутри блока, у правого верхнего угла. Отдельной строки ленты она не
занимает. Тап по ней кладёт команду в буфер. У команды и текста промта в
обратных кавычках своя кнопка в одной рамке с кодом. Короткое упоминание
(путь, флаг) стоит в абзаце без кнопки, и тап по самому фрагменту кладёт его
в буфер. Вертикальный центр фрагмента совпадает с центром строки текста.
Голый адрес http или https внутри блока и внутри кавычек открывается тапом в
соседней вкладке.

## Выкат

- 2026-09-25 слито: aacf171c2, 15b27a64f, 8af2749bb, 9aa38785c
- smoke прогнан, 2026-09-25
- 2026-09-25 слито: 11275da80, a7f2f0418, 3fb65dc50, 6477ad2d3
- smoke прогнан, 2026-09-25

## Проверка

- Пользовательская часть с телефона 2026-09-24 не принята, три замечания, строка возвращена в работу:
  1. Кнопка копирования у блока кода стоит над блоком отдельной строкой. Она визуально оторвана от блока и съедает целую строку ленты. Ожидание: кнопка внутри блока. Кнопка легла внутрь `.mdcode`, поверх правого верхнего угла `pre`. Исправлено.
  2. Фрагменты строчного кода с кнопкой не выровнены по вертикальному центру строки текста, сидят ниже базовой линии. У группы сменён `vertical-align` на `middle`. Исправлено.
  3. Кнопка копирования у строчного фрагмента стоит вне фрагмента, из-за чего фрагмент не выглядит цельным. Ожидание: кнопка внутри фрагмента. Фон и рамка кода переехали на саму группу `.mdicode`, кнопка попала в общий контур. Исправлено.
- 2026-09-24 пользователь делегировал пользовательскую часть проверки агенту сессии: после починки трёх замечаний приёмка с телефона заменяется снимками панели в мобильном окне headless-браузера, глазами их сверяет агент, закрытие по его слову.

- Исключение: тесты (правка везёт тест `tools/dashboard/testdata/poc_panel.mjs`, regcheck подтвердил красноту на старом коде; ворот `shipctl merge` узнаёт тест только по расширению `_test.go`/`_test.py`/`_test.sh`/`_test.rs` и каталогам Rust/JVM, `.mjs` в `testdata/` мимо эвристики, препятствие devkit)

- Обкатка: 2026-09-25 13:49, свежее дерево 80af4cf18a63, сценарий 83de72e8, временный HOME, утилит дерева 11, шагов 1, все зелёные.

Шаг 1 (зелёный):

```console
$ cd tools/dashboard && node testdata/poc_mdlinks.mjs static/app.js && node testdata/poc_panel.mjs static/app.js
poc_mdlinks: ok
окно чатов: адреса, фильтр, дорога реплики, модель в строке отправки, шапка с поиском, отправка обоих видов чата, оптимистичный пузырь, лента, разметка, выделение, хват над полем, черновик, чат из раздела агентов, навигация, память задачи, тихие уведомления
ожидание задачи: без чипа, вопрос с адресом ответа подсказкой строки состояния, поле ввода одно, реплика припаркованной строки уходит ручкой задачи
постановка на форме: один блок описания в каждом режиме, просмотр по умолчанию
боковой журнал: заказ субагенту не двоится пузырём, чужие реплики служебной строкой
кромки ленты: отступ задаёт строка, у карточек своего нет
пузырь: копирование сообщения целиком
блок кода: кнопка копирования, буфер без разметки ссылок
инлайн-код: своя кнопка копирования у каждого упоминания
```

- Агентская часть 2026-09-25 на выкаченном коде, прогон opus: оба стенда
  заканчиваются без падения, в выводе стоят обе ожидаемые строки. Правку
  сверил и с живым дашбордом: статика вшита `go:embed`, бинарь в `~/go/bin`
  собран выкатом поезда в 13:50, внутри лежит и `mdicode` из `app.js`, и
  правило `.md .mdicode` со `vertical-align:middle` из `style.css`, и
  `position:absolute` у кнопки блока. Шаг человека с телефона остаётся ему.

```console
$ cd tools/dashboard && node testdata/poc_mdlinks.mjs static/app.js && node testdata/poc_panel.mjs static/app.js
poc_mdlinks: ok
окно чатов: адреса, фильтр, дорога реплики, модель в строке отправки, шапка с поиском, отправка обоих видов чата, оптимистичный пузырь, лента, разметка, выделение, хват над полем, черновик, чат из раздела агентов, навигация, память задачи, тихие уведомления
ожидание задачи: без чипа, вопрос с адресом ответа подсказкой строки состояния, поле ввода одно, реплика припаркованной строки уходит ручкой задачи
постановка на форме: один блок описания в каждом режиме, просмотр по умолчанию
боковой журнал: заказ субагенту не двоится пузырём, чужие реплики служебной строкой
кромки ленты: отступ задаёт строка, у карточек своего нет
пузырь: копирование сообщения целиком
блок кода: кнопка копирования, буфер без разметки ссылок
инлайн-код: своя кнопка копирования у каждого упоминания
```

- Пользовательская часть 2026-09-25 принята с замечанием, строка возвращена в
  работу: кнопка у короткого инлайн-кода в теле сообщения выглядит избыточно.
  Кнопка уместна, когда фрагмент это команда или текст промта для
  копирования, а короткие упоминания (путь, флаг) она загромождает. Ожидание:
  для таких фрагментов кнопку убрать, копирование делать кликом по самому
  фрагменту. Решение развилки «взятие инлайна» (кнопка у каждого упоминания)
  пересмотреть по этому замечанию.

  Исправлено. Кнопку носит фрагмент, который уносят в буфер целиком, и это
  несколько слов через пробел (команда, текст промта) либо строка со знаком
  «!» впереди. Одно слово (путь, флаг, имя функции) кнопки не носит и
  копируется кликом по себе; клик по ссылке внутри кавычек при этом остаётся
  открытием вкладки. Пересмотр записан развилкой «разводка инлайна» от
  2026-09-25. Фрагменты разводит форма, а не длина, потому что порог длины
  пришлось бы угадывать.

- Агентская часть 2026-09-25 на выкаченном коде (после круга по замечанию),
  прогон opus: оба стенда без падения, строка стенда сменилась на «инлайн-код:
  кнопка у команды и промта, короткое упоминание берётся кликом». Бинарь
  `~/go/bin/dashboard` пересобран выкатом в 17:34, `strings` находит
  `inlineCodeTaken` внутри. Шаг человека с телефона остаётся ему.

```console
$ cd tools/dashboard && node testdata/poc_mdlinks.mjs static/app.js && node testdata/poc_panel.mjs static/app.js
poc_mdlinks: ok
окно чатов: адреса, фильтр, дорога реплики, модель в строке отправки, шапка с поиском, отправка обоих видов чата, оптимистичный пузырь, лента, разметка, выделение, хват над полем, черновик, чат из раздела агентов, навигация, память задачи, тихие уведомления
ожидание задачи: без чипа, вопрос с адресом ответа подсказкой строки состояния, поле ввода одно, реплика припаркованной строки уходит ручкой задачи
постановка на форме: один блок описания в каждом режиме, просмотр по умолчанию
боковой журнал: заказ субагенту не двоится пузырём, чужие реплики служебной строкой
кромки ленты: отступ задаёт строка, у карточек своего нет
пузырь: копирование сообщения целиком
блок кода: кнопка копирования, буфер без разметки ссылок
инлайн-код: кнопка у команды и промта, короткое упоминание берётся кликом
```
