# DK-1121: Команды разбора не делают чат работой строки, кнопка запуска остаётся

## Что происходит

2026-09-22, груминг DK-879 из свободного чата дашборда (chat-5). После `taskctl set DK-879` реестр `~/.devkit/sessions.log` получил строку с источником «работа». Строка доски пришла с полями run: chat, run_busy: true. Вместо кнопки «Выполнить» у строки стоял «Стоп». В перечне touchCmds в tools/taskctl/touch.go работой числятся add, move, close, ask, fail, set, file, review, dep и progress. В комментарии к taskChats в tools/dashboard/chats.go сказано обратное: груминг, привязка рукой и разговор о задаче строку не присваивают. Привязку снял ручкой POST /api/projects/{p}/sessions/{sid}/task с пустой задачей. Поля run у строки после этого пропали. Разбор воспроизвёл предмет на синтетической доске во временном репозитории с подменным HOME. Команда `taskctl set XX-1 --cost M` из сессии харнеса положила в реестр строку «источник работа повод taskctl set XX-1».

Каждый груминг из чата зовёт add и set. Разговор становится работой над строкой. Пока чат жив, у строки «Стоп» вместо запуска, и кнопкой конвейер не поднять. После смерти чата кнопка становится «Продолжить» и ведёт в разговор разбора, исполнителя она не поднимает. Такое случается на каждой задаче, оформленной из чата; оформление с экрана черновика этого не задевает.

## Чего хотим

Задача, оформленная или поправленная из чата, стоит в Backlog с кнопкой «Выполнить», а чат разбора в списке чатов остаётся разговором о задаче без признака работы.

## Границы

Задача не заводит записи «снята» ни на снятых командах, ни на своей команде отвязки. У сужённого перечня привязка не появляется вовсе. В реестре сегодня нет ни одной строки, за которой привязку записали старые чаты разбора. `grep «источник работа» ~/.devkit/sessions.log` пуст на 501 строке с 14 сентября. Разовая чистка не требуется. Задача не трогает подхват строки, уже стоящей в In progress. Повторный `move` отбивается ошибкой «уже в in-progress», а `agentctl` ставит привязку такой сессии своим перечнем.

Условий, посильных только человеку, не нашёл. Доступов, секретов и необратимых шагов наружу работа не просит. Проверка идёт на временном репозитории и на своём же реестре.

## Развилки

- «кейсы»: какие пользовательские кейсы у DK-1121? рекомендую сбалансированный: узкий роняет кейс (4), с которым человек столкнётся руками сразу после выката, а кейс (5) широкого это сам предмет развилки про состав перечня, а не отдельный путь
  - решает: человек
  - рекомендация: сбалансированный состав, 4 кейса: (1) груминг из свободного чата зовёт add --id и set, строка остаётся в Backlog с кнопкой «Выполнить»; (2) исполнитель берёт строку командой move в In progress, привязка ставится, «Стоп» на строке законен; (3) разговор о задаче правит метаданные командами set, file и dep, строка своей не становится; (4) строки, за которыми привязку записали старые чаты разбора, отпускаются без ручки
  - вариант: узкий состав, 2 кейса: (1) груминг из свободного чата зовёт add --id и set, строка остаётся в Backlog с кнопкой «Выполнить»; (2) исполнитель берёт строку командой move в In progress, привязка ставится, «Стоп» на строке законен
  - вариант: широкий состав, 5 кейсов: (1) груминг из свободного чата зовёт add --id и set, строка остаётся в Backlog с кнопкой «Выполнить»; (2) исполнитель берёт строку командой move в In progress, привязка ставится, «Стоп» на строке законен; (3) разговор о задаче правит метаданные командами set, file и dep, строка своей не становится; (4) строки, за которыми привязку записали старые чаты разбора, отпускаются без ручки; (5) сессия, отчитывающаяся одним progress без move, остаётся видна работой
  - решено человеком 2026-09-22: сбалансированный состав, 4 кейса: (1) груминг из свободного чата зовёт add --id и set, строка остаётся в Backlog с кнопкой «Выполнить»; (2) исполнитель берёт строку командой move в In progress, привязка ставится, «Стоп» на строке законен; (3) разговор о задаче правит метаданные командами set, file и dep, строка своей не становится; (4) строки, за которыми привязку записали старые чаты разбора, отпускаются без ручки
- «перечень»: какой состав остаётся работой над строкой?
  - решает: человек
  - рекомендация: сузить до move в In progress, ask, fail и review, убрав add, set, file, dep и progress: ни один живой тест не держится за эти пять имён, а взятие строки всё равно идёт через move
  - вариант: сузить, но оставить progress: отчёт о ходе работы по смыслу ближе к ask и fail, чем к правке метаданных
  - вариант: перечень не трогать, а научить утилиту различать сессию разбора и исполнителя
  - источник: код, tools/taskctl/touch.go:20 и tools/dashboard/registry.go:262, работой строку делает слово источника в реестре
  - решено человеком 2026-09-22: сузить до move в In progress, ask, fail и review, убрав add, set, file, dep и progress: ни один живой тест не держится за эти пять имён, а взятие строки всё равно идёт через move
- «снята»: нужна ли запись «снята» на командах разбора?
  - решает: исполнитель
  - рекомендация: записи снятия не заводить: у сужённого перечня привязка на add --id и set не появляется вовсе, и отвязывать нечего
  - источник: код, internal/sessions/sessions.go:218, запись «снята» гасит уже поставленную привязку
- «отвязка»: заводить ли команду отвязки сессии от строки?
  - решает: исполнитель
  - рекомендация: своей команды не заводить: привязку снимают кнопка «Стоп» и ручка сессии, а строки, за которыми привязку записали старые чаты, разбираются ими же
  - источник: код, tools/dashboard/runchat.go:92, отдельной команды отвязки в taskctl нет
- «подхват»: чем привязывается сессия, подхватившая строку, которая уже стоит в In progress?
  - решает: исполнитель
  - рекомендация: отдельной правки не просит: привязку подхватившей сессии ставит agentctl своим перечнем (pick, run, stage), и сужение taskctl его не задевает
  - источник: код, tools/taskctl/ops.go:555, повторный move в тот же статус отбивается ошибкой

## DoD

- перечень `touchCmds` в `tools/taskctl/touch.go` держит `move` в In progress, `ask`, `fail` и `review`; `add`, `set`, `file`, `dep` и `progress` из него сняты
- прогон на синтетической доске: `taskctl add --id` и `taskctl set` из сессии харнеса реестра не трогают, `taskctl move <ID> in-progress` кладёт запись «работа»
- тест проверяет состав перечня по командам, падает на старом коде и проходит на исправленном
- у строки, оформленной или поправленной из живого чата, дашборд показывает пустой `run` и кнопку «Выполнить»
- решение 8 LLD `docs/lld/DK-430-task-chat.md` получает приписку про состав перечня, комментарии к `touchCmds` и к `taskChats` совпадают

## Ранг

`50+8+1+5+2 = 66`, P1.

- серьёзность 50: конвейер встаёт на человека там, где обещана автономия. «Пока чат жив, у строки «Стоп» вместо запуска, и кнопкой конвейер не поднять», а обход идёт ручкой API мимо кнопки
- ценность 8: пачка становится автономнее, «такое случается на каждой задаче, оформленной из чата»
- неопределённость 1: состав перечня назван ответом человека, правка живёт в одном файле и одном языке
- поправка на баг 5: в комментарии к `taskChats` сказано, что разговор строку не присваивает, а перечень её присваивает
- рычаг 2: ускорение своего потока по одной теме, задача не разблокирует чужой работы

## Ход работы

- Из черновика DK-1121 «Команды груминга не делают чат работой задачи, кнопка запуска остаётся»: записан 2026-09-22, оформлен 2026-09-22.
- Вычитка: 1 файл, 11 правок, 1 пометка, 2026-09-22.
- Постановка: разбор черновика, сессия startup, 2026-09-22 20:38-21:18.
- Вычитка: субагент sonnet по определению proofread, работа acd1e90b45de117b8, минут 4, 2026-09-22 20:58-21:02.

## Ревью

Уровень 3 до 6e158c1: неопределённость 1 из pick, но диффа задел critical_paths=tools/taskctl (два да триажа: доступ к пайплайну автономии и путь DoD фичи), уровень поднят до третьего

- Вердикт: без замечаний до 6e158c1. touchCmds сужен до move/close/ask/fail/review, состав сверен с решёнными развилками кейсы и перечень; TestTouchCmdsComposition и TestGroomingCommandsLeaveRegistryAlone подтверждены regcheck (красный на main, зелёный на дереве); go test ./... в tools/taskctl зелёный; go build tools/dashboard чистый; TestQuotaNoHarnessNamesInCode красный только из-за постороннего бинаря в рабочей копии, не от диффа, после удаления зелёный и на main тоже зелёный; живой сценарий обоих шагов прогнан руками ревьювера на свежесобранном taskctl; комментарии touch.go/chats.go и приписка к решению 8 DK-430-task-chat.md сверены между собой и с кодом; docs/TASKS.md и DK-1120.md в диффе против main это отставание ветки от main, а не правка автора

## Сценарий проверки

Агентский. Каждый шаг гоняет установленный `taskctl` на синтетической доске
во временном доме, строкой без переносов, по образцу сценария DK-1003. Два
шага бьют по двум концам одного разрыва: груминговые команды реестра не
трогают, взятие строки в работу трогает.

Шаг 1. `add --id` и `set` из сессии харнеса реестр не трогают.

```sh
d=$(mktemp -d); export HOME="$d/home"; mkdir -p "$HOME"; root="$d/proj"; mkdir -p "$root"; sess="44444444-4444-4444-8444-444444444444"; unset CLAUDE_CODE_SESSION_ID; export CLAUDE_CODE_SESSION_ID="$sess"; taskctl init -C "$root" --prefix XR --here --name t >/dev/null; taskctl add -C "$root" --id XR-1 --title "из чата" --type task --rank "0+1+1+0+1" --accept agent >/dev/null; taskctl set -C "$root" XR-1 --cost M >/dev/null; log="$HOME/.devkit/sessions.log"; n=$(test -f "$log" && grep -c "сессия $sess" "$log" || echo 0); echo "записей после add и set: $n"; [ "$n" = "0" ]
```

Ожидается «записей после add и set: 0» и зелёный шаг. Груминг из чата не
привязывает сессию к строке, кнопка на дашборде остаётся «Выполнить».

Шаг 2. `move` в In progress реестр трогает как раньше.

```sh
d=$(mktemp -d); export HOME="$d/home"; mkdir -p "$HOME"; root="$d/proj"; mkdir -p "$root"; sess="55555555-5555-4555-8555-555555555555"; unset CLAUDE_CODE_SESSION_ID; export CLAUDE_CODE_SESSION_ID="$sess"; taskctl init -C "$root" --prefix XR --here --name t >/dev/null; taskctl add -C "$root" --id XR-1 --title "из чата" --type task --rank "0+1+1+0+1" --accept agent >/dev/null; taskctl move -C "$root" XR-1 in-progress >/dev/null; log="$HOME/.devkit/sessions.log"; src=$(tail -1 "$log" | grep -c "источник работа"); echo "источник работа: $src"; [ "$src" = "1" ]
```

Ожидается «источник работа: 1» и зелёный шаг. Взятие строки в работу
привязку ставит, «Стоп» на ней законен.

## Из черновика DK-1042 «Оформление черновика делает сессию груминга работой строки» (записан 2026-09-17)

### Черновик

#### Ситуация

17.09.2026 сессия чата доски разобрала черновик DK-1041 и оформила его
командой `taskctl add --id DK-1041 ... --push`. После этого на форме задачи
в дашборде вместо кнопки «Выполнить» стоял «Стоп» (замечание пользователя).
В реестре `~/.devkit/sessions.log` появилась запись сессии по DK-1041 с
источником «работа» и поводом `taskctl add --id`. Команда `add` входит в
`touchCmds` (`tools/taskctl/touch.go`), и оформление строки записалось как
работа над ней. Дашборд по `rowOurRun` показывает «Стоп» строке, чью работу
ведёт живой разговор.

#### Осложнение

Строку, только что заведённую грумингом, с дашборда нельзя запустить, пока
жива сессия груминга, а «Стоп» на ней снял бы разговор с человеком, а не
работу по задаче. Слово «работа» у сессии, которая задачу не делает,
противоречит DK-430.11: живой разговор это третье состояние строки, а не
работа.

#### Вопрос

Считать ли `add` работой вовсе, или только `add` без `--id`. Заведение
строки из черновика это конец разбора, исполнение начинается позже. Нужна ли
сессии команда отвязки руками для таких случаев.

#### Гипотеза

После `taskctl add --id` строка на дашборде показывает «Выполнить», а сессия
груминга остаётся разговором без признака работы.

### Грумминг

- 2026-09-22, отложен: дубль DK-1121: то же место в touchCmds, там же шире вопрос про весь перечень; приписывается к строке DK-1121 после её оформления

## Проверка


- Обкатка: 2026-09-25 13:23, свежее дерево ceb0bd49c779, сценарий d53658be, временный HOME, утилит дерева 11, шагов 2, все зелёные.

Шаг 1 (зелёный):

```console
$ d=$(mktemp -d); export HOME="$d/home"; mkdir -p "$HOME"; root="$d/proj"; mkdir -p "$root"; sess="44444444-4444-4444-8444-444444444444"; unset CLAUDE_CODE_SESSION_ID; export CLAUDE_CODE_SESSION_ID="$sess"; taskctl init -C "$root" --prefix XR --here --name t >/dev/null; taskctl add -C "$root" --id XR-1 --title "из чата" --type task --rank "0+1+1+0+1" --accept agent >/dev/null; taskctl set -C "$root" XR-1 --cost M >/dev/null; log="$HOME/.devkit/sessions.log"; n=$(test -f "$log" && grep -c "сессия $sess" "$log" || echo 0); echo "записей после add и set: $n"; [ "$n" = "0" ]
записей после add и set: 0
```

Шаг 2 (зелёный):

```console
$ d=$(mktemp -d); export HOME="$d/home"; mkdir -p "$HOME"; root="$d/proj"; mkdir -p "$root"; sess="55555555-5555-4555-8555-555555555555"; unset CLAUDE_CODE_SESSION_ID; export CLAUDE_CODE_SESSION_ID="$sess"; taskctl init -C "$root" --prefix XR --here --name t >/dev/null; taskctl add -C "$root" --id XR-1 --title "из чата" --type task --rank "0+1+1+0+1" --accept agent >/dev/null; taskctl move -C "$root" XR-1 in-progress >/dev/null; log="$HOME/.devkit/sessions.log"; src=$(tail -1 "$log" | grep -c "источник работа"); echo "источник работа: $src"; [ "$src" = "1" ]
источник работа: 1
```
