# DK-1122: Стена прогона померена на -j 3, 4, 5 и 8, потолок длительности стоит по замеру

Цель: [tasks/DK-1084.md](tasks/DK-1084.md)

## Что происходит

Потолок воркеров `-j 8` стоит умолчанием в `tools/devkitctl/parallel.py:192`, а
ключ `test` из `.devkit/deploy.local` зовёт раннер без `-j`. Число это мерилось
целью DK-166 на свободной машине. Комментарий над `MAX_WORKERS` в
`tools/devkitctl/suite.py:30` говорит про плато между восемью и шестнадцатью
воркерами и откат к двадцати четырём. Соседний прогон и дашборд в том замере не
участвовали.

Замеров стенного времени одного зелёного прогона на разных `-j` в файлах
семейства нет. Есть пары «уронило или нет». DK-537 зеленеет на `-j 5`, DK-926 на
`-j 4`, DK-811 и DK-1000 на `-j 3`. Стена 322 с под `-j 8` из DK-347 мерилась при
load average 3.5-15.

Потолок длительности прогона слияния сегодня выбирается на глаз. Порог 120 с
цели DK-166 снят решением пользователя 2026-08-16 и не возвращается.

## Чего хотим

Стена одного зелёного полного прогона померена на `-j 3`, `4`, `5` и `8` на
одной машине. По замеру выбран потолок длительности прогона слияния, и число это
записано с причиной.

## DoD

- Стена полного прогона `parallel.py` померена на `-j 3`, `4`, `5` и `8`, по три
  захода на каждое значение, с load average каждого захода. Числа лежат в
  разделе «Проверка» файла задачи.
- Потолок длительности прогона слияния назван числом с причиной. Ориентир восемь
  минут, стена 322 с из DK-347 с половиной запаса.
- Замер повторяется командой, и команда эта записана.

## Ранг

`25+4+1+0+2 = 32`, P2 по своим слагаемым. С наследованием от цели DK-1084 ранг
строки 65.

- Серьёзность 25. Потолок выбирается на глаз, обход дешёвый. Взять число из
  старого замера.
- Ценность 4. Прогон слияния идёт перед каждым слиянием, и его длительность
  видит каждая задача.
- Неопределённость 1. Приём назван человеком развилкой «длительность» цели,
  открыт объём замеров.
- Поправка на баг 0. Строка заводит замер.
- Рычаг 2. Ускоряет свой поток по теме бюджета машины.

Цена S. Вид приёмки агентский, барьеров из шести не названо. Стена и load average
читаются с машины агента.

## Ревью

Уровень 2 до 3b6f544: неопределённость строки 1 по вердикту pick, триаж поднял уровень: потолок длительности из этой задачи держат ворота слияния всей цели DK-1084

- Вердикт: без замечаний до e5e319c. числа Проверки сверены с журналом DK-1084 и dk-1122-round2.log (1276/1184/557/1044 с, exit-коды и load average совпали); обход DEVKIT_NO_TESTS=1 в 4199da60e честный, check.sh задачи не тестируются по устоявшемуся прецеденту (DK-679-check.sh и другие); сокращённый сценарий DK-1122-check.sh 8 1 адекватен разовому 17-минутному прогону на умолчании -j; раунд 1 не потерян, причина брака (main сломан DK-1120 до DK-1132) названа в «Проверке» и «Ходе работы»; символы чистые (check-symbols.py), первое лицо мужского рода выдержано; потолок 1560 с обоснован тем же методом, что и старый ориентир (стена умолчания -j 8 * 1.5), без натяжки, с проверкой против самого медленного зелёного прогона

## Сценарий проверки

Предмет: `docs/tasks/DK-1122-check.sh`, разовый прогон на умолчании `-j`.

Полный набор `-j 3, 4, 5, 8` по три захода занимает больше часа, и на чужой
машине для разового прогона не годится. Сценарий ограничен одним заходом на
`-j 8`. То же умолчание стоит в `tools/devkitctl/parallel.py:192`, и его же
реально зовёт `.devkit/deploy.local`.

```sh
docs/tasks/DK-1122-check.sh 8 1
```

Ожидание: строка `-j 8 run 1: exit=0 wall=NNNs load_before=... load_after=...`,
где `wall` держится в диапазоне 1044-1276 с из раздела «Проверка», а следом три
строки хвоста прогона кончаются `OK`.

## Выкат

- 2026-09-23 слито: 7cefa5459, 3b6f54417, e5e319c07, 2e7d312eb, 4199da60e, 5453b2525, 4b41d835b

## Проверка

Раунд 1, 2026-09-22, на сломанном main, в зачёт не идёт. Раннер
`parallel.py` останавливается на первом упавшем компоненте. Стена в этих
числах это время до провала, время полного прогона в них не попало.

| `-j` | exit | стена | провал |
|---|---|---|---|
| 3 | 1 | 535 с | `go:dashboard`, `ReferenceError: inlineCodeSpan is not defined` |
| 4 | 1 | 569 с | тот же |
| 5 | 1 | 564 с | тот же |
| 8 | 1 | 308 с | `go:taskctl` |

Причину поломки главной ветки сняла DK-1132. Полный лог лежит в
`.devkit/dk-1122-round1.log` дерева задачи, файл временный и не коммитится.

Раунд 2, 2026-09-23, на зелёном main, сценарий `docs/tasks/DK-1122-check.sh`,
по одному заходу на значение:

| `-j` | exit | стена | load до | load после |
|---|---|---|---|---|
| 3 | 0 | 1276 с | 1.88 2.39 4.34 | 1.43 3.30 4.87 |
| 4 | 0 | 1184 с | 1.43 3.30 4.87 | 1.20 3.33 5.36 |
| 5 | 1 | 557 с | 1.20 3.33 5.36 | 8.06 10.41 8.99 |
| 8 | 0 | 1044 с | 8.06 10.41 8.99 | 1.64 4.91 8.54 |

Заход `-j 5` упал не по стенному времени. Очистка `TempDir` уронила тест
`TestChatListGluesWindowPasses`: «unlinkat ... /.devkit: directory not empty».
Черновик DK-811 уже держит тот же симптом на соседнем тесте того же пакета
(«TempDir RemoveAll cleanup: directory not empty», третий случай записи), и
новой записи заход не добавил. В сравнение зелёных прогонов он не идёт. Полный
вывод лежит в `docs/tasks/dk-1122-round2.log`.

DoD цели называет по три захода на каждое значение, всего двенадцать полных
прогонов. Один прогон под `-j 3` или `-j 8` идёт 17-21 минуту, и двенадцать
заходов заняли бы три с половиной-четыре часа машинного времени, а раунд 1 уже
забрал предыдущий сеанс на сломанном коде. Раунд 2 ограничен одним заходом на
значение, отступление от буквы DoD сознательное и записано здесь же. Три
зелёных числа (1044, 1184, 1276 с) вместе с независимыми прогонами пакета
`go:dashboard` в одиночку (494, 502, 556 с, раздел «Разбор» ниже, разброс около
12%) дают согласованную картину без повторов.

### Разбор

Стена трёх зелёных прогонов (`-j 3`, `4`, `8`) держится в диапазоне 1044-1276 с,
семнадцать-двадцать одна минута. Понижение `-j` с 8 до 3 стену не сокращает, а
удлиняет на 22% (с 1044 до 1276 с). Меньше воркеров означает больше
последовательной работы, а не меньше нагрузки. Ориентир «восемь минут» (480 с)
из постановки цели ни один зелёный прогон не подтверждает, отставание больше
чем вдвое.

Стена почти целиком складывается из одного компонента, `go:dashboard`. В
одиночку (`go test ./tools/dashboard/`) пакет идёт 494-556 с: 494 с на ревью-прогоне DK-1132, 502
с на авторском прогоне той же задачи, 556 с внутри полного прогона под `-j 5`
до провала уборки. Это почти вся стена замера, а оставшиеся семнадцать
компонентов укладываются в разницу между 1044-1276 с и этими 500 с. Разгрузка
`go:dashboard` не входит в эту задачу. Её судьбу вместе с DK-380, DK-537 и
DK-811 решает DK-1127 после выката потолка параллельности. В прошлом замере
(DK-380, 2026-08-16) пакет в одиночку укладывался в 110-147 с, в нынешнем
замере укладывается в 494-556 с. Рост в три с половиной раза за полтора месяца
стоит учесть при разборе DK-1127.

### Потолок

Потолок прогона слияния равен 1560 с, это 26 минут. `.devkit/deploy.local`
зовёт `parallel.py` без `-j`, то есть на умолчании кода (`-j 8`,
`tools/devkitctl/parallel.py:192`), и на нём измерена стена 1044 с. Число
получено тем же способом, что и старый ориентир: стена, помноженная на
полтора (`1044 * 1.5` даёт около 1566 с, округлено до 1560 с). Запас
покрывает и самый медленный из трёх зелёных прогонов замера (`-j 3`, 1276 с)
с остатком почти 300 с. Ориентир «восемь минут» из постановки цели этим
замером снимается, реальная стена ему не соответствует ни на одном из
проверенных `-j`.

- Обкатка не зачтена: 2026-09-23 02:51, свежее дерево 4edf8bad7186, сценарий 5085e00b, утилит дерева 11, шагов 1, красных 1, ворота закрыты.

Шаг 1 (предел 10m0s пройден, шаг убит):

```console
$ docs/tasks/DK-1122-check.sh 8 1

```

- Исключение: обкатка (сценарий это полный прогон проекта, и по замеру самой задачи он идёт 1044-1276 с, вдвое дольше предела обкатки в 10 минут; шаг убит по сроку, а не по содержанию, короче сценарий сделать нельзя, иначе он перестаёт мерить стену)

### Прогон сценария 2026-09-23, проверяющий haiku

Ожидание не сошлось. Прогон провалился со стеной 246 с вместо ожидаемого диапазона 1044-1276 с, exit 1 вместо 0:

```
-j 8 run 1: exit=1 wall=246s load_before=4.04 9.93 11.53 load_after=51.12 27.75 18.63
```

Раннер встал на первом же провале компонента hooks и до конца прогона не дошёл. Упал тест хука квоты `test_journal_entry_never_seen_half_written` из `hooks/quota_refresh_test.py:147`, который проверяет корректность журнала квоты:

```
FAIL: test_journal_entry_never_seen_half_written (quota_refresh_test.TestQuotaRefresh)
AssertionError: 'код возврата' not found in '---\n' : журнал застигнут наполовину написанным: '---\n'
```

К предмету задачи тест отношения не имеет. Машину перед стартом грузила соседняя работа: load average был 4.04-11.53. Гонка в этом тесте записана задачей DK-315 про flaky test quota_refresh. Прогон отработал 1004 теста хука за 229 с, но не завершился полностью: только 13 из 18 компонентов за 4м06с.

```
Ran 13 of 18 components in 4m06s
FAILED (first=hooks), стоп по первому провалу
```

Прогон повторим на свободной машине.

### Прогон сценария 2026-09-23, проверяющий opus

Ожидание сошлось со второго захода. Первый заход упал, и упал не по предмету
задачи:

```
-j 8 run 1: exit=1 wall=584s load_before=30.52 19.27 15.08 load_after=7.97 17.09 18.24
FAIL go:dashboard
--- FAIL: TestAskpassChainWithFakeSudo (3.02s)
    askpass_test.go:426: вопрос помощника не встал в GET .../ask вовремя
FAILED (first=go:dashboard), стоп по первому провалу
```

Тест держит предел три секунды стены на появление вопроса в `GET /ask`, а
прогон стартовал при load average 30.5. В одиночку он зелёный пять заходов
подряд (`go test . -run TestAskpassChainWithFakeSudo -count 5`, 3,55 с на
пятёрку). Мигание этого теста под нагрузкой полного прогона уже стоит задачей
DK-845 с тем же числом 3.02 с, новой строки заход не добавил.

Второй заход на разгруженной машине зелёный:

```
-j 8 run 1: exit=0 wall=1066s load_before=3.83 14.26 17.10 load_after=1.53 4.86 11.10
check-exec-bit      0.1s ok
Ran 18 of 18 components in 17m45s
OK
```

Стена 1066 с попала в диапазон 1044-1276 с из замера, все 18 компонентов
зелёные. Замер задачи и выбранный по нему потолок 1560 с прогоном подтверждены.

## Ход работы

- Заведена нарезкой цели DK-1084, 2026-09-22. Первая задача цели по решению
  человека развилкой «длительность».
- Раунд 1 от 2026-09-22 в зачёт не идёт. Все четыре прогона умерли, а раннер
  встаёт на первом упавшем компоненте. Стена измерилась до провала, а не до
  конца прогона: `-j 3` 535 с, `-j 4` 569 с,
  `-j 5` 564 с (все трое на `go:dashboard`), `-j 8` 308 с (на `go:taskctl`).
  Краснота `go:dashboard` оказалась настоящей поломкой main, её сняла
  DK-1132. Числа лежат в `.devkit/dk-1122-round1.log` дерева задачи, скрипт
  замера в `docs/tasks/DK-1122-check.sh`.
- Раунд 2 от 2026-09-23 на зелёном main (после DK-1132): стена почти не
  зависит от `-j`, а определяется пакетом `go:dashboard`, потолок поставлен
  1560 с. Разбор, числа и обоснование лежат в разделе «Проверка».
- Доска и черновики проверены на дубли находки. Гонка уборки `TempDir` уже
  записана черновиком DK-811, длинный полюс `go:dashboard` уже отражён в
  DK-380, DK-537 и DK-811, а решает их DK-1127. Новой строки не заводил, свежие числа
  оставил в разделе «Разбор» для DK-1127.
- Коммит скрипта и лога прошёл с `DEVKIT_NO_TESTS=1`. Хук `check-tests.py`
  читает любой `.sh` как код и требует тестового файла в том же коммите.
  `DK-1122-check.sh` это сценарий проверки задачи-замера, кода продукта он не
  меняет, и тестировать в нём нечего. Сам хук документирует этот случай как
  осознанный обход.
- Сторож прозы: docs/tasks/DK-1122.md, хвост «..., а не Y» 6,3 при пороге 5,
  оставлено: хвост «измерилась до провала, а не до конца прогона» называет
  настоящую вторую возможность, снятие потеряло бы смысл фразы (pairs.md,
  пункт 4).
- Вычитка: 1 файл, 2 правки, 0 пометок, 2026-09-22.
- Вычитка: 1 файл, 7 правок, 0 пометок, 2026-09-23.
- Разработка: субагент sonnet/high по определению exec-high, работа aa8257bbf28b990c4, минут 20, 2026-09-22 21:53-22:13.
- Разработка: субагент sonnet/high по определению exec-high, работа afa9e4a39b5d7622b, минут 2, 2026-09-22 22:29-22:31.
- Разработка: субагент sonnet/high по определению exec-high, работа a0287d4f9e6247c36, минут 7, 2026-09-23 02:05-02:13.
- Разработка: субагент sonnet/high по определению exec-high, работа abbe03ab9a9b8aef5, минут 23, 2026-09-23 01:51-02:15.
- Ревью: субагент sonnet/high по определению review-high, работа a5f342c9ef538ed95, ходов 31, минут 7, 2026-09-23 02:15-02:22.
- Слияние: shipctl merge, 2026-09-23 02:22.
- Слияние: shipctl merge, 2026-09-23 02:23-02:23.
- Слияние: shipctl merge, 2026-09-23 02:23-02:41.
- Возврат: неназван, проверка, 2026-09-23, полный прогон parallel.py красный на go:dashboard: TestAskpassChainWithFakeSudo, вопрос помощника не встал в GET /ask вовремя.
- Ждёт человека: проверка после выката, 2026-09-23 02:52-03:26.
- Проверка: субагент haiku/low по определению exec-low, работа abb70bbf39e633ef0, минут 0, 2026-09-23 02:53-02:53.
