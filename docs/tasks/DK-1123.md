# DK-1123: Потолок параллельности один на прогон и раздан компонентам внутрь

Цель: [tasks/DK-1084.md](tasks/DK-1084.md)

## Что происходит

Потолков параллельности в прогоне слияния три, и общего бюджета у них нет.

Верхний раннер `tools/devkitctl/parallel.py` поднимает компоненты по потоку на
компонент, потолок `-j 8` стоит умолчанием на строке 192. Компонент devkitctl
внутри себя гонит сюиту `tools/devkitctl/suite.py` отдельным подпроцессом, и там
свой потолок `MAX_WORKERS = min(12, os.cpu_count() or 4)` на строке 30.
Go-компоненты зовут `go test` без `-p` и без `GOMAXPROCS`, и пакеты идут по числу
ядер.

Потолки перемножаются. На десяти ядрах 2026-09-20 в 18:53 под
`shipctl merge DK-1081` шли семь-восемь процессов python unittest по 95-100% CPU
каждый при load average 16. Прогон длился больше двенадцати минут. 2026-09-19
около 02:00 под `shipctl merge DK-881 --train` load average вырос с 8 до 78.

Исполнитель после такого отбоя сливает с `-j 3` или `-j 5` руками, в обход
штатной команды.

## Чего хотим

У прогона слияния один бюджет параллельности от числа ядер машины. Верхний
раннер раздаёт доли компонентам внутрь: сюите аргументом, `go test` флагом
`-p`. Переменную `GOMAXPROCS` постановка звала тоже, её сняли замером третьего
круга (разбор в «Ходе работы»). Сумма работающих процессов прогона
укладывается в бюджет при любом составе компонентов.

## Развилки

- «рычаг»: чем снимать перемножение потолков: общим потолком, nice или тем и другим?
  - решает: исполнитель
  - рекомендация: один потолок параллельности от числа ядер в parallel.py, раздаваемый компонентам внутрь: suite.py аргументом, go test флагом -p и GOMAXPROCS через окружение; семафор через границу процессов не нужен, доли раздаёт верхний раннер; множителей три, а не два, go test без -p берёт число ядер

## DoD

- `parallel.py` считает общий бюджет параллельности от числа ядер и раздаёт доли
  компонентам. Число воркеров верхнего уровня и потолки внутри компонентов
  складываются в этот бюджет.
- `suite.py` берёт свой потолок аргументом от раннера, умолчание `MAX_WORKERS`
  остаётся для одиночного запуска.
- Go-компоненты получают `-p` из того же бюджета. Переменную `GOMAXPROCS`
  раннер не ставит: замер 2026-09-23 снял её с раздачи, разбор в «Ходе работы».
- Тест проверяет раздачу долей фактом. Числа в собранных командах компонентов
  сходятся с бюджетом, стенного времени в тесте нет.
- Под полным прогоном число живых процессов прогона укладывается в бюджет, и это
  видно замером.

## Ранг

`50+8+2+0+4 = 64`, P1 по своим слагаемым. С наследованием от цели DK-1084 ранг
строки 65.

- Серьёзность 50. Обещана автономия, а исполнитель сливает с `-j 3` руками, в
  обход штатной команды.
- Ценность 8. Каждая пачка и каждый виток цели становятся дешевле, лишние прогоны
  по пять минут уходят.
- Неопределённость 2. Подход назван развилкой «рычаг», открыт стык трёх процессов
  в двух языках.
- Поправка на баг 0. Строка заводит механику.
- Рычаг 4. Зелёный прогон удешевляет каждое следующее слияние.

Цена M, правка в раннере и в сюите вместе с тестами. Вид приёмки агентский,
барьеров из шести не названо. Число процессов и состав команд читаются с машины
агента.

## Ход работы

- Заведена нарезкой цели DK-1084, 2026-09-22. Развилка «рычаг» перенесена из
  файла цели тем же заходом.
- Вычитка: 1 файл, 3 правки, 0 пометок, 2026-09-22.
- Отработка ревью, 2026-09-23. Доля стала живой: класс `_LiveShare`
  (`tools/devkitctl/parallel.py`) считает её по числу воркеров, ещё не
  исчерпавших очередь, а не статичным расчётом на всю пачку разом. Способ
  честно не решает случай компонента первого залпа. `go:dashboard`, часть
  начальной волны из десяти воркеров, держит статичную долю навсегда: `-p` и
  `GOMAXPROCS` домешать после старта процесса нельзя. Три подряд живых замера
  `parallel.py` без `--list` дали стену 508с/510с/514с при потолке 1560с из
  DK-1122, раздутия стены нет. Все три упали на посторонней гонке TempDir в
  `go:dashboard`, не связанной с этой правкой: она видна и в изолированном
  прогоне тех же тестов, count=8, и под GOMAXPROCS=1, и на умолчании. Заведён
  черновик DK-1136, эту задачу он не держит. Добавлен тест
  `test_explicit_j_flag_reaches_the_share` на связку `args.jobs ->
  component_share` через явный `-j`. Оба замечания ревью закрыты
  `taskctl review resolve`.
- Сторож прозы: docs/tasks/DK-1123.md, средняя длина предложения 28,8 при
  пороге 23, предложений длиннее 25 слов 38,1 при пороге 30, абзац кончается
  обобщением 50,0 при пороге 6 - оставлено: текст замечания ревью и его
  исхода живёт машинной строкой раздела «Ревью» (`taskctl review resolve`),
  ручная перестройка фразы там ломает разбор строки кодом доски (хук
  check-machine-lines, рубеж DK-1058).
- Ревью, круг 2: блокирующее замечание на замер и на go:dashboard, замечание
  на покрытие run_all тестом. Сторож прозы: docs/tasks/DK-1123.md, средняя
  длина предложения 42,7 при пороге 23, предложений длиннее 25 слов 72,7 при
  пороге 30, довод в той же фразе 36,4 при пороге 29 - оставлено: та же
  причина, текст замечания живёт машинной строкой раздела «Ревью»
  (`taskctl review add`), рубеж DK-1058.
- Отработка ревью, круг 3, 2026-09-23. Сперва замер причинности. Пакет
  dashboard в одиночку под `GOMAXPROCS=1` зелёный на свободной машине, 494 с.
  Под двенадцатью счётчиками нагрузки он тоже зелёный, 658 с при load average
  32. Без переменной под той же нагрузкой 653 с. Стену полюса переменная не
  двигает. Под полным прогоном картина обратная. С `GOMAXPROCS=1` провалились
  пять прогонов из пяти, с `GOMAXPROCS=2` шестой. Без переменной зелёными
  прошли и старый код (1178 с), и новая раздача (1320 с). Историческая база без
  переменной это один красный прогон из шести. Тесты в провалах разные, путь
  один, `<имя теста>/001/.devkit: directory not empty`.
- Механику видно в коде дашборда. Список чатов заказывает заголовок фоном.
  Горутина ждёт подпроцесса клиента и пишет файл в дом стенда уже после конца
  тела теста. С одним-двумя полюсами запись попадает в окно уборки
  `t.TempDir`. Формулировка «гонка посторонняя» из записи выше снята, черновик
  DK-1136 переписан числами.
- Раздача долей переделана по тому же замеру. Полюс прогона это сюита
  devkitctl, а `go:dashboard` идёт за ней. Сюите 1161 с при десяти своих
  воркерах и 1205 с при пяти, dashboard 526-586 с. Равная доля по числу лайнов
  свела бы сюиту к последовательному прогону ста пятидесяти семи классов.
  Раннер получает половину бюджета (`runner_share`), вторая половина уходит
  лайнам (`lane_count`, шесть на десяти ядрах). Очередь начинается с раннера
  (`queue_order`). Взятый последним, он удлинил бы стену на всю свою длину.
- `_LiveShare` заменён на `_Budget`. Счёт идёт по компонентам, работающим
  прямо сейчас. Прежняя мерка по воркерам не меняла ничего: воркер объявляет
  очередь пустой ровно тогда, когда стартовать уже некому. `GOMAXPROCS` раннер
  не ставит, DoD поправлен с доводом.
- Краснота показана дважды. `regcheck --base main --tests
  tools/devkitctl/parallel_test.py` говорит «тест краснеет на main и проходит
  на текущем коде». Сверх того связка раздачи обрубалась по одной части
  (`with_share`, `command_env`, `runner_share`, `queue_order`), и каждое
  обрубание валит свой тест.
- Зелёный полный прогон итогового кода на свободной машине. Стена 1295 с при
  потолке 1560 с из DK-1122, 18 компонентов из 18 ok. Процессов прогона,
  жгущих процессор, в пике шесть при бюджете десять. Всего процессов дерева 47,
  и это подпроцессы самих тестов, которых никакой потолок не считает. Тот же
  прогон рядом с чужим полным прогоном взял 1830 с, и это цена соседа. Оба
  замечания круга закрыты `taskctl review resolve`.
- Сторож прозы: docs/tasks/DK-1123.md, средняя длина предложения 20,6 при
  пороге 19, предложений длиннее 25 слов 22,6 при пороге 20 - оставлено:
  сторож считает по всему файлу, а половина его это машинные строки раздела
  «Ревью» (`taskctl review add` и `resolve`), ручная перестройка фразы там
  ломает разбор строки кодом доски (рубеж DK-1058).
- Замечание круга 3: снятие компонента с очереди сведено со счётом доли в один
  шаг `_Budget.pull` под одним замком. Порознь компонент висел в пути: из
  очереди вынут, в счёт работающих не попал, хвоста очереди не занимает.
  Стартующий в эту щель сосед брал долю как единственный на машине. Оба случая
  ревьювера я повторил на старом коде числами: 15 и 12 при бюджете 10. Тесты
  `LiveBudgetTest` гоняют теперь оба порядка и сам замок, краснота показана
  `regcheck --base HEAD`. В обычном порядке доли остались прежние: `pull`
  читает хвост очереди там же, где его читал воркер. Замер стены 1295 с
  поэтому в силе.
- Сторож прозы: docs/tasks/DK-1123.md, средняя длина предложения 143,0 при
  пороге 23, предложений длиннее 25 слов 100,0 при пороге 30, довод в той же
  фразе 100,0 при пороге 48, хвост «..., а не Y» 7,0 при пороге 5, абзац
  кончается обобщением 100,0 при пороге 6 - оставлено: строка уровня ревью
  круга 4 живёт машинной строкой раздела «Ревью» (`taskctl review level`),
  ручная перестройка фразы там ломает разбор строки кодом доски (рубеж
  DK-1058).

## Ревью

Уровень 3 до 3725b6d: круг 4 до 3725b6d: неопределённость и триаж не изменились; замечание 5 закрыто - снятие с очереди и счёт доли сведены в _Budget.pull под одним замком, инвариант держит порядок проверил сам симуляцией (случай O1/O2 и боевой с раннером), лока-регрессию проверил мутацией (вынес dequeue из-под замка на копии файла) - только test_queue_is_drained_under_the_share_lock её ловит, это и есть корректный якорь щели к замку; доли обычного порядка и замер стены 1295с не задеты

- [возврат: реализация, 2026-09-23] блокирует: tools/devkitctl/parallel.py:254-255,114-124 - share = component_share(jobs, budget) считается один раз перед всей пачкой и держится статичным всё время прогона, а не по факту одновременно живых компонентов. На этой машине parallel.py --list уже показывает -p 1 и -j 1 для всех 10 делимых компонентов (jobs по умолчанию равен числу ядер), то есть go:dashboard, известный длинный полюс (494-556 с в одиночку по замеру DK-1122, раунд 2), получит GOMAXPROCS=1 даже в хвосте прогона, когда прочие компоненты уже завершились и 9 из 10 долей бюджета простаивают. Тот же замер DK-1122 показал, что понижение параллельности именно этому пакету стену прогона удлиняет, а не сокращает. DoD строки 'Под полным прогоном число живых процессов прогона укладывается в бюджет, и это видно замером' (docs/tasks/DK-1123.md) не проверен: сценарий проверки гоняет только юниты и --list, ни один процесс не запускается, а отчёт исполнителя не содержит живого замера стены на новом коде. Риск - раздутие стены выше потолка 1560 с, поставленного DK-1122, без единого прогона это обнаружившего. Нужен либо живой замер полного прогона (parallel.py без --list) на новом коде со сверкой с потолком DK-1122, либо пересмотр раздачи так, чтобы доля не душила компонент, оставшийся в очереди последним и работающий фактически один.: исправлено, оба выхода сразу: доля стала живой (класс _LiveShare, tools/devkitctl/parallel.py), считается по числу воркеров, ещё не исчерпавших очередь, а не статичным расчётом на пачку; компонент, дождавшийся опустевшей очереди, получает растущую долю. Честный предел: компонент первого залпа (случай go:dashboard) получает статичную долю навсегда, GOMAXPROCS после старта процесса не домешать. Живой замер: три подряд прогона parallel.py без --list дали стену 508с/510с/514с при потолке 1560с из DK-1122, раздутия стены нет; все три упали на посторонней гонке TempDir в go:dashboard (TestChatListGluesWindowPasses/TestChatListShowsQuotaWait), подтверждённой изолированным прогоном тех же тестов (count=8, зелёный и на GOMAXPROCS=1, и на умолчании) - гонка всплывает от прогона всего пакета, не от доли; DK-1122 раунд 2 её уже ловил без всякого GOMAXPROCS. Заведён черновик DK-1136, эту задачу он не держит
- [возврат: правила, 2026-09-23] не блокирует: tools/devkitctl/parallel_test.py - раздачу доли через CLI (main с явным -j) не проверяет ни один тест: component_share/with_share/command_env закрыты юнитами напрямую, а --list проверен только на умолчании (mock cpu_count). Стоило бы добавить прогон main(['-j', 'N', '--list']) с проверкой, что в jobs и в доле участвует именно переданное значение, а не только бюджет по умолчанию - сама склейка args.jobs -> component_share сейчас без покрытия.: исправлено, добавлен parallel_test.py::MainTest.test_explicit_j_flag_reaches_the_share: main(['-j','3','--only-go','--list']) при mock cpu_count=8 проверяет, что доля 2 (не доля на умолчании 1 и не budget=jobs 8) дошла и до jobs, и до component_share именно через явный -j
- [возврат: реализация, 2026-09-23] блокирует: tools/devkitctl/parallel.py:220-236,297-331 - живой замер (508с/510с/514с) не годится сверкой с потолком 1560с из DK-1122: все три прогона упали, а `secs` в main() считается до возврата run_all(), который срабатывает сразу по stop.set() на первом провале (остальные компоненты убиваются SIGTERM/SIGKILL и в замер не попадают) - это время до провала, а не стена законченного прогона. Ровно эту ошибку сама цель DK-1084 уже поймала и переиграла в замере DK-1122 (Журнал DK-1084, запись 2026-09-22 23:41: "раунд 1 мерил время до первого провала, а не стену зелёного прогона"). Ни одного зелёного полного прогона на новом коде нет, а сценарий проверки (шаг 3, docs/tasks/DK-1123.md) ждёт "OK", не "FAILED". Отдельно: `_LiveShare` не решает случай go:dashboard, который и был предметом первого замечания - `go_tools()` ставит его вторым из девяти модулей по алфавиту, и при workers=jobs=число ядер он неизбежно попадает в первую волну, забирая ту же статичную долю, что и до правки: поведение самого длинного полюса эта правка не меняет ни на бит. Черновик DK-1136 (заведён этим же исполнителем) пишет "похоже, GOMAXPROCS=1 делает эту гонку систематической" про ту же гонку TempDir, которую DK-1122 раунда 2 поймал один раз из четырёх (-j5, до всякой раздачи долей), а три живых прогона нового кода - три раза из трёх; в docs/tasks/DK-1123.md та же гонка названа уверенно "посторонней, не связанной с этой правкой", хотя черновик самого исполнителя эту связь не исключает, а осторожно предполагает. Изолированный прогон двух тестов (TestChatListGluesWindowPasses, TestChatListShowsQuotaWait) я перепроверил сам: `go test -run ... -count=8` зелёный и на умолчании, и под GOMAXPROCS=1 - это подтверждает диагностику черновика (гонка не в этих тестах поодиночке), но не снимает вопрос про полный пакет под низким GOMAXPROCS. Нужен либо валидный зелёный прогон нового кода со сверкой с потолком 1560с, либо честный пересмотр раздачи так, чтобы go:dashboard не держал статичную долю весь хвост прогона - второй путь, названный ещё в первом замечании, для этого компонента фактически не пройден.: исправлено, причинность разобрана замером, раздача переделана. Зелёный полный прогон итогового кода на свободной машине есть: 1295 с при потолке 1560 с из DK-1122, 18 компонентов из 18 ok, процессов прогона, жгущих процессор, в пике шесть при бюджете десять (прежние 508-514 с были временем до провала, замечание принято). Причинность числами: пакет dashboard в одиночку под GOMAXPROCS=1 зелёный и на свободной машине (494 с), и под двенадцатью счётчиками нагрузки (658 с против 653 с без переменной), то есть стену полюса переменная не двигает; под полным прогоном GOMAXPROCS=1 дал пять красных из пяти и GOMAXPROCS=2 шестой, а без переменной зелёными прошли и старый код (1178 с), и новая раздача (1320 с) при исторической базе один красный из шести. Гонка TempDir поэтому не посторонняя, а следствие переменной: фоновая горутина заголовка (titleOrder -> chatStoreWrite в дом стенда) с одним-двумя полюсами ждёт планировщика и попадает в окно уборки. Формулировка в файле задачи и черновик DK-1136 приведены в согласие. Раздача переделана: GOMAXPROCS раннер не ставит вовсе (DoD поправлен с доводом), _LiveShare заменён на _Budget со счётом по работающим компонентам (прежняя мерка по воркерам не меняла ничего: воркер объявляет очередь пустой ровно тогда, когда стартовать уже некому), сюита devkitctl как полюс прогона (1161-1205 с против 526-586 с у dashboard, при доле один это последовательные 157 классов) получила половину бюджета, лайнов стало шесть, и очередь начинается с раннера (queue_order)
- [возврат: правила, 2026-09-23] не блокирует: tools/devkitctl/parallel_test.py - ни один тест не проверяет, что run_all()/worker() реально прокидывает `live.share()` в `with_share`/`command_env` для настоящих go: или devkitctl компонентов. `RunAllTest` гоняет фейковые shell-компоненты (a.sh, fail.sh...), которых `with_share` не трогает по имени, а CLI-тесты (`test_explicit_j_flag_reaches_the_share`, `test_list_shows_go_shares_matching_the_budget`) проверяют только статичный превью-путь `--list` в main(), не сам `run_all()`. `LiveShareTest` проверяет класс `_LiveShare` в изоляции, без интеграции с `worker()`. Регрессия в связке "run_all вызывает live.share()/with_share на каждом заходе цикла" осталась бы незамеченной юнитами. Стоило бы завести в `RunAllTest` фейковый компонент с именем "go:x" и проверить, что его argv/env меняются между первой и второй раздачей внутри одного run_all().: исправлено, добавлен parallel_test.py::RunAllTest.test_share_reaches_go_and_runner_components: run_all гонит настоящий го-компонент (свой go первым в PATH, имя ищется по PATH того самого окружения, что собрал command_env) и компонент-раннер, оба пишут argv и окружение подпроцесса в файл, стенд ждёт у го-компонента -p 4 при пустом GOMAXPROCS и GOWORK=off, у раннера -j 4 (бюджет 8, два лайна: ни доля по умолчанию, ни бюджет целиком на эти числа не похожи). Краснота показана обрубанием with_share, command_env и runner_share поодиночке, каждое валит тест. Рост доли между раздачами внутри одного run_all остался на юнитах _Budget: какой воркер освободится первым решает планировщик ОС, и стенд на потоках был бы флаким (та же причина, что у стенда DK-635)
- [возврат: реализация, 2026-09-23] блокирует: tools/devkitctl/parallel.py:249-283 - _Budget.take() читает `pending.qsize()` вне блокировки live._lock (вызов `live.take(name, pending.qsize())` в worker()), а очередь уменьшается уже в момент pending.get_nowait(), до входа в take(). Между этими двумя строками воркер может быть вытеснен планировщиком ОС (ровно та нагрузка, ради которой затевалась вся раздача), и компонент оказывается «в пути»: не в _running (take() ещё не вызван) и не в waiting (уже вынут из очереди). Другой воркер, добравшийся до live.take() первым, посчитает busy+waiting без него и получит долю больше положенной; когда «застрявший» воркер наконец дойдёт до take(), он получит долю уже с поправкой на соседа, но обратно долю первого не убавить (интерфейс класса это и не предполагает). Проверил на самом классе без единого потока (воспроизводит инвариант, а не гонку потоков): `live=_Budget(10,3,0); live.take("O2",waiting=0)` даёт 10, следующий `live.take("O1",waiting=0)` даёт 5, сумма 15 > бюджета 10; на конфигурации боевой машины (`live=_Budget(10,6,parallel.runner_share(10))`, раннер уже взял свою долю) тот же порядок на хвосте очереди даёт 5+5+2=12 > 10. DoD 'Сумма работающих процессов прогона укладывается в бюджет при любом составе компонентов' (docs/tasks/DK-1123.md) на такой порядок не проверен: LiveBudgetTest прямыми вызовами take/drop гоняет только happy path (арривалы в порядке очереди), а тест на настоящих потоках сам класс отказывается писать ('гонка настоящих потоков это не ловит детерминированно') - это верно для ПОРЯДКА завершения воркеров, но не оправдывает отсутствие теста именно на этот инвариант: он проверяется прямыми вызовами take() в невыгодном порядке, без единого потока, как я и сделал выше. Нужен тест на этот сценарий (take() в порядке, где поздно дошедший до лока воркер видит меньше занятых/ожидающих, чем есть на самом деле) и правка, которая не даёт сумме долей активных компонентов превышать бюджет независимо от порядка вызовов take() - например, читать qsize() внутри той же критической секции, что и остальной учёт, либо явно засчитывать «вынутый из очереди, но ещё не взявший долю» компонент в busy сразу при dequeue.: исправлено, снятие с очереди сведено со счётом доли в один шаг _Budget.pull под тем же замком: qsize воркер больше не читает сам, и компонент между очередью и счётом работающих не висит. Оба случая проверил на старом коде числами: _Budget(10,3,0) даёт 10 и 5 (сумма 15), боевая конфигурация с уже взятым раннером даёт 5, 5 и 2 (сумма 12); на новом коде те же порядки дают 5 и 5, и 5, 2, 2. Тесты parallel_test.py::LiveBudgetTest: test_neighbour_in_flight_holds_the_first_share_down (первый случай), test_tail_order_keeps_the_invariant_under_the_runner (второй, с ушедшим соседом), test_queue_is_drained_under_the_share_lock (щель держит замок, а не удачный порядок вызовов), test_pull_on_the_empty_queue_gives_nothing. Краснота: regcheck -C tools/devkitctl --base HEAD --tests tools/devkitctl/parallel_test.py -- python3 parallel_test.py LiveBudgetTest, класс краснеет на старом коде и проходит на новом. Краснота тут по интерфейсу, а не по арифметике: щель живёт ровно в двухшаговом take(name, waiting), и никакая правка, закрывающая её, старым вызовом не выражается. Доли в обычном порядке не изменились: pull читает хвост очереди там же, где его читал воркер, ожидания прежних юнитов (5,1,1,1,1,1 на полном старте, 1 на хвосте очереди, 5 и 10 на редеющем прогоне) остались как были, и замер стены 1295 с при потолке 1560 с в силе
- Вердикт: без замечаний до 3725b6d. круг 4: замечание 5 проверено - симуляция инварианта на _Budget.pull (случай O1/O2 5+5<=10, боевой с раннером 5+2+2<=9), полный прогон parallel_test.py (48 тестов) зелёный, доли обычного порядка не изменились по диффу тестов, мутацией (dequeue вне замка) подтверждено, что test_queue_is_drained_under_the_share_lock ловит именно эту регрессию - щель пришита к замку, а не к форме кода; исход замечания оформлен по регэкспу taskdoc.go:129; символы и README чистые

## Сценарий проверки

1. Юниты раннера зелёные, включая новые тесты бюджета: `cpu_budget`,
   `component_share`, `with_share`, `command_env` и раздачу долей в выводе
   `--list`. Ждём `OK`.

```sh
cd tools/devkitctl && python3 -m unittest discover -p "parallel_test.py" -q 2>&1 | tail -3
```

2. Перечень компонентов несёт долю общего бюджета. Первой строкой идёт раннер
   `devkitctl` со своей половиной бюджета (`-j 5` на десяти ядрах), у каждого
   го-компонента перед списком пакетов стоит `-p <доля>`. Переменной
   `GOMAXPROCS` в командах нет. Ждём `devkitctl` первой строкой и одинаковое
   число после `-p` у всех го-компонентов.

```sh
python3 tools/devkitctl/parallel.py --list
```

3. Живой замер: полный прогон на живом коде зелёный, а его стена укладывается
   в потолок 1560 с, поставленный замером DK-1122. Машина под прогон нужна
   свободная: соседний полный прогон растягивает стену до 1830 с. Прогон идёт
   20-25 минут. Ждём `OK` и число секунд не больше 1560.

```
time python3 tools/devkitctl/parallel.py
```

4. Замер процессов: пока идёт шаг 3, из второго окна считается, сколько
   процессов прогона жрут процессор. Ждём число не больше числа ядер машины
   (десять на замере 2026-09-23, в пике насчитано шесть).

- Исключение: обкатка (шаг живёт только рядом с живым прогоном шага 3, а без
  него grep находит ноль совпадений и падает; отдельно от прогона шаг не
  проверить)

```sh
ps -Ao pcpu,command | awk '$1 > 50' | grep -c -E "go test|suite.py|unittest"
```

## Выкат

- 2026-09-23 слито: 9df3e0a49, 7af16f3a2, 5bd410ea4, ade00f004, f120bb2bf, b8256c2dd, e5fbcd0f0, c1c080552, 926498646, db48b08ef, 0ccd52303, c73c7f9fe, 099945bf2, f28941f67, 3907b7a45, e52cecece, 825af2296, 2dcbe8941, 0f9b8c95a

## Проверка

- Обкатка не зачтена: 2026-09-24 13:05, свежее дерево 6004a9197637, сценарий da7ff02a, утилит дерева 11, шагов 3, красных 1, ворота закрыты.

Шаг 1 (зелёный):

```console
$ cd tools/devkitctl && python3 -m unittest discover -p "parallel_test.py" -q 2>&1 | tail -3
Ran 55 tests in 15.574s

OK
```

Шаг 2 (зелёный):

```console
$ python3 tools/devkitctl/parallel.py --list
devkitctl        (tools/devkitctl) nice -n 19 /Library/Developer/CommandLineTools/usr/bin/python3 suite.py -j 5
go:agentctl      (tools/agentctl) nice -n 19 go test -count=1 -timeout=20m -p 1 ./...
go:cmdout        (tools/cmdout) nice -n 19 go test -count=1 -timeout=20m -p 1 ./...
go:dashboard     (tools/dashboard) nice -n 19 go test -count=1 -timeout=20m -p 1 ./...
go:obeycheck     (tools/obeycheck) nice -n 19 go test -count=1 -timeout=20m -p 1 ./...
go:regcheck      (tools/regcheck) nice -n 19 go test -count=1 -timeout=20m -p 1 ./...
go:secretctl     (tools/secretctl) nice -n 19 go test -count=1 -timeout=20m -p 1 ./...
go:shipctl       (tools/shipctl) nice -n 19 go test -count=1 -timeout=20m -p 1 ./...
go:taskctl       (tools/taskctl) nice -n 19 go test -count=1 -timeout=20m -p 1 ./...
go:trackctl      (tools/trackctl) nice -n 19 go test -count=1 -timeout=20m -p 1 ./...
hooks            (hooks) nice -n 19 /Library/Developer/CommandLineTools/usr/bin/python3 -m unittest discover -p *_test.py
skills           (kit/skills) nice -n 19 /Library/Developer/CommandLineTools/usr/bin/python3 -m unittest discover -p *_test.py
check-skills     (kit/skills) nice -n 19 /Library/Developer/CommandLineTools/usr/bin/python3 check-skills.py
check-exec-bit   (hooks) nice -n 19 /Library/Developer/CommandLineTools/usr/bin/python3 check-exec-bit.py
board-task       (kit/skills/board-task) nice -n 19 /Library/Developer/CommandLineTools/usr/bin/python3 -m unittest discover -p *_test.py
goal-loop        (kit/skills/goal-loop) nice -n 19 /Library/Developer/CommandLineTools/usr/bin/python3 -m unittest discover -p *_test.py
prose            (kit/skills/prose) nice -n 19 /Library/Developer/CommandLineTools/usr/bin/python3 -m unittest discover -p *_test.py
doctor           (.) nice -n 19 /Library/Developer/CommandLineTools/usr/bin/python3 tools/devkitctl/devkitctl.py doctor --layout
компонентов: 18
```

Шаг 3 (провал: exit status 1):

```console
$ ps -Ao pcpu,command | awk '$1 > 50' | grep -c -E "go test|suite.py|unittest"
0
```
