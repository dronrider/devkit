# DK-1129: Живой чат продолжает ход после обрыва связи посреди ответа

## Что происходит

Харнес Claude Code 2.1.263 при обрыве соединения посреди стрима печатает в чат
«API Error: Connection lost mid-response. The response above may be incomplete.»
и ждёт ввода человека. Замечено 2026-09-22, повторяется в живых сессиях по
несколько раз в день. По официальной доке (errors.md, network-config.md,
hooks.md, справка 2026-09-22) ретраи харнеса покрывают только ошибки до начала
стрима. Обрыв посреди ответа кончает ход с пометкой «неполный». Дописать
обрезанный ответ харнес сам просит только в печатном режиме `-p` и у
субагентов, в живом чате этого нет. Хук `StopFailure` срабатывает на упавший
ход, но решения не возвращает и продолжить ход не может. Это умеет только хук
`Stop` через `"ok": false`, а на API-ошибке срабатывает не он. Ключи среды
`CLAUDE_ENABLE_STREAM_WATCHDOG` и `CLAUDE_STREAM_IDLE_TIMEOUT_MS` двигают только
пороги таймаутов. Чейнджлог 2026 года возобновления не добавлял. В 2.1.227
текст ошибки переименован из «closed» в «lost», в 2.1.222 починены ложные
таймауты на шлюзах.

Закрытая DK-172 (архив) разбирала тот же класс обрывов и остановилась на
громком баннере «ход упал» через `notify.py` на `StopFailure`. Продолжения хода
она не дала. Ключ `CLAUDE_CODE_RETRY_WATCHDOG` в замере разницы не показал.
DK-156 закрывала обрывы витка цели перезапуском сторожком, живой чат вне цикла
она не трогает.

Каждый обрыв ставит работу сессии до ручного «продолжай». Человек замечает
стоп с задержкой в минуты или десятки минут. За это время исполнители и
конвейер стоят, а сессия, поднятая на ночь, доживает до утра ни с чем.
Баннер DK-172 делает стоп заметным, но не снимает ручной шаг.

Внутри харнеса нет рычага, продолжение строится снаружи. Обход 2026-09-22
разложил каналы доставки реплики в сессию. Файловый вход `chat-in.py`
подхватывается только хуком `PostToolUse`, и после упавшего хода нет ходов
инструмента. Этот канал не годится. Годятся два канала дашборда. Сокет
харнеса `/tmp/cc-socks/<pid>.sock` (функция `peerSay`, `tools/dashboard/peers.go`)
будит простаивающую сессию за секунды, включая окно vscode (проба DK-397).
Второй канал это `tmux send-keys` для сессий, поднятых дашбордом в tmux
(`chats.go`, функция `chatSend`, с проверкой живости pty из DK-863). В архиве
нет прямой пробы «сокет будит сессию, стоящую после `StopFailure`». Все
пробы были на штатно простаивающей сессии, стендом это проверяется первым
шагом. Стенд DK-480 (`tools/dashboard/asksay_test.go`, `askroad_test.go`)
уже гоняет доставку в спящую сессию и берётся за образец.

DK-172 не строила ограничителя попыток, её вклад это баннер на `StopFailure`.
Счётчик подряд идущих попыток есть только у оболочки витка цели (DK-169, три
попытки с паузой), и для живого чата он пишется заново. Событие несёт
категорию в поле `error` (`server_error`, `unknown`) и текст в
`last_assistant_message` (образец `hooks/testdata/claude-code/turn-failed.json`).
Харнес не даёт словаря категорий, обрыв связи отличается от кончившегося
кредита разбором текста. Стенд-стаб DK-172 в репозиторий не коммитился и на
диске отсутствует, повторяется по рецепту из архива.

## Чего хотим

После обрыва соединения посреди ответа живая сессия сама продолжает ход с
места обрыва, без ввода человека, а исчерпание попыток кончается громким
уведомлением.

## Границы

Продолжение положено ходу живой сессии, упавшему по обрыву соединения. Ошибки
другой природы (кончившийся кредит, битый запрос, отказ авторизации) получают
прежний баннер DK-172 и ручной шаг. Печатный режим `-p` и субагентов харнес
дописывает сам, они вне задачи. Виток цели переживает обрывы оболочкой DK-169,
и его эта задача не трогает. Число попыток ограничено, исчерпание кончается
громким уведомлением, а не тихим циклом.

## Развилки

- «кейсы»: какие пользовательские кейсы у DK-1129? рекомендую сбалансированный: узкий не различает tmux и vscode, а каналы у них разные (кейс 2); кейсы (5) и (6) широкого это защита от гонки и граница, они уходят в раздел границ и в стенд, а не в кейсы
  - решает: человек
  - рекомендация: сбалансированный состав, 4 кейса: (1) сессия в tmux упала обрывом посреди ответа и сама доводит ход до конца; (2) сессия вне tmux (vscode, терминал человека) получает продолжение сокетом; (3) сеть лежит, попытки кончаются громким уведомлением с их числом; (4) ошибка не про сеть (кредит, битый запрос) продолжения не получает, баннер прежний
  - вариант: узкий состав, 2 кейса: (1) сессия упала обрывом посреди ответа и сама доводит ход до конца; (2) сеть лежит, попытки кончаются громким уведомлением
  - вариант: широкий состав, 6 кейсов: (1) сессия в tmux упала обрывом и сама доводит ход; (2) сессия вне tmux получает продолжение сокетом; (3) сеть лежит, попытки кончаются уведомлением; (4) ошибка не про сеть продолжения не получает; (5) человек успел напечатать своё в ту же секунду, тексты не смешиваются; (6) обрыв на ходе субагента или в печатном режиме -p, где харнес дописывает сам, хук молчит
  - решено человеком 2026-09-22: сбалансированный состав, 4 кейса: (1) сессия в tmux упала обрывом посреди ответа и сама доводит ход до конца; (2) сессия вне tmux (vscode, терминал человека) получает продолжение сокетом; (3) сеть лежит, попытки кончаются громким уведомлением с их числом; (4) ошибка не про сеть (кредит, битый запрос) продолжения не получает, баннер прежний
- «баннер»: гасить ли баннер «ход упал» DK-172 на обрывах, которые хук продолжил сам?
  - решает: человек
  - рекомендация: баннер DK-172 остаётся только на исчерпании попыток, а каждое продолжение пишет строку в notify.log без баннера: шум на каждом обрыве при рабочем продолжении делает баннер фоновым и его перестанут замечать
  - источник: возражение, hooks/README.md раздел «Возобновление после обрыва связи», архив DK-172
  - вариант: баннер на каждом StopFailure, как сейчас, плюс продолжение поверх
  - решено человеком 2026-09-22: баннер DK-172 остаётся только на исчерпании попыток, а каждое продолжение пишет строку в notify.log без баннера: шум на каждом обрыве при рабочем продолжении делает баннер фоновым и его перестанут замечать
- «канал»: каким каналом хук доносит «продолжай» до упавшей сессии?
  - решает: исполнитель
  - рекомендация: сокет /tmp/cc-socks/<pid>.sock через ту же механику, что peerSay дашборда, как единый канал для tmux и vscode; tmux send-keys запасным путём, если стенд покажет, что сокет сессию после StopFailure не будит
  - источник: код, tools/dashboard/peers.go (peerSay), tools/dashboard/chats.go (chatSend), доска DK-397 и DK-480
- «ограничитель»: сколько попыток продолжения и с какой паузой, где живёт счётчик?
  - решает: исполнитель
  - рекомендация: три попытки подряд с паузой между ними по образцу оболочки витка DK-169, счётчик сбрасывается штатным концом хода; больше трёх это ожидание сети, а не обрыв
  - источник: доска, архив DK-169 (счётчик оболочки витка) и DK-172 (ограничителя нет)
- «классификация»: какие тексты ошибки считаются обрывом связи?
  - решает: исполнитель
  - рекомендация: белый список текстов из last_assistant_message: Connection lost mid-response, ENOTFOUND, timed out, 529 Overloaded; всё остальное продолжения не получает, словарь лежит в хуке одним местом и растёт по журналу notify.log
  - источник: код, hooks/testdata/claude-code/turn-failed.json, поля error и last_assistant_message

## DoD

1. Стенд повторяет обрыв посреди стрима (стаб по рецепту DK-172, чистое
   окружение через `env -i`). Он показывает, что после `StopFailure` сессия
   сама получает реплику продолжения и доводит ход до конца без ввода
   человека.
2. Ограничитель попыток стоит и проверен стендом. При лежащей сети попытки
   кончаются, и ход падает громким уведомлением с числом попыток.
3. Ошибка не про обрыв связи не получает продолжения, стенд это показывает на
   образце `server_error` с текстом кредита или битого запроса.
4. Раскладку хука кладёт `devkitctl doctor --fix`, отсутствие это находка
   доктора. В `hooks/README.md` раздел «Возобновление после обрыва связи»
   переписан под новое поведение, с входом для новичка.
5. Работа хука видна снаружи. На каждом продолжении в `~/.devkit/notify.log`
   пишется строка с поводом и номером попытки. Баннер DK-172 на продолженном
   обрыве не показывается, он остаётся исчерпанию попыток (развилка «баннер»).

## Ранг

`25+6+3+0+4 = 38`, P2.

- Серьёзность 25 (Major): обрыв ставит любую живую сессию до ручного
  «продолжай», обход есть и дешёвый, но повторяется по несколько раз в день
  («уже надоела», 2026-09-22).
- Ценность 6: прямая просьба пользователя, сессии на ночь и исполнители
  перестают простаивать до утра.
- Неопределённость 3: подход виден (хук на `StopFailure` плюс канал дашборда),
  но будит ли сокет сессию в состоянии после падения, проверяется только
  стендом, и стенд пишется заново.
- Поправка на баг 0: поведение харнеса, а не регресс devkit.
- Рычаг 4: ускоряет работу агента в каждой сессии, конвейер не стоит из-за
  сети.
- Условий, посильных только человеку, не нашёл: секретов, доступов и
  необратимых шагов наружу у задачи нет, стенд идёт на стабе.

## Ход работы

- Из черновика DK-1129 «Живой чат продолжает ход после «Connection lost mid-response»»: записан 2026-09-22, оформлен 2026-09-22.
- Вычитка: 1 файл, 10 правок, 0 пометок, 2026-09-22.
