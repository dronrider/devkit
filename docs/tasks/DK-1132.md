# DK-1132: Юнит рендера ленты вырезает из статики все нужные функции, go:dashboard зелёный

Цель: [tasks/DK-1084.md](tasks/DK-1084.md)

## Что происходит

Юниты рендера markdown гоняют саму статику. Помощник
`mdSource` в `tools/dashboard/messages_test.go:897` вырезает из `static/app.js`
три константы и восемь функций по именам, склеивает их в `md.mjs` и пускает под
node с игрушечным DOM.

Коммит 766c5b400 (DK-1120) добавил в статику `inlineCodeSpan`, и `mdInline`
теперь зовёт её на каждом куске кода в обратных кавычках. В список вырезки
функция не попала. Собранный модуль падает под node на
`ReferenceError: inlineCodeSpan is not defined`. От этого красные
`TestMarkdownEscapesInjection` и `TestMarkdownBlocks`.

Цена красноты больше самих тестов. Раннер `tools/devkitctl/parallel.py` встаёт
на первом упавшем компоненте. `go:dashboard` в его порядке первый, и прогон
`test` из `.devkit/deploy.local` красный всегда. Слить сейчас нельзя ничего.
Поймал я это замером DK-1122 2026-09-22. Из четырёх полных прогонов на `-j 3`,
`4`, `5` и `8` три умерли на `go:dashboard` через 534, 569 и 563 секунды.
Нагрузка тут ни при чём, `ReferenceError` детерминированный.

Одним именем в списке дело не кончится. `inlineCodeSpan` зовёт `codeLinkify` и
`copyBtn`, а `copyBtn` собирает кнопку через `icon`, `setAttribute`,
`addEventListener`, `classList`, `window.navigator` и `setTimeout`. Игрушечный
DOM теста (`messages_test.go:938`) ничего из этого не умеет, и вырезки одних
имён не хватит.

## Чего хотим

Собранный из статики модуль содержит все функции, которые рендер зовёт на
самом деле, а игрушечный DOM поддерживает кнопку копирования. Юниты ожидают,
что код в обратных кавычках приезжает обёрткой с кнопкой. Компонент
`go:dashboard` зелёный, прогон слияния доходит до конца.

Расхождение списка вырезки с тем, что зовёт рендер, обнаруживается раньше
слияния, по внятному сообщению теста.

## DoD

- `go test ./tools/dashboard/` зелёный, вывод прогона лежит в разделе
  «Проверка» файла задачи.
- Новое имя в статике, не попавшее в список вырезки, валит юнит внятным
  сообщением про несобранный рендер. Это проверяется тестом.
- Краснота сверена `regcheck`: юнит падает на 766c5b400 и проходит на
  исправленном коде.

## Ранг

`75+0+1+5+0 = 81`, P0.

- Серьёзность 75. Слить нельзя ничего. Прогон `test` красный на первом же
  компоненте, а подмена команды тестов остаётся операционной мерой.
- Ценность 0. Починка ради статус-кво, снаружи её никто не заметит.
- Неопределённость 1. Корень найден, место правки очевидно, открыт объём
  игрушечного DOM под кнопку.
- Поправка на баг 5. Это регресс, до 766c5b400 юниты были зелёные.
- Рычаг 0. Остановка доставки уже учтена в серьёзности, а будущей работе
  строка возвращает прежнее состояние, не больше.

Цена S. Вид приёмки агентский, из шести барьеров не назван ни один. Прогон
читается с машины агента.

## Ревью

Уровень 2 до a93e00f: неопределённость 1 (agentctl pick), триаж: правка снимает единственный ныне блокирующий red на пути слияния, критичных путей конфига (tools/shipctl, tools/taskctl, hooks) не задевает

- [возврат: реализация, 2026-09-22] не блокирует: TestMdSourceCutsEveryCalledName ловит вызов bare-именем регэкспом mdCallRe без разбора синтаксиса: вложенное `function foo(...) {` внутри любой из mdSourceFuncs (например добавленное позже в mdRender) тоже совпадёт с шаблоном 'имя(' и попадёт в 'known' не будет, тест закраснеет на легитимной правке статики сообщением про несуществующий пропущенный вызов. Сейчас в перечисленных телах нет ни одной вложенной function-декларации, поэтому по факту не мешает, но это ловушка для следующего, кто добавит приватный хелпер внутрь одной из этих функций.: исправлено, mdCollectKnown кладёт имя вложенной function-декларации в known через mdNestedFuncDeclRe, а не в пропущенные вызовы; тест TestMdCallRegexSeesDeclarationsNotCalls закрепляет разбор

## Выкат

- 2026-09-23 слито: 21fa16825, 518e8cd73, 33d6f8717, 23a6e2fe0, cb0c90e88, d38422ab0

## Проверка

Сценарий проверки. Команда `GOWORK=off go test ./tools/dashboard/` из корня
дерева задачи. Итог проверяется по строке `ok`.

Полный пакет:

```
GOWORK=off go test ./tools/dashboard/
ok  	github.com/dronrider/devkit/tools/dashboard	502.749s
```

Точечный прогон новых и изменённых юнитов:

```
GOWORK=off go test ./tools/dashboard/ -run 'TestMarkdownEscapesInjection|TestMarkdownBlocks|TestMarkdownLinksSafe|TestMdSourceCutsEveryCalledName' -v
--- PASS: TestMarkdownEscapesInjection (0.05s)
--- PASS: TestMarkdownLinksSafe (0.04s)
--- PASS: TestMarkdownBlocks (0.04s)
--- PASS: TestMdSourceCutsEveryCalledName (0.00s)
PASS
```

Сторож `TestMdSourceCutsEveryCalledName` проверен и обратным ходом. Убрал
`mdCodeBlock` из списка вырезки. Тест упал с сообщением «рендер зовёт
mdCodeBlock, а mdSource этого не вырезает из static/app.js, сборка неполна,
под node будет ReferenceError». Строку списка вернул на место.

`regcheck` с базой на самом коммите 766c5b400 остаётся зелёным и на старом
коде тоже. Причина простая. Коммит уже нёс все нужные функции в статике.
Разошлась только вырезка теста, а её при транспланте `regcheck` всегда берёт
текущей. Краснота получена на базе, взятой на родителе этого коммита. Там
кнопки копирования в статике ещё не было.

```
GOWORK=off regcheck -C tools/dashboard --base 766c5b400^ -- go test ./ -run 'TestMarkdownEscapesInjection|TestMarkdownBlocks|TestMdSourceCutsEveryCalledName' -v
тест краснеет на 766c5b400^ и проходит на текущем коде, регрессия закрыта
(тесты: tools/dashboard/messages_test.go, tools/dashboard/testdata/poc_mdlinks.mjs,
tools/dashboard/testdata/poc_panel.mjs)
```

### Прогон сценария 2026-09-23, проверяющий haiku

Точечный прогон новых и изменённых юнитов:

```
sh -c 'cd /Users/rider/projects/devkit/tools/dashboard && GOWORK=off go test ./ -run "TestMarkdown|TestMdSource|TestMdCallRegex" -count=1'
ok  	github.com/dronrider/devkit/tools/dashboard	0.870s
```

Полный прогон компонента:

```
sh -c 'cd /Users/rider/projects/devkit/tools/dashboard && GOWORK=off go test -count=1'
ok  	github.com/dronrider/devkit/tools/dashboard	494.311s
```

Оба прогона завершены успешно. Краснота `ReferenceError: inlineCodeSpan is not defined` отсутствует.

- Обкатка: 2026-09-23 12:06, свежее дерево ff8683d61fbc, сценарий 836bf8bb, временный HOME, утилит дерева 11, шагов 2, все зелёные.

Шаг 1 (зелёный):

```console
$ sh -c 'cd /Users/rider/projects/devkit/tools/dashboard && GOWORK=off go test ./ -run "TestMarkdown|TestMdSource|TestMdCallRegex" -count=1'
ok  	github.com/dronrider/devkit/tools/dashboard	0.842s
```

Шаг 2 (зелёный):

```console
$ sh -c 'cd /Users/rider/projects/devkit/tools/dashboard && GOWORK=off go test -count=1'
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	522.407s
```

## Сценарий проверки

Предмет: `tools/dashboard/messages_test.go`, помощник `mdSource` и сторож
`TestMdSourceCutsEveryCalledName`.

Агентская часть, из основного чекаута после слияния. Первая строка показывает,
что юниты рендера собираются и проходят, вторая что компонент целиком зелёный.

- Исключение: обкатка (сценарий прогнан зелёным дважды за сегодня, отметка
  протухает только от чужих коммитов соседних задач того же поезда. Рубеж
  свежести смотрит на любой файл подряд, а не на предмет задачи, находка
  ушла черновиком devkit)

```sh
sh -c 'cd /Users/rider/projects/devkit/tools/dashboard && GOWORK=off go test ./ -run "TestMarkdown|TestMdSource|TestMdCallRegex" -count=1'
sh -c 'cd /Users/rider/projects/devkit/tools/dashboard && GOWORK=off go test -count=1'
```

Ожидание: обе строки кончаются `ok  github.com/dronrider/devkit/tools/dashboard`.
Красноты `ReferenceError: inlineCodeSpan is not defined` в выводе нет.

## Ход работы

- Заведена витком цели DK-1084 2026-09-22, находка замера DK-1122.
- Вычитка: 1 файл, 10 правок, 0 пометок, 2026-09-22.
- Ворота выката (сессия DK-879, поезд DK-879+DK-1120+DK-1132+DK-908+DK-764):
  второй шаг сценария звал `go test ./tools/dashboard/` из корня devkit, а
  модуль пакета лежит в `tools/dashboard/go.mod`, не в корне, и `go`
  отвечал «cannot find main module». Команда исправлена на `cd
  .../tools/dashboard && go test`, тем же корнем, что у первого шага,
  2026-09-23.
- Разработка: субагент sonnet/high по определению exec-high, работа a4c2609b779a57a19, минут 3, 2026-09-22 23:37-23:41.
- Разработка: субагент sonnet/high по определению exec-high, работа a0346a692de26e7c5, минут 31, 2026-09-22 23:11-23:42.
- Ревью: субагент sonnet/high по определению review-high, работа aaad448686b99491f, ходов 71, минут 12, 2026-09-22 23:44-23:56.
- Слияние: shipctl merge, 2026-09-22 23:56-23:56.
- Слияние: shipctl merge, 2026-09-22 23:56-23:56.
- Слияние: shipctl merge, 2026-09-23 00:02-00:02.
- Слияние: shipctl merge, 2026-09-23 00:02-00:06.
- Слияние: shipctl merge, 2026-09-23 00:06-00:06.
- Слияние: shipctl merge, 2026-09-23 00:07-00:24.
- Разработка: субагент haiku/low по определению exec-low, работа a0eaf350da2b68973, минут 10, 2026-09-23 00:28-00:38.
- Проверка: сценарий прогнал haiku, Прогон сценария: точечный тест 0.870s, полный тест 494.311s, оба прошли успешно без ошибок ReferenceError, 2026-09-23 00:38-02:28.
- Ждёт очереди: shipctl ship ждёт замок конвейера, 2026-09-23 02:28-13:00.
- Выкат: shipctl ship, поезд: DK-879, DK-1120, DK-1132, DK-908, DK-764, 2026-09-23 13:00-13:00.
