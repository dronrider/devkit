# DK-1157: Работа поднимается после смерти сессий, включая перезагрузку машины

## Что происходит

Ноут перезагрузили 24 сентября в 13:51, и с этого момента работа встала целиком.
Службы поднялись сами, обе метки launchd взведены с RunAtLoad. Дашборд работает,
сторожок цикла цели ходит по расписанию. Не поднялась ни одна сессия исполнителя.

Шесть строк в In progress стоят без исполнителей, две из них помечены брошенными.
Цель DK-1084 держит файл надзора со ссылкой на сессию 9b6af57e, которой после
перезагрузки нет, носителем цикла записана оболочка. Сторожок каждый тик пишет
«простой 8ч, звали в 20:16», а подавать реплику некому.

Причина одна на все случаи. Возобновление в devkit привязано к носителю. Состояние доски на подъём не
влияет. Подъём упавшего хода из DK-510 читает повод turn_failed в журнале
уведомлений, находит панель tmux и подаёт в неё реплику. После перезагрузки панели
нет, и ветка мёртвой панели говорит «разговор кончился вместе с ней», попытка даже
не тратится. Страховка брошенного ожидания паркует строку, когда от хода остался
признак ожидания. Пробуждение ждущих поднимает строки из Blocked. Строка в работе
без парковки и без признака ожидания не видна ни одному из трёх путей.

Состава работ никто не хранит. Реестр чатов знает, какая сессия какую задачу вела,
и живость меряет свежестью транскрипта, то есть тем же носителем. Файл надзора есть
только у цели. Ответа на вопрос «что шло до перезагрузки и что из этого надо поднять»
не даёт ни один компонент.

Рядом лежит уборка. Пять замков задач в ~/.devkit держат pid процессов, умерших от
недели назад, самый старый от 11 сентября. Замок мёртвого владельца снимается при
взятии, но строке, которую никто не берёт, это не помогает. Сорок восемь процессов
python3 -c 'while True: pass' остались от нагрузочного шага сценария DK-1135 и висят
сиротами с родителем launchd. Их trap не отработал, когда сняли обёртку. Средняя
загрузка 64, и дашборд каждый круг пишет «опрос доски не поспел за экраном».

Частные дырки этой темы уже записаны черновиками DK-953, DK-841, DK-1006 и задачей
DK-504. Общей механики подъёма нет, и тема просит одного общего решения.

## Чего хотим

## Развилки

- «признак работы»: Какая строка после перезагрузки считается требующей подъёма
  - решает: человек
  - рекомендация: этап строки, потому что In progress держит и ожидающие строки, а этап их уже различает
  - вариант: поднимать строки, чей этап это работа (разработка, слияние, проверка), а ожидающие не трогать
  - вариант: поднимать любую строку In progress без живой сессии
  - вариант: поднимать только те, что были в реестре чатов до перезагрузки
  - решено человеком 2026-09-24: этап строки, потому что In progress держит и ожидающие строки, а этап их уже различает
- «носитель подъёма»: Кто поднимает работу
  - решает: человек
  - рекомендация: тик сторожка плюс команда руками, потому что сторожок уже переживает перезагрузку и ходит по корням
  - вариант: тик goal-watch поднимает сам, та же механика доступна командой
  - вариант: только команда человека, автоматического подъёма нет
  - вариант: отдельная служба launchd, срабатывающая один раз после старта машины
  - решено человеком 2026-09-24: тик сторожка плюс команда руками, потому что сторожок уже переживает перезагрузку и ходит по корням
- «объём подъёма»: Сколько строк поднимать за раз
  - решает: человек
  - рекомендация: по одной через ворота ёмкости слота, потому что шесть сессий разом задавят машину
  - вариант: по одной через ворота ёмкости, как обычный заказ работы
  - вариант: все строки разом
  - вариант: автоматически не поднимать, отдать человеку отчёт со списком
  - решено человеком 2026-09-24: по одной через ворота ёмкости слота, потому что шесть сессий разом задавят машину
- «уборка мусора»: Входит ли уборка следов умерших сессий в ту же механику
  - решает: человек
  - рекомендация: той же командой, потому что после перезагрузки мусор виден в одном месте и живого владельца у него нет
  - вариант: тот же заход снимает замки мёртвых pid, сиротские процессы и деревья прогонов
  - вариант: уборка остаётся находкой devkitctl doctor, подъём её не трогает
  - вариант: уборка отдельной задачей, здесь только подъём
  - решено человеком 2026-09-24: той же командой, потому что после перезагрузки мусор виден в одном месте и живого владельца у него нет

## DoD

Команда подъёма проходит по корням, находит строки в работе без живой сессии и
поднимает их по одной через ворота ёмкости слота. Тот же заход снимает замки
мёртвых владельцев, сиротские процессы прогонов и брошенные деревья.

Тик сторожка зовёт ту же механику, поэтому после перезагрузки машины работа
поднимается без участия человека.

Заход печатает отчёт: что найдено, что поднято, что оставлено и почему. Молчание
без находок отличается от молчания без работы.

Прогон на текущем состоянии доски devkit поднимает вставшие строки и убирает
сорок восемь сиротских процессов от сценария DK-1135.

## Режим POC

Работа начиналась в режиме POC: механика доведена до живого прогона, а тесты,
ревью и дока отложены были до вердикта человека. Вердикт пришёл с просьбой
закрыть найденные дыры, и вместе с ними приехали тесты и ревью. Отложенной
осталась дока.

Что сделано и проверено на живой машине 24 сентября:

- команда `devkitctl lift` обходит корни под надзором, находит строки, чей этап
  означает работу, а живой сессии за ними нет, и поднимает их заказом
  `taskctl run` адресной репликой;
- подъём идёт по одной строке в пределах свободной ёмкости: потолок берётся у
  `agentctl budget`, из него вычитаются строки с живыми сессиями;
- тот же заход снимает замки задач с мёртвым владельцем и процессы нагрузки,
  пережившие родителя;
- тик сторожка зовёт ту же механику пятой работой, после страховки ожидания и
  до закрытия агентских задач;
- ключ `--dry-run` показывает находки, ничего не трогая.

Прогон на состоянии доски после перезагрузки: снято пять замков (старейший от
11 сентября) и сорок восемь процессов нагрузки от сценария DK-1135, средняя
загрузка машины упала с 64 до 2. Подняты DK-1121 на этапе слияния и DK-937 на
проверке.

### Перечень дыр

Дыры первого прогона закрыты по просьбе человека, и на их месте стоят тесты.

- Реплика подъёма стала адресной: она называет строку и запрещает брать другую
  работу с доски. До этого поднятая сессия ушла за чужой ведущейся строкой.
- Выдержка в 15 минут держит повторный подъём, пока сессия заводит транскрипт.
- Журнал подъёма чистится от записей старше суток.
- Образец сиротского процесса накрывает холостой цикл нагрузки на любом питоне
  и прогоны из временных деревьев, обычный питон под образец не идёт.
- Потолок подъёма на машину сквозной по корням, отдельно от потолка корня.
- Уборка деревьев прогона идёт разбором доктора, своего счёта возраста тут нет.

Прогон нашёл ещё одну беду и её же закрыл. Сессия строки в Check гибнет сразу
после старта, заказ при этом уходит успешно, и строка возвращается в находки
следующего захода. Теперь строка поднимается не больше трёх раз подряд, дальше
заход говорит, что она ждёт человека, и больше её не трогает. Счётчик попыток
забывается через сутки.

Что осталось незакрытым:

- Гибель сессии на старте у строк в Check не разобрана. Механика подъёма её
  только перестала усугублять, причина лежит в другом месте.
- Дока не написана, раздела в README нет.

## Сценарий проверки

Первый шаг воспроизводит падение тика, из-за которого сторожок вставал
целиком. Дальше идут находки захода и его сухой прогон.

1. Работа тика отдаёт строки отчёта, а не пару. До починки тут падал
   TypeError, и с ним вставал весь обход корней.

```sh
python3 -c "import sys; sys.path.insert(0, 'tools/devkitctl'); import watch; r = watch.lift_rows('/nonexistent'); assert isinstance(r, list) and all(isinstance(x, str) for x in r), r; print('тик отдал строки:', r)"
```

2. Тесты подъёма и уборки зелёные.

```sh
python3 -m unittest discover -s tools/devkitctl -t tools/devkitctl -p lift_test.py 2>&1 | tail -3
```

3. Сухой прогон команды печатает находки и ничего не трогает.

```sh
python3 tools/devkitctl/devkitctl.py lift --dry-run
```

4. Команда знает свои ключи.

```sh
python3 tools/devkitctl/devkitctl.py lift --help
```

## Ранг

`50+8+2+0+4 = 64`, P1.

## Приёмка

- вид: user
- барьер «глаза»: подъём видно по живым сессиям и по разгруженной машине, и
  судит это человек за своим экраном
  - утверждённый макет: не годится, сравнивать нечего, у отчёта захода нет
    заранее утверждённого эталона
  - headless-браузер с замером: не годится, заход печатает текст в терминал, а
    не рисует экран
  - тест по разметке и стилям: годится для слов отчёта и покрыт тестами, но
    вид поднятых сессий и падение нагрузки машины им не проверить

## Ход работы

- Из черновика DK-1157 «Работа не поднимается после перезагрузки машины»: записан 2026-09-24, оформлен 2026-09-24.
- Разработка: субагент sonnet/high по определению exec-high, работа a1e0fe7b643a4e319, 2026-09-24 20:47-20:48.

## Ревью

Уровень 2 до 48f9ba2: ревью прошло, пять замечаний, четыре блокирующих, все исправлены

- [возврат: реализация, 2026-09-24] блокирует: коммит 046add833/84adf7461 менял watch.lift_rows на lift.lift_root, а тот стал возвращать пару (строки, число поднятых) вместо списка строк. Тик watch.run делал for lline in lift_rows(...): out.write(lline + "\n") без обёртки, и на любом обходе корня это падало TypeError: can only concatenate list to list. Сторожок вставал целиком на каждом тике, не только для lift. Проверено прогоном: python3 -c с Fake-подстановкой board() воспроизвёл TypeError. Правка: вернуть только lines, отбросив число поднятых.: исправлено, исправлено коммитом 37e689022 (watch.py: lines, _ = lift.lift_root(...); return lines), покрыто TickCase.test_watch_work_returns_lines в lift_test.py; watch_test.py 149/149 и lift_test.py 20/20 прогнаны зелёными на HEAD 37e6890
- [возврат: реализация, 2026-09-24] блокирует: образец ORPHAN_PATTERNS для прогонов (lift.py:76) - re.compile(r"(?i)\bgo\s+(test|build)\b.*(/T/(shipctl|taskctl)-\w+|devkit-dk-)") - ловит подстроку «devkit-dk-» где угодно в командной строке, без привязки к временному каталогу. Рабочие деревья задач называются devkit-dk-<ID> (раскладка worktree devkit), и любой go test/go build с путём внутри такого дерева в аргументах (частый случай: go test с абсолютным путём пакета) совпадёт с образцом. Строка станет сиротой при первом же переходе родителя в pid 1 (killed таймаутом обёртки, оборванный foreground) - и уйдёт под kill -9 без разбора, жива она или нет. Сам файл декларирует принцип «список узкий нарочно... широкий образец тут дороже пропущенного мусора», а этот вариант образца ему не следует, в отличие от соседних двух, привязанных к /T/. Проверено: re.search на 'go test github.com/x/devkit-dk-1157/tools' и на 'go test ./... /Users/x/projects/devkit-dk-1157/tools/devkitctl' - оба совпадают. Предложение: сузить альтернативу до пути во временном каталоге прогона (тот же /T/ префикс, что у третьего образца), а не до голой подстроки имени рабочего дерева.: исправлено, образец привязан к каталогу временного прогона, коммит 45e07c72a, покрыто test_pattern_spares_work_tree_build
- [возврат: правила, 2026-09-24] блокирует: stale_trees (lift.py:375) снимает брошенные деревья прогонов через runtrees.abandoned()/drop() - действие необратимое, удаляет каталог и делает git worktree prune. DoD прямо называет деревья прогонов в объёме уборки, а lift_test.py (20 тестов) её не касается вовсе: нет ни мока runtrees, ни проверки dry-run (act=False), ни проверки пути ImportError/Exception. Локи и сиротские процессы той же уборки тестами покрыты (LocksCase, OrphanProcCase), деревья нет. Правка без теста на новый код это незакрытая правка по RULES.core.md. Предложение: тест с моком runtrees (monkeypatch модуля или атрибута) на три исхода stale_trees - есть находки и act=True снимает, act=False только считает, ImportError/исключение runtrees.abandoned не роняет sweep.: исправлено, TreesCase на четыре исхода: снятие, сухой прогон, пусто, отказ разбора
- [возврат: правила, 2026-09-24] блокирует: у команды devkitctl lift (devkitctl.py) нет доки - ни раздела в README, ни примера первого запуска (ключ -C, --dry-run, что печатает отчёт). Правило RULES.core.md «Дока едет вместе с правкой» карve-аута под POC не знает, а формального решения «POC сливается как есть» (DK-459.5) в записи задачи нет - приёмка ещё не пройдена. Файл задачи сам называет пробел в «Что осталось незакрытым», это не находка, а подтверждение. Перед слиянием нужен либо минимальный раздел о команде (что делает, когда её зовёт тик, как проверить дай-раном), либо явное решение человека мерджить POC как есть с доводкой доки отдельным заходом.: исправлено, раздел «Подъём работы после смерти сессий» в README с первым запуском
- [возврат: реализация, 2026-09-24] не блокирует: watch.lift_rows (watch.py:963) зовёт lift.lift_root(root, call=call, taskctl=taskctl) без home, хотя соседние вызовы того же тика (park_stale(..., home=home)) home пробрасывают. Внутри lift_root это уходит в recent()/mark_lifted() с home=None, то есть реально пишет и читает ~/.devkit/lift.log на машине, а не catalog из watch.run(home=...). В проде watch.default_home() и так ~/.devkit, разницы не видно, но в тестах и в любом другом home (второй харнес, песочница) это тихая утечка в настоящий домашний каталог вместо песочницы теста. Предложение: watch.lift_rows(root, call=None, taskctl=None, home=None) и передать home дальше в lift.lift_root.: исправлено, home пробрасывается из тика в lift_root

## Выкат

- 2026-09-24 слито: 0dd399b0b, 4a9364501, 22ba26014, e61c374dc, 722fc25d7, 8cb13f00c, aec5fbbef, 9a38f021e, bd88ffad6

## Проверка

- Обкатка: 2026-09-25 00:02, свежее дерево a057fd4addf0, сценарий 6b7ee869, временный HOME, утилит дерева 11, шагов 4, все зелёные.

Шаг 1 (зелёный):

```console
$ python3 -c "import sys; sys.path.insert(0, 'tools/devkitctl'); import watch; r = watch.lift_rows('/nonexistent'); assert isinstance(r, list) and all(isinstance(x, str) for x in r), r; print('тик отдал строки:', r)"
тик отдал строки: ['корень /nonexistent: доска не прочиталась, taskctl list завершился с кодом 1']
```

Шаг 2 (зелёный):

```console
$ python3 -m unittest discover -s tools/devkitctl -t tools/devkitctl -p lift_test.py 2>&1 | tail -3
Ran 25 tests in 0.053s

OK
```

Шаг 3 (зелёный):

```console
$ python3 tools/devkitctl/devkitctl.py lift --dry-run
корней под надзором нет: поднимать нечего
сиротских процессов нагрузки нет
брошенных деревьев прогона нет
```

Шаг 4 (зелёный):

```console
$ python3 tools/devkitctl/devkitctl.py lift --help
usage: devkitctl lift [-h] [-C DIR] [--dry-run]

optional arguments:
  -h, --help  show this help message and exit
  -C DIR      один корень; без ключа обходятся все корни под надзором
  --dry-run   показать находки, ничего не поднимая и не снимая
```
