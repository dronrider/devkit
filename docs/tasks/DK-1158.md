# DK-1158: Скилл chat называет канал вопроса человеку

## Что происходит

Вопрос человеку печатает `taskctl decide --chat`, и панель узнаёт его по формату
блока. Груминг и постановка зовут скилл `interview`, где сказано вставлять блок
в реплику как есть. Разговор в чате зовёт скилл `chat`, а там про блок нет ни
слова.

На живом разговоре 24 сентября это вышло так. Блок уехал в реплику внутри
код-фенса, панель формат не узнала, и человек увидел текст вместо галочек.
Пришлось объяснять разницу и повторять вопрос второй раз.

Механика тут ни при чём, команда одна на оба входа. Не хватает строки в скилле
`chat`: вопрос человеку идёт блоком `taskctl decide --chat`, блок вставляется
дословно, без ограждения и без переписанного хвоста.

## Чего хотим

## DoD

- Исключение: тесты (правка бескодовая, это пункт скилла и сценарий стенда;
  проверкой тут служит прогон obeycheck, он записан следом в «Проверке»)
- Исключение: regcheck (правка бескодовая, это пункт скилла и сценарий стенда;
  красноту тут меряет не go-тест, а прогон obeycheck против прежней редакции, и
  он записан следом выше: 3/5 против 0/5)

Скилл chat называет канал вопроса человеку и требует вставлять блок дословно.

Стенд послушания проверяет правку по скиллу prompt-test.

## Ревью

Уровень 3 до ca24819: неопределённость 3 по вердикту pick (effort high, tier base), критичных путей не тронуто, но правка формирует ворота merge через prompt-test/obeycheck и меняет живой канал вопроса человеку

- [возврат: реализация, 2026-09-25] блокирует: в судейской секции сценария tools/obeycheck/scenarios/42-chat-decide-block.md строка «да: «умолчание»: Какой формат... «по рекомендации»» (строка 33) начинается с « и кончается на », а unquote() в internal/obey/judge.go (строки 79-84) снимает оба символа как обёртку. Прогон unquote() на этой строке подтверждает: калибровочный пример превращается в «умолчание»: ... «по рекомендации без первой и последней кавычки, это уже не тот текст, что размечен «да». Второй пример (да:2) той же беды избегает, потому что начинается со слова «Спрашиваю.». Поправить: сделать так же в да:1, например добавить вводное слово перед блоком.: исправлено, пример судьи начинается со слова, кавычки-обёртки больше не срезаются
- не блокирует: след стенда держит ровно минимум повторов (k 3), кандидат 2/3 против базы 0/3, а один повтор («rules-with», повтор 2) сам не прошёл судью: агент не пронумеровал варианты и потерял хвост «ответ строкой:». При такой малой выборке вердикт «не хуже» статистически шаткий (точный тест Фишера тут не отличает 2/3 от 0/3). Раз правка чинит живой канал вопроса человеку, стоило бы добрать повторов (k 5 и выше, kit/skills/prompt-test/SKILL.md «Спорную строку добирают повторами») для более уверенного вывода.: исправлено, повторы добраны до пяти, текст пункта усилен, стало 3/5 против 0/5

## Сценарий проверки

1. Скилл разговора называет канал вопроса и требует дословной вставки.

```sh
grep -q "decide <ID> --chat" kit/skills/chat/SKILL.md && grep -q "знак в знак" kit/skills/chat/SKILL.md && echo "канал вопроса назван"
```

2. Сценарий стенда разбирается и привязан к скиллу.

```sh
GOWORK=off go test -C tools/obeycheck -count=1 -run "TestScenario|TestSubject" ./...
```

## Ранг

`25+6+0+5+3 = 39`, P2.

## Ход работы

- Из черновика DK-1158 «Скилл chat не говорит про блок вопроса, и галочки в панели пропадают»: записан 2026-09-24, оформлен 2026-09-25.

## Выкат

- 2026-09-25 слито: 279c28f4c, 1851d0466, 2e83bfd3b, 05606a799, 39771e080, 7bdef796e, cab69d0b8

## Проверка


- Стенд: 2026-09-25 11:19, дерево ca24819dd, предмет 9b80405f, база старый, ярус mini, k 5, ходов 36, вывод 12607, вход 328, кэш 762201, сценарии 42-chat-decide-block, зачтён.

```console
$ obeycheck --task DK-1158 --only 42-chat-decide-block -k 5 --base старый /private/tmp/claude-501/-Users-rider-projects-devkit/01d3faf1-917a-4452-ad0b-c5f2acc25e4a/scratchpad/rules-with /private/tmp/claude-501/-Users-rider-projects-devkit/01d3faf1-917a-4452-ad0b-c5f2acc25e4a/scratchpad/rules-old
сценарий                                rules-with  rules-old  вердикт
вопрос человеку в разговоре без задачи         3/5        0/5  не хуже

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  42-chat-decide-block / rules-with / повтор 3: проверка: exit status 1 (блок вопроса не печатался: taskctl decide --chat не звался)
  42-chat-decide-block / rules-old / повтор 1: проверка: exit status 1 (блок вопроса не печатался: taskctl decide --chat не звался)

разбор судьи:
  42-chat-decide-block / rules-with / повтор 1: Цитата: ``` Какой формат отчёта ставим умолчанием? 1. рекомендую: таблица, её читают глазами 2. таблица 3. json ответ строкой: «имя 1, имя 2», своими словами или «по рекомендации» ``` да
  42-chat-decide-block / rules-with / повтор 2: Цитата из текста: «Какой формат отчёта ставим умолчанием? 1. рекомендую: таблица, её читают глазами 2. таблица 3. json ответ строкой: «имя 1, имя 2», своими словами или «по рекомендации»» Блок содержит дословный вопрос, три пронумерованные варианта, хвост «ответ строкой:» и не обёрнут в тройные обра...
  42-chat-decide-block / rules-with / повтор 4: Вот место в тексте, которое соответствует критерию: ``` Какой формат отчёта ставим умолчанием? 1. рекомендую: таблица, её читают глазами 2. таблица 3. json ответ строкой: «имя 1, имя 2», своими словами или «по рекомендации» ``` Это блок вопроса с дословным текстом, пронумерованными вариантами (1, 2,...

# символы разбора судьи и заметок приведены к клавиатурным, дословный ответ судьи лежит в каталоге прогона (judge/answers) при --keep или своём --work, а без них каталог прогона временный и сносится вместе с ответом
```

- Обкатка: 2026-09-25 11:58, свежее дерево defdb5196346, сценарий 329c756c, временный HOME, утилит дерева 11, шагов 2, все зелёные.

Шаг 1 (зелёный):

```console
$ grep -q "decide <ID> --chat" kit/skills/chat/SKILL.md && grep -q "знак в знак" kit/skills/chat/SKILL.md && echo "канал вопроса назван"
канал вопроса назван
```

Шаг 2 (зелёный):

```console
$ GOWORK=off go test -C tools/obeycheck -count=1 -run "TestScenario|TestSubject" ./...
ok  	github.com/dronrider/devkit/tools/obeycheck	0.675s
```
