# DK-1158: Скилл chat называет канал вопроса человеку

## Что происходит

Вопрос человеку печатает `taskctl decide --chat`, и панель узнаёт его по формату
блока. Груминг и постановка зовут скилл `interview`, где сказано вставлять блок
в реплику как есть. Разговор в чате зовёт скилл `chat`, а там про блок нет ни
слова.

На живом разговоре 24 сентября это вышло так. Блок уехал в реплику внутри
код-фенса, панель формат не узнала, и человек увидел текст вместо галочек.
Пришлось объяснять разницу и повторять вопрос второй раз.

Механика тут ни при чём, команда одна на оба входа. Не хватает строки в скилле
`chat`: вопрос человеку идёт блоком `taskctl decide --chat`, блок вставляется
дословно, без ограждения и без переписанного хвоста.

## Чего хотим

## DoD

Скилл chat называет канал вопроса человеку и требует вставлять блок дословно.

Стенд послушания проверяет правку по скиллу prompt-test.

## Сценарий проверки

1. Скилл разговора называет канал вопроса и требует дословной вставки.

```sh
grep -q "decide <ID> --chat" kit/skills/chat/SKILL.md && grep -q "дословно" kit/skills/chat/SKILL.md && echo "канал вопроса назван"
```

2. Сценарий стенда разбирается и привязан к скиллу.

```sh
GOWORK=off go test -C tools/obeycheck -count=1 -run "TestScenario|TestSubject" ./...
```

## Ранг

`25+6+0+5+3 = 39`, P2.

## Ход работы

- Из черновика DK-1158 «Скилл chat не говорит про блок вопроса, и галочки в панели пропадают»: записан 2026-09-24, оформлен 2026-09-25.

## Проверка

- Стенд: 2026-09-25 10:57, дерево 5d41a7370, предмет e5ab0894, база старый, ярус mini, k 3, ходов 30, вывод 12259, вход 271, кэш 659713, сценарии 42-chat-decide-block, зачтён.

```console
$ obeycheck --task DK-1158 --only 42-chat-decide-block -k 3 --base старый /private/tmp/claude-501/-Users-rider-projects-devkit/01d3faf1-917a-4452-ad0b-c5f2acc25e4a/scratchpad/rules-with /private/tmp/claude-501/-Users-rider-projects-devkit/01d3faf1-917a-4452-ad0b-c5f2acc25e4a/scratchpad/rules-old
сценарий                                rules-with  rules-old  вердикт
вопрос человеку в разговоре без задачи         2/3        0/3  не хуже

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  42-chat-decide-block / rules-with / повтор 2: судья: нет
  42-chat-decide-block / rules-old / повтор 1: проверка: exit status 1 (блок вопроса не печатался: taskctl decide --chat не звался)

разбор судьи:
  42-chat-decide-block / rules-with / повтор 1: **Цитата:** ``` «умолчание»: Какой формат отчёта ставим умолчанием? 1. рекомендую: таблица, её читают глазами 2. таблица 3. json ответ строкой: «имя 1, имя 2», своими словами или «по рекомендации» ``` **Анализ:** -  Блок вопроса присутствует дословно -  Пронумерованные варианты (1, 2, 3) -  Хвост...
  42-chat-decide-block / rules-with / повтор 2: В тексте есть вопрос: **"Какой формат ставим умолчанием: **таблица** или **json**?"** Однако этот вопрос не соответствует критерию, так как: - Варианты ответа не пронумерованы (просто перечислены) - Отсутствует хвост «ответ строкой:» нет
  42-chat-decide-block / rules-with / повтор 3: Цитирую релевантную часть текста: ``` «умолчание»: Какой формат отчёта ставим умолчанием? 1. рекомендую: таблица, её читают глазами 2. таблица 3. json ответ строкой: «имя 1, имя 2», своими словами или «по рекомендации» ``` В этом фрагменте присутствуют: - Блок вопроса дословно  - Пронумерованные ва...

# символы разбора судьи и заметок приведены к клавиатурным, дословный ответ судьи лежит в каталоге прогона (judge/answers) при --keep или своём --work, а без них каталог прогона временный и сносится вместе с ответом
```
