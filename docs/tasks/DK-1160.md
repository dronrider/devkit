# DK-1160: Цикл цели поднимается после смерти своего носителя

## Что происходит

Подъём строк доски после смерти сессий закрыт задачей DK-1157, а цель осталась
в стороне. У цели свой носитель. Файл надзора в ~/.devkit/goals держит поля
carrier и session. После перезагрузки 24 сентября там осталась запись
carrier = shell и сессия 9b6af57e, которой на машине нет.

Сторожок этих полей не читает вовсе. Он меряет одно, время последнего движения
цикла, и при простое дольше порога зовёт человека. Текст зова говорит «цикл
стоит 22ч» и в обоих случаях один и тот же.

А случаев два, и они разные. Цикл мог встать по вердикту витка wait-human, то
есть сам позвал человека и ждёт ответа. Мог и осиротеть: оболочка, которая
вела виток, умерла вместе с машиной, и продолжать некому. Первый случай зова и
просит, второму нужен подъём.

Разбор в tools/devkitctl/README.md запрещает поднимать цикл вслепую, и доводы
там верные. Оболочка со своими тремя попытками стоп уже не пережила, живой чат
оболочкой не продолжается, а чужие сессии за деньги квоты без спроса не
запускают. Все три довода про осознанный стоп. Про мёртвый носитель там нет
ничего.

## Чего хотим

Сторожок читает носителя и маркер записи надзора. Зов человеку называет, что
именно случилось с циклом.

Цикл, потерявший носителя и не имеющий маркера стопа, поднимается заново
оболочкой. Цикл с маркером wait-human не поднимается: он ждёт человека, и зов
об этом так и говорит.

## DoD

Отчёт сторожка и текст зова называют состояние носителя цели. Видно, ждёт ли
цикл человека или остался без носителя.

Цель с мёртвым носителем и без маркера стопа поднимается оболочкой цикла, с
потолком попыток и выдержкой, как у задач.

Цель с маркером wait-human подъёмом не трогается.

Тест ловит оба случая и красен на старом коде.

## Ревью

Уровень 3 до 49b4cde: неопределённость 2 (agentctl pick), триаж критичности два «да»: правка меняет путь, от которого зависит DoD (различение стопа и смерти носителя), и трогает деньги (raise_goal тратит квоту без ожидания)

- [возврат: реализация, 2026-09-25] блокирует: на потолке попыток (raise_goal, tools/devkitctl/watch.py, ветка tries > GOAL_TRIES) сторожок не зовёт человека вовсе: строка «дальше ждёт человека» уходит только в отчёт тика (out.write), а shout()/hooks/notify.py не вызывается ни разу. У аналогичного потолка resume_failed() на этот случай явно зовёт shout(«ход упал: подъём не помог», ...). Тест test_tries_have_a_ceiling это не ловит: проверяет отсутствие вызова goal-run.py, но не проверяет call.argv_with("notify.py"), в отличие от соседнего test_stop_marker_is_left_to_the_human. Без правки осиротевший цикл после трёх неудачных попыток молча остаётся без хозяина.: исправлено, на потолке попыток идёт зов человеку, покрыто test_ceiling_still_shouts
- [возврат: реализация, 2026-09-25] блокирует: raise_goal (tools/devkitctl/watch.py) запускает оболочку через call = subprocess.run(...): в проде call не передаётся (watch.run(idle=...) в devkitctl.py вызывается без call), значит идёт настоящий subprocess.run, а он ждёт завершения дочернего процесса. goal-run.py это долгий цикл витков (RETRY_LIMIT, sleep между попытками), и watch-тик зависнет на весь его срок вместо мгновенного возврата: следующие записи реестра (другие цели, другие корни) в этом тике не проверятся, checkrun и опрос тредов ревью тоже встанут. start_new_session=True тут не спасает от ожидания, он только про группу процессов. В этом же файле для несинхронного запуска уже есть свой приём: subprocess.Popen(...) без .wait()/.communicate() (см. parallel.py:393 и сам watch.py:363 для сравнения). Нужен Popen без ожидания, а не run().: исправлено, запуск через Popen без ожидания, тик не встаёт
- [возврат: реализация, 2026-09-25] блокирует: поле raised записи надзора (tools/devkitctl/watch.py, raise_goal) нигде не сбрасывается. Ветка gap < idle в look() чистит entry «stopped» и «shouts» при возврате движения, но не «raised»; ветка tries > GOAL_TRIES тоже не трогает запись. DoD задачи обещает потолок «как у задач», а у аналогичного счётчика (resume_failed, docstring в watch.py: «Снимает счёт любое событие сессии, кроме падения») сброс явный. Тут его нет: стоит циклу осиротеть три раза за всю жизнь, хоть с месяцами здоровой работы между случаями, и дальше сторожок эту цель больше никогда не поднимет, сразу отдавая потолок. Нужен сброс raised там же, где сейчас чистятся stopped/shouts.: исправлено, движение снимает поле raised, покрыто test_movement_clears_the_raise_count
- [возврат: правила, 2026-09-25] блокирует: правка меняет прежнее осознанное решение, разобранное в tools/devkitctl/README.md, а сам README не тронут диффом (git diff main..HEAD --stat правки не показывает). Абзацы по-прежнему говорят, что «цикл цели он не поднимает и после стопа» (район строки 1699) и что «продолжение цикла сторожок на себя не берёт» с четырьмя доводами (район строки 1360), хотя raise_goal теперь именно это и делает для осиротевшего носителя. Три из четырёх доводов (три попытки оболочки, живой чат оболочкой не продолжается, деньги квоты без спроса) докстрока raise_goal разбирает, а четвёртый («сторожок работает и там, где цикла быть не должно: autonomous снят, цель на паузе») не упомянут нигде ни в коде, ни в комментарии, ни в README. Дока едет вместе с правкой: нужно переписать оба абзаца, объяснив новое различение стоп-маркер/мёртвый носитель и почему доводы относятся только к первому случаю.: исправлено, README переписан: два случая, потолок, флаг автономии
- [возврат: реализация, 2026-09-25] не блокирует: raise_goal не проверяет флаг autonomous перед запуском, полагаясь на то, что goal-run.py сам откажет в preflight (autonomous_flag_set), но вывод оболочки уходит в DEVNULL, а код возврата не читается (call без ожидания), так что при выключенном autonomous сторожок всё равно рапортует «цикл поднят оболочкой» и тратит попытку впустую, вслепую. Дешёвая проверка autonomous прямо в raise_goal (как это уже читает deploy.local сам run()) избавила бы от ложного успеха и не требовала бы дожидаться дочернего процесса.: исправлено, подъём идёт только при autonomous = true, покрыто test_no_autonomy_means_no_raise
- [возврат: правила, 2026-09-25] не блокирует: GoalCarrierTest не покрывает пустое/незнакомое поле session (entry без session или с session, которого нет в реестре сессий), оба явно разобранных в carrier_state веток («носитель не записан» и путь, где session_alive не находит запись). Сейчас оба случая проверены только по чтению кода. Стоит добавить пару строк-тестов, чтобы будущая правка carrier_state не перевернула эти безопасные умолчания незаметно.: исправлено, покрыто test_unknown_session_is_left_alone

## Сценарий проверки

1. Сторожок различает осознанный стоп и смерть носителя, потолок попыток держит.

```sh
python3 -m unittest discover -s tools/devkitctl -t tools/devkitctl -p watch_test.py -k GoalCarrierTest 2>&1 | tail -3
```

2. Пакет сторожка зелёный целиком.

```sh
python3 -m unittest discover -s tools/devkitctl -t tools/devkitctl -p watch_test.py 2>&1 | tail -3
```

## Ранг

`50+8+2+0+4 = 64`, P1.

## Ход работы

- Из черновика DK-1160 «Цикл цели после перезагрузки не поднимается, сторожок только кричит»: записан 2026-09-25, оформлен 2026-09-25.

## Выкат

- 2026-09-25 слито: 8e5b28b04, 9d21d9b12, f3be73f93, 14a25127f

## Проверка

- Обкатка: 2026-09-25 12:24, свежее дерево 796adfbe7e57, сценарий 515a3d3d, временный HOME, утилит дерева 11, шагов 2, все зелёные.

Шаг 1 (зелёный):

```console
$ python3 -m unittest discover -s tools/devkitctl -t tools/devkitctl -p watch_test.py -k GoalCarrierTest 2>&1 | tail -3
Ran 8 tests in 0.293s

OK
```

Шаг 2 (зелёный):

```console
$ python3 -m unittest discover -s tools/devkitctl -t tools/devkitctl -p watch_test.py 2>&1 | tail -3
Ran 157 tests in 51.030s

FAILED (failures=1)
```
