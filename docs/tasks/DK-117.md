# DK-117: Первый виток цели идёт в чате, а ход фоновых витков виден до их завершения

По разделу «Цикл в чате» скилла goal-loop первый виток после команды
«продолжай цель» сессия ведёт сама в чате, следующие поднимаются фоновыми
headless-сессиями, и после каждого диспетчер коротко пересказывает итог. На
живом прогоне DK-109 (2026-08-04) поведение дважды разошлось с описанием, и
оба раза для пользователя это выглядело одинаково: чат замолкает, и непонятно,
работает цикл или завис.

## Разбор

Диспетчер поднял headless-виток сразу, без первого витка в чате. Этот виток
вырос из проверочного прогона. Шаг сценария DK-113 проверял, что
headless-сессии доступны права машинного контура, промптом запуска стояло
«продолжай цель DK-109 по скиллу goal-loop», и проверка прав незаметно
переросла в полноценный рабочий виток. Сам виток продуктивен (в нём слит и
закрыт DK-106 с тремя заходами на merge, взят DK-105), претензий к работе
нет, но чат молчал почти час.

Нет средств для наблюдения за витком, так как вывод `claude -p` буферизуется
до конца сессии и файл перенаправления пуст, пока виток идёт. Лог-файл
`.devkit/goal-<ID>.log` наполняется только оболочкой goal-run, которую
нельзя запустить из чата. Остаются `.devkit/log` (строки утилит без
контекста) и `git log`; про оба нужно знать заранее и опрашивать их руками.

Строка «Журнала» цели о проверке прав закоммичена раньше, чем виток
закончился: запись «headless-сессия отработала утилиты доски и запись в
журнал сама» появилась, когда процесс ещё работал. Как отметка о проверке
прав это честно, как строка о витке преждевременно.

## Что чинить

- В раздел «Цикл в чате» скилла goal-loop записать, что первый виток после
  «продолжай цель» всегда ведётся в чате, без исключения на «сначала проверю
  окружение». Проверочный запуск headless-сессии не считается витком и не
  выполняет рабочую фазу; для проверки используется ограниченный промпт,
  а не «продолжай цель».
- Завести лог хода фонового витка, то есть файл, который наполняется по
  ходу работы, а не после её завершения; путь к файлу диспетчер называет в
  чате при запуске витка.
- Строка «Журнала» о витке пишется после его завершения, не раньше. Правило
  фиксируется одной фразой там же, где описан формат строки.

## Развилки

1. Лог хода можно вести тремя способами: выводом
   `claude -p --output-format stream-json` в файл, дозаписью витка в
   `.devkit/goal-<ID>.log` без оболочки или отдельным файлом на виток.
   Дозапись в общий лог цикла выглядит самым дешёвым вариантом: формат строк
   уже есть, и вопрос «работает ли цикл» решается одним файлом при любом
   способе запуска.
2. Пересказ по ходу возможен двумя способами: либо диспетчер периодически
   читает лог хода и пересказывает его в чат, либо молчит до конца витка, но при
   запуске даёт команду для слежения (`tail -f ...`). Опрос расходует
   контекст диспетчера, команда перекладывает слежение на пользователя.
   Возможно, достаточно второго варианта плюс пересказа по завершении.
3. Окружение без рабочего витка проверяется отдельным проверочным промптом
   («прогони утилиты доски и выйди») или предварительной проверкой goal-run
   из DK-113, если она подходит для чата. Второй вариант не плодит
   сущностей, но эта проверка встроена в оболочку.

## Пересмотр ранга 2026-08-07

На груминге DK-112 было обнаружено, что источником живого статуса для
дашборда служат результаты работы витка, а по итогам разведки DK-156 тот же
механизм даст сторожку DK-153 критерий движения. Связь оформлена на доске
(задача держит DK-112),
ранг пересчитан по инварианту зависимости из RANKING.md: предпосылка не
стоит ниже задачи, которую держит.

- Ценность 5 -> 8: инцидент 2026-08-06 показал цикл, молча стоящий часами.
  Видимость хода витка из чата это прямой вклад в контроль автономного
  цикла, а не удобство.
- Рычаг 4 -> 5: без лога хода не собирается живой статус DK-112 и стоит
  чужая работа; сторожку DK-153 нужен тот же критерий движения.

Итог 25+8+3+0+5 = 41, вровень с DK-112.

## Решения по развилкам 2026-08-09

Постановку местами обогнала жизнь. Разбор писался по прежнему тексту раздела
«Цикл в чате», где первый виток шёл в чате, а следующие фоновыми
headless-сессиями; коммит 25713e5 того же дня оставил в чате все витки и
уводить их в фон запретил вовсе. Фоновый виток поэтому теперь это виток под
оболочкой `goal-run` и проверочный запуск из чата, а нужда видеть ход осталась
та же: вывод `claude -p` буферизуется до конца сессии при любом хозяине витка.

1. **Лог хода.** Дозапись в общий `.devkit/goal-<ID>.log`, как и намечала
   постановка. Строку кладёт та же оболочка новым ключом
   `goal-run.py <ID> --say "<строка>"`: цикла, замка и tmux он не поднимает,
   предполётных проверок не делает и годится витку любого происхождения, в том
   числе витку живого чата, где нет ни `autonomous`, ни оболочки. Формат строки
   тот же, что у строк цикла (`2026-08-09T20:54:48 DK-101 отдан исполнителю`),
   иначе один `tail` показывал бы в одном файле два разных журнала.
   Отвергнуты: `--output-format stream-json` в файл (разбирать поток некому, а
   вопрос «жив ли цикл» решается строкой) и отдельный файл на виток (у цели
   тогда два места правды, и `tail -f` пришлось бы перенацеливать на каждом
   витке, а дашборд DK-112 читает `.devkit/goal-*.log`).
   Заодно оболочка пишет строку о подъёме витка до запуска сессии, а не после:
   без неё журнал молчал бы всё время первого витка, даже когда сам виток
   исправно пишет ход.
2. **Пересказ по ходу.** Второй вариант: диспетчер не опрашивает журнал, а
   называет пользователю путь и `tail -f` при подъёме цикла и пересказывает
   итог по завершении витка. Опрос жёг бы контекст диспетчера ровно там, где
   его беречь, а команда слежения даёт пользователю ход в любой момент.
3. **Проверка окружения.** Отдельного проверочного витка не нужно: права
   машинного контура показывает `python3 tools/devkitctl/perms.py` без всякой
   сессии (она же стоит предполётной у оболочки), а где нужна именно живая
   headless-сессия, промпт ей даётся ограниченный и витком такой запуск не
   считается. Записано в раздел «Цикл в чате».

## Сценарий проверки

Агентский.

1. Стенд с целью DK-100, фальшивым `claude` вместо сессии (виток работает
   десяток секунд и пишет ход) и `autonomous = true`. Поднять цикл
   `goal-run.py DK-100 -C <стенд> --foreground` и во время работы витка
   читать `.devkit/goal-DK-100.log`: файл существует и растёт строками
   подъёма и хода, строка итога витка появляется только после его завершения.
2. `python3 -m unittest discover -p '*_test.py'` в `kit/skills/goal-loop/` и
   `regcheck -- python3 -m unittest discover -s kit/skills/goal-loop -p
   '*_test.py'`: регрессионный тест краснеет на старом коде.
3. Греп по `kit/skills/goal-loop/SKILL.md`: есть слова про первый виток в чате
   без исключения «сначала проверю окружение», про ограниченный проверочный
   промпт и про то, что такой запуск витком не считается; в шаге 5 есть фраза
   про строку «Журнала» по завершении витка.

## Ход работы

- Исполнение: субагент opus/high по вердикту pick (квота: week_all 81%, снимок 4м назад, сдвига нет), 2026-08-09.
- Правка: ключ `--say` в `kit/skills/goal-loop/goal-run.py` (строка хода мимо
  цикла, отказы на пустой строке, чужой цели и паре с `--foreground`), строка
  о подъёме витка до сессии, разделы «Ход витка» и правки «Цикла в чате» и
  шага 5 в `SKILL.md`, строка наблюдаемости в
  `docs/lld/DK-079-autonomous-loop.md`.
- Тесты: четыре в `goal_run_test.py`, из них регрессионный
  `test_log_grows_while_the_turn_is_still_running` (стаб витка снимает журнал
  посреди своей работы). `regcheck`: «тест краснеет на HEAD и проходит на
  текущем коде, регрессия закрыта».
- Обкатка шага 1 на стенде в `/private/tmp`, журнал во время витка:

  ```text
  == через 2 с ==
  2026-08-09T20:54:43 цикл цели DK-100 начат в <стенд>/proj, pid 59226
  2026-08-09T20:54:43 виток 1 поднят, ход витка ниже
  2026-08-09T20:54:43 фаза рабочая, готова DK-101
  == через 6 с ==
  ...
  2026-08-09T20:54:48 DK-101 отдан исполнителю opus/high
  == после витка ==
  ...
  2026-08-09T20:54:52 DK-101 слита и выкачена
  2026-08-09T20:54:52 виток 1 маркер done код 0 записей в журнале 0 -> 0
  2026-08-09T20:54:52 цикл цели DK-100 остановлен: done, витков 1
  ```

- Не проверено до выката: живая headless-сессия, пишущая ход по скиллу. Стенд
  играл виток фальшивым `claude`, и что настоящий виток зовёт `--say` на
  поворотах, покажет только первый прогон цели с новым текстом скилла.
