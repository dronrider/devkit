# DK-1180: Ревью, интервью и определения агентов сверяют правку с docs/ARCHITECTURE.md и docs/STYLEGUIDE.md

Цель: [tasks/DK-1178.md](tasks/DK-1178.md)

## Что происходит

Скилл `review` фильтрует расхождение правки с кодом как вкус. Правило
«вкусовщина не пишется» стоит, а адреса правила у ревьювера нет, потому что
проектные правила приходят одним каналом, ключом `checks` из
`.devkit/review.conf`. Определения исполнителей `kit/agents/exec-*.md` и
ревьюверов `review-*.md` ни архитектуры, ни стайлгайда не упоминают, обходчик
`interview` приносит развилки с источниками «код|дока|доска|возражение» без
слоя архитектуры. Корень разобран в файле цели DK-1178.

## Чего хотим

Тексты по решениям LLD DK-1179.

- `RULES.core.md` получает один абзац про якоря и расхождение, разбор идёт в
  `RULES.md`; бюджет ядра держится, доктор находок веса резидента (DK-190) не
  ставит.
- Скилл `review` получает вопрос второго уровня про границы архитектуры и
  соглашения стайлгайда с адресом правила, а расхождение с кодом без
  записанного правила пишет развилкой «кандидат в стандарт» через
  `taskctl decide` с рекомендацией «как в коде»; блока и молчания в этом
  случае нет.
- Определения exec-* и review-* получают строку чтения `docs/ARCHITECTURE.md`
  и `docs/STYLEGUIDE.md` до правки, когда файлы есть.
- Обходчик `interview` получает источник «архитектура» для слоя, который
  задача трогает.
- Стенд `obeycheck` получает сценарий, где дифф расходится с кодом без
  записанного правила, и правка прогоняется по скиллу `prompt-test` со
  следом в файле задачи.

## Границы

Доктор, скелеты и шапка карты идут в DK-1181. Содержимое якорей чужим
проектам не пишется. Стиль прозы и символов (RULES.md, «Код и тексты») не
трогается.

## DoD

- Стенд `obeycheck` зелёный на сценарии «дифф расходится с кодом без
  записанного правила»: ревьювер пишет развилку вместо блока или молчания.
- `RULES.core.md` несёт абзац про якоря, `RULES.md` разбор, exec-* и review-*
  строку чтения обоих файлов, `interview` источник «архитектура».
- `devkitctl doctor` не ставит находок веса резидента.

## Ранг

`50+8+1+0+4 = 63`, P1.

- Серьёзность 50. Основной сценарий командной разработки деградирует, обход
  это ручное ревью человеком.
- Ценность 8. Ревью получает адрес правила вместо вкуса, правила накапливаются
  сами.
- Неопределённость 1. Форма решена в LLD, осталось написать.
- Поправка на баг 0. Новая работа.
- Рычаг 4. Каждое следующее ревью дешевле.

## Ход работы

- Вычитка: 10 файлов, 12 правок, 0 пометок, 2026-09-26.
- Вычитка: 8 файлов, 19 правок, 0 пометок, 2026-09-26.

## Сценарий проверки

```sh
grep -c 'ARCHITECTURE' RULES.core.md kit/skills/review/SKILL.md kit/skills/interview/SKILL.md kit/agents/exec-high.md kit/agents/review-high.md
python3 -c "n = len(open('RULES.core.md', encoding='utf-8').read()); print(n); assert n <= 5500, 'ядро шире порога 5500: %d' % n"
```

Ожидание: первый шаг печатает ненулевой счёт по каждому файлу, второй
печатает число символов ядра и падает, если оно больше порога 5500 (карман
резидента, DK-190). Второй шаг раньше гонял `devkitctl doctor` и ждал 0 совпадений
слова «резидент» в его выводе, но `pockets_report` печатает строку с этим
словом всегда, счёт не бывает нулевым, и шаг был неисправен независимо от
правки. Прямая проверка размера файла надёжнее и не зависит от того, что
`doctor`, будучи позван из worktree, читает ядро по фиксированному пути
глобальной точки (`~/projects/devkit/RULES.core.md`), а не из дерева задачи.
След стенда `obeycheck` стоит в «Ходе работы».

## Проверка

- Стенд: 2026-09-26 16:47, дерево a362a0414, предмет a35794e2, база старый, ярус base, k 3, ходов 132, вывод 61452, вход 264, кэш 6042406, сценарии 57-review-standard-candidate, зачтён.

```console
$ obeycheck --task DK-1180 --tier base --only 57-review-standard-candidate -k 3 --base старый /private/tmp/claude-501/-Users-rider-projects-devkit/889cf616-e9a8-481c-bd1e-78449b655cbe/scratchpad/obey-DK-1180/rules-new /private/tmp/claude-501/-Users-rider-projects-devkit/889cf616-e9a8-481c-bd1e-78449b655cbe/scratchpad/obey-DK-1180/rules-old
сценарий                                                                rules-new  rules-old  вердикт
ревью находит расхождение без записанного правила: развилка, а не блок        2/3        0/3  не хуже

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  57-review-standard-candidate / rules-new / повтор 2: проверка: exit status 1 (развилки «стандарт ...» в разделе «Развилки» нет)
  57-review-standard-candidate / rules-old / повтор 1: проверка: exit status 1 (развилки «стандарт ...» в разделе «Развилки» нет)
```

- Обкатка: 2026-09-26 17:27, свежее дерево d267e5cb0440, сценарий 3d266a9c, временный HOME, утилит дерева 11, шагов 2, все зелёные.

Шаг 1 (зелёный):

```console
$ grep -c 'ARCHITECTURE' RULES.core.md kit/skills/review/SKILL.md kit/skills/interview/SKILL.md kit/agents/exec-high.md kit/agents/review-high.md
RULES.core.md:1
kit/skills/review/SKILL.md:1
kit/skills/interview/SKILL.md:1
kit/agents/exec-high.md:1
kit/agents/review-high.md:1
```

Шаг 2 (зелёный):

```console
$ python3 -c "n = len(open('RULES.core.md', encoding='utf-8').read()); print(n); assert n <= 5500, 'ядро шире порога 5500: %d' % n"
5487
```
