# DK-1194: Реплика в чат задачи без живой головы лежит без подъёма и без зова

## Что происходит

2026-09-26 21:41 из дашборда ушла реплика задаче DK-892, стоявшей в Check с отметкой smoke. Голова задачи (окно tmux task-DK-892) к тому времени была снята: слияние и перевод в Check сделала соседняя сессия в 19:53. Строка легла во вход `.devkit/chat/task-DK-892.in` с ответом «её возьмёт первый же ход сессии задачи», журнал дашборда написал «реплика задаче DK-892 легла в чат task-DK-892» без приписки «без ведущей сессии». Повторы в 21:45 и 21:48 отбиты как дубли. Живые чаты по соседству каждый ход отказывали в подхвате записью chat-in «разговор принадлежит не этой сессии, безадресная строка ждёт хозяина» (DK-430.2). Тик сторожка по строке писал «подъём не нужен, отметка smoke прогнан стоит». В ручке отправки дашборда (tools/dashboard/chat.go, handleTaskMessage) подъём зовётся только в ветке parkedByAsk (DK-922). Оживил руками `taskctl run DK-892`, лестница подняла новое окно с первого раза.

Проверил по коду 2026-09-26 перед оформлением. Подъём в ручке отправки стоит только в ветке parkedByAsk (tools/dashboard/chat.go:585). Прочей реплике достаётся общий разбор и приписка про ожидание во входе. Тик сторожка по вопросу обходит одни строки Blocked с разрядом «вопрос:» (tools/devkitctl/watch.py, parked_rows и wake). Обход ждущих берёт «слияние:», «закрытие:» и взведённый Backlog. Строку в Check не берёт и сам подъём. wakeFrom пускает две секции, Blocked и In progress. На прочих в ответ идёт «подъём не нужен, строка в Check» (tools/taskctl/wake.go:209). В ручке отправки ведущей считается любая живая сессия, которая задачу касалась. Список задач разговора собирает ownTasks по касаниям реестра (tools/dashboard/chats.go:952). Поэтому за адресата сошла соседняя сессия, слившая DK-892, и пометки о недоставленной реплике человек не увидел.

Вторая находка того же вечера. В 22:04 лестница подъёма отработала и завела новое окно tmux, сессия 7352ff77. Оболочка головы встала до первого хода. В preflight стоит проверка waits_user, и по ней строка Check с видом mixed и стоящей отметкой smoke считается ждущей человека (kit/skills/board-task/task-run.py:1345 и 597). Окно снялось через секунду. Реплика осталась во входе, к 22:06 их там лежало две. Тот же стоп записан в подъёме сирот. Строки Check с приёмкой человека lift пропускает нарочно (tools/devkitctl/lift.py, orphan_rows, живой прогон DK-937). Голову такой строке не поднять, и чат её мёртв по конструкции.

Реплика человека лежит в очереди без адресата и без срока, пока кто-то не поднимет голову руками. Ни дашборд, ни тик, ни соседние сессии её не двигают, зова человеку с готовой командой тоже нет, и тишина в панели неотличима от штатного ожидания. Это ровно тот случай, который цель DK-902 закрывала лестницей подъёма («кнопка дашборда, close, merge и тик запускают одну команду»), но к входу чата задачи лестница не подключена. Человек ждал ответа в чате 20 минут и не получил ни ответа, ни отказа.

У строки в Check с приёмкой человека обхода нет и у человека. Кнопка «Запуск» и `taskctl run` дают тот же стоп приёмки, и окно гаснет до первого хода. Ответить в такой чат нельзя никак, пока задачу не закроют.

## Чего хотим

Безадресная реплика человека, лежащая во входе чата задачи, это повод для хода головы в любом статусе строки. Подъём идёт той же лестницей, что и ответ на вопрос. Стоп приёмки на такой ход не распространяется. Голова отвечает в ленту и снова засыпает. Реплику из входа забирает первый же ход. Отказ лестницы виден человеку громким зовом с готовой командой, а панель до появления адресата держит реплику недоставленной.

## Границы

Чат цели той же дорогой не правится, это DK-1009. Вид пузыря недоставленной реплики и кнопки на нём эта задача не трогает, они лежат черновиком DK-954. Возврат привязки чата после ответа на вопрос идёт своей строкой DK-1191.

Стоп приёмки не отменяется. Строка в Check по-прежнему ждёт человека, ход по реплике её статуса не двигает, а голова после ответа засыпает тем же стопом.

Условий, посильных только человеку, не нашёл. Сценарий гоняется на машине агента, доступов и секретов правке не нужно, наружу она не ходит.

## Развилки

- «зов»: кто зовёт подъём по лежащей реплике: ручка отправки дашборда или тик сторожка?
  - решает: человек
  - рекомендация: оба входа, как у остальных ожиданий DK-902: ручка отправки зовёт лестницу сразу, тик добирает пропущенное
  - вариант: только ручка дашборда
  - вариант: только тик сторожка
  - решено агентом 2026-09-26: оба входа, как у остальных ожиданий DK-902: ручка отправки зовёт лестницу сразу, тик добирает пропущенное
  - источник: код tools/dashboard/chat.go:585
- «стоп приёмки»: останавливает ли стоп «задача ждёт приёмки человеком» ход по лежащей реплике?
  - решает: человек
  - рекомендация: не распространяется: лежащая реплика это повод для хода, голова отвечает в ленту и снова засыпает тем же стопом
  - вариант: распространяется, чат строки в Check остаётся мёртвым
  - решено агентом 2026-09-26: не распространяется: лежащая реплика это повод для хода головы, она отвечает в ленту и снова засыпает тем же стопом, статус строки не двигается
  - источник: код kit/skills/board-task/task-run.py:1345
- «подъём вне секций»: чем поднимать строку, которой обход ждущих отказывает по секции?
  - решает: исполнитель
  - рекомендация: звать лестницу taskctl run напрямую, как зовут её lift и кнопка дашборда: секции wakeFrom не расширять и статус строки не двигать
  - вариант: расширить wakeFrom на Check и прочие секции
  - источник: код tools/taskctl/wake.go:209
- «ведущая сессия»: по чему ручка отправки считает, что у задачи есть ведущая сессия?
  - решает: исполнитель
  - рекомендация: считать по замку головы и имени окна task-<ID>, как считает подъём: касания реестра остаются чипам списка чатов
  - вариант: оставить ownTasks и мерить по касаниям реестра
  - источник: код tools/dashboard/chats.go:952
- «зов человеку»: чем человеку видно, что лестница отказала и реплику никто не заберёт?
  - решает: исполнитель
  - рекомендация: последней ступенью лестницы DK-902: громкий зов с готовой командой taskctl run <ID> плюс пометка недоставленной реплики в панели
  - вариант: молчаливый отказ, строка ждёт следующего тика
  - источник: доска docs/tasks/archive/2026/DK-902.md
- «потолок повторов»: чем ограничен повторный подъём по реплике в строку, снятую воронкой?
  - решает: исполнитель
  - рекомендация: брать потолок и выдержку у подъёма сирот (LIFT_TRIES, SETTLE в tools/devkitctl/lift.py), своего счётчика не заводить
  - вариант: свой потолок на реплику во входе
  - источник: код tools/devkitctl/lift.py:56

## DoD

- Реплика в чат задачи без живой головы поднимает голову в любом статусе строки, включая Check с приёмкой человека, и вход после хода пуст.
- Отказ лестницы человек видит зовом с готовой командой, а панель держит реплику недоставленной с причиной.
- Ведущей сессией задачи считается только её голова. Касание реестра соседней живой сессией на адресата не тянет.
- Реплику, легшую во вход мимо ручки дашборда, добирает тик сторожка.

## Ранг

`50+8+3+5+2 = 68`, P1.

- Серьёзность 50: конвейер встаёт на человека там, где обещана автономия, и у дашборда отваливается главная функция, разговор с агентом задачи.
- Ценность 8: реплика в задачу идёт каждый день, и любая из них перестаёт ждать ручного подъёма.
- Неопределённость 3: подход назван, но правка идёт через три процесса и два языка, службу дашборда, тик launchd и оболочку головы. На стыке сидят развилки «подъём вне секций» и «ведущая сессия».
- Поправка на баг 5: лестница DK-902 обещала одну команду подъёма всем входам, а к входу чата задачи она не подключена. Лента при этом обещает человеку подъём по реплике.
- Рычаг 2: своя польза по одной теме, разговор с задачей. Тесты, выкат и доктор задача не трогает.

## Ход работы

- Из черновика DK-1194 «Реплика в чат задачи без живой сессии лежит без подъёма и без зова»: записан 2026-09-26, оформлен 2026-09-26.
- Разработка: субагент opus/xhigh по определению exec-xhigh, работа ac7ea596be0992e7e, минут 22, 2026-09-26 22:05-22:27.

## Сценарий проверки

Шаги гоняются на живой машине после выката. Нужны служба дашборда, tmux и харнес. Синтетическая доска берётся временным корнем, задача стенда заводится на ней.

1. Строка стенда стоит в Check с видом mixed и отметкой smoke, окна задачи нет, и `tmux list-windows -a | grep task-<ID>` пусто. Реплика уходит ручкой дашборда (`POST /api/projects/<корень>/tasks/<ID>/message` с текстом «Продолжай»). Ответ ручки несёт `"raised": true`, `tmux list-windows -a` показывает окно task-<ID> в пределах минуты, `.devkit/chat/task-<ID>.in` после первого хода пуст, в ленте разговора стоит ответ головы, строка осталась в Check.
2. Та же строка, реплика кладётся во вход мимо ручки (`printf` строкой в `.devkit/chat/task-<ID>.in`), дашборд не звали. `devkitctl watch` печатает подъём по лежащей реплике, окно поднято, вход пуст.
3. Лестница отказала, потолок деревьев занят. Реплика ложится, ответ ручки несёт `undelivered` с причиной, а человек получает зов с готовой командой `taskctl run <ID>` в панели и уведомлением.

## Из черновика DK-953 «Реплика в задачу без ведущей сессии не поднимает её голову» (записан 2026-09-12)

### Черновик

#### Ситуация

2026-09-12 в 03:47 оболочка конвейера сняла окно task-DK-947 по воронке (три холостых прохода), строка осталась в In progress. В 09:54 в панель задачи написали «Продолжи работу». Журнал дашборда записал: «реплика задаче DK-947 в devkit легла в чат task-DK-947 без ведущей сессии». Ручка сообщения задаче (tools/dashboard/chat.go, ветка parkedByAsk) зовёт подъём taskWake только для строки, припаркованной вопросом. Тик сторожка (tools/devkitctl/watch.py, wake) будит только строки из Blocked с причинами «вопрос:», «автор:», «слияние:», «закрытие:». Сама функция taskWake строку в in-progress принимает, но с этого входа её никто не зовёт. Реплика лежит во входе, пока человек не нажмёт «Запуск» или не позовёт taskctl run DK-947 из терминала.

В ленте у мёртвого разговора стоит приписка из chatwatch.go: «сессия task-DK-947 прожила 3 ч 25 мин и кончилась: терминала у разговора больше нет, и ответ на вопрос задачи DK-947 поднимет её сессию заново».

#### Осложнение

Человек пишет в задачу и ждёт ответа, а работы нет и не будет, пока он сам не найдёт кнопку запуска. Строка в работе без сессии, без парковки и без вопроса невидима и панели, и тику, о чём прямо сказано в комментарии wake.go. Приписка про смерть сессии обещает подъём по ответу на вопрос, а вопроса у строки нет, и текст про терминал и сроки жизни человеку ни о чём не говорит. Для него случилось одно: работа остановлена.

#### Вопрос

Поднимать голову по любой реплике в строку in-progress без ведущей сессии тем же taskWake, или сначала парковать строку машинным разрядом, чтобы её видел и тик? Как быть с репликой в строку, которую воронка сняла за сломанное окружение: подъём повторит воронку, и нужен ли тут потолок.

#### Гипотеза

Реплика в задачу без ведущей сессии поднимает её голову той же дорогой, что ответ на вопрос, и первый ход новой сессии забирает реплику из входа. Приписка о конце сессии в ленте стандартная и короткая, для человека это «работа остановлена» с причиной стопа одной фразой, без слов про терминал и подъём по ответу.
