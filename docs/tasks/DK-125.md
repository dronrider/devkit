# DK-125: Инструкция подключения проекта: два сценария, обычный и корп, без раскладки на пользователя

Найдено вопросом пользователя сразу после закрытия цели
[DK-111](archive/2026/DK-111.md): как выглядят шаги развёртывания devkit для корп-репозитория
и описаны ли они. Оказалось, что описаны, но по кускам трёх README, а цельной
короткой инструкции нет ни для корп-проекта, ни для обычного.

## Что не так

`devkitctl corp` делает свою часть и молчит про остальное. После неё проект не
работает, пока человек не сделает пять шагов, ни разу не собранных в одном
месте:

1. контур компании `~/.devkit/tracker/<имя>.local` (адаптер, адрес, `token_env`,
   пользователь, `cost_*`, таблица `[status]`, поля `[fields_*]`), формат живёт в
   `trackctl/README.md`;
2. токен в переменную окружения, названную в `token_env`;
3. ключи привязки `contour`, `key`, `branch` в `.devkit/tracker.local`:
   подключение кладёт туда только `repo` и остальное оставляет человеку;
4. личный приватный remote боковой директории, без него обрыв машины теряет
   доску;
5. проверка `devkitctl doctor` и `trackctl status`.

Список ручных шагов в README это ровно то, что `RULES.md` называет
перекладыванием раскладки на пользователя.

Отдельная ловушка в первом же шаге: префикс доски надо брать отличным от ключа
проекта в трекере, иначе рубеж следов не отличает локальный ID от ключа тикета
и правило про ID снимает (DK-124). Сказано это в `trackctl/README.md`, а
`--prefix` пользователь выбирает раньше, чем доходит до того файла, и пример в
корневом `README.md` про это молчит.

У обычного проекта своя версия той же дыры: `new` описан абзацем, а что делать
дальше (доска, выкат, тесты, харнесы), собрано по соседним разделам.

## Что делать

Короткая инструкция подключения на два сценария, обычный и корп, одним местом.
Место выбрать самому: годится раздел корневого `README.md` либо отдельный файл
рядом, но ссылки из `devkitctl/README.md` и `trackctl/README.md` обязаны вести
в него, а не пересказывать шаги заново. Инструкция короткая: последовательность
команд с одной строкой пояснения на шаг, разбор остаётся в README утилит.

Ручных шагов при этом должно стать меньше, иначе короткой инструкции не
выйдет. Минимум:

- `corp` заканчивает печатью того, что осталось сделать человеку, с путями
  файлов и именами ключей, а не молчит;
- `corp` предупреждает, когда `--prefix` совпадает с ключом проекта в привязке
  (если та уже есть), и называет последствие;
- то, что подключение может спросить или вывести само, оно спрашивает или
  выводит: решать, какие ключи привязки писать за человека, а какие оставить,
  исполнителю, но молчаливых пустых мест остаться не должно.

Найденное про `new` починить тем же заходом, если это не разносит задачу: у
обычного сценария шагов меньше и они проще.

## Сценарий проверки

Агентский. На чистом временном окружении пройти инструкцию буквально, слово в
слово, двумя прогонами: обычный проект и корп-проект. Считается пройденным,
когда по одной инструкции, без чтения других файлов, получается работающий
проект: у обычного доска и `shipctl start`, у корп-проекта доска в боковой
директории, `trackctl status` без находок и `shipctl start` с веткой по ключу
тикета. Вывод обоих прогонов вкладывается в файл задачи.

Прогон общего тест-набора из `.devkit/deploy.local` (ключ `test`) с кодом 0.

## Ход работы

- Исполнение: субагент opus/medium по вердикту pick (квота: week_all 24%, снимок 25м назад, сдвига нет), 2026-08-05.

## Прогон инструкции

Оба сценария CONNECT.md пройдены слово в слово на чистом временном окружении
(свой HOME, свой ~/go/bin, чужая инфраструктура имитирована голыми
репозиториями). Подстановка одна: вместо клона devkit с github раскладывается
рабочее дерево ветки, иначе прогон шёл бы по старому коду.

### Обычный проект

```
$ cd ~/projects/devkit && for u in taskctl shipctl agentctl trackctl regcheck obeycheck; do (cd $u && go build -o ~/go/bin/$u .); done
код: 0

$ python3 ~/projects/devkit/devkitctl/devkitctl.py doctor --fix
находок: 4
вес резидента devkit по карманам (харнес claude-code):
  AGENTS.md проекта                                 568 симв       232 ток
  тонкий CLAUDE.md                                   87 симв        36 ток
  RULES.board.core.md (импорт тонкого файла)      1 201 симв       490 ток
  листинг определений agents/                     1 846 симв       753 ток
  листинг скиллов                                 2 815 симв     1 149 ток
  итого                                           6 517 симв     2 660 ток
починено: git-хуки: core.hooksPath = hooks
починено: хук харнеса на PostToolUse: python3 ~/projects/devkit/hooks/check-symbols.py --hook
починено: хук харнеса на PostToolUse: python3 ~/projects/devkit/hooks/check-memory.py --hook
починено: хук харнеса на PostToolUse: python3 ~/projects/devkit/hooks/check-sensitive.py --hook
починено: хук харнеса на SessionStart: sh ~/projects/devkit/hooks/quota-refresh.sh
починено: хук харнеса на Notification: python3 ~/projects/devkit/hooks/notify.py --hook claude-code
починено: хук харнеса на Stop: python3 ~/projects/devkit/hooks/notify.py --hook claude-code
починено: хук харнеса на SubagentStop: python3 ~/projects/devkit/hooks/notify.py --hook claude-code
починено: хук харнеса на UserPromptSubmit: python3 ~/projects/devkit/hooks/notify.py --hook claude-code
починено: права машинного контура дописаны в ~/.claude/settings.json, правил 35: Bash(taskctl:*), Bash(shipctl:*), Bash(agentctl:*), Bash(regcheck:*), Bash(obeycheck:*), Bash(python3:*), Bash(git:*), Bash(sh:*) и ещё 27
починено: определение агента exec-high.md положено в ~/.claude/agents
починено: определение агента exec-low.md положено в ~/.claude/agents
починено: определение агента exec-medium.md положено в ~/.claude/agents
починено: определение агента exec-xhigh.md положено в ~/.claude/agents
починено: определение агента review-high.md положено в ~/.claude/agents
починено: определение агента review-low.md положено в ~/.claude/agents
починено: определение агента review-medium.md положено в ~/.claude/agents
починено: определение агента review-xhigh.md положено в ~/.claude/agents
починено: скилл board-batch положен в ~/.claude/skills/board-batch
починено: скилл board-groom положен в ~/.claude/skills/board-groom
починено: скилл board-ship положен в ~/.claude/skills/board-ship
починено: скилл board-task положен в ~/.claude/skills/board-task
починено: скилл goal-loop положен в ~/.claude/skills/goal-loop
починено: скилл goal-start положен в ~/.claude/skills/goal-start
починено: скилл live-core положен в ~/.claude/skills/live-core
починено: скилл test-standard положен в ~/.claude/skills/test-standard
починено: ~/.claude/CLAUDE.md сгенерирован: глобальная точка правил харнеса claude-code
починено: .devkit/deploy.local создан: вписать команду выката, autonomous при готовности
машина: нет снимка квоты в ~/.devkit/quota: корректор pick двигать вердикт не будет; снять: agentctl quota refresh
.devkit/deploy.local: пустой deploy=, shipctl нечего выкатывать; вписать команду выката
.devkit/deploy.local: пустой test=, shipctl merge будет требовать --test на каждый вызов, а процедура пачки сочинять его не умеет; вписать команду тестов проекта
docs/tasks/DK-125.md:4: битая ссылка (DK-111.md)
код: 1

$ python3 ~/projects/devkit/devkitctl/devkitctl.py new -C ~/projects/myproj --prefix MP
AGENTS.md создан из шаблона
git-хуки: core.hooksPath = ../devkit/hooks
доска создана в ~/projects/myproj: docs/TASKS.md, docs/TASKS-archive.md, docs/tasks/
.devkit/deploy.local создан: вписать команду выката, autonomous при готовности
.gitignore: добавлен .devkit/*.local
.gitignore: добавлен .devkit/log
CLAUDE.md сгенерирован для харнеса claude-code

осталось сделать:
1. обвязка выката ~/projects/myproj/.devkit/deploy.local: вписать test =, deploy =
проверка: devkitctl doctor -C ~/projects/myproj
код: 0

$ $EDITOR ~/projects/myproj/.devkit/deploy.local   (вписаны test и deploy)

$ python3 ~/projects/devkit/devkitctl/devkitctl.py doctor -C ~/projects/myproj
находок: 1
машина: нет снимка квоты в ~/.devkit/quota: корректор pick двигать вердикт не будет; снять: agentctl quota refresh
код: 1

$ taskctl -C ~/projects/myproj add --title "первая задача" --type task --rank "25+5+1+0+0" --cost S
MP-001 заведена в backlog: P2, R=31
код: 0

$ shipctl -C ~/projects/myproj start MP-001
ветка mp-001 в worktree ~/projects/myproj-mp-001
доска: MP-001: backlog -> in-progress, коммит 29c003f
работать в ~/projects/myproj-mp-001, по готовности: shipctl merge MP-001 (оттуда же или из основного чекаута)
код: 0
```

Ненулевой код один, у доктора: снимок квоты снимается из живой сессии
(`agentctl quota refresh`), в headless-прогоне снимать его нечем. Остальное
прошло с нуля, `shipctl start` завёл ветку и дерево задачи.

### Проект корп-контура

```
машинный контур разложен, строк отчёта:       39

$ git clone <корп-репозиторий> ~/projects/corp-proj
код: 0

$ python3 ~/projects/devkit/devkitctl/devkitctl.py corp -C ~/projects/corp-proj --prefix CB --contour corp --key ABC --remote <личный приватный remote доски>
боковая директория ~/projects/corp-proj-local заведена
git-репозиторий боковой директории заведён: доска пушится в личный приватный remote, а не в корп-origin
AGENTS.md создан из шаблона в боковой директории
доска создана в ~/projects/corp-proj-local: docs/TASKS.md, docs/TASKS-archive.md, docs/tasks/
.devkit/deploy.local создан: вписать команду выката, autonomous при готовности
.gitignore: добавлен .devkit/*.local
.gitignore: добавлен .devkit/log
git-хуки: core.hooksPath = ../devkit/hooks
боковая директория: CLAUDE.md сгенерирован для харнеса claude-code
редирект корня: git config devkit.local ../corp-proj-local
.devkit/tracker.local: вписан repo = ../corp-proj (по нему обвязка клона находится после переклонирования)
.devkit/tracker.local: вписан contour = corp
.devkit/tracker.local: вписан key = ABC
контур компании ~/.devkit/tracker/corp.local заведён болванкой: адрес и пользователь вписать, таблицу [status] сверить с трекером
remote доски: origin <темп>/board-remote.git
CLAUDE.md сгенерирован для харнеса claude-code
.git/info/exclude: спрятан CLAUDE.md
хук pre-commit: цепочка установлена
хук commit-msg: цепочка установлена

осталось сделать:
1. контур компании ~/.devkit/tracker/corp.local: заполнить base_url, user
2. токен трекера: export TRACKER_TOKEN=<токен>, имя переменной названо в ~/.devkit/tracker/corp.local
3. обвязка выката ~/projects/corp-proj-local/.devkit/deploy.local: вписать test =
проверка: devkitctl doctor -C ~/projects/corp-proj, дальше trackctl status -C ~/projects/corp-proj-local
код: 0

$ $EDITOR ~/.devkit/tracker/corp.local   (вписаны base_url и user, таблица [status] сверена)

$ export TRACKER_TOKEN=<токен трекера>
$ $EDITOR ~/projects/corp-proj-local/.devkit/deploy.local   (вписан test, deploy пустой)

$ trackctl -C ~/projects/corp-proj sync
зеркальных строк на доске нет: ни одна ссылка не ведёт на тикет ABC
код: 0

$ python3 ~/projects/devkit/devkitctl/devkitctl.py doctor -C ~/projects/corp-proj
находок: 1
машина: нет снимка квоты в ~/.devkit/quota: корректор pick двигать вердикт не будет; снять: agentctl quota refresh
код: 1

$ trackctl -C ~/projects/corp-proj status
привязка:	~/projects/corp-proj-local/.devkit/tracker.local
проект:	ABC, ветки {key}-{slug}
контур:	corp (~/.devkit/tracker/corp.local)
адрес:	https://tracker.example, пользователь ivanov
токен:	на месте
статусы:	расписаны все секции
адаптер:	jira
операции:	все на месте
sync:	0 мин назад
код: 0

$ taskctl -C ~/projects/corp-proj add --title "первая задача" --type task --rank "25+5+1+0+0" --cost S --link "ABC-123"
CB-001 заведена в backlog: P2, R=31
код: 0

$ shipctl -C ~/projects/corp-proj start CB-001
ветка ABC-123 в worktree ~/projects/corp-proj-abc-123
доска: CB-001: backlog -> in-progress, коммит a4c9003
работать в ~/projects/corp-proj-abc-123, по готовности: shipctl merge CB-001 (оттуда же или из основного чекаута)
trackctl take не прошёл: exit status 1 (ошибка: не достучался до Jira (GET /rest/api/3/issue/ABC-123?fields=summary,status,issuetype,timetracking): Get "https://tracker.example/rest/api/3/issue/ABC-123?fields=summary,status,issuetype,timetracking": dial tcp: lookup tracker.example: no such host); доска не встаёт из-за трекера, довести руками
код: 0
```

Ненулевые коды те же два и оба про имитацию: снимок квоты и `trackctl take` в
конце `start`, который стучится в несуществующий трекер. Доктор корп-обвязку
находок не дал, `trackctl status` чист, ветку `start` завёл по ключу тикета
(ABC-123), а не по локальному ID доски.
