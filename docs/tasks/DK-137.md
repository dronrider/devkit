# DK-137: Цель: обёртка, отдающая агенту выжимку вместо всего вывода команды

записан 2026-08-05

## Цель

Прогон тестов, сборка и линтер уезжают в контекст целиком, и на длинном выводе
это съедает заметный кусок окна впустую: агенту нужны упавшие тесты и первые
ошибки компилятора, а не тысяча строк «ok» подряд.

Замысел: обёртка, которая запускает команду, полный вывод кладёт файлом (рядом
с журналом в `.devkit`), а в ответ отдаёт код возврата, строки с ошибками,
хвост и путь к полному тексту. Дальше агент читает полный вывод только тогда,
когда выжимки не хватило. Идея пришла из разбора чужого проекта mcp-ai-helper
(https://github.com/alvnukov/mcp-ai-helper): там это ядро замысла, но завёрнуто
в MCP-сервер со своим конфигом и маршрутизацией в сторонние модели. Нам нужен
только сам механизм, и он ложится обычной утилитой, как `regcheck`.

Разведка грумминга по репозиторию:

- своего примитива нет, есть слепой хвост в 30 строк, размноженный копипастой:
  `regcheck/check.go:35`, `shipctl/ops.go:43` (зовётся примерно из десяти мест),
  плюс `obeycheck/run.go:68` с тремя строками для ячейки таблицы;
- `regcheck` гоняет команду дважды и вывод второго прогона выбрасывает совсем,
  так что при неожиданном исходе смотреть нечего;
- в `.devkit` копится `log` (2233 строки), ротации и чистки нет ни у чего,
  прецедента уборки в репозитории не существует;
- `PreToolUse` в devkit не подключён, на Bash не висит ни один хук; сегодня
  утилиты зовутся руками, как `regcheck`.

Замер по транскриптам сессий (2026-08-06, 972 сессии, 57861 вызов
инструментов) двигает рамку постановки. Прогоны тестов и сборки, с которых
замысел начат, набирают около процента вывода Bash (`go test` 420K символов за
всю выборку, `cargo test` 652K), а объём лежит в другом месте. Головные команды
вызова по объёму: `git diff` 9.1M (18.2% вывода Bash, в среднем 4.6K за вызов),
`sed -n` чтением фрагмента 8.6M, `grep` 8.1M при среднем всего 1.2K, `cat` 3.8M.
Половину же всего вывода инструментов даёт не Bash, а `Read`: 49.5% против
46.1% при вчетверо меньшем числе вызовов. Форма расхода хвостовая, вызовы
тяжелее 20K символов это 1.33% вызовов и 23.4% объёма, а медиана вывода 342
символа.

Отсюда два следствия для дизайна. Первыми потребителями обёртки идут `git diff`
и `git show`, а не прогоны тестов, иначе утилита накроет процент проблемы.
Порог включения выжимки ставится по хвосту распределения, а не по любому
выводу: ниже медианы резать нечего. Чтение файлов из этой же картины вынесено
отдельно, в DK-146 (повторные чтения одного файла) и DK-147 (чтение целиком
против чтения куском).

Границы, названные на постановке:

- потребители переезжают на обёртку: `regcheck`, `shipctl` и `obeycheck`
  перестают резать вывод своими копиями `tail()`;
- у длинных прогонов появляется правило в долгоживущей доке, иначе утилита
  останется мёртвой;
- чистка старых выводов входит в цель, копиться без границ они не должны;
- как агент попадает на обёртку вместо голой команды (правило в ядре, хук на
  Bash или оба), решается дизайном внутри цели, а не на постановке.

DoD:

1. Утилита работает и разложена автоматикой: собирается строкой `deploy` из
   `.devkit/deploy.local`, стоит в списке бинарей `devkitctl`, а `devkitctl
   doctor` на машине без неё печатает находку. Прогон на команде с длинным
   выводом печатает код возврата, значимые строки, хвост и путь к полному
   тексту, и файл по этому пути читается целиком.
2. Молчаливой потери нет: выжимка сама называет, сколько строк осталось за
   кадром, так что «утилита спрятала важное» отличимо от «важного не было».
3. Развилка входа снята документом в `docs/lld/`: решено и записано, зовёт ли
   обёртку сессия по правилу, подменяет ли команду хук, и почему.
4. Копий примитива не осталось: `regcheck`, `shipctl` и `obeycheck` режут
   вывод обёрткой, а не своими `tail()`; `regcheck` при провале отдаёт пути к
   полным логам обоих прогонов, а не теряет второй.
5. Правило про длинные прогоны лежит в долгоживущей доке, и сессия видит его
   без чтения кода утилиты.
6. Старые выводы не копятся: правило чистки работает и проверяется прогоном на
   временном репозитории, каталог вывода лежит в гитигноре, а `devkitctl
   doctor` замечает, когда его там нет.
7. Прогон `test` из `.devkit/deploy.local` зелёный, у самой утилиты своя
   пирамида тестов.

## Бюджет

бюджет: week_all <= 20
бюджет: week_max <= 8

Потолка яруса нет, вердикт `pick` работает как обычно.

Бакеты общие на машину, параллельная сессия тратит тот же `week_all`, так что
числа это верхняя граница трат машины за время цикла, а не счётчик одной цели.
На постановке снимок показывал `week_all` 27% и `week_max` 12%.

## Задачи цели

Дизайн входа и формата выжимки (DoD №3) едет первым витком прямо в
`docs/lld/DK-137-cmdout-wrapper.md`, отдельной строки на него нет. Развилка
входа снята гибридом: правило в долгоживущей доке как основной канал плюс
мягкий `PreToolUse`-хук-напоминание (без блокировки) на паттернах `git diff`
и `git show`. Жёсткая подмена хуком отметена: хук не видит вывода до вызова и
не отличает длинный вывод от короткого, а мягкое напоминание на двух головных
командах замера снимает забывчивость, не ломая вызовов, где нужен полный
текст. Инфраструктура `PreToolUse` на Bash уже стоит от DK-228 (расширение
`HOOK_LAYOUT`, разбор `tool_input.command` в `hookio.py`, каркас
`check-read-secret.py`), хук-напоминание садится на неё минимально.

Нарезка первым витком 2026-08-11, пять строк DK-264..DK-268. Корректировка
состава в тот же день привязала серию к общему модулю каркаса (DK-237):
`cmdout` строится на общем go-модуле через `import`, а не подпроцессом, и
идёт за DK-237, которая решает формат общего модуля и въезжает первой
очередью одним cmdout-примитивом. Порядок серии теперь DK-237 -> DK-264 ->
остальное.

- DK-237 (LLD, M, R=39). Формат общего go-модуля каркаса: где лежит, чем
  подключается, как переживает `devkitctl build`, сборку тарболлов и голый
  `go build`. Рамка сужена под связку с серией: первой очередью в модуль
  въезжает один cmdout-примитив, остальные функции замера едут следующими
  волнами. Идёт первой, держит DK-264.
- DK-264 (task, M, R=41). `cmdout` на общем go-модуле: бинарь-обёртка для
  агента плюс пакет-примитив, который импортируют потребители. Запускает
  команду, полный вывод кладёт файлом в `.devkit/cmdout/`, агенту отдаёт код
  возврата, значимые строки, хвост и путь к полному тексту. Раскладка выкатом
  (`tools/cmdout/`), находка doctor через `check_binaries()`. Слагаемые: 25
  серьёзность (Major, без cmdout у агента нет выжимки, а shipctl не переедет
  на import), 8 ценность (прямой вклад в экономию квоты, та же оценка, что у
  цели), 3 неопределённость (подход ясен из LLD, открыты детали формата
  значимых строк и порога), 0 баг, 5 рычаг (разблокирует потребителей на
  import). После DK-237.
- DK-265 (bug, M, R=41). `regcheck`: регрессия чинится своим тонким
  примитивом внутри утилиты, без cmdout и без общего модуля. Оба прогона
  пишут полный вывод файлом, при провале агенту видны хвосты и пути к полным
  логам обоих прогонов. Дедупликация копии `tail()` уезжает в DK-237 своим
  чередом. Слагаемые: 25 серьёзность, 5 ценность, 2 неопределённость, +5 баг
  (`check.go:308` обрезает хвост первого прогона, второго не виден), 4 рычаг
  (регчек гоняется каждым прогоном тестов). Независимо.
- DK-266 (task, M, R=33). `shipctl` на import общего модуля: 13 вызовов
  `tail()` в `ops.go:94-98` переезжают на пакет cmdout. Слагаемые: 25
  серьёзность, 4 ценность, 1 неопределённость, 0 баг, 3 рычаг. После DK-264.
- DK-268 (task, S, R=9). Правило про длинные прогоны в долгоживущей доке
  (`RULES.md`), без правила утилита остаётся мёртвой. Слагаемые: 0
  серьёзность, 4 ценность (DoD №5), 1 неопределённость, 0 баг, 4 рычаг.
  Независимо, базируется на LLD.
- DK-267 (task, S, R=6). Чистка старых выводов: правило (возраст или размер),
  проверка на временном репозитории, `.devkit/cmdout/` в гитигноре, находка
  doctor через `ensure_gitignore()`. Слагаемые: 0 серьёзность, 2 ценность, 2
  неопределённость, 0 баг, 2 рычаг. После DK-264.

Зависимости: DK-237 держит DK-264; DK-264 держит DK-266, DK-267; DK-265 и
DK-268 независимо. `obeycheck` из постановки в серию не вошёл: у него нет
`tail()`, функция `trimNote()` делает другое (примечание сценария в три
строки), переезжать нечего. Постановка на этом месте была неточна,
корректировка отмечена явно.

Порядок: DK-237 (формат общего модуля, cmdout-примитив первой очередью) ->
DK-264 (`cmdout` на общем модуле) -> DK-266 и DK-267 по готовности; DK-265
чинит регрессию regcheck независимо и может ехать параллельно с любой
стадией, DK-268 независимо от кода. DoD №4 в этой рамке закрывается так:
`shipctl` режет вывод общим модулем (DK-266), `regcheck` закрывает
регрессию локальным примитивом (DK-265), а устранение самой копии `tail()`
между утилитами съезжает в волны DK-237; `obeycheck` копии не имеет.

## Входящие

- 2026-08-12 13:29, из дашборда: Что там с DK-265?
- 2026-08-12 13:30, из дашборда: Что там с DK-265?

## Журнал

- 2026-08-11, нарезка первым витком: пять задач DK-264..DK-268 (`cmdout`,
  переезд `regcheck` с отдачей путей к обоим прогонам, переезд `shipctl`
  с 13 вызовов `tail()`, правило в долгоживущей доке, чистка старых выводов).
  Дизайн входа и формата выжимки снят этим витком в
  `docs/lld/DK-137-cmdout-wrapper.md`: гибрид, правило плюс мягкий
  `PreToolUse`-хук-напоминание на `git diff` и `git show` (без блокировки),
  жёсткая подмена отметена. Порог включения выжимки 4K символов или 100
  строк, ниже порога утилита отдаёт вывод как есть. Первые потребители `git
  diff` и `git show` по замеру, не прогоны тестов. Зависимости: DK-264 держит
  DK-265, DK-266, DK-267, DK-268 независимо. `obeycheck` из постановки в
  серию не вошёл, у него нет `tail()`. Первой берётся DK-264; continue
- 2026-08-11, корректировка нарезки (решение пользователя): серия привязана
  к общему модулю каркаса (DK-237). `cmdout` (DK-264) строится на общем
  go-модуле через `import`, не подпроцессом, и идёт после DK-237; рамка
  DK-237 сужена до механизма (где модуль лежит, чем подключается, как
  переживает `devkitctl build` и тарболлы), первой очередью въезжает один
  cmdout-примитив, остальные функции замера едут волнами. DK-265 (bug
  регрессии regcheck) развязана: чинится своим тонким примитивом внутри
  regcheck, зависимость от DK-264 снята, за DK-237 не ставится, дедупликация
  копии `tail()` уезжает в DK-237. Порядок серии теперь DK-237 -> DK-264 ->
  остальное, DK-265 и DK-268 независимо. Зависимости на доске поправлены;
  continue

## Итог

Все семь DoD закрыты серией из шести задач: DK-237 (формат общего модуля
`internal/frame`), DK-264 (утилита `cmdout` + `frame.Capture`/`Summarize`),
DK-265 (`regcheck` на `frame.Summarize`, оба прогона с путями к полным логам),
DK-266 (`shipctl`, 13 вызовов `tail()` -> `frame.Summarize`), DK-267 (чистка
`cmdout clean` + находка `doctor` о скоплении), DK-268 (правило в `RULES.md`).

Сверка по DoD:

1. Утилита собирается и раскладывается выкатом, в обвязке бинарей. Прогон
   `seq 1 5000` под обёрткой печатает `exit: 0`, `lines_total: 5000`,
   `lines_hidden: 4970`, хвост и путь к файлу, файл по пути читается целиком.
   [DK-264]
2. Молчаливой потери нет: `lines_hidden` в выжимке отличает «спрятала важное» от
   «важного не было». [DK-237/DK-264, `frame.Summary`]
3. Развилка входа снята LLD `docs/lld/DK-137-cmdout-wrapper.md`: правило в
   `RULES.md` как основной канал плюс мягкий `PreToolUse`-хук-напоминание на
   `git diff`/`git show` (инфра DK-228), жёсткая подмена отметена. [LLD + DK-268]
4. Копий примитива нет: `regcheck` оба прогона через `frame.Summarize` с путями
   [DK-265], `shipctl` -> `frame.Summarize` [DK-266]. `obeycheck` копии `tail()`
   не имеет, в серию не вошёл, постановка о нём была неточна и отмечена явно.
5. Правило лежит в `RULES.md`, раздел «Длинные прогоны под обёрткой», между
   «Тесты обязательны» и «Фича доезжает до пользователя», со ссылкой на LLD и
   `internal/frame` без цифр порога. [DK-268]
6. Старые выводы не копятся: `cmdout clean [--days N] [--dry-run]` по порогу
   возраста (проверено на временном репозитории), `.devkit/cmdout/` в гитигноре,
   `doctor` через `check_cmdout` замечает скопление. Отсутствие гитигнор-записей
   `.devkit/cmdout/` и `.devkit/ship.lock` тоже находка: их раскладывает `new`,
   а чинит `doctor --fix` (DK-278). [DK-267, DK-278]
7. Прогон `test` из `.devkit/deploy.local` зелёный (гоняется на каждом слиянии
   серии), у утилиты своя пирамида: `internal/frame` (`Capture`, `Summarize`,
   `Clean`), `tools/cmdout` (бинарь и разбор флагов), `tools/devkitctl`
   (интеграция `doctor` <-> `clean`).

Пробел DoD №6 закрыт DK-278: `doctor` через `check_machine_ignore` замечает
отсутствие гитигнор-записей `.devkit/cmdout/` и `.devkit/ship.lock`, а `new` и
`doctor --fix` раскладывают их общим списком `MACHINE_IGNORE_ENTRIES`. Сама
запись стоит от DK-264, так что каталог под гитом не оказывался и прежде, теперь
же doctor подстрахует, если запись кто-то снимет.

Замысел цели достигнут: обёртка работает, потребители переехали, правило лежит,
выводы чистятся, молчаливой потери нет.

## Сценарий проверки (агентский)

Приёмка цели это семь DoD, проверяемых командами.

1. `cmdout seq 1 5000` печатает выжимку: `exit: 0`, `lines_total: 5000`,
   `lines_hidden: 4970`, хвост и `path:` к файлу; файл по пути читается целиком.
   Так DoD №1 и №2.
2. Во временном git-репозитории положить `.devkit/cmdout/<старый>/out` (mtime на
   30 дней раньше) и `<свежий>/out`; `cmdout clean --days 7 --dry-run` называет
   старый и не трогает каталоги, `cmdout clean --days 7` удаляет старый и
   оставляет свежий, оба exit 0. DoD №6 (чистка).
3. `devkitctl doctor` в репозитории со скоплением печатает находку про
   `.devkit/cmdout` и выходит 1; `doctor --fix` чистит; повторный `doctor`
   выходит 0. DoD №1 (находка) и №6.
4. В коде потребителей ручной `tail(out)` отсутствует вне тестов и комментариев:
   `grep -rn "tail(out" tools/regcheck tools/shipctl internal` пуст по живому
   коду. DoD №4.
5. `grep -n cmdout RULES.md` находит правило (exit 0), а
   `grep -nE "4096|4K|100 строк|threshold" RULES.md` пуст (exit 1): правило
   есть, цифр порога не дублирует. DoD №5.
6. Прогон `test` из `.devkit/deploy.local` зелёный. DoD №7.
7. Развилка входа (DoD №3) описана в `docs/lld/DK-137-cmdout-wrapper.md` и
   реализована правилом в `RULES.md` плюс мягким хуком-напоминанием:
   `grep -rn "cmdout" docs/lld/DK-137-cmdout-wrapper.md` непуст.

Шаги 1, 2, 3 обкатаны при закрытии DK-264 и DK-267 (вывод в их файлах задач);
4, 5, 7 проверяются grep; 6 гоняется на каждом слиянии серии. Итог сверяется
кодами возврата и выводом grep, не на глаз.

Шаг 3 после закрытия DK-278 проверяет и гитигнор: `doctor` замечает отсутствие
записей `.devkit/cmdout/` и `.devkit/ship.lock`, а `doctor --fix` их дописывает.
До DK-278 шаг проверял только скопление, а не гитигнор.

### Прогон сценария (2026-08-12)

Шаги 1-3 обкатаны при закрытии DK-264 и DK-267 (вывод в
`docs/tasks/archive/2026/DK-264.md` и `DK-267.md`): выжимка `seq 1 5000` несёт
`exit: 0`, `lines_total: 5000`, `lines_hidden: 4970`, tail и `path`; чистка
разделяет старый и свежий каталоги; doctor находит скопление (exit 1), `--fix`
чистит, повторный exit 0.

Шаги 4, 5, 7 на свежем main:

- `grep -rn "tail(out" tools/regcheck tools/shipctl internal` находит только
  комментарии и докстринги, живого вызова нет.
- `grep -n cmdout RULES.md` exit 0 (правило в строках 28, 30),
  `grep -nE "4096|4K|100 строк|threshold" RULES.md` пуст.
- `grep -cn cmdout docs/lld/DK-137-cmdout-wrapper.md` = 7: развилка входа
  описана.

Шаг 6 зелёный: прогон `test` из `.devkit/deploy.local` гоняется на каждом
слиянии серии, последнее слияние DK-268 прошло.

Итог: сценарий проходит, пробел №6 (гитигнор через doctor) узкий и в черновике
DK-278. PASS.

## Ход работы

- 2026-08-07: ценность 7 -> 8 решением пользователя при разборе приоритетов:
  экономия квоты это прямой вклад в главную ценность devkit, больше агентской
  работы за ту же неделю, а не удобство. Итог 25+8+3+0+4 = 40.
- Грумминг: подпроцесс glm-5.2/xhigh по вердикту pick (квота: объявления нет, корректор выключен), 2026-08-11.
- Вычитка: 2 файла, 1 правка, 2 пометки, 2026-08-13.
