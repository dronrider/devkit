# DK-166: Цель: полный прогон тестов укладывается в две минуты

записан 2026-08-07

## Цель

Полный прогон тестов devkit вырос до 4-5 минут, а прогонов на одну задачу
выходит несколько: regcheck гоняет тест бага на двух ревизиях, свои прогоны
зовут ревью и слияние поезда. Ожидание складывается в заметный тормоз всей
работы над задачей. Хотим, чтобы прогон перестал быть узким местом.

DoD: холодный прогон команды `test` из `.devkit/deploy.local` (go test с
`-count=1`, питоновые unittest, проверочные скрипты) укладывается в 120 секунд.
Проверка агентская: `time` вокруг команды `test`, порог утверждён
пользователем 2026-08-07.

## Бюджет

бюджет: week_all <= 20
бюджет: week_max <= 8

## Разбор 2026-08-07

Актуальность проверена замером: холодный последовательный прогон команды
`test` занял 433 секунды. Раскладка по компонентам:

- py tools/devkitctl: 289 с (две трети всего прогона)
- go tools/shipctl: 59 с
- py hooks: 30 с
- go tools/agentctl: 19 с
- go tools/taskctl и tools/obeycheck: по 10 с
- py kit/skills/goal-loop: 10 с
- go tools/regcheck и tools/trackctl: по 3 с
- остальное (kit/skills, check-skills, check-exec-bit, doctor --layout): ~0 с

Внутри devkitctl время по модулям: devkitctl_test 122 с, update_test 55 с,
rules_test 31 с, corp_test 29 с, weigh_test 19 с, harness_test 11 с, perms_test
и build_test по 8 с, layout_test 4 с, остальные по 0-1 с.

Компоненты в команде `test` независимы и идут строго последовательно, так что
параллель срезает сумму до самого тяжёлого компонента, но пока devkitctl
держит 289 секунд, порог 120 не берётся: тяжёлые наборы придётся ускорять и
сами. Возможные направления, все открыты: профилировка тяжёлых модулей (стенды
с временным HOME, вызовы через subprocess), параллель компонентов, кэш go test
(какие прогоны честно гонять с `-count=1`). Смежные закрытые DK-141 и DK-142
переводили раннеры тестов на python, самих тестов не ускоряли.

Предмет: медленный прогон платится по несколько раз на каждую задачу доски,
теряют все конвейеры (исполнитель, ревью, слияние, выкат). Обхода нет, команда
`test` зовётся автоматикой целиком. Ускорять прогон ценой прорех в покрытии
нельзя: набор тестов остаётся полным, режется время, а не проверки.

## Задачи цели

Нарезка материализована 2026-08-15: DK-346, DK-347, DK-348, порядок держат dep.

- DK-346 (task, M, R=39: 25+7+3+0+4), закрыта 2026-08-15. Раннер сюиты
  devkitctl suite.py: классы отдельными процессами, потолок 12 воркеров,
  сверка счёта с discover. Сюита 289 -> 121-143 с в тишине, счёт 490 совпал.
- DK-347 (task, M, R=37: 25+6+2+0+4), закрыта 2026-08-16. Параллель
  компонентов команды test: parallel.py, 15 компонентов, потолок 8, go
  с -count=1, ключ test в deploy.local основного чекаута переведён на раннер.
- DK-348 (task, M, R=38: 25+6+3+0+4), закрыта 2026-08-16. Разгрузка тяжёлых
  юнитов по профилю: poll_interval у сервера релизов в тестах, кеш git-чтений
  в doctor, диспетчер заглушек симлинками. MachineContourTest 46,9 -> 37,9 с,
  сюита 2:06 -> 1:24-1:37, счёт 506 -> 516, снят только добавлением.

## Журнал

- 2026-08-15, материализация нарезки: DK-346..DK-348 строками с dep-порядком;
  сверька оценки: week_all 3-9 из 20 (влезла), week_max 4.5-9 из 8 (остаток
  внутри вилки, верх на пункт выше потолка); continue
- 2026-08-15, DK-346 закрыта по агентскому сценарию (490 тестов, 95 классов,
  счёт совпал, два прогона зелёные); попутно закрыта чужая DK-358, державшая
  очередь выката; continue
- 2026-08-16, DK-347 закрыта по сценарию (15/15 компонентов, стена 322 с под
  нагрузкой); DK-348 закрыта по сценарию (сюита 516 тестов за 1:25,
  MachineContourTest 38,5 с); DoD прогнан дважды: 235 с и 254 с, порог 120 с
  не взят, стена упёрлась в go:dashboard; done c вопросом
- 2026-08-16, пользователь принял итог: прогон ужат вдвое (433 -> 235 с),
  порог 120 с снят решением пользователя, на go:dashboard заведён отдельный
  черновик; цель закрыта

## Проверка

Прогон DoD на выкаченном main 2026-08-16, дважды, команда `test`
из `.devkit/deploy.local` (parallel.py):

```text
Ran 15 of 15 components in 3m55s  OK   real 235.20  (load 3.5-5.2)
Ran 15 of 15 components in 4m14s  OK   real ~254     (load 5.6-6.8)
go:dashboard 254.4s / go:shipctl 215.2s / hooks 159.1s / devkitctl 152.4s
```

Прогоны зелёные, порог 120 с не взят: стена упирается в go:dashboard.
Разбор и решение пользователя в «Итоге».

## Итог

DoD: холодный прогон команды `test` из `.devkit/deploy.local` под 120 секунд.
Прогон на выкаченном main 2026-08-16, дважды:

```text
Ran 15 of 15 components in 3m55s  OK   real 235.20  (load 3.5-5.2)
Ran 15 of 15 components in 4m14s  OK   real ~254     (load 5.6-6.8)
go:dashboard 254.4s / go:shipctl 215.2s / hooks 159.1s / devkitctl 152.4s
```

Порог не взят, и все три задачи цели свою часть сняли честно: py-сюита
devkitctl (было две трети прогона, 289 с) уложена в 1:24-1:37 с параллелью
классов и разгрузкой юнитов, компоненты идут параллельно со сходом стены
к самому тяжёлому звену, счёт тестов вырос (506 -> 516), ни одна проверка
не срезана. Остаток сидит в go:dashboard: 254 с под общим прогоном и
110-147 с в одиночку, половина порога ещё до чужих компонентов. Нарезка цели
до go-сюит не дотягивалась: замер разбора держал py tools/devkitctl за
две трети времени, go-инструменты виделись точечными (59 с shipctl, 19 с
agentctl), реальный вес dashboard вскрылся только на параллели, когда стена
перестала быть суммой.

Машина за весь цикл тихей не была (load 3.5-15 от посторонних процессов),
но go:dashboard тянет стену и в одиночку на 110-147 с: даже в полной тишине
порог 120 с держится впритык или не берётся. Срезания DoD нет, но и взятия
нет: цель снимает заявленный выигрыш (прогон 433 -> 235-254 с, почти вдвое),
а порог 120 с требует четвёртой задачи по go:dashboard, которой в бюджете
цели не осталось. Решение пользователя 2026-08-16: итог принят, на go:dashboard
заводится отдельный черновик доски, порог 120 с уходит туда вместе с профилем
его сюиты.

Потрачено по бакетам: week_all 0 из 20 (гейт не увидел расхода, у харнеса
glm-code секция квоты пуста), фактическая работа: 3 задачи M, каждая по
полному кругу конвейера.
