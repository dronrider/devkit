# DK-168: Связь из груминга обязательна: вход без маркера «после» это находка doctor

## Задача

Груминг находит связь между задачами и записывает её прозой в файл, а доска
этого не отражает. Живой случай: груминг DK-112 назвал DK-117 входом живого
статуса прозой в файле задачи, маркера «после» у строки не было, и
`taskctl dep list` эту связь не показывал до коммита 51ece38. Для диспетчера
такой связи не существует, и задача берётся в работу раньше своей
предпосылки.

Рубеж держит `devkitctl doctor`: в файле задачи другая живая задача названа
входом, а маркера «после» у строки нет, значит находка. Сегодня doctor проверяет
доску только на наличие (`devkitctl.py:1096`), про маркеры «после» там ни
строчки. Вместе с рубежом правило оседает в процедурах груминга и работы над
задачей: связь, найденная разбором, ставится командой `taskctl dep add`, а не
остаётся фразой в тексте.

## DoD

1. На синтетической доске задача, в чьём файле вход назван без маркера
   «после», даёт находку doctor; с маркером находки нет. Проверяется кодом
   возврата и строкой находки.
2. Правило записано в долгоживущих доках: процедуры груминга и работы над
   задачей называют постановку связи командой обязательным шагом.
3. Прогон `test` из `.devkit/deploy.local` зелёный.

## Приёмка

Вид agent: оба пункта проверяются прогоном на временном репозитории с
синтетической доской, эталон берётся из кода возврата и строки находки.

## Ход работы

- 2026-08-07: постановка диалогом после разбора приоритетов, строка заведена
  целью XL из трёх дыр (связи из груминга, порядок «что брать сейчас», гейт окна
  квоты в постановке цели). Бюджет 15/6 пп, потолка яруса нет.
- 2026-08-07: груминг, три ворот пройдены проверками. Дубля нет: накопитель
  просмотрен целиком, по Backlog смежная DK-071 (то же семейство doctor-находок,
  предмет другой), в архиве DK-050 закрывал отбор кандидатов batch, а не порядок
  внутри полосы, DK-010 и DK-012 корректируют вердикт pick по квоте, не старт
  цели.
- 2026-08-17: цель распущена грумингом DK-395, DK-397 и DK-168 одним заходом,
  журнал витков был пуст, цикл не запускался, терять нечего. Дыра про порядок
  «что брать сейчас» уехала в цель DK-395 пунктом 4 её нарезки: формула
  планировщика (score = R / W внутри полосы P, где W учитывает пройденный рубеж
  и плату за возврат) это тот же порядок, только богаче, и делать сначала
  простой порядок, а потом переписывать его под рубеж и плату значит платить
  дважды за один код. Гейт окна квоты в `goal-start` встал отдельной строкой
  DK-398. Эта строка держит первую дыру и из цели XL стала задачей S.

  Ранг пересчитан: 25+6+1+0+4 = 36. Серьёзность 25 (заметное трение, обход это
  ручная постановка связи, о которой вспоминают не всегда), ценность 6
  (улучшение потока, которое пользователь прямо попросил), неопределённость 1
  (подход назван целиком, осталось сделать), поправки на баг нет, рычаг 4
  (диспетчер и планировщик получают связи, которых раньше не видели). Цена S.
- Вычитка: 1 файл, 3 правки, 0 пометок, 2026-08-17.
