# DK-168: Связь из груминга обязательна: вход без маркера «после» это находка doctor

## Задача

Груминг находит связь между задачами и записывает её прозой в файл, а доска
этого не отражает. Живой случай: груминг DK-112 назвал DK-117 входом живого
статуса прозой в файле задачи, маркера «после» у строки не было, и
`taskctl dep list` эту связь не показывал до коммита 51ece38. Для диспетчера
такой связи не существует, и задача берётся в работу раньше своей
предпосылки.

Рубеж держит `devkitctl doctor`: в файле задачи другая живая задача названа
входом, а маркера «после» у строки нет, значит находка. Сегодня doctor проверяет
доску только на наличие (`devkitctl.py:1096`), про маркеры «после» там ни
строчки. Вместе с рубежом правило оседает в процедурах груминга и работы над
задачей: связь, найденная разбором, ставится командой `taskctl dep add`, а не
остаётся фразой в тексте.

## DoD

1. На синтетической доске задача, в чьём файле вход назван без маркера
   «после», даёт находку doctor; с маркером находки нет. Проверяется кодом
   возврата и строкой находки.
2. Правило записано в долгоживущих доках: процедуры груминга и работы над
   задачей называют постановку связи командой обязательным шагом.
3. Прогон `test` из `.devkit/deploy.local` зелёный.

## Приёмка

Вид agent: оба пункта проверяются прогоном на временном репозитории с
синтетической доской, эталон берётся из кода возврата и строки находки.

## Ход работы

- 2026-08-07: постановка диалогом после разбора приоритетов, строка заведена
  целью XL из трёх дыр (связи из груминга, порядок «что брать сейчас», гейт окна
  квоты в постановке цели). Бюджет 15/6 пп, потолка яруса нет.
- 2026-08-07: груминг, три ворот пройдены проверками. Дубля нет: накопитель
  просмотрен целиком, по Backlog смежная DK-071 (то же семейство doctor-находок,
  предмет другой), в архиве DK-050 закрывал отбор кандидатов batch, а не порядок
  внутри полосы, DK-010 и DK-012 корректируют вердикт pick по квоте, не старт
  цели.
- 2026-08-17: цель распущена грумингом DK-395, DK-397 и DK-168 одним заходом,
  журнал витков был пуст, цикл не запускался, терять нечего. Дыра про порядок
  «что брать сейчас» уехала в цель DK-395 пунктом 4 её нарезки: формула
  планировщика (score = R / W внутри полосы P, где W учитывает пройденный рубеж
  и плату за возврат) это тот же порядок, только богаче, и делать сначала
  простой порядок, а потом переписывать его под рубеж и плату значит платить
  дважды за один код. Гейт окна квоты в `goal-start` встал отдельной строкой
  DK-398. Эта строка держит первую дыру и из цели XL стала задачей S.

  Ранг пересчитан: 25+6+1+0+4 = 36. Серьёзность 25 (заметное трение, обход это
  ручная постановка связи, о которой вспоминают не всегда), ценность 6
  (улучшение потока, которое пользователь прямо попросил), неопределённость 1
  (подход назван целиком, осталось сделать), поправки на баг нет, рычаг 4
  (диспетчер и планировщик получают связи, которых раньше не видели). Цена S.
- Вычитка: 1 файл, 3 правки, 0 пометок, 2026-08-17.
- Разработка: подпроцесс glm-5.3/high по вердикту pick (квота: window5h_all 0%, week_all 33%, снимок 1м назад, сдвига нет), 2026-08-26 00:32-00:32.
- Обкатка сценария на ветке, 2026-08-26: шаги 1 и 2 прогнаны на временном
  репозитории, вывод вложен в сценарий. Doctor по живой доске devkit дал
  одну находку. DK-161 называет входом DK-145 (фраза «DK-145 (прогресс цели)
  читает Журнал как вход»), маркера нет ни у одной из строк. Это настоящий
  неразобранный случай из того же семейства, разбор вынесен в отчёт задачи.
- Ревью: подпроцесс glm-5.3/high по вердикту pick (квота: window5h_all 0%, week_all 33%, снимок 47м назад, протух, сдвига нет), 2026-08-26 01:18-21:54.
- 2026-08-28: после выката прогнан шаг 3 сценария, вывод в разделе сценария.
  Doctor из установленной обвязки нашёл названный вход на свежей доске и после
  `taskctl dep add` строку про связь печатать перестал.

## Ревью

- Файл задачи бьёт сам себя: абзац обкатки в «Ходе работы» цитирует фразу-триггер из живого случая DK-161 в абзаце, где названы DK-161 и DK-145, маркеров у обеих пар нет, и после слияния doctor по доске devkit печатает две постоянные находки по docs/tasks/DK-168.md. Проверено прогоном новой проверки на слитом состоянии: доска main после 76bcc7f3 плюс файл задачи из ветки дают ровно эти две строки. Исправлять перефразированием абзаца, чтобы цитата не совпадала с маской, либо пропусканием цитат в «ёлочках»: в настоящем случае DK-161 фраза стоит вне кавычек, а эта цитата целиком внутри них: исправлено, цитаты в «ёлочках» вырезаются до поиска маски, слитое состояние прогнано и чисто
- README tools/devkitctl, раздел doctor: перечень «Список для случая, когда корень свой» перечисляет каждую проверку с условиями и причиной, а строки про новую находку там не появилось. Список стоит дополнить: связь, названная в файле задачи, обязана стоять маркером «после» хотя бы у одной из двух строк; у маски две формы фразы, живые задачи только; находка гасится маркером в любую сторону и на релизном чекауте молчит вместе с lint. Без строки свежий агент по одному README не разберёт условия срабатывания: исправлено, строка добавлена в перечень doctor рядом с пунктом lint
- Мелочи прозы. kit/skills/board-task/SKILL.md: «taskctl dep add <своей ID> <ID предпосылки>» расходится с корпусом в роде, справка taskctl пишет «ID делается после DEP-ID», точнее «<свой ID>». kit/skills/board-groom/SKILL.md: «Связь ставится маркером `taskctl dep add`» читается так, будто маркер называется taskctl dep add, а маркер это «после»; точнее «ставится командой». Шаг 3 сценария: «молчит с маркером» неточно, восемь машинных находок остаются и doctor не молчит, а перестаёт печатать строку про связь, как сформулировано в шаге 2: исправлено, поправлены все три места: свой ID, ставится командой, шаг 3 переформулирован
- tools/devkitctl/README.md:270-273. Абзац про релизный чекаут из того же раздела doctor перечисляет, какие пункты списка молчат «вовсе»: git-хуки, `taskctl lint` вместе со всей обвязкой выката и порог веса. Новая проверка связей в коде стоит под тем же гейтом `in_git and not release_self`, что lint (devkitctl.py:2111), то есть на релизном чекауте самого devkit молчит, а в перечне гаснущих не названа. Замечание первого ревью прямо требовало донести это условие («на релизном чекауте молчит вместе с lint»), в новой строке 142 его нет, и свежий агент по одному README решит, что проверка связей на релизе продолжает работать. Дополнить перечисление в абзаце 272 новой проверкой либо добавить оговорку в сам пункт 142.: исправлено, проверка входов названа в перечне гаснущих рядом с обвязкой lint

## Сценарий проверки

1. Синтетическая доска. `git init /tmp/dk168-scene && python3
   tools/devkitctl/devkitctl.py new --prefix BD -C /tmp/dk168-scene`, в
   `.devkit/deploy.local` непустые `deploy =` и `test =`. На доске BD-2 в
   In progress и BD-1 в Backlog, у обеих файлы. Файл BD-1 называет вход
   фразой «Результат BD-2 нужен как вход», маркера «после» у строки нет.
   `python3 tools/devkitctl/devkitctl.py doctor -C /tmp/dk168-scene`
   отвечает кодом 1 и печатает находку про названный вход.
2. `taskctl -C /tmp/dk168-scene dep add BD-1 BD-2` печатает «BD-1: после
   BD-2». Повторный doctor строку про вход не печатает, счётчик находок
   падает на единицу. Код возврата в обоих прогонах 1, восемь машинных
   находок отстают от раскладки чекаута и к рубежу не относятся.
3. После выката. `devkitctl doctor -C /tmp/dk168-scene` из обновлённой
   установки на той же доске печатает находку из шага 1 без маркера, а с
   маркером строку про связь не печатает, как во втором прогоне шага 2.
   Шаг гоняет выкаченный рубеж, тесты ветки он не повторяет.

Обкатка шагов 1 и 2 на ветке, 2026-08-26. Машинные находки и таблица веса
вырезаны, их восемь.

```
$ python3 tools/devkitctl/devkitctl.py doctor -C /tmp/dk168-scene; echo rc=$?
находок: 9
docs/tasks/BD-1.md: BD-2 названа входом, а маркера «после» нет ни у одной из строк; поставить связь taskctl dep add
rc=1
$ taskctl -C /tmp/dk168-scene dep add BD-1 BD-2
BD-1: после BD-2
$ python3 tools/devkitctl/devkitctl.py doctor -C /tmp/dk168-scene; echo rc=$?
находок: 8
rc=1
```

Прогон шага 3 после выката, 2026-08-28. Доска собрана заново, doctor зовётся
из установленной обвязки (`devkitctl` в PATH), а не из дерева. Машинные находки
и таблица веса вырезаны, их пять.

```
$ devkitctl doctor -C /tmp/dk168-check; echo rc=$?
находок: 6
docs/tasks/BD-1.md: BD-2 названа входом, а маркера «после» нет ни у одной из строк; поставить связь taskctl dep add
rc=1
$ taskctl -C /tmp/dk168-check dep add BD-1 BD-2
BD-1: после BD-2
$ devkitctl doctor -C /tmp/dk168-check; echo rc=$?
находок: 5
rc=1
```

## Выкат

- 2026-08-28 слито: 47572226, e0d81a79, e611d843, 3350327a, 2e909187, bcd4239e, aba3f988
- smoke прогнан, 2026-08-28
