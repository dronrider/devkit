# DK-174: Скилл командной работы над одним проектом: захват задачи и разбор столкновений

Доска одна, а работают по ней уже несколько: пачка `board-batch` гоняет
исполнителей параллельно, каждый в своём дереве, и вторая подписка добавит
второе живое окно. Половина фундамента под это в devkit стоит: дерево на задачу
(`shipctl start`), статусы доски, правило немедленного пуша коммитов доски,
ребейз ветки до ревью.

Чего нет, так это процедуры. Кто и как захватывает строку, что делать, когда
доска ушла вперёд или строка уже занята, чьё дерево где лежит: сегодня это
держится на том, что за доской сидит один человек и помнит всё сам. Правила
devkit писались под одиночную работу, и места, где одиночная и командная
расходятся, нигде не названы.

## Чего хотим

Скилл в `kit/skills`, который собирает стоящий фундамент в процедуру:

- захват задачи это перевод в In progress с немедленным пушем коммита доски, а
  перед захватом pull доски: строка на origin и есть общий источник правды;
- работа только в своём дереве задачи, основной чекаут остаётся на main;
- названы столкновения и разбор каждого: строка уже захвачена другим, доска
  ушла вперёд и пуш отбит, дерево задачи занято, ревьювер и исполнитель гребут
  один файл;
- сказано, чем командная работа отличается от одиночной там, где правила
  расходятся, и где скилл кончается, отдавая работу `board-batch` и
  `board-ship`.

## Развилки исполнителю

1. Кто арбитр захвата: git (чей коммит строки лёг в origin первым, тот и взял)
   против отдельного замка. Ведущий вариант первый, замка не заводить: доска и
   так общий источник правды, а второй механизм разъедется с первым в первый же
   обрыв.
2. Нужен ли `taskctl` свой рычаг под захват (pull, перевод и push одним шагом)
   или процедура собирается из имеющихся команд. Ведущий вариант второй:
   сначала процедура из того, что есть, а рычаг отдельной строкой доски, если
   процедура окажется длиннее пары шагов.
3. Граница со скиллами `board-batch` и `board-ship`: что скилл говорит своими
   словами, а на что ссылается, чтобы две процедуры не разъезжались.

## Ранг

`25+7+2+0+4 = 38`, P2.

- Серьёзность 25 (Major). Параллельная работа по одной доске идёт уже сейчас, и
  столкновения разбираются на глаз. Обход есть и он дешёвый (внимательность и
  договорённость), но с числом рук он не масштабируется.
- Ценность 7. Третий пункт DoD цели и то, ради чего цель звала усилители
  параллельности: без процедуры второе окно на второй подписке упирается в ту
  же доску.
- Неопределённость 2. Фундамент стоит, подход выбран, открыты набор столкновений
  и граница с соседними скиллами. Границ чужих процессов правка не пересекает,
  это текст скилла плюс его проверка.
- Поправка на баг 0. Новая работа.
- Рычаг 4. Процедура делает каждую следующую параллельную задачу дешевле, как
  любая часть живого цикла разработки.

## Сценарий проверки

Агентский.

1. `python3 kit/skills/check-skills.py` выходит 0.
2. Прогон процедуры захвата на синтетической доске: временный `HOME`, общий
   голый репозиторий и два клона. Первый клон захватывает строку и пушит, второй
   по процедуре видит занятую строку и её не перетирает, и видно это состоянием
   строки после pull и кодом возврата отбитого пуша.
3. Скилл разложен на машину: `devkitctl doctor` зелёный, файл скилла лежит в
   каталоге скиллов харнеса.

## Решения по развилкам

1. Арбитр захвата это git, как и вёл ведущий вариант. Замка нет, задачу взял
   тот, чей коммит строки лёг в origin первым, а проигравшему об этом говорит
   отбитый пуш.
2. Своего рычага у `taskctl` не заведено: процедура собралась из команд, которые
   и так зовутся (`git pull --rebase`, `taskctl show`, `taskctl move ... --push`,
   `shipctl start`). Про рычаг в скилле сказано вслух, чтобы следующий заход не
   переоткрывал развилку.
3. Граница с соседями держится ссылками. Своими словами в скилле только то,
   чего в одиночной работе нет (арбитр, захват, столкновения), а строка, ветка,
   ревью, слияние и отбор пачки уходят в `board-task`, `board-ship` и
   `board-batch` разделом «Где процедура кончается».

Отступление от постановки одно, и его нашёл прогон сценария. Заготовка разбора
опиралась на конфликт в `docs/TASKS.md` как признак того, что строку взял сосед.
Живой git ведёт себя иначе: захват, совпавший с чужим до строчки, ребейз
отбрасывает как уже применённый (`skipped previously applied commit`) и никакого
конфликта не показывает. Поэтому в скилле исход захвата решается состоянием
строки после `taskctl show`, а конфликт остаётся лишь одним из двух путей туда.

## Сценарий проверки (агентский)

1. `python3 kit/skills/check-skills.py` выходит 0.
2. Синтетическая доска на временном `HOME`: голый репозиторий, два клона со
   своими подписями. Первая рука зовёт
   `taskctl move XR-001 in-progress -m "..." --push`, вторая по устаревшей доске
   зовёт то же самое. Ожидание: пуш второй руки отбит с кодом 1 и словом
   `rejected`, а после `git pull --rebase` строка стоит в `in-progress` коммитом
   первой руки, и `taskctl lint` зелёный.
3. `devkitctl doctor --fix` на временном `HOME` кладёт `board-team` в
   `~/.claude/skills/`, повторный `doctor` находок про скиллы не даёт.

## Ход работы

- Исполнение: субагент opus/medium по вердикту pick (квота: week_all 58%, снимок 13м назад, сдвига нет), 2026-08-07.
- Скилл `kit/skills/board-team/SKILL.md`, проверка `check_team` в
  `kit/skills/check-skills.py` с шестью юнитами в `check_skills_test.py`,
  перечень скиллов в `README.md` пополнен.

Прогон сценария, 2026-08-07:

```
ok: скиллы, проверено 9
exit=0
=== 1. общий голый репозиторий и синтетическая доска ===
=== 2. обе руки видят строку свободной ===
XR-001 в backlog
| XR-001 | первая | task | P2 | 33 (25+5+1+0+2) | S | [tasks/XR-001.md](tasks/XR-001.md) |
XR-001 в backlog
| XR-001 | первая | task | P2 | 33 (25+5+1+0+2) | S | [tasks/XR-001.md](tasks/XR-001.md) |
=== 3. первая рука захватывает и пушит ===
XR-001: backlog -> in-progress, коммит fe688b9, запушено
код возврата захвата: 0
=== 4. вторая рука захватывает по устаревшей доске ===
код возврата отбитого пуша: 1
 ! [rejected]        main -> main (fetch first)
hint: Updates were rejected because the remote contains work that you do not
=== 5. разбор по процедуре: pull --rebase ===
warning: skipped previously applied commit ade3b5f
конфликт в доске: []
=== 6. строка после разбора: захвачена первой рукой, не перетёрта ===
XR-001 в in-progress
| XR-001 | первая | task | P2 | 33 (25+5+1+0+2) | S | [tasks/XR-001.md](tasks/XR-001.md) |
fe688b9 docs(tasks): XR-001 взята первой рукой
доска и архив в порядке
```

Раскладка на машину, тот же день, временный `HOME` и клон ветки:

```
починено: установлено 9 скиллов в /tmp/dk174doc.1osmWo/home/.claude/skills: board-batch,
board-groom, board-ship, board-task, board-team, goal-loop, goal-start, live-core, test-standard
--- файл скилла на машине:
SKILL.md
--- повторный doctor, находки про скиллы:
повторный doctor: находок про скиллы нет
```
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 58%, снимок 29м назад, сдвига нет), 2026-08-07.

## Ревью

- board-team/SKILL.md, раздел «Несколько рук по одной доске»: цитата «немедленный пуш коммита доски это п. 9 раздела «Ветки, ревью и деплой», дерево на задачу п. 3 того же раздела» неверна по номерам и разделу. П. 9 раздела «Ветки, ревью и деплой» это «Поезд выката» (п. 3 того же раздела это «Замечания ревью живут в файле задачи»). Немедленный пуш доски это п. 9 раздела «Трекинг задач» («Синхронизация доски»), а дерево на задачу это п. 1 раздела «Ветки, ревью и деплой» (проверено чтением RULES.board.md). Исправить ссылки на точные номера и разделы, иначе сверка скилла с правилом ведёт не туда.
