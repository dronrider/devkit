# DK-184: Скилл proofread для вычитки прозы с парами, словарём и сторожевым корпусом

записан 2026-08-12, первая задача реализации серии DK-173

## Постановка

Первая задача реализации принятого LLD `docs/lld/DK-173-agent-prose.md`.
Материал для пар и типологии лежит в исходном разборе
`docs/tasks/DK-173.md`: 16 примеров с комментариями пользователя и
выведенная из них типология из восьми повторяющихся проблем. Открытые
вопросы LLD решены пользователем 2026-08-08: словарь (а), «ярус» меняется
на «уровень» (б), эталон стиля при споре это норма жанра (а).

Создаётся новый скилл `kit/skills/proofread/`:

- `SKILL.md` с процедурой вычитки. На входе список файлов, вычитку гонит
  субагент со свежим контекстом, не тот, что писал текст, по тому же
  доводу, по которому ревью не отдают автору кода. Модель sonnet: haiku
  не вытягивает суждение о прозе, opus для страницы избыточен. На выходе
  правки формы прямо в файлах и короткий список помеченного, но не
  исправленного: спорные слова, кандидаты в словарь.
- Пары «плохо/хорошо» по восьми пунктам типологии из DK-173, по паре на
  пункт. Одна копия, в доку не дублируются.
- Словарь devkit: поезд, виток, лестница, накопитель, сторожок, заход,
  ворота, рубеж, дорезка. Вычитка помечает слово, которого нет ни в
  словаре, ни в общем техническом русском. Пополняет словарь
  пользователь.
- `corpus.md` со сторожевым прогоном: 16 фраз из DK-173. На плохой
  половине вычитка обязана дать находку по каждому пункту типологии, на
  эталонной, переписанные DK-089, DK-117 и DK-165 после калибровки,
  пройти почти без замечаний. Пороги вида «не меньше 14 находок из 16» и
  «не больше двух ложных на эталоне» фиксируются первым прогоном при
  реализации и записываются в сценарий проверки. Вычитка
  недетерминирована, требовать всех 16 из 16 значит сделать прогон
  плавающим.

Границы вычитки записаны в самом скилле: правится форма, а факты, числа,
ID задач, команды, пути и термины словаря не трогаются. Изменение смысла
при вычитке это порча, а не редактура.

Правки в долгоживущей доке:

- `RULES.core.md`, раздел «Мимикрия»: одна строка, долгоживущий текст
  перед коммитом проходит вычитку скиллом `proofread`;
- `RULES.md`, разбор мимикрии: ссылка на скилл и фраза про одну копию
  пар;
- `README.md` devkit, список скиллов: строка про `proofread`.

Формат следа отработки живёт в самом скилле и пишется в раздел «Ход
работы» файла, к которому относится текст: «Вычитка: 2 файла, 5 правок,
1 пометка, ГГГГ-ММ-ДД». Строка пишется и при нуле правок, иначе чистый
текст неотличим от пропущенного вызова.

В эту задачу не входят точки вызова, правка `board-ship`, определения
`review-*` и `exec-xhigh`, `board-groom`, нарезка `goal-loop`, и заход по
написанному. То и то отдельные задачи серии [DK-185], [DK-186], [DK-187],
они идут после этого скилла.

## DoD

Скилл `proofread` лежит в `kit/skills/`, вызывается списком файлов и
правит форму по типологии DK-173, не трогая факты, команды и термины
словаря. Сторожевой прогон по `corpus.md` держит порог, зафиксированный
первым прогоном и записанный в сценарии проверки. В `RULES.core.md`,
`RULES.md` и `README.md` лежат соответственно строка про вычитку, ссылка
на скилл и пункт списка скиллов. Тест по стандарту devkit на сторожевом
корпусе; сценарий проверки пишет исполнитель.

## Ход работы

- Исполнение: подпроцесс glm-5.2/medium по вердикту pick (квота: объявления нет, корректор выключен), 2026-08-12.
- Состав скилла `kit/skills/proofread/`: `SKILL.md` (процедура), `pairs.md`
  (восемь пар плохо/хорошо по типологии DK-173), `dictionary.md` (девять
  терминов devkit), `corpus.md` (16 плохих фраз и эталонная половина из
  DK-089, DK-117, DK-165). Структурные проверки в `kit/skills/check-skills.py`
  (`check_proofread`), юниты в `kit/skills/check_skills_test.py`
  (`TestProofread`, восемь случаев).
- Долгоживущая дока: `RULES.core.md` (раздел «Мимикрия», строка про вычитку),
  `RULES.md` (разбор мимикрии, ссылка на скилл и фраза про одну копию пар),
  `README.md` (список скиллов, строка про proofread).
- Ревью: подпроцесс glm-5.2/high по вердикту pick (квота: объявления нет, корректор выключен), 2026-08-12.

## Сценарий проверки

Символы и структура:

```
python3 hooks/check-symbols.py RULES.core.md RULES.md README.md \
    kit/skills/proofread/SKILL.md kit/skills/proofread/pairs.md \
    kit/skills/proofread/dictionary.md kit/skills/proofread/corpus.md \
    kit/skills/check-skills.py kit/skills/check_skills_test.py
python3 kit/skills/check-skills.py
python3 -m unittest kit.skills.check_skills_test
```

Сторожевой прогон корпуса. Гоняется субагентом со свежим контекстом (не
автор текстов) по модели sonnet: haiku не вытягивает суждение о прозе, opus
для страницы избыточен. Субагенту передаются список файлов скилла и корпус,
на выходе находки по 16 плохим фразам и число ложных на эталонной половине.
Пороги зафиксированы первым прогоном 2026-08-12:

- плохая половина: не меньше 14 находок из 16;
- эталонная половина: не больше двух ложных.

Прогон гоняется при правке скилла, пар или словаря, в CI не встаёт (стоит
модельные токены).

### Вывод первого прогона (2026-08-12)

```
ПЛОХАЯ ПОЛОВИНА
1. пункт 1: двоеточие вместо длинного тире, фраза развалилась
2. пункт 1: двоеточие вместо длинного тире, требуется перестройка
3. пункт 2: «входа нет» вместо «нет входа» - инверсия порядка слов
4. пункт 2: «Наблюдать за ходом витка нечем» - инверсия
5. пункт 3: «честно говорит» - одушевление утилиты
6. пункт 3: «называет диагностика», «говорит», «не молчит» - одушевление
7. пункт 4: «неузнанная панель» - выдуманный термин
8. пункт 4: «без стопа витка» - выдуманный термин
9. пункт 5: пять фактов в одном предложении, читать приходится дважды
10. пункт 5: перегруженное предложение с придаточными
11. пункт 6: заголовок зашифрован во внутреннем жаргоне «ярус», «тратит»
12. пункт 6: «захват задачи и разбор столкновений» - загадка без контекста
13. пункт 7: запись тащит лишнюю информацию о квоте и возрасте снимка
14. пункт 7: «звался, а не забывался» - канцелярит
15. пункт 8: «ярус» вместо «уровень» или «модель»
16. пункт 8: «дыра в devkit» - неудачное слово для понятия

НАЙДЕНО НАХОДОК: 16 / 16
НЕ НАЙДЕНО ПУНКТОВ: []

ЭТАЛОННАЯ ПОЛОВИНА
- нет ложных находок

ЛОЖНЫХ НАХОДОК: 0
```

Итог прогона: плохая 16/16, эталон 0 ложных. Пороги 14/16 и две ложных
держатся с запасом, при недетерминированности вычитки это рабочая граница,
а не плавающий прогон.

## Ревью

- Правочное. Собственный текст скилла и README несут три грамматические ошибки того самого класса, который скилл обязан ловить: README.md:152 «в долгоживящих текстах» (норма по всему дереву «долгоживущих»); SKILL.md:21 «между абзацами граница поведения нет» (при «нет» нужен родительный: «границы поведения нет»); SKILL.md:34 «с явной передаче списка файлов» (при «с» нужен творительный: «с явной передачей»). Скилл вычитки, выходящий с такими ошибками в собственном теле, держит не ту норму, которую предъявляет.: исправлено, исправлено в fa8af354: долгоживущих, границы поведения, явной передачей
- Заметка. В corpus.md:21-22 «на который должна дать находку вычитка» та же инверсия порядка слов, пункт 2 той типологии, которой корпус учит. Прямой порядок «вычитка должна дать находку». Постпозиция субъекта в относительном придаточном допустима, но это рамочная проза корпуса, и место для неё сомнительное.: исправлено, прямой порядок в corpus.md, fa8af354
