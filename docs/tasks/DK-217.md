# DK-217: Каркас дашборда: сервер с аутентификацией и доска проектов на чтение [после DK-215, DK-216]

записан 2026-08-09

Задача цели DK-112 ([DK-112.md](DK-112.md)).

## Чего хотим

Первый живой срез дашборда: Go-сервер с вшитой статикой по раскладке из
DK-215, вход по своей аутентификации, доска на чтение. Сервер читает список
корней из конфига, проектом считает каталог с `docs/TASKS.md`, доску и
статусы берёт через taskctl тем способом, который назвал LLD, своей базы нет.
Экран доски по макету DK-216: задачи и цели со статусами нескольких проектов,
видно, что сейчас в работе. Правок доски в этом срезе нет.

Состав:

- инструмент в `tools/` под именем из LLD, go, статика вшита в бинарь;
- вход по механизму из LLD, без входа не отдаётся ни одной строки данных;
- API на чтение: список проектов, доска проекта со статусами и текущими
  работами;
- экран доски под телефон и ноутбук;
- конфиг корней, запуск процесса и выкат по решению LLD; нехватку раскладки
  называет диагностика, а не молчание.

После слияния и выката дашборд стоит на машине и отвечает из домашней сети с
телефона: наблюдение за агентами работает само по себе, и цикл может встать
на любой следующей строке без полуфабриката на проде.

## Ранг

`25+8+3+0+4 = 40`, P2.

- Серьёзность 25 (Major). Первая половина основного сценария цели: доска
  видна без машины. Обход есть, сидеть за машиной, поэтому Major, а не
  Critical.
- Ценность 8. «Видно, над чем агенты работают прямо сейчас» это первый пункт
  цели, ради наблюдения она и затевается.
- Неопределённость 3. Подход решён LLD, но правка пересекает границу
  сервер-клиент, порог RANKING.md: развилки стыка видны живым прогоном, а не
  чтением исходников.
- Поправка на баг 0. Новая работа.
- Рычаг 4. Сервер, вход, статика и конфиг это каркас, на котором едут все
  следующие срезы, каждая задача серии дешевле.

## Сценарий проверки

Агентский.

1. Собранный дашборд поднимается на свободном порту с конфигом, где корень
   поиска указывает на временный каталог с синтетическим проектом (фикстурная
   `docs/TASKS.md`); окружение задаётся явно, живая доска не участвует.
2. Запрос без входа получает отказ (код по решению LLD: 401 или редирект на
   вход), в теле нет ни одной строки доски.
3. После входа API проектов отдаёт синтетический проект, API доски отдаёт его
   задачи со статусами, коды 200, строки сверяются с фикстурой командой.
4. Экран доски отдаётся тем же сервером из вшитой статики, в HTML нет ссылок
   на внешние хосты (греп по ответу).
5. На проде без правок: GET к стоящему на машине дашборду после входа отдаёт
   живую доску devkit, шаги ничего не меняют.

## Ход работы

- Исполнение: субагент opus/high по вердикту pick (квота: week_all 87% профицит, week_max 69% профицит, снимок 4ч 22м назад, протух, сдвига нет), 2026-08-10.
- Исполнение: субагент fable/high по вердикту pick (маппинг opus, корректор: профицит week_all, week_max; квота: week_all 88% профицит, week_max 70% профицит, снимок 0м назад), 2026-08-10.
- 2026-08-10, сделано по LLD DK-112 без новых развилок:
  - taskctl: флаг `--json` у `list`, `show` и `dep list` (json.go), печатный
    вывод не тронут, `list --json` отдаёт Backlog целиком; тесты json_test.go;
  - tools/dashboard: go-сервер, команды `serve` и `secret [--rotate]`; вход
    токеном с HMAC-кукой на 30 дней, пауза после провала, Origin на
    изменяющих, CSP; конфиг `~/.devkit/dashboard.local` (root повторяемый,
    addr/port, token, права 600, незнакомый ключ ошибка старта); API
    `/api/projects` (счётчики секций, живые работы из tmux по префиксу доски
    и реестра целей по корню) и `/api/projects/{p}/board` подпроцессом
    `taskctl list --json`; `/healthz` без входа с версией и ошибками конфига;
    статика экрана доски по макету DK-216 вшита `go:embed`, ключ `--static`
    для разработки, оба форм-фактора, внешних хостов нет; журнал
    `~/.devkit/dashboard.log`; самонаблюдение за инодой бинаря под выкат;
  - devkitctl: doctor проверяет и `--fix` кладёт launchd-агент
    `ru.devkit.dashboard` (dashboard.py по образцу watch.py, после проверки
    бинарей), с worktree ветки задачи доводка отказывается, как у сторожка;
    живость меряется по `/healthz` после первого старта (конфиг кладёт сам
    serve), ошибки конфига оттуда становятся находкой; стенд testenv сеет
    агента дашборда рядом со сторожком; в сборку build инструмент попал сам
    по правилу tools/*/go.mod, в ключ test обвязки `.devkit/deploy.local`
    на машине dashboard дописан этой же задачей;
  - дока: tools/dashboard/README.md заведён, tools/taskctl/README.md про
    `--json`, tools/devkitctl/README.md про носителя, README.md devkit
    пополнен именем в списке go-инструментов;
  - сценарий проверки: шаги 1-4 прогнаны на синтетическом проекте (порт 7891,
    временный HOME), без входа 401/302 и ни одной строки доски, после входа
    строки сходятся с фикстурой, статика без внешних хостов; шаг 5 на проде
    за диспетчером после выката.

## Ревью

- Нет теста на паузу после провала входа. LLD DK-112, раздел «Тесты», называет её прямо, а tools/dashboard/server_test.go только обнуляет loginPause в newTestEnv и больше к ней не возвращается: выкинь строку time.Sleep(loginPause) из handleLogin (server.go:177), и весь пакет останется зелёным. Нужен тест, который эту мутацию красит: поднять сервер с ненулевой паузой и померить, что провал входа занял её, а верный вход нет.
- Секрет входа может уехать в журнал с правами 644. config.go:75 на строке без знака равенства печатает её целиком («жду строку «key = value», получил %q»), а cmdServe отдаёт эту ошибку в fatal, то есть в stderr, который launchd-агент льёт в ~/.devkit/dashboard.log. Достаточно руками написать «token abc123» без «=», и секрет из файла с правами 600 ляжет в журнал, который читает кто угодно. Печатать в этой ошибке номер строки без её содержимого, тест на то, что секрета в тексте ошибки нет.
- У демона нет ни одного таймаута, а слушает он все интерфейсы. http.Server в main.go:159 собран голым: без ReadHeaderTimeout молчащее соединение висит вечно, и набрать их можно сколько угодно. Рядом exec.Command(taskctl ...) в board.go:31 и tmux в board.go:105 идут без контекста со сроком: подвисший подпроцесс держит горутину запроса и после того, как клиент ушёл. WriteTimeout ставить нельзя, дальше по серии едет SSE, а ReadHeaderTimeout и срок подпроцессам поставить надо, с тестом на то, что зависший taskctl оборачивается ошибкой ответа, а не вечным ожиданием.
- show --json теряет заголовок черновика. В json.go:154 findDraft возвращает Draft с полем Title, но в ответ уходит только id, sect и file, тогда как печатный show отдаёт и заголовок, и текст. Машинному читателю про черновик известно меньше, чем человеку, а поле уже под рукой: положить Title в jsonRow и накрыть тестом рядом с TestShowJSONArchiveAndMissing.
