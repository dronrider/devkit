# DK-218: Запуск и стоп задачи и цели с дашборда [после DK-217]

записан 2026-08-09

Задача цели DK-112 ([DK-112.md](DK-112.md)).

## Чего хотим

Управление с дашборда: запустить задачу или цель в работу и остановить.
Запуск зовёт тот же механизм, что и руками (какой именно, решил LLD DK-215:
goal-run для цели, конвейер задачи для строки), стоп это стоп сессии, и
интерфейс честно называет его стопом: паузы нет, возобновление это новый
виток, читающий состояние с диска. Кнопки на экранах доски и задачи по макету
DK-216, те же действия доступны по API, на них дальше встаёт smoke DK-222.

## Ранг

`25+8+3+0+3 = 39`, P2.

- Серьёзность 25 (Major). Вторая половина основного сценария: без запуска и
  стопа дашборд остаётся смотрелкой, за управлением по-прежнему садиться за
  машину. Обход дешёвый и очевидный, полоса Major.
- Ценность 8. Полное управление процессом с любого устройства это заявленная
  ценность цели, запуск и стоп её ядро.
- Неопределённость 3. Граница сервер-клиент плюс стык с процессами: tmux,
  goal-run, живые сессии; что вылезет на стыке, покажет прогон.
- Поправка на баг 0. Новая работа.
- Рычаг 3. Цели запускаются и снимаются без привязки к рабочему месту, каждый
  следующий виток любой цели стартует раньше.

## Сценарий проверки

Агентский.

1. На синтетическом проекте с задачей-фикстурой (временный HOME, свой корень,
   окружение задаётся явно) запуск через API отвечает 200, строка переходит в
   In progress (`taskctl show`), процесс исполнителя виден тем признаком,
   который назвал LLD (pid или tmux-сессия).
2. Стоп через API отвечает 200, процесса больше нет, ответ и экран называют
   состояние стопом; слова «пауза» в статике нет (греп).
3. Повторный запуск после стопа поднимает новый виток, читающий состояние с
   диска.
4. Живая доска в сценарии не участвует, прод шаги не меняют.

## Ход работы

- Исполнение: субагент fable/high по вердикту pick (маппинг opus, корректор: профицит week_all, week_max; квота: week_all 92% профицит, week_max 79% профицит, снимок 25м назад), 2026-08-10.
- Код: ручки `POST /api/projects/{p}/runs` и `DELETE /api/projects/{p}/runs/{id}`, экран задачи с кнопкой «В работу»/«Стоп» и стоп на карточках живых работ. Цель поднимает `goal-run.py`, найденный по корням конфига; задача это tmux-сессия `task-<ID>` с `claude -p` и промптом про скиллы board-task и board-ship (промпт квотится одной строкой, tmux склеивает хвост new-session пробелами). Отказы называются словами: строка не на доске, живая сессия, занятый замок (код 3 оболочки это 409), пропавшие tmux, claude, goal-run; цель из реестра без tmux-сессии на стоп отвечает «ведётся снаружи». 2026-08-10.
- Сценарий, шаги 1-3: прогнан на синтетическом проекте (временный HOME, свой корень, фикстуры claude и tmux на своём сокете, доска настоящим taskctl). Запуск 200, строка в In progress, tmux-сессия task-XR-001 видна; стоп 200 со state «стоп» и снятой сессией, греп слова «пауза» по статике пуст (это же держит тест TestStaticNoPauseWord); повторный запуск поднял новую сессию поверх состояния с диска. 2026-08-10.
- Ревью: субагент opus/high по вердикту pick (маппинг opus, корректор: профицит week_all, week_max; квота: week_all 94% профицит, week_max 81% профицит, снимок 0м назад), 2026-08-10.

## Ревью

- Стоп не привязан к проекту, которым его позвали, и снимает чужую сессию. DK-217 завёл правило: tmux-сессии на машине общие, к проекту они привязываются префиксом ID с его доски (liveWorks в board.go). handleRunStop это правило не применяет: он берёт общий список tmux-сессий и бьёт по goal-<id>/task-<id>, а found участвует только в goalLedOutside и в -C у --say. Проверено тестом на месте: доска demo с префиксом XR, живая сессия goal-DK-777, запрос DELETE /api/projects/demo/runs/DK-777 отвечает 200 «стоп», снимает kill-session -t =goal-DK-777 и до этого зовёт goal-run.py DK-777 -C <корень demo> --say, то есть заводит журнал .devkit/goal-DK-777.log в чужом проекте. У запуска такой дыры нет, там строка ищется на доске именно этого проекта, и стоп обязан быть симметричен: искать id среди liveWorks(found.Path, prefix, s.cfg.Home) и на промах отвечать 404 «работа не идёт», а не резать общий список. Тест на этот случай тоже нужен.
