# DK-220: Переписка с агентом через состояние цели [после DK-219]

записан 2026-08-09

Задача цели DK-112 ([DK-112.md](DK-112.md)).

## Чего хотим

Переписка так, как решила цель: ход и ответы читаются из транскрипта сессии
(API из DK-219), а сообщение человека дашборд кладёт в состояние цели, откуда
его берёт следующий виток. Писать в идущий процесс цель не берётся, механики
для этого в devkit нет, двусторонний живой чат остаётся черновиком DK-136.

В задачу входит и сторона витка: goal-loop читает сообщение из состояния цели
и отдаёт его витку, формулировка держится тестами скилла. Формат и место
записи назвал LLD DK-215. Экран переписки по макету DK-216, с честной
надписью, что сообщение уйдёт следующему витку.

## Ранг

`25+8+3+0+2 = 38`, P2.

- Серьёзность 25 (Major). Ответ на wait-human сегодня требует сесть за
  машину; обход есть, но это трение ровно того сценария, ради которого цель
  затевалась.
- Ценность 8. Шаг DoD «отправить сообщение агенту и увидеть его подхваченным»
  и главный смысл управления с любого устройства: цель спрашивает, человек
  отвечает откуда угодно.
- Неопределённость 3. Граница сервер-клиент плюс стык с goal-loop: формат
  сообщения, порядок чтения витком, гонка с идущей записью состояния.
- Поправка на баг 0. Новая работа.
- Рычаг 2. Механика «сообщение в состоянии цели» пригодится и вне дашборда,
  ответ витку можно будет класть любым инструментом; польза своя и небольшая.

## Сценарий проверки

Агентский.

1. На синтетической цели (временный HOME, фикстурный файл цели) отправка
   сообщения через API отвечает 200, текст лежит в состоянии цели ровно в том
   месте и формате, которые читает виток; сверка файлом.
2. Тесты goal-loop держат формулировку про чтение сообщения из состояния
   цели, прогон зелёный.
3. Экран переписки показывает ход из фикстурного транскрипта и отправленное
   сообщение с пометкой, что оно уйдёт следующему витку.
4. Сквозной подхват живым витком проверяет smoke DK-222, здесь прод не
   трогается.

## Ход работы

- Исполнение: субагент fable/high по вердикту pick (маппинг opus, корректор: профицит week_all, week_max; квота: week_all 97% профицит, week_max 87% профицит, снимок 0м назад), 2026-08-10.
- Сделано. Сервер: `POST` и `GET /api/projects/{p}/goals/{id}/message`
  (messages.go): запись строкой «- дата, из дашборда: текст» в раздел
  «Входящие» файла цели, без раздела он встаёт перед «Журналом», коммит с
  пушем как у правки `docs/tasks/`, провал пуша полем `note`; отказы
  словами (строка не на доске, не цель, файла цели нет), GET отдаёт лежащие
  строки для пометки «ждёт витка». Сторона витка: SKILL.md goal-loop читает
  «Входящие» на шаге состояния и убирает подхваченное записью витка,
  формулировки держит goal_run_test.SkillInboxTests. Экран
  `#проект/chat/DK-NNN` по макету «04 Переписка»: лента текстовых реплик из
  транскриптного API DK-219, сообщения из «Входящих» с пометкой «ждёт
  витка», честная надпись про следующий виток, «Стоп цикла» поверх стопа
  DK-218, пустоты различимы. Уточнение в LLD: место раздела, GET той же
  ручки, схлопывание сообщения в одну строку.
- Прогоны 2026-08-10: `GOWORK=off go test -count=1 ./...` в tools/dashboard
  зелёный; `python3 -m unittest goal_run_test` (30) и `check_skills_test`
  (36) зелёные, `check-skills.py` ok (9). `regcheck --base main` подтвердил
  красноту на main и для tools/dashboard/messages_test.go, и для
  SkillInboxTests.
- Сценарий проверки: пункты 1 и 2 закрыты стендом messages_test.go
  (временный HOME, фикстурные доска и файл цели, сверка файлом) и тестами
  скилла; надписи пункта 3 держит TestStaticChatHonesty, живой экран
  смотрится после выката; пункт 4 остаётся за smoke DK-222.
- Ревью 2026-08-10: три замечания, все исправлены (исходы ниже). Пустые
  «Входящие» показываются словами note, отказ сверх предела 16 КБ называет
  предел (TestMessageBodyLimit), XR-002 получил файл в стенде и отказ «не
  цель» сверяется с нетронутой фикстурой: мутация isGoalTitle краснеет на
  записи в файл обычной задачи, проверено руками. Прогоны после правок:
  go test зелёный, regcheck --base main краснота подтверждена.
- Ревью: субагент opus/high по вердикту pick (маппинг opus, корректор: профицит week_all, week_max; квота: week_all 97% профицит, week_max 88% профицит, снимок 0м назад), 2026-08-10.

## Ревью

- Пустые «Входящие» на экране переписки молчат: loadPending при пустом pending просто ничего не рисует, а note сервера («во «Входящих» пусто: непрочитанных сообщений нет») роняет. README при этом обещает «Пустоты различимы: ... во «Входящих» пусто», и пустая коробка неотличима от неотрисованной. Либо показывать note словами и держать надпись тестом рядом с TestStaticChatHonesty, либо убрать обещание из README.: исправлено, loadPending показывает note сервера или свои слова, надпись держит TestStaticChatHonesty
- Предел размера сообщения есть (MaxBytesReader 16 КБ), но отбой врёт про причину: тело сверх предела падает в тот же ответ 400 «жду JSON {"text": "..."}», хотя JSON был нормальный. Проверено стендом: сообщение в 40 тысяч букв даёт ровно эти слова. Отказ надо называть своими словами (сообщение длиннее предела, столько-то), предел назвать в README ручки и закрыть тестом: сейчас его ни в доке, ни в messages_test.go нет.: исправлено, отказ сверх предела называет предел 16 КБ, предел в README, TestMessageBodyLimit на оба конца
- Стенд «не цель» не доказывает главного: у XR-002 на фикстурной доске нет файла docs/tasks/XR-002.md, и со снятой проверкой isGoalTitle отказ даёт соседний рубеж (404 про отсутствующий файл), а не запись в чужой файл. Проверено мутацией: краснота есть, но она про другое. Завести XR-002 файл задачи и сверить, что после отказа он не тронут: тогда тест держит именно «в файл не-цели сообщение не ложится».: исправлено, XR-002 получил файл в стенде, отказ сверяется с нетронутой фикстурой, мутация isGoalTitle краснеет на защите файла

## Выкат

- 2026-08-10 слито: b871ddb, 5340806, 7fbecfd, 32177ce, d9d4bc1, e710698, cc840fc
