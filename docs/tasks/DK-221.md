# DK-221: Правка задачи и цели с дашборда, зависимости в обе стороны [после DK-217]

записан 2026-08-09

Задача цели DK-112 ([DK-112.md](DK-112.md)).

## Чего хотим

Правка задачи и цели с дашборда: заголовок, слагаемые ранга, цена, файл
задачи. Зависимости видны наглядно и в обе стороны: кого ждёт строка и кто
ждёт её. Правки идут через taskctl (`set`, `dep`), а не мимо него: доска
остаётся источником правды, порядок строк выводится из ранга по RANKING.md,
произвольного перетаскивания мимо ранга нет. Экран задачи по макету DK-216.

## Ранг

`25+5+3+0+2 = 35`, P2.

- Серьёзность 25 (Major). Груминг и переранжировка без этой задачи требуют
  сесть за машину; обход дешёвый и очевидный, но трение сидит в заявленном
  сценарии полного управления.
- Ценность 5. Ощутимое удобство: бэклог разбирается с телефона. Наблюдение и
  запуск к этому моменту закрыты соседними строками, это углубление, а не
  ядро цели.
- Неопределённость 3. Граница сервер-клиент, свои развилки на стыке с доской:
  конкуренция с параллельной правкой доски идущим циклом, чем отбивается
  кривая разбивка ранга.
- Поправка на баг 0. Новая работа.
- Рычаг 2. Разобранный по дороге бэклог ускоряет следующий состав работ,
  польза своя и небольшая.

## Сценарий проверки

Агентский.

1. На синтетической доске правка заголовка, разбивки ранга и цены через API
   отвечает 200, строка изменена (`taskctl show`), сумма R и бакет P
   пересчитаны.
2. После правки ранга порядок в ответе API доски отсортирован по рангу;
   запроса «поставить строку на место N» у API нет.
3. API зависимостей на фикстуре с цепочкой A -> B -> C называет для B обе
   стороны: ждёт A, держит C.
4. Правка файла задачи через API доезжает до `docs/tasks/<ID>.md` фикстуры и
   видна повторным чтением.

## Ход работы

- Исполнение: субагент opus/high по вердикту pick (квота: week_all 0%, week_max 1%, снимок 0м назад, сдвига нет), 2026-08-10.
- Правка едет ручками задачи: `GET` и `PATCH /api/projects/{p}/tasks/{id}`,
  `POST` и `PUT .../file`, `POST` и `DELETE .../deps`. Строку меняет только
  taskctl подпроцессом, своей записи в доску сервер не завёл; сумму R, бакет P
  и место строки в Backlog считает утилита, ручки «поставить строку на место
  N» в API нет. Коммит доски идёт тем же порядком, что у сообщения витку, и
  провал пуша называется полем `note`. Экран задачи правит заголовок, тип,
  цену и слагаемые ранга по месту (подсказки из RANKING.md) и показывает
  зависимости в обе стороны с кнопками «Добавить» и «Снять», 2026-08-10.
- Сценарий проверки прогнан на синтетической доске настоящими бинарями
  taskctl и dashboard, с origin в локальном bare-репозитории: правка строки
  ответила 200 и дала «R 30 -> 80, P P2 -> P0», порядок доски в ответе API
  вывелся из ранга (XR-002 встала выше XR-001), запрос с полем `position`
  отбит словами, XR-002 названа обеими сторонами (после XR-001, держит
  XR-003), текст файла задачи виден повторным чтением, а три коммита доски
  ушли в origin.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 1%, week_max 2%, снимок 0м назад, сдвига нет), 2026-08-10.

## Ревью

- Сито ID (goalIDRe в taskRow) и сито dep (в handleTaskDepAdd) не подтверждены тестом: убрал их поочерёдно на мутации, tools/dashboard/tasks_test.go остался зелёным (go test -run TestTask ./...). Нужен кейс с кривым id/dep (например, содержащим слэш) на одной из шести ручек, отвечающий 400 «не похоже на ID задачи», иначе обход пути через {id} и {dep} ничем не защищён от регресса.: исправлено, тест TestTaskIDSieve гоняет «..%2F» через все шесть ручек и «../» в теле dep add; краснота снята ручной мутацией (regcheck тут пустой: сита новые, в main их не было): снятие сита в taskRow даёт 404 вместо 400 на всех шести, снятие сита в dep add даёт слова taskctl «нет ни на доске», в dep rm «не зависит от»

## Выкат

- 2026-08-10 слито: a591448, 5c47abf, 7e58c24, c3ae521
