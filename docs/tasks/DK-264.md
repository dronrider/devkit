# DK-264: cmdout: утилита-обёртка на общем go-модуле, полный вывод в файл, агенту выжимку

записан 2026-08-11

Первая очередь серии DK-137. Механизм общего модуля решён LLD DK-237 (каталог
`internal/` верхнего уровня, относительный `replace` в `go.mod` потребителя), и
этой задачей в модуль въезжает единственный примитив, ради которого механизм
заводился: захват вывода команды в файл с построением выжимки. Дизайн входа
(гибрид правила и мягкого PreToolUse-хука-напоминания) и формат выжимки лежат в
`docs/lld/DK-137-cmdout-wrapper.md` и здесь не пересказываются. Остальные
функции каркаса из замера DK-237 (`corp.go`, TOML, версия) едут следующими
волнами, не в этой задаче.

Две части одной задачей: бинарь без примитива не собирается, примитив без
бинаря некому позвать.

- Пакет `internal/frame`, функция захвата: запускает команду, полный вывод
  пишет в `.devkit/cmdout/<timestamp>-<slug>/out`, строит выжимку по формату
  LLD. Модуль `github.com/dronrider/devkit/internal`, относительный `replace`
  по LLD DK-237.
- Бинарь `tools/cmdout/` со своим `go.mod`, импортирует `internal/frame` и
  зовёт функцию захвата. Порог (4K символов или 100 строк), значимые паттерны
  и схема пути берутся из LLD без переоткрытия.

## DoD

- Бинарь `cmdout` собирается строкой `deploy` из `.devkit/deploy.local`,
  стоит в списке бинарей `devkitctl`, а `devkitctl doctor` на машине без него
  печатает находку через `check_binaries()`.
- Пакет `internal/frame` подключён к `tools/cmdout/` относительным `replace`
  по LLD DK-237, сборка переживает `GOWORK=off`, тарболл и голый `go build`.
- Прогон `cmdout <команда>` с длинным выводом печатает выжимку в фиксированном
  порядке полей (`exit`, `lines_total`, `lines_hidden`, `significant`, `tail`,
  `path`), и файл по `path` читается целиком. `lines_hidden` отличает «спрятали
  важное» от «важного не было» (DoD №2 цели DK-137).
- Ниже порога (4K символов или 100 строк) вывод отдаётся как есть, выжимка не
  строится.
- У пакета и бинаря своя пирамида тестов: пакет как библиотека (захват, порог,
  значимые строки, путь), бинарь тонкий (разбор аргументов, вывод).
- `tools/cmdout/` с `go.mod` по правилу раскладки, `internal/` пятым каталогом,
  `devkitctl doctor --layout` молчит.

## Ход работы

- Вердикт pick: (заполняется)
- Исполнение: подпроцесс glm-5.2/high по вердикту pick (квота: объявления нет, корректор выключен), 2026-08-11.
- Коммиты ветки dk-264:
  - `feat(internal): DK-264 пакет frame захвата вывода команды` (модуль
    `github.com/dronrider/devkit/internal`, функция `Capture`, тесты как
    библиотека);
  - `feat(cmdout): DK-264 обёртка над internal/frame, выжимка по порогу`
    (бинарь `tools/cmdout/` с `require` и относительным `replace` на
    `../../internal`, `.devkit/cmdout/` в `.gitignore`);
  - `docs(tasks): DK-264 ход работы и сценарий` (этот файл).
- Тесты: `GOWORK=off go test ./...` в `internal/` и `tools/cmdout/` зелёные,
  15+7 случаев. `go vet ./...` чист на обоих модулях.
- `devkitctl doctor --layout` молчит: «раскладка в порядке». `internal/` уже
  в `TOP_LEVEL` от DK-237, `lang_rule` пускает go в `internal` как в `tools/`.
- `check_binaries` увидит `cmdout` после слияния ветки в основной чекаут:
  `build.tools()` выводит бинари из дерева `tools/*/go.mod`, и `cmdout`
  попадает в список автоматически. Из worktree doctor бегает по основному
  чекауту, где `tools/cmdout/` ещё нет, поэтому находка появится на машине
  после слияния.
- Ревью: подпроцесс glm-5.2/high по вердикту pick (квота: объявления нет, корректор выключен), 2026-08-11.

## Сценарий проверки

Агентский. `cmdout` на команде с длинным выводом отдаёт код возврата,
`lines_total`, `lines_hidden`, значимые строки, хвост и путь; файл по пути
читается целиком, и `lines_total` сходится с числом строк в файле. На коротком
выводе выжимка не строится, вывод возвращается как есть. `devkitctl doctor` на
машине без бинаря печатает находку, после выката молчит. `devkitctl doctor
--layout` на `tools/cmdout/` и `internal/frame` молчит.

Проверено прогонами из worktree dk-264:
- `internal/frame`: `TestCaptureBelowThreshold` (короткий вывод, raw),
  `TestCaptureThresholdLines`/`TestCaptureThresholdRunes`/`TestCaptureThresholdMultibyte`
  (порог 100 строк и 4K рун, мультибайтный вывод не перескакивает по байтам),
  `TestSignificant` (5 маркеров, hidden = 115),
  `TestSignificantOverflow` (30 + пометка, hidden = 90), `TestSignificantTailOverlap`
  (significant в хвосте не считается дважды, hidden = 120), `TestPathWrittenToFile`
  (`os.Stat`, читается целиком), `TestExitCode`, `TestCaptureNoArgs`, `TestSlug`,
  `TestRender`/`TestRenderEmpty` (порядок полей LLD), `TestCaptureNotFound`,
  `TestSignificantMarkersGuard` (стартовый набор из LLD).
- `tools/cmdout`: `TestPrintVersion*`, `TestVersionFlagOnTheBinary` (сборка с
  `GOWORK=off`), `TestBinaryNoArgs` (exit 2 и usage), `TestBinaryShortOutput`
  (короткий вывод как есть), `TestBinaryLongOutput` (выжимка в порядке полей,
  path указывает на читаемый файл), `TestBinaryExitCodePassThrough` (код 7
  проходит).
- `devkitctl doctor --layout` молчит, `check-symbols.py` чист на новых файлах.

## Ревью

- Порог включения выжимки измеряется в байтах (len(out) >= thresholdBytes), а LLD DK-137 формулирует его в символах («4K символов», медиана в замере тоже в символах). На мультибайтном выводе это расходится до двух раз: git diff русских RULES-файлов на 2K рун пройдёт порог как 4K. Замер LLD велся в символах, поэтому стоит или считать руны (utf8.RuneCountInString), или прямо сказать в LLD «байт», чтобы порог и его обоснование были в одних единицах.: исправлено, порог в utf8.RuneCount (рунах), thresholdRunes, добавлен TestCaptureThresholdMultibyte
- Ход работы в файле задачи (строка 56) говорит «9+7 случаев», а по факту internal/frame несёт 14 тестов, не 9. Перечисление в «Сценарии проверки» ниже то же и верное, расходится только сводная цифра. Стоит поправить на 14+7, чтобы запись о ходе не вводила читателя в заблуждение.: исправлено, цифра 9+7 исправлена на 15+7
- TestPathWrittenToFile (frame_test.go:248-254) обещает комментарием «каждая из 200 строк seq должна найтись в файле», но цикл 200 раз делает одно и то же: strings.SplitN(body, "\n", 2)[0] и сравнение с «1», не используя i. Реально проверяется только первая строка. Для сверки всех строк пусть тест пройдётся по телу файла и сравнит каждую; иначе убрать обещание из комментария, чтобы тест не приписывал себе проверку, которой нет.: исправлено, цикл сверяет все 200 строк через strconv.Itoa(i+1)

## Выкат

- 2026-08-12 слито: 8f330d2, fb15c63, 2b1840b, 3849fe6, 9274731
