# DK-292: LLD: критерии вида приёмки задачи и место, где он назначается

## Что происходит

Вид приёмки (сценарий агентский или пользовательский) решает, закроет задачу
агент сам или она будет ждать захода пользователя. Решение принимается позже
всех разумных точек, самым заинтересованным в дешёвом ответе участником, и до
Check его никому не видно.

Правило занимает одно предложение: сценарий агентский, когда каждый шаг
выполним с машины агента, итог проверяется объективно и шаги на проде ничего не
меняют, а при сомнениях сценарий пользовательский (`RULES.board.md`, п. 6
раздела «Трекинг задач»). Умолчание сдвинуто в сторону пользователя дважды:
самим правилом и механикой, где пользовательский вид получается, если не
сделать ничего. Список признаков есть, процедуры ответа нет, поэтому «при
сомнениях» и стал рабочим ответом на всё незнакомое.

Платит за это очередь выката: пользовательский Check держал её девять часов на
DK-272, семь на DK-232, пять на DK-223, и каждый раз стояли несвязанные задачи
(черновик DK-275). Обход у пользователя есть, но ручной: открыть файл задачи и
прочитать заголовок раздела.

## Что проверено при разборе

- Вид кодируется одним заголовком в файле задачи: `## Сценарий проверки
  (агентский)` против `## Сценарий проверки` (`tools/taskctl/age.go`,
  `agentScenarioSection`). Отдельного поля нет ни в строке доски, ни в файле.
- Заголовок пишет исполнитель последним шагом перед переводом в Check: раньше
  сценария не существует. Утилиты не выбирают вид и не могут его выбрать, это
  названо прямо в коде (`tools/shipctl/advice.go`, `nextAfterMerge`).
- Ворота `taskctl move check` требуют только наличия раздела, агентский вариант
  заголовка проходит по префиксу (`tools/taskctl/gate.go`, `checkGate`).
  `taskctl lint` держится того же рубежа.
- Метка «сценарий агентский или пользовательский» печатается только под
  строками секции Check (`tools/taskctl/view.go`, `rowNoteParts`). В Backlog и
  In progress не видно вида, да его там ещё и нет.
- В воротах готовности груминга вовсе нет вопроса про вид: слово «сценарий» в
  `skills/board-groom/SKILL.md` не встречается ни разу.
- Прецедент раннего назначения уже работает в одном входе: нарезка цели ставит
  сценарий вместе с рангом и ценой, «агентский везде, где он возможен», и
  отправляет задачи с пользовательским видом в хвост цикла
  (`skills/goal-loop/SKILL.md`, шаг 3). У этого прецедента нет критериев и
  хранения, держится он на добросовестности нарезающего.
- По архиву агентский вид у 42 задач из 179 со сценарием, то есть примерно у
  каждой четвёртой.
- Дубля нет. DK-275 лечит следствие (очередь и окна проверок), а не выбор вида;
  DK-262 переводит готовый сценарий в приёмочный тест; DK-133 решает, где стоят
  ворота заведения строки, а не что они спрашивают; DK-092 про ворота правки
  перед `merge`, это другая фаза.

## Что решить

Задача не в правке одного абзаца правил, а в решении, из чего собран ответ, где
он живёт и кто его пересматривает. Документ ложится в `docs/lld/` и кончается
выбранным вариантом, отвергнутое названо с причиной прямо в нём.

- **Критерии.** Закрытый ли список барьеров делает сценарий пользовательским
  (итог виден только глазами, нужен физический доступ, шаг необратимо меняет
  мир снаружи, нет доступа или секрета, итог это суждение о вкусе, ждём
  неподделываемого события) и требуется ли против каждого барьера названный
  обход, чтобы барьер засчитался. Отсюда же решение, чем «при сомнениях»
  заменяется: сомнение это не барьер, а повод задать вопрос.
- **Сколько значений у вида.** Оставить два или завести третье, смешанное, для
  задач, где агенту доступна часть шагов. Смешанный вид меняет экономику
  простоя: агент прогоняет свою часть после выката и оставляет человеку один
  шаг с готовым выводом, вместо того чтобы вся задача ждала захода. Цена в том,
  что «прогнать сценарий» перестаёт быть одним действием.
- **Где живёт вид.** Заголовок раздела для груминга не годится: там нет ни
  файла задачи, ни сценария. Развилка между машиночитаемым полем строки доски
  (по образцу маркера `[после <ID>]`, видно в `taskctl list` с Backlog) и полем
  файла задачи, который тогда заводится раньше. Тут же цена правки формата
  доски и `taskctl`.
- **Ворота и пересмотр.** Что проверяет `move check`: сегодня наличие раздела,
  кандидат это названный барьер у пользовательского вида. Отдельно порядок
  пересмотра по ходу работы, когда исполнитель нашёл барьер, которого не
  видели, или наоборот нашёл ручку: повышение до агентского свободное,
  понижение с названной причиной строкой в файле задачи и сверкой с тем видом,
  что назначен на груминге.
- **Кто спрашивает.** Отдельный скилл с критериями и анкетой против абзаца в
  `RULES.board.md`. Все три места зовут скилл (груминг, исполнитель на
  пересмотре, ревьювер), а резидентное ядро от него не растёт; абзац в правилах
  всегда в контексте, но длинной анкете там не место.
- **Обратная связь.** Нужна ли сверка назначенного вида с фактом закрытия,
  чтобы ошибки назначения были видны и критерии было чем чинить.

## Готовность

Документ в `docs/lld/` принят: по каждому из шести вопросов назван выбранный
вариант с причиной, отвергнутое названо там же. Критерии записаны так, что по
ним отвечают одинаково два разных разбора одной задачи, и это показано на
примерах из архива, где вид назначен спорно. Реализация нарезана строками доски
(`taskctl add`, порядок через `taskctl dep add`), и в них видно, какие правки
едут в `RULES.board.md`, какие в скиллы, какие в `taskctl`.

## Смежное

- DK-275 (черновик): очередь выката под пользовательским Check, окна проверок и
  поезда по виду приёмки. Лечит следствие и опирается на вид, который тут
  становится определённым.
- DK-289 (черновик): дашборд закрывает задачу подъёмом сессии агента даже там,
  где пользователь уже принял её глазами. Прямой потребитель машиночитаемого
  вида.
- `taskctl batch`: в поезд берут «желательно агентский» сценарий, но критерия в
  коде нет, отбор идёт на глаз.
- DK-262: приёмочный тест из сценария проверки, упирается в тот же вопрос, что
  из сценария вообще исполнимо.
- DK-133: единые ворота заведения строки доски. Если вид спрашивают на груминге,
  вопрос встраивается в те ворота, которые эта задача выберет.

## Ход работы

- Вычитка: 1 файл, 9 правок, 1 пометка (кандидат в словарь: «нарезающий»),
  2026-08-13.
- Исполнение: субагент opus/xhigh по вердикту pick (квота: week_all 41%, week_max 43%, снимок 2ч 15м назад, протух, сдвига нет), 2026-08-13.
- Готово: `docs/lld/DK-292-check-kind.md` с решением по всем шести вопросам.
  Критерии это закрытый список из шести барьеров с классом обходов против
  каждого, умолчание перевёрнуто на агентский вид, сомнение барьером не
  считается. Значений у вида три, смешанное заведено; агентскую часть прогоняет
  исполнитель сразу после выката, очередь смешанный держит наравне с
  пользовательским до решения DK-275. Хранение разрезано: значение суффиксом
  `[приёмка: ...]` в заголовке строки доски (переживает `close` наравне с
  `[блок: ...]`), барьер разделом «Приёмка» файла задачи. Критерии живут
  отдельным `ACCEPTANCE.md` рядом с `RANKING.md`, скилла не заводится. Замер по
  архиву: заголовочная пометка стоит у 43 задач из 183, а из 140 «пользовательских»
  103 названы агентскими прозой и ещё 22 называют обе части, то есть метка
  расходится с текстом у 125 файлов. Прогон критериев по выборке из 25 закрытых
  задач (15 task, 7 bug, 3 LLD, оба месяца) идёт в двух колонках: по написанному
  тогда сценарию 13 агентских, 5 смешанных, 7 пользовательских, по критериям 16
  агентских, 4 смешанных, 5 пользовательских; спорные случаи названы отдельно.
  Реализация нарезана семью строками доски, заводит их диспетчер. 2026-08-13.
- Ревью дизайна (opus/xhigh): шесть замечаний, все исправлены. Пересчитаны числа
  архива и доски, суффикс приёмки переживает `close`, названы три входа
  заведения строки (сессия, дашборд, строка цели) и порядок включения
  обязательности флага, сведён ответ про LLD (`user` по умолчанию, `agent` только
  со строкой «Согласие: ревью дизайна»), уточнён разбор DK-284, заведён рубеж
  против фиктивного агентского сценария (вопрос ревью плюс ворота `close`) с
  названным остаточным риском. В разбивку добавились строки рубежа и дашборда.
  2026-08-13.
- Второй круг ревью (opus/xhigh): четыре замечания, все исправлены. Исключение
  «согласие закрывает ревью дизайна» снято целиком, у LLD вид всегда `user`, а у
  барьера «согласие» запрещено повышение; ворота `close` остались без исключений,
  и у задач без выката прогон читается как прогон после слияния в main; назван
  класс мелочи без ветки, где рубежа ревью нет, и он попал в остаточный риск и
  третьим следом в сводку; названо старшинство правил (самодостаточность старше
  обхода, обход засчитывается только исполнимый в самой приёмке), DK-284 и DK-243
  сведены к одному ответу, счёт стал 13 агентских, 5 смешанных, 7
  пользовательских. Новых строк в разбивке не появилось. 2026-08-13.
- Круг приёмки у пользователя: одно замечание, прогон по архиву судил написанный
  сценарий вместо природы задачи и занижал эффект. Критерий в решении 1 развёрнут
  в развилку: обход неисполним в приёмке значит барьер стоит, обход исполним, а
  шага под него не написано, значит барьер снят и это замечание ревью сценария, а
  не основание отдать задачу человеку. Прогон пересчитан по природе и показывает
  обе величины колонками: по написанному 13/5/7, по критериям 16/4/5, то есть 20
  задач из 25 перестают целиком ждать человека. Пересчитаны DK-192, DK-243 и
  DK-245 (барьер снят исполнимым обходом) и DK-284 (написанный вердикт занижал,
  стала смешанной), разбор пары DK-284 и DK-243 переписан, добавлен разбор DK-064
  как границы, где барьер устоял. Числа сведены в «Живом материале», решении 2,
  разбивке и DoD. 2026-08-13.
- Замечание 11 (довод про остаточный риск устарел после развилки): абзац
  переписан. Промах теперь один, ненаписанный шаг под исполнимый обход при верном
  виде; у задачи с веткой его ловят автор и ревьювер, у мелочи без ветки не ловит
  никто, и ворота `close` берут на себя больше, чем задумывались, отличая пустое
  закрытие от непустого, но не вывод про выкаченное от вывода про дерево. Сказано,
  что развилка этот класс расширила, и названы ограничитель (спорный вид приёмки
  это повод завести ветку) и цена ошибки в границах определения мелочи. Счёт,
  таблица и структура решений не тронуты. 2026-08-13.
- Первый коммит LLD уехал под чужим ID: соседняя сессия захватила незакоммиченные
  `docs/lld/DK-292-check-kind.md` и `docs/tasks/DK-292.md` в свой коммит
  `ec4839b1` с subject «docs(tasks): DK-296 чат обычной задачи ведёт в тупик».
  Коммит уже в origin, поэтому subject не переписан, и по ID эти 692 строки
  находятся только через эту запись. 2026-08-13.
- Вычитка: 1 файл, 13 правок, 0 пометок, 2026-08-13.
- Нарезка: семь строк заведены диспетчером, DK-298 (taskctl), DK-299
  (ACCEPTANCE.md), DK-300 (рубеж), DK-301 (дашборд), DK-302 (shipctl и batch),
  DK-303 (проход по открытым строкам), DK-304 (сводка `kinds`). Порядок держат
  зависимости, `taskctl lint` зелёный. 2026-08-13.
- Вычитка: 2 файла, 5 правок, 0 пометок, 2026-08-13.

## Сценарий проверки

Приёмку закрывает согласие с принятыми решениями, поэтому сценарий
пользовательский: машине судить, тот ли вариант выбран, нечем.

1. Прочитать `docs/lld/DK-292-check-kind.md`, разделы «Решение 1» ... «Решение
   6». По каждому вопросу постановки назван выбранный вариант и отвергнутое с
   причиной.
2. Сказать, принимается ли каждое из решений. Четыре меняют привычный порядок
   сильнее прочих, и смотреть стоит с них: перевёрнутое умолчание (агентский вид
   по умолчанию, пользовательский только с барьером из шести), три значения вида
   вместо двух, хранение суффиксом `[приёмка: ...]` в заголовке строки доски
   вместо колонки, и критерии переносимым `ACCEPTANCE.md` рядом с `RANKING.md`
   вместо отдельного скилла.
3. Проверить нарезку: `taskctl show DK-298` ... `taskctl show DK-304` и
   `taskctl dep list` показывают семь строк с рангами из раздела «Разбивка на
   задачи» и порядок между ними.

Ожидаемый итог: решения приняты, задача закрывается `taskctl close`. Несогласие
с любым из решений возвращает задачу в In progress с названным решением, которое
переделывается; строки нарезки при этом переоформляются под новый ответ.

Что уже проверено без пользователя: ревью дизайна прошло два круга (десять
замечаний, все закрыты), `python3 hooks/check-symbols.py` на LLD и файле задачи
зелёный, `taskctl lint` зелёный, семь строк стоят в Backlog с зависимостями.

## Ревью

- Числа раздела «Живой материал» не сходятся между собой и с архивом. Фактически в docs/tasks/archive/2026 заголовок «## Сценарий проверки (агентский)» стоит у 43 файлов, а не у 42, поэтому 42+139 не даёт заявленных 182. Таблица категорий даёт 104+22+8+6 = 140 при знаменателе 139. Фраза «расходится с текстом у 125 файлов» противоречит собственной таблице (104+22 = 126) и повторена трижды (в «Живом материале», в решении 2 и в «Почему не поле файла задачи»). Отдельно: «у всех 57 открытых строк доски devkit» это счёт одного Backlog, а открытых строк 64 (Backlog 57, In progress 5, Check 2), и то же 57 стоит в разбивке у строки «Открытые строки доски получают вид». Пересчитать все четыре числа одним прогоном и свести их между собой.: исправлено, пересчитал одним прогоном по логике утилиты (ограждённые блоки пропускаются): 183 файла со сценарием = 43 с агентским заголовком + 140 остальных, таблица 103+22+8+7 = 140, расхождение метки и текста 125; открытых строк доски 63 (57 Backlog, 5 In progress, 1 Check) со снимком на дату, в разбивке то же число
- Сводке `taskctl kinds` (решение 6) нечего считать: суффикс заголовка не доживает до архива. При закрытии архивная строка пересобирается заново (`tools/taskctl/ops.go`, около строки 675: `archBase, _, _, archBlockSuf := splitTitle(row.Title)` и дальше `joinTitle(archBase, nil, "", archBlockSuf)`), то есть в `docs/TASKS-archive.md` уезжает основа плюс только `[блок: ...]`, а сама архивная строка шестиколоночная и вида в ней нет негде. Решение 6 при этом обещает «данные для всех четырёх лежат в строке доски, в файле задачи и в архиве, новых записей заводить не нужно», и три счёта из четырёх (распределение по видам, ошибка в дорогую сторону, ошибка в дешёвую сторону) считаются именно по закрытым задачам. Сказать в решении 3 явно, что `[приёмка: ...]` переживает `close` наравне с `[блок: ...]`, добавить это в цену первой строки разбивки, либо назвать другой источник данных для сводки.: исправлено, в решении 3 добавлен абзац: суффикс приёмки переживает close наравне с [блок: ...], правка названа и включена в цену первой строки разбивки; там же вторая цена колонки (архивная таблица шестиколоночная, ранга и цены в ней нет вовсе); решение 6 теперь ссылается на это, а не обещает данные вообще
- Обязательный `--accept` у `taskctl add` ломает два уже работающих входа заведения строки, и ни один не назван. Первый: дашборд заводит задачу подпроцессом, `tools/dashboard/newtask.go` собирает аргументы `[]string{"add", "--title", ..., "--type", ..., "--rank", ...}` (ручка `POST /api/projects/{p}/tasks`, `tools/dashboard/README.md`), и отказ `add` без флага кладёт форму «Новая задача» с телефона целиком; та самая задача DK-245 стоит в прогоне критериев строкой выше. Второй: `kit/skills/goal-start/SKILL.md`, п. 5 заводит строку самой цели тем же `add --title "Цель: ..." --type task --rank ... --cost XL`, и какой вид приёмки у строки цели, документ не говорит. Назвать оба входа: что шлёт дашборд и откуда берёт барьер на телефоне, какой вид у строки цели, и завести правку дашборда строкой в разбивке (сейчас её там нет, и в «Затрагиваемой документации» дашборд упомянут только как потребитель JSON).: исправлено, в решении 4 добавлен раздел «Входы заведения строки»: три входа названы, дашборд получает выбор вида и барьера закрытыми списками и шлёт флаги, вид строки цели ставится по DoD и понижается витком, если нарезка дала не агентскую задачу; обязательность флага включается строкой дашборда, до неё add работает с предупреждением; в разбивку добавлена строка дашборда 36 (25+6+2+0+3), цена M
- Решение 2 и прогон по архиву расходятся на случае «приёмку закрывает ревью дизайна». Решение 2 отвергает четвёртое значение вида словами «этот случай выражается смешанным видом с барьером согласие и строкой в DoD», а разбор DK-129 в прогоне говорит прямо обратное: гигиена документа агентской частью смешанного вида не считается, потому что не самодостаточна, и все три LLD в таблице стоят «пользовательский, согласие». То есть в предложенной системе LLD-задача всегда `user`, смешанной она стать не может, а «согласие дало ревью дизайна до слияния» остаётся фразой в DoD, которую ни ворота, ни сводка не читают. Свести два места и сказать одним ответом, каким значением вида и каким машинным следом закрывается LLD-задача, включая сам DK-292.: исправлено, правило про LLD собрано в одном месте (решение 1, абзац про барьер «согласие»): по умолчанию LLD это user, смешанным не бывает (гигиена документа не самодостаточна), а случай «согласие закрывает ревью дизайна» объявляется заранее строкой «Согласие: ревью дизайна» в разделе «Приёмка» и даёт вид agent с машинным следом в разделе «Ревью»; решение 2 и разбор DK-129 переписаны под этот ответ, про сам DK-292 сказано, что он закрывается пользователем
- DK-284 в прогоне противоречит собственным критериям документа. Сценарий `docs/tasks/archive/2026/DK-284.md` кроме грепа по статике и медиа-запросам называет `TestStaticTaskNarrowWidths`, который меряет раскладку на узком экране headless-chrome, то есть ровно тот обход, который таблица решения 1 ставит против барьера «глаза» («headless-браузер с замером»). По правилу того же решения барьер засчитывается, только когда названа причина непригодности обхода, а разбор DK-284 этот тест не упоминает вовсе и описывает агентскую часть как «греп по статике репозитория, медиа-запросы в app.js». Либо назвать причину, по которой замер не годится (тест пропускается без браузера, и это надо сказать словами), либо поменять вердикт на смешанный. Пример, где критерии не воспроизводятся на своём же прогоне, обесценивает весь раздел, а DK-284 закрыта вчера и её файл под рукой у любого читателя.: исправлено, вердикт DK-284 сменён на смешанный: headless-замер TestStaticTaskNarrowWidths это названный обход барьера «глаза», он уходит в агентскую часть, человеку остаётся сверка с эталоном, фокус и живой шрифт; оговорка «без браузера тест пропускается» названа как обход с оговоркой, который пишут в раздел «Приёмка»; DK-243 разобран отдельно и остаётся пользовательским, счёт пересчитан на 13 агентских, 6 смешанных, 6 пользовательских
- Ошибка в дешёвую сторону обратной связью не ловится, а перевёрнутое умолчание держится именно на обещании, что ловится. Решение 6 считает такой ошибкой «задачу с видом agent, вернувшуюся из Check провалом», но задачу с агентским видом закрывает сам агент по своему же сценарию: если сценарий собран из повтора тестов ветки (прогон показывает, что так уже писали DK-243 и DK-284), он зелёный, `taskctl fail` не случается никогда, и неверное назначение остаётся невидимым. Правило «повтор тестов ветки в приёмку не входит» из решения 1 машиной не проверяется: `checkGate` (`tools/taskctl/gate.go`) смотрит только наличие раздела, а в автономном режиме (`RULES.board.md`, п. 8 раздела «Ветки, ревью и деплой») человека в цепочке нет вовсе, и про автономный режим документ молчит. Назвать, чем ловится молча закрытая задача с фиктивным агентским сценарием (кандидат один: ревью ветки, и тогда это надо записать рубежом в решение 4 и шагом в `board-ship`), либо признать риск и назвать его цену рядом с решением перевернуть умолчание.: исправлено, в решении 4 добавлен раздел «Рубеж против фиктивного агентского сценария»: рубеж это ревью ветки с проверяемым вопросом (у агентского вида обязан быть шаг после выката, не повторяющий тесты ветки), оно обязательно и в автономном режиме, а замечание держит слияние механизмом shipctl merge; машинная подпорка это ворота taskctl close на вложенный вывод прогона; остаточный риск назван прямо с ценой. В решении 6 у ошибки в дешёвую сторону теперь два следа (провал и понижение вида на ревью), в разбивку добавлена строка «Рубеж против фиктивного агентского сценария» 37 (25+7+2+0+3), цена S, третьей по порядку
- Новое правило про LLD объявляет согласие «заранее», но объявить его задним числом ничто не мешает, и хранить его негде. Решение 1 говорит: строка «Согласие: ревью дизайна» ставится в раздел «Приёмка» при заведении задачи, и тогда вид `agent`. Но решение 3 заводит раздел «Приёмка» только когда вид не агентский («агентский не требует ни файла, ни строки»), то есть у строки с `--accept agent` этого раздела нет и никто его не создаёт; инвариант решения 3 после нового правила неверен. Ворота `add` при `agent` не спрашивают ничего, а повышение по решению 4 свободное, значит исполнитель LLD-задачи, заведённой как `user` с барьером «согласие», в любой момент (в том числе после ревью) зовёт `taskctl set --accept agent`, дописывает строку про согласие и закрывает себя сам. Что барьер «согласие» обхода не имеет и потому повышению не подлежит, вывести из текста можно, но сказано это нигде, и машина не проверяет. Закрыть надо все три: где живёт объявление у агентского вида, чем «заранее» отличимо от «задним числом» (кандидат: строка обязана быть в файле на момент заведения и понижение/повышение по «согласию» запрещено), и что на это отвечают ворота.: исправлено, исключение под «согласие закрывает ревью дизайна» снято целиком: у LLD вид всегда user с барьером «согласие», агентского не бывает; в решении 1 названо, почему исключение отвергнуто (хранить негде, «заранее» от «задним числом» неотличимо, повышение свободное), и добавлено правило «у барьера согласие повышение вида запрещено, обхода у него нет по определению»; решение 2 переписано под тот же ответ, инвариант решения 3 снова верен
- Две новые конструкции сталкиваются: ворота `taskctl close` требуют у вида `agent` вложенного вывода прогона (решение 4, «Рубеж против фиктивного агентского сценария»), а единственный легальный агентский LLD из решения 1 («Согласие: ревью дизайна») прогона не имеет по построению, его приёмку закрывает ревью, и машинный след назван там же разделом «Ревью». По воротам такая задача не закроется вовсе. Тот же вопрос у задач без выката (LLD, дока, разборы), которым `RULES.board.md`, п. 6 раздела «Ветки, ревью и деплой» очередь не даёт держать: что считается «вложенным выводом прогона» у них. Либо назвать исключение из ворот с проверяемым признаком (кандидат: у вида `agent` требуется либо вывод прогона, либо закрытые замечания раздела «Ревью» при объявленном согласии), либо снять ворота и оставить рубеж на одном ревью, сказав это прямо.: исправлено, конфликт снят вместе с исключением: агентского LLD не бывает (замечание 7), поэтому случая «вид agent без прогона по построению» нет, и ворота close работают без исключений; отдельно сказано, что у задач без выката прогон идёт после слияния в main и вкладывается тем же порядком, это же уточнение внесено в правило самодостаточности в решении 1
- Рубеж против фиктивного агентского сценария не покрывает класс задач, где он нужнее всего. Раздел говорит «Рубеж один, и он стоит раньше выката: это ревью ветки. Ревью обязательно перед слиянием и в автономном режиме тоже», но ветка нужна не всякой задаче: по `RULES.board.md`, п. 1 раздела «Ветки, ревью и деплой» мелочь (опечатка, правка доков, однокоммитный багфикс с тестом, подкрутка конфига) коммитится прямо в main, и п. 2 там же говорит про неё «обязательного ревью не требует, её страхуют тесты». У такой задачи есть строка доски, вид приёмки и сценарий, но ветки нет, ревьювера нет, `shipctl merge` с его отказом на открытом замечании не зовётся, а сценарий из повтора тестов ветки у правки доков это самый вероятный случай из всех. Остаётся только вторая подпорка, ворота `close` на наличие вывода. Назвать этот класс и сказать, чем он закрыт (кандидат: у задачи без ветки вид `agent` требует шага, который гоняется после коммита в main, и это проверяют те же ворота `close`), либо признать, что для него рубежа нет, и посчитать это в остаточном риске, который сейчас сформулирован только про невнимательного ревьювера.: исправлено, класс мелочи без ветки назван в разделе «Рубеж»: ревью там нет, остаются ворота close (они про файл задачи, а не про ветку) и второе основание правила «при сомнениях заводить ветку» (спорный вид приёмки это повод позвать ревьювера); остаточный риск переписан и теперь называет этот класс прямо, а сводка решения 6 считает его третьим следом: агентские задачи, закрытые без раздела «Выкат» и без замечаний ревью
- DK-284 и DK-243 в прогоне разобраны по двум разным правилам, и счёт 13/6/6 стоит именно на этом. DK-284 стала смешанной, потому что `TestStaticTaskNarrowWidths` это названный обход барьера «глаза», и тут же признано: «Чтобы агентская часть стала самодостаточной по правилу решения 1, замер надо гонять против выкаченного дашборда, а не против файла в дереве», то есть как написано, агентская часть правилу самодостаточности не отвечает. DK-243 при тех же условиях (агентская часть названа, гоняется против дерева, после выката знания не добавляет) осталась пользовательской, и подсказка ей дана та же самая, «перепиши шаг против поднятого сервера». Получается, у DK-284 вид считается по тому, каким сценарий станет после переписывания, а у DK-243 по тому, каков он сейчас. Сказать, какое из двух правил решения 1 старше при столкновении (барьер снят обходом против несамодостаточной агентской части), и привести оба разбора к одному ответу; если старше самодостаточность, DK-284 возвращается в пользовательские и счёт становится 13/5/7. Это ровно то место, где DoD требует одинакового ответа у двух разных разборов.: исправлено, в решении 1 названо старшинство: правило самодостаточности старше правила про обход, и обход засчитывается только когда исполним в самой приёмке (после выката или слияния); DK-284 и DK-243 сведены в один разбор и обе стали пользовательскими, счёт пересчитан на 13/5/7, барьеры семи пользовательских разложены заново
- Новая развилка решения 1 обесценила довод, которым закрыт остаточный риск в решении 4, и довод этот не поправлен. В конце раздела про рубеж стоит: «ошибка требует двух промахов сразу, неверного вида и сценария из тестов ветки». После развилки первый промах не нужен вовсе: вид `agent` ставится по природе задачи и он верный, а шага под исполнимый обход в сценарии может не быть, и это ровно случай DK-192, DK-243 и DK-245 из прогона, где вид правильный, а сценарий человеческий. Остаётся один промах, ненаписанный шаг, и для задачи с веткой его ловит ревью, а для мелочи без ветки не ловит никто: ревьювера нет, `move check` смотрит только наличие раздела, ворота `close` проверяют наличие вывода, а вывод у сценария из тестов ветки есть. То есть развилка расширила ровно тот класс, который абзацем выше назван местом, где ошибка в дешёвую сторону может пройти целиком. Переписать довод под новое правило (кандидат: для задачи с веткой промаха по-прежнему два, автор сценария и ревьювер, а для мелочи без ветки промах один, и потому спорный вид приёмки это повод завести ветку) и сказать, вырос ли этот класс от развилки.: исправлено, абзац остаточного риска переписан под развилку: довод про два промаха снят, промах теперь один (шаг под исполнимый обход не написан при верном виде, случай DK-192, DK-243, DK-245), сказано что развилка класс расширила; дальше два случая раздельно, у задачи с веткой промахов два (автор и ревьювер) и держит их механизм замечания, у мелочи без ветки промах один и ворота close берут на себя больше, чем задумывались, отличая пустое закрытие от непустого, но не вывод про выкаченное от вывода про дерево; названы ограничитель (спорный вид это повод завести ветку) и цена ошибки в границах определения мелочи, плюс счётчик сводки как сигнал чинить порог ветки
