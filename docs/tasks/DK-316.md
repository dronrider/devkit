# DK-316: dashboard: перерисовка дёргает экран, позиция скролла и чат прыгают

записан 2026-08-13, оформлен нарезкой цели DK-327 2026-08-14

## Что происходит

На списках задач и черновиков позиция пролистывания сбивается при каждом
обновлении, а чат с агентом дёргается на приходе событий. При нажатии
«Продолжить» или «Закрыть» экран прыгает, нажатие выглядит несостоявшимся, и
человек жмёт второй раз. Базовая беда одна: `refresh()` висит на focus окна и на
hashchange и пересобирает экран целиком (`static/app.js`, около строки 2985).
`renderBoard`, `renderDrafts` и лента чата строят DOM заново, и позиция скролла
теряется. Спокойное обновление сделано только форме задачи: `renderTask` при
несохранённой правке вешает плашку вместо перерисовки.

Размер материала: в `static/app.js` три тысячи строк и тринадцать функций
`render*`, из них экран пересобирает не меньше пяти на каждый возврат в браузер.
Поймано пользователем на приёмке 2026-08-13 поимённо: прыжок экрана при нажатии
кнопок, дёргающийся чат, сбитая позиция списка черновиков.

## Чего хотим

Общий слой перерисовки для экранов, а не заплатка на каждый: изменившиеся узлы
обновляются, позиция пролистывания и фокус переживают обновление, а нажатие
кнопки не сдвигает экран.

## DoD

- Слой частичной перерисовки закрывает списки задач и черновиков и ленту чата,
  то есть места, названные приёмкой; целиком экран на обновлении не
  пересобирается.
- Позиция пролистывания и фокус переживают обновление по focus окна и по приходу
  события: юнит статики под node с игрушечным DOM сверяет их до и после
  перерисовки и падает на старом коде.
- Нажатие кнопки действия не сдвигает экран: узел строки обновляется на месте.
- Поведение описано в README дашборда, раздел про перерисовку.

## Ранг

`25+8+3+5+4 = 45`, P2.

- Серьёзность 25 (Major). Заметное трение на каждом действии: список уезжает,
  чат дёргается, нажатие выглядит несостоявшимся. Обход дешёвый и очевидный,
  проскроллить обратно и нажать ещё раз, поэтому не Critical.
- Ценность 8. Сквозное требование шестнадцатого сценария цели DK-327: пока экран
  прыгает, не читается ни одно нажатие, а это все пятнадцать остальных сценариев.
- Неопределённость 3. Подход понятен (частичная перерисовка вместо полной), а
  развилки сидят в деталях: что считать изменившимся узлом, чем держать фокус и
  как быть с лентой, которая растёт снизу.
- Поправка на баг 5. Дефект поведения экрана, а не отсутствующая работа:
  обновление обязано было оставлять человека на том же месте.
- Рычаг 4. На частичную перерисовку опираются отклик закрытия (DK-289), признак
  работы в строке (DK-317) и вкладка разговора на телефоне (DK-305); без неё
  каждая из них чинит своё место поверх полной пересборки.

Инвариант зависимости: строка держит DK-317 (43), DK-289 (43) и DK-305 (37), её
ранг выше всех трёх.

## Приёмка

- вид: mixed
- барьер «глаза»: сквозное требование сценария 16, экран не прыгает и позицию
  пролистывания не теряет
  - утверждённый макет: не годится, предмет тут не раскладка, а поведение экрана
    на обновлении, и макета на такое не бывает;
  - headless-браузер с замером: годится для чисел, позиция пролистывания и число
    пересобранных узлов замеряются и уходят агенту;
  - тест по разметке и стилям: годится для правил, а дёрганье экрана под рукой им
    не ловится.

## Итог

В `static/app.js` завёлся общий слой частичной перерисовки: `sync` обновляет
детей коробки по описаниям с ключом и отпечатком, `viewSnap` с `viewBack`
снимают и возвращают место (прокрутку списка, фокус с кареткой, якорь сдвига
раскладки). На слое держатся доска, накопитель черновиков, полоса живых работ
и лента чата, то есть все места, названные приёмкой.

Что изменилось с экрана:

- обновление по фокусу окна не трогает узлы, у которых отпечаток тот же:
  прокрутка, фокус на кнопке и раскрытый текст черновика остаются на месте;
- нажатие кнопки строки не сдвигает экран. Ответ на него приходит карточкой
  поверх экрана и в поток документа не входит вовсе (форвард-фикс по приёмке,
  ниже), а поднявшаяся работа прибавляет карточку в полосу живых работ, и
  список едет на её высоту: якорь возвращает картинку на место;
- разговор собирается один раз на заход. Экранная развилка `refresh` разошлась
  надвое: `refresh` снимает и возвращает место, `paint` собирает экран. Живые
  потоки закрываются при уходе с экрана, а не на каждом обновлении, поэтому
  перечитанная доска больше не рвёт поток событий чата и не собирает ленту
  заново; на экране чата обновление меняет только шапку и кнопку стопа.

Экран, который собирается целиком (задача, лента, главная, «Агенты», форма
заведения), тем же снимком держит хотя бы позицию пролистывания. Переход это
другой экран, и он открывается сверху.

Тесты: стенд `testdata/screen_keep.mjs` поднимает статику в node с игрушечным
DOM, где опустевшая коробка сбрасывает прокрутку, а снятый с дерева узел
теряет фокус. После обновления стенд смотрит прокрутку, фокус, раскрытую
запись, набранное в поле чата и число открытых потоков. Go-тест
`TestScreenKeepsPlaceOnRefresh` зовёт его. Четыре старых проверки текста статики
поправлены под новые имена (`paint` вместо `refresh` у развилки экранов,
`sync(box, items)` вместо `box.replaceChildren()` в ленте чата).

## Сценарий проверки

Смешанный. Агентская часть гоняется командами из корня чекаута, барьер «глаза»
остаётся человеку.

1. Регрессия закрыта, на старом коде стенд краснеет:

   ```sh
   regcheck --base main \
     --tests tools/dashboard/board_test.go,tools/dashboard/testdata/screen_keep.mjs \
     -- bash -c 'cd tools/dashboard && GOWORK=off go test -run TestScreenKeepsPlaceOnRefresh .'
   ```

   Ожидание: «тест краснеет на main и проходит на текущем коде».

2. Стенд частичной перерисовки проходит сам по себе, и видно, что он проверял:

   ```sh
   cd tools/dashboard && node testdata/screen_keep.mjs static/app.js
   ```

   Ожидание: строка «частичная перерисовка: доска, черновики и лента чата
   держат место и фокус, экран агента держит выбранный разговор, ответ на
   нажатие не двигает раскладку», код возврата 0.

3. Остальной дашборд не сломан:

   ```sh
   cd tools/dashboard && GOWORK=off go test ./...
   ```

   Ожидание: `ok`.

Пользовательская часть, барьер «глаза»: открыть доску с телефона или ноутбука,
пролистать список вниз, переключиться на другое окно и вернуться. Список стоит
на том же месте, чат при обновлении не дёргается, а нажатие «Продолжить» или
«Закрыть» отвечает карточкой в углу экрана, не двигая список из-под пальца, и
повторно жать не хочется.

## Ход работы

- Вычитка: 1 файл, 2 правки, 1 пометка, 2026-08-14.
- Вычитка: 4 файла, 10 правок, 0 пометок, 2026-08-14.
- Вычитка: 8 файлов, 16 правок, 1 пометка, 2026-08-14.
- Исполнение: субагент opus/high по вердикту pick (квота: week_all 55%, week_max 52%, снимок 0м назад, сдвига нет), 2026-08-14.
- 2026-08-14, исполнение: слой перерисовки (`sync`, `viewSnap`, `viewBack`,
  `findKey`) и перевод на него доски, черновиков, полосы живых работ и ленты
  чата; `refresh` разошёлся на снимок места и сборку экрана (`paint`). Стенд
  `testdata/screen_keep.mjs`, раздел «Перерисовка экрана» в README дашборда.
  Предметы DK-317, DK-289 и DK-305 не трогались.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 55%, week_max 52%, снимок 32м назад, сдвига нет), 2026-08-14.
- 2026-08-14, приёмка пользователем: экран держит место, но осталась одна
  механика, которая его двигала. Ответ на нажатие («конвейер задачи DK-136
  поднят в tmux-сессии task-DK-136») стоял строкой над списком, в потоке
  документа: пустой он свёрнут стилями (решение DK-284), а со словами о
  нажатии прибавлял свою высоту и уводил доску вниз. Задача возвращена в
  работу форвард-фиксом.
- 2026-08-14, форвард-фикс: ответ уехал из потока документа в карточку поверх
  экрана. Каркас взят у флеш-уведомления ленты и вынесен в общий `toast`
  (карточка, крестик, полоска остатка времени, потолок числа карточек), сам
  контейнер `.flashes` уже стоял `position:fixed`. Удача гаснет по таймеру,
  отказ ждёт крестика, ответ на экране один: слова о начале сменяются словами
  об исходе. Узел `#actmsg` и его стили ушли из разметки, а вместе с ними и
  замер DK-284 про высоту пустой строки: мерить стало нечего. На его месте в
  том же chrome-стенде стоит смок сегодняшней разметки, видна ли карточка и
  стоит ли при ней список на месте; регресс приёмки держит не он, а
  `testdata/screen_keep.mjs` с настоящим `sayResult`.
  Отвергнутый путь: класть ответ строкой в ленту уведомлений
  (`#проект/feed`). Лента это журнал уведомителя с сервера: время, группировка
  по дням, точка на колокольчике. Ответ на нажатие живёт секунды, своего
  события в журнале не имеет и на другой машине не нужен, а строка там
  зажигала бы «непрочитанное» на каждый тычок кнопки. Отвергнут и
  простой путь резервирования высоты под строку: он вернул бы на каждый экран
  отступ, снятый DK-284, ради слов, которых обычно нет.

## Ревью

- Вердикт: без замечаний. Путь от симптома до правки: refresh() (main:static/app.js, около строки 2985) на каждый focus/hashchange звал closeAgentLive() и groups.replaceChildren(), это и сбрасывало прокрутку .groups, снимало фокус и рвало EventSource ленты чата. Правка разводит refresh/paint (app.js:3292-3401): refresh снимает viewSnap() и возвращает viewBack() место, paint зовёт closeAgentLive только при смене экрана или уходе с чата (app.js:3310). Слой sync/keyed/findKey/focusSnap/focusBack/anchorKey/viewSnap/viewBack (app.js:28-159) обновляет узлы по ключу и отпечатку; rowSign (app.js:450) в отпечаток строки берёт JSON.stringify(row), секцию и work.via, то есть статус, ранг и кнопку разом, узел без изменений остаётся тем же DOM-элементом. renderBoard, renderDrafts, renderLive и лента чата (wireChatFeed draw(), app.js:1766) переведены на sync; удаление лишних узлов идёт по одному (insertBefore и removeChild с хвоста), replaceChildren в этих местах не осталось. sayResult (app.js:264) снимает и возвращает место вокруг .actmsg, чтобы нажатие кнопки не сдвигало список. renderChat собирает разговор один раз на заход и на re-render меняет только кнопку стопа (fill), поток не переоткрывается; showError (app.js:3263) закрывает agentLive перед заменой экрана целиком, утечки нет. Из звеньев, которых в диффе нет: renderTask (плашка DK-284 при несохранённой правке) не тронут и не должен был, работает как раньше поверх общего viewBack; renderAgent/transcriptLive (живой статус отдельной задачи) не входит в приёмку (доска, черновики, лента чата) и по-прежнему закрывается на каждом refresh, как до правки, регресса тут нет. Хвост diff main..dk-316 про messages.go, messages_test.go (TestMessageLandsAtLinkedGoalPath) и docs/tasks/DK-270.md это дрейф main (DK-270 слит в main после ветвления dk-316, merge-base 341c7f36), в коммитах ветки (1d4842eb, adcef7ea) этих файлов нет, не находка. Стенд testdata/screen_keep.mjs не тавтологичен: гоняет прокрутку с фокусом на доске, нажатие кнопки без сдвига экрана, раскрытую запись и прокрутку черновиков, набранный текст с кареткой и счётчик открытых потоков чата, приход реплики без пересборки ленты, смену шапки при поднятой работе без пересборки треда. regcheck --base main --tests board_test.go,screen_keep.mjs -- go test -run TestScreenKeepsPlaceOnRefresh подтвердил красноту на main и зелень на правке. node testdata/screen_keep.mjs static/app.js и GOWORK=off go test ./... -count=1 из tools/dashboard зелёные. README раздел «Перерисовка экрана» на месте, check-symbols.py чист.
- Замер narrow["toast-shift"]/narrow["toast-h"] (testdata/task_narrow.js, TestStaticTaskNarrowWidths в tasks_test.go) не даёт регрессионного покрытия бага: regcheck --base main --tests tools/dashboard/tasks_test.go,tools/dashboard/testdata/task_narrow.js -- go test -run TestStaticTaskNarrowWidths . отвечает «тест зелёный и на старом коде (main), регрессию он не ловит». Причина: chromeStand (tasks_test.go:1103) подменяет реальный <script type="module" src="/assets/app.js"> на файл замера, то есть настоящий app.js (sayResult, toast) в этом стенде вовсе не грузится; task_narrow.js вручную кладёт разметку TOAST в #flashes через innerHTML мимо sayResult. Контейнер #flashes уже стоял position:fixed до форвард-фикса, тот же .flashes{position:fixed...} нашёлся в style.css главной ветки: он существовал под флеш-уведомления ленты задолго до DK-316, а баг сидел не в позиции #flashes, а в том, что sayResult писал в отдельный #actmsg, стоявший в потоке. Замер меряет свойство контейнера, которое эта правка не трогала, и не способен покраснеть ни при какой реализации toast() в app.js, раз сам app.js в этом стенде не выполняется. Комментарий в task_narrow.js («иначе проверка пересказывала бы саму себя») и запись в docs/tasks/DK-316.md, «Ход работы» («замер DK-284 про пустую строку заменён замером сдвига списка от карточки») выдают его за замену старой регрессионной проверки, а по факту замена не состоялась: настоящую регрессию ловит отдельный узел, screen_keep.mjs, и regcheck на нём честно подтвердил красное на main и зелёное на правке, проверил сам. Прошу исполнителя либо снять из task_narrow.js и DK-316.md формулировку, будто toast-shift ловит регресс DK-284/DK-316, и оставить его самостоятельной CSS-проверкой сегодняшней разметки с опорой на регресс явно у screen_keep.mjs, либо переделать замер так, чтобы он действительно гонял ответ через настоящий app.js. Остальное по существу подтвердилось: поток документа не меняется при sayResult (screen_keep.mjs сверяет узлы #groups, #live и соседей до и после), .flashes честно fixed по настоящему style.css, .actmsg и его CSS убраны без остатка и пустой отступ DK-284 не вернулся, toast() снимает прежний ответ перед новым и гасит таймер per-card в dismiss через clearTimeout, sayResult("") гасит карточку без создания новой, showFlash поверх toast сохранил дот, время, смахивание и переход в ленту (notifications_test.go), outbox_offline.mjs получает toast()===null безопасно: сообщение об ошибке в очереди держится состоянием пузыря и меткой, а не sayResult, проверено по коду makeOutbox. node testdata/screen_keep.mjs static/app.js и GOWORK=off go test ./... -count=1 из tools/dashboard зелёные, check-symbols.py чист. исправлено: завышенное утверждение снято, замер оставлен смоком сегодняшней разметки в браузере (карточка видна, список при ней стоит на месте), регресс приёмки явно отдан стенду testdata/screen_keep.mjs; переписаны шапка замера в testdata/task_narrow.js, комментарий проверки в tasks_test.go и запись в «Ходе работы». Переделывать замер под настоящий app.js не стал: chromeStand подменяет модуль статики файлом замера, и поднять там sayResult нечем без второго стенда со своим сервером.
- Вердикт: без новых замечаний, второй круг. Замечание 2 закрыто по существу, не на глаз: task_narrow.js и tasks_test.go (81f6a3c0) больше не называют toast-shift/toast-h регрессионной проверкой, шапка прямо говорит «это смок сегодняшней разметки и стилей, а не проверка механики» и «настоящий app.js в этом стенде не грузится вовсе», регресс приёмки явно отдан testdata/screen_keep.mjs; та же формулировка ушла в DK-316.md, «Ход работы». Отказ переделывать замер под настоящий app.js обоснован в исходе замечания (chromeStand подменяет модуль статики файлом замера, поднять там sayResult нечем без второго стенда со своим сервером) и не потерялся: исход «исправлено: ...» дописан в ту же физическую строку замечания 2, taskctl review show печатает [исправлено], не заводит третьей отдельной строки. Прогоны: node testdata/screen_keep.mjs static/app.js печатает «...ответ на нажатие не двигает раскладку» с кодом 0; GOWORK=off go test ./... -count=1 из tools/dashboard зелёный (73.3с), TestStaticTaskNarrowWidths отдельно тоже зелёный. check-symbols.py на tasks_test.go, task_narrow.js и DK-316.md чист.

## Выкат

- 2026-08-14 слито: 43446dfe, e397f1b5, fae51f8f
- 2026-08-14 слито: d162913b, 5ba42456, c170cb46, 3df8cf49, eaee1dc8, ac825703

## Проверка

Агентская часть, прогон диспетчером цикла DK-327 поверх выката, 2026-08-14:
regcheck против кода до слияния («тест краснеет на fae51f8f^ и проходит на
текущем коде, регрессия закрыта»), стенд `node testdata/screen_keep.mjs
static/app.js` печатает «частичная перерисовка: доска, черновики и лента чата
держат место и фокус» с кодом 0, полный `GOWORK=off go test ./...` дашборда
`ok 85.672s`. Пользовательская часть с барьером «глаза» (список стоит на
месте, чат не дёргается, нажатие не сдвигает экран) остаётся за
пользователем.
