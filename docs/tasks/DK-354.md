# DK-354: LLD: черновик на дашборде правится и грумится одним процессом

записан 2026-08-15, оформлен 2026-08-15; черновик был записан готовым решением
ценой выше M, до оформления его разобрал несогласный критик, находки в разделе
«Что нужно разобрать в LLD»

## Что происходит

Записанный черновик с дашборда не правится вовсе: у накопителя есть ручки
чтения, удаления и подъёма груминга (tools/dashboard/server.go, строки 84-89),
а ручки записи текста нет. Опечатка или дописка правятся только редактором
на машине. Экран записи после DK-321 и DK-328 показывает ход и исход груминга
и даёт повторный заход с уточнением, но разговор с грумером этим и
исчерпывается: реплика уходит только новой headless-сессией, а не в идущую.

Процесс работы с черновиком разорван: запись, правка, разбор и ответы на
вопросы агента живут в разных местах и разных механиках, и пользователь
собирает их в одно целое руками. Хочется единого процесса: форма записи, где
текст правится, рядом разговор с агентом, запуск груминга виден этим же
разговором, а вопрос агента приходит диалогом, а не догадкой по следам на
диске.

## Чего хотим

Принятый документ в docs/lld/, который проектирует экран черновика как одно
место работы с записью: правка текста, разговор с грумером, запуск и остановка
разбора, вопросы и исход. К нему переработанные макеты в проекте Claude Design
«Дашборд devkit» (экран «12 Груминг черновика» и его соседи по навигации).
Реализация нарезается задачами по итогам документа, как это было сделано в
DK-328.

Решение, уже принятое на груминге и обязательное для дизайна: после
«Сохранить» пользователь возвращается в список черновиков, а «Сохранить и
грумить» поднимает разбор и открывает экран записи с его ходом. Так снимается
столкновение с черновиком DK-352, где предлагался возврат в список: обе
кнопки живут рядом, и каждая ведёт туда, где продолжается её работа.

## Что нужно разобрать в LLD

Находки критика не закрывают затею, но каждая требует ответа в документе, а не
обхода молчанием:

- Разговор с headless-сессией груминга не имеет носителя до DK-345 (почта
  любой живой сессии, P3, едет последней в серии DK-136), а поле ввода,
  которое никуда не ведёт, письменно отвергнуто в DK-328. Либо в документе
  называется, чем разговор жив до DK-345 (повторный заход уже есть), либо
  DK-345 двигается вперёд зависимостью.
- «Универсальный компонент разговора как в расширении vscode» пересекается с
  отвергнутыми вариантами развилки DK-136 (интерактивный клиент, потоковый
  ввод) и с уже построенной лентой чата цели. Второй механизм чата в кодовой
  базе не допускается (DK-316, DK-328), так что универсальность
  доказывается переиспользованием ленты, а не новым экземпляром.
- Правка текста при идущем груминге это конфликт записи: агент читает тот же
  файл, дописывает (draft defer) и удаляет (attach, drop). В документе нужно
  определить, что происходит с редактором, пока разбор идёт.
- Вопрос агента диалогом упирается в ту же механику доставки: сегодня вопрос
  оседает исходом «кончился вопросом» с полем уточнения. Чем диалог лучше
  повторного захода и что он стоит, нужно измерить, а не брать на веру.

## Следы симптомов из черновика

Жалоба «на форме никакой реакции, пока не перезайдёшь» на живом стенде не
воспроизвелась: свежий бинарь показывает поднятую работу первым же снимком
после подъёма, пять заходов из пяти, а подписи частичной перерисовки экрана
записи учитывают идущий разбор (draftPhase). Наблюдение было на билде
1587-g48086234 с теми же экранами, так что механизм не найден. Ловить его
нужно при следующем живом груминге с дашборда, для этого и сделана эта
пометка.

Жалоба «после груминга сессия висит активной» воспроизведена и уехала
отдельным багом DK-358: завершённый headless-разбор до 12 минут поднимается из
транскрипта интерактивной сессией, у которой стоп отвечает 409.

## DoD

- В docs/lld/ лежит принятый документ с разобранными развилками из раздела
  выше и нарезкой реализации на задачи.
- Макеты экрана черновика в Claude Design переработаны под единый процесс и
  приняты глазами.
- Строки реализации заведены на доску с зависимостями по нарезке.

## Приёмка

- вид: user
- барьер «согласие»: решения документа и вид макетов принимает пользователь,
  обхода у согласия нет.

## Ранг

`25+8+3+0+4 = 40`, P2.

- Серьёзность 25 (Major). Текст записи с дашборда не правится вовсе, разговор
  с грумером собирается из повторных заходов; обход дешёвый и очевидный,
  редактор и терминал на машине.
- Ценность 8. Черновики пишутся с телефона, и в цель DK-327 входят сценарии
  дашборда с экрана целиком; единый процесс записи и разбора это прямой вклад
  в эту ценность, и пользователь просит его сам.
- Неопределённость 3. Общий подход понятен (переработка экрана поверх серии
  DK-136), в деталях развилки: носитель разговора, судьба редактора при идущем
  разборе, граница сервер-клиент. Снятие их и есть предмет LLD.
- Поправка на баг 0. Правки текста и разговора не было никогда, это новое
  поведение.
- Рычаг 4. Доказанная универсальность ленты разговора нужна другим местам
  дашборда, а нарезка документа разблокирует реализацию серии.

## Ход работы

- Вычитка: 2 файла, 12 правок, 0 пометок, 2026-08-15.
- Исполнение: opus/xhigh по вердикту pick, 2026-08-15.
- LLD: документ `docs/lld/DK-354-draft-screen.md` с ответами на четыре находки
  и нарезкой реализации; замер снят с трёх живых заходов груминга.
  Макеты «12 Груминг черновика» и «07 Заведение» переработаны в Claude Design,
  ждут приёмки глазами. 2026-08-15.
- Пользовательский круг приёмки: жаргон вычищен из документа и макетов,
  вопрос грумера переехал в ленту структурным блоком по образцу
  AskUserQuestion (решение 4 документа), нарезка выросла до пяти строк.
  2026-08-15.
- Вычитка: 2 файла, 7 правок, 0 пометок, 2026-08-15.
- Ревью: субагент opus/xhigh по вердикту pick, 2026-08-15.
- Вычитка: 2 файла, 5 правок, 0 пометок, 2026-08-15.

## Из черновика DK-352 (записан 2026-08-15)

### Черновик

Дашборд. После записи черновика появляется промежуточная форма с кнопками "Добавить ещё" и "На доску". Нужно убрать эту форму, а после записи черновика пользователя должно возвращать в список черновиков, аналогично в случае создания задачи возврат идёт в список задач. Также должна быть возможность помимо просто записи черновика запустить его груминг. То есть по сути две кнопки "Сохранить" и "Сохранить и грумить".

## Ревью

- Нарезка против инварианта зависимости RANKING.md: «правка текста» (ранг 37) названа жёсткой предпосылкой «ленты черновика» (ранг 38, строка «лента черновика стоит на выносе и на редакторе»), а инвариант требует ранг предпосылки не ниже держимой задачи. Либо ранг правки текста подтягивается до 38, либо зависимость от редактора объявляется мягкой (лента может выйти поверх старого просмотра текста) и уходит из «После».: исправлено, зависимость от редактора объявлена мягкой и убрана из После, лента стоит только на выносе (ранги равны)
- Решение 3 не закрывает требование постановки «судьба набранного текста» для двух исходов ручки PUT: у 409 «жива сессия разбора» (гонка: разбор поднят из списка или вторым окном, пока человек набирал) и у 404 «файла нет» судьба набранного не названа, в отличие от 409 базы, где сказано «набранное человеком не пропадает». Дописать в документ, что экран делает с набранным в этих двух исходах.: исправлено, в решение 3 дописана судьба набранного у 409 живой сессии и 404
- Раздел «Задача и рамка» расходится с кодом: в диапазоне server.go 84-89 строка 84 это POST /drafts (handleDraftPost, запись текста нового черновика), так что «нет вовсе ручки записи текста ... там чтение, удаление и подъём разбора» неточна. Правильнее «нет ручки правки текста существующей записи».: исправлено, формулировка исправлена: нет ручки правки существующей записи, строка 84 названа записью нового черновика
- Слово «композер» (7 употреблений) это англицизм против домашнего словаря: README дашборда и app.js везде говорят «поле ввода». Заменить по документу.: исправлено, композер заменён на поле ввода по всему документу
- круг пользовательский: слово «ходка» это уголовный жаргон, вычистить из LLD-документа, своих добавлений в файл задачи и видимых текстов макетов; замена из лексики пользователя: заход, прогон, сессия разбора: исправлено, слово заменено на заход по документу, файлу задачи и видимым текстам макета 12, грамматика согласована, цитаты ревью не тронуты
- круг пользовательский: поле уточнения при исходе «кончился вопросом» это костыль; вопрос грумера приходит в ленту структурным блоком по образцу AskUserQuestion (текст, варианты с подписями и пояснениями, всегда «Другое» со свободным текстом), выбор заказывает следующий заход разбора; транспорт прежний, разобрать источник вариантов (формат исхода и промпт груминга), жизнь блока в ленте без второго механизма чата, деградацию без вариантов; переработать решение о вопросе, нарезку и макет 12: исправлено, решение 4 документа: блок вопроса по образцу AskUserQuestion, формат исхода в скилле board-groom, блок в общей ленте без второго механизма, деградация до свободного текста; нарезка выросла до пяти строк, макет 12 переработан
- После перенумерации решений ссылка в пункте 2 нарезки разъехалась: «Кнопки формы ... возвраты по решению 4» указывает теперь на блок вопроса, а возвраты кнопок живут в решении 5. Поправить номер.: исправлено, ссылка пункта 2 нарезки поправлена на решение 5
- Правки приёмки вклеены в строки без переформата абзаца: строки 84 и 413 документа вылезли до 104-109 символов при выдержанной по документу ширине ~79 (строка 40 с 96 символами тянется с первой редакции). Перебить эти абзацы; таблицы нарезки не в счёт.: исправлено, строки 40, 84 и 413 переформатированы под ширину 79, заодно строка 189; таблицы не тронуты
- Замечание 8 закрыто наполовину: переформат перебил только вылезшие строки, не перетёк абзацы до конца, и остались огрызки посреди прозы: строка 42 «(sessionTask). Следы пишут» на 28 символов и строка 193 «захода, и» на 11. Перетечь оба абзаца целиком до ровной ширины.: исправлено, оба абзаца перетекли целиком до ширины 79, огрызков не осталось
