# DK-365: Цель: квота второй подписки под контролем конвейера

## Цель

15 августа выход GLM-5.3 сменил тарификацию Coding Plan, и цель DK-327 выжгла
пятичасовое окно второй подписки вслепую: снимка остатка glm в devkit нет,
назначение исполнителей остаток не смотрит, конвейер собирал отказы посреди
витка. Цель закрывает эту слепоту готовой тройкой задач: съёмщик остатка
(DK-181), гейт назначения (DK-364), счёт ступени по её подписке (DK-182).

DoD, все три пункта проверяются командами:

1. `agentctl quota --harness glm-code` печатает остаток обеих шкал подписки
   (пятичасовое окно и неделя) со временами сброса.
2. Прогон `pick`/`run` с подменённым пустым снимком glm уводит ступень base на
   claude-code либо ставит в очередь с временем сброса, и это видно в выводе;
   с живым снимком назначение прежнее.
3. Расход ступени считается по подписке, на которой она работала.

## Бюджет

бюджет: week_all <= 10
бюджет: week_max <= 6

## Задачи цели

Состав собран грумингом 2026-08-15 из готовых строк доски, нарезка первому
витку не нужна:

- DK-181 съёмщик остатка второй подписки: quota --harness (P2, R=40, M)
- DK-364 назначение исполнителя с учётом остатков подписок (P2, R=39, M,
  после DK-181)
- DK-182 корректор считает ступень по её подписке (P3, R=11, M, после DK-181)

## Журнал

Наполняют витки: строка на виток и снимки квоты.

## Итог

Пишется последним витком.
