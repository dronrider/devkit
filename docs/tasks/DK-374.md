# DK-374: Реплика в работающий заход: подхват получает адрес черновика [после DK-373]

Постановка: [lld/DK-354-draft-screen.md](../lld/DK-354-draft-screen.md)

## DoD

## Расхождение с решениями DK-430

2026-08-18, нарезка цели DK-397: подхват реплики адрес черновика получает не
своей правкой, а общим правилом адресности из решения 2 документа
`docs/lld/DK-430-task-chat.md`: вход принадлежит разговору, реплика сессии идёт
с адресатом, реплика разговору без него. Строка сводится к тому, чтобы черновик
завёл себе вход по общему правилу, отдельной механики отметок в ней нет.

## Проверка

2026-08-28, ветка POC `poc-chat`. Своей правки в строке не осталось: предмет
сняли коммиты ветки, пока строка стояла в Backlog.

- `5c4a82c2` «ожидание по черновику слушает тот же вход, что и задача, и вид
  записи узнаёт само» и `1fed2812` «имя разговора у записи одно, отдельного
  draft-ID больше нет»: разговор черновика зовётся `task-<ID>` в общем
  `.devkit/chat`, и своего адреса, которому подхват учился бы отдельно, у него
  нет вовсе (живой случай DK-517).
- `dcb731a0`: безадресную строку разговора задачи забирает только его хозяин,
  сессия из реестра чатов либо работающая в боковом дереве задачи
  (`owns_chat` в `hooks/chat-in.py`). Сессию разбора дашборд поднимает с
  `DEVKIT_TASK=<ID>` (`launchEnv`), поэтому запись реестра у грумера есть, и
  хозяином разговора записи он считается по общему правилу.
- Отметок доставки у входа нет ни у задачи, ни у записи: доставленная строка
  уходит из входа под тем же замком, которым пишется. Дока про это стоит в
  `hooks/README.md`, раздел «Вход разговора любой сессии», где грумер черновика
  назван прямо.

Живая проверка, шаг 5 прогона DK-373 (запись накопителя DK-551, сессия разбора
`aa74cd11-4453-4edf-be63-0d074edd9477`, поднята дашбордом). Реплика послана в
идущий заход ручкой `POST /api/projects/devkit/sessions/{sid}/message`:

```text
{"chat":"task-DK-551","line":"2026-08-28 14:49, сессии aa74cd11-4453-4edf-be63-0d074edd9477, из дашборда: канал проверен, больше по этой записи ничего не делай и заканчивай заход","message":"реплика легла в чат task-DK-551 дерева сессии aa74cd11-4453-4edf-be63-0d074edd9477: подхват доставит её в идущий ход","session":"aa74cd11-4453-4edf-be63-0d074edd9477","tree":"/Users/rider/projects/devkit"}
```

Подхват забрал строку в ход, шедший в тот момент, за три секунды. Журнал
`~/.devkit/chat-in.log`:

```text
2026-08-28T14:49:10 сессия aa74cd11-4453-4edf-be63-0d074edd9477 адрес task-DK-551 доставлено строк 1, первая «канал проверен, больше по этой записи ничего не делай и заканчивай заход»
```

Заход реплику увидел и по ней остановился, вход после доставки пуст.
