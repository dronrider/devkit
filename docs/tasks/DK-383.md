# DK-383: Приоритет груминга накопителя: три уровня и сортировка draft list

записан 2026-08-16, оформлен 2026-08-16

## Что происходит

Накопитель черновиков дорос до 38 файлов, груммить всё подряд не хочется, а
выбирать нечем. RANKING.md на черновик не ложится: слагаемые ранга оценивают
задачу, которой ещё нет, неопределённость у всех черновиков высокая по
определению и различающего слагаемого нет. Ранг строки доски отвечает на вопрос
«что исполнять», черновику нужен ответ на «что разбирать следующим», это другая
ось.

Проверено грумингом 2026-08-16, чтобы исполнитель не проверял второй раз:

- команды приоритета в taskctl нет: греп «prio» по tools/taskctl пуст,
  сортировка одна, в loadDrafts по номеру ID (tools/taskctl/draft.go), её берут
  печать, --json и хвост taskctl list;
- дубля нет: DK-201 ищет похожие задачи, DK-238 и DK-209 про сроки и стоимость
  закрытых, предметы не пересекаются;
- несогласный критик со свежим контекстом прогнан (цена M, готовое решение),
  его находки вплетены в раздел «Чего хотим».

## Чего хотим

Грубая шкала приоритета разбора high/mid/low, на глаз, без чисел и привязки к
слагаемым RANKING: приоритет разбора не сводится к серьёзности будущей задачи,
идея с нулевой серьёзностью может заслуживать разбора раньше мелкого бага.
Ставится автором при записи (--prio) или любым проходом списка, маркировка не
дороже записи. Механика по образцу defer:

- taskctl draft prio <ID> high|mid|low со снятием через --clear; в файле
  черновика строка рядом с «записан»: «приоритет: высокий», русское слово в
  файле и печати, латинские имена в команде и в поле prio json, как у суффикса
  приёмки;
- сортировка в loadDrafts, один порядок для печати, json, хвоста list и
  дашборда: high, mid, low, затем немаркированные отдельной группой, отложенные
  после всех; внутри уровня по возрастанию ID, как на доске;
- перенос add --id и приписка draft attach выкидывают строку метки из файла:
  метаданные в файле задачи не дублируются (п. 3 правил доски), метка не
  переживает черновик;
- defer метку не трогает: отложенные стоят после всех независимо от метки,
  defer --clear возвращает черновик на место по метке;
- prio повторяет ensureWritten: первой записи в старый черновик строка
  «записан» ставится из ModTime до самой записи (решение LLD DK-129);
- парсер метки читает только шапку до «## », строка «приоритет:» в теле идеи
  меткой не ложится;
- справка, текст отказа draftTextGuard и карта подкоманд дополняются prio;
- дашборд рисует чип уровня в строке черновика (draftRow): порядок списка
  меняется, и перемена должна быть видна на экране, а экран черновиков это
  вход в грумминг (DK-328.2, DK-354.4);
- п. 5 RULES.board.md получает абзац про приоритет разбора с оговоркой
  коллизии: слово «приоритет» в п. 2 занято выводом из ранга, приоритет
  черновика это очередь разбора на глаз, живёт и умирает в накопителе;
- скилл board-groom доезжает: разбор начинается с верха списка с учётом меток.

Границы: черновики остаются одной кучей без секций и переходов, уровни не
статусы; авто-гниения метки по возрасту нет, возраст и так напечатан рядом;
немаркированный черновик нормальное состояние, как прочерк цены в бэклоге.
Разовая чистка накопителя (живость плюс простановка меток) идёт после этой
задачи и с оговоркой: путь add --id с неагентским видом приёмки и --link
затирает перенесённый файл (живой баг DK-329), чистка либо ждёт его закрытия,
либо обходит связку --link с неагентским видом.

## Соседние строки, которые задача не дублирует и не ждёт

- DK-201 ищет похожие задачи по доске и архиву при заведении и груминге, а не
  порядок разбора накопителя: предметы не пересекаются.
- DK-329 чинит затирание перенесённого файла: предметов пересечения нет, но
  массовая чистка накопителя зависит от него оговоркой выше.

## DoD

На синтетической доске: draft prio ставит метку, перезаписывает и снимает;
draft list печатает группы high, mid, low, немаркированные, отложенные, внутри
уровня по возрастанию ID, пометка вида «(9 дней, высокий)» в строке и в хвосте
taskctl list; --json отдаёт поле prio; defer метку не трогает, defer --clear
возвращает черновик на место по метке; add --id и draft attach выкидывают
строку метки из перенесённого текста. Отказ у draft называет prio. Дашборд
печатает чип уровня. RULES.board.md и скилл board-groom дописаны.

## Ранг

`25+6+2+0+2 = 35`, P2.

- Серьёзность 25 (Major). «Грумить всё подряд не хочется, а выбирать нечем»:
  заметное трение рабочего потока, обход (выбор на глаз) сам и есть источник
  боли. Критик предлагал Minor, боль названа пользователем вслух, держу Major.
- Ценность 6. Разбор накопителя перестаёт быть перебором: ощутимое улучшение,
  которого ждут.
- Неопределённость 2. Подход закрыт, открыты детали, названные критиком и
  решённые выше: место сортировки, судьба метки при переносе и откладывании.
- Поправка на баг 0. Новая работа: приоритета разбора не было никогда.
- Рычаг 2. Разбор с верха списка удешевляет каждый следующий заход груминга:
  небольшая своя польза.
