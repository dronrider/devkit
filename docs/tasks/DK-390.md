# DK-390: board-groom держит полное описание трёх ворот, прочие входы ссылаются туда

## Ход работы

- Разработка: подпроцесс glm-4.7/low по вердикту pick (потолок цели: pro, вердикт и так не выше; квота: объявления нет, корректор выключен), 2026-08-17 02:05-02:06.

- В раздел «Ворота готовности» скилла board-groom после фразы «Ворот трое, и
  все три проверяются, а не ощущаются» добавлена фраза про единственное место
  с полным описанием трёх ворот.
- Таблица входов и проверок из LLD в скилл не перенесена: её строка про
  грумминг повторяет то, что уже написано в разделе ворот и шагах процедуры,
  а остальные строки (board-task, нарезка цели, постановка цели) описывают
  чужие скиллу входы. Решение: ограничиться фразой.

## Сценарий проверки

Подготовительные шаги по ветке, до слияния:

- Таблицы входов в скилле нет: `grep -n 'Таблица входов' ...` пуст, причина
  отказа названа выше.
- `python3 ~/projects/devkit/hooks/check-symbols.py kit/skills/board-groom/SKILL.md
  docs/tasks/DK-390.md` чист.

Агентский шаг, после слияния в main:

- В свежем main `grep -n 'единственное место с полным' kit/skills/board-groom/SKILL.md`
  находит фразу, и лежит она в разделе «Ворота готовности» того же файла:
  раздел, который RULES.board.md п. 5 называет по имени, в слитом main
  действительно держит полное описание трёх ворот. Это якорь, на который
  ссылаются задачи DK-391, DK-392 и DK-393.

## Исключения

- Исключение: тесты (правка бескодовая: фраза в тексте скилла и файл задачи).

## Ревью

- Сценарий проверки не держит агентской части: все три шага (греп фразы, греп отсутствия таблицы, check-symbols) гоняются по файлам ветки и зелёные до слияния, а вид приёмки в нарезке LLD agent. По правилу «Самодостаточность агентской части» в ACCEPTANCE.md агентской считается шаг, прогоняемый после слияния в main у задачи без выката и доказывающий что-то про слитое. Здесь это, например, греп фразы в слитом main вместе с проверкой, что раздел, который RULES.board.md называет по имени, в main её держит: якорь, на который ссылаются задачи DK-391, DK-392 и DK-393. Сценарий переписывается, вид остаётся agent.: исправлено

## Выкат

- 2026-08-17 слито: 40622f7a, b941e0c3, 6447b69a
