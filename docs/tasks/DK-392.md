# DK-392: goal-loop и goal-cut: ворота заменяются связью с целью, ссылка на цель уходит в файл задачи нарезки

## Ход работы

- Разработка: подпроцесс glm-5.3/high по вердикту pick (потолок цели: pro, вердикт и так не выше; квота: объявления нет, корректор выключен), 2026-08-17 02:05-02:06.
- В goal-loop, шаг 3, в конец абзаца про единое место правил нарезки добавлена фраза «Ворота здесь заменяются связью с целью и бюджетом (см. LLD DK-133)» дословно из строки 4 нарезки LLD. В goal-cut переписан абзац «Список ID и есть состав цели»: старое объяснение про ссылку в ячейке до первого `taskctl file` убрано, новый текст держит механику решения 4 (`taskctl add --link` заводит файл задачи вместе со строкой, ссылка на файл цели уходит в болванку, ячейку строки занимает файл задачи) и наполнение DoD строк нарезки из файла цели при заведении. Соседний абзац материализации не тронут.
- Ревью: подпроцесс glm-5.3/high по вердикту pick (потолок цели: pro, вердикт и так не выше; квота: объявления нет, корректор выключен), 2026-08-17 02:17-02:46.

## Исключения

- Исключение: тесты (правка бескодовая: два текстовых фрагмента скиллов, кода не тронут, смена проверяется агентским сценарием грепом и check-symbols).

## Сценарий проверки (agent)

1. `grep -n "Ворота здесь заменяются" kit/skills/goal-loop/SKILL.md` выдаёт единственную строку внутри шага 3, между строками «3. Нарезка, первым витком» и «4. Рабочая фаза», и следующая строка кончается текстом «связью с целью и бюджетом (см. LLD DK-133).»
2. `grep -n "taskctl file" kit/skills/goal-cut/SKILL.md` ничего не выдаёт: старый абзац «ссылка в строке доски живёт до первого `taskctl file <ID>`» убран.
3. `grep -n "уходит в болванку" kit/skills/goal-cut/SKILL.md` выдаёт строку абзаца «Список ID и есть состав цели», а `grep -n "DoD строк нарезки" kit/skills/goal-cut/SKILL.md` выдаёт последнюю строку того же абзаца, с наполнением DoD из файла цели при заведении.
4. `python3 ~/projects/devkit/hooks/check-symbols.py kit/skills/goal-loop/SKILL.md kit/skills/goal-cut/SKILL.md docs/tasks/DK-392.md` завершается кодом 0.

## Выкат

- 2026-08-17 слито: 077aebee, 11b6322b
