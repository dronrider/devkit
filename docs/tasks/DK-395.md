# DK-395: Цель: вопрос человеку паркует задачу, а не конвейер

записан 2026-08-17

## Цель

Сегодня вопрос человеку гасит всю петлю: `wait-human` это маркер витка цели, и
оболочка останавливает цикл на любом маркере кроме `continue`
(`kit/skills/goal-loop/goal-run.py`, MARKERS и STOP_KEY). Человек уехал на
выходные, и конвейер стоит целиком, хотя из четырёх штатных поводов
`wait-human` три относятся к одной задаче, а не к цели.

Замысел. Вопрос принадлежит задаче, а не циклу. Агент, упёршийся в трудность,
инициирует вопрос с адресом задачи, задача паркуется, а конвейер получает не
стоп, а повод выбрать работу заново: подождать заданный таймаут (человек может
быть за клавиатурой), потом посмотреть на своё состояние (сколько задач в
работе, что стоит на доске, что из этого не связано с ждущими ответа) и решить,
брать ли новую задачу или заняться подготовительной работой вроде груминга
накопителя. Ответ приходит, когда придёт, и возвращает в оборот свою задачу, а не
размораживает всё скопом.

Возврат при этом не автоматический. Ответ это повод принять решение заново:
конвейер детерминированно смотрит на состояние задачи и на состояние бэклога и
выбирает между «доделать» и «встать в очередь». Задача, у которой остались
тесты, доделывается без остановки; задача, брошенная в середине разработки при
полном бэклоге, ждёт своей очереди на общих основаниях. Привилегии перед доской
у припаркованной нет, частичный задел это довод в её пользу, а не пропуск без
очереди.

## Что уже есть и переиспользуется

- Парковка механически существует: `taskctl move <ID> blocked "причина"`, в
  тестах живут ровно такие случаи («ждём ответ хостера», «ждём железо»).
- Зависимости на месте: `taskctl dep add`, и `dep.go` не пускает в работу
  задачу с незакрытой предпосылкой.
- Вопрос с таймаутом написан: `goal-run --ask` кричит уведомителем, держит ящик
  признаком с внутренним сроком, опрашивает «Входящие» и по истечении срока
  возвращается без ответа, не залипая. Не хватает того, чтобы на это время
  конвейер уходил работать, а не спал в опросе.
- Ящик и почтальон (`hooks/inbox.py`), лента и чат дашборда.
- Потолок пачки по квоте (`agentctl budget`), отбор кандидатов
  (`taskctl batch`), поезд и очередь слияния (LLD DK-306).
- Этапы задачи с видом деятельности и временем начала лежат в
  `~/.devkit/runs/<ID>-<slug>.run`, среди видов есть `уточнение` с текстом
  вопроса, и дашборд их читает.
- Адрес вопроса у задачи и общий носитель писем кладёт цель DK-397 пунктами 3
  и 4 своей нарезки, эта цель их наследует и второй копии не заводит.

## Чего нет

1. Состояния «ждёт ответа» с пробуждением. `Blocked` терминальный: никто не
   следит за приходом ответа и не поднимает строку обратно. Нужна машинная
   причина, связь с вопросом и тик, который возвращает задачу в кандидаты;
   `devkitctl watch` умеет тикать.
2. Достигнутого рубежа. Запись `.run` отвечает, чем задача занята, а решение
   «доделать или отложить» требует ответа, что уже готово: код написан, тесты
   зелёные, ревью пройдено, слито, выкачено. Плюс пакет этапов стирается на
   смене статуса, уезжая прозой в «Ход работы».
3. Порядка «что брать сейчас». Ранг отвечает, что важнее вообще, а сведение
   ранга, цены, связей и остатка квоты в решение по слоту системно не делает
   никто. Пункт унаследован от распущенной цели DK-168, где он стоял второй из
   трёх дыр.
4. Полки работ для простоя. `taskctl batch` отвечает только на вопрос, какие
   задачи брать. Списка того, что всегда можно делать без человека (груминг
   накопителя до вопроса, вычитка, карта, дожатие тестов), нет.
5. Потолка незакрытых деревьев и потолка висящих вопросов. Без первого
   конвейер накопит хвостов, без второго человек вернётся к простыне вопросов
   вместо пачки.
6. Правил возврата к остывшей задаче: ребейз на уехавший main, свежий
   исполнитель, ответ человека в постановку.

## Формула выбора

Ранг не трогается: `RANKING.md` сознательно не делит важность на трудозатраты,
это ответ на вопрос «что важнее вообще». Планировщик отвечает на другой
вопрос, «что делать в этом слоте сейчас», и цена там законна: это прямо
названо в разделе «Цена исполнения». Считается внутри полосы P, иначе дешёвый
P3 выдернет наверх P0 и сломается инвариант плюс-минус одной полосы.

Три слоя, не смешивать. Ворота булевы (незакрытая предпосылка, неопределённость
выше 1, нет цены, висящий вопрос без ответа, нет квоты на нужный ярус, дерево
или файлы заняты, выбран потолок деревьев, человек недоступен и путь упирается
в приёмку вида user). Счёт числом для прошедших ворота. Полка фона, когда после
ворот кандидатов нет.

    score = R / W,  внутри своей полосы P
    W = C * (1 - F) + V

R это ранг строки, C цена в пунктах, F доля пройденного пути, V плата за
возврат к остывшей задаче. Плата нужна потому, что припаркованная задача
дорожает сама: свежему исполнителю входить в контекст, ветку ребейзить на
уехавший main (`git rev-list main ^branch --count` считает расхождение машинно).

## Замер по 149 закрытым задачам

Маркерные коммиты доски дают секундные метки переходов («в работу», «замечания
ревью», «коммиты слияния», «в Check», «закрыта»). Активное время считается с
вырезкой простоев дольше 90 минут. Медиана активного времени задачи 55 минут.
Числа таблицы печатает вложенный ниже скрипт, прогон 2026-08-17.

| Фаза | Доля средняя | Доля медианная | Минут (медиана) |
|---|---|---|---|
| вход и основной код | 35.1% | 34.7% | 15.2 |
| доводка кода и тестов | 20.4% | 18.5% | 7.2 |
| ревью, слияние, выкат | 33.6% | 29.5% | 13.9 |
| приёмка | 10.9% | 4.1% | 1.4 |

Первую строку нельзя читать как чистую цену входа в контекст: первый коммит
кода несёт 69% всех строк задачи (медиана по 130 задачам), то есть до него
пишется основная работа. Честная формулировка: 35% времени уходит на всё, что
случается до первого коммита.

Доли по цене почти не расходятся (вход у S 36%, у M 35%, у L 27%), а сама цена
слабо предсказывает время: S медиана 47 минут, M 58, L 69. Отношение S к M это
0.81, тогда как курс квоты из `goal-cut` даёт 0.375. Расхождение не ошибка:
курс меряет расход токенов, замер меряет занятость слота. Ресурса два, и делить
надо на тот, который дефицитен.

Приёмка двухголовая: медиана 1.4 минуты (агентские сценарии), но 17 задач
простояли в Check дольше часа, максимум 12.2 часа. Кругов ревью с замечаниями:
у 50 задач один, у 8 два, у 3 три.

Плата за возврат замерена прямо. Нашлось 9 задач с двумя заходами (возврат из
Check обратно в работу): второй заход стоит 52% первого (медиана 36.9 минуты
против 71), при том что делалась там правка по замечанию, а не задача заново. У
двух задач с паузой в 8-10 часов (DK-087, DK-284) возврат вышел дороже, вплоть
до случая, где второй заход дороже первого. Девять точек это не закон, но
направление видно: чем дольше задача стыла, тем дороже возврат.

Отсюда числа.

| Рубеж | F |
|---|---|
| строка заведена, дерево есть | 0.00 |
| первый коммит кода лёг | 0.35 |
| код и тесты готовы | 0.56 |
| ревью пройдено, слито, выкачено | 0.89 |
| приёмка пройдена | 1.00 |

Промежуточных узлов между ревью и выкатом нет сознательно: слияние с выкатом
занимают 0.1 минуты медианы. Все пять рубежей наблюдаемы машинно, и это важнее
стройности таблицы.

Цена C: курс квоты (S 0.75, M 2, L 5.5 пункта) при дефиците подписки и время
слота (S 0.8, M 1.0, L 1.2) при дефиците параллелизма.

Плата за возврат: `V = C * min(0.3 + 0.02 * h, 0.55)`, где h часы простоя. Даёт
0.32 при паузе в час и 0.46 при паузе в восемь.

Проверка на живых строках. DK-331, свежая S с рангом 42: `W = 0.8`, score 52.
Припаркованная M с рангом 40 после ревью (F 0.89), простоявшая сутки:
`W = 0.11 + 0.55 = 0.66`, score 61, доделываем. Она же, остановленная в
середине кода (F 0.45) после часа простоя: `W = 0.55 + 0.32 = 0.87`, score 46,
уступает свежей S и встаёт в очередь.

Неприятный вывод из чисел: хвост дорог. Задача после ревью с остатком в 11%
работы стоит две трети цены целой задачи, потому что повторный вход съедает
0.55. Значит главный выигрыш даёт не умный выбор между хвостом и новой задачей,
а недопущение хвостов: жёсткий потолок незакрытых деревьев и правило доезжать
до ближайшего рубежа перед парковкой стоят больше самой формулы. Формула нужна,
чтобы решение было объяснимым и одинаковым.

## Границы замера

Доли посчитаны по времени, а не по токенам, и для второго ресурса они могут
лежать иначе: ревью гоняет модель high, вход читает много файлов. Прямой счёт
по журналам сессий это черновики DK-377, DK-378 и DK-209; когда они закроются,
таблицу F стоит пересчитать в токенах и сравнить. Скрипт замера вложен ниже,
чтобы пересчёт был командой, а не разовой работой.

## Что решать дизайном

Первой задачей цели идёт LLD, вид приёмки у него user по барьеру «согласие».
Развилок на входе столько: где живёт рубеж и кто его двигает, как устроена
парковка и пробуждение, кто считает формулу (утилита или сессия), из чего
собрана полка фона, каковы потолки деревьев и висящих вопросов, что остаётся от
`wait-human` в `goal-loop`. Порядок реализации виден так: рубеж машинно, затем
парковка с пробуждением, затем планировщик, затем полка фона с потолками, затем
отвязка `wait-human` от стопа цикла.

## DoD

1. Рубеж задачи читается машинно и переживает смену статуса: на синтетической
   доске задача, дошедшая до ревью, отдаёт свой рубеж командой, а не прозой
   раздела «Ход работы».
2. Вопрос исполнителя паркует свою задачу и только её: на синтетической доске с
   тремя задачами вопрос по одной оставляет две другие в кандидатах, а сам
   цикл не останавливается.
3. Ответ будит припаркованную задачу тиком `devkitctl watch` и возвращает её в
   кандидаты, а не сразу в работу; решение «доделать или в очередь» принимает
   планировщик с указанием причины.
4. Планировщик печатает порядок кандидатов внутри полосы с разбором слагаемых
   (ранг, цена, рубеж, плата за возврат) и причиной отказа по каждому
   отсеянному воротами.
5. При пустых кандидатах конвейер берёт работу с полки фона, а не спит, и это
   видно снаружи.
6. Потолок незакрытых деревьев и потолок висящих вопросов выбираются и
   отбиваются с внятным отказом.
7. `wait-human` больше не останавливает цикл цели, когда вопрос адресован
   задаче; стоп остаётся только на поводе, который относится к самой цели.
8. Правила осели в долгоживущих доках (`RULES.board.md`, скиллы `board-batch`,
   `goal-loop`, `board-task`), прогон `test` из `.devkit/deploy.local` зелёный.

## Приёмка

Вид agent: каждый пункт DoD проверяется прогоном на временном репозитории с
синтетической доской, эталон берётся из кода возврата и строк вывода. LLD цели
получит свою строку с видом user по барьеру «согласие» при нарезке.

## Сценарий проверки

Восемь шагов по пунктам DoD, эталон в командах и строках вывода, прогон
2026-08-17 вложен в раздел «Итог».

1. `taskctl progress <ID>` на закрытых задачах цели печатает рубеж 1.00.
2. На стенде вопрос паркует свою строку (`taskctl move <ID> blocked
   --reason "вопрос: ..."`), соседние остаются кандидатами `taskctl slot`,
   цикл не останавливается.
3. Ответ в ящик плюс тик `devkitctl watch` возвращают строку в работу,
   будящий коммит уходит в origin сам; возврат решает планировщик.
4. `taskctl slot` печатает порядок внутри полосы с разбором W (C, F, V) и
   отказы группами по воротам.
5. На цели с пустым слотом виток берёт работу с полки, строка «Журнала»
   называет работу и признак.
6. На стенде третий вопрос цели получает отказ «вопросов висит 2 из 2»,
   взятие сверх потолка деревьев отказ «потолок деревьев N исчерпан» со
   строкой «ждут разбора».
7. `grep -n "wait-human" kit/skills/goal-loop/SKILL.md` находит стоп только
   на поводах уровня цели, задачные поводы паркуют задачу.
8. `grep` находит новые правила в RULES.board.md и скиллах board-batch,
   board-ship, goal-loop, board-task; прогон `test` из
   `.devkit/deploy.local` зелёный.

Ожидаемый итог: все восемь шагов сходятся, несошедшийся шаг возвращает
задачу цели в работу, а не цель в Check.

## Бюджет

бюджет: week_all <= 40
бюджет: week_max <= 15

Рамка поднята с 25/10 пользователем при запуске цикла 2026-08-17.

Потолка яруса нет, вердикт pick работает как обычно.

Бакеты общие на машину, параллельная сессия тратит тот же week_all, так что
числа это верхняя граница трат машины за время цикла, а не счётчик одной цели.
На постановке снимок показывал week_all 97% и week_max 100% при сбросе в тот же
день в 14:59.

## Задачи цели

Пробная нарезка 2026-08-17 материализована тем же днём, порядок держит dep,
сверена с состоянием кода и доски.

- DK-400 (LLD, M, R=65, приёмка user по барьеру «согласие»). Развилки цели
  решаются документом, отвергнутые варианты с причинами.
- DK-401 (task, M, R=37). Рубеж задачи машинным состоянием, переживающим
  смену статуса. После DK-400.
- DK-345 (task, M, R=65). Почта любой живой сессии: общий носитель писем
  с адресацией сессией из нарезки DK-397. Взята в цель как предпосылка
  DK-402 решением пользователя 2026-08-17, ранг поднят по инварианту
  предпосылки. До DK-402.
- DK-402 (task, M, R=63). Парковка задачи по вопросу и пробуждение тиком
  `devkitctl watch`. После DK-400 и DK-345 (транспорт DK-397).
- DK-403 (task, S, R=63). Отвязка `wait-human` от стопа цикла. После DK-402.
- DK-404 (task, L, R=39). Планировщик слота: ворота, счёт внутри полосы,
  разбор отказа. После DK-401 и DK-402.
- DK-405 (task, M, R=35). Полка фона и потолки деревьев и вопросов.
  После DK-404.
- DK-406 (task, M, R=62). Двухступенчатый Check из DK-275, с правкой пункта 6
  RULES.board.md под новое слово «непроверенный». После DK-400.
- DK-407 (task, S, R=35). Карта переходов из DK-384: docs/workflow.md плоскими
  строками со сторожем полноты в devkitctl doctor. Добавлена решением 8 LLD
  DK-400. После DK-401.

оценка: 9 задач (2 S, 6 M, 1 L), week_all 11-28 из 40, week_max 12-25
из 15; по week_all рамка сходится, по week_max остаток внутри вилки, верх
считается по худшему курсу всех M

## Смежное на доске

- DK-168 (цель «доска сама называет, что брать сейчас») распущена этим
  грумингом: её дыра про порядок «что брать сейчас» приехала сюда пунктом 4,
  две оставшиеся стоят своими строками.
- DK-384 (карта переходов работы) поглощается почти целиком: та же карта нужна
  машинной, а не только рисунком для читателя. Текст приписан ниже.
- DK-275 (двухступенчатый Check) входит в состав. Без него очередь выката съест
  выигрыш через две-три задачи: пользовательский Check держит её часами, замер
  в самом черновике называет 9, 7 и 5 часов. Текст приписан ниже.
- DK-373 (ящик и `draft ask`) становится несущей деталью, а не украшением
  дашборда: вопросы исполнителей и грумера ложатся в один ящик.
- DK-181 и DK-364 (остатки подписок в назначении) дают воротам данные о квоте.
- DK-396 держит вторую половину того же разбора: чем меньше поводов звать
  человека после старта, тем реже конвейеру приходится парковать задачи.

## Граница с DK-397

Общая несущая деталь одна, это адрес вопроса у задачи и ящик под него. Кладёт
её цель DK-397 пунктами 3 и 4 своей нарезки, эта цель наследует. Дублировать
носитель нельзя: третья копия механики отметок и ожидания разъедется с первыми
двумя молча. Зависимость стоит на уровне задач, а не целей: LLD пишется
параллельно с DK-397, а пункт 3 нарезки вешается на её транспортные строки
маркером «после».

## Журнал

- 2026-08-17, виток 1: пробная нарезка материализована строками DK-400..DK-406,
  LLD DK-400 написан и слит после двух проходов ревью, решением LLD добавлена
  DK-407 (карта переходов DK-384), порядок отрегулирован: DK-406 едет раньше
  DK-403, потому что фактический dep DK-402 -> DK-345 это и даёт; DK-400 в
  Check под согласие (приёмка user), остальные строки за dep; wait-human

- 2026-08-17, виток 2: DK-401 закрыта (рубеж задач читается командой
  `taskctl progress`, все пять отметок наблюдаемы, смена статуса рубеж не
  стирает), DK-406 закрыта (двухступенчатый Check: выкат с отметкой smoke
  освобождает очередь, пункт 6 RULES.board.md переписан под слово
  «непроверенный», скиллы board-ship, board-batch и goal-loop согласованы
  с новой очередью, три прохода ревью); обе по агентским сценариям поверх
  выката, DK-407 разблокирована; continue

- 2026-08-17, виток 3: DK-407 закрыта (docs/workflow.md с картой переходов
  плоскими строками, сторож полноты в devkitctl doctor: скиллы, статусы,
  рубежи читаются из носителей, пустой источник сам находка), заодно карта
  компонентов перегенерена с индексом решений DK-400, два прохода ревью;
  очередь цели упёрлась: DK-402 ждёт DK-345 из нарезки DK-397; continue

- 2026-08-17, виток 4: DK-345 закрыта (общий носитель писем .devkit/mail
  с адресацией сессией, почтальон доставляет вторым проходом, ручка
  дашборда пишет под тем же замком, отметок доставки нет: доставленная
  строка уходит из ящика; живой прогон доставки поверх выката), взята в
  цель решением пользователя как предпосылка DK-402, ранг поднят 40 -> 65,
  LLD DK-400 согласован с транспортом; DK-402 разблокирована; continue

- 2026-08-17, виток 5: DK-402 прошла ревью за четыре прохода (будящий move
  коммитит и пушит доску сам, страховка префикса ловит заглавную форму и
  ведущий пробел, тест обеих веток выбора бинаря), пережила паузу по слову
  пользователя, после продолжения слита fast-forward и выкачена; живой
  прогон: болванка DK-419 припаркована вопросом, разбужена тиком
  `devkitctl watch`, будящий коммит в origin сам; закрыта рубежом 1.00,
  DK-403 и DK-404 разблокированы, parallel.py 15 из 15; continue

- 2026-08-17, виток 6: DK-403 и DK-404 исполнены параллельно и закрыты. Первая
  отвязала `wait-human` от стопа цикла: стоп остаётся только на поводе уровня
  цели, задачные поводы паркуют задачу, поезд уезжает выкатом по освобождении
  очереди; вторая дала `taskctl slot`: ворота, счёт внутри полосы, слагаемые
  в печати, отказы группами, курс переключается ключом --resource. Обе
  прогнаны поверх выката, ручной счёт сошёлся с печатью; слияние DK-404 ушло
  в поезд при занятой очереди и выкачано составом с чужой DK-420; continue

- 2026-08-17, виток 7: DK-405 закрыта (полка фона легла в shelf.md у
  goal-loop, потолок висящих вопросов два на цель с чтением цели по файлу
  ссылки, отказ потолка деревьев допечатывает «ждут разбора»; два замечания
  ревью закрыты, живой прогон полки подъёмом витка на стенд-цели: слот пуст,
  с полки взят груминг накопителя, строка «Журнала» называет работу
  с признаком); DoD цели прогнан, восемь из восьми, итог ниже; цель в Check

## Итог

Прогон DoD последним витком, 2026-08-17, каждая точка живой командой или
живым прогоном поверх выката.

1. Рубеж читается машинно и переживает смену статуса: `taskctl progress
   DK-405` и `DK-402` печатают рубеж 1.00 у задач, прошедших все переходы
   до архива (DK-401).
2. Вопрос паркует свою задачу и только её: живой прогон DK-402 на болванке
   DK-419, соседние строки оставались кандидатами, цикл не останавливался.
3. Ответ будит припаркованную тиком `devkitctl watch`: тот же прогон,
   будящий возврат коммитил и пушл доску сам; решение «доделать или в
   очередь» принял планировщик, осиротевшая строка проходила кандидатом.
4. Планировщик печатает порядок с разбором слагаемых и отказами: живой
   вывод на доске в день закрытия, первый кандидат с разбором W (C, F, V)
   и отказы группами по воротам (DK-404).
5. При пустых кандидатах конвейер берёт работу с полки, и это видно
   снаружи: живой виток на стенд-цели, слот пуст, с полки взят груминг
   накопителя, строка «Журнала» назвала работу и признак (DK-405).
6. Потолки отбиваются отказом с числом: «вопросов висит 2 из 2 (XR-001,
   XR-002)» и «потолок деревьев 0 исчерпан» со строкой «ждут разбора»,
   прогнано на стенде с бинарем выката (DK-405).
7. `wait-human` больше не стоп цикла для задачных поводов: скилл
   goal-loop переписан, стоп остаётся только на поводах уровня цели,
   задачные поводы паркуют задачу (DK-403).
8. Правила осели в долгоживящих доках: RULES.board.md (слово
   «непроверенный»), скиллы board-batch, board-ship, goal-loop (полка
   и потолки), board-task (парковка вопроса и пробуждение тиком, правка
   последним витком под этот пункт); прогон `test` из `.devkit/deploy.local`
   зелёный, parallel.py 15 из 15.

Состав: девять задач нарезки закрыты (DK-400, DK-401, DK-345, DK-402,
DK-403, DK-404, DK-405, DK-406, DK-407), дорезок не понадобилось, оценка
нарезки (9 задач, week_all 11-28 из 40) не разошлась с фактом. Согласие на
LLD DK-400 дал пользователь в чате 2026-08-17, остальные сценарии агентские.

Потрачено по бакетам: замер недоступен, у харнеса glm-code секция квоты
пуста и гейт считал расход по журналу, точек расхода 0; гейт семи витков
отвечал ok. Цель уходит в Check: сценарий DK-400 держал барьер «согласие»,
закрывает пользователь.

## Ход работы

- 2026-08-17: груминг черновиков DK-395 и DK-397 вместе со строкой DK-168 одним
  заходом. Три ворот пройдены.

  Актуальность. Стоп петли на вопросе воспроизводится чтением
  `kit/skills/goal-loop/goal-run.py`: MARKERS перечисляет четыре маркера, STOP_KEY
  оставляет продолжение только `continue`. Парковка `taskctl move blocked` на
  месте и терминальна, пробуждения нет. Записи `.run` с видом «уточнение» есть,
  рубежа в них нет. Замер по 149 закрытым задачам прогнан 2026-08-17, скрипт
  вложен разделом ниже и повторяется командой.

  Дубля нет: по Backlog DK-168 пересекалась и распущена решением пользователя,
  DK-181 и DK-364 дают данные воротам другим предметом, DK-373 остаётся за
  черновиком дашборда. По накопителю DK-384 и DK-275 приписаны сюда, DK-396
  держит вторую половину разбора и остаётся отдельным черновиком.

  Ранг 50+9+3+0+4 = 66: серьёзность 50 (автономная работа конвейера, ради
  которой devkit и делается, ломается на первом вопросе, обход это ручной
  перезапуск с потерей часов простоя), ценность 9 (прямой вклад в главную
  ценность), неопределённость 3 (замер сделан и формула предложена, но шесть
  развилок открыты и идут в LLD), поправки на баг нет, рычаг 4 (каждый
  следующий выбор работы дешевле). Цена XL по определению цели.

  Порядок с DK-397 и бюджет 25/10 утверждены пользователем на этом же груминге.
- Вычитка: 1 файл, 5 правок, 0 пометок, 2026-08-17.
- 2026-08-17: LLD DK-400 (docs/lld/DK-400-slot-and-parking.md) закрыл развилки
  цели. Порядок реализации перечитан по решениям, два расхождения с пробной
  нарезкой. Первое: DK-406 идёт раньше DK-403, зависимость DK-403 от DK-406
  добавляется: повод «задача ждёт проверки» законно уходит из `wait-human`
  только при короткой очереди, а без двухступенчатого Check она занята часами.
  Второе: карте переходов из DK-384 нужна своя строка после DK-401, в нарезке
  её нет; заводит диспетчер.
- Вычитка: 1 файл, 5 правок, 3 пометки, 2026-08-17.

## Скрипт замера

Считает таблицу фаз и плату за возврат по git-истории проекта. Гоняется из
корня репозитория, ничего не пишет.

```python
#!/usr/bin/env python3
"""Фазы конвейера по git-истории: доли рубежей и цена повторного захода."""
import re
import statistics
import subprocess
from collections import defaultdict

GAP = 90 * 60  # разрыв между коммитами задачи, дальше которого это простой
BOARD = re.compile(r"^docs\(tasks\)|^docs\(board\)")

log = subprocess.run(["git", "log", "--pretty=format:%at|%s"],
                     capture_output=True, text=True, check=True).stdout.splitlines()
byid = defaultdict(list)
for line in reversed(log):
    ts, subj = line.split("|", 1)
    for tid in set(re.findall(r"DK-\d+", subj)):
        byid[tid].append((int(ts), subj))


def marks(commits, pattern, tid):
    rx = re.compile(tid + r"[.\d]* " + pattern)
    return [ts for ts, subj in commits if rx.search(subj)]


def active(commits, a, b):
    """Активные секунды между метками: простои длиннее GAP не считаются."""
    pts = [ts for ts, _ in commits if a <= ts <= b]
    if len(pts) < 2:
        return min(max(b - a, 0), GAP)
    return sum(pts[i] - pts[i - 1] for i in range(1, len(pts))
               if pts[i] - pts[i - 1] <= GAP)


KEYS = ("вход", "код и тесты", "ревью, слияние, выкат", "приёмка")
shares, phases, totals = defaultdict(list), defaultdict(list), []
first_lap, second_lap = [], []
for tid, commits in byid.items():
    starts = marks(commits, "в работу", tid)
    checks = marks(commits, "в Check", tid)
    closed = marks(commits, "закрыта", tid)
    if not (starts and checks and closed):
        continue
    t0, tcheck, tclose = starts[0], checks[0], closed[0]
    if len(checks) > 1:  # повторный заход: цена возврата к остывшей задаче
        first_lap.append(active(commits, t0, checks[0]))
        second_lap.append(active(commits, checks[0], checks[1]))
    if not t0 < tcheck <= tclose:
        continue
    code = sorted(ts for ts, s in commits if t0 <= ts <= tcheck and not BOARD.match(s))
    if not code:
        continue
    rev = marks(commits, "замечания ревью", tid)
    dev_end = max([ts for ts in code if ts <= rev[0]] or [code[0]]) if rev else code[-1]
    p = {"вход": active(commits, t0, code[0]),
         "код и тесты": active(commits, code[0], dev_end),
         "ревью, слияние, выкат": active(commits, dev_end, tcheck),
         "приёмка": active(commits, tcheck, tclose)}
    total = sum(p.values())
    if total < 60:
        continue
    totals.append(total)
    for k, v in p.items():
        phases[k].append(v)
        shares[k].append(v / total)

print("задач в выборке: %d, медиана активного времени %.0f мин\n"
      % (len(totals), statistics.median(totals) / 60))
acc = 0.0
for k in KEYS:
    acc += statistics.mean(shares[k])
    print("%-24s доля ср %5.1f%%  мед %5.1f%%  минут мед %5.1f  рубеж F %.2f"
          % (k, statistics.mean(shares[k]) * 100, statistics.median(shares[k]) * 100,
             statistics.median(phases[k]) / 60, acc))
print("\nповторных заходов: %d, второй заход к первому: %.2f"
      % (len(second_lap), statistics.median(second_lap) / statistics.median(first_lap)))
```

## Из черновика DK-384 (записан 2026-08-16)

### Черновик

Карта переходов работы и сторож её полноты

Путь работы от идеи до архива нигде не нарисован. Схема в README про доставку
правил в контекст, раздел «Состав» это каталог компонентов, docs/map.md по
замыслу карта уровня компонентов и индекс решений LLD. Связь между шагами у нас
держит состояние доски (статус строки, вердикт pick, очередь выката, поезд,
маркер «после ID»), а не текст, и потому цепочка нигде и не была написана.

Половина графа уже лежит в скиллах перекрёстными ссылками: board-task ведёт к
board-groom, board-ship и board-batch, goal-loop к goal-cut, board-batch и
live-core, board-team к трём соседним. Разрывы того же графа видно замером: у
board-groom нет ни одной исходящей ссылки на соседний скилл (оформили строку и
дальше тишина), board-batch зовёт только утилиты.

Чего хотим: документ со схемой, где узлы это состояния работы (идея, черновик,
строка, ветка, ревью, слияние, очередь выката, приёмка, архив), а рёбра
подписаны тем, кто переход делает, скиллом или командой. Разрывы графа чинятся
тем же заходом, отдельной строки на две фразы в скиллы заводить не надо.

Ценность не в витрине. На карте видно все входы в узел, и дыра вида DK-133
(ворота готовности стоят в board-groom, а строку заводят ещё три места мимо
них) находится чтением, а не живым случаем на цели DK-128, где так проехали
четыре строки из восьми.

Сторож полноты в devkitctl doctor по образцу того, как он стережёт docs/map.md:
каждый скилл и каждый статус доски на карте упомянут, ссылки живые. Без сторожа
карта протухнет на первом же новом скилле, а протухшей карте верят, и это хуже
её отсутствия. Сторожа можно отрезать отдельной строкой, если груминг решит,
что цена вышла великовата.

Карта пишется руками, а не генерится: смысловые переходы вида «после груминга
строка идёт под вердикт pick» из текстов автоматически не выводятся. Решать
иначе значит заводить дизайн со своим разбором, что генерится, что пишется и
как они уживаются.

Повод: сравнение с плагином claude superpowers, где цепочка от бреинсторма до
закрытия ветки написана прямо в скиллах, потому что связывать их больше нечем.
У нас связь сильнее (переживает сессию, ветвится, держит несколько рук), но её
не видно ни человеку, ни новой сессии.

## Из черновика DK-275 (записан 2026-08-12)

### Черновик

Пользовательский Check держит очередь выката часами, при том что задачи с агентским сценарием закрываются за минуты: DK-272 держала очередь около девяти часов (ночь), DK-232 около семи, DK-223 около пяти, и каждый раз стояли несвязанные задачи. Два процессных кандидата на грумминг, оба поверх снятия блокировки слияний (черновик DK-274). Первый: двухступенчатый Check, очередь держится до прогона агентской части сценария после выката, а не до закрытия пользователем; приёмка глазами остаётся в Check и очередь не держит, по образцу уже принятого «приёмка с замечаниями очередь не держит», где непроверенный глазами код тоже остаётся на проде. Меняет смысл инварианта «непроверенный выкат один»: непроверенным считается выкат без прогнанного smoke, а не незакрытая задача; риск в том, что поломку, видимую только глазами, найдут после следующих выкатов, revert по ID задачи при этом остаётся. Второй: планирование окнами, taskctl batch и диспетчер отделяют задачи с пользовательским сценарием, катят их последним выкатом перед известным окном проверок и копят пачкой, чтобы один заход пользователя закрывал несколько; чисто процессная правка без изменения кода очереди.
