# DK-396: Сводка считает остановки конвейера по разрядам парковки

## Что происходит

Разбор 2026-08-17 про автономность конвейера. Половина работы едет черновиком
DK-395 (конвейер не встаёт на вопросе), здесь вторая: чем меньше поводов
звать человека после старта, тем реже конвейеру приходится парковать задачи.
Материал разбора ниже, предмет разбит на четыре куска, и груминг решает, какие
из них едут строками, а какие сливаются.

## Чего хотим

## Границы

Строка везёт только счёт остановок. Эталон вида до заведения строки уехал в
DK-886. Кусок 2 закрыт задачей DK-552, кусок 4 закрыт задачей DK-801.

## Развилки

- «кусок-3»: Кусок 3 (эталон вида утверждается до заведения строки) нарезка цели DK-565 намеренно оставила в накопителе. Заводить его строкой сейчас или ждать повода?
  - решает: человек
  - рекомендация: завести строкой сейчас: шаг дешёвый, механику проверки макета ACCEPTANCE.md уже держит, не хватает только шага в board-groom и goal-cut
  - вариант: строкой сейчас
  - вариант: оставить в накопителе до первой задачи про вид экрана
  - вариант: удалить, механики ACCEPTANCE.md хватает
  - решено человеком 2026-09-09: завести строкой сейчас: шаг дешёвый, механику проверки макета ACCEPTANCE.md уже держит, не хватает только шага в board-groom и goal-cut
- «счёт-остановок»: Остаток куска 1: возвраты задач считает review stats, полосу считает pilot, а сколько раз конвейер вставал по разрядам «вопрос:», «окружение:», «автор:» и «спор:», не считает никто. Заводить сводку строкой?
  - решает: человек
  - рекомендация: завести строкой: разряды парковки уже машинные, работа сводится к сводной команде поверх них
  - вариант: строкой, сводка по четырём разрядам парковки
  - вариант: считать покрытым review stats и pilot, кусок вычеркнуть
  - вариант: отложить до накопления фактов
  - решено человеком 2026-09-09: завести строкой: разряды парковки уже машинные, работа сводится к сводной команде поверх них
- «серьёзность»: Серьёзность по RANKING.md для того, что останется от черновика. Разница ставит ранг около 40 против около 65, то есть «полежит в Backlog» против «пойдёт из головы очереди».
  - решает: человек
  - рекомендация: 25 Major: трение заметное, но обход у вас есть и он ручной, вы приходите и отвечаете
  - вариант: 25, Major
  - вариант: 50, Critical
  - вариант: 0, Minor
  - решено человеком 2026-09-09: 50, Critical

## DoD

- Команда печатает за окно счёт остановок по четырём разрядам парковки:
  «вопрос:», «окружение:», «автор:», «спор:».
- У каждого разряда стоит число, доля и медиана простоя.
- Дока называет источник счёта. Это история переводов доски в Blocked и записи
  runs.
- Сценарий проверки гоняет команду на подставной истории. Числа сверяются с
  ожидаемыми.

## Ранг

`50+5+1+0+4 = 60`, P1.

- Серьёзность 50. Ответ человека 2026-09-09, развилка «серьёзность». Конвейер
  на остановке встаёт, обход ручной и болезненный. Замер черновика: 17 задач
  простояли в Check дольше часа, максимум 12.2 часа.
- Ценность 5. Цифра остановок не чинит, она называет частый класс. Без неё
  правка ворот идёт вслепую.
- Неопределённость 1. Разряды парковки машинные с DK-400 и DK-756. Остаётся
  сводная команда поверх них.
- Поправка на баг 0. Работа новая, сломанного поведения нет.
- Рычаг 4. С этой цифры начинается остальная работа про поводы звать человека.

Цена M. Сводка читает историю доски тем же путём, что `taskctl pilot`.

## Классы вмешательства после старта, по фактам

- Согласие на дизайн приходит после написанного документа. DK-133: LLD написан,
  вычитан, прошёл ревью xhigh. Только потом приёмка дала возражение на решения
  2 и 4, документ переписан, нарезка переделана. Развилка («файл и DoD
  заводятся на `add` или на взятии в работу») была известна на груминге.
  Правило груминга относит такую развилку не к вопросу, а к неопределённости
  (`board-groom`, шаг «Собрать вопросы»). Для развилок реализации это верно,
  для процессных, меняющих правила всем входам на доску, оборачивается
  потерянным заходом.
- Барьер «глаза» снимается макетом, но макетов до старта никто не заводит.
  `ACCEPTANCE.md` сам называет обход: «утверждённый заранее макет, против
  которого проверка идёт структурно». В цикле DK-327 макеты рисовались по
  ходу и дважды ставили цикл в `wait-human`. Следом три задачи вернулись с
  приёмки форвард-фиксами: DK-325 курсор в поле поиска, DK-321 колонка на
  телефоне, DK-324 жест против pointercancel. Всего форвард-фиксы поминают 17
  файлов архива, а два перевода в Check по git-истории насчитались у 9 задач.
- Внешние условия выясняются посреди цикла. DK-138 та же классика. Решение
  DK-149 строилось на «ассеты видны без токена», а репозиторий был приватным.
  Это закрывает п. 2 DoD цели DK-161, но только для постановки цели и LLD. Обычный
  черновик проходит трое ворот, где про доступы, секреты и окружение не
  спрашивается ничего.
- Состав работы неизвестен на старте: дорезка целей штатна, DK-327 поминает её
  восемь раз, DK-112 шесть.
- Стоп по бюджету с добавкой (DK-327 встала на витке 13) и решения о
  направлении по итогам замера (DK-297 кончилась вопросом про генерализацию
  карты и висящей веткой poc/map в чужом репозитории).
- Пользовательская приёмка редка, но стоит дорого. `taskctl kinds` считает 81
  агентскую строку против 3 mixed и 3 user. 17 задач простояли в Check дольше
  часа, максимум 12.2 часа (замер по git-истории, он же в DK-395).

## Четыре куска предмета

1. Счёта вмешательств нет. Ни одна команда не отвечает, сколько раз и по какому
   поводу цикл звал человека. `wait-human` живёт прозой в журнале, класс
   причины нигде не фиксируется, форвард-фикс от дефекта кода не отличить. Пока
   этого нет, «повышать качество груминга» это стрельба вслепую. Неизвестно,
   какой класс вопроса всплывает после старта чаще прочих. Работа
   дешёвая (класс остановки в файле задачи плюс сводка командой) и по порядку
   идёт первой.
2. У ворот готовности нет ворот про условия. Трое сегодняшних (актуальность,
   предмет, конец работы) отвечают на вопрос, дозрел ли предмет. Ни одни не
   спрашивают, что понадобится человеку, пока задача делается: доступы и
   секреты, эталон для глаз, согласие на процессную развилку, невозвратный шаг.
   Место готово, ворота уже стоят рубежом заведения строки (LLD DK-133).
3. Эталон вида не заводится до старта. Задача про вид экрана, у которой макет
   утверждён заранее, теряет барьер «глаза» и закрывается агентом. Сегодня шага
   «утвердить эталон до строки» нет ни в груминге, ни в нарезке цели, и человек
   приходит смотреть на готовое.
4. Обратной связи от исхода к воротам нет. Задача вернулась форвард-фиксом или
   возражением, а груминг об этом не узнаёт, и следующий черновик того же
   класса проходит теми же воротами. Единственная ниточка это решение DK-292.6,
   и оно только про ошибку назначения вида приёмки.

## Смежное

Цель DK-161 закрывает внешние условия для постановки цели и LLD, здесь речь про
обычный черновик, и пересечение разбирается на груминге. DK-381 (скилл
проектирования LLD) держит самопроверку готового дизайна, а кусок 2 стоит
раньше, перед строкой доски. DK-201 (поиск похожих задач при заведении) кладётся
рядом с воротами. Счёт вмешательств смыкается с DK-209 (фактическая стоимость
закрытых задач), но меряет другое: не цену задачи, а поводы звать человека.

## Что снял разбор

Прогон ворот 2026-08-25, чтобы исполнитель не проверял то же второй раз.

- Кусок 1 стоит не на пустом месте. Машинный разряд причины парковки уже есть.
  `parkPrefixes` в `tools/taskctl/ops.go` держит «вопрос:» и «окружение:»
  (решение DK-400.2), по ним сторожок `devkitctl watch` будит припаркованную
  строку. Работа сводится к перечню классов поверх этих двух и к сводной
  команде.
- Кусок 4 для одного класса уже машинный. `taskctl kinds` печатает сводку по
  видам приёмки со счётом, ошибками назначения и пересмотрами. Непокрытыми
  остаются форвард-фикс и возражение на дизайн.
- Кусок 3 упирается только в шаг груминга. Механику проверки утверждённого
  макета `ACCEPTANCE.md` уже держит разделом «Особый случай: барьер «глаза» и
  утверждённый макет», вплоть до формы ссылки и проверки возраста макета по
  истории репозитория. Слова «макет» нет ни в `board-groom`, ни в `goal-cut`,
  оно встречается только в `goal-loop`.
- Кусок 2 подтвердился. Ворот в скилле `board-groom` ровно трое (актуальность,
  предмет, конец работы), и про доступы, секреты и окружение не спрашивают ни
  одни.
- Дубля нет. DK-395 и DK-400 закрыты, черновики DK-201 (поиск похожих задач) и
  DK-209 (фактическая стоимость закрытых задач) лежат рядом по месту, но меряют
  другое.

Слагаемые ранга, которые держатся на фактах черновика: ценность 5-7
(автономность конвейера), неопределённость 2-3, рычаг 4. Серьёзность и цена
ждут ответа на вопросы ниже.

Прогон 2026-09-06, повторный: за двенадцать дней два куска сдвинулись.

- Кусок 2 закрыт. Ворот готовности в `board-groom` теперь четверо, и четвёртые
  это ровно «Условия». Это то, что понадобится от человека, пока задача
  делается, и то, чего агент не добудет сам, от доступа и секрета до согласия
  на развилку, меняющую правила другим строкам. Каждое условие пишется развилкой
  `taskctl decide` с пометкой «решает: человек», и открытая держит старт.
  Ворота поставила DK-552 (закрыта 2026-09-06, решение 5), и они одни на все
  входы постановки.
- Кусок 4 усох до одного класса. `taskctl review stats` печатает возвраты с
  классом (постановка, правила, реализация) и долю возвратов по постановке, на
  сегодня 3 из 52, 5%. Класс ставится и замечанию ревью (`review add --class`),
  и провалу проверки (`fail --class`). Возражение на дизайн этим
  разрядом уже ловится. Непокрытым остаётся форвард-фикс с приёмки.
- Кусок 1 стоит там же, но опора под ним шире. `parkPrefixes` в
  `tools/taskctl/ops.go` держит четыре разряда вместо двух: «вопрос:»,
  «окружение:», «автор:» и «спор:». Два последних добавило ревью чужого MR
  (LLD DK-756). Сводной команды по остановкам так и нет.
- Кусок 3 не тронут. Слова «макет» нет ни в `board-groom`, ни в `goal-cut`, оно
  встречается только в `goal-loop` и в корпусе прозы.
- Дубля нет и сегодня. Рядом встала DK-657 (чем конвейер ловит fail-open, когда
  неверна сама постановка). Она про три прохода внутри цикла, а здесь про
  сигнал от промаха обратно в ворота груминга.

Сверка с целью DK-565, 2026-09-07. Раскладка кусков на строки решена не
груммингом, а нарезкой цели 2026-08-29, материализованной 2026-09-05. Кусок 2
уехал кандидатами 3 и 5 (строка DK-552, закрыта), кусок 4 кандидатом 1 (строка
DK-801, закрыта). Про кусок 3 в разделе «Задачи цели» сказано прямо: в цель
он не идёт и остаётся в накопителе. Кусок 1 в цель не брали. Сводка по
возвратам живёт в `taskctl review stats`, счётчики полосы в `taskctl pilot`, а
счёта остановок по разрядам парковки нет ни там, ни там.

## Ход работы

- Из черновика DK-396 «Человек до старта задачи: счёт вмешательств по классам, ворота внешних условий в груминге, эталон вида до заведения строки и обратная связь от промаха обратно к воротам»: записан 2026-08-17, оформлен 2026-09-09.
- 2026-09-07, отложен: живым остался кусок 3 (эталон вида до заведения строки), нарезка цели DK-565 оставила его в накопителе намеренно, и остаток куска 1 (счёт остановок по четырём разрядам парковки, возвраты уже считает review stats). Ждём ответа человека с разбора 2026-08-25 про серьёзность, повторно вопрос не задаём
- Вычитка: 21 правка, 0 пометок, 2026-09-09.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 4%, снимок 1м назад, сдвига нет), 2026-09-18 15:31-15:31.
- Вычитка: 3 файла, 6 правок, 1 пометка, 2026-09-18.
- Сторож прозы: tools/taskctl/README.md, хвост «..., а не Y» 8,1 при пороге 5. Оставлено: фраза про LLD DK-933 называет настоящую альтернативу (задачу могли принять за повод звать человека), пункт 4 типологии такой хвост не снимает.
- Исключение: обкатка (сценарий обкатан зелёным на main 2c5a691d после слияния, вывод в «Проверке»; отметку гасит коммит отметок соседей по поезду)

## Ревью

Уровень 2 до aac93b6: неопределённость 1 из pick --role review; триаж: правка вводит новую команду счёта, от точности которой напрямую зависит DoD (числа доли и медианы), риск ошибки арифметики поднимает на ступень; доступы/данные/деньги не тронуты, горячего пути нагрузки нет

- Вердикт: без замечаний. DoD сверен построчно: четыре разряда, число, доля и медиана у каждого, источник счёта («Ход работы» плюс живые runs), сценарий обкатан.

  Арифметику доли и медианы проверила вручную на тестовых числах, значения совпали. Границу `--since`, живую остановку (задача в Blocked прямо сейчас) и пустую историю (явное сообщение вместо нулей) покрывают тесты. Исключение «слияние:» и «закрытие:» сверила с `parkPrefixes` в `ops.go`. `go test ./...` пакета `taskctl` зелёный целиком, а искусственная поломка в `stopsClassOf` (сняла `Cut` по двоеточию) валит `TestStopsCounts`, значит тест ловит регрессию.

  По постановке (раздел «Границы») `ACCEPTANCE.md` и `RANKING.md` не входят в эту строку, они относятся к другим кускам предмета (кусок 3 и серьёзность ранга). README даёт рабочий пример первого запуска. Символы в диффе чистые.

## Сценарий проверки

Агентский, целиком послевыкатный. `taskctl` собирается из дерева и печатает
счёт остановок на подставной истории, обкатка гоняет его на HEAD с временным
HOME.

1. Пять задач стенда несут закрытые остановки в разделе «Ход работы»: по две
   строки «вопрос:», по одной «окружение:», «автор:» и «спор:». `taskctl
   stops --since 2026-09-01` печатает число, долю и медиану простоя по каждому
   разряду и итог.

```sh
mkdir -p "$HOME/stops/docs/tasks" "$HOME/bin" && printf '%s\n' '# доска' '' '## In progress' '' 'Нет.' '' '## Check' '' 'Нет.' '' '## Backlog' '' 'Нет.' '' '## Blocked' '' 'Нет.' > "$HOME/stops/docs/TASKS.md" && test -f "$HOME/stops/docs/TASKS.md"
printf '%s\n' '# DK-901: вопрос, первая строка' '' '## Ход работы' '' '- Снаружи: блок: вопрос: нужен эталон вида, 2026-09-02 09:00-10:00.' > "$HOME/stops/docs/tasks/DK-901.md" && test -f "$HOME/stops/docs/tasks/DK-901.md"
printf '%s\n' '# DK-902: вопрос, вторая строка' '' '## Ход работы' '' '- Снаружи: блок: вопрос: ждём ответа человека, 2026-09-03 09:00-12:00.' > "$HOME/stops/docs/tasks/DK-902.md" && test -f "$HOME/stops/docs/tasks/DK-902.md"
printf '%s\n' '# DK-903: окружение' '' '## Ход работы' '' '- Снаружи: блок: окружение: нет доступа к стенду, 2026-09-04 09:00-14:00.' > "$HOME/stops/docs/tasks/DK-903.md" && test -f "$HOME/stops/docs/tasks/DK-903.md"
printf '%s\n' '# DK-904: автор' '' '## Ход работы' '' '- Снаружи: блок: автор: правка чужого MR, 2026-09-05 09:00-11:00.' > "$HOME/stops/docs/tasks/DK-904.md" && test -f "$HOME/stops/docs/tasks/DK-904.md"
printf '%s\n' '# DK-905: спор' '' '## Ход работы' '' '- Снаружи: блок: спор: автор отказал в правке, 2026-09-06 09:00-11:00.' > "$HOME/stops/docs/tasks/DK-905.md" && test -f "$HOME/stops/docs/tasks/DK-905.md"
cd tools/taskctl && go build -o "$HOME/bin/taskctl" . && test -x "$HOME/bin/taskctl"
out="$("$HOME/bin/taskctl" -C "$HOME/stops" stops --since 2026-09-01 --runs "$HOME/stops-runs" 2>&1)" && printf '%s\n' "$out" && printf '%s\n' "$out" | grep -Fx 'остановки конвейера по разрядам парковки, с 2026-09-01' && printf '%s\n' "$out" | grep -Fx 'вопрос: 2 (40.0%), медиана простоя 2.0 ч' && printf '%s\n' "$out" | grep -Fx 'окружение: 1 (20.0%), медиана простоя 5.0 ч' && printf '%s\n' "$out" | grep -Fx 'автор: 1 (20.0%), медиана простоя 2.0 ч' && printf '%s\n' "$out" | grep -Fx 'спор: 1 (20.0%), медиана простоя 2.0 ч' && printf '%s\n' "$out" | grep -Fx 'итого остановок: 5'
```

## Выкат

- 2026-09-18 слито: 93ef4f57, 696c3eb6, 0471b46c, d146f15e, 0d30b0e4

## Проверка


- Обкатка: 2026-09-18 17:50, свежее дерево 2c5a691d4854, сценарий f9515503, временный HOME, утилит дерева 11, шагов 8, все зелёные.

Шаг 1 (зелёный):

```console
$ mkdir -p "$HOME/stops/docs/tasks" "$HOME/bin" && printf '%s\n' '# доска' '' '## In progress' '' 'Нет.' '' '## Check' '' 'Нет.' '' '## Backlog' '' 'Нет.' '' '## Blocked' '' 'Нет.' > "$HOME/stops/docs/TASKS.md" && test -f "$HOME/stops/docs/TASKS.md"

```

Шаг 2 (зелёный):

```console
$ printf '%s\n' '# DK-901: вопрос, первая строка' '' '## Ход работы' '' '- Снаружи: блок: вопрос: нужен эталон вида, 2026-09-02 09:00-10:00.' > "$HOME/stops/docs/tasks/DK-901.md" && test -f "$HOME/stops/docs/tasks/DK-901.md"

```

Шаг 3 (зелёный):

```console
$ printf '%s\n' '# DK-902: вопрос, вторая строка' '' '## Ход работы' '' '- Снаружи: блок: вопрос: ждём ответа человека, 2026-09-03 09:00-12:00.' > "$HOME/stops/docs/tasks/DK-902.md" && test -f "$HOME/stops/docs/tasks/DK-902.md"

```

Шаг 4 (зелёный):

```console
$ printf '%s\n' '# DK-903: окружение' '' '## Ход работы' '' '- Снаружи: блок: окружение: нет доступа к стенду, 2026-09-04 09:00-14:00.' > "$HOME/stops/docs/tasks/DK-903.md" && test -f "$HOME/stops/docs/tasks/DK-903.md"

```

Шаг 5 (зелёный):

```console
$ printf '%s\n' '# DK-904: автор' '' '## Ход работы' '' '- Снаружи: блок: автор: правка чужого MR, 2026-09-05 09:00-11:00.' > "$HOME/stops/docs/tasks/DK-904.md" && test -f "$HOME/stops/docs/tasks/DK-904.md"

```

Шаг 6 (зелёный):

```console
$ printf '%s\n' '# DK-905: спор' '' '## Ход работы' '' '- Снаружи: блок: спор: автор отказал в правке, 2026-09-06 09:00-11:00.' > "$HOME/stops/docs/tasks/DK-905.md" && test -f "$HOME/stops/docs/tasks/DK-905.md"

```

Шаг 7 (зелёный):

```console
$ cd tools/taskctl && go build -o "$HOME/bin/taskctl" . && test -x "$HOME/bin/taskctl"

```

Шаг 8 (зелёный):

```console
$ out="$("$HOME/bin/taskctl" -C "$HOME/stops" stops --since 2026-09-01 --runs "$HOME/stops-runs" 2>&1)" && printf '%s\n' "$out" && printf '%s\n' "$out" | grep -Fx 'остановки конвейера по разрядам парковки, с 2026-09-01' && printf '%s\n' "$out" | grep -Fx 'вопрос: 2 (40.0%), медиана простоя 2.0 ч' && printf '%s\n' "$out" | grep -Fx 'окружение: 1 (20.0%), медиана простоя 5.0 ч' && printf '%s\n' "$out" | grep -Fx 'автор: 1 (20.0%), медиана простоя 2.0 ч' && printf '%s\n' "$out" | grep -Fx 'спор: 1 (20.0%), медиана простоя 2.0 ч' && printf '%s\n' "$out" | grep -Fx 'итого остановок: 5'
остановки конвейера по разрядам парковки, с 2026-09-01
вопрос: 2 (40.0%), медиана простоя 2.0 ч
окружение: 1 (20.0%), медиана простоя 5.0 ч
автор: 1 (20.0%), медиана простоя 2.0 ч
спор: 1 (20.0%), медиана простоя 2.0 ч
итого остановок: 5
остановки конвейера по разрядам парковки, с 2026-09-01
вопрос: 2 (40.0%), медиана простоя 2.0 ч
окружение: 1 (20.0%), медиана простоя 5.0 ч
автор: 1 (20.0%), медиана простоя 2.0 ч
спор: 1 (20.0%), медиана простоя 2.0 ч
итого остановок: 5
```
