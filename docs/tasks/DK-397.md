# DK-397: Цель: дашборд ведёт разговор вокруг задачи

записан 2026-08-17

## Цель

Разбор 2026-08-17 по итогам недели работы с дашбордом. Главная его функция это
разговор с агентом, который ведёт конкретную задачу, и без неё замысел теряется.
В vscode список чатов удобен ровно до сотни, дальше в нём теряешься. Дашборд
должен систематизировать чаты вокруг задачи, не связывая руки: произвольный
разговор в контексте всей доски остаётся. Велосипед не изобретаем, прототип чата
это панель claude в vscode, своя работа только в связке с доской и задачами,
которой у vscode нет.

Развилки диалога закрыты пользователем в том же разборе: работа оформляется
целью XL со своим LLD; доска остаётся домом, а чат открывается справа панелью
как в vscode; чатом задачи считается любая живая сессия и писать можно во все;
вопрос агента ловится протоколом с эвристикой в запасе.

Пять точечных болей:

1. «Живой статус» и «Чат с агентом» по наполнению одно и то же, разница только
   в том, что в первом нельзя написать. Пользователю это непонятно, все хотят
   чат, где агенту отвечают.
2. Панель tmux бесполезна и всегда пуста.
3. Кнопка «раньше» костыльна: лента должна листаться так, чтобы подгрузки не
   было заметно, как в чате vscode.
4. С экрана задачи чат не открывается, хотя чатов у задачи бывает несколько и
   привязаны они к ней.
5. Задача встаёт на первом же вопросе агента, и человек этого не видит вовсе.
   Задача простаивает, а чинится это тем, что другому агенту заказывают найти
   сессию и доделать.

## Почему сейчас так

Двусторонний канал построен под цель, а не под задачу. Письма лежат в
`.devkit/goal-<ID>.mail`, почтальон `hooks/inbox.py` вносит их в контекст на
ходе инструмента, а `goal-run.py --ask` умеет ждать ответа (LLD DK-136). У
задачи ящика нет, поэтому экран `/chat/<ID>` выдаётся только работам вида goal
(`app.js`, `renderChat`), а задаче остаётся чтение транскрипта. Это не два
взгляда на одно, это два разных транспорта, и первая боль растёт отсюда.

Панель tmux берёт снимок пейна у сессий `goal-<ID>` и `task-<ID>`, которые
поднимает сам дашборд (`runs.go`). Работа, начатая из vscode, не имеет своей
tmux-сессии, и панель остаётся пустой.

Привязка чата к задаче угадывается: `sessionTask` (`sessions.go`) пробует каталог
дерева, ID в первой реплике и имя ветки. Отсюда чат с вопросом про задачу
попадает на дашборд её живой работой (черновик DK-360), а у нераспознанной
сессии карточка пустая, и зайти в неё нельзя (черновик DK-291).

## Что уже есть и переиспользуется

- Носитель писем: ящик, отметки доставки, признак ожидания и замок разобраны в
  LLD DK-136 и живут для цели.
- Ящик черновика с инструментом `taskctl draft ask` запроектирован в LLD DK-354
  и стоит строками DK-373 и DK-374.
- Сигнал «сессия ждёт ввода» уже течёт: харнес шлёт событие `Notification` с
  поводом `idle_prompt`, уведомитель пишет его в `~/.devkit/notify.log`
  (`hooks/notify.py`), дашборд читает этот журнал лентой. Эвристику не надо
  выдумывать из транскрипта.
- Лента чата со стримом, очередью исходящих и состояниями реплики написана для
  цели, а её вынос в переиспользуемые куски стоит строкой DK-371.
- Записи этапов `~/.devkit/runs/<ID>-<slug>.run` содержат вид деятельности
  «уточнение» с текстом вопроса.

## Чего нет

1. Явной привязки чата к задаче. Нужен реестр «сессия ведёт задачу», куда пишет
   дашборд при подъёме чата и хук старта сессии в дереве задачи; угадывание
   остаётся запасным, а источник виден в подписи.
2. Адреса у ящика. Чатов у задачи несколько, значит письмо адресуется сессии, а
   не работе. Носитель берётся общим, а не третьей копией рядом с целью и
   черновиком.
3. Общего инструмента ожидания. `goal-run --ask` ждёт за цель, `draft ask`
   запроектирован за черновик, а исполнителю задачи спросить нечем, и он
   спрашивает репликой в конце хода, после чего заход кончается.
4. Состояния «стоит и ждёт человека» на доске. Точный признак это файл ожидания,
   запасной это повод `idle_prompt` из журнала уведомителя; ни один сегодня не
   доходит ни до карточки задачи, ни до уведомления.
5. Панели чата и списка чатов задачи, а с ними и второго чата по одной задаче:
   `runs.go` отказывает поверх живой сессии с тем же именем.
6. Непрерывной ленты: пагинация назад идёт кнопкой «раньше» через `?before=`.

## Что решать дизайном

Первой задачей цели идёт LLD, вид приёмки у него user по барьеру «согласие».
Развилки на входе такие:

- Где живёт реестр чатов, кто в него пишет и что делать со старыми сессиями,
  которых там нет.
- Адресация писем: ящик на сессию или ящик на задачу с полем сессии, и как это
  ложится на уже написанный ящик цели и запроектированный ящик черновика.
- Форма общего инструмента ожидания и правка скиллов `exec-*` и `board-*`:
  вопрос человеку задаётся инструментом, а не последней репликой захода.
- Устройство панели: ширина и её память, поведение при переходе между задачами,
  вид на телефоне. Кандидат по телефону: панель не ужимается в колонку, а
  открывается отдельным маршрутом на весь экран с возвратом на прежнее место
  доски и сохранением позиции ленты. Причина в том, что чат на 375 точках рядом
  с доской нечитаем, а маршрут даёт настоящую кнопку «назад» и ссылку.
- Что остаётся от пары «Живой статус» и «Чат с агентом»: кандидат в том, что это
  один элемент, у которого поле ввода гаснет с названной причиной, когда писать
  некуда.
- Общий чат доски без задачи: кандидат в том, что это та же панель с пустой
  привязкой, а не свой экран.

## DoD

1. С экрана задачи открывается её чат, список чатов задачи виден, и новый чат
   заводится с дашборда; несколько живых чатов на одну задачу не отбиваются.
2. Реплика с дашборда доезжает до любой живой сессии, а не только до витка цели;
   носитель писем один на цель, задачу и черновик, третьей копии механики
   отметок и ожидания нет, и это видно чтением кода.
3. Исполнитель задачи спрашивает человека инструментом ожидания и ждёт ответа в
   том же заходе, а не заканчивает заход репликой. Скиллы `exec-*` и `board-*`
   переписаны под это.
4. Задача, ждущая человека, видна на доске состоянием, в ленте и уведомлением;
   когда признака нет, состояние берётся запасным поводом `idle_prompt`, и
   источник назван в подписи.
5. Лента листается вверх без кнопки «раньше», позиция сохраняется, панель tmux с
   экрана задачи убрана.
6. Прогон `test` из `.devkit/deploy.local` зелёный, поведение чата цели не
   изменилось (проверяется смоком выноса DK-371).

## Приёмка

- вид: mixed
- барьер «глаза»: читаемость панели чата рядом с доской и её вид на телефоне

Обход утверждённым макетом годится частично: LLD цели задаёт структуру панели, и
по ней проверка идёт разметкой, но плотность экрана и то, читается ли чат рядом
с доской, это согласие человека, а не факт. Замер headless-браузером отвечает на
вопрос про размеры, а не про читаемость. Тест по разметке и стилям закрывает
структуру и уже входит в агентскую часть. Она самодостаточна: реестр чатов,
доставка письма в живую сессию, инструмент ожидания, состояние «ждёт человека» в
ручках и непрерывность подгрузки ленты проверяются смоком и юнитами.

## Бюджет

бюджет: week_all <= 30
бюджет: week_max <= 12

Потолка яруса нет, вердикт pick работает как обычно.

Бакеты общие на машину, параллельная сессия тратит тот же week_all, так что
числа это верхняя граница трат машины за время цикла, а не счётчик одной цели.
На постановке снимок показывал week_all 97% и week_max 100% при сбросе в тот же
день в 14:59, то есть цикл стартует с почти пустого бакета и первым же гейтом
бюджета упрётся в ожидание сброса.

## Задачи цели

Заводит нарезка первым витком. Кандидаты с груминга, транспорт первым, экран
поверх него:

1. LLD цели: реестр чатов, адресация писем, форма инструмента ожидания,
   устройство панели.
2. Реестр чатов задачи и ручка их списка, угадывание запасным с подписью
   источника.
3. Почта любой живой сессии: общий носитель писем, адресация сессией, доставка
   почтальоном в идущий заход.
4. Общий инструмент ожидания и правка скиллов `exec-*` и `board-*`.
5. Состояние «ждёт человека» в API, на карточке задачи, в ленте и в уведомлении.
6. Вынос ленты чата в переиспользуемые куски (стоящая строка DK-371).
7. Панель чата справа на доске и на экране задачи, слияние «Живого статуса» с
   «Чатом», маршрут телефона.
8. Вкладка чатов задачи: список с временем, подпиской и состоянием, кнопка
   нового чата.
9. Непрерывная лента с подгрузкой вверх и сохранением позиции, кнопка «раньше»
   уходит.
10. Новый чат по задаче с дашборда: несколько живых чатов на задачу, именование
    сессий, снятие отказа «работа уже идёт» для чатов.
11. Уборка панели tmux с экрана задачи: имя сессии и стоп остаются чипом в шапке
    чата.

Непрерывная лента идёт отдельной строкой поверх выноса, а не внутри него: у
выноса ценность ровно в том, что поведение цели не меняется ни на символ и это
проверяемо смоком, а смена механики листания это уже своё поведение.

## Смежное на доске

- DK-345 (почта любой живой сессии) это и есть пункт 3, строка остаётся на
  доске и входит в цель. Ранг ей поднят с 11 до 40 по месту в цели.
- DK-371 входит в цель пунктом 6, она предпосылка всей экранной части.
- DK-372, DK-373 и DK-374 остаются за черновиком, но после пунктов 3 и 4
  переезжают на общий носитель и дешевеют. LLD DK-354 планировал обратный
  порядок (черновик первым, DK-345 наследует за ним), и смена порядка называется
  вслух в новом документе.
- Черновики DK-291 и DK-360 закрываются пунктом 2, их текст приписан ниже.
- DK-359 (лента не показывает размышления) едет попутно с пунктом 6.
- DK-355 и DK-356 (кружок статуса в строке доски, карта хода работы на экране
  задачи) плану не противоречат, но их макеты сверяются с новым лейаутом.
- DK-339 и DK-353 живут на тех же экранах и подходят для общей пачки.

## Граница с DK-395

DK-395 держит ту же боль со стороны конвейера: вопрос человеку не должен гасить
петлю, задача паркуется, планировщик выбирает работу в освободившемся слоте.
Здесь пользовательская сторона того же: вопрос виден на доске, ответ уходит в
один тап из любого места, разговор привязан к задаче. Общая несущая деталь одна,
это адрес вопроса у задачи и ящик под него, и кладёт её эта цель пунктами 3 и 4,
а DK-395 наследует. Дублировать носитель нельзя ни в коем случае: третья копия
механики отметок и ожидания разъедется с первыми двумя молча.

Зависимость стоит на уровне задач, а не целей: LLD DK-395 пишется параллельно, а
его строки реализации вешаются на пункты 3 и 4 этой цели маркером «после».

## Журнал

Пусто, цикл ещё не запускался.

## Итог

Пишет последний виток.

## Ход работы

- 2026-08-17: груминг черновиков DK-395 и DK-397 вместе со строкой DK-168 одним
  заходом. Три ворот пройдены.

  Актуальность. Боли живые и проверены по коду: экран `/chat/<ID>` заведён
  только работам вида goal (`tools/dashboard/app.js`, `renderChat`), ящик лежит
  по пути `.devkit/goal-<ID>.mail` и не имеет адреса задачи, `sessionTask`
  (`tools/dashboard/sessions.go`) угадывает задачу тремя способами, панель tmux
  берёт снимок только у сессий, поднятых самим дашбордом (`runs.go`). Повод
  `idle_prompt` в `hooks/notify.py` на месте и до карточки задачи не доходит.

  Дубля нет: по Backlog смежная DK-345 поглощается и снимается, DK-371 входит в
  цель, DK-372-374 держат черновик и остаются, DK-355, DK-356, DK-339 и DK-353
  живут на тех же экранах другим предметом. По накопителю DK-291 и DK-360
  закрываются пунктом 2 и приписаны сюда, оба были отложены до этого разбора.

  Ранг 50+9+3+0+4 = 66: серьёзность 50 (основной сценарий дашборда, разговор с
  агентом задачи, не работает, обход через список чатов vscode болезненный и
  теряется после сотни), ценность 9 (ради этого дашборд и затевался),
  неопределённость 3 (четыре развилки закрыты пользователем, шесть остаются и
  идут в LLD), поправки на баг нет, рычаг 4 (общий носитель писем удешевляет
  DK-395, DK-372-374 и закрывает DK-345). Цена XL по определению цели.

  Порядок с DK-395 и бюджет 30/12 утверждены пользователем на этом же груминге.
- Вычитка: 1 файл, 3 правки, 0 пометок, 2026-08-17.

## Из черновика DK-291 (записан 2026-08-13)

### Черновик

Карточка нераспознанной сессии на дашборде бесполезна: подпись «интерактивная сессия, задача не распознана» не говорит ни чьё это окно, ни чем оно занято, а зайти внутрь нельзя, потому что экран агента открывается по ID задачи, которого у такой работы нет. Место на экране она занимает (на телефоне заметно), пользы не даёт. Показ таких сессий завела DK-263 сознательно («пропавшая работа неотличима от несделанной»), и решение это верное, вопрос в том, что карточка молчит. На груминге сначала посмотреть живьём, что это за сессии вообще: узнать задачу пытаются тремя способами (каталог бокового дерева, упоминание ID в первой реплике, имя ветки, sessionTask в tools/dashboard/sessions.go), и мимо всех трёх идут как минимум диспетчерские сессии, которые ведут несколько задач сразу и ни к одной не привязаны, живой чат про devkit без ID в первой реплике и окна, начатые с произвольного разговора. По итогам решить, что с ними делать: показывать признак (первая реплика, каталог, время начала), открывать транскрипт по ID сессии, а не по ID задачи, сворачивать несколько нераспознанных в одну строку либо не считать работой ту, что задачу не назвала. Замечено пользователем 2026-08-13, повторно.

### Грумминг

- 2026-08-14, отложен: нарезка цели DK-327: сначала DK-294 открывает разговор по id сессии, и только на живых нераспознанных сессиях видно, что показывать в карточке; часть запроса (список сессий задачи с переключателем) закрывает DoD стоящей строки DK-290

## Из черновика DK-360 (записан 2026-08-15)

### Черновик

Дашборд приписывает сессию-вопрос задаче тем же способом, что сессию-заказ: sessionTask (tools/dashboard/sessions.go) узнаёт задачу по ID в тексте первой реплики, исходя из того, что первая реплика это заказ работы («Выполни цель DK-112»). Вопрос про задачу в первой реплике («а что происходит с задачей DK-194?») ловится той же регуляркой taskIDTextRe, сессия привязывается к задаче «по первой реплике», а живость считается по свежести транскрипта (sessionLiveTTL), и разговорный чат загорается на карточке задачи как её живая работа, конкурируя с настоящим headless-исполнителем. Замечено пользователем 2026-08-15: чат разбора запущенной с дашборда DK-194 отображался на дашборде как активная работа по задаче. Кандидат: различать вопрос и заказ в первой реплике, опираться не только на первую реплику, либо подписывать признак источника рядом со способом узнавания. Смежное: DK-291 про бесполезную карточку нераспознанной сессии смотрит на ту же регулярку с другой стороны.

### Грумминг

- 2026-08-15, отложен: предмет один с DK-291 (какая сессия считается работой задачи), возвращаться к обоим вместе, когда DK-291 снимется с нарезки DK-327
