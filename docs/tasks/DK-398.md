# DK-398: goal-start сверяет бюджет цели с остатком бакета до сброса

## Задача

Постановка цели задаёт бюджет в процентных пунктах бакетов квоты и снимает
снимок `agentctl`, но с остатком до сброса его не сверяет. Цель с бюджетом
`week_all <= 40` при остатке 49 пп формально влезает, хотя съедает почти весь
остаток машины, а предупреждения об этом нет ни на постановке, ни в цикле.
Человек узнаёт о том, что бакет выбран, на гейте бюджета посреди работы.

Скилл `goal-start` задаёт формат бюджета и снимок на постановке, сверки в нём
нет. Нужна одна: остаток бакета до сброса против бюджета цели, и когда старт
съедает почти весь остаток, постановка печатает предупреждение с датой
сброса. Отказом это не становится, решение остаётся за человеком.

## DoD

1. Прогон постановки на подставном снимке квоты, где бюджет цели больше остатка
   до сброса, печатает предупреждение с датой сброса; при достаточном остатке
   постановка проходит без предупреждения.
2. Правило записано в скилле постановки цели.
3. Прогон `test` из `.devkit/deploy.local` зелёный.

## Приёмка

Вид agent: сверка гоняется на подставном снимке квоты, эталон берётся из строки
вывода и кода возврата.

## Ход работы

- 2026-08-17: строка выделена из распущенной цели DK-168 грумингом DK-395,
  DK-397 и DK-168 одним заходом. Была третьей дырой цели и к вопросу человека
  отношения не имеет, поэтому в DK-395 не поехала.

  Ранг 25+6+1+0+3 = 35: серьёзность 25 (заметное трение, обход это ручной
  взгляд на `agentctl quota` перед постановкой), ценность 6 (улучшение потока,
  которое пользователь прямо попросил), неопределённость 1 (подход назван
  целиком), поправки на баг нет, рычаг 3 (каждая следующая цель ставится
  точнее, но чужой работы это не разблокирует). Цена S.
- Вычитка: 1 файл, 3 правки, 0 пометок, 2026-08-17.
- Разработка: субагент opus/medium по вердикту pick (маппинг sonnet, корректор: профицит week_all, week_max; квота: week_all 42% профицит, week_max 60% профицит, снимок 22м назад), 2026-09-17 22:51-23:29.
- 2026-09-17: сверка легла отдельной командой `agentctl fit --goal <файл цели>`.
  Она читает раздел «Бюджет» файла цели, берёт остаток бакета до сброса из
  снимка квоты и отвечает первой строкой машинно: `ok`, `tight` либо `unknown`.
  Постановка зовёт её шагом 7 порядка, до заполнения файла цели.
- Развилка, где живёт сверка. Гейт `spend` отвечает на вопрос витка, сколько
  цель уже потратила из потолка, и отметкой в реестре сторожка он же двигает
  цикл. Ключ у гейта завёл бы постановку в реестр работающих целей, поэтому
  вопрос постановки уехал своей командой. Считает она по тем же разборщикам
  (`parseGoalBudget`, `snapshot`), своего формата не заводит.
- Порог `tight` это четыре пятых остатка. Бакеты общие на машину, соседняя
  сессия тратит те же проценты, и точность тут мнимая. Порог отделяет «цель
  влезает» от «цель выбирает бакет до сброса».
- Третье значение `unknown` заведено против молчания. Снимка нет, он пуст,
  бакета в нём не показалось, дата сброса позади: `ok` читался бы тогда как
  проверенный запас. Причина уходит хвостом строки вместе с зовом
  `agentctl quota refresh`.
- Перерасход бакета считается нулевым остатком. Панель процентов больше сотни
  не показывает, доля в снимке приходит числом, и `bucketLeft` жмёт остаток к
  нулю. Бюджет тогда не влезает ни при каком лимите, ответ `tight`. Край накрыт
  тестами `TestBucketLeft` и `TestCmdFitExhaustedBucket`.
- Стенд: правка `goal-start` это появившийся шаг 7, порог повода `prompt-test`
  пройден, и прогон сделан. Сценарий `49-goal-budget-fit` сужен до шага 7:
  подставной снимок квоты с остатком 49 пп, файл цели с бюджетом
  `week_all <= 40`, машинная проверка ловит вызов `agentctl fit` в транскрипте,
  целость чисел бюджета и цель, оставшуюся в Backlog. Пара раскладок с базой
  «старый» (прежняя редакция скилла), ярус mini, k 3: 3/3 против 0/3, вердикт
  «польза». Отметка стенда в разделе «Проверка».
- regcheck: `GOWORK=off regcheck -C tools/agentctl --base df8881e5^ --tests
  tools/agentctl/fit_cli_test.go -- go test -count=1 -run TestFitCLI .`,
  2026-09-17 краснеет на базе и проходит на текущем коде. Прогон через CLI
  потому, что остальные тесты зовут новые символы (`cmdFit`, `fitTightBucket`),
  и база с ними не собирается вовсе.
- Прогон `test` из `.devkit/deploy.local` до зелёного не доехал: раннер валится
  на `go:dashboard` и стоп идёт по первому провалу. Поодиночке пакет
  `tools/dashboard` зелёный (321с), падения разные от прогона к прогону
  (`TestAskpassChainWithFakeSudo` по времени ожидания, `TempDir RemoveAll
  cleanup` по непустому каталогу). Так флакует нагрузка раннера. Пакет
  `tools/agentctl` прогнан целиком, зелёный (132с).

## Ревью

Уровень 2 до 9052d87: неопределённость 1 из pick, тронута зона гейта бюджета в tools/agentctl, новая команда с README требует уровень 2

- [возврат: правила, 2026-09-18] блокирует: пометка «- Исключение: стенд» в docs/tasks/DK-398.md не подходит под категорию из kit/skills/prompt-test/SKILL.md. Там назван повод для прогона. Это случай, когда в скилле шаг появился, пропал или поменял исход. Шаг 7 в kit/skills/goal-start/SKILL.md появился, нумерация сдвинулась с 7-9 на 8-10.: исправлено, Прогон стенда сделан: завела сценарий 49-goal-budget-fit с предметом kit/skills/goal-start/SKILL.md «Порядок» и «Бюджет и потолок яруса», сужённый до шага 7 (подставной снимок квоты с остатком 49 пп, файл цели с бюджетом week_all <= 40, машинная проверка ловит вызов agentctl fit в транскрипте, целость чисел бюджета и цель, оставшуюся в Backlog). Пара раскладок core с базой «старый» (прежняя редакция скилла), ярус mini, k 3: 3/3 против 0/3, вердикт «польза», отметка стенда в разделе «Проверка». Пометку исключения из «Хода работы» сняла.

  Исполнитель объясняет пропуск тем, что шаг машинный, его проверяют го-тесты и regcheck. Эти тесты проверяют код agentctl fit. Модель, которая ведёт goal-start, они не проверяют. Модель должна позвать fit в нужный момент, различить ok, tight и unknown, сообщить пользователю без остановки постановки. Ровно это поведение меряет obeycheck.

  Нужен прогон сценария по шагу 7, предмет можно сузить до вызова fit и разбора ответа, не до всего диалога, либо обоснование пропуска по действительной категории из SKILL.md.
- [возврат: правила, 2026-09-18] не блокирует: bucketLeft усекает остаток нулём, когда бакет потрачен больше чем на 100 процентов, но случая с исчерпанным бакетом нет ни в fit_test.go, ни в разделе «Ход работы». Код от этого не портится, fitTightBucket на нулевом остатке даёт tight при любом положительном лимите. Стоит добавить тест на такой бакет и одну строку в ходе работы про то, что перерасход считается нулевым остатком: исправлено, Край накрыт: TestBucketLeft проверяет остаток при доле 0.5, 0.515, 1 и 1.2 (перерасход даёт ноль, а не отрицательный остаток), TestCmdFitExhaustedBucket гоняет выбранный целиком бакет через cmdFit и ждёт fit: tight со строкой «бюджет 1 пп из остатка 0 пп». В «Ход работы» добавлена строка про то, что перерасход считается нулевым остатком, шаг 4 сценария проверки расширен обоими тестами.

## Сценарий проверки

1. Тесная рамка предупреждает. Бюджет `week_all <= 40` при остатке 49 пунктов
   формально влезает, и это главный случай задачи.

```sh
GOWORK=off go -C tools/agentctl test -count=1 -run TestCmdFitWarnsOnTightBudget .
```

Тест проходит. Он сверяет первую строку `fit: tight`, числа «бюджет 40 пп из
остатка 49 пп» и дату сброса в той же строке.

2. Запаса хватает, и сверка молчит. Два бакета, оба с запасом.

```sh
GOWORK=off go -C tools/agentctl test -count=1 -run TestCmdFitQuietOnRoomyBudget .
```

Тест проходит. Ответ `fit: ok`, слова «съедает почти весь остаток» в выводе
нет, второй бакет посчитан своей строкой.

3. Сверять не с чем. Снимка нет вовсе, дата сброса позади, бакета бюджета в
   снимке не показалось.

```sh
GOWORK=off go -C tools/agentctl test -count=1 -run 'TestCmdFitWithoutSnapshot|TestCmdFitExpiredWindow|TestCmdFitMissingBucket' .
```

Все три проходят. Первые два ждут `fit: unknown` с причиной и зовом
`agentctl quota refresh`, третий ищет в строке «бакета week_max в снимке нет».

4. Порог и края разбора. Четыре пятых остатка это уже tight, раздел «Бюджет»
   без лимитов это отказ, протухший снимок считается, но зовёт переснять,
   перерасход бакета читается нулевым остатком.

```sh
GOWORK=off go -C tools/agentctl test -count=1 -run 'TestFitTightBucket|TestCmdFitWithoutLimits|TestCmdFitStaleSnapshot|TestBucketLeft|TestCmdFitExhaustedBucket' .
```

Все пять проходят.

5. Команда зарегистрирована в main, путь цели берётся от корня доски, без
   `--goal` она отказывает, и в общей справке она есть.

```sh
GOWORK=off go -C tools/agentctl test -count=1 -run TestFitCLI .
```

Тест проходит.

6. Тест краснеет на старом коде.

```sh
GOWORK=off regcheck -C tools/agentctl --base df8881e5^ --tests tools/agentctl/fit_cli_test.go -- go test -count=1 -run TestFitCLI .
```

Утилита печатает «тест краснеет на df8881e5^ и проходит на текущем коде,
регрессия закрыта». База это коммит до правки. Остальные тесты зовут новые
символы, и база с ними не собирается, поэтому краснота снята прогоном через
CLI.

## Проверка

- Стенд: 2026-09-18 00:51, дерево 5154882b, предмет 1b52f68d, база старый, ярус mini, k 3, сценарии 49-goal-budget-fit, зачтён.

```console
$ obeycheck --task DK-398 --only 49-goal-budget-fit -k 3 --tier mini --base старый --no-preflight --work /private/tmp/claude-501/-Users-elenavnukova-projects-devkit/40a3cd86-5be9-4fdb-af69-18537ec607e1/scratchpad/run3 /private/tmp/claude-501/-Users-elenavnukova-projects-devkit/40a3cd86-5be9-4fdb-af69-18537ec607e1/scratchpad/rules-with /private/tmp/claude-501/-Users-elenavnukova-projects-devkit/40a3cd86-5be9-4fdb-af69-18537ec607e1/scratchpad/rules-old
сценарий                                          rules-with  rules-old  вердикт
постановка цели сверяет бюджет с остатком бакета         3/3        0/3  польза

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  49-goal-budget-fit / rules-old / повтор 1: проверка: exit status 1 (бюджет с остатком не сверялся: agentctl fit в транскрипте нет)
```

- Обкатка: 2026-09-18 00:55, свежее дерево ddeca4da9ff8, сценарий 727dce21, временный HOME, утилит дерева 11, шагов 6, все зелёные.

Шаг 1 (зелёный):

```console
$ GOWORK=off go -C tools/agentctl test -count=1 -run TestCmdFitWarnsOnTightBudget .
ok  	github.com/dronrider/devkit/tools/agentctl	0.532s
```

Шаг 2 (зелёный):

```console
$ GOWORK=off go -C tools/agentctl test -count=1 -run TestCmdFitQuietOnRoomyBudget .
ok  	github.com/dronrider/devkit/tools/agentctl	0.307s
```

Шаг 3 (зелёный):

```console
$ GOWORK=off go -C tools/agentctl test -count=1 -run 'TestCmdFitWithoutSnapshot|TestCmdFitExpiredWindow|TestCmdFitMissingBucket' .
ok  	github.com/dronrider/devkit/tools/agentctl	0.390s
```

Шаг 4 (зелёный):

```console
$ GOWORK=off go -C tools/agentctl test -count=1 -run 'TestFitTightBucket|TestCmdFitWithoutLimits|TestCmdFitStaleSnapshot|TestBucketLeft|TestCmdFitExhaustedBucket' .
ok  	github.com/dronrider/devkit/tools/agentctl	0.469s
```

Шаг 5 (зелёный):

```console
$ GOWORK=off go -C tools/agentctl test -count=1 -run TestFitCLI .
ok  	github.com/dronrider/devkit/tools/agentctl	8.638s
```

Шаг 6 (зелёный):

```console
$ GOWORK=off regcheck -C tools/agentctl --base df8881e5^ --tests tools/agentctl/fit_cli_test.go -- go test -count=1 -run TestFitCLI .
тест краснеет на df8881e5^ и проходит на текущем коде, регрессия закрыта (тесты: tools/agentctl/fit_cli_test.go)
```
