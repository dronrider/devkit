# DK-401: Рубеж задачи машинным состоянием, переживающим смену статуса

Цель: [docs/tasks/DK-395.md](docs/tasks/DK-395.md)

## Что происходит

Решение «доделать или отложить» требует ответа, что уже готово, а запись
`~/.devkit/runs/<ID>-<slug>.run` отвечает только, чем задача занята сейчас:
пакет этапов стирается на смене статуса и уезжает прозой в «Ход работы».
Таблица рубежей F замерена в файле цели (0.00, 0.35, 0.56, 0.89, 1.00), все
пять наблюдаемы машинно по коммитам и статусам.

## Чего хотим

Рубеж читается командой и переживает смену статуса: где хранить и кто двигает,
решает DK-400, здесь исполняется. Дашборд и планировщик DK-404 читают одно
и то же состояние, второй копии рубежа нет.

## DoD

На синтетической доске во временном репозитории задача, дошедшая до ревью,
отдаёт свой рубеж командой числом, а не прозой «Хода работы»; смена статуса
(ревью, замечания, Check) рубеж не стирает; все пять рубежей наблюдаемы.
Тесты модуля зелёные, regcheck зелёный.

## Ранг

`25+7+1+0+4 = 37`, P2.

- Серьёзность 25 (Major). Без рубежа планировщик считает знаменатель вслепую:
  трение, обход это ручная оценка состояния.
- Ценность 7. Ощутимое улучшение: машинный рубеж читают планировщик, возврат
  и дашборд.
- Неопределённость 1. После DK-400 всё ясно, осталось сделать.
- Поправка на баг 0. Новая работа.
- Рычаг 4. Фундамент планировщика и возврата к остывшей задаче.

## Ход работы

- Разработка: подпроцесс glm-5.3/medium по вердикту pick (квота: объявления нет, корректор выключен), 2026-08-17 14:28-14:29.
- Исполнение: подпроцесс glm-5.3/high, 2026-08-17.
- Правка: новая команда `taskctl progress <ID>` в `tools/taskctl/progress.go`.
  Рубеж нигде не хранится, по решению 1 LLD DK-400 он считается по требованию
  из долговечных меток, порядок проверки от старшего признака к младшему:
  строка в архиве (1.00), раздел «Выкат» (0.89), раздел «Ревью» (0.56), живая
  ветка с коммитами впереди main (0.35, тот же `unmergedTaskBranch`, что у
  ворот Check), иначе 0.00. Разделы читает `taskDocSections`, заголовки внутри
  ограждённых блоков остаются чужим выводом прогона. Печать одна строка с
  числом, названием рубежа и признаком; флаг `--json` отдаёт то же состояние
  полями `f`, `mark`, `sign` для планировщика слота и дашборда DK-404. Отдельных
  точек записи рубежа нет: двигают его штатные команды (коммит, первое
  замечание ревью, слияние, закрытие).
- Тесты: `tools/taskctl/progress_test.go`, синтетическая доска фикстуры во
  временном репозитории. Все пять рубежей прогнаны (архивная строка фикстуры
  закрывает 1.00, git-ветка 0.35), смена статусов Check -> In progress ->
  Blocked не стирает 0.89, цитата «## Выкат» в блоке кода рубеж не поднимает,
  JSON и печать сверены построчно. Прогон regcheck красный на старом коде,
  зелёный на новом.
- Ревью: подпроцесс glm-5.3/high по вердикту pick (квота: объявления нет, корректор выключен), 2026-08-17 14:56-15:14.


## Сценарий проверки

Вид агентский, прогон после выката в основном чекауте devkit.

1. `taskctl progress DK-388` печатает рубеж 1.00 (закрытая задача из архива).
2. `taskctl progress DK-401` печатает рубеж 0.89 (раздел «Выкат» после
   слияния), несмотря на смену статуса в Check.
3. `taskctl progress DK-999` отказывает с именем задачи и кодом возврата 1.
4. `GOWORK=off go -C tools/taskctl test ./...` зелёный: все пять рубежей
   покрыты тестами на синтетической доске.

## Выкат

- 2026-08-17 слито: f8cd1c35, 7d3dd3c8
