# DK-407: Карта переходов работы плоскими строками со сторожем полноты

Цель: [docs/tasks/DK-395.md](docs/tasks/DK-395.md)

## Что происходит

Путь работы от идеи до архива нигде не нарисован: схема в README про доставку
правил, docs/map.md это карта компонентов и индекс решений, а связь между
шагами держит состояние доски, а не текст. Черновик DK-384 приписан в файл
цели, решение по нему принял DK-400: карта переходов живёт рядом с рубежом,
ручной docs/workflow.md плоскими строками со сторожем полноты
в `devkitctl doctor`.

## Чего хотим

Документ со схемой, где узлы это состояния работы (идея, черновик, строка,
ветка, ревью, слияние, очередь выката, приёмка, архив), а рёбра подписаны тем,
кто переход делает, скиллом или командой. Карта пишется руками, сторож
проверяет полноту по образцу сторожа docs/map.md: каждый скилл и каждый статус
доски упомянут, ссылки живые. С новыми понятиями DK-400 (рубеж, парковка,
пробуждение) карта согласована.

## DoD

docs/workflow.md держит карту переходов плоскими строками; сторож полноты
в `devkitctl doctor` ловит неупомянутый скилл и неупомянутый статус доски
на синтетическом прогоне; карта покрывает новый путь парковки и возврата
из решения 2 LLD DK-400. Тесты сторожа зелёные, regcheck зелёный.

## Ранг

`25+5+1+0+4 = 35`, P2.

- Серьёзность 25 (Major). Без карты дыры переходов вида DK-133 находятся
  живым случаем, а не чтением: трение, обход это разбор скиллов руками.
- Ценность 5. Долгоживущая дока, читатель видит путь работы целиком.
- Неопределённость 1. Формат и место решены DK-400, осталось написать.
- Поправка на баг 0. Новая работа.
- Рычаг 4. Сторож держит карту свежей при каждом новом скилле.

## Ход работы

- Разработка: подпроцесс glm-5.3/high по вердикту pick (квота: объявления нет, корректор выключен), 2026-08-17 15:49-15:49.
- 2026-08-17: написана карта `docs/workflow.md` плоскими строками по участкам
  пути (зарождение, работа, ревью и слияние, выкат и приёмка, парковка
  и возврат, цель, рубеж, несколько рук). Рёбра подписаны командами и
  скиллами, двухступенчатый Check вошёл отметкой smoke и освобождением
  очереди, парковка вошла путём «вопрос:» в Blocked и пробуждением тиком
  сторожка по решению 2 LLD DK-400.
- Сторож полноты лёг модулем `tools/devkitctl/workflow.py` и позван из
  `doctor` рядом с раскладкой. Списки ожидаемого читаются из носителей:
  скиллы из `kit/skills/*`, статусы доски из таблицы секций
  `tools/taskctl/board.go`, рубежи из констант `tools/taskctl/progress.go`;
  источник, который перестал разбираться, сам становится находкой. Вне
  чекаута devkit сторож молчит. Тесты в `tools/devkitctl/workflow_test.py`:
  синтетика на неупомянутый скилл, статус и рубеж, отсутствие карты, чужой
  проект и неразбираемый источник, инвариант живого чекаута и проводка обеих
  находок через doctor на стенде.
- Находка по ходу, чужая для ветки: `docs/map.md` на main отстал от индекса
  решений (нет строк DK-400.1-DK-400.8), doctor на исправном main красит
  «карта правлена руками, хеш разошёлся»; чинится перегенерацией отдельной
  правкой.
- Вычитка: 3 файла, 3 правки, 0 пометок, 2026-08-17.
- Ревью, замечания 1-3: фикстуре DoctorResidencyTest добавлена болванка
  docs/workflow.md (tiny-skill, четыре статуса, пять рубежей) по образцу
  болванок README для DK-071: чекаут фикстуры сам себе devkit, доктор на нём
  обязан выходить чистым, а карта в этом стенде не предмет проверки. У
  источника скиллов появилась симметричная находка NO_SKILLS: пустой
  или уехавший kit/skills больше не разоружает проверку скиллов молча.
  Тест неразбираемого источника разбит на три пути: board.go, progress.go
  и kit/skills, регрессия NO_SKILLS красная на коде до правки.
- Ревью: подпроцесс glm-5.3/high по вердикту pick (квота: объявления нет, корректор выключен), 2026-08-17 16:14-17:11.

## Ревью

- Ветка красит соседний набор: python3 -m unittest discover в tools/devkitctl даёт 3 падения в weigh_test.DoctorResidencyTest (test_1_clean_checkout_prints_the_pockets, test_2_oversized_skill_body, test_5_total_over_the_ceiling); доктор на синтетическом чекауте этой фикстуры выходит с находкой «карты переходов docs/workflow.md нет». Фикстуре нужен минимальный docs/workflow.md с упоминанием tiny-skill, четырёх статусов доски и пяти рубежей, либо другой способ погасить новую находку в этом стенде.: исправлено
- Сторож молчит, если у kit/skills не осталось ни одного SKILL.md: skills() вернёт пустой список без находки «источник не читается», тогда как у board.go и progress.go такие есть (NO_STATUSES, NO_MARKS). Это расходится с правилом, заявленным самим модулем и README devkitctl («источник, который перестал разбираться, сам становится находкой»): переезд или переименование каталога скиллов разоружит проверку скиллов молча. Добавить третью находку и тест на неё.: исправлено
- Тест неразбираемого источника гоняет только board.go: путь NO_MARKS (progress.go отсутствует или константы progress* из него пропали) тестом не покрыт. Добавить симметричный случай.: исправлено
- В файле задачи нет раздела «Сценарий проверки»: ворота Check без него откажут, а у агентского вида в сценарии нужен шаг, прогоняемый после выката и не повторяющий тесты ветки (например, doctor на свежем чекауте потребителя после devkitctl update). Написать до слияния. : исправлено

## Сценарий проверки

Вид агентский, прогон после выката в основном чекауте devkit.

1. `python3 tools/devkitctl/workflow.py` отвечает «карта переходов полна»:
   каждый скилл, все четыре статуса доски и пять рубежей упомянуты,
   `devkitctl doctor` находок по карте не даёт.
2. `grep -n 'shipctl smoke\|taskctl progress' docs/workflow.md` находит оба:
   карта согласована со свежим конвейером, двухступенчатым Check и рубежом.
3. `GOWORK=off python3 tools/devkitctl/parallel.py` зелёный: тесты сторожа
   (неупомянутый скилл, статус, рубеж, пустые источники) в общем прогоне.

## Выкат

- 2026-08-17 слито: ad796410, 71271ab4, 1a2a424c, f7258900, dce5a7f0, 910bb81e, 3c2905ff, e4c383c7, 17ff8a17, 09a9c08c, 650cf612
