# DK-408: obeycheck: контрольная ветка без правил, зелень без текста не в счёт

## Что происходит

держится ли правило без своего текста в контексте. Контрольной ветки без правил
вообще у стенда нет, и от этого часть зелени ничего не доказывает: сценарий,
который зеленеет и на пустой раскладке, говорит не про силу ядра, а про то, что
модель так делает и без всякого текста. Сейчас такой сценарий выглядит как
успешный вынос строки.

Поймано на стороннем прогоне 2026-08-17, где сравнивались два промпта по 12
зондам. Девять зондов из двенадцати оказались бесполезны ровно по этой причине:
дефект, который они ловили, у ветки без промпта не воспроизводился ни разу (0 из
54), поэтому сравнение мерило вред одного текста, а не пользу другого. Пока в
счёт шли все зонды подряд, разница между ветками пряталась: 18% против 10%. На
трёх зондах, где контрольная ветка вообще могла провалиться, та же разница
читалась как 72% против 33%.

Кандидат: пустая раскладка третьей веткой прогона, флагом вроде --control.
Сценарий, зелёный на пустой раскладке, уходит в итог отдельной строкой и в
доказательство выноса не годится: правило соблюдается и без текста, значит
разговор идёт не про вынос в полный текст, а про удаление строки.

Открытое: гонять контроль каждым прогоном (треть лишней квоты) или разово при
заведении сценария, запоминая исход в самом файле сценария.

## Чего хотим

## Границы

Задача даёт стенду контрольную точку и вердикт. Пересмотр самих сценариев
`tools/obeycheck/scenarios` по новому вердикту и решения о выносе правил из ядра
едут отдельно.

## DoD

У прогона появляется третья ветка, пустая раскладка, и вердикт сценария считается
с оглядкой на неё: сценарий, зелёный без всякого текста правил, уходит в таблицу
отдельным вердиктом и в доказательство выноса строки не годится. Развилка
«контроль каждым прогоном против разового при заведении сценария» решена в
задаче и записана в README стенда причиной. Таблица прогона и её тест показывают
новый вердикт, README раздел про вердикты дополнен, прогон на готовых сценариях
`tools/obeycheck/scenarios` печатает контрольную колонку.

## Ранг

`25+5+2+0+2 = 34`, P2.

- Серьёзность 25: трение с дешёвым обходом. Стенд сравнивает любые две
  раскладки, поэтому контроль сегодня достижим вторым прогоном «полная против
  пустой», но сводится он глазами и стоит трети лишней квоты.
- Ценность 5: вердикт «держится» перестаёт быть словом стенда и становится
  доказанным; по следу стороннего прогона девять зондов из двенадцати мерили
  вред одного текста вместо пользы другого, а разница между ветками читалась как
  18 против 10 там, где на самом деле было 72 против 33.
- Неопределённость 2: подход назван (пустая раскладка третьей веткой, флаг вроде
  `--control`), открыто одно, гонять контроль каждым прогоном или разово при
  заведении сценария с запоминанием исхода в файле сценария.
- Поправка на баг 0: стенд считает то, что задумано, не хватает ему третьей
  точки отсчёта.
- Рычаг 2: дешевле идёт работа по одной теме, вынос правил в ядро и обратно.

Цена M: правка прогона и таблицы в go с тестами, одна сессия.

Вид приёмки agent: прогон печатает вердикт колонкой, сверка объективная.

## Что проверено на груминге

- Нехватка на месте: `--control` и пустой раскладки в `tools/obeycheck/` нет,
  README называет вердикты «держится», «регрессия» и «красный на обеих», четвёртого
  нет.
- Обход существует и он в интерфейсе: «сравниваются любые две (полная против
  ядра, ядро против ядра без одной строки, вчерашняя против сегодняшней)»,
  README раздел «Раскладка».
- Дублей нет: соседний DK-410 про судейскую проверку сценария, предмет другой.

## Ход работы

- Из черновика DK-408 «obeycheck сравнивает две раскладки правил (полная против ядра) и отвечает,»: записан 2026-08-17, оформлен 2026-08-20.
