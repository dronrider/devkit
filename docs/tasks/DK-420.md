# DK-420: Дока запускатора окна второй подписки: порядок первого запуска с нуля

записан 2026-08-17

## Что происходит

Окно второй подписки поднимает `shipctl code <ID>` (DK-175, DK-192, DK-214),
и в `tools/shipctl/README.md` про него три раздела: «Окно на второй
подписке», «Копия на окно, а не на задачу», «Окружение живёт в директории,
а не в запуске». Пользователь, читая их 2026-08-17, не нашёл ответа на
вопрос «как поднять окно на новой машине с нуля»: разделы разбирают
устройство, а порядка первого запуска не дают. Проверено чтением
`tools/shipctl/README.md` (строки 108-231) и `tools/devkitctl/README.md`
(строки 870-895): сводного списка шагов нет ни там, ни там.

Что не так конкретно:

- порядок первого запуска разложен по трём местам и нигде не собран: ключ
  `home` секции `[glm-code]` в `~/.devkit/harness.local` (shipctl, абзац про
  машинный слой), болванка каталога конфига от `devkitctl doctor --fix` и
  находки про пустые ключи (devkitctl, раздел доктора), значения
  `ANTHROPIC_BASE_URL`, `ANTHROPIC_AUTH_TOKEN`, `ANTHROPIC_MODEL` из кабинета
  подписки, потом `shipctl code <ID>`. Сводного «шаг 1, 2, 3, проверка
  `--dry-run` и `--probe`» нет;
- вводный абзац раздела «Окно на второй подписке» объясняет рычаг через
  `CLAUDE_CONFIG_DIR` и экспорт переменной перед каждым запуском, а раздел
  «Окружение живёт в директории» ниже говорит, что после DK-192 ключи лежат в
  самой копии в `.claude/settings.local.json` и окну переменная при запуске
  не нужна. Абзац остался от старой схемы, две картины спорят друг с другом
  (та же старая формулировка в шапке `tools/shipctl/editor.go:18`);
- откуда берётся имя поставщика (`glm`), путь копии `../<проект>-glm` и что
  такое профиль `kit/harness/glm-code.toml`, читатель узнаёт из корневого
  README и доки devkitctl, а не из доки запускатора.

Обход есть и дешёвый: собрать порядок из трёх мест самому либо спросить в
чате, что и случилось. На чужой машине без человека, знающего фичу, обход
уже не работает: без порядка запуска фича до пользователя не доезжает.

## Чего хотим

Раздел про окно второй подписки в `tools/shipctl/README.md` открывается
списком «первый запуск с нуля» на новой машине: заведение ключа `home` в
машинном слое, `devkitctl doctor --fix` за болванкой каталога, вписывание
значений из кабинета подписки, `shipctl code <ID>`, чем проверить
(`--dry-run`, `--probe`, находки доктора). Из списка на 100% понятно, что и
в каком порядке делать, а разбор устройства идёт после него. Вводный абзац
переписан под текущую схему: окружение живёт в директории копии,
`CLAUDE_CONFIG_DIR` упоминается только там, где он ещё участвует (машинный
слой, подпроцесс делегирования); та же правка в шапке `editor.go`. Раздел
доктора в `tools/devkitctl/README.md` ссылается на этот порядок, а не
дублирует его. Запуск без задачи не сюда: это DK-203, в списке на неё
отсылка, а вход без ID дописывается, когда она сделана.

## DoD

В `tools/shipctl/README.md` есть список первого запуска окна второй подписки
с нуля, по которому чужой контекст на временном `HOME` с синтетическим
машинным слоем доходит до успешного `shipctl code <ID> --dry-run` без
подглядывания в код и в другие доки; вводный абзац раздела и шапка
`editor.go` описывают текущую схему (окружение в директории копии), а раздел
доктора в devkitctl ссылается на список. Правка проходит вычитку
`proofread` и `check-symbols`.

## Приёмка

Вид agent. Барьер «глаза» (понятность текста) обходится прогоном чужого
контекста по списку на временном `HOME`: дошёл до `--dry-run` без подсказок
значит список полный, застрял значит место пробела названо шагом.

## Ранг

`25+5+1+0+2 = 33`, P2.

- Серьёзность 25 (Major). Заметное трение: первый запуск на новой машине
  требует собрать порядок из трёх мест или спросить знающего; обход дешёвый
  и очевидный, пока знающий рядом.
- Ценность 5. Ощутимое улучшение, которого просят: пользователь назвал пробел
  сам и требует стопроцентной понятности запуска.
- Неопределённость 1. Подход ясен (список шагов в начале раздела, правка
  вводного абзаца, ссылка из доктора), правка только в доке и одном
  комментарии, осталось сделать.
- Поправка на баг 0. Дефекта в коде нет, недостаёт доки.
- Рычаг 2. Небольшая своя польза: каждый следующий подъём окна на другой
  машине дешевле, вопрос в чат не уходит.

## Ход работы

- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 2%, снимок 43м назад, сдвига нет), 2026-08-17 21:51-21:51.
- Раздел «Окно на второй подписке» в `tools/shipctl/README.md` открыт
  нумерованным списком первого запуска с нуля (ключ `home` секции
  `[glm-code]` в `~/.devkit/harness.local`, `devkitctl doctor --fix` за
  болванкой каталога, значения из кабинета подписки, `shipctl code <ID>`,
  проверка `--dry-run`/`--probe`/находками доктора), со ссылкой на DK-203 для
  запуска без задачи. Вводный абзац раздела переписан под текущую схему:
  ключи подписки лежат в настройках копии окна, `CLAUDE_CONFIG_DIR`
  упомянут только там, где ещё участвует (раскладка машинного хозяйства,
  окружение подпроцесса делегирования). Та же правка в шапке
  `tools/shipctl/editor.go` (комментарий над блоком констант).
- Раздел доктора в `tools/devkitctl/README.md` (пункт про конфиг второй
  подписки) больше не повторяет порядок запуска, а ссылается на новый список
  в `tools/shipctl/README.md`; сама специфика доктора (болванка, пустые
  ключи, битый json) осталась на месте.
- Факты сверены с кодом, а не с памятью: `altConfigDir`/`loadAltSub` в
  `tools/shipctl/editor.go`, находки `check_alt_sub` в
  `tools/devkitctl/devkitctl.py`, `windowTree`/`windowSuffix` (путь копии
  `../<проект>-glm`), профиль `kit/harness/glm-code.toml`.

## Сценарий проверки

Барьер «глаза» (вид agent) обходится прогоном списка чужим контекстом на
временном `HOME` с синтетическим машинным слоем, без подглядывания в код и
в другие доки, кроме самого списка в `tools/shipctl/README.md`.

1. Собрать бинарь: `cd tools/shipctl && GOWORK=off go build -o /tmp/dk420/shipctl .`
2. Завести синтетический проект: временный `HOME=/tmp/dk420/home`, каталог
   проекта `/tmp/dk420/proj` с `git init -b main`, коммитом и минимальной
   доской `docs/TASKS.md` (шапка секции Backlog хватает, реальной строки
   задачи для `--dry-run` не нужно).
3. Пройти список из README буквально: вписать в
   `$HOME/.devkit/harness.local` секцию `[glm-code]` с ключом `home`, позвать
   `HOME=/tmp/dk420/home python3 tools/devkitctl/devkitctl.py doctor --fix -C /tmp/dk420/proj`,
   вписать в разложенный `settings.json` (секция `env`) синтетические
   значения `ANTHROPIC_BASE_URL`, `ANTHROPIC_AUTH_TOKEN`, `ANTHROPIC_MODEL`.
4. Ожидание: `HOME=/tmp/dk420/home /tmp/dk420/shipctl code DK-1 --dry-run` из
   `/tmp/dk420/proj` печатает путь копии, base URL, модель, признак токена
   и строку `dry-run: редактор не запускался`, код возврата 0. Если по ходу
   список потребовал заглянуть в код или в другую доку -- место разрыва
   называется шагом номер, на котором список перестал быть понятен буквально.
5. Дополнительно (не обязательно для DoD, но проверяет ссылку доктора):
   `HOME=/tmp/dk420/home python3 tools/devkitctl/devkitctl.py doctor -C /tmp/dk420/proj`
   до заполнения ключей называет находкой пустые `ANTHROPIC_BASE_URL`,
   `ANTHROPIC_AUTH_TOKEN`, `ANTHROPIC_MODEL`, после заполнения находка
   пропадает.

## Проверка

Прогнан целиком шаг за шагом на `/private/tmp/.../dk420-verify` (временный
`HOME` и синтетический проект с этой машины, значения подписки
синтетические).

Шаг 1, машинный слой:

```
$ cat home/.devkit/harness.local
[glm-code]
home = "~/.devkit/claude-glm"
```

Шаг 2, `devkitctl doctor --fix` (вывод обрезан до находок про вторую
подписку, остальные находки от пустого синтетического проекта к делу не
относятся):

```
починено: разложена болванка конфига второй подписки .../claude-glm/settings.json
(ключи ANTHROPIC_BASE_URL, ANTHROPIC_AUTH_TOKEN, ANTHROPIC_MODEL), права сужены:
там будет лежать токен
...
машина: в конфиге второй подписки .../claude-glm/settings.json пустые ключи
ANTHROPIC_BASE_URL, ANTHROPIC_AUTH_TOKEN, ANTHROPIC_MODEL: пока они пусты, окно
shipctl code либо не откроется, либо уйдёт на первую подписку молча; вписать
значения второй подписки (в devkit они не едут, машинный слой лежит вне
репозиториев)
```

Шаг 3, значения вписаны в `env` разложенного `settings.json`
(`ANTHROPIC_BASE_URL`, `ANTHROPIC_AUTH_TOKEN`, `ANTHROPIC_MODEL`), повторный
`devkitctl doctor` без `--fix` находки про пустые ключи больше не называет.

Шаг 4/5, `shipctl code DK-1 --dry-run`:

```
$ HOME=.../home ./shipctl code DK-1 --dry-run
копия окна: .../proj-glm (ещё не заведена, заведёт запуск без --dry-run)
ветка задачи dk-1 переключается внутри копии (тем же путём, что shipctl start)
машинный слой: .../home/.devkit/claude-glm/settings.json
окружение копии: .../proj-glm/.claude/settings.local.json (ANTHROPIC_BASE_URL,
ANTHROPIC_AUTH_TOKEN, ANTHROPIC_MODEL, DEVKIT_HARNESS=glm-code)
base URL: https://endpoint.example/anthropic
модель: модель-синтетическая
токен: есть
заголовок окна: [GLM] proj
редактор: code .../proj-glm
dry-run: редактор не запускался
exit=0
```

Список довёл до успешного `--dry-run` без единого обращения к коду или к
другой доке помимо самого раздела README. Дополнительно: `go build ./...` в
`tools/shipctl` зелёный, `python3 hooks/check-symbols.py` на трёх правленых
файлах чист (exit 0).
