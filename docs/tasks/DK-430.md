# DK-430: LLD: реестр чатов задачи, адрес письма, инструмент ожидания и панель чата

Цель: [DK-397.md](DK-397.md)

## Что происходит

Цель DK-397 закрывает главную функцию дашборда: разговор с агентом, который
ведёт конкретную задачу. Четыре развилки пользователь закрыл на груминге (работа
идёт целью со своим LLD, доска остаётся домом и чат открывается панелью справа,
чатом задачи считается любая живая сессия и писать можно во все, вопрос агента
ловится протоколом с эвристикой в запасе), остальные решает этот документ. Он
первая строка цели, и на нём стоят семь оставшихся.

Транспорт достался цели готовым. Пока DK-397 стояла в Backlog, цель DK-395
прошла свой цикл целиком: общий носитель писем `.devkit/mail` с адресацией
сессией закрыт строкой DK-345, парковка задачи вопросом и пробуждение тиком
`devkitctl watch` строкой DK-402, отвязка `wait-human` от стопа цикла строкой
DK-403. Признак ожидания `<имя>.ask` в носителе описан и читается
(`hooks/inbox.py`, `tools/devkitctl/watch.py`), но ставить его некому: писателя
нет, и комментарий в почтальоне зовёт его «будущим инструментом ожидания».

## Что решать

1. **Реестр чатов задачи.** Где живёт запись «сессия ведёт задачу», кто её
   кладёт (дашборд при подъёме чата, хук старта сессии в дереве задачи), что
   делать с сессиями, которых в реестре нет, и как выглядит подпись источника
   привязки. Сегодня привязка только угадывается: `sessionTask`
   (`tools/dashboard/sessions.go`) пробует хвост каталога дерева, ID в первой
   реплике и имя ветки, и на этом горят оба приписанных черновика: разговорная
   сессия про задачу загорается её живой работой (DK-360), а нераспознанную не
   открыть вовсе (DK-291).
2. **Адресация писем.** Ящик на сессию или ящик на задачу с полем сессии, как
   это ложится на готовый `.devkit/mail` (ящики `task-<ID>.inbox` и
   `sess-<sid>.inbox`, `tools/dashboard/mailbox.go`), на старый ящик цели
   `.devkit/goal-<ID>.mail` и на запроектированный ящик черновика (LLD DK-354,
   строки DK-373 и DK-374). Третьей копии механики отметок и ожидания не
   заводить.
3. **Форма инструмента ожидания и его стык с парковкой.** Исполнителю задачи
   спросить человека нечем: `goal-run.py --ask` ждёт за цель, `draft ask` только
   запроектирован. Решить, чем задаётся вопрос из захода, кто ставит `.ask` и
   как приходит ответ. Отдельно решить границу с DK-402: вопрос ждёт ответа в
   том же заходе или паркует задачу, и по какому признаку исполнитель выбирает.
   Развилки этой на груминге не было, парковка выкатилась после постановки цели.
   Решение доводится до скиллов `exec-*` и `board-*`: вопрос задаётся
   инструментом, а не последней репликой захода.
4. **Состояние «ждёт человека».** Точный признак это файл ожидания, запасной
   повод `idle_prompt` из журнала уведомителя (`hooks/notify.py`). Решить, где
   состояние живёт в API, как попадает на карточку задачи, в ленту и в
   уведомление, и как в подписи называется источник. Чип «блок: вопрос: ...»
   на строке доски уже приехал с парковкой, дальше него сегодня ничего нет.
5. **Устройство панели.** Ширина и её память, поведение при переходе между
   задачами, вид на телефоне. Кандидат по телефону: панель не ужимается в
   колонку, а открывается отдельным маршрутом на весь экран с возвратом на
   прежнее место доски и сохранением позиции ленты. Причина в том, что чат на
   375 точках рядом с доской нечитаем, а маршрут даёт настоящую кнопку «назад»
   и ссылку.
6. **Что остаётся от пары «Живой статус» и «Чат с агентом».** Наполнение у них
   одно (обе читают транскрипт сессии, узнанной по `?task=`), разница в поле
   ввода и в том, что чат заведён только целям (`app.js`, `renderChat` отбивает
   не-цель). Кандидат: это один элемент, у которого поле ввода гаснет с
   названной причиной, когда писать некуда.
7. **Общий чат доски без задачи.** Кандидат: та же панель с пустой привязкой,
   а не свой экран.

## DoD

- Документ лежит в `docs/lld/DK-430-task-chat.md`, каждая развилка закрыта
  решением с номером, отвергнутые варианты названы с причиной.
- Решения ложатся на состав цели: по документу видно, что делает каждая из
  строк DK-431, DK-432, DK-433, DK-371, DK-434, DK-435 и DK-436, и где проходит
  граница между ними. Разошедшийся с решениями состав правится строками доски,
  а расхождение называется в документе.
- Носитель писем остаётся один: решение по адресации проверяется чтением кода
  `tools/dashboard/mailbox.go` и `hooks/inbox.py`, третьей механики отметок и
  ожидания документ не заводит.
- Индекс решений в `docs/map.md` перегенерён (`devkitctl doctor --fix`).

## Приёмка

- вид: user
- барьер «согласие»: решения по развилкам это выбор устройства, а не проверяемый
  факт; обхода у барьера нет, субагент-критик даёт находки, но не согласие

## Проверка

Приёмка пользовательская, барьер «согласие»: решения читаются глазами, машинной
части у них две строки.

1. `docs/lld/DK-430-task-chat.md` на месте, семь развилок постановки закрыты
   решениями с номерами, у каждого назван отвергнутый вариант с причиной.
2. Раздел «Что меняется в строках цели» называет предмет каждой из семи строк и
   четыре расхождения с составом.
3. `python3 tools/devkitctl/devkitctl.py doctor --fix` не находит расхождения
   карты: индекс решений DK-430.1..DK-430.7 стоит в `docs/map.md`.
4. `python3 hooks/check-symbols.py docs/lld/DK-430-task-chat.md docs/tasks/DK-430.md`
   молчит.

## Ход работы

- Разработка: opus/xhigh по вердикту pick, 2026-08-18 01:28-01:31.
- 2026-08-18: написан LLD, семь решений.

  Реестр чатов вышел машинным журналом `~/.devkit/sessions.log` по образцу
  журнала уведомителя: пишет SessionStart-хук, потому что ID сессии в момент
  подъёма есть только у него (дашборд отдаёт команду tmux и уходит), задачу берёт
  из `DEVKIT_TASK` в команде сессии либо из хвоста дерева, а человек привязывает
  разговор ручкой. Главное следствие: привязка получила два разряда, «ведёт» по
  записи и «говорит о» по угадыванию, и работой задачи считается только первый.
  Этим закрываются оба приписанных черновика, DK-360 и DK-291.

  Адресация писем осталась на готовом носителе, поменялись три правила: ящик
  принадлежит разговору, адресуется только живой собеседник (иначе ответ на
  вопрос припаркованной задачи лёг бы адресованным мёртвой сессии и не достался
  никому), а признак `.ask` стал структурой со сроком первой строкой, ждущей
  сессией и пачкой вопросов. Из структуры у почтальона появился частичный
  отказ, и два живых чата одной задачи перестали глушить друг друга. Черновик DK-354
  переезжает на общий носитель, ящик цели остаётся своим, и цена перевода
  названа.

  Инструмент ожидания это `taskctl ask <ID>`: язык выбран правилом README (в
  PATH, зовётся из любого проекта), дом выбран хозяйством (уведомление, доска и
  парковка уже там). Go-механика ящика переезжает в `internal/mail`. Границу с
  парковкой DK-402 решили так: исполнитель выбирает не «ждать или парковать», а
  момент вопроса, а дальше инструмент сам ждёт до 480 секунд под потолком хода
  Bash в 600 и по таймауту паркует задачу.

  Панель сделана хвостом адреса `/chat/<адрес>` с `pushState`, и отдельный
  маршрут для телефона отпал: кнопку «назад» и ссылку даёт сам адрес. «Живой
  статус» и «Чат» слиты в один элемент, панель tmux убрана совсем, поле ввода
  гаснет с одной из трёх названных причин. Общий чат доски это та же панель с
  пустой привязкой.

  Диспетчеру: строка DK-431 выросла и просится либо в L, либо в разрез по шву
  «журнал и хук» против «адрес письма и ручка почты задачи»; DK-433 зависит ещё
  и от DK-431; постановки DK-373 и DK-374 расходятся с решением 2; кандидатом в
  накопитель просится слияние ящика цели с общим носителем.
- Вычитка: 2 файла, 15 правок, 0 пометок, 2026-08-18.
