# DK-432: Инструмент ожидания у задачи: вопрос задаётся из захода, а не последней репликой

Цель: [tasks/DK-397.md](tasks/DK-397.md)

## Что происходит

Исполнителю задачи иногда нужно спросить человека, и сегодня такой вопрос
идёт последней репликой захода. Последняя реплика это возврат диспетчеру, и
человек в дашборде её не видит вовсе: вопрос теряется вместе с заходом.
Штатный обход один, ручная парковка `taskctl move <ID> blocked --reason
"вопрос: ..."` (DK-400): вопрос виден, сторожок будит задачу ответом, но
разговора нет, заход уже кончился и ответ ждёт нового. Запись этапа работы
видом «уточнение» (`~/.devkit/runs/<ID>-<slug>.run`) для того и заведена, а
отмечать её сегодня некому, о чём прямо сказано в `tools/agentctl/stage.go`.
Наблюдение собрано LLD DK-430, закрытым 2026-08-18.

## Чего хотим

Вопрос человеку задаётся инструментом посреди захода. Решения приняты LLD
DK-430 (решение 3 и раздел «Что меняется в строках цели»), исполнение идёт
по ним:

- `taskctl ask <ID>` принимает вопрос ключом `--question` или пачку с
  вариантами структурой JSON на stdin (формат DK-354); реализация одна на
  два входа, второй это `taskctl draft ask <ID>`;
- go-механика входа переезжает из `tools/dashboard/chat.go` в пакет
  `internal/chat`: дашборд пишет реплики, `taskctl` их ждёт, разбор один;
- признак ожидания `.ask` пишется во вход задачи основного чекаута и
  опрашивается вместе со входом своего дерева, ответ доезжает и от ручки
  задачи, и от панели;
- подхват входов `.devkit/chat` уезжает с `hooks/chat-in.py` в
  `taskctl chat --hook`, доктор переводит `settings.json`;
- вызов отмечает этап «уточнение», зовёт уведомитель громким поводом
  `task_ask`, опрашивает вход до срока и печатает ответ агенту, признак
  снимается на любом выходе, включая падение;
- не дождавшись, паркует задачу сам: `taskctl -C <основной чекаут> move <ID>
  blocked --reason "вопрос: <суть>"`, рук диспетчера это не ждёт; срок по
  умолчанию 480 секунд под потолок хода Bash, `--wait` двигает его, а
  `--wait 0` паркует сразу;
- страховка от убитого хода: сторожок `devkitctl watch` на тике паркует
  протухший признак, если реестр чатов (DK-431) не видит живой сессии задачи,
  живой сессии страховка молчит;
- скиллы `exec-*` и `board-task` переписаны одним правилом: вопрос задаётся
  инструментом посреди захода, вызов идёт с явным сроком хода (`--wait 480`
  при `timeout` 540000), и стенд послушания ловит заход, кончившийся
  вопросом вместо вызова.

## Границы

- Поле `waiting`, чип на карточке и врезка вопроса в ленте едут строкой
  DK-433, правило адресности и фильтр адресата у сторожка строкой DK-438.
- Разговор цели (входы `goal-<ID>`, `goal-run.py`) остаётся на своём
  носителе и своём читателе: перевод цели на общий носитель это черновик
  DK-439, не эта задача.
- `AskUserQuestion` харнеса не годится: до headless-исполнителя он не
  доходит, для того у devkit и есть вход разговора.

## Развилки

- Подхват реплики остаётся на python. LLD решением 3 переводит его в
  `taskctl chat --hook`, но перевод едет половиной «носитель» этой же строки,
  а сюда диспетчер отдал половину «инструмент». Частичный отказ при живом
  признаке поэтому написан там, где подхват живёт сегодня, в
  `hooks/chat-in.py`, и структуру признака он читает своим разбором
  (`ask_fields`), тем же, каким берёт срок сторожок. Перевод на go эту функцию
  унесёт целиком, а не перепишет.
- Реестр чатов читают трое, и разбор у них не размножен: строка журнала
  разбирается пакетом `internal/sessions`, дашборд остался на нём же тонкими
  обёртками, `taskctl ask` берёт оттуда ждущую сессию. Третий читатель это
  сторожок, и он на python, поэтому разбор строки у него свой, рядом со своим
  чтением доски; общего с ним тут только имена полей.
- Срок ожидания и срок хода разведены явно, потому что путаница тут стоит
  задачи. Умолчание `--wait` это 480 секунд под потолок хода Bash в 600, а
  умолчание самого хода 120, и вызов без явного `timeout` убился бы на второй
  минуте. Правило «`--wait 480` при `timeout` 540000» стоит теперь в скиллах
  `exec-*`, в `board-task` и в README рядом с примером вызова, а стенд
  послушания держит сам факт вызова инструмента сценарием `12-ask-human`.
- Признак ожидания не переживает смерти хода тремя способами сразу: снимается
  штатным выходом, снимается сигналом (`SIGINT`, `SIGTERM`, `SIGHUP`) и не
  переживает `SIGKILL` дольше своего срока, потому что срок лежит внутри файла,
  а страховка сторожка паркует строку при мёртвой сессии. При живой сессии
  страховка молчит: убитый ход ожидания не значит убитой сессии.
- `--wait 0` признака не пишет вовсе. Ждущего при нём нет, а лежащий признак
  запер бы у подхвата безадресные строки на пустом месте.
- Корни для страховки берутся не только из реестра целей. Запись
  `~/.devkit/goals/*.watch` кладёт гейт бюджета внутри цикла цели, а задача,
  взятая напрямую, такой записи не получает вовсе, и страховка молчала бы ровно
  там, где вопрос задан вне цикла (замечание 2 ревью). Вторым источником взяты
  записи этапов `~/.devkit/runs/*.run`: поле `root` в них это основной чекаут, а
  кладёт запись сам вопрос. Реестр чатов на эту роль не годится: он называет
  дерево сессии, а им бывает боковое дерево задачи, и правка доски пошла бы из
  него. Тем же обходом расширено и пробуждение ответом.
- Неуехавшая доска не оставляет признак лежать. Парковка страховкой это
  `move ... --push`, и хвост её спотыкается там, где push отбит: строка при этом
  уже в Blocked. Признак снимается и в этом случае, а неуехавшая доска
  называется отдельной строкой отчёта; отказ до правки доски читается иначе, по
  строке, оставшейся в In progress (замечание 1 ревью).
- Повод `task_ask` типизирован в ленте дашборда как `wait` одной строкой
  таблицы поводов. Поле `waiting`, чип и врезка едут DK-433, но событие без
  типа падало бы в «прочие», и человек снова не увидел бы вопрос, ради
  видимости которого всё затевалось.
- Черновик паркует не доску, а файл исхода `.devkit/draft-<ID>.question`, как
  его назвал LLD DK-354: строки на доске у черновика нет, и парковать там
  нечего.

## DoD

- `taskctl ask` в двух входах, задача и черновик, печатает агенту ответ из
  входа и снимает признак на любом выходе;
- заход, спросивший человека, продолжает работу ответом, а не дождавшись
  паркуется сам причиной «вопрос:»;
- убитый ход не оставляет строку в In progress: сторожок паркует её при
  мёртвой сессии и молчит при живой;
- стенд послушания находкой зовёт заход, кончившийся вопросом вместо вызова
  ask;
- `hooks/chat-in.py` больше не читает входы `.devkit/chat` задач, у него
  остаются только входы цели.

## Ранг

`25+9+3+0+3 = 40`, P2.

- Серьёзность 25: вопрос последней репликой человек в дашборде не видит
  вовсе, обход ручной парковкой есть, трение заметное.
- Ценность 9: живой разговор вокруг задачи и есть предмет цели DK-397, ради
  него цель затевалась.
- Неопределённость 3: решения съедены LLD, но правка пересекает границы:
  go-пакет, доктор, хуки, скиллы и стенд послушания.
- Поправка на баг 0: новая работа, ничего работающего не ломается.
- Рычаг 3: правило «вопрос инструментом» становится общим для исполнителей
  и держится стендом, каждой следующей задаче с вопросом дешевле.

## Ход работы

- Нарезка цели: 2026-08-18, виток 1 цели DK-397; цена поднята с M до L
  вместо разреза «носитель/инструмент» (расхождение 2 LLD DK-430).
- Грумминг: 2026-08-18, файл наполнен по форме из LLD DK-430, решение 3 и
  «Что меняется в строках цели».
- Разработка: 2026-08-19, инструмент ожидания, пакеты `internal/chat` и
  `internal/sessions`, структура признака, частичный отказ подхвата, страховка
  сторожка, скиллы и стенд послушания.
- regcheck: четыре прогона, по одному на набор тестов (подхват, сторожок,
  `taskctl ask`, пакеты `internal`), каждый краснеет на HEAD и проходит на
  правке.
- Прогон стенда: живой `taskctl ask` на временной доске в /tmp прошёл оба
  исхода, ответ и парковку; отказ парковки без origin показал, что «не вышло»
  врёт при переехавшей строке, и формулировка отказа переписана.
- Ревью: 2026-08-19, три замечания, два исправлены правкой кода (страховка
  обходит корни задач из записей этапов, признак снимается и при неуехавшей
  доске), третье тестом на снятие признака сигналом; «Сценарий проверки»
  переписан под стенд со своим origin.
- regcheck по правкам сторожка: краснеет на HEAD, проходит на правке. У теста
  сигнальной ветки красноты нет по устройству: он закрывает непокрытую ветку
  уже работающего кода. Тест проверен мутацией: снятие `select` по каналу
  сигналов роняет его строкой «сигнал прошёл мимо».
- Вычитка: 10 файлов, 8 правок, 0 пометок, 2026-08-19.
- Вычитка: 3 файла, 6 правок, 0 пометок, 2026-08-19.

## Ревью

- Шаги 3 и 4 «Сценария проверки» не воспроизводятся так, как в них написано, и это видно на их же стенде (git init без remote): --push у cmdMove на шаге 3 падает с «git push: exit status 128 (No configured push destination)», и это в документированном «Ожидании» не упомянуто. На шаге 4 та же ошибка идёт через park_stale (tools/devkitctl/watch.py): вместо обещанного «припаркована страховкой: ход ожидания убит, живой сессии за строкой нет» печатается «taskctl отказал с кодом 1: ...git push...». Строка при этом реально уходит в Blocked (локальный коммит cmdMove отработал до push), но os.remove(ask["path"]) стоит только в ветке успеха (returncode == 0), и файл .devkit/chat/task-<ID>.ask остаётся лежать навсегда, хотя докстрока park_stale обещает «признак снимается вместе с парковкой». Проверено дважды с чистого /tmp/ask-stand по шагам файла задачи один в один, воспроизводится стабильно оба раза.: исправлено, признак снимается и при неуехавшей доске, стенд сценария поднимает свой origin
- Страховка park_stale работает только для корней, у которых в ~/.devkit/goals лежит активная запись *.watch, а её кладёт исключительно agentctl spend --goal внутри живого цикла цели (tools/agentctl/watch.go, watchRegister). Тик devkitctl watch обходит на страховку только entries(home) из этого реестра (tools/devkitctl/watch.py, run -> park_stale вызывается лишь внутри цикла по watched-целям). Правило вызова taskctl ask в kit/agents/exec-*.md и board-task не ограничено работой внутри цели: обычная задача, взятая напрямую через board-task/board-ship без активного goal-loop, watch-записи не получает вовсе. Проверено: на временном корне с протухшим .ask и мёртвой сессией в реестре, но без записи в ~/.devkit/goals, watch.run() не трогает ни доску, ни признак и печатает только «целей под надзором нет». Пункт DoD «убитый ход не оставляет строку в In progress: сторожок паркует её при мёртвой сессии» в таком случае не выполняется, а ни файл задачи, ни README tools/devkitctl не называют это ограничение прямо.: исправлено, корни обхода берутся и из записей этапов ~/.devkit/runs, запись цели больше не нужна
- Ветка снятия признака сигналом (SIGINT/SIGTERM/SIGHUP) в runAsk, tools/taskctl/ask.go («ожидание прервано сигналом, признак снят, задача не припаркована») не покрыта ни одним тестом: тестовый стенд askStand.run всегда зовёт runAsk с sig=nil (tools/taskctl/ask_test.go), а поле askDeps.Sig нигде не присваивается и не читается, то есть мёртвое. LLD DK-430 решение 3 явно называет снятие сигналом одним из трёх способов, которыми признак не переживает смерть хода, но тестами закрыты только обычный выход и таймаут.: исправлено, тест на снятие признака сигналом, мёртвый параметр liveDeps убран

## Сценарий проверки

Шаги гоняются в дереве задачи `~/projects/devkit-dk-432`, доска и прод не
трогаются: парковка проверяется на временной доске стенда, а не на живой.

1. Тесты и краснота:

   ```sh
   cd ~/projects/devkit-dk-432
   ( cd internal && GOWORK=off go test ./chat/... ./sessions/... )
   ( cd tools/taskctl && GOWORK=off go test ./... )
   ( cd tools/devkitctl && python3 watch_test.py )
   ( cd hooks && python3 chat_in_session_test.py )
   regcheck --tests tools/taskctl/ask_test.go -- \
     sh -c 'cd tools/taskctl && GOWORK=off go test ./... -run Ask'
   ```

   Ожидание: все прогоны `ok`, regcheck печатает «тест краснеет на HEAD и
   проходит на текущем коде».

2. Стенд с доской и своим origin. Доска коммитится и пушится парковкой, как
   любая правка доски, поэтому у стенда обязан быть свой origin: без него
   `git push` отбивается, а шаги 3 и 4 идут другой веткой вывода.

   ```sh
   cd /tmp && rm -rf ask-stand ask-origin && git init -q --bare ask-origin
   mkdir ask-stand && cd ask-stand && git init -q -b main .
   git remote add origin /tmp/ask-origin
   ( cd ~/projects/devkit-dk-432/tools/taskctl && GOWORK=off go build -o /tmp/ask-stand/taskctl . )
   ./taskctl init --prefix OB --name стенд
   git add -A && git commit -q -m "chore: стенд" && git push -q -u origin main
   ./taskctl add --id OB-001 --title "Вопрос человеку" --type task \
     --rank "25+2+1+0+1" --accept agent
   ./taskctl file OB-001 && git add -A && git commit -q -m "docs(tasks): OB-001 заведена"
   ./taskctl move OB-001 in-progress
   ```

   Ожидание: `taskctl show OB-001` видит строку в In progress.

3. Живое ожидание с ответом, там же:

   ```sh
   mkdir -p .devkit/chat
   ( sleep 3 && printf '%s\n' "2026-08-19 12:00, из дашборда: бери первый вариант" \
     > .devkit/chat/task-OB-001.in ) &
   ./taskctl ask OB-001 --question "первый вариант или второй" --wait 30
   ```

   Ожидание: команда печатает «вопрос человеку», затем «ответ человека через
   ...: бери первый вариант» и «ожидание снято, заход продолжается»; файла
   `.devkit/chat/task-OB-001.ask` после выхода нет.

4. Парковка вопросом без ожидания, там же:

   ```sh
   ./taskctl ask OB-001 --question "нужен доступ к роутеру" --wait 0
   ./taskctl show OB-001
   ```

   Ожидание: команда печатает «in-progress -> blocked, ... коммит <sha>,
   запушено» и «задача припаркована вопросом: заход кончается рубежом»; строка
   стоит в Blocked с причиной «[блок: вопрос: нужен доступ к роутеру]», а
   признака ожидания при `--wait 0` не заводилось (`ls .devkit/chat` его не
   показывает).

5. Страховка брошенного ожидания, на том же стенде:

   ```sh
   ./taskctl move OB-001 in-progress
   printf '2026-01-01T00:00:00\nсессия dead-1\nзадача OB-001\n{"questions": [{"text": "брошенный вопрос"}]}\n' \
     > .devkit/chat/task-OB-001.ask
   python3 -c "import sys; sys.path.insert(0, '$HOME/projects/devkit-dk-432/tools/devkitctl'); \
     import watch; from datetime import datetime; \
     print('\n'.join(watch.park_stale('/private/tmp/ask-stand', datetime.now(), \
       taskctl='/private/tmp/ask-stand/taskctl')))"
   ./taskctl show OB-001 && ls .devkit/chat
   ```

   Ожидание: тик печатает «задача OB-001 в /private/tmp/ask-stand припаркована
   страховкой: ход ожидания убит, живой сессии за строкой нет», строка стоит в
   Blocked с причиной «вопрос: брошенный вопрос», файла `task-OB-001.ask` в
   каталоге больше нет.

6. Страховка доходит до задачи без цели, целым прогоном тика:

   ```sh
   cd ~/projects/devkit-dk-432/tools/devkitctl && python3 watch_test.py \
     RunRootsTest -v
   ```

   Ожидание: `ok` по обоим тестам. Запись этапа
   `~/.devkit/runs/<ID>-<slug>.run`, которую кладёт сам вопрос, приносит
   страховке корень, поэтому запись цели для парковки больше не нужна, а
   запись снесённого проекта пропускается.

7. Частичный отказ подхвата:

   ```sh
   cd ~/projects/devkit-dk-432/hooks && python3 chat_in_session_test.py \
     ChatDeliveryTest.test_ask_flag_holds_only_what_the_waiter_takes -v
   ```

   Ожидание: `ok`, то есть при живом признаке реплика чужой сессии доезжает
   ходом, а безадресная и адресованная ждущему остаются во входе.

8. Стенд послушания видит правило:

   ```sh
   cd ~/projects/devkit-dk-432/tools/obeycheck && GOWORK=off go run . --list
   ```

   Ожидание: в списке стоит `12-ask-human спросить человека посреди работы`;
   боевой прогон стенда квоты стоит отдельно и в этот сценарий не входит.
