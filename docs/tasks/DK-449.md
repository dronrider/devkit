# DK-449: prompt-test для формы DK-444: сценарии черновика и файла задачи на стенде

## Что происходит

DK-444 завела форму черновика (заголовок до 72 символов, подразделы SCQA),
скилл board-draft и страницу TASKFORM.md, и проверена она тестами кода и
чужим контекстом на доке. Прогона, который показал бы, что агент с этими
текстами пишет черновик и файл задачи иначе, чем без них, не было, и рост
контекста на сессию не замерен. Замечено пользователем 2026-08-18.

## Чего хотим

Первое применение скилла prompt-test (DK-448) к форме. Два сценария
`obeycheck`: «запиши черновик по находке» (на входе поломка абзацем; проверка:
первая строка не длиннее 72 и без точки, «Ситуация» и «Осложнение» непусты,
`taskctl lint` молчит, судья на «заголовок называет исход, а не занятие») и
«оформи черновик в файл задачи» (проверка: `add --id` прошёл, `lint` зелёный,
«Что происходит» не повторяет H1, судья на «в «Чего хотим» нет технического
решения»). Раскладки: с board-draft и TASKFORM.md, без них (README taskctl до
DK-444). Замер `devkitctl doctor` до и после DK-444 и объём чтения TASKFORM.md
субагентами по журналам (кусками или целиком). Итог в разделе «Проверка»
DK-449 и строкой в файле DK-444.

## Границы

Правка самой формы по итогам прогона идёт отдельной строкой, здесь только
замер. Судья калибруется глазами на нескольких ответах до полного круга.

## DoD

Два сценария лежат в `tools/obeycheck/testdata`, прогон на двух раскладках с
`-k 3` на ярусе base записан в «Проверку» с числами по клеткам и вердиктом
(текст полезен, бесполезен или правило не держится на обеих раскладках);
дельта резидента и объём чтения TASKFORM.md записаны там же.

## Ранг

`25+5+1+0+2 = 33`, P2.

- Серьёзность 25: форма выкачена и работает, вопрос в доказательстве пользы.
- Ценность 5: числа решат, сокращать ли страницу и скилл.
- Неопределённость 1: процедура придёт из DK-448.
- Поправка на баг 0.
- Рычаг 2: одна форма, два сценария.

Цена S: два сценария и прогон.

## Ход работы

- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 15%, снимок 36м назад, сдвига нет), 2026-09-18 22:10-22:10.
- Исключение: тесты (правка бескодовая, задача-замер, сценарии стенда 53 и 54).
- Вычитка: 2 файла, 12 правок, 0 пометок, 2026-09-18.
- Сторож прозы: docs/tasks/DK-449.md, средняя длина предложения 40,4 при пороге 23, оставлено: строку пишет taskctl review add, вручную не переписывается.
- Сторож прозы: docs/tasks/DK-449.md, предложений длиннее 25 слов 80,0 при пороге 30, оставлено: строку пишет taskctl review add, вручную не переписывается.
- Сторож прозы: docs/tasks/DK-449.md, довод в той же фразе 60,0 при пороге 48, оставлено: строку пишет taskctl review add, вручную не переписывается.
- Сторож прозы: docs/tasks/DK-449.md, двоеточие в середине фразы 14,9 при пороге 11, оставлено: строку пишет taskctl review add, вручную не переписывается.
- Сторож прозы: docs/tasks/DK-449.md, абзац кончается обобщением 100,0 при пороге 6, оставлено: строку пишет taskctl review add, вручную не переписывается.
- Сторож прозы: docs/tasks/DK-449.md, средняя длина предложения 35,2 при пороге 23 (второй круг), оставлено: строку пишет taskctl review add, вручную не переписывается.
- Сторож прозы: docs/tasks/DK-449.md, предложений длиннее 25 слов 50,0 при пороге 30 (второй круг), оставлено: строку пишет taskctl review add, вручную не переписывается.
- Сторож прозы: docs/tasks/DK-449.md, довод в той же фразе 75,0 при пороге 48 (второй круг), оставлено: строку пишет taskctl review add, вручную не переписывается.
- Сторож прозы: docs/tasks/DK-449.md, двоеточие в середине фразы 21,3 при пороге 19 (второй круг), оставлено: строку пишет taskctl review add, вручную не переписывается.
- Сторож прозы: docs/tasks/DK-449.md, хвост «..., а не Y» 7,1 при пороге 5 (второй круг), оставлено: строку пишет taskctl review add, вручную не переписывается.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 18%, снимок 44м назад, сдвига нет), 2026-09-18 23:49-00:33.

## Ревью

Уровень 2 до c663fc0: неопределённость 1, задача-замер без критичного пути, DoD прогнан руками

- [возврат: правила, 2026-09-19] блокирует: в «Проверке» блок «разбор судьи» несёт цитаты судьи, переписанные руками: пять длинных тире заменены на двоеточие и запятую, чтобы пройти хук символов. Это правка машинного вывода obeycheck --task и подмена тире двоеточием разом, а RULES.core («Символы») прямо запрещает подменять убранное тире двоеточием или запятой, требуя перестроить предложение. Исправить: либо пересказать смысл цитат своей прозой вне консольного блока, не трогая сам блок, либо завести отдельной строкой доски задачу на нормализацию символов в цитатах судьи внутри obeycheck перед записью в файл, а до её решения оставить блок как есть без ручной правки: исправлено, Цитаты судьи из блока сняты целиком, смысл пересказан прозой вне блока (исход/занятие для 53, исход/способ для 54), отметка «- Стенд:» и таблица счёта не тронуты; нормализация символов в obeycheck перед записью уйдёт отдельной строкой доски
- [возврат: правила, 2026-09-19] блокирует: в «Ход работы» нет пометки «- Исключение: тесты (причина)». Дифф ветки против main трогает не только docs/ (значит бескодовой ветка не считается), а из некодовых файлов только два новых tools/obeycheck/scenarios/*.md, ни один из них не подпадает под isTestFile в tools/shipctl/advice.go (не *_test.go/py/sh, не test_*). testsGate откажет shipctl merge по этой ветке с сообщением про отсутствие тестовых файлов. Добавить в docs/tasks/DK-449.md, раздел «Ход работы», строку «- Исключение: тесты (сценарии сами и есть проверка, отдельного теста к бескодовой мере нет)»: исправлено, Строка «- Исключение: тесты (правка бескодовая, задача-замер, сценарии стенда 53 и 54)» добавлена в «Ход работы»
- [возврат: правила, 2026-09-19] блокирует: пересказ «На сценарии 53...» (docs/tasks/DK-449.md) утверждает «Единственная красная клетка сценария упала раньше судьи, на пустом разделе «Осложнение»», а по таблице клеток 53-draft-outcome-title красных клеток две, не одна: without даёт 1/3 зелёных, то есть 2 красных из 3 повторов, а «первый красный прогон каждого угла» по коду table.go печатает только первый красный на угол и обрывается, не называя второй. В «разбор судьи» до правки было 4 цитаты для 53 (with 1-3, without только повтор 2), доля judged-попыток без повтора 3 «без» подтверждает: повтор 3 не дошёл до судьи, то есть тоже красный по проверке, а не по судье. Пересказ округляет две красные клетки до одной и тем самым занижает число сбоев в «without». Поправить: назвать обе красные клетки «without» (или явно сказать, что причина второй неизвестна, раз её текст не сохранился) вместо «единственная»: исправлено, пересказ называет обе красные клетки раскладки без текстов, причина второй в записи не сохранилась
- Вердикт: без замечаний. второй круг: цитаты сняты целиком (не посимвольно), стенд-отметка и таблица не тронуты, исключение тесты распознаётся, пятые ворота задачу не спросят (agentDiff не находит агентских файлов); третий круг: пересказ называет обе красные клетки 53 и верно говорит, что до судьи не дошла ни одна, символьный хук чист

## Сценарий проверки

Вид приёмки agent.

```sh
obeycheck --list | grep -q "^53-draft-outcome-title "
obeycheck --list | grep -q "^54-draft-groom-no-solution "
grep -q "^- Стенд" docs/tasks/DK-449.md
grep -q "DK-449" docs/tasks/archive/2026/DK-444.md
```

## Выкат

- 2026-09-19 слито: 0086cf7d, ccb523da, d59fe38d, f59e63d1, 4293b321, afd1b482, 854cc393, 75eb2d39, a85d3fbc, bc56380f, c6211ef2

## Проверка

- Стенд не зачтён: 2026-09-18 23:31, дерево 8893b87e, предмет 1fc7e6e0, база пусто, ярус base, k 3, сценарии 53-draft-outcome-title, 54-draft-groom-no-solution, ворота закрыты.

```console
$ obeycheck --tier base --task DK-449 --only 53-draft-outcome-title,54-draft-groom-no-solution -k 3 --base пусто --keep /Users/elenavnukova/rules-dk449-with /Users/elenavnukova/rules-dk449-without
сценарий                                   rules-dk449-with  rules-dk449-without  вердикт
записать неразобранную находку черновиком               3/3                  1/3  не хуже
оформить черновик в файл задачи                         2/3                  0/3  не хуже

итог: сценариев 2, не зачтено 2, база пусто
  53-draft-outcome-title: не хуже, разница незначима, а с базой «пусто» зачтена только польза
  54-draft-groom-no-solution: не хуже, разница незначима, а с базой «пусто» зачтена только польза

первый красный прогон каждого угла:
  53-draft-outcome-title / rules-dk449-without / повтор 1: проверка: exit status 1 («Осложнение» пусто)
  54-draft-groom-no-solution / rules-dk449-with / повтор 1: судья: нет
  54-draft-groom-no-solution / rules-dk449-without / повтор 1: судья: нет
```

Дословных цитат судьи здесь нет. Утилита пишет ответ судьи в файл дословно, а
типографские символы модели рубеж символов на коммите не пропускает.
Нормализация символов в цитатах судьи внутри obeycheck перед записью уйдёт
отдельной строкой доски, её заведёт диспетчер.

На сценарии 53 судья читал заголовок черновика. В каждой зелёной клетке
заголовок называл исход находки, что именно не так с total. Занятия вроде
«разобраться» или «посмотреть» в заголовке не было. Красных клеток у
сценария две, обе в раскладке без текстов, и до судьи не дошла ни одна.
Первая упала на пустом разделе «Осложнение». Причина второй в записи не
сохранилась, потому что утилита печатает только первый красный прогон
каждого угла.

На сценарии 54 судья читал раздел «Чего хотим» файла задачи. В зелёных
клетках раздел называл только внешний исход. Время монитора переставало
расти на неизменном списке, а результат оставался точным. В красных клетках
раздел называл сам способ вместо исхода. Назывались кэш суммы, более редкие
вызовы total, пересчёт по изменению списка вместо каждого тика.

Калибровка судьи прошла до прогона на обоих сценариях. Haiku сошёлся с
разметкой «да»/«нет» без единого расхождения (README стенда, раздел
«Судья»). До этого нашлись две неточности в калибровочных примерах.
Пример 53 путал заголовок черновика с первой строкой «Ситуации». Критерий
уточнён словами «строка «# OB-003: ...»». В примере 54 названо техническое
решение («не тратит время на пересчёт»). Его заменили на исход без намёка на
способ.

Вердикт обеих строк «не хуже». Раскладка с board-draft и TASKFORM.md берёт
верх по счёту клеток на обеих строках. Заголовок черновика даёт 3/3 против
1/3, перенос гипотезы в «Чего хотим» даёт 2/3 против 0/3. При k 3 точный тест
Фишера не признаёт эту разницу значимой. С базой «пусто» зачтена только
«польза», и вердикт остаётся «не зачтено». Числа показывают «не хуже», а не
«полезен». Перевес есть на обеих строках и в одну сторону, но выборка для
строгого вывода мала. Больше повторов не будет, полный круг по постановке
один.

Замер контекста, `devkitctl doctor` на коммитах 9b5b5e28 (до DK-444) и
89c07d7b (после), каждый своим деревом:

```console
до:    листинг скиллов 4135 симв/1688 ток, листинг агентов 1846/753, итого 14269/5824 ток
после: листинг скиллов 4483/1830 ток, листинг агентов 1846/753, итого 14617/5966 ток
```

Резидент вырос на 142 токена, это 2,4 процента. Весь прирост лежит в строке
«листинг скиллов». Это frontmatter нового `board-draft`, единственное, что
едет в контекст любой сессии независимо от вызова. `TASKFORM.md` резидент не
меняет вовсе. У него нет своей строки в раскладке, файл лежит в проекте и
читается по ссылке, как задумано.

Чтение `TASKFORM.md` разобрано по журналам сессий на раскладке «с текстом».
Из шести сессий самого прогона файл открыли две, одна целиком, а вторая
куском в 130 строк с отступом 40. В четырёх агент обошёлся текстом скилла и
подсказкой утилиты. По более широкой выборке разведочных прогонов (десять
сессий «с текстом» считая холостые заходы на других ярусах) файл открыт в
трёх. Только один раз это было чтение без смещения, на длинном заходе через
board-groom.

- Обкатка: 2026-09-19 00:32, свежее дерево 3031783d572a, сценарий 08ead708, временный HOME, утилит дерева 11, шагов 4, все зелёные.

Шаг 1 (зелёный):

```console
$ obeycheck --list | grep -q "^53-draft-outcome-title "

```

Шаг 2 (зелёный):

```console
$ obeycheck --list | grep -q "^54-draft-groom-no-solution "

```

Шаг 3 (зелёный):

```console
$ grep -q "^- Стенд" docs/tasks/DK-449.md

```

Шаг 4 (зелёный):

```console
$ grep -q "DK-449" docs/tasks/archive/2026/DK-444.md

```
