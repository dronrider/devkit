# DK-449: prompt-test для формы DK-444: сценарии черновика и файла задачи на стенде

## Что происходит

DK-444 завела форму черновика (заголовок до 72 символов, подразделы SCQA),
скилл board-draft и страницу TASKFORM.md, и проверена она тестами кода и
чужим контекстом на доке. Прогона, который показал бы, что агент с этими
текстами пишет черновик и файл задачи иначе, чем без них, не было, и рост
контекста на сессию не замерен. Замечено пользователем 2026-08-18.

## Чего хотим

Первое применение скилла prompt-test (DK-448) к форме. Два сценария
`obeycheck`: «запиши черновик по находке» (на входе поломка абзацем; проверка:
первая строка не длиннее 72 и без точки, «Ситуация» и «Осложнение» непусты,
`taskctl lint` молчит, судья на «заголовок называет исход, а не занятие») и
«оформи черновик в файл задачи» (проверка: `add --id` прошёл, `lint` зелёный,
«Что происходит» не повторяет H1, судья на «в «Чего хотим» нет технического
решения»). Раскладки: с board-draft и TASKFORM.md, без них (README taskctl до
DK-444). Замер `devkitctl doctor` до и после DK-444 и объём чтения TASKFORM.md
субагентами по журналам (кусками или целиком). Итог в разделе «Проверка»
DK-449 и строкой в файле DK-444.

## Границы

Правка самой формы по итогам прогона идёт отдельной строкой, здесь только
замер. Судья калибруется глазами на нескольких ответах до полного круга.

## DoD

Два сценария лежат в `tools/obeycheck/testdata`, прогон на двух раскладках с
`-k 3` на ярусе base записан в «Проверку» с числами по клеткам и вердиктом
(текст полезен, бесполезен или правило не держится на обеих раскладках);
дельта резидента и объём чтения TASKFORM.md записаны там же.

## Ранг

`25+5+1+0+2 = 33`, P2.

- Серьёзность 25: форма выкачена и работает, вопрос в доказательстве пользы.
- Ценность 5: числа решат, сокращать ли страницу и скилл.
- Неопределённость 1: процедура придёт из DK-448.
- Поправка на баг 0.
- Рычаг 2: одна форма, два сценария.

Цена S: два сценария и прогон.

## Ход работы

- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 15%, снимок 36м назад, сдвига нет), 2026-09-18 22:10-22:10.
- Вычитка: 2 файла, 12 правок, 0 пометок, 2026-09-18.

## Сценарий проверки

Вид приёмки agent.

```sh
obeycheck --list | grep -q "^53-draft-outcome-title "
obeycheck --list | grep -q "^54-draft-groom-no-solution "
grep -q "^- Стенд" docs/tasks/DK-449.md
grep -q "DK-449" docs/tasks/archive/2026/DK-444.md
```

## Проверка

- Стенд не зачтён: 2026-09-18 23:31, дерево 8893b87e, предмет 1fc7e6e0, база пусто, ярус base, k 3, сценарии 53-draft-outcome-title, 54-draft-groom-no-solution, ворота закрыты.

```console
$ obeycheck --tier base --task DK-449 --only 53-draft-outcome-title,54-draft-groom-no-solution -k 3 --base пусто --keep /Users/elenavnukova/rules-dk449-with /Users/elenavnukova/rules-dk449-without
сценарий                                   rules-dk449-with  rules-dk449-without  вердикт
записать неразобранную находку черновиком               3/3                  1/3  не хуже
оформить черновик в файл задачи                         2/3                  0/3  не хуже

итог: сценариев 2, не зачтено 2, база пусто
  53-draft-outcome-title: не хуже, разница незначима, а с базой «пусто» зачтена только польза
  54-draft-groom-no-solution: не хуже, разница незначима, а с базой «пусто» зачтена только польза

первый красный прогон каждого угла:
  53-draft-outcome-title / rules-dk449-without / повтор 1: проверка: exit status 1 («Осложнение» пусто)
  54-draft-groom-no-solution / rules-dk449-with / повтор 1: судья: нет
  54-draft-groom-no-solution / rules-dk449-without / повтор 1: судья: нет

разбор судьи:
  53-draft-outcome-title / rules-dk449-with / повтор 1: # OB-003: total(values) в мониторе OB-002 пересчитывает сумму зря каждый тик В заголовке черновика констатируется конкретная проблема: функция `total(values)` «пересчитывает сумму зря каждый тик». Это описание исхода находки: что конкретно не работает эффективно. Заголовок не содержит глаголов заня...
  53-draft-outcome-title / rules-dk449-with / повтор 2: Процитируемое место: **«# OB-003: монитор дашборда OB-002 пересчитывает total на каждом тике впустую»** Заголовок называет исход находки: конкретно, что произошло с `total` (пересчитывается впустую на каждом тике), а не занятие типа "разобраться", "посмотреть" или "исправить". да
  53-draft-outcome-title / rules-dk449-with / повтор 3: Цитата: «# OB-003: монитор дашборда пересчитывает total(values) вхолостую каждый тик» Заголовок называет конкретный исход находки: описывает, что происходит не так (ненужный пересчёт суммы на каждом тике), а не призывает к действию вроде «разобраться», «проверить» или «посмотреть». да
  53-draft-outcome-title / rules-dk449-without / повтор 2: **Цитата:** > # OB-003: total(values) в tool.py пересчитывает сумму на каждом тике монитора Заголовок описывает конкретный исход находки: что именно не так (функция пересчитывает сумму на каждом тике вместо кеширования). Это не занятие вроде «разобраться», «посмотреть» или «проверить», а именно опи...
  54-draft-groom-no-solution / rules-dk449-with / повтор 1: Процитирую раздел "Чего хотим": > На неизменном списке между тиками `total` не пересчитывает сумму заново, и время монитора на длинном прогоне с этим больше не растёт. **Анализ:** Первая часть фразы, "не пересчитывает сумму заново", описывает техническое решение (как именно должна работать функция...
  54-draft-groom-no-solution / rules-dk449-with / повтор 2: ## Раздел "Чего хотим": > На длинном прогоне монитора время, уходящее на total, заметно снижается, > а сумма при этом остаётся точной после каждого изменения списка. Раздел описывает внешний исход (снижение времени работы монитора и корректность результата), а не техническое решение. Техническое реш...
  54-draft-groom-no-solution / rules-dk449-with / повтор 3: Цитата: > ## Чего хотим > На длинном прогоне монитора время тика перестаёт расти вместе с длиной списка, пока сам список между тиками не меняется. Суждение: В разделе описан желаемый исход: изменение поведения системы (время тика больше не зависит от длины списка). Технические решения (кэширование,...
  54-draft-groom-no-solution / rules-dk449-without / повтор 1: Ищу раздел «Чего хотим» и проверяю, называет ли он исход, что изменится снаружи. **Цитата:** > Если `total` не гонять по всему списку на каждом тике, время монитора на длинном прогоне заметно упадёт, а сумма на выходе останется той же. **Анализ:** Раздел начинается с технического решения: «не гонять...
  54-draft-groom-no-solution / rules-dk449-without / повтор 2: **Цитата из раздела "Чего хотим":** > Пересчёт, привязанный к изменению списка, а не к каждому тику, снимет эту лишнюю работу. Это описание технического решения (кэширование или редкие вызовы), а не исход, который изменится снаружи. Исход есть в последнем предложении ("станет заметно по меньшей нагр...
  54-draft-groom-no-solution / rules-dk449-without / повтор 3: ## Цитата из раздела «Чего хотим» > Тормозит сам пересчёт: `total` каждый раз проходит весь список заново, а между тиками список не меняется, то есть работа тратится на то же самое значение. Проверяется это долей `total` в общем времени тика на длинном списке. ## Анализ В этом разделе описывается те...
```

Калибровка судьи прошла до прогона на обоих сценариях. Haiku сошёлся с
разметкой «да»/«нет» без единого расхождения (README стенда, раздел
«Судья»). До этого нашлись две неточности в калибровочных примерах.
Пример 53 путал заголовок черновика с первой строкой «Ситуации». Критерий
уточнён словами «строка «# OB-003: ...»». В примере 54 названо техническое
решение («не тратит время на пересчёт»). Его заменили на исход без намёка на
способ.

Вердикт обеих строк «не хуже». Раскладка с board-draft и TASKFORM.md берёт
верх по счёту клеток на обеих строках. Заголовок черновика даёт 3/3 против
1/3, перенос гипотезы в «Чего хотим» даёт 2/3 против 0/3. При k 3 точный тест
Фишера не признаёт эту разницу значимой. С базой «пусто» зачтена только
«польза», и вердикт остаётся «не зачтено». Числа показывают «не хуже», а не
«полезен». Перевес есть на обеих строках и в одну сторону, но выборка для
строгого вывода мала. Больше повторов не будет, полный круг по постановке
один.

Замер контекста, `devkitctl doctor` на коммитах 9b5b5e28 (до DK-444) и
89c07d7b (после), каждый своим деревом:

```console
до:    листинг скиллов 4135 симв/1688 ток, листинг агентов 1846/753, итого 14269/5824 ток
после: листинг скиллов 4483/1830 ток, листинг агентов 1846/753, итого 14617/5966 ток
```

Резидент вырос на 142 токена, это 2,4 процента. Весь прирост лежит в строке
«листинг скиллов». Это frontmatter нового `board-draft`, единственное, что
едет в контекст любой сессии независимо от вызова. `TASKFORM.md` резидент не
меняет вовсе. У него нет своей строки в раскладке, файл лежит в проекте и
читается по ссылке, как задумано.

Чтение `TASKFORM.md` разобрано по журналам сессий на раскладке «с текстом».
Из шести сессий самого прогона файл открыли две, одна целиком, а вторая
куском в 130 строк с отступом 40. В четырёх агент обошёлся текстом скилла и
подсказкой утилиты. По более широкой выборке разведочных прогонов (десять
сессий «с текстом» считая холостые заходы на других ярусах) файл открыт в
трёх. Только один раз это было чтение без смещения, на длинном заходе через
board-groom.

- Обкатка: 2026-09-18 23:47, свежее дерево 3eaa8ba4d4b6, сценарий 08ead708, временный HOME, утилит дерева 11, шагов 4, все зелёные.

Шаг 1 (зелёный):

```console
$ obeycheck --list | grep -q "^53-draft-outcome-title "

```

Шаг 2 (зелёный):

```console
$ obeycheck --list | grep -q "^54-draft-groom-no-solution "

```

Шаг 3 (зелёный):

```console
$ grep -q "^- Стенд" docs/tasks/DK-449.md

```

Шаг 4 (зелёный):

```console
$ grep -q "DK-449" docs/tasks/archive/2026/DK-444.md

```
