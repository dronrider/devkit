# DK-451: Цикл цели в чате встаёт отчётом витка: скилл не называет границу хода

## Что происходит

Замечено пользователем 2026-08-18 на цикле цели DK-397: диспетчер закрыл две
задачи, написал отчёт и отдал ход, хотя работа была готова, а стоп-маркера не
случилось. Пользователь спросил, почему цикл требует его участия, и разбор
нашёл дефект в тексте самого скилла, а не в решении диспетчера.

Ход агента в чате кончается текстом, и любой абзац это передача управления
человеку: обратно ход приходит только внешним событием (реплика человека,
уведомление о субагенте, конец фоновой команды). Пока шла цепочка субагентов,
уведомления возвращали ход сами, и цикл ехал. Кончились субагенты, кончился и
цикл.

Раздел «Цикл в чате» скилла `goal-loop` при этом требует двух несовместимых
вещей сразу. Сначала «сессия остаётся диспетчером цикла до стоп-маркера и ведёт
витки сама», а маркер `continue` описан как «под оболочкой его поднимает её
цикл, в чате сама сессия». Следом идёт «пользователю после витка идёт отчёт
парой строк», и это ровно то действие, которое цикл останавливает. Написанное
конкретнее выигрывает, поэтому диспетчер отчитывается и встаёт.

Усиливают три вещи. Граница витка в чате нигде не определена, и диспетчер берёт
за неё естественный шов вроде конца пачки. Для headless-режима механика есть,
она написана в `goal-run.py`, а для чата нет ничего, кроме фразы «ведёт сама»,
и соседний запрет уводить работу в фон читается как запрет на единственный
доступный способ вернуть себе ход. Наконец, в харнесе отдельно есть напоминание
отчитываться о законченной работе, а в тексте скилла этому ничего не
противопоставлено.

## Чего хотим

В разделе «Цикл в чате» граница хода названа прямо: отчёт витка не завершает
ход, после него диспетчер тем же ходом гонит гейт следующего витка и берёт
работу, а ход отдаётся человеку только на стоп-маркере или на вопросе, без
которого работа не едет. Признак остановки назван машинно: текст в чат, за
которым не идёт вызов инструмента, это стоп цикла, что бы в тексте ни стояло.
Промежуточные отчёты уходят в журнал цикла, в чат идёт сводка на стопе.

## Границы

- Оболочка `goal-run.py` и маркеры не трогаются: дефект в описании чат-режима.
- Границы автономии цикла не двигаются: в правке сказано, когда ход отдаётся
  человеку, а не что цикл вправе делать без него. Поводы стопов остаются те же.

## DoD

- В разделе «Цикл в чате» скилла `goal-loop` названы граница хода и признак
  остановки, а требование отчёта стоит рядом с продолжением, не вместо него.
- Правка доезжает до машины раскладкой доктора, а не правкой копии руками.
- Форма скиллов сходится: `python3 kit/skills/check-skills.py` завершается без
  ошибок.

## Ранг

`50+9+1+5+2 = 67`, P1.

- Серьёзность 50: конвейер встаёт на человеке там, где обещана автономия.
- Ценность 9: каждый виток цели становится автономнее, а цикл в чате перестаёт
  зависеть от того, есть ли внешнее событие.
- Неопределённость 1: предмет правки назван разбором, открыты формулировки.
- Поправка на баг 5: дефект текста, а не новая работа.
- Рычаг 2: ускоряет свой поток, чужие задачи не удешевляет.

## Ход работы

- 2026-08-18: правка раздела «Цикл в чате» скилла `goal-loop`. Дописан абзац
  про границу хода: ход диспетчера кончается текстом, а не намерением, и
  признак стопа машинный: текст в чат без следующего за ним вызова инструмента.
  Названы два случая, когда ход отдаётся человеку (стоп-маркер и вопрос, без
  которого работа не едет), и сказано, что закрытая пачка это шов между
  витками, а не конец цикла. Отдельной строкой снята двусмысленность про фон:
  в фон нельзя уводить виток, а сторож на долгий шаг это та самая механика,
  которой цикл в чате и держится.

  Правило пришлось ужать: тело скилла `goal-loop` стояло в 101 символе от
  порога в 16 000, и развёрнутый абзац вывел его за предел (находка теста
  `weigh_test.test_14_live_skills_are_under_the_limit`). Скилл несёт сухое
  правило, разбор уехал в `docs/lld/DK-079-autonomous-loop.md` разделом
  «Граница хода в чате», а соседние абзацы раздела ужаты без потери фактов.
  Совет теста «резать скилл надвое» остаётся в силе и в эту строку не входит.

  Тестом такая правка не закрывается: предмет это текст резидентного правила, а
  не поведение утилиты. Форму скиллов проверяет `kit/skills/check-skills.py`,
  раскладку до машины `devkitctl doctor`, а соблюдение правила без его текста
  в контексте мерит стенд послушания `obeycheck`, и это отдельная работа с
  живыми прогонами, в цену этой строки она не входит.

- Вычитка: 2 файла, 8 правок, 1 пометка, 2026-08-18.

- Исключение: тесты (в диффе только текст скилла и файл задачи, кода нет;
  форму скиллов проверяет kit/skills/check-skills.py, раскладку до машины
  devkitctl doctor, а соблюдение правила без его текста в контексте мерит
  стенд послушания obeycheck отдельной работой)
- Исключение: regcheck (правка бескодовая, предмет это текст резидентного
  правила в kit/skills/goal-loop/SKILL.md; краснеть нечему, поведение правила
  мерит стенд послушания obeycheck отдельной работой, форму держит
  kit/skills/check-skills.py)

## Ревью

- Перенос строк разъехался: docs/tasks/DK-451.md строка 24 после вычитки стала 120 знаков и рвёт 80-колоночную вёрстку файла посреди абзаца; там же строка 32 (81) и kit/skills/goal-loop/SKILL.md строка 287 (83) чуть за пределом. Перезавернуть абзацы по 80: исправлено, абзацы перезавёрнуты по 80 символов, длиннее остались только команды в блоках кода и строка самого замечания
- Перезавёртка в docs/tasks/DK-451.md сделана разрывом строки, а не перевёрсткой абзаца: строка 34 состоит из одного слова «ход,», строка 25 обрывается на «Для чата» при полных соседних. Перелить оба абзаца ровно по 80, как сделано в SKILL.md: исправлено, оба абзаца перелиты целиком по 80, перенос по дефису у goal-run.py снят
- Перенос по дефису внутри кода не снят, вопреки ответу на замечание 2: в docs/tasks/DK-451.md строки 24-25 код-спан разорван на «goal-» и «run.py», рендер склеит это в «goal- run.py». Имя файла держать одной строкой, перелив абзац так, чтобы «goal-run.py» начинал строку: исправлено, абзац переписан, имя goal-run.py стоит внутри строки и по дефису не рвётся
- Грамматика в docs/lld/DK-079-autonomous-loop.md, раздел «Граница хода в чате», первая строка: «по находке DK-451, поймана на живом прогоне» - причастие не согласовано с «находке», нужно «пойманной на живом прогоне цели DK-397»: исправлено, согласование поправлено: пойманной

## Сценарий проверки

1. `grep -n "текст в чат" kit/skills/goal-loop/SKILL.md`: признак стопа стоит
   в разделе «Цикл в чате» словами.
2. `python3 kit/skills/check-skills.py`: «ok: скиллы, проверено 12».
3. `python3 tools/devkitctl/devkitctl.py doctor --fix` из основного чекаута
   после слияния: копия скилла на машине обновляется, повторный `doctor`
   расхождения `goal-loop` не находит.
4. `diff ~/.claude/skills/goal-loop/SKILL.md kit/skills/goal-loop/SKILL.md`
   после раскладки: пусто.

## Выкат

- 2026-08-19 слито: d64fb01e, a7c4d7a1, b6e282c2, aa12fbf7, f37284aa, bf2f1bac, df9b5a0d, a338d9f2, 56a56389
