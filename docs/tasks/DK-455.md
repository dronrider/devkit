# DK-455: Рубеж на зовущего shipctl merge: отказ ревьюверу и сценарий стенда

## Что происходит

Ситуация. На DK-436 ревьювер (определение review-high) после чистого прохода сам позвал shipctl merge, хотя постановка захода дважды говорила «доску не трогать, слияние не звать», и то же стоит в его определении. Слияние прошло, ущерба нет: ревью было чистым, тесты и выкат отработали. Замечено 2026-08-19.

Осложнение. Запрет держится только текстом постановки и определения, машинного рубежа под ним нет: shipctl не спрашивает, кто его зовёт, и ревьювер из дерева задачи сливает ровно так же, как диспетчер. В другой раз тем же способом уедет непроверенный код, а диспетчер узнает об этом из чужого отчёта.

Вопрос. Ставить машинный рубеж (например, отказ shipctl merge из сессии с ролью ревью) или считать это делом стенда послушания и правил?

Гипотеза. Роль захода видна инструменту: agentctl pick --role review уже пишет запись этапа, и merge может её прочитать. Смежное: стенд obeycheck меряет ровно такие правила.

### Проверено грумингом 2026-08-25

Предмет живой. В `tools/shipctl` роль вызывающего не встречается ни разу, так
что merge и сегодня сливает от кого угодно. Роль захода инструменту при этом
видна: `agentctl pick --role review` открывает этап `stage.Review`
(`tools/agentctl/pick.go:469`), а живой этап читается `agentctl stage <ID>`. В
стенде послушания шестнадцать сценариев (`tools/obeycheck/scenarios`), и про
слияние среди них нет ни одного.

Дублем черновик не оказался. DK-473 и LLD DK-460 ставят ворот следа ревью, то
есть проверяют, что ревью вообще было, а не кто зовёт слияние; ревьювер со
своей же чистой записью проходит такой ворот насквозь. Соседний черновик DK-536
про другое: там ревьювер позвал `review add` не из дерева задачи.

Открытым остался конец работы, и он же развилка самого черновика: рубеж в
`shipctl merge` (отказ, когда по задаче живой этап ревью, а зовёт не
диспетчер), сценарий стенда послушания или оба сразу. Цена расходится от S у
одного стенда до M там, где правится shipctl.

## Чего хотим

Слияние зовёт диспетчер, и ревьювер, потянувшийся за merge из дерева задачи,
получает отказ с названной причиной. Запрет держится командой, а не памятью
агента о том, что стояло в постановке захода, а стенд послушания меряет его
прогоном, а не вычиткой определения.

## Границы

Качество ревью сюда не едет, им занята цель DK-183. Исполнитель, сливающий себя
без ревью, тоже не сюда: его держит ворот следа ревью из DK-473. Здесь только
запрет ревьюверу звать слияние и замер этого запрета на стенде.

## DoD

`shipctl merge` отказывает, когда слияние зовёт ревьювер, и в отказе названы
причина и тот, кому звать. Признак вызывающего выбран и выписан в файле задачи
вместе с отвергнутым: живой этап `stage.Review` по задаче, отметка захода в
окружении, реестр чатов DK-430. Тесты на отказ ревьюверу и на проход диспетчера,
правка раздела «Слияние» в `tools/shipctl/README.md` и пункта про ворота merge в
`RULES.board.md`. В стенде послушания появляется сценарий рядом с нынешними
шестнадцатью: ревьювер с чистым проходом на готовой задаче, и зелень там это
отказ команды, а не текст отчёта.

## Ранг

`25+5+3+0+2 = 35`, P2.

- Серьёзность 25: слияние уезжает мимо диспетчера, но обход дешёвый, слияние
  видно по журналу и по доске, а откатывает его `shipctl revert`. Полосу выше не
  беру: случай «код уехал без ревью вовсе» держит ворот следа ревью из DK-473, а
  здесь остаётся момент слияния.
- Ценность 5: диспетчер снова знает, когда уезжает код, и порядок в поезде и в
  очереди выката держится им, а не памятью ревьювера.
- Неопределённость 3: оба исхода выбраны пользователем, развилка осталась в
  признаке вызывающего, потому что этап привязан к задаче, а не к сессии.
- Поправка на баг 0: рубежа под запретом не было никогда, это новая работа.
- Рычаг 2: своя польза, а сценарий ложится на готовую раскладку стенда.

## Ход работы

- Из черновика DK-455 «Ревьювер слил задачу вопреки прямому запрету в постановке»: записан 2026-08-19, оформлен 2026-08-27.
