# DK-459: LLD: режим POC для работы с высокой неопределённостью результата

## Что происходит

На примере разработки дашборда я столкнулся с проблемой когда несмотря на проработку и подготовку LLD, тщательное ревью и тестирование 2 итерации не дали мне того рабочего решения которое было нужно. В итоге работа фактически была частично проделана впустую. Визуально и концептуально решение выглядело рабочим, но когда дошло дело до эксплуатации то выяснилось что в таком виде пользоваться этим невозможно.

Всё осложняется даже не временем а затратами подписки и токенов. Запускать цель или просто крупную задачу в обычном режиме можно только в том случае если итоговый результат цели или задачи окончательно и однозначно определён. В противном случае мы получаем дорогое решение которое не работает.

## Чего хотим

В противовес классическому подходу разработки, запустив режим POC для дашборда я за один день получил рабочее решение, которое уже потом.
Суть режима POC в том, что мы сразу делаем рабочее решение, не проводя полновесного тестирования, написания автотестов, проведение код ревью, документирование и прочие операции которые входят в пайплайн разработки. Это не значит что мы пропускаем эти важные этапы. Просто мы их откладываем до утверждения POC пользователем.
Разработка скилла POC может ускорить AI разработку для задач с высокой неопределённостью и сократить затраты токенов.

## Границы

Документ решает порядок работы и точки входа. Сам скилл, правки `goal-start`,
`goal-cut`, `board-ship` и `test-standard` и машинные ворота под режим едут
отдельными строками по его решениям. Замер экономии квоты на живой цели в
границы не входит.

## Развилки

- Стоит ли сразу сливать рабочий и проверенный POC или после подтверждения его работоспособности запускать цикл задач на его доводку, покрытие тестами, написание авто-тестов, документирование и всё остальное что входит в стандартный рабочий процесс?
- Как определить в задаче по какому пути идти: делать POC или идти классическим способом? Для каких типов задач применять (фронтенд, бэкенд)?
- Где применять скилл? На этапе груминга?
- Какими проверками покрывать решение чтобы не отдавать совсем непроверенное решение?

## DoD

Документ `docs/lld/DK-459-*.md` с решениями и отвергнутыми вариантами по каждому:
по какому признаку работа уходит в режим POC вместо обычного порядка и кто этот
признак называет, где режим включается (грумминг черновика, нарезка цели,
отдельная команда пользователя), что POC везёт из обычного пайплайна и что
откладывает до утверждения, какими проверками POC покрывается перед показом
пользователю, и что происходит с веткой POC после утверждения: слияние как есть
с последующими строками доводки либо переписывание набело, и какая часть
правил едет в контекст сессии POC, а какая возвращается после утверждения
(приписка DK-461). Скилл POC и урезанная раскладка правил это правки промптов,
поэтому решение про проверки и список кандидатов называют скилл `prompt-test`
(DK-448) поимённо: им проверяется сам скилл POC, а не то, что в режиме POC
сделано. Правки скиллов и утилит, которые из решений следуют, названы
кандидатами в строки и заведены отдельно, а не сделаны этим же документом.
Решения ушли в индекс `docs/map.md` перегенерацией, пользователь с ними
согласился.

## Ранг

`50+8+3+0+2 = 63`, P1.

- Серьёзность 50: конвейер отрабатывает полный порядок и приводит не туда.
  По следу цели DK-397 две итерации с LLD, ревью и тестами не дали годного в
  эксплуатации решения, работа оказалась частично проделанной впустую, а обход
  (вписать нестандартный порядок в постановку руками, как в разделе «Требования
  к разговору после отбитой приёмки» файла DK-397) применяет только тот, кто уже
  обжёгся на этой же цели.
- Ценность 8: работа с неопределённым видом результата перестаёт сжигать бюджет
  цели на негодное решение, и каждая следующая такая цель идёт дешевле по квоте.
- Неопределённость 3: форма режима опробована живьём на DK-397, но открыты все
  четыре развилки из раздела «Вопрос», от критерия выбора пути до судьбы POC
  после утверждения.
- Поправка на баг 0: новое поведение, ничего не ломалось.
- Рычаг 2: дешевле идут цели с высокой неопределённостью, а не каждая следующая
  задача доски.

Цена M: документ решений пишется одной сессией, как соседняя LLD DK-441.

Вид приёмки user, барьер «согласие»: приёмка это согласие с направлением работы,
а не проверка факта, и обход у этого барьера один и тот же вырожденный.

## Что проверено на груминге

- Нехватка на месте: скилла POC в `kit/skills/` нет, режим не назван ни в одном
  из двенадцати скиллов, порядок работы задавался прозой в файле цели.
- Прецедент живой: цель DK-397 шла именно так («не нарезка на строки, а грубый
  полнофункциональный POC в отдельной ветке, без тестов, ревью и доки, и только
  после его проверки строки на доводку, тесты, макеты с ревью и
  документирование»), витки 5 и 6 её журнала описывают два круга POC.
- Дублей нет: по накопителю и по доске ближайшие соседи это DK-396 (человек до
  старта задачи), DK-161 (цель про вопросы человеку до старта) и DK-381 (скилл
  проектирования LLD), предмет у всех другой.

## Приёмка

- вид: user
- барьер «согласие»: приёмка это согласие с направлением работы, а не
  проверка факта
  - субагент-критик: не годится, находки он даёт, а согласия не даёт

## Ход работы

- Из черновика DK-459 «Скилл POC для задач с высокой неопределённостью»: записан 2026-08-20, оформлен 2026-08-20.
- 2026-08-21 разговор про очередь: сам документ стенда `prompt-test` не требует,
  приёмка у него согласием пользователя. Стенд нужен строке скилла POC, которая
  из решений выпадет, и приписке DK-461; DK-448 стоит в очереди раньше, так что
  зависимость `после` не заводим, а связь держим упоминанием в DoD.
- Разработка: субагент opus/xhigh по вердикту pick (квота: week_all 47%, week_max 36%, снимок 44м назад, сдвига нет), 2026-08-21 16:02-16:02.
- 2026-08-21 написан `docs/lld/DK-459-poc-mode.md`: шесть решений по числу
  развилок DoD, у каждого названы отвергнутые варианты. Фактура снята с прецедента
  DK-397 (витки 5-12, живая ветка `poc-chat`, перечень дыр в журнале, `.poc-bak`
  у правленых настроек) и с кода: суффиксы заголовка строки, ось глубины правил
  в `rules.py`, потолки карманов резидента в `weigh.py`, стенд `obeycheck` под
  процедурой DK-448. Гипотеза черновика DK-461 отклонена решением 6: раскладка
  правил не режется, о приостановке двух пунктов ядра сказано в самих правилах, и
  вместо урезания в состав вошла правка `RULES.core.md`. Из решений выпало семь
  кандидатов в строки доски, ни один этой задачей не сделан. Карта
  `docs/map.md` перегенерирована `devkitctl map --write`, решения DK-459.1-6 в
  индексе.
- Вычитка: 2 файла, 12 правок, 3 пометки, 2026-08-21.
- 2026-08-21 после вычитки откачены четыре правки, менявшие смысл: глаголы
  первого абзаца («ревью проверяет отклонения» вместо «ловит»), «предписывает»
  дважды в одном предложении решения 6, «в контексте текущего захода» вместо «в
  контексте одного захода» и перетёкшие после этих замен строки.
- 2026-08-21 разобраны шесть замечаний ревью, все шесть исправлены. Решение 5
  переписано на одиночное слияние с именной пометкой «- Исключение: тесты», с
  прогоном сюиты и выкатом под непроверенной очередью, а поезд отвергнут по
  своим же условиям п. 9 `RULES.board.md` и по отказу `shipctl`. В решении 6
  оговорка ядра доски переставлена на пункт про вердикт `pick`, а нарезка
  строками названа нерезидентной и отдана кандидатам. Поправлены числа: исходы
  сверки `goal-cut` (три исхода, три варианта вопроса, POC четвёртым), витки
  прецедента (восемь, 5-12). Кандидатов стало восемь, добавлены машинные ворота
  режима в `shipctl`. След вычитки и разбора ревью лёг в «Ход работы» самой
  LLD, как у соседей DK-400 и DK-430.
- 2026-08-21 второй круг ревью, замечание 7 исправлено. Абзац про выкат в
  решении 5 приведён к механике `tools/shipctl/ops.go`: очередь держит прод, а
  не `main`, чужие слияния она переводит в поездные, а не отбивает. Дописано
  следствие, которого не было: при занятой очереди одиночный `merge` сам уходит
  в поезд молча, поэтому POC ждёт освобождения очереди, а не сливается. Кандидат
  6 расширен на молчаливый перевод, отметка smoke отделена от показного прогона.
  Наблюдение про `goal-start` принято: диалог постановки вошёл в решение 2 и в
  кандидат 4, точек предложения режима стало четыре, обещание раздела «Границы»
  теперь закрыто.
- 2026-08-21 третий круг ревью, замечание 8 исправлено. У диалога `goal-start`
  три вопроса одной пачкой (цель и DoD, потолки бакетов, ярус моделей), поэтому
  вопрос про вид результата встаёт в пачке четвёртым, а не третьим. Число и
  порядковый выправлены в решении 2 и в кандидате 4.

## Ревью

- Решение 5, слияние: shipctl merge отбивает ветку POC своими же воротами. testsGate валит слияние, когда в диффе ветки против main нет тестовых файлов (tools/shipctl/advice.go:245), а решение 3 тесты откладывает; вдобавок без ключа test в .devkit/deploy.local merge отказывает вовсе, ветка сливается только зелёной. Порядок исполним лишь через пометку «- Исключение: тесты (причина)» в файле задачи, но про неё не говорят ни решение 5, ни раздел «Режим POC», ни семь кандидатов, хотя «Границы» обещают машинные ворота кандидатом.: исправлено
- Решение 5, поезд: merge --train для POC идёт против п. 9 RULES.board.md. В поезд берут цену S или M, независимые по файлам задачи и не больше 3-5, крупное едет одиночным выкатом, а признак решения 1 даёт POC ценой от L или целой целью, и строки доводки трогают ровно те же файлы. Пока POC лежит в поезде, одиночное слияние любой чужой задачи отбито (tools/shipctl/ops.go:730), а первый shipctl ship увезёт POC на прод вместе с попутчиками. Нужен разбор этого в самом решении либо кандидат на правку правила и shipctl.: исправлено
- Решение 6: оговорку про нарезку строками ставить в ядро доски негде. RULES.board.core.md это пять пунктов (доску правит taskctl, вердикт pick на исполнителя и ревьювера, ID в subject, пуш коммита доски, чувствительное), требования нарезки работы строками там нет: оно живёт в RULES.board.md и в скиллах goal-cut и board-task, а они не резидентны, и довод «громче резидент, лечится оговоркой в самом громком тексте» на них не ложится. Кандидат 2 в нынешнем виде мимо места правки.: исправлено
- Решение 2, точка goal-cut: у сверки оценки с остатком бюджета исходов три (вилка влезла целиком, остаток внутри вилки со строкой «Журнала», расхождение в полтора раза и дальше выводит на человека), и у вопроса человеку три варианта: поднять числа, срезать состав, вести цель как есть. «Четырьмя исходами» и «пятым встаёт» со скиллом не сходятся, число и порядковый править вместе.: исправлено
- Решение 6: «POC на прецеденте это шесть витков и 83 коммита» расходится со своим же «Живым материалом», где POC это витки 5-12, то есть восемь витков (шесть это только 7-12).: исправлено
- Форма: раздел «Ход работы» в конце LLD пустой. След вычитки по скиллу proofread кладётся в «Ход работы» того файла, к которому относится текст, и у соседей DK-400 и DK-430 он лежит в самой LLD; здесь строка ушла только в docs/tasks/DK-459.md, а заголовок висит без содержимого.: исправлено
- Решение 5, абзац про выкат: очередь держит прод, а не main. По п. 6 RULES.board.md и по tools/shipctl/ops.go занятая очередь чужие слияния не отбивает, а переводит их в поездной режим (отбивает она выкаты, ship), поэтому «очередь стоит и чужих слияний не пускает» неверно. Важнее второе следствие того же механизма: при занятой очереди одиночное слияние самого POC переведёт себя в поезд молча, и «слияние POC поэтому одиночное» машиной не держится. Сказать, чего POC ждёт при занятой очереди, и добавить этот путь в кандидат 6, где отказ сегодня только на явный --train.: исправлено
- Решение 2, новая точка goal-start: «третьим вопросом диалога, рядом с DoD и бюджетом» со скиллом не сходится. Диалог goal-start это три вопроса одной пачкой (что считать целью и по какому DoD, потолок по бакетам квоты, ярус моделей), так что вопрос про вид результата встаёт четвёртым. Число и порядковый править вместе, как в замечании 4.: исправлено

## Сценарий проверки

Вид user, барьер «согласие»: проверка это чтение документа человеком, у неё нет
машинного шага.

1. Пользователь читает `docs/lld/DK-459-poc-mode.md` и соглашается с
   направлением: признак и его владелец (решение 1), точка включения
   (решение 2), состав отложенного (решение 3), барьеры показа и роль стенда
   `prompt-test` (решение 4), судьба ветки после утверждения (решение 5),
   раскладка правил (решение 6, оно же ответ черновику DK-461).
2. Восемь кандидатов в строки доски из раздела «Кандидаты в строки доски»
   заводит диспетчер после согласия, а не эта задача.

## Из черновика DK-461 «В режиме POC в контекст летит только нужная часть правил» (записан 2026-08-20)

### Черновик

#### Ситуация

Резидентная раскладка правил кладёт в каждую сессию около двадцати килобайт
(RULES.core.md, RULES.board.core.md, AGENTS.md и карта проекта), и весь этот
текст рассказывает про порядок, которого в POC нет: ветка и слияние, ревью,
тесты, вычитка прозы, дока вместе с правкой.

#### Осложнение

В режиме POC этот текст не работает и просто съедает контекст и токены, а режим
затевается ровно ради экономии. Обратное тоже верно: после утверждения POC
работа идёт полным потоком, и там нужна вся раскладка целиком.

#### Гипотеза

Раскладка правил становится частью режима: POC получает урезанный набор, где
остаётся только нужное ему, а утверждение POC возвращает полную раскладку.
Решает это LLD DK-459 вместе с остальным устройством режима.
