# DK-466: Дашборд: истёкший логин чата виден состоянием и чинится перезапуском

## Что происходит

2026-08-21, POC разговора DK-397 (ветка poc-chat, экземпляр на 7131). Диалог
это сессия харнеса в tmux, и на реплику с дашборда сессия отвечает
«Login expired. Please run /login»: OAuth-токен харнеса истёк. Дашборд про
такой ответ знает только одно: titleJunk в tools/dashboard/chats.go не пускает
его в заголовок разговора.

Разговор встаёт, и с дашборда его не оживить: /login это интерактивный
OAuth-заход в терминале, кнопки или подсказки на экране чата нет. Пользователь
видит отказ без пути к починке, а смотрят POC в том числе с телефона, где
терминала машины нет вовсе.

Разбор того же дня: релогин в соседнем окне (vscode) живые сессии не оживил.
Свежий токен лёг в связку ключей, новый процесс логинится сразу, а поднятые
вчера chat-DK-397-1 и chat-DK-397-2 продолжали отвечать «Not logged in» и
«Login expired»: процесс держит старый токен в памяти и после чужого /login
сам не перечитывает. Вылечено снятием их tmux-сессий, продолжение поднял
штатный резюм дашборда.

## Чего хотим

Истёкший логин виден на дашборде явным состоянием чата, а не отказом в ленте,
и рядом лежит путь к починке: после релогина на машине дашборд сам
перезапускает застрявшую сессию резюмом, без захода в терминал.

## Развилки

Докуда вести пользователя: явная плашка «сессия разлогинена, зайдите на машину
и выполните /login» или починка с самого дашборда? Релогин в терминале кладёт
свежий токен в связку на всех, но живым сессиям он не помогает, им нужен
перезапуск процесса, а механика для него у дашборда уже есть: снять tmux и
поднять резюм. В POC-контуре ещё и фейковый HOME со связкой ключей симлинком.

## DoD

- Истёкший логин сессии виден на экране чата явным состоянием, а не строкой
  отказа в ленте; состояние гаснет само, когда сессия снова отвечает.
- После релогина на машине застрявшая сессия перезапускается резюмом с
  дашборда, без захода в терминал; с телефона путь тот же.
- Повторение симптома 2026-08-22 (chat-DK-397-1 с «Not logged in» после
  релогина) на исправленном дашборде заканчивается живым разговором.

## Ранг

`50+5+3+0+4 = 62`, P1.

- Серьёзность 50: разговор встаёт, и с дашборда его не оживить; обход
  болезненный (заход на машину, снятие tmux), с телефона его нет вовсе.
- Ценность 5: ощутимое улучшение, которого просят: симптом пришёл второй раз
  за два дня.
- Неопределённость 3: подход из гипотезы понятен, развилки в деталях: чем
  распознаётся истёкший логин (о нём знает только titleJunk в chats.go), как
  дашборд узнаёт о релогине, POC-контур с фейковым HOME и связкой.
- Поправка на баг 0: оживления с дашборда не было, это новое поведение.
- Рычаг 4: застрявшие сессии останавливают разговорный цикл цели DK-397,
  починка делает каждый следующий чат дешевле.

## Приёмка

- вид: mixed
- барьер «глаза»:

## Ход работы

- Из черновика DK-466 «Сессия чата просит /login, а с дашборда залогиниться нечем»: записан 2026-08-21, оформлен 2026-08-22.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 63%, week_max 53%, снимок 13м назад, сдвига нет), 2026-08-22 19:38-19:38.
- Уточнение: вопрос: DK-466: ветка dk-466 отрезана от main, а весь чат-контур задачи живёт только в ветке poc-chat. В main нет ни titleJunk (он в poc-chat:tools/dashboard/chats.go, там 1275+ строк против 157 в main), ни резюма чата (claude --resume и chatCmd тоже только в poc-chat), ни экрана чата с лентой в нынешнем виде. Делать DK-466 против main не на чем. Куда класть работу: (а) влить poc-chat в ветку dk-466 и писать поверх, слияние потом идёт в poc-chat, а не в main; (б) коммитить прямо в poc-chat коммитами с DK-466 в subject, ветку dk-466 бросить; (в) отложить задачу до слияния POC в main. По умолчанию беру (а). Тесты по вашему слову POC не писал вовсе, а тут постановка требует пирамиду: писать тесты на новое поведение поверх POC-кода или тоже отложить строкой доводки?, 2026-08-22 19:41-19:49.
- Снаружи: блок: вопрос: DK-466: ветка dk-466 отрезана от main, а весь чат-контур задачи живёт только в ветке poc-chat. В main..., 2026-08-22 19:49-23:57.
- Развилка «куда класть работу» снята диспетчером. Работа идёт прямо в ветке poc-chat коммитами с DK-466 в subject, ветка dk-466 брошена, тесты пишутся обычным порядком.
- Признак разлогина считает сервер по последнему ответу агента в хвосте транскрипта. Узнаёт отказ `loginGone` рядом с titleJunk, дальше он оседает полем `Bye` шапки, полем `login` строки списка и пометкой `logout` у записи ленты. Панель английских слов не разбирает, она поднимает плашку по признаку и гасит её следующим настоящим ответом.
- Починка сделана кнопкой, а не самолечением. Перезапуск сам по себе не лечит, пока на машине не сделан /login, и поднятый заново клиент был бы разлогинен так же. Шаги те же, что у клина, снять сессию (`/stop` с `drop`) и поднять резюмом (`/say`). У разговора чужого окна кнопки нет вовсе, вместо неё стоят слова о том, почему её нет.
- Разработка: субагент opus/high, 2026-08-28 23:20 - 2026-08-29 00:40.
- В том же дереве работает соседний исполнитель (goal.go, sessions_test.go, свой порядок плана в sessions.go). В коммит DK-466 взяты только свои куски диффа, чужие оставлены в рабочем дереве.

## Сценарий проверки

1. `cd tools/dashboard && GOWORK=off go test . -run 'Login' -count=1`. Узнавание отказа входа, состояние строки списка и его угасание, пометка записи ленты, драйвер стенда панели.
2. `node testdata/poc_login.mjs static/app.js` из tools/dashboard. Плашка, слова починки, порядок вызовов кнопки, разговор чужого окна.
3. Живьём на экземпляре 7131. Транскрипт, чей последний ответ агента это «Login expired. Please run /login», даёт строке списка состояние «сессия разлогинена» и пометку `logout` у записи ленты. Дописанный настоящий ответ агента состояние гасит.

## Проверка

Шаг 1, тесты сервера и драйвер стенда:

```
--- PASS: TestLoginGoneWords (0.00s)
--- PASS: TestChatEntryLoginGone (1.04s)
--- PASS: TestFeedMarksLoginReply (0.86s)
--- PASS: TestStaticLoginGonePlate (0.09s)
```

Шаг 2, стенд DOM:

```
ок: разлогин виден плашкой отдельно от ленты, слова говорят порядок починки,
кнопка снимает сессию и поднимает резюм, живой ответ гасит состояние сам
```

На прежнем коде тот же стенд падает первым же барьером («плашки разлогина
нет»), а go-тесты там не собираются вовсе, поля состояния в chatEntry ещё нет.

Шаг 3, выкат и живая проверка, 2026-08-29:

```
302  (curl http://127.0.0.1:7131/)
466aa000 login= 'сессия разлогинена' stuck= None
user      logout= None | проверка DK-466: продолжай работу
assistant logout= True | Login expired. Please run /login
```

После дописанного ответа агента:

```
466aa000 login= None
assistant logout= None | продолжаю с того места, где остановился
```

Истёкший токен живьём не воспроизвести. Своего /login у дашборда нет, а чужой
токен просрочить нечем, поэтому симптом повторён подставным ответом сессии,
тем самым, каким его пишет клиент. На экране он кончается плашкой с кнопкой.

Человек увидит над полем ввода плашку «Сессия разлогинена» со словами про
/login на машине и кнопкой «Перезапустить». Лента при этом остаётся лентой.
Кнопка снимает tmux-сессию и поднимает продолжение резюмом, а плашка гаснет
сама, как только сессия ответит по делу.
