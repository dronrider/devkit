# DK-472: Процедура записи чистого вердикта в board-ship и промтах ревьювера

## Что происходит

В разделе «Ревью» скилла `board-ship` (пп. 5-6, `SKILL.md:91-95`) сказано только
«строка на замечание, суть и исход», про вердикт без замечаний нет ничего.
Диспетчер читает процедуру и не находит, чем записать чистое ревью, поэтому
команда из DK-471 сама по себе останется невостребованной.

Строка вторая в плане [LLD DK-460](../lld/DK-460-review-gate.md), раздел «Что
меняется в строках», пункт 2.

## Чего хотим

Процедура называет запись вердикта шагом ревью, и ревьювер уходит с задачи,
оставив след при любом исходе.

## DoD

Раздел «Ревью» скилла `kit/skills/board-ship` и промты `kit/agents/review-*`
называют последним шагом ревьювера запись вердикта командой из DK-471, включая
чистый исход. Перечень подкоманд в `SKILL.md:92` дополнен той же командой.
Проверка стендом послушания, а не чтением глазами.

Строка ложится на те же файлы, что чужая DK-185 (точки вызова вычитки и формат
служебных записей, постановка в LLD DK-173). Предметы разные, поглощения нет,
но параллельно эти строки не едут: идущая второй подхватывает формулировки
первой.

## Ранг

`25+4+1+0+4 = 33`, P2.

- Серьёзность 25: команда есть, а процедура про неё молчит, шаг держится
  памятью того, кто читал LLD.
- Ценность 4: ощутимое улучшение процедуры, наружу не видно.
- Неопределённость 1: место названо строками, осталось написать.
- Поправка на баг 0: шага в процедуре не было.
- Рычаг 4: без неё ворот DK-473 приходил бы на честную работу.

## Ход работы

- Нарезана из LLD DK-460 после утверждения пользователем, 2026-08-22.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 4%, снимок 1м назад, сдвига нет), 2026-09-18 15:31-15:31.
- Вычитка: 5 файлов, 6 правок, 0 пометок, 2026-09-18.
- Перечень подкоманд в `board-ship/SKILL.md` уже нёс `clean` (коммит
  fd823dd69, переписка ревью по скиллу `review`), а промты `review-*` уже
  называли `review clean` вслед за `level` и `add` (коммиты 3f54b0b36,
  75e4d23a4, «review v2»). Строке осталось явно назвать запись итога
  последним шагом ревьювера перед уходом с задачи, для чистого исхода
  отдельно: правка легла в `board-ship/SKILL.md` («Ревью», пункт 3) и во все
  четыре `review-*.md`.
- Сторож прозы: docs/tasks/DK-472.md, хвост «..., а не Y» выше порога,
  оставлено: обе фразы называют настоящую вторую возможность («заводит новый
  текст с нуля», «есть ли польза против пустоты»), правило вычитки такой
  хвост не трогает.
- Отработка замечания (см. «Ревью»). Фраза «Этим ревьювер завершает работу
  над задачей» снята из всех четырёх `review-*.md`. Часть DoD про промты
  `review-*` закрыли более ранние коммиты 3f54b0b36 и 75e4d23a4 («review
  v2»). Промты уже называли `review clean` последним в перечне `level`,
  `add`, `clean` до этой задачи. Добавленная фраза этот факт не меняла.
  Прогон на базе «пусто» перед этим раундом пользы не показал (см.
  «Проверка»).
- Часть DoD про `board-ship/SKILL.md` осталась в силе. Её читает диспетчер,
  а не ревьювер-субагент. Сценарий стенда перестроен на «конец: сессия» с
  предметом только `board-ship/SKILL.md` (см. новый сценарий и «Проверка»
  ниже). Новый сценарий ставит второй круг ревью. Замечание найдено и
  закрыто `resolve`, второй круг новых замечаний не находит. В
  `board-ship/SKILL.md` (пункт 4) для этого случая добавлена фраза. Чистый
  проход после `resolve` записывает тот же `review clean`, с новым sha.
- Исключение: тесты (правка бескодовая, текст скилла проверяет сценарий стенда 51-board-ship-second-round)
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 7%, снимок 12м назад, сдвига нет), 2026-09-18 16:27-17:52.

## Ревью

Уровень 2 до 6c43c3b: второй уровень по определению ревью задачи доски; критичность обычная (правка промптов и скиллов, не критичный путь по конфигу), неопределённость строки низкая

- [возврат: правила, 2026-09-18] блокирует: стенд обкатки (docs/tasks/DK-472.md, «Проверка») дал «зелёный на обеих» на базе «старый». README стенда (tools/obeycheck/README.md, «Правило решения») называет это прямо. «Новый текст, без которого агент ведёт себя так же, в резидент не едет». Объяснение находится без гадания. Review-medium.md, предмет сценария, уже до этой правки нёс «чистый вердикт через review clean с тем, что проверено». Команда была названа раньше. Правка лишь дописала одну поясняющую фразу. Board-ship/SKILL.md, второй предмет сценария, читает диспетчер. Ревьювер-субагент его не читает. Сценарий с «конец: субагент» и --agent-def review-medium к этому файлу не обращается. В review-medium.md нет ссылки на скилл board-ship, только на скилл review. Прогон технически не мог показать пользу ни от одной из двух правок. «Зачтён» тут не то же самое, что «доказал». В «Проверку» нужно вписать решение. Либо строки в review-*.md и board-ship/SKILL.md избыточны, и их стоит снять. Либо сценарий нужно перестроить так, чтобы он нагружал обе правки. Пример: «конец: сессия» для board-ship или база без исходного упоминания review clean для review-medium.md. Выбранный вариант и причину нужно записать: исправлено, фраза в review-*.md снята (уже была закрыта коммитами 3f54b0b36, 75e4d23a4), board-ship правка осталась и доказывается новым сценарием на «конец: сессия»
- Вердикт: без замечаний. круг 2 до 72e7fdf: замечание 1 сверено с диффом (review-*.md откатились к main побайтово, board-ship/SKILL.md п.4 явно называет чистый проход второго круга). Новый сценарий 51-board-ship-second-round (конец: сессия, предмет board-ship/SKILL.md) подтверждён отдельным --list, старый субагентский сценарий удалён. Итог стенда на базе старый, mini, k=3: 1/3 против 1/3, не хуже. Зачтено формально по правилам стенда (README «Правило решения»: не хуже при базе старый это зачёт). Разница не в тексте: обе раскладки в 2 из 3 повторов писали вердикт руками вместо review clean. Тело скилла не резидентно, цена только по вызову. DoD требовал названного шага в документации, а не доказанной пользы для текста, переписывающего работающую процедуру. Этот случай прямо описан в разделе Раскладки скилла prompt-test как есть, а не пусто. Решение оставить правку принято

## Сценарий проверки

1. Текст про запись итога на месте в `board-ship/SKILL.md`, пункты 3 и 4:

```sh
grep -c "Последним шагом ревьювер" kit/skills/board-ship/SKILL.md
grep -c "Чистый проход второго круга" kit/skills/board-ship/SKILL.md
```

Ожидание: обе команды печатают `1`. Промты `review-*.md` из проверки сняты.
Фраза, которую они несли, не доказала пользы стендом (см. «Ход работы» и
«Ревью») и убрана. Проверять там больше нечего.

2. Сценарий стенда `51-board-ship-second-round` разбирается без прогона
   моделей:

```sh
obeycheck --list --only 51-board-ship-second-round
```

Ожидание: одна строка с ID сценария, заголовком и предметом
(`kit/skills/board-ship/SKILL.md «Ревью» (конец: сессия)`), без спавна
сессий.

3. `taskctl review clean` на синтетической доске во временном каталоге
   оставляет в файле задачи строку вердикта, реальный `docs/tasks/DK-472.md`
   не трогается:

```sh
S=$(mktemp -d) && mkdir -p "$S/docs/tasks" && cp docs/TASKS.md docs/TASKS-archive.md "$S/docs/" && taskctl -C "$S" review clean DK-472 "проверка процедуры чистого вердикта" && grep -n "Вердикт: без замечаний" "$S/docs/tasks/DK-472.md"
```

Ожидание: `taskctl` печатает «вердикт без замечаний записан», `grep` находит
строку `- Вердикт: без замечаний. проверка процедуры чистого вердикта` в
файле песочницы.

## Выкат

- 2026-09-18 слито: 4ad41b49, f860a9ae, c248d0bb, 3d12d20b, 66534bde, 9d1124ee, b8781f6a, 1fca0736, 58a91547

## Проверка

Правка переписывает работавшую процедуру, а не заводит новый текст с нуля.
База прогона поэтому «старый», вопрос «не стало ли хуже» из раздела
«Раскладки» скилла `prompt-test`, а не «есть ли польза против пустоты».
Первый прогон пустили с базой «пусто» по ошибке, он не зачтён. Его запись
снята, вторая осталась.

Первый сценарий (`51-review-clean-verdict`, «конец: субагент», предмет
review-medium.md плюс board-ship/SKILL.md) удалён вместе со своей записью.
Он проверял ревьювера-субагента, который board-ship/SKILL.md не читает.
После снятия лишней фразы из `review-*.md` проверять там больше нечего.
Дальше только новый сценарий, `51-board-ship-second-round`, «конец: сессия»,
предмет только `board-ship/SKILL.md`.

- Стенд: 2026-09-18 17:23, дерево bbcd56e7, предмет 248a1469, база старый, ярус mini, k 3, сценарии 51-board-ship-second-round, зачтён.

```console
$ /tmp/obeycheck-dk472 --scenarios tools/obeycheck/scenarios --only 51-board-ship-second-round -k 3 --base старый --devkit /Users/elenavnukova/projects/devkit-dk-472 --timeout 5m --task DK-472 /tmp/dk472b-with /tmp/dk472b-without
сценарий                                                     dk472b-with  dk472b-without  вердикт
диспетчер доводит второй круг ревью до записанного вердикта          1/3             1/3  не хуже

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  51-board-ship-second-round / dk472b-with / повтор 1: проверка: exit status 1 (чистого вердикта в файле нет)
  51-board-ship-second-round / dk472b-without / повтор 1: проверка: exit status 1 (чистого вердикта в файле нет)
```

Честно про разницу: with и without дали одинаковый счёт, 1 из 3. Вердикт
«не хуже», не «польза». Правка board-ship/SKILL.md пользы в этом прогоне не
доказала: обе раскладки на ярусе mini в двух прогонах из трёх писали в файл
задачи произвольную строку руками («Уровень 2: ...», «Уровень 2 после
<sha>: ...») вместо вызова `taskctl review clean`, и лишь один раз из трёх
на каждой стороне дошли до настоящей команды. Разведочный прогон на ярусе
`base` (k=1, не зачтён в счёт) прошёл зелено на обеих раскладках, то есть
при более сильной модели процедура соблюдается независимо от правки, а при
слабой не соблюдается тоже независимо от правки. Ворота стенда правку не
блокируют («не хуже» зачтено при базе «старый»), но пользы для этого DoD
она не доказала. Решение, снимать ли фразу из `board-ship/SKILL.md` вслед
за `review-*.md`, оставлено пользователю.

- Обкатка: 2026-09-18 17:51, свежее дерево 4942a1f36ceb, сценарий 1a737d7b, временный HOME, утилит дерева 11, шагов 4, все зелёные.

Шаг 1 (зелёный):

```console
$ grep -c "Последним шагом ревьювер" kit/skills/board-ship/SKILL.md
1
```

Шаг 2 (зелёный):

```console
$ grep -c "Чистый проход второго круга" kit/skills/board-ship/SKILL.md
1
```

Шаг 3 (зелёный):

```console
$ obeycheck --list --only 51-board-ship-second-round
51-board-ship-second-round диспетчер доводит второй круг ревью до записанного вердикта kit/skills/board-ship/SKILL.md «Ревью» (конец: сессия)
```

Шаг 4 (зелёный):

```console
$ S=$(mktemp -d) && mkdir -p "$S/docs/tasks" && cp docs/TASKS.md docs/TASKS-archive.md "$S/docs/" && taskctl -C "$S" review clean DK-472 "проверка процедуры чистого вердикта" && grep -n "Вердикт: без замечаний" "$S/docs/tasks/DK-472.md"
DK-472: вердикт без замечаний записан, элементов в ревью 1, файл задачи создан
15:- Вердикт: без замечаний. проверка процедуры чистого вердикта
```
