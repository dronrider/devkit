# DK-490: Цель: режим POC из решений LLD DK-459 доезжает до конвейера

## Цель

Решения LLD `docs/lld/DK-459-poc-mode.md` доведены до конвейера: режим POC
включается командой пользователя, виден пометкой на строке, предлагается в
четырёх точках, сливается и выкатывается по решению 5, а сам скилл проверен
стендом. Нарезка задач сделана постановкой цели из восьми кандидатов LLD
(кандидат 2 склеен с первым, LLD велит везти их вместе).

DoD: все строки раздела «Задачи цели» закрыты; `kit/skills/poc/SKILL.md`
существует; `devkitctl doctor` без находок по запасу карманов резидента;
`taskctl show` печатает пометку режима poc; `shipctl merge --train` у ветки
POC отказывает явно; прогон `prompt-test` по скиллу POC зелёный.

## Бюджет

бюджет: week_all <= 20
бюджет: week_max <= 20

## Задачи цели

Нарезка сделана постановкой 2026-08-23 из раздела «Кандидаты в строки доски»
LLD DK-459, строки заведены сразу.

- DK-491 (task, M, R=61). Скилл `poc` в `kit/skills/poc/` плюс оговорки в
  `RULES.core.md` и `RULES.board.core.md` (кандидаты 1 и 2 LLD).
- DK-492 (task, M, R=32). Пометка режима poc в `taskctl`: постановка и снятие
  командой, печать в `show`, видимость на дашборде (кандидат 3).
- DK-493 (task, M, R=34). Предложение режима в четырёх точках: `goal-start`,
  `board-groom`, `goal-cut`, разбор отбитой приёмки (кандидат 4). После DK-491.
- DK-494 (task, M, R=33). Порядок слияния POC в `board-ship` и развилка в
  `test-standard` (кандидат 5). После DK-491.
- DK-495 (task, M, R=32). Машинные ворота режима в `shipctl` (кандидат 6).
  После DK-492.
- DK-496 (task, M, R=33). Нарезка доводки по диффу ветки в `goal-cut`
  (кандидат 7).
- DK-497 (task, S, R=31). Прогон скилла POC стендом `prompt-test`, оба сценария
  и три раскладки (кандидат 8). После DK-448 и DK-491.

оценка: 7 задач (6 M, 1 S), week_all 6.5-19 из 20, week_max 9.5-19 из 20; остаток
бакетов на постановке 21 и 16, верх вилки по week_max за остаток вылезает,
сброс 2026-08-24T15:00.

## Журнал

## Итог

## Ход работы

- Постановка цели из кандидатов LLD DK-459, строки состава заведены сразу,
  2026-08-23.
- Вычитка: 8 файлов, 9 правок, 0 пометок, 2026-08-23.
