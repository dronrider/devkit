# DK-503: LLD: потолок захода исполнителя и передача хвоста работы

## Что происходит

Субагент-исполнитель DK-181 прожил с 23.08 21:13 до 24.08 07:52 по UTC одним
заходом: 347 обращений к модели, 211 вызовов Bash, 58 правок, средний контекст
100 тысяч токенов при максимуме 167 тысяч. В 06:39 сработало автоматическое
сжатие (167376 токенов до 14703, отброшено 152673), после чего агент пошёл
перечитывать те же файлы и через час умер по отказу 429. Задача не сделана.
Группа этой сессии съела 43.4 миллиона токенов, из них 41.5 миллиона пришлось
на субагентов, а на главную сессию только 1.9 миллиона.

Стоимость одного захода растёт быстрее числа шагов: материал, влитый на десятом
обращении из 347, оплачивается ещё 337 раз, и 88 процентов кредитов уходит на
прогонку накопленного, а не на ответы модели. Потолка у исполнителя нет ни по
обращениям, ни по контексту, поэтому длинная задача неизбежно доезжает до
сжатия, а сжатие обнуляет не расход, а только пользу от прочитанного. Отдать ту
же работу двумя заходами по своему куску дешевле, чем одним длинным.

### Проверено на грумминге

Потолка захода в механике нет нигде: `agentctl budget` через скилл board-batch
ограничивает только размер пачки (`batch: N`), а определения исполнителей
exec-* держат совет «читай узко» без механического рубежа. Ограничивать заход
и резать работу на несколько коротких сегодня нечем.

## Чего хотим

Задача, которая не влезает в один заход, доезжает несколькими короткими вместо
одного длинного, и ни один заход не доходит до автоматического сжатия контекста.

## Границы

Соседние черновики того же разбора расхода DK-181 сюда не входят: льготное
окно расписания (DK-502), сохранность наработок при обрыве харнеса (DK-504),
рубежи повторных чтений (DK-499, DK-500). Здесь только плановый потолок захода
и передача хвоста работы.

## Развилки

Чем ограничивать заход: числом обращений, накопленным контекстом или временем?
И кто режет работу дальше, когда потолок достигнут: сам исполнитель сдаёт хвост
диспетчеру или диспетчер поднимает следующего с короткой вводной.

## DoD

В docs/lld лежит принятый документ с решениями по обеим развилкам: чем меряется
потолок захода (обращения, контекст или время) и кто продолжает работу после
потолка (исполнитель сдаёт хвост или диспетчер поднимает следующего с короткой
вводной). Из документа нарезаны задачи реализации со строками на доске.

## Ранг

`25+9+3+0+4 = 41`, P2.

- Серьёзность 25: длинная задача доезжает до сжатия и умирает («задача не
  сделана», 43.4 миллиона токенов на группу сессии), но обход дешёвый и
  очевидный: резать задачи мельче на грумминге до цены S/M.
- Ценность 9: прямой вклад в экономию квоты, «88 процентов кредитов уходит на
  прогонку накопленного», из 43.4 миллиона токенов 41.5 пришлось на субагентов.
- Неопределённость 3: подход назван гипотезой (несколько коротких заходов
  вместо одного длинного), развилки в деталях: чем мерить потолок и кто режет
  хвост.
- Поправка на баг 0: потолка не было никогда, это новое поведение, а не
  регресс.
- Рычаг 4: удешевляет каждый длинный заход всякого исполнителя, правка живёт в
  механике диспетчера и субагентов целиком.

## Приёмка

- вид: user
- барьер «согласие»: предмет решений это устройство исполнителя и диспетчера
  devkit, и согласие на него даёт тот, чью работу решения меняют
  - обхода нет: субагент-критик со свежим контекстом даёт находки, но не
    согласие

## Ход работы

- Из черновика DK-503 «исполнитель растит контекст до сжатия и перечитывает всё заново»: записан 2026-08-24, оформлен 2026-08-24.
- Разработка: субагент sonnet/xhigh по вердикту pick (маппинг opus, корректор: дефицит week_max; квота: week_all 91% профицит, week_max 100% дефицит, снимок 7м назад), 2026-08-24 13:55-13:56.
- Документ [docs/lld/DK-503-exec-ceiling.md](../lld/DK-503-exec-ceiling.md) принят: Решение 1 (потолок это время с открытия этапа «разработка», не обращения и не токены), Решение 2 (исполнитель сдаёт хвост сам, диспетчер поднимает следующего повторным pick). Нарезаны задачи реализации: DK-506 (`taskctl elapsed`), DK-507 (абзац потолка и формат хвоста в exec-*.md, после DK-506).
- Правки по ревью: семь замечаний исправлено. Решение 1 переписано под то, что `move <ID> in-progress` флашит этап «разработка» раньше, чем исполнитель начинает работать (найдено ревью на живом примере самой задачи DK-503), заведена третья задача DK-511 (повторный `pick --record` перед спавном в `board-ship`/`board-batch`), DK-507 теперь зависит от неё тоже. Решение 2 и связанный нештат переписаны под то, что `taskctl batch`/`board-batch` смотрят только Backlog. `docs/map.md` перегенерирован (`devkitctl map --write`). Числа калибровки разведены: заход 639 минут, автосжатие на 566-й минуте. Дописаны «Приёмка» и «Сценарий проверки», двойной дефис заменён перестройкой фраз по всему документу.
- Правки по второму кругу ревью: три замечания исправлено. Правило DK-511 переформулировано от спавна исполнителя, а не от `shipctl start`: найдены и закрыты ещё три точки спавна без вызова (продолжение по хвосту в готовом дереве, подъём после снятой парковки `taskctl ask`, спавн исполнителя из `board-ship` раздела «Ревью»), DK-511 расширена на `board-task`. Решение 1 дополнено абзацем про побочный эффект (вторая строка «Разработка» в «Ход работы») и выигрыш (дашборд видит живой этап). Три абзаца переразбиты по ширине 76 символов.
- Правки по третьему кругу ревью: два замечания исправлено. Найдена четвёртая точка, но не спавн: делегирование через `agentctl run <ID> --workdir <дерево>` при несовпадении `via` с харнесом, у команды свой вердикт внутри (`pickVerdict` в `cmdRun`), чинится флагом `--record` на самой команде, а не вторым вызовом. DK-511 дополнена этим случаем. Шаг сценария проверки переписан без номеров строк живой доски (grep без -n, счёт через grep -c), которые успели разъехаться за один круг ревью.
- Правки по четвёртому кругу ревью: одно замечание исправлено. Машинный блок сценария пересобран заново (строка DK-511 с актуальным хвостом «и флаг у agentctl run», счёт review show вырос до 13), человеческая часть переписана под нынешний состав задачи DK-511 (три скилла плюс отдельный флаг agentctl run), объём работы для согласия больше не занижен.

## Ревью

- Решение 1 держится на записи .run, которую штатный порядок стирает до старта захода. taskctl move <ID> in-progress безусловно зовёт flushStages (tools/taskctl/ops.go:599 -> stage.Flush), а обе процедуры зовут move сразу после pick --record (board-task, шаги 2 и 4; board-batch, шаг 5). Видно на самой DK-503: коммит 0fa848ac в 13:56:39 вписал в «Ход работы» строку «Разработка: ... 2026-08-24 13:55-13:56», то есть этап длиной в минуту, это зазор pick->move, а не заход. К моменту, когда исполнитель начинает работать, открытого этапа «разработка» в записи нет, и taskctl elapsed уходит в ветку «этап не открыт, потолок не проверить» из раздела «Нештат»: рубеж не срабатывает на пути по умолчанию. Утверждение «Живого материала» («Flush уносит этапы только на смене статуса доски, посреди работы запись не трогается») верно буквально, но смена статуса случается в начале захода, а не в конце. Нужен ответ в документе: либо повторный pick --record в момент спавна называется частью процедуры (тогда это правка board-task и board-batch, то есть третья нарезанная задача, и DK-507 про неё молчит), либо метрика вешается на то, что flush не стирает: исправлено, решение 1 переписано: этап открывается не pick --record из board-task/board-batch (его тут же флашит move в in-progress), а вторым вызовом pick --record перед спавном исполнителя, для которого заведена задача DK-511 (правка board-ship и board-batch); DK-507 теперь зависит и от неё
- Решение 2 неверно описывает подбор через пачку. Сказано: «taskctl batch/board-batch уже отбирают такие строки по гейту «занятая ссылка» -- как только заход закончился, ссылка свободна, и строка снова обычный кандидат». Обе половины расходятся с кодом и доком. Первое: batch обходит только Backlog, «остальные секции в отбор не входят вовсе и в отказах не печатаются» (tools/taskctl/README.md:503), значит строка, оставленная на рубеже потолка в In progress, кандидатом не станет никогда и даже в группу отказов не попадёт. Второе: гейт ссылки это дедупликация серии среди уже отобранных, «ссылка не совпадает со ссылкой уже отобранной задачи (одинаковая непустая ссылка это признак серии)» (README.md:510), к живости захода он отношения не имеет. Отсюда же неверен нештат «Хвост, который никто не подбирает»: там сказано «строка может ждать своей очереди в бэклоге как любая другая», а строка стоит в In progress, куда пачка не смотрит. Для agentctl pick утверждение верно (board.go:13, sectPrefixes включает In progress), для board-batch нет, и это надо либо переписать, либо назвать правкой отбора отдельной строкой: исправлено, переписано: taskctl batch обходит только Backlog (README.md:503) и в In progress не заглядывает, занятая ссылка это дедупликация серии, а не признак свободного слота; нештат про хвост тоже переписан на строку в In progress, которую подбирает только именной pick, автоматическое возвращение в пачку вынесено в «Что не входит»
- Карта проекта не поехала вместе с документом. В дереве задачи grep -c DK-503 docs/map.md даёт 0, а devkitctl doctor оттуда говорит «карта правлена руками, хеш разошёлся; перегенерировать: devkitctl doctor --fix»; та же команда в основном чекауте про карту молчит, то есть находку принёс именно этот дифф. Индекс решений из docs/lld это часть карты (DK-194.5), и оба заголовка «Решение N» обязаны там оказаться, как оказались решения DK-460. Чинится devkitctl doctor --fix, docs/map.md идёт тем же коммитом: исправлено, devkitctl map -C /Users/rider/projects/devkit-dk-503 --write перегенерировал docs/map.md, строки DK-503.1 и DK-503.2 на месте (grep -n подтверждает), коммит идёт вместе с остальной правкой
- Раздел «Приёмка» не дописан: стоит «- барьер «согласие»:» без текста барьера и без перебора обходов. У «согласия» список вырожденный, но не пустой: одна строка, «Обхода нет: субагент-критик даёт находки, но не согласие» (ACCEPTANCE.md, таблица шести барьеров), и ворота acceptGate (tools/taskctl/gate.go) считают строки перебора, так что taskctl move check откажет числом. Дописать надо и причину барьера, и строку обхода, как это сделано у соседнего LLD DK-460: «барьер «согласие»: предмет решений это процедура доски и правила ревью, и согласие на них даёт тот, чью работу они меняют» плюс вложенная строка про то, что критик со свежим контекстом даёт находки, но выбор между инструментом и прозой это решение пользователя. Вид приёмки при этом остаётся user: обход у «согласия» неисполним, повышение запрещено: исправлено, дописан текст барьера согласия и единственная строка перебора обхода (субагент-критик даёт находки, но не согласие), по образцу DK-460; вид приёмки остался user
- Раздела «Сценарий проверки» в файле задачи нет вовсе, а без него checkGate не пускает ни в Check, ни в слияние (TASKFORM.md, контрактные разделы). Вид user не отменяет машинной части: у DK-460 сценарий начинается словами про то, что согласие даёт человек, а дальше идёт прогнанный блок с реальным выводом (счёт «## Решение», счёт отвергнутых, devkitctl doctor --fix и строки DK-460 в docs/map.md, hooks/check-symbols.py с rc=0). Здесь проверяемого не меньше: два решения на месте, обе развилки закрыты, карта содержит DK-503.1 и DK-503.2, строки DK-506 и DK-507 стоят на доске с рангом 38 и зависимостью, символы чистые. Написать сценарий парами «прогон, ожидание» и вложить реальный вывод, а шагом согласия оставить человека: исправлено, раздел «Сценарий проверки» написан по образцу DK-460: машинный блок с реальным выводом (счёт решений и отвергнутых, перегенерация и grep карты, три строки доски с рангом и зависимостью, taskctl lint, check-symbols rc=0) и человеческая часть с единственным спорным местом (задача DK-511)
- Мимикрия: в документе восемь мест, где длинное тире заменено двойным дефисом вместо перестроенного предложения (строки 27 в примере вывода elapsed, 109, 198, 217, 270, 274, 313, 316). В docs/lld этого приёма нет ни разу: двойной дефис по репозиторию встречается только внутри командных строк (git log -1 -- path, secretctl exec <имя> -- <команда>) и в подписях рёбер mermaid. Правило прямо говорит не подменять тире, а перестраивать фразу. Рядом тот же счёт к капслоку для нажима: НЕВОЛЬНЫЙ и ДОБРОВОЛЬНУЮ (строки 27-28), ВСЕ потоки (строка 76), в docs/lld такого выделения тоже нет. Хук символов зелёный, запрещённых знаков нет, речь про чужую руку в тексте. Отдельно важно, что дефис протёк в проектируемый вывод команды («потолок 120 минут: ПОТОЛОК -- сдавай хвост») и оттуда уехал в DoD задачи DK-506: формат вывода надо поправить в обоих местах, иначе он застынет в коде: исправлено, все восемь мест с « -- » перестроены (не заменены двоеточием/запятой), капслок для нажима убран (все потоки, потолок пройден); формат вывода taskctl elapsed поправлен и в LLD, и в DoD DK-506; прогнан скилл proofread вторым заходом, нашёл и поправил остаточный капслок и две формы одушевления/кальки
- Число, на котором стоит калибровка потолка, расходится с собственными данными документа. В разделе «Живой материал» сказано «тот заход, что дошёл до автосжатия, шёл 9 часов 26 минут», и в решении 1 та же длительность названа как 566 минут. Но по первому абзацу заход шёл с 23.08 21:13 до 24.08 07:52, это 10 часов 39 минут (639 минут); 9 часов 26 минут это время до автосжатия в 06:39, а не длина захода, после сжатия агент прожил ещё час и умер по 429. Отсюда же поплывут «в 5-9 раз дольше обычного» и «останавливает аномальный в первой пятой части пути». Назвать надо оба числа раздельно: длину захода и момент компактации, потому что потолок сравнивается именно со вторым: исправлено, числа разведены: заход 21:13-07:52 это 639 минут целиком, автосжатие в 06:39 это 566 минут от старта, а не длина захода; калибровка потолка 120 минут пересчитана на обоих числах (меньше пятой части пути, впятеро раньше автосжатия)
- Точка вставки повторного pick выбрана так, что потолок работает на первом заходе и молчит на всех следующих, то есть ровно наоборот нужному. DK-511 привязывает второй вызов к месту «после shipctl start <ID>» и правит два раздела: «Нужна ли ветка» в board-ship и шаг 6-7 в board-batch. Но заходы, которые создаёт решение 2, стартуют без shipctl start вовсе: продолжение по хвосту это спавн в уже готовом дереве, дерево и ветка целы по самому решению. Туда же попадает подъём после парковки, а парковка сама стирает запись: taskctl ask, не дождавшись ответа, зовёт cmdMove(main, id, SectBlocked, ...) (tools/taskctl/ask.go, liveDeps.Park), то есть флашит этап, а пользоваться taskctl ask исполнителю велят сами определения exec-*.md (exec-medium.md:39-47). Есть и третья точка спавна без старта: board-ship, раздел «Ревью», «Тем же синхронным спавном зовётся и исполнитель, когда задачу ведёт диспетчер» (SKILL.md:58-59), и pick --record там не назван. Утверждение решения 1 «этап живёт до самого момента, когда задача покидает In progress» верно буквально, но выход из In progress бывает и посреди работы. Правило надо формулировать от спавна, а не от старта дерева («перед каждым спавном исполнителя»), и DK-511 обязана назвать все точки спавна, включая продолжение по хвосту и подъём после парковки, иначе второй и дальнейшие заходы уходят в ветку «этап не открыт, потолок не проверить»: исправлено, правило переформулировано от спавна: перед каждым спавном исполнителя (первый старт, продолжение по хвосту в готовом дереве, подъём после снятой парковки), а не после shipctl start; DK-511 расширена на все три точки плюс board-ship раздел «Ревью» (строки 58-59) и board-task (короткая заметка про продолжение), где раньше вызов не был назван вовсе
- Побочный эффект приёма на «Ход работы» не назван ни в документе, ни в DK-511. После правки в файле каждой задачи окажется две строки «Разработка»: минутный огрызок от зазора между вердиктом и move (тот самый «13:55-13:56», которым документ и доказывает дыру) и настоящая, которую флашит уже перевод в Check. Сегодня строка одна. Раздел контрактный (TASKFORM.md), в него же DK-507 кладёт хвост потолка, и читают его люди. Сказать надо одной фразой: это принятое следствие, а первый вызов --record остаётся ради дашборда, который читает живую запись ~/.devkit/runs. Заодно стоит записать и выигрыш, он усиливает решение: сегодня запись стирается переводом в работу, и во время захода дашборд живого этапа не видит вовсе, а после правки видит: исправлено, решение 1 дополнено абзацем «побочный эффект принят сознательно»: второй Разработка-строкой в Ход работы (минутный огрызок плюс настоящий этап) и выигрышем рядом (сегодня дашборд живой записи не видит вовсе, после правки видит с первой минуты настоящей работы)
- Вёрстка после переписывания рваная в трёх местах, документ в остальном держит перенос по 76 символам. Строка 251 вытянулась на 145 символов («запуск, которым начинается любой другой заход, только с ID, названным явно. Диспетчер передаёт...»): предложение приклеили к концу абзаца, не переразбив. Строки 265-266: «Автоматическое / возвращение хвостовых строк в / пачечный отбор» с обрывком в 29 символов посреди абзаца. Строки 319-320: «Это ожидаемое поведение, а не / утечка: задача не / потеряна» с обрывком в 19 символов. Переразбить эти три абзаца по общей ширине: исправлено, все три абзаца переразбиты по общей ширине ~76 символов (textwrap): строка на 145 символов, и оба обрывка (29 и 19 символов) убраны
- Перечень точек спавна всё ещё неполон: осталась четвёртая, и она не спавн, а делегирование командой. Когда строка via не совпала с активным харнесом, задача уходит не спавном субагента, а командой agentctl run <ID> --workdir <дерево>, и так велят обе процедуры: board-task/SKILL.md:78 и board-batch/SKILL.md:52. Вызов pick туда не приставить снаружи: cmdRun делает вердикт сам, внутри себя, строкой pickVerdict(root, id, record, role, goal) (tools/agentctl/run.go:261), а флаг record по умолчанию выключен (main.go:193), и в скиллах run зовут без него. Значит на этом маршруте этап «разработка» опять не открывается и потолок молчит весь заход, а маршрут живой: через него в devkit ходит вся делегация в glm. Чинится это не отдельным вызовом перед спавном, а флагом на самой команде (agentctl run <ID> --record --workdir <дерево>; флаг у run есть, agentctl/README.md:25 и 1061), поэтому формулировка DoD у DK-511 («тот же вызов agentctl pick <ID> --record -C <worktree> перед спавном») на этот случай не ложится и нужна отдельным пунктом. Заодно поедет счёт в отвергнутом варианте решения 1: там сказано «Дисциплина трёх скриптованных мест», а мест становится четыре. В той же фразе неверна и отсылка: «решение 1 ниже перечисляет их» стоит внутри отвергнутых самого решения 1, а перечень лежит выше по тексту, в теле решения: исправлено, четвёртая точка добавлена в решение 1 отдельным абзацем: agentctl run сам считает вердикт (pickVerdict внутри cmdRun) и делегирует харнесу тем же вызовом, второй pick --record не нужен, чинится флагом --record на самой команде; DK-511 (DoD, Что происходит, Чего хотим) и заголовок строки на доске обновлены; счёт скриптованных мест в отвергнутом варианте решения 1 переписан без точного числа, отсылка исправлена на «выше»
- Машинная часть сценария пиннит номера строк живой доски, и они уже разъехались за один круг ревью. В блоке стоит «53:| DK-506 ... 54:| DK-507 ... 55:| DK-511», а сегодня тот же grep даёт 54, 55 и 56: доска общая, над этими строками успела встать чужая. Проверяемое тут не номер, а наличие трёх строк с рангом 38 и зависимостью «[после DK-506, DK-511]», и номер только шумит: человек, который прогонит сценарий на приёмке, увидит расхождение и полезет разбираться в пустом месте. Убрать -C и -n из этого шага (или грепать по ID без номеров), остальные шаги воспроизводятся дословно, я прогнал их целиком: счёт решений 2, счёт отвергнутых 2, карта 108-109, taskctl lint зелёный, check-symbols rc=0: исправлено, шаг сценария переписан без номеров строк: grep без -n по docs/map.md и по docs/TASKS.md, плюс grep -c на три совпадения; проверяет наличие и содержимое строк, а не их позицию на доске
- Сценарий проверки снова не пересняли после последней же правки этого круга, и теперь он расходится с доской по содержимому строки, а не по номеру. В блоке вывода стоит «| DK-511 | board-ship, board-batch, board-task: pick --record перед каждым спавном исполнителя |», а живая доска отдаёт «...перед каждым спавном исполнителя и флаг у agentctl run»: заголовок строки переименовали тем же коммитом 18c0da2c, которым закрывали замечание 11, а шаг сценария остался от предыдущего прогона. Прогнал блок целиком, всё остальное сходится дословно (2 решения, 2 блока отвергнутых, карта с DK-503.1 и DK-503.2, grep -c даёт 3, lint зелёный, check-symbols rc=0, двенадцать исправленных). Тем же непереснятым остался и человеческий шаг: там сказано, что спорное место это «правка board-ship и board-batch, второй вызов agentctl pick --record перед спавном исполнителя», а согласие у человека теперь спрашивают на три скилла плюс флаг --record у agentctl run. Это ровно тот абзац, по которому пользователь принимает решение, и занижать в нём объём нельзя. Переснять шаг с доской и переписать человеческую часть под нынешний состав DK-511: исправлено, машинный блок пересобран заново: строка DK-511 теперь с хвостом «и флаг у agentctl run», счёт review show вырос до 13. Человеческая часть переписана под нынешний состав DK-511 (три скилла плюс отдельный флаг agentctl run), объём работы больше не занижен

## Сценарий проверки

Вид приёмки user, барьер «согласие»: предмет задачи это решения, меняющие
устройство исполнителя и диспетчера, поэтому машинная часть проверяет только
целость и полноту документа и нарезки, а согласие с самими решениями даёт
человек.

Машинная часть прогнана в дереве задачи после четырёх кругов ревью и
правок по всем тринадцати замечаниям (2026-08-24).

```text
$ grep -c "^## Решение" docs/lld/DK-503-exec-ceiling.md
2
$ grep -c "^Отвергнутое:" docs/lld/DK-503-exec-ceiling.md
2
$ python3 tools/devkitctl/devkitctl.py map -C /Users/rider/projects/devkit-dk-503 --write
Карта записана: /Users/rider/projects/devkit-dk-503/docs/map.md
$ grep "^DK-503" docs/map.md
DK-503.1 Потолок это время с открытия этапа «разработка», не обращения и не токены
DK-503.2 Исполнитель сдаёт хвост сам, диспетчер поднимает следующего повторным pick
$ grep -c "DK-506\|DK-507\|DK-511" /Users/rider/projects/devkit/docs/TASKS.md
3
$ grep "DK-506\|DK-507\|DK-511" /Users/rider/projects/devkit/docs/TASKS.md
| DK-506 | taskctl elapsed: минуты открытого этапа «разработка» против потолка захода | task | P2 | 38 (25+8+1+0+4) | S | [tasks/DK-506.md](tasks/DK-506.md) |
| DK-507 | exec-*: абзац потолка захода и формат хвоста в «Ход работы» по DK-503 [после DK-506, DK-511] | task | P2 | 38 (25+8+1+0+4) | M | [tasks/DK-507.md](tasks/DK-507.md) |
| DK-511 | board-ship, board-batch, board-task: pick --record перед каждым спавном исполнителя и флаг у agentctl run | task | P2 | 38 (25+8+1+0+4) | M | [tasks/DK-511.md](tasks/DK-511.md) |
$ taskctl -C /Users/rider/projects/devkit lint
доска и архив в порядке
$ python3 hooks/check-symbols.py docs/lld/DK-503-exec-ceiling.md docs/tasks/DK-503.md docs/map.md; echo rc=$?
rc=0
$ taskctl review show DK-503 | grep -c "\[исправлено\]"
13
```

Три строки нарезки на месте с одним рангом (38) и верной зависимостью:
DK-507 стоит после обеих остальных, DK-506 и DK-511 друг от друга не
зависят, как и требует «Разбивка на задачи». Документ содержит оба решения с
двумя блоками отвергнутых вариантов (8 пунктов на двоих: 5 у решения 1, 3 у
решения 2), карта проиндексировала оба заголовка, доска и архив линтуются
чисто, символы во всех трёх файлах чистые, все тринадцать замечаний
ревью закрыты исходом «исправлено».

Человеческая часть: прочитать решения 1 и 2 и либо сказать «согласен», либо
назвать, что переделать. Спорное место одно, и оно названо прямо в
документе: решение 1 не работает без отдельной задачи DK-511, которой не
было в первой версии документа и которая нашлась только по факту ревью на
живом примере самой задачи DK-503. За три круга ревью её состав вырос:
правка трёх скиллов (`board-ship`, `board-batch`, `board-task`) вторым
вызовом `agentctl pick --record` перед каждым спавном исполнителя (первый
старт, продолжение по хвосту, подъём после парковки), плюс отдельный флаг
`--record` на команде `agentctl run` для делегации, где второй вызов не
приставить (у неё вердикт и спавн в одном вызове). Это удорожание нарезки
на одну строку с тем же рангом, а не смена решения: согласие нужно на сам
приём (тот же вызов, что уже называет ревьювера, плюс готовый флаг у
`agentctl run`), а не на альтернативу.
