# DK-516: Тик watch закрывает вид agent из Check, status называет ждущих человека

## Что происходит

2026-08-25, сессия «Выполни DK-463»: задача слита поездом (8ef4bc68), сценарий проверки прогнан после выката, `shipctl smoke DK-463` отмечен, строка встала в Check с видом agent. Сессия закончилась раньше `taskctl close`, и `shipctl status` до утра показывал «следующий шаг: закрыть задачу», а закрывать было некому.

Задача вида agent, у которой всё прогнано, висит в Check до следующей живой сессии; пользователь видит вставший конвейер, хотя по правилам агент закрывает такую задачу сам. Хвосты DK-306 (DK-312, DK-313) чинят разлив поезда, а этот шаг после разлива ни одна автоматика не делает.

## Чего хотим

Задача вида agent в Check с отметкой smoke и зелёным прогоном сценария закрывается тиком watch без живой сессии, задачи вида user остаются человеку и видны в status как ожидающие приёмки.

## Границы

- Громкого уведомления на закрытие не заводим: смысл задачи в том, чтобы
  человека не дёргать, а закрытая задача это не повод подойти к машине.
  Допущение груминга 2026-08-25, вопрос человеку задавался, ответа за отведённое
  время не было; передумать стоит одной строки в `watch.py`.
- Вид mixed тик не закрывает наравне с user: часть приёмки там за человеком.

## Развилки

Класть в тик `devkitctl watch` рядом с `ship --drain` (DK-313) или отдельной проверкой в taskctl? Задачи вида user не трогать: им в status нужна отдельная отметка «ждёт приёмки».

## DoD

- тик `devkitctl watch` закрывает задачу вида agent, которая стоит в Check, у
  которой отметка smoke действует на последний выкат (`smokeDone`) и у которой
  непуст раздел «Проверка» файла задачи (`closeAgentGate`); закрытие идёт
  командой `taskctl close`, доска коммитится и уезжает в origin тем же вызовом,
  как это уже делает страховка парковки;
- задача, у которой не сошлось хоть одно из четырёх условий, остаётся в Check
  нетронутой, и в первую очередь это виды user и mixed;
- `shipctl status` после smoke называет поимённо, кто из Check ждёт человека, и
  с каким видом приёмки, вместо нынешней общей фразы «приёмка глазами за
  пользователем»;
- закрытие видно строкой в журнале watch, а строка уехала в Done;
- тесты: тик закрывает подходящую задачу на временном стенде, не трогает вид
  user и mixed, не трогает agent без отметки smoke и agent с пустым разделом
  «Проверка»;
- дока: поведение тика описано в `tools/devkitctl/README.md`, а правило про то,
  что агентскую задачу из Check закрывает автоматика, стоит в `RULES.board.md`.

## Ранг

`25+6+2+0+4 = 37`, P2.

- серьёзность 25: «пользователь видит вставший конвейер», обход дешёвый и
  очевидный, следующая живая сессия зовёт `taskctl close` руками, а очередь
  выката такая строка не держит, её освободил smoke;
- ценность 6: «по правилам агент закрывает такую задачу сам», конвейер доезжает
  до конца без человека;
- неопределённость 2: подход назван гипотезой (тик watch), машинная опора
  готова, открыты детали вызова;
- поправка на баг 0: автоматики этого шага не было никогда, чинить нечего;
- рычаг 4: живой цикл разработки без ручного хвоста, каждая следующая задача
  доезжает дешевле.

## Разбор 2026-08-25

Ворота готовности пройдены, черновик живой:

- актуальность: автоматики нет, `tools/devkitctl/watch.py` не зовёт `taskctl close`
  ни на тике, ни страховкой, а `shipctl status` после smoke печатает «приёмка
  глазами за пользователем» одинаково всем видам приёмки;
- предмет: задача вида agent с прогнанным сценарием стоит в Check до следующей
  живой сессии, обход есть и он дешёвый (руками `taskctl close`), очередь выката
  такая строка не держит, smoke её уже освободил;
- конец работы: тик закрывает такую задачу сам, и это видно журналом watch плюс
  уходом строки в Done.

Машинная опора под гипотезу уже лежит, своего разбора заводить не надо:
`smokeDone` в `tools/shipctl/record.go` отвечает, действует ли отметка smoke на
последний выкат, `closeAgentGate` в `tools/taskctl/gate.go` требует у вида agent
непустой раздел «Проверка». Признак закрытия собирается из четырёх готовых
условий: строка в Check, вид agent, smoke на последний выкат, непустой раздел
«Проверка».

Права на самостоятельную правку доски у тика тоже есть: `park_stale` в watch.py
уже двигает строку командой `taskctl move ... --push` без человека, отдельно
спрашивать про коммит и пуш закрытия нечего.

Дублей нет ни на доске, ни в архиве, ни в накопителе. Родня по семейству это
DK-312 и DK-313 (разлив поезда хвостами DK-306), но они кончаются на выкате, а
шага после разлива не делают.

Ранг размечен вчерне: 25 (заметное трение, обход дешёвый) + 6 (конвейер доезжает
без человека) + 2 (подход назван гипотезой, открыты детали) + 0 + 4 (рычаг, каждая
следующая задача дешевле) = 37, тип task, вид приёмки agent.

## Ход работы

- Из черновика DK-516 «Тик watch закрывает задачу вида agent из Check после smoke и сценария»: записан 2026-08-25, оформлен 2026-08-25.
- Уточнение: вопрос: Груминг DK-516, два уточнения по границам. 1) Кусок про вид user. shipctl status уже печатает после smoke: «очередь свободна: smoke прогнан за DK-XXX, приёмка глазами за пользователем, выкат очередь не держит». Хватает ли этого, или в задачу входит отдельная отметка, которая перечисляет поимённо, кто в Check ждёт человека (виды user и mixed)? От ответа цена: только автозакрытие agent в тике watch это S, вместе с правкой status это M. 2) Наблюдаемость. Тик watch закрывает задачу сам, коммитит доску и пушит (как уже делает страховка парковки). Нужно ли на закрытие громкое уведомление (shout, как на «задача в Check»), или довольно строки в журнале watch и того, что строка уехала в Done? Ранг вижу так: 25 (заметное трение, обход дешёвый: следующая сессия закрывает руками) + 6 (ценность, конвейер доезжает без человека) + 2 (подход назван гипотезой, открыты детали) + 0 + 4 (рычаг, дешевле каждая следующая задача) = 37, тип task, вид приёмки agent., 2026-08-25 11:57-12:10.
- Уточнение: вопрос: Ответ «1» читаю так: в обоих пунктах вариант первый. То есть shipctl status остаётся как есть (отдельной отметки про виды user и mixed задача не заводит), громкого уведомления на закрытие тиком нет, довольно журнала watch и ухода строки в Done. Цена тогда S, предмет один: тик watch закрывает задачу вида agent из Check. Так? Если «1» это ответ только на первый пункт, скажите отдельно про уведомление., 2026-08-25 12:10-12:18.
- Уточнение: вопрос: Громкое уведомление это зов hooks/notify.py: баннер macOS через terminal-notifier с переходом по клику к строке доски, плюс событие в ленте дашборда. Тем же способом taskctl сейчас кричит о трёх поводах (task_check, task_blocked, task_fail), то есть «задача доехала до Check», «встала на блокере», «проверка провалена»: поводы, ради которых человек подходит к машине. Тихая альтернатива это строка в журнале watch (~/.devkit/watch.log), которую никто не увидит, пока не заглянет. Мой совет: на закрытие не кричать. Смысл задачи ровно в том, чтобы человека не дёргать, а закрытая задача это не повод подойти к машине; в ленте дашборда уход строки в Done виден и без баннера. Если хочется знать, что ночью что-то закрылось, дешевле утренняя сводка, а не баннер на каждую строку. Итог тогда такой: цена M, предмет два куска (тик watch закрывает вид agent, status называет поимённо, кто из Check ждёт человека), уведомления нет. Оформляю?, 2026-08-25 12:18-00:36.
- Вычитка: 8 файлов, 12 правок, 0 пометок, 2026-08-26.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 30%, week_max 3%, снимок 10м назад, сдвига нет), 2026-08-26 01:02-23:03.

## Ревью

- shipctl status обещает то, чего не сделает тик: checkParts (tools/shipctl/advice.go) не смотрит на failSuf (провал), в отличие от closable() в tools/taskctl/closable.go, которая рядом с этим же условием явно отказывает строке с непогашенным провалом. Строка Check вида agent, у которой есть отметка smoke и непустая «Проверка», но висит [провал: ...], после smoke классифицируется в agentSmoked и cmdStatus печатает «человека в Check никто не ждёт: агентские XR-009 закроет тик devkitctl watch» рядом со строкой «провал проверки за XR-009 ...; merge и ship отказывают, пока признак не погашен». Проверено прогоном (временный тест в tools/shipctl, сценарий: rowCheck с [провал: ...] + deployedCheckTask + smoke + непустая «Проверка» -> cmdStatus действительно печатает обе строки, а closable такую строку не отдаст). checkParts должен либо пропускать строки с failSuf, либо относить их к отдельной группе, чтобы совет status не расходился с тем, что вернёт taskctl closable.: исправлено, checkParts пропускает строку с непогашенным провалом, про неё status кричит своей строкой; тест TestStatusSilentOnFailedCheck красен на старом коде

## Сценарий проверки

Агентский. Гоняется на выкаченном main, шаги 1-3 после выката.

1. Спросить вердикт отбора на живой доске:

   ```sh
   taskctl -C ~/projects/devkit closable
   ```

   Ждём голые ID строк Check с четырьмя сошедшимися условиями. Ниже перечень
   отказов, и у каждого своя причина. Вид приёмки не agent, нет отметки smoke,
   пуст раздел «Проверка», нет файла задачи.

2. Прогнать тик сторожка руками, тем же вызовом его будит launchd:

   ```sh
   devkitctl watch
   tail -5 ~/.devkit/goal-watch.log
   ```

   Ждём в журнале строку «задача <ID> в /Users/rider/projects/devkit закрыта
   тиком: вид agent, smoke прогнан, раздел «Проверка» непуст» на каждый ID из
   шага 1. На доске эти строки должны уйти из Check в архив
   (`taskctl -C ~/projects/devkit list check`). Строки видов user и mixed
   должны остаться на месте.

3. Спросить конвейер:

   ```sh
   shipctl -C ~/projects/devkit status | tail -3
   ```

   Ждём строку «ждут человека в Check: <ID> (mixed|user), приёмка глазами и
   закрытие за пользователем». Прежней общей фразы «приёмка глазами за
   пользователем» быть не должно. В совете следующего шага ждём разделение по
   видам. Агентские отданы тику, за видами user и mixed назван человек.

4. Прогон тестов ветки:

   ```sh
   cd ~/projects/devkit && GOWORK=off python3 tools/devkitctl/parallel.py
   ```

## Выкат

- 2026-08-28 слито: bb72f5a6, 79c6bb1d, 953efe5d, f70cd3de, fa181b5b, 4af3722d
- smoke прогнан, 2026-08-28

## Проверка
