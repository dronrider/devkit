# DK-522: Корпус эталонов прозы: фрагменты из журналов и трекеров, словарь, жанры

Цель: [tasks/DK-446.md](tasks/DK-446.md)

## Что происходит

Правило «изучить тексты пользователя в проекте» указывает на тексты, которые
почти целиком написаны агентами. Агент подражает агенту, и манера усиливается
до карикатуры. В репозитории нет эталона, на который можно сослаться вместо этого. Решение пользователя на постановке цели: эталон собирается из двух
источников. Первый это его собственные реплики из журналов сессий
(`~/.claude/projects/*/*.jsonl`, роль user). Второй это тексты людей из
публичных трекеров, не только баги, но и продуктовые задачи. Один источник даёт
бедный словарь, второй теряет манеру.

## Чего хотим

Корпус в `kit/skills/`, который едет с devkit в другие проекты. В нём словарь
пользователя, эталонные абзацы по четырём жанрам (постановка, решение LLD, вход
README, шаг скилла) и сборщик, который режет реплики из журналов сессий. Отбор
фрагментов делает сборщик, а вычитывает пользователь до коммита.

Решения по ходу работы.

- Корпус лежит в `kit/skills/prose/corpus/`, по файлу markdown на жанр, имя
  файла это идентификатор жанра. Отвергнут один файл со всеми жанрами:
  вычитывать его пользователю пришлось бы целиком, а по `ls` было бы не понять,
  какие жанры вообще есть. Отвергнут и json: корпус читает человек
  раньше, чем скилл.
- Шапка фрагмента идёт сразу под заголовком `## N` и кончается первой пустой
  строкой. Отвергнута шапка через пустую строку: двоеточие в первой фразе тела
  уезжало в поле, и тест это поймал.
- Сборщик и выборка живут одним модулем `prose.py` рядом с `SKILL.md`, по тому
  же контракту, что и `goal-run.py` у `goal-loop`. Отвергнута go-утилита в
  `tools/`: наружу её никто не зовёт, а сборка окупает только то, что стоит в
  PATH.
- Журналы субагентов сборщик не читает. Ролью user там записан промпт
  диспетчера, то есть опять текст агента, а ради ухода от него всё и затевается.
- Выгрузка сборщика не коммитится, в корпус фрагмент переносится руками. Прямая
  запись из журналов в корпус отвергнута: личные тексты уехали бы в git без
  чтения человеком.
- Перечень скилловых тестов в `tools/devkitctl/parallel.py` переведён с
  ручного списка на открытие по факту `*_test.py`. Ручной список уже расходился
  с деревом молча (находка DK-367 про `cmdout`), и тесты нового скилла просто
  не гонялись бы.

## Развилки

Приёмка первого круга отбраковала девять абзацев корпуса, взятых из первого
коммита devkit. Ниже что убрано, чем заменено и что с этим делать замеру цели.

Убрано девять фрагментов. В `corpus/skill.md` это фрагменты 4-7, все с пометкой
«devkit, RULES.md первого коммита». В `corpus/readme.md` это все пять
фрагментов. Первый из них взят из README.md первого коммита, остальные четыре
из RULES.md того же коммита. Шапки обоих файлов обещали читателю тексты,
написанные пользователем до прихода агентов. Обещание снято, шапки переписаны.

Взамен в `corpus/skill.md` встали три реплики пользователя из журналов сессий.

- 2026-08-11, «Берём вариант 1 (DK-237 вперёд), но с двумя поправками».
  Два нумерованных шага, у каждого своя граница («не ставь её за DK-237»).
- 2026-08-19, «Далее мы пойдём нестандартным путём». Порядок работ и запрет
  начинать («Приступай к выполнению по отдельной команде!»).
- 2026-07-31, «Я согласен с концепцией, что плагин в целом не нужен». Команда
  действовать с оговоркой про каждый кусок промпта.

В `corpus/readme.md` встали пять реплик из журналов сессий.

- 2026-08-10, про слово «клон» в README devkit. Читатель назван прямо, вместе
  с его ошибкой.
- 2026-08-16, про цепочку скиллов brainstorm и рабочее демо. Почему читателю
  непонятно, что у нас есть.
- 2026-07-24, про пачку задач на одну сборку. Наблюдение, цена, предложение
  обсудить.
- 2026-08-01, про команду, которая берёт несколько задач сразу. Зачем оно
  нужно и кому.
- 2026-08-03, про «главный на Opus, черновая работа на GLM». Назначение
  инструмента одной фразой.

Отвергнутые варианты.

- Добрать вход README из трекеров. Материала там нет. Проверены все девять
  ссылок калибровки DK-173 и три продуктовые задачи. Восемь тел это описания
  багов с шагами воспроизведения. Три оставшихся (torrt 28, vanessa 1898,
  vk 834) написаны для человека, который инструмент уже держит в руках, и все
  три уже стоят в корпусе в других жанрах.
- Оставить жанр `readme` на трёх фрагментах, как велит нижний порог DoD 3.
  Выборка скилла берёт по 3-4 фрагмента на заход, и из трёх она вырождается в
  один и тот же набор.
- Переписать отбракованные абзацы своими словами и оставить как эталон. Агент
  подражал бы агенту. Цель DK-446 заведена ради ухода от этой петли.
- Взять README чужих проектов, лежащих на машине. Источников у корпуса два, и
  оба названы в постановке цели DK-446. Третий источник называет пользователь.
- Сходить в трекеры за новым материалом. Сеть закрыта разрешениями второй
  заход подряд, `curl` и `WebFetch` отбиты.

Замер цели без этих абзацев. Пересчитывать его не надо. Замер считал первый
коммит целиком, RULES.md на 1,6 тыс. слов, и девять вынутых абзацев в нём
отдельной величиной не были. Опора просела. Колонка «RULES v1» в таблице «Что
показал замер» была вторым человеческим ориентиром, и на ней стоят три порога
предупреждения в `kit/prose.toml`. Это `colon_mid` 15 при 7,5 у трекеров и
13,9 у RULES v1, `argued` 35 при 22% и 30%, `but_not_tail` 3 при 1,1 и 2,4.
Комментарий конфига так и говорит, что пороги проверены первым коммитом.
Прогон `check-prose.py` по девяти отбракованным абзацам даёт довод в той же
фразе 30,0%, двоеточие 14,7 и хвост «а не» 4,9 на 1000 слов. Хвост тут выше
собственного порога предупреждения и стоит рядом с агентской колонкой замера,
где 4,4. Числами замер держится на колонке трекеров. Она больше, 4,4 тыс. слов
против 1,6, и её никто не трогал. Пересмотр трёх порогов это отдельная строка
доски, DK-521 и DK-524 в неё не входят.

## DoD

1. Сборщик достаёт реплики роли user из журналов сессий, отсеивает короткие и
   служебные, складывает словарь.
2. Фрагменты из трекеров взяты по URL из архива DK-173 и дополнены
   продуктовыми задачами.
3. Корпус лежит в `kit/skills/`, разложен по четырём жанрам, у каждого жанра не
   меньше трёх эталонных абзацев.
4. Формат корпуса читается скиллом письма (DK-523): фрагменты помечены жанром,
   набор берётся случайной выборкой.
5. Пользователь вычитал отобранное, и в коммит уехало вычитанное.

## Ранг

`25+5+3+0+4 = 37`, P2.

- Серьёзность 25: трение с дешёвым обходом.
- Ценность 5: сам по себе корпус ничего не меняет, скилл письма читает его.
- Неопределённость 3: открыты правила отбора, формат хранения и объём выборки.
- Рычаг 4: без корпуса не едут скилл `prose` (DK-523) и перекалибровка
  `proofread` (DK-524). Ранг подтянут под инвариант зависимости.

## Приёмка

- вид: mixed
- барьер «согласие»: фрагменты пользователя это его тексты, отбор утверждает он
  - субагент-критик со свежим контекстом: не годится, даёт находки, а не
    согласие
- сравнение с числами замера обходом не считается: слоп оно отсеивает, а выбор
  того, что считать своим голосом, всё равно остаётся за пользователем

## Ход работы

- Вычитка: 1 файл, 1 правка, 0 пометок, 2026-08-25.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 14%, week_max 1%, снимок 3м назад, сдвига нет), 2026-08-25 14:36-14:36.
- Сети в заходе не было: `gh`, `curl`, `WebFetch` и `WebSearch` отбиты
  разрешениями, и по девяти ссылкам калибровки сходить не вышло. Дословные тела
  issue взяты из журнала сессии замера DK-446, где их выкачивали в августе;
  ссылка на первоисточник стоит у каждого фрагмента, так что сверить их можно
  глазами.
- Прогон сборщика 2026-08-25: 2793 журнала, 6034 реплики роли user, 4235
  служебных отсеяно, 1959 коротких и нерусских, осталось 179 кандидатов,
  словарь 186 слов от пяти реплик. Отсев служебного собирался в три захода:
  без отбоя `<task-notification>` восемь кандидатов из десяти оказались
  отчётами субагентов, дальше вылезли заготовки стенда `obeycheck` и приписка
  хука про план работ, которая поднимала «веди», «пиши», «массивом» в верх
  словаря.
- На согласие пользователю вынесено: 33 фрагмента корпуса (12 его реплик из
  журналов, 9 абзацев первого коммита devkit, 12 цитат из трекеров: все девять
  ссылок калибровки DK-173 плюс три продуктовые задачи) и словарь
  из 53 опорных слов и девяти оборотов. Спорным считаю фрагменты первого
  коммита: замер цели брал их за человеческий ориентир, но написаны ли они без
  агента, знает только он.
- Вычитка: 3 файла, 3 правки, 0 пометок, 2026-08-25.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 18%, week_max 2%, снимок 0м назад, сдвига нет), 2026-08-25 15:46-16:49.
- Приёмка 2026-08-25 отбраковала девять абзацев первого коммита, задача
  вернулась в работу. Корпус пересобран, разбор в «Развилках».
- Прогон сборщика вторым кругом, 2026-08-25: 2801 журнал, 6060 реплик роли
  user, 4257 служебных отсеяно, 1607 коротких и нерусских, осталось 179
  кандидатов, словарь 74 слова от восьми реплик. Отбор кандидатов сошёлся с
  первым кругом числом.
- Краснота теста `test_фрагменты_только_из_журналов_и_трекеров` снята
  прогоном `regcheck -C kit/skills/prose --tests kit/skills/prose/prose_test.py
  -- python3 -m unittest discover -p '*_test.py'`. Вердикт: «тест краснеет на
  HEAD и проходит на текущем коде, регрессия закрыта». На старом корпусе он
  падал на `readme: devkit, README.md первого коммита`.
- Полный прогон `python3 tools/devkitctl/parallel.py` вторым кругом: 17
  компонентов из 17, 4m53s, OK.
- Находка мимо этой задачи. Сторож прозы даёт на новых фрагментах жанра
  `readme` и `skill` «абзац кончается обобщением» 4,5% при пороге
  предупреждения 2, и это реплики самого пользователя. Метрика ловит его
  манеру закрывать реплику предложением обсудить. Порог DK-521 и пункт
  типологии DK-524 стоит перепроверить на живом корпусе.
- На согласие пользователю вынесено вторым кругом: 32 фрагмента корпуса (20
  реплик из журналов, 12 цитат из трекеров) и прежний словарь из 53 опорных
  слов и девяти оборотов.
- Вычитка: 3 файла, 4 правки, 1 пометка, 2026-08-25.

## Ревью

- Раздел «Приёмка» файла задачи: барьер «согласие» у ACCEPTANCE.md вырожден, обход один («субагент-критик, не годится»). В файле под барьером стоят две строки перебора (субагент-критик и «сравнение с числами замера»), а gate taskctl требует ровно то число строк, что в acceptBarriers для барьера. Проверено live-прогоном acceptGate против docs/tasks/DK-522.md: команда возвращает «DK-522: у барьера «согласие» обходов 1, а перебор в «Приёмка» имеет строк 2 (нужно столько же, по строке на обход с исходом)». В нынешнем виде `taskctl move DK-522 check` откажет этой же ошибкой. Убрать вторую строку перебора (или, если хочется её сохранить как довод, вынести из-под барьера в другую часть раздела), оставив под «согласие» ровно одну строку с исходом.: исправлено, довод про числа замера вынесен из-под барьера отдельной строкой раздела, под «согласие» остался один обход; прогон acceptGate против рабочего дерева проходит, против версии из HEAD отказывает прежней ошибкой
- Отсев служебного в prose.collect тестами закрыт частично. TestCollect.test_служебные_вставки_отсеиваются проверяет только SERVICE_MARKS (command-name, system-reminder), слэш-команду и голую ссылку. Два других фильтра остаются без единого теста: SERVICE_PREFIXES (заготовки obeycheck «ЗАДАНИЕ:», «ДИАЛОГ:», запрос заголовка хука «[devkit-title]», «Разговор завис») и TAIL_MARKS (приписка хука «Веди план работ файлом», «Долгие дела»). По собственному «Ход работы» задачи именно эти два фильтра дались тяжелее всего: без них заготовки obeycheck и приписка хука лезли в кандидаты и поднимали слова вроде «веди», «пиши», «массивом» в словарь. Раз регрессия не заперта тестом, будущая правка регулярки или списка может тихо вернуть агентскую прозу под видом реплики пользователя, и заметить это будет некому. Добавить тесты на оба фильтра, по образцу уже существующих case для SERVICE_MARKS.: исправлено, добавлены два теста на синтетическом журнале: test_заготовки_стенда_и_запросы_хука_отсеиваются на все шесть SERVICE_PREFIXES и test_приписка_хука_режется_а_реплика_остаётся на оба TAIL_MARKS со сверкой словаря; с пустыми списками фильтров оба падают
- Фрагмент corpus/skill.md #4 (2026-08-11, «Берём вариант 1 (DK-237 вперёд)...») сверен с журналами. Текст дословно совпадает с записью роли user в 1c8015d8-...jsonl, но это не самостоятельная реплика пользователя. Его сочинил ассистент в параллельной сессии 32e2b6e8-...jsonl (2026-08-11T16:08:33Z, role: assistant), там прямо сказано «Вот промт для агента в соседнем окне», и дальше идёт тот же текст. Пользователь скопировал черновик ассистента и вставил его в другое окно (glm-worktree). Там реплика осела ролью user. Тот же текст стоит и в выводе субагента «Отбор реплик под два жанра» (сессия 289faf29), кандидат строка 690, без пометки о происхождении. Источник «журнал сессии, роль: пользователь» формально не врёт, запись правда там лежит. По факту это агентский текст, ретранслированный через copy-paste, ровно тот случай «агент подражает агенту», ради ухода от которого заведена цель DK-446 и сама задача DK-522. Заменить фрагмент на другую реплику или явно разобрать этот случай с пользователем на барьере «согласие» перед закрытием.

## Сценарий проверки

1. Сборщик на журналах машины печатает число найденных реплик и словарь.
2. `ls kit/skills/prose/corpus` показывает четыре жанра, в каждом не меньше
   трёх абзацев.
3. Пользователь читает отобранное и говорит, что публикуемое совпадает с его
   голосом.

## Выкат

- 2026-08-25 слито: dab42203, 91c77c70, 9605c76f, 006fa0a7, c6258ad7, 948da5bd
- smoke прогнан, 2026-08-25

## Проверка

2026-08-25, агентская часть сценария прогнана вторым кругом, после того как
приёмка отбраковала девять абзацев первого коммита. Числа ниже сняты на
пересобранном корпусе. Третий шаг за пользователем, задача ждёт его в Check.

Шаг 1, сборщик на журналах машины.

```
$ python3 kit/skills/prose/prose.py collect --out /private/tmp/dk522-collect
журналов: 2801
реплик роли user: 6060
служебных отсеяно: 4257
коротких и нерусских отсеяно: 1607
кандидатов: 179
словарь: 74 слов от 8 реплик
```

Числа держатся на прогоне первого круга. Журналов на машине прибавилось за
сутки, кандидатов по-прежнему 179.

Шаг 2, четыре жанра и по фрагментам в каждом.

```
$ ls kit/skills/prose/corpus
lld.md   readme.md   skill.md   task.md
$ grep -c '^## ' kit/skills/prose/corpus/*.md
lld.md:9  readme.md:5  skill.md:6  task.md:12
```

Всего 32 фрагмента против 33 на первом круге. Девять абзацев первого коммита
ушли, восемь реплик из журналов встали на их место.

Шаг 3 ждёт пользователя. Надо прочитать 32 фрагмента корпуса и словарь и
сказать, совпадает ли публикуемое с его голосом. Новыми на этом круге стали
три фрагмента в `corpus/skill.md` и все пять в `corpus/readme.md`, список с
датами лежит в «Развилках».
