# DK-530: Стенд obeycheck авторизуется сам и ловит негодную затравку до раскладки

Цель: [tasks/DK-446.md](tasks/DK-446.md)

## Что происходит

Затравка стенда собирается руками, дампом связки ключей в файл
~/.devkit/obey-home/.claude/.credentials.json, и живёт до первого отзыва
токена. На витке цели DK-446 задача DK-523 упёрлась в неё: токен затравки
отозван 4 августа, claude -p во временном HOME отвечает 401, а пересобрать
дамп сессия не может, запись секрета режется правами. Пустой HOME тоже не
годится, там «Not logged in».

Выходит, всякий прогон prompt-test упирается в человека с его связкой ключей,
и незамеченным это остаётся до самого прогона: сценарий написан, раскладки
собраны, а стенд молчит про 401 до пробной сессии.

Вопрос в том, откуда стенду брать авторизацию, чтобы агент гонял его сам.

Гипотеза: временный HOME держит симлинк на ~/.claude/.credentials.json
настоящего дома вместо копии дампа, тогда отзыв токена лечится обычным логином
пользователя, а не пересборкой затравки. Второй вариант дешевле: obeycheck
проверяет свежесть затравки до раскладки и падает внятной находкой с готовой
командой пересборки.

Проверено 2026-08-27 прогоном стенда на сценарии 16-proofread-voice. Две
сессии подряд вернули «Not logged in, Please run /login», файл-ловушка остался
нетронутым, а стенд насчитал две красные клетки. Дамп в затравке лежит с
4 августа. Красные клетки тут говорят только про то, что сессия не поднялась
вовсе. Поведение агента они не показывают.

## Чего хотим

Решение пользователя 2026-08-27, обе половины сразу.

Первая. Стенд сам берёт свежий `~/.claude/.credentials.json`, кладёт копию во
временный HOME и стирает её вместе с ним. Ручная пересборка дампа уходит,
отзыв токена лечится обычным логином. Копия на прогон выбрана против
постоянного симлинка. Дорожки к настоящим ключам в `~/.devkit/obey-home` тогда
не остаётся, а токен виден сессии ровно столько, сколько идёт стенд.

Вторая. Стенд проверяет затравку до раскладки и падает внятной находкой с
готовой командой. Ловить она должна все четыре вида негодности, от пропавшего
файла и битого симлинка до вышедшего срока и прав шире 600. Проверка на один
просроченный токен повторила бы первую половину и молчала бы там, где нужна.

## Развилки

Откуда стенду брать авторизацию, чтобы агент гонял его сам. Решено 2026-08-27,
разбором по ходу работы, и постановку это решение поправило.

Дамп связки самим стендом отвергнут. Снимает его команда `security`. Из
неинтерактивной сессии она поднимает системный диалог доступа и вешает прогон.

Копия файла `~/.claude/.credentials.json` отвергнута фактом. На этой машине в
нём лежит одна запись `mcpOAuth` про единственный сервер. Ключей харнеса там
нет, поля со сроком тоже нет, и временный дом такая копия не авторизует.

Взято то, что на машине работает месяц. POC-контур дашборда держит фейковый дом
со ссылкой `~/.devkit/dashboard-poc/Library/Keychains` на связку настоящего
дома. Сессии в нём живут с 20 августа.

Граница у решения широкая, и назвать её надо прямо. Через ссылку временный дом
видит всю связку пользователя целиком. Ключами харнеса дело не ограничивается.
На этой же границе стоит POC-контур.

## DoD

1. Прогон стенда на машине с живым логином идёт без ручной затравки. Ссылку на
   связку кладёт сам стенд, каталог `~/.devkit/obey-home` для этого не нужен.
2. Негодная авторизация ловится до раскладки правил, одной находкой с командой
   починки. Проверены три случая: каталога связки в доме пользователя нет,
   связка это ссылка в никуда, каталога из флага `--home-seed` нет.
3. Ссылка не переживает прогон, а связка пользователя цела. Тест смотрит и то,
   и другое.
4. Тесты не читают живые ключи машины и не зависят от того, авторизован ли
   пользователь.
5. Прогон сценария 16-proofread-voice на живом логине даёт клетки, которые
   отражают поведение агента. Отсутствие сессии в них больше не попадает.

## Ранг

`25+6+1+0+3 = 35`, P2.

`25+6+1+0+3 = 35`, P2.

- Серьёзность 25, трение с дешёвым обходом. Обход ручной, живёт до отзыва
  токена и упирается в человека со связкой ключей.
- Ценность 6. Работает на каждом прогоне стенда, а стенд остаётся
  единственной проверкой правок промптов.
- Неопределённость 1. Путь выбран пользователем, открыты детали раскладки
  копии и формы находки.
- Рычаг 3. Без него не едут DK-449, DK-497 и четвёртый шаг сценария проверки
  DK-524.

## Ход работы

- Из черновика DK-530 «Стенд obeycheck поднимает сессию без ручной затравки HOME»: записан 2026-08-25, оформлен 2026-08-27.
- Разработка: субагент opus/medium по вердикту pick (квота: week_all 42%, week_max 3%, снимок 7м назад, сдвига нет), 2026-08-27 15:22-15:27.
- Уточнение: вопрос: DK-530, вскрылось расхождение с постановкой до первой правки. Факт: ~/.claude/.credentials.json на этой машине держит только запись mcpOAuth (351 байт, ключи верхнего уровня я посмотрел, значения не читал). Записи claudeAiOauth с токеном и полем expiresAt в нём нет. Ключи харнеса лежат в связке, и старая затравка ~/.devkit/obey-home (568 байт, 4 августа) это как раз дамп связки командой security find-generic-password -s "Claude Code-credentials" -w, так и написано в README стенда. Значит копия одного только ~/.claude/.credentials.json временный HOME не авторизует, и DoD 5 на живом прогоне не сойдётся. Как быть, выбери: A. Стенд берёт ключи оттуда, где их держит харнес: на macOS дампит связку сам той же командой security, на прочих системах читает ~/.claude/.credentials.json. Копия на прогон, права 600, стирается вместе с прогоном. Ручная пересборка затравки всё равно уходит. Риск: security из неинтерактивной сессии может поднять системный диалог про доступ к связке и подвиснуть. B. Держаться буквы постановки: копируем только файл ~/.claude/.credentials.json. Тогда на этой машине стенд честно упадёт находкой «в ключах нет записи харнеса, залогинься», а DoD 5 закрыть нечем, пока ты не положишь ключи в файл. C. Твой вариант., 2026-08-27 15:27-15:35.
- Снаружи: блок: вопрос: DK-530, вскрылось расхождение с постановкой до первой правки. Факт: ~/.claude/.credentials.json на этой..., 2026-08-27 15:35-15:39.
- 2026-08-27, разбор до первой правки: постановка правится вариантом C,
  ключи харнеса лежат в связке, а не в файле.
- Вычитка: 2 файла, 4 правки, 0 пометок, 2026-08-27.

## Ревью

- в файле задачи нет раздела «## Сценарий проверки». Он обязателен по TASKFORM.md и RULES.board.md, п. 6 «Трекинг задач». shipctl merge проверяет его гейтом hasHeading(doc, "Сценарий проверки") в tools/shipctl/advice.go:223. Без раздела слияние откажет. Раздел нужно дописать. Первый шаг воспроизводит исходную поломку: Not logged in из-за протухшей затравки. Дальше проверка на симлинк-решение. Вид агентский, значит нужен минимум один шаг после выката, не повторяющий тесты ветки. Живой прогон под это уже есть в разделе «Проверка». Его надо оформить сценарием: результата одного мало
- в коммит 13e3dcb9 попал скомпилированный бинарник tools/obeycheck/obeycheck, 3.1 МБ, Bin 0 -> 3143938 bytes. Это случайный артефакт go build. Он не в .gitignore, хотя для двух других утилит там уже есть похожий паттерн: tools/taskctl/taskctl, tools/shipctl/shipctl. Убрать файл из индекса веткой и добавить tools/obeycheck/obeycheck в .gitignore по тому же образцу
- раздел «Чего хотим» не поправлен под принятое решение варианта C. Он противоречит «Развилкам», DoD и коду. Он всё ещё описывает отвергнутый план: «кладёт копию во временный HOME и стирает её вместе с ним... Копия на прогон выбрана против постоянного симлинка». По факту принят симлинк на связку, без копии вообще. Это прямо противоположно написанному. Там же вторая часть говорит про проверку «вышедшего срока» и «прав шире 600». Их в checkAuth нет и не может быть: связка не файл с полем срока. Это тоже осталось от старого плана. Переписать «Чего хотим» под фактическое решение: ссылка на связку, три случая негодности плюс вырожденный, без проверки срока и прав
- в разделе «## Ранг» строка формулы «`25+6+1+0+3 = 35`, P2.» напечатана дважды подряд. Убрать повтор
- в «## Ранг» формула содержит нулевой четвёртый терм (Поправка на баг), а строки-обоснования для него нет вовсе. По конвенции доски даже нулевое слагаемое сопровождается строкой вида «Поправка на баг 0. <причина>» (см. DK-203, DK-366, DK-379 и другие). Дописать такую строку: почему задача не тянет на регресс (затравка никогда не работала автоматически, это не «перестало работать как раньше», а изначальный design gap) или на регресс тянет, тогда поправить формулу на 5

## Проверка

Живой прогон на сценарии 16-proofread-voice, 2026-08-27, ярус mini, повтор
один, раскладки полная и ядро.

```
прогон: сценариев 1, раскладки 2, повторов 1, всего 2 сессий (конец: сессия)
проба: команда прогона отвечает, идём полным проходом
[1/2] 16-proofread-voice / rules-full / повтор 1: красно: проверка: exit status 1 (следа отработки в «Ходе работы» нет, вычитка не звалась)
[2/2] 16-proofread-voice / rules-core / повтор 1: красно: проверка: exit status 1 (следа отработки в «Ходе работы» нет, вычитка не звалась)

сценарий                                             rules-full  rules-core  вердикт
вычитать постановку, не тронув лексику пользователя         0/1         0/1  красный на обеих

итог: сценариев 1, регрессий нет
красных на обеих раскладках: 1, это находка про сами правила, а не повод для выноса
```

Ручной затравки в прогоне не было, флага `--no-preflight` тоже. Пробная сессия
прошла сама, обе рабочие сессии поднялись. Прогон 2026-08-27 до правки давал на
тех же клетках «Not logged in», теперь этого нет, и авторизация свою половину
работы закрыла.

Красные клетки остались, и причина у них другая. Дом прогона держит каталоги
`agents` и `projects`, а каталога скиллов в нём нет вовсе. Звать вычитку сессии
было нечем. Проверено чтением сохранённого дома прогона. Раскладку скиллов
чинит своя задача цели DK-446, здесь она названа в «Границах».
