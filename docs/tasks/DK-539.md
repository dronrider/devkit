# DK-539: Отметка работы по факту правки не доезжает: --touch нет в шаблонах раскладки

записан 2026-08-26

## Что происходит

POC цели DK-397 завёл в `hooks/session-task.py` режим `--touch`. Он отмечает
работу по факту правки файла, узнаёт правящие инструменты по списку
`WORK_TOOLS` и пишет строку в журнал сессий. Код на месте и разобран
функциями `touch_record`, `known_touch` и `run_touch`.

Позвать этот режим на машине пользователя нечем. Раскладку хуков пишут два
шаблона, и оба ставят один и тот же вызов на `SessionStart` с флагом
`--hook claude-code`. В `tools/devkitctl/devkitctl.py` это строка списка хуков,
в `tools/devkitctl/testenv.py` то же самое для временного стенда. Хука на ход
инструмента среди них нет вовсе, поэтому `--touch` не зовётся ни разу.

Снаружи это выглядит работающим. Журнал сессий пополняется отметками от
`taskctl`, `shipctl` и `agentctl`, которые пишут его своим порядком через общий
`sessions.Touch`. Пустоты в журнале не видно. Правка файла в чужом дереве, где
утилиты доски не зовутся, отметки не оставляет.

## Чего хотим

Шаблоны раскладки ставят вызов `--touch` на ход инструмента, и отметка работы
по факту правки появляется на машине сама, без ручной правки настроек. Диагностика
проекта видит отсутствие этого хука находкой и чинит его тем же `doctor --fix`.

## DoD

- Шаблон хуков в `tools/devkitctl/devkitctl.py` ставит вызов `--touch` на ход
  инструмента, и свежая раскладка получает его без ручной правки.
- Тот же вызов приезжает во временный стенд из `tools/devkitctl/testenv.py`.
- `devkitctl doctor` находит раскладку без этого хука и чинит её флагом `--fix`.
- Тест падает на старом коде и проходит на исправленном, краснота подтверждена
  прогоном `regcheck`.
- Правка файла в боковом дереве оставляет отметку в журнале сессий на живом
  прогоне, и это видно с дашборда.

## Ранг

`25+6+1+5+2 = 39`, P2.

- Серьёзность 25 (Major). Написанная фича у пользователя молчит, обход состоит
  в ручной правке настроек.
- Ценность 6. Дашборд начинает видеть работу в чужих деревьях, ради чего режим
  и писался.
- Неопределённость 1. Подход понятен, открыт выбор события харнеса, на которое
  вешается вызов.
- Поправка на баг 5. Дефект доставки, код есть и бездействует молча.
- Рычаг 2. Отметками кормится кольцо этапов и карта хода работы.

## Приёмка

- вид: agent
- барьер «глаза» рассмотрен и не стоит. Итог виден в журнале сессий и проверяется
  прогоном, согласия человека предмет не требует.

## Ход работы

- 2026-08-26, строка заведена разбором доски под приёмку POC цели DK-397. Работа
  идёт в ветке POC, потому что режим `--touch` живёт только там.

## Проверка

Прогон 2026-08-28 в дереве ветки `poc-chat`, каталог `tools/devkitctl`.
Переменная `DEVKIT_HOME` из окружения снята: с ней тесты берут хуки мимо
стаба.

```
$ env -u DEVKIT_HOME python3 -m unittest devkitctl_test -v \
    -k test_4b2_session_task_touch_hook -k test_hooks_are_laid_out_once
test_hooks_are_laid_out_once (devkitctl_test.HarnessHooksTest.test_hooks_are_laid_out_once) ... ok
test_4b2_session_task_touch_hook (devkitctl_test.ProjectFindingsTest.test_4b2_session_task_touch_hook) ... ok

Ran 2 tests in 9.612s

OK
```

Краснота снята откатом кода на коммит до правки
(`git checkout f4884fd4^ -- devkitctl.py testenv.py`): оба теста падают, и
падают на самой находке.

```
FAIL: test_hooks_are_laid_out_once
AssertionError: Regex didn't match: 'PostToolUse-хук session-task\.py --touch не подключён' not found
FAIL: test_4b2_session_task_touch_hook
AssertionError: 'PostToolUse-хук session-task.py --touch' not found in '... обвязка в порядке' :
нет находки про хук отметки работы

Ran 2 tests in 7.976s

FAILED (failures=2)
```

Выдача доктора в обоих отказах длинная и подрезана: в первом падении она
перечисляет 18 находок, среди которых своей нет, во втором доктор говорит
«обвязка в порядке».
