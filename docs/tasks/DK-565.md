# DK-565: Цель: мысль до первой правки хранится машинно, а правила проверяются прогоном

## Что происходит

devkit до сих пор рос в сторону машинерии после того, как понятно, что
делать. Доска, ранг, ворота слияния, поезд, выкат, бюджет квоты и сторожа
работают с задачей, у которой постановка уже есть. Наборы скиллов других
авторов я сравнивал с devkit не раз и в разное время, и по совокупности
видно направление, куда devkit не шёл. У них сильны интервью по дереву
решений, правило границ фаз, письмо для агентов с вопросом «меняет ли строка
поведение» и ворота «сигнал раньше гипотезы». Всё это про работу до кода и
про сам текст инструкций.

Точки роста, собранные парами к чужим приёмам (DK-552..DK-561), в лучшем
случае догоняют. У других наборов это направление тоже сделано текстом и
держится на памяти агента, и их пользователи жалуются на потерю решений и
пропуск шагов. Пока направление не названо, черновики по нему приходят
поодиночке и уходят в накопитель как мелочь.

## Чего хотим

Вектор назван и записан в файле цели одним абзацем. Неопределённость
снимается до старта задачи и хранится машинно, а не пересказом. Правила и
скиллы проверяются прогоном как код тестами, и правка без прогона считается
незакрытой. Стык фаз и остаток окна считает сторожок по журналу сессии. У
вектора есть цифра на доске, доля задач, вернувшихся из ревью или провала по
расхождению с постановкой. Задачи вектора DK-566..DK-569.

## Развилки

Решены на груминге 2026-08-29: цель заводится сразу, мерой служит доля
возвратов по расхождению с постановкой из DK-569. Открытым остаётся стык с
целью DK-161, его разбирает пробная нарезка.

## DoD

- У задачи есть перечень развилок с состоянием, и `shipctl start` отказывает
  при открытой блокирующей развилке, называя её.
- Дифф файлов, которые читают агенты, без сценария стенда и следа прогона в
  файле задачи отбивается воротами слияния.
- На стыке фаз сторожок печатает в ленту сессии подсказку с действием и
  остатком окна.
- Сводка taskctl показывает долю возвратов по расхождению с постановкой, и
  в файле цели записан первый замер.

## Ранг

`50+8+3+0+3 = 64`, P1.

- Серьёзность 50: решения теряются между сессиями и правила живут без
  проверки, потери повторяются на каждом витке.
- Ценность 8: пользователь назвал направление упущенным и требующим
  повышенного внимания.
- Неопределённость 3: две задачи из четырёх это дизайн.
- Поправка на баг 0.
- Рычаг 3: интервью, груминг, цикл цели и prompt-test опираются на результат.

## Ход работы

- Из черновика DK-565 «Вектор: качество мысли до первой правки и промпт как измеряемый предмет»: записан 2026-08-28, оформлен 2026-08-29.
- Вычитка: 5 правок, 0 пометок, 2026-09-05.
- Транзит через Check 2026-09-05: коммит правки скилла с ID цели затянул строку
  в состав поезда, выкат прогнан, строка возвращена в работу; пометка обкатки
  снята вместе с транзитом.
- Снаружи: проверка после выката, 2026-09-05 15:24-15:24.
- Проверка: сценарий прогнал claude-opus-5, 2026-09-05 15:31-15:33.
- Парковка снята витком 2026-09-05: сценарий цели гоняется последним витком,
  когда все задачи закрыты, и провал на середине цикла это не провал прода.
  Строка возвращена в работу, `taskctl fail` по ней не звался.
- Уточнение: вопрос: Сценарий DK-565 провален, но прод не сломан: три механизма DoD просто не написаны, пять задач цели не сделаны (DK-803 прямо сейчас взята в работу другой сессией). taskctl fail поставит признак «прод сломан» и остановит очередь выката всей доски, где параллельно идут DK-753, DK-713, DK-716, DK-803. Что делать со строкой?, 2026-09-05 15:33-15:33.
- Снаружи: блок: вопрос: Сценарий DK-565 провален, но прод не сломан: три механизма DoD просто не написаны, пять..., 2026-09-05 15:33-16:46.
- Снаружи: блок: вопрос: Сценарий DK-565 провален, но прод не сломан: три механизма DoD просто не написаны, пять задач цели не сделаны (DK-803 прямо сейчас взята в работу другой сессией). taskctl fail поставит признак «прод сломан» и остановит очередь выката всей доски, где параллельно идут DK-753, DK-713, DK-716, DK-803. Что делать со строкой?, 2026-09-05 16:47-19:35.

## Сценарий проверки

Прогон DoD цели, по команде на пункт. Гоняется он последним витком, когда все
задачи цели закрыты.

1. Ворота старта. `shipctl start <ID>` на задаче с открытой развилкой
   `решает: человек`. Ожидание: отказ с именем развилки, после `taskctl decide`
   с ответом ветка заводится.
2. Ворота слияния. `shipctl merge <ID>` на ветке с правкой `kit/skills` без
   сценария стенда. Ожидание: отказ; с пометкой «- Исключение: стенд» слияние
   идёт.
3. Подсказка на стыке фаз. `taskctl move <ID> check` при занятом окне сессии.
   Ожидание: строка подсказки с остатком окна и действием; на пустом журнале
   сессии подсказки нет.
4. Цифра качества постановки. `taskctl review stats --goal DK-565`. Ожидание:
   строка «возвраты по постановке: N из M», и тот же замер записан в «Итоге».

## Цель

devkit рос в сторону машинерии после постановки, а направление «мысль до
первой правки и промпт как измеряемый предмет» оставалось на памяти агента и
на вычитке. Цель закрывает его четырьмя механизмами. Решения по задаче
хранятся машинно и держат ворота старта. Правила и скиллы едут с прогоном
стенда, как код с тестами. Стык фаз считает сторожок. Качество постановки
видно цифрой на доске.

DoD агентский, четыре пункта раздела «DoD» выше, каждый проверяется командой:
`shipctl start` на задаче с открытой блокирующей развилкой печатает отказ с
её именем; `shipctl merge` на диффе скилла без сценария печатает отказ;
переход `taskctl move` при малом остатке окна даёт строку подсказки в ленте;
сводка taskctl печатает долю возвратов по постановке. Прогон стенда на самом
слиянии не влезает по цене (десятки вызовов модели), поэтому ворота требуют
сценарий и след прогона, а прогон гоняет автор правки.

Цель DK-161 остаётся отдельной и сужена до прогона и проверки: её пункт
про внешние условия и вопросы до старта переехал сюда кандидатом 5. Общие
места кода у кандидатов 1 и 7 с ней снимаются очерёдностью.

## Бюджет

бюджет: week_all <= 100
бюджет: week_max <= 100

Потолок пользователь не ограничил сознательно: направление сложное и про
промпты. Числа стоят верхней границей машины, а не рамкой. Ярус без потолка,
но и без пола ниже pro: нарезка не режет задачи цели до S, а задаче S, если
такая всё же появится, исполнитель ставит строку `Модель: pro` в файле.

## Задачи цели

Пробная нарезка 2026-08-29, материализована строками 2026-09-05. Кандидаты
собраны из черновиков DK-566..DK-569, DK-552, DK-553, DK-554, DK-559, DK-396 и
DK-410; при материализации черновики приписываются к заведённым строкам, а
кусок 3 черновика DK-396 (эталон вида приёмки) в цель не идёт и остаётся в
накопителе.

- кандидат 1, DK-801 (task, M, R=38). Возврат задачи несёт машинный класс
  причины, а сводка печатает долю расхождений с постановкой. Класс из закрытого
  списка
  появляется у `taskctl fail` и `taskctl review add`, сводка считает долю за
  окно и по цели. Цифра заводится первой. Иначе эффект остальных не измерить.
  Проверка агентская: `taskctl fail DK-X --reason ... --class постановка`
  кладёт класс в строку, сводка печатает «возвраты по постановке: N из M».
  Из DK-569, DK-396.
- кандидат 2, DK-803 (task, M, R=64). Сторожок считает остаток окна и печатает
  на стыке фаз подсказку с действием. Хук читает usage журнала сессии и берёт
  оценку следующей фазы по прошлым задачам той же цены; правило выбора между
  продолжить, субагент, передача и сжатие встаёт в ядро. Проверка агентская:
  на подставном журнале с занятым окном хук после `taskctl move DK-X check`
  печатает подсказку с остатком, на пустом молчит. Из DK-568, DK-559.
- кандидат 3, DK-552 (LLD, M, R=65). Развилки задачи хранятся машинно: формат,
  состояния, автор решения, признак блокировки старта. Документ выбирает между
  разделом файла задачи и командой `taskctl decide`, делит развилки на
  блокирующие и оставленные исполнителю, решает, ложатся ли внешние условия
  в тот же перечень. Проверка пользовательская: согласие с решениями.
  Из DK-566, DK-552, DK-396.
- кандидат 4, DK-804 (task, M, R=62). Ворота старта отказывают при открытой
  блокирующей развилке и называют её. `shipctl start` читает перечень и
  отбивает заведение ветки, дашборд показывает открытые развилки рядом со
  статусом. После кандидата 3. Проверка агентская: `shipctl start DK-X` на
  задаче с открытой блокирующей развилкой печатает отказ с её именем, после
  закрытия ветка заводится. Из DK-566.
- кандидат 5, DK-552 (task, M, R=40). Постановка любого входа идёт одним
  примитивом интервью: черновик на груминге, цель в goal-start и LLD зовут его
  же.
  Открытые развилки выкладываются разом, к каждой идёт рекомендация с
  доводом, ответы оседают в перечне развилок, и прерванный разговор
  продолжается с него. Внешние условия (доступы, токены, публичность, шаги,
  посильные только человеку) это развилки с признаком «решает человек,
  блокирует старт», отдельного раздела у них нет; к трём воротам готовности
  добавляются четвёртые про них. Пункт взят у DK-161 (DoD 2) решением
  груминга 2026-08-29. После кандидата 4. Проверка агентская: сценарий
  стенда на постановку требует записи ответов в файл, `check-skills.py`
  держит формулировку ворот у goal-start, board-groom и порядка LLD.
  Из DK-552, DK-396.
- кандидат 6, DK-805 (LLD, M, R=65). Правка правил и скиллов измеряется
  прогоном стенда. Документ решает, чем сценарий привязан к пункту правил или
  скиллу,
  где идёт прогон (на слиянии он не влезает по цене), каков порог просадки
  и как устроена судейская секция. Документ называет границу между
  `check-skills` (дословная подстрока) и `obeycheck` (поведение). Проверка
  пользовательская:
  согласие с решениями. Из DK-567, DK-410.
- кандидат 7, DK-806 (task, M, R=62). Дифф файлов, которые читают агенты, без
  сценария стенда отбивается воротами слияния. Четвёртые ворота встают рядом
  с тремя по образцу ворот теста, гасятся той же пометкой-исключением,
  подсказка `move check` из DK-448 становится отказом. После кандидата 6.
  Проверка агентская: `shipctl merge DK-X` на ветке с правкой `kit/skills` без
  сценария печатает отказ, с пометкой «- Исключение: стенд» проходит.
  Из DK-567.
- кандидат 8, DK-807 (task, M, R=37). У сценария стенда есть судейская секция с
  калибровкой. Рядом с машинной проверкой лежит критерий на естественном
  языке, стенд отдаёт транскрипт судье со свежим контекстом и берёт вердикт
  кодом возврата; сценарий не идёт в зачёт, пока судья не сошёлся с
  размеченными примерами. После кандидата 6. Проверка агентская: прогон
  сценария с судейской секцией печатает вердикт судьи, подставной ответ мимо
  стиля краснеет. Номер 17 в постановке кандидата занят `17-verify-runner`,
  сценарий заводится следующим свободным. Из DK-410.
- кандидат 9, DK-808 (task, M, R=39). Ревизия ядра и реестра скиллов стендом:
  пустое уходит, вес падает, послушание держится. Резидентные пункты проходят через
  вопрос «меняет ли строка поведение», спорные меряются прогоном,
  человеческие скиллы уходят из модельного реестра, обе цифры до и после
  ложатся в файл задачи. После кандидатов 7 и 8; оговорка про DK-161 снята,
  та закрыта 2026-09-01. Проверка пользовательская: согласие
  с тем, какие пункты уходят; агентская часть это падение веса в
  `devkitctl weigh` при зелёном стенде. Из DK-553, DK-554.

Материализация 2026-09-05, груминг черновиков. Кандидаты 3 и 5 сведены в одну
строку DK-552 (LLD). Где спрашивают и где ответ лежит, решается одним
документом, и разрывать это дорого. Черновик DK-566 приписан к ней и из
накопителя ушёл, так что кандидат 4 берёт постановку ворот из раздела «Из
черновика DK-566» файла DK-552 и заводится строкой после неё.

Материализация строками 2026-09-05, виток цикла: DK-801 (кандидат 1), DK-803
(кандидат 2), DK-804 (кандидат 4, после DK-552), DK-805 (кандидат 6), DK-806
(кандидат 7, после DK-805), DK-807 (кандидат 8, после DK-805), DK-808
(кандидат 9, после DK-806 и DK-807).

Сверка с кодом при материализации прошла по семи кандидатам. Класса причины нет
ни у `taskctl fail`, ни у `review add`. Счёт возвратов у `taskctl pilot` идёт
без разбивки, так что кандидат 1 расширяет готовую сводку. Хука про остаток
окна нет вовсе. Раздел «Развилки» в форме задачи есть, но прозой, без состояний
и признака блокировки. `shipctl start` файла задачи не читает. У слияния четыре
ворот, стенда среди них нет. При этом `promptHint` в `taskctl` уже опознаёт
агентские файлы и печатает совет без отказа, на нём и вырастает кандидат 7.
Судьи в `obeycheck` нет, есть только `OBEY_TRANSCRIPT` для сценария.
`devkitctl weigh` на месте, а признака человеческого скилла в реестре нет.
Список уехал в строки без ревизии. Поправлен только номер сценария у
кандидата 8.

оценка после материализации: 8 строк (8 M), week_all 8-24 из 100 остатка,
week_max 12-24 из 100 остатка; вилка влезла целиком, рамка сходится

Стык с DK-161 по кандидатам. Кандидаты 1 и 7 правят те же места, что DK-161
(разделы файла задачи через `internal/taskform`, ворота в `cmdMerge`), и
параллельно не идут; кто первый, тот заводит общее чтение секции. Кандидат 3
даёт формат, на который DK-161 садится разделом внешних условий, раздел один
на обе цели. Кандидат 5 забрал DoD 2 DK-161 целиком, и DK-161 в goal-start
больше не заходит, так что идти она может первой и независимо. Кандидаты 2, 4, 8
независимы.

Стык с DK-161 снят. Цель закрыта 2026-09-01, и общие места кода остались за
кандидатами 1 и 7 без очерёдности с чужой целью.

## Журнал

Пробная нарезка 2026-08-29. Два LLD (кандидаты 3 и 6) стоят в середине
состава. Кандидаты 4, 5, 7, 8 без их решений не берутся, и плановые остановки
на согласии дизайна лягут в середину цикла.
- снимок 2026-09-05T12:23: week_all 2%, week_max 2%
- 2026-09-05 12:23-13:36, материализация: восемь строк цели (DK-801, DK-803..DK-808 плюс DK-552), зависимости и приписка семи черновиков; сверка с кодом расхождений не дала, стык с DK-161 снят её закрытием; находка про переиспользование ID черновика ушла черновиком DK-809; continue
- снимок 2026-09-05T13:09: week_all 3%, week_max 2%
- 2026-09-05 13:09-15:27, рабочая фаза: DK-801 выкачена, прогнана sonnet и закрыта, первый замер цифры по цели дал «возвратов с классом причины пока нет»; LLD DK-552 и DK-805 прошли по два круга ревью, слиты и ждут согласия пользователя; дорезка состава (три строки за DK-552, деление DK-806 надвое за DK-805) отложена до приёмки документов; цель прошла транзитом через Check из-за коммита правки скилла с её ID и возвращена в работу; препятствия витка: краснота go:dashboard под параллельной нагрузкой (черновик DK-811, слияния гнались на -j 3) и незнание ревьюверами ключа -C у taskctl (дописано в скилл review); continue
- снимок 2026-09-05T15:28: week_all 11%, week_max 2%

## Итог

Пишет последний виток.

## Проверка

Агентская часть сценария прогнана на выкаченном коде 2026-09-05. Из четырёх
шагов не проходит ни один целиком. Трёх механизмов DoD в коде нет вовсе, а у
четвёртого есть команда, но нет замера в «Итоге». Причина не в поломке прода,
а в том, что пять задач цели из восьми ещё не сделаны. Это DK-803 (сторожок
остатка окна), DK-804 (ворота старта), DK-806 (ворота слияния), DK-807 и
DK-808.
Строка уехала в Check транзитом поезда, о чём в «Ходе работы» стоит пометка
обкатки.

```text
== шаг 1: ворота старта ==
$ taskctl decide DK-804
неизвестная команда "decide"
$ grep -rn "Развилки" internal/taskform/ tools/shipctl/
internal/taskform/taskform.go:26:       Forks        = "## Развилки"

== шаг 2: ворота слияния ==
$ shipctl (предусловия merge)
  merge <ID> [--test "cmd"]       предусловия (чистое дерево, задача в
        [--deploy "cmd"] [--push] In progress, прод не сломан, Check пуст,
        [--train]                 ревью без открытых замечаний), ребейз ветки
                                  на main, тесты,
                                  fast-forward-слияние, выкат, перевод в Check
$ grep -rn "стенд" tools/shipctl/
tools/shipctl/advice.go:404:// заглушку стенда, и данные примера, лежащие там же.

== шаг 3: подсказка на стыке фаз ==
$ ls hooks | grep -iE "phase|window|hint"
$ grep -rn "остаток окна" tools/taskctl hooks

== шаг 4: цифра качества постановки ==
$ taskctl review stats --goal DK-565
срез: цель DK-565
возвратов с классом причины пока нет
задач с ревью: 4 (живых 2, в архиве 2)
замечаний 7: исправлено 7, отклонено 0, открыто 0
доля исправленных среди закрытых: 100%
уровни ревью:
  0: ревью нет
  1: ревью нет
  2: ревью нет
  3: 3 ревью, ходы и минуты не записаны
```

Шаг 1. Команды `taskctl decide` нет, задачу с машинной развилкой завести
нечем, а `shipctl start` перечня развилок не читает, и раздел «Развилки»
знает только форма файла задачи. Шаг неисполним.

Шаг 2. Среди предусловий слияния стенда нет, в коде `shipctl` слово «стенд»
встречается один раз в комментарии про заглушку. Шаг неисполним.

Шаг 3. Сторожка остатка окна нет ни среди хуков, ни в `taskctl`. Шаг
неисполним.

Шаг 4. Срез по цели команда печатает, разбивка по классу причины на месте
(DK-801 слит), но возвратов с классом пока не было, строки «возвраты по
постановке: N из M» нет, и замер в «Итоге» не записан: там стоит «Пишет
последний виток». Шаг проходит наполовину.
