# DK-577: Вход в клиент проходит с телефона: ссылка в чате, код отдельным полем

Цель: [tasks/DK-577.md](tasks/DK-577.md)

## Что происходит

Разлогиненную сессию дашборд теперь узнаёт и перезапускает кнопкой (DK-466), но
сам вход остаётся ручным. Плашка говорит зайти на машину и выполнить `/login` в
терминале. С телефона такого хода нет вовсе, а смотрят POC в том числе с телефона.

Живой случай 2026-08-29. У клиента истёк вход прямо посреди работы, разговор
встал, и починка потребовала терминала на машине.

## Чего хотим

Человек с телефона видит разлогин, жмёт кнопку входа, открывает ссылку
авторизации, логинится и отдаёт код обратно дашборду. Дальше сессия поднимается той же кнопкой
перезапуска, что уже стоит на плашке.

## Развилки

Где логиниться. `/login` можно послать в ту же сессию, где встал разговор, а
можно поднять отдельную одноразовую сессию только под вход. Второе чище. Чужой
транскрипт не засоряется диалогом входа, а свежий токен ложится в связку ключей
и достаётся всем. Живые сессии всё равно перезапускаются кнопкой из DK-466,
они держат старый вход в памяти.

Откуда берётся ссылка. Клиент печатает адрес авторизации в свою панель, и
дашборд уже умеет читать виджеты клиента разбором панели (`tmux.go`). Открыт
вопрос, насколько разбор устойчив к смене вида. У входа есть ещё выбор способа
курсором, до ссылки надо дойти.

Как ходит код. Код авторизации это одноразовый ключ от учётной записи. В ленту
разговора он попасть не должен, лента пишется журналом и остаётся на диске.
Отсюда своё поле, которое ничего не сохраняет, и отправка кода сразу в
сессию нажатиями.

Что показывать при отказе. Тут три случая, и каждый называется словами. Ссылка
не нашлась, код не подошёл, вход отвалился на середине.

## DoD

- С телефона разлогиненный контур чинится целиком, без захода в терминал
  машины.
- Ссылка авторизации приходит в разговор кликабельной, код вводится отдельным
  полем и в журнал ленты не попадает.
- После входа рабочие сессии поднимаются кнопкой перезапуска, разговор
  продолжается.
- Отказ на каждом шаге назван словами.

## Ранг

`25+7+3+0+3 = 38`, P2.

- Серьёзность 25: обход есть, но болезненный, с телефона его нет.
- Ценность 7: снимает единственный шаг, который выбивает человека в терминал.
- Неопределённость 3: развилок три, разбор панели входа не смотрели.
- Поправка на баг 0: новое поведение.
- Рычаг 3: разлогин останавливает разговорный цикл цели.

## Приёмка

- вид: mixed
- барьер «глаза»: как экран входа выглядит на телефоне: читается ли ссылка
  авторизации целиком и стоит ли код отдельным полем. Машиной не снять,
  тут судит глаз.
  - утверждённый макет: не годится, макета экрана входа на момент работы нет
  - headless-браузер с замером: годится наполовину, числа снимает агент
    (цельность ссылки, границы блоков, отсутствие бокового скролла), а
    читаемость экрана в руках остаётся человеку
  - тест по разметке и стилям: не годится, вид экрана на телефоне стилями не доказать

## Порядок работы

Задача идёт доводкой цели DK-397 прямо в ветке `poc-chat`, дерево
`/Users/rider/projects/devkit-poc-chat`. Своей ветки и своего дерева у неё нет,
`shipctl start` звать не надо: весь чат-контур живёт только в ветке POC, и
против main работать не на чем.

- Коммит: `feat(dashboard): DK-577 <суть одной строкой>`, одна строка, без
  следов ассистента. Префиксы только feat и docs, `fix` хук отбивает.
- Пуш не делать, ветка уедет слиянием целиком.
- Тесты обязательны: стенд DOM `node testdata/<имя>.mjs static/app.js` из
  `tools/dashboard` плюс go-драйвер рядом с соседними, стенд обязан падать на
  прежнем коде.
- Выкат экземпляра POC: остановить службу `com.devkit.dashboard-poc`, удалить
  `~/.devkit/dashboard-poc/dashboard`, собрать
  `GOWORK=off go build -o ~/.devkit/dashboard-poc/dashboard .` из
  `tools/dashboard`, подписать `codesign -f -s -`, поднять службу, проверить
  `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:7131/` -> 302.
  Сборка поверх живого бинаря ломает подпись, и служба падает с
  `OS_REASON_CODESIGNING`.
- Фоновых прогонов и фоновых субагентов не заводить, всё в foreground. Пакет
  `tools/dashboard` целиком за один заход не влезает, гнать частями.

Опора по коду: плашка разлогина и кнопка перезапуска стоят в
`static/app.js` (`loginPlate`, `loginRestart`), признак считается в
`tools/dashboard/chats.go` (`loginGone`) и `sessions.go` (`tailFacts`), разбор
виджетов клиента лежит в `tools/dashboard/tmux.go`.

## Сценарий проверки

Гоняется на выкаченном экземпляре 7131 после выката, тесты ветки не
повторяет.

1. Подъём входа живьём: ручка POST /api/projects/devkit/chats/login
   отвечает 200, ссылка авторизации цельная (без пробелов и переносов,
   длина в сотни знаков), имя одноразовой сессии входа названо.
2. Повторный экран входа: та же ссылка и та же сессия, вторая не
   поднимается.
3. Забытый вход снимается сам: к ручке не возвращаются, за срок жизни
   сессии и минуту тика уборки tmux-сессия login-N исчезает, журнал
   демона говорит об истёкшем сроке.
4. Глаза, агентская мера: в headless-браузере на ширине телефона экран
   входа показывает ссылку целиком и поле кода отдельным полем.
5. Глаза, мера человека: пройти вход с телефона от начала до конца, с
   настоящей страницей авторизации и вторым фактором. Что смотреть, сказано
   разделом «Что остаётся за глазами пользователя».

## Проверка

Сценарий гонялся на выкаченном экземпляре 7131. Демон поднялся в 11:11 с
коммитами ветки.

1. Подъём входа живьём. POST /api/projects/devkit/chats/login ответил 200 и
   назвал tmux-сессию login-1. Ссылка авторизации вышла в 450 знаков и
   цельная. Замер не нашёл в ней ни пробела, ни перевода строки.
2. Повторный экран. Второй POST тем же ходом получил ту же сессию и ту же
   ссылку. Журнал демона записал повторную отдачу ссылки.
3. Забытый вход. К ручке не возвращались, подъём стоял с 11:18. Срок вышел
   десятью минутами позже, и сессия login-1 исчезла из tmux сама к 11:29.
   Журнал назвал причину снятия строкой «срок истёк: к ссылке не
   вернулись». Первая попытка прогона не в счёт. Через шесть минут после её
   подъёма параллельная работа пересобрала и перезапустила экземпляр,
   перезапуск стёр состояние подъёма, и сессия осталась ничья. Ту снял
   руками, прогон повторён на устоявшемся демоне.
4. Глаза. chrome-headless-shell на окне 390x844 открыл экран разговора, чей
   транскрипт кончается ответом «Login expired. Please run /login», нажал
   «Войти» скриптом в странице и смерил плашку. На POST /login странице
   ответил прокси между браузером и дашбордом, второй клиент не поднимался.
   Ссылка пришла той же длины, что живая из шага 1, а экран собрал
   настоящий app.js и стили экземпляра. Ссылка легла в десять строк, в
   границах экрана и без выхода за свой блок. Поле кода отдельное, видимое,
   в границах. Страница вбок не скроллится. Подопытный транскрипт снят
   после меры.

### Повторный прогон после замечаний 4-6, 2026-08-29

Экземпляр 7131 пересобран и поднят коммитами 93f67028 и 1a8fcfd7.

1. Подъём входа живьём. POST /api/projects/devkit/chats/login ответил 200 и
   назвал login-1. Ссылка вышла в 450 знаков, без пробела и перевода строки,
   с одной схемой и хостом claude.com.
2. Повторный экран. Второй POST получил ту же сессию и ту же ссылку, вторая
   не поднялась.
3. Родство обрывков на живой панели. Клиент разложил ссылку пятью обрывками
   по 80 знаков, хвост вышел в 50, и за ним стоит пустая строка. Разбор
   собрал ровно свои пять кусков и соседних строк панели не тронул.
4. Перезапуск службы, штатный шаг выката. После `launchctl kickstart -k`
   следующий заход получил ту же login-1 и ту же ссылку в 450 знаков, вторая
   сессия не поднялась, а журнал записал «сессия входа login-1 узнана после
   перезапуска службы». Прежде тут оставалась сирота, которую снимали рукой.
5. Срок дороги. Сессия входа стояла живой дольше прежних десяти минут. Та,
   что снималась бы уборкой в 12:53, была на месте в 12:57 со своей ссылкой.
   Снятие по сроку целиком живьём не повторялось, держать поднятый клиент
   получас ради повтора нечем, эту сторону закрывает стенд
   TestClientLoginSweepDropsExpired. Подопытная сессия снята рукой.

Шаг 4 первого прогона (headless на ширине телефона) не повторялся: разметку и
стили входа эта правка не трогала.

### Прогон после замечаний 8 и 9, 2026-08-29

Экземпляр 7131 пересобран и поднят коммитом 6a317c98.

1. Подъём входа. 200, login-1, ссылка в 450 знаков. Сессия получила метку:
   `tmux show-environment -t =login-1 DEVKIT_LOGIN_OWNER` отдал путь конфига
   экземпляра POC.
2. Посторонняя сессия рядом. Руками поднята login-2 с посторонним адресом в
   панели, рождением позже нашей и без метки. После перезапуска службы заход
   получил свою login-1 и свою ссылку в 450 знаков. Постороннего адреса в
   ответе нет, login-2 стоит нетронутой, журнал записал «сессия входа login-1
   узнана после перезапуска службы». Обе подопытные сессии сняты рукой.
3. Тот же угол, найденный без ревью. Код авторизации подавался в сессию по
   имени из памяти, без повторной проверки метки. Умри наша сессия, имя
   достанется соседу, и одноразовый ключ уедет нажатиями в чужую панель.
   Проверка встала перед самой подачей, а снятие сессии теперь тоже смотрит на
   метку. Живьём: метка снята с login-1 рукой, подача кода отбита 409 со
   словами «сессия входа login-1 сменилась под своим именем», сама сессия
   осталась цела. Стенд TestClientLoginCodeRefusedToStranger.

### Что остаётся за глазами пользователя

Барьер «глаза» вида mixed: агент мерил числа, человек проходит вход руками.
Осталось пройти его с телефона целиком, от кнопки «Войти» до перезапущенного
разговора. Человек увидит на плашке разлогина ссылку авторизации, тапнет её,
уйдёт на страницу входа, пройдёт второй фактор и вернётся к плашке с кодом.
Человеку судить, попадает ли палец в ссылку и в поле кода, не мешает ли
десятистрочная ссылка добраться до поля, доносит ли клавиатура телефона код
без потери знаков и говорят ли слова после отправки, что делать дальше.
Отдельно смотреть, хватает ли терпения дороге. Возврат со вторым фактором
занимает минуты, и вход всё это время должен ждать человека.

## Ревью

- ссылка авторизации ломается при переносе строки в панели: capture-pane без -J рвёт длинный OAuth-URL по ширине пейна, loginLinkRe матчит только первый обрывок строки и отдаёт человеку нерабочую усечённую ссылку. Проверено живьём: tmux capture-pane -p при ширине 80 без -J режет длинный https://... URL пополам, и regex забирает только первый кусок. Нужен -J у capture-pane (или сборка URL по нескольким строкам) плюс тест на разорванную ссылку: исправлено, capture-pane снят с клеем -J, а ссылку, порванную собственными переводами строк клиента, разбор собирает из обрывков, продолжения узнаются целиком из URL-знаков; стенд гоняет четыре обрывка живого вида, живой подъём отдал 450 знаков цельной ссылкой
- заброшенный вход не снимается проактивно: loginRunTTL проверяется только лениво при следующем клике «Войти», фонового снятия по сроку нет (в отличие от других тикеров сервера). Открыл ссылку и не вернулся (телефон убрали, вкладку закрыли) -> tmux-сессия login-N и живой клиент в ней висят бессрочно. Нужен периодический sweep, снимающий просроченный loginRun сам, а не только при повторном заходе: исправлено, loginKeeper тикает минутой рядом с quotaKeeper и снимает просроченный вход сам; живая проверка сняла сессию через двадцать секунд после срока и записала в журнал «срок истёк: к ссылке не вернулись», стенд TestClientLoginSweepDropsExpired
- раздел «Приёмка» не дописан: «барьер «глаза»:» стоит без фразы и без перебора трёх обходов (утверждённый макет, headless-браузер, тест по разметке), а «Сценарий проверки» в файле задачи вовсе отсутствует. По ACCEPTANCE.md это работа исполнителя вместе со сценарием, до Check ворота move check это не пропустят: исправлено, «Приёмка» дописана фразой барьера «глаза» и тремя строками перебора обходов по ACCEPTANCE.md, «Сценарий проверки» стоит перед «Ревью» и прогнан на выкаченном экземпляре, итог записан разделом «Проверка»
- замечание 1 не снято до конца: loginLinkOf после патча 0dfe6fec/81db00d7 клеит следующую строку панели к найденной ссылке без всякой проверки родства, если между ними нет пустой строки, а строка целиком состоит из URL-знаков. Живьём прогнал loginLinkOf на панели с двумя разными https-ссылками подряд без пустой строки между ними: склеились в одну кашу; так же склеилась ссылка со следующей строкой-хэшем без пробелов. Оба живых стенда (TestClientLoginWrappedLinkJoins, TestClientLoginHardWrappedLinkJoins) держат только настоящий вид панели с пустой строкой после ссылки, обратный случай не проверен нигде. Нужна проверка результата перед отдачей (url.Parse, один хост, разумная длина) или явная граница склейки, а не тихое поглощение любой похожей строки: исправлено, обрывок узнаётся по колонке разрыва, а не по одним URL-знакам: строка с чужой схемой, строка шире колонки и хвост, за которым стоит контент вместо пустой строки, обрывками не считаются, а собранная ссылка перед отдачей меряется разбором (одна схема, живой хост, потолок длины). Стенды TestClientLoginSiblingLinkNotGlued и TestClientLoginHashAfterLinkNotGlued краснеют на прежнем коде поодиночке; живая панель подтвердила вид: пять обрывков по 80 знаков, хвост в 50 и пустая строка за ним
- замечание 2 закрыло только брошенную сессию, но открыло новую дыру: Started у loginRun не обновляется никогда, и фоновый loginSweep рвёт живой возврат человека, если тот провозился с мобильным OAuth и 2FA дольше десяти минут. Живым тестом (клиент ставит время на TTL+5с, зовёт loginSweep, затем шлёт верный код) подтвердил: код входа отклоняется 409 'вход ещё не поднят', хотя пользователь ничего не бросал и код верный. Плюс собственный шаг 3 'Проверка' в этом же файле поймал соседний провал того же класса живьём: рестарт демона (штатный шаг выката) стирает loginRun из памяти, tmux-сессия login-N остаётся сиротой без учёта, и исполнитель снимал её руками. Обе дыры нужно закрыть до Check: не рвать сессию, если по ней идёт активный запрос (или продлевать TTL по активности), и не терять счёт сессии входа через рестарт демона (переживающий рестарт учёт или снятие всех login-N при старте): исправлено, срок идёт от последнего признака жизни (его ставят повторная отдача ссылки и попытка кода) и поднят с десяти минут до получаса: он мерян дорогой через телефон со вторым фактором, а не терпением за своей машиной. После перезапуска служба узнаёт живую сессию входа по панели со ссылкой, а прочие login-N снимает сиротами. Живьём: перезапуск экземпляра 7131, следующий заход получил ту же login-1 и ту же ссылку в 450 знаков, журнал записал «сессия входа login-1 узнана после перезапуска службы». Стенды TestClientLoginSurvivesMobileOAuth, TestClientLoginAliveFromLastTouch, TestClientLoginAdoptedAfterRestart и TestClientLoginOrphanSweptAfterRestart краснеют на прежнем коде поодиночке. Заход прежнего исполнителя держал признаком жизни любой открытый поток уведомлений; это снято: вкладка дашборда открыта сутками, и сессия входа при таком счёте становилась бессмертной, отменяя фоновую уборку замечания 2
- по содержанию (не по пометке): раздел «Приёмка» и сценарий формально дописаны, но за барьером «глаза» не осталось ни одного шага для человека. Барьер назван словами «читается ли ссылка авторизации целиком и стоит ли код отдельным полем», а перебор сам же говорит, что headless-замер это закрывает целиком ('годится, цельность ссылки и раскладку полей... мерит агент'), и шаг 4 сценария назван 'агентская мера' без остатка. У соседней задачи DK-466, тот же день, тот же барьер «глаза» на той же плашке, закрытие держит отдельный раздел «Что остаётся за глазами пользователя» с прямыми словами, что именно человеку ещё смотреть. У DK-577 такого раздела нет вовсе, и по факту вида mixed сценарий читается как agent целиком. Либо дописать, что именно остаётся глазам человека (по образцу DK-466), либо не изображать mixed там, где перебор сам снимает барьер целиком: исправлено, за барьером «глаза» встал шаг человека по образцу DK-466: раздел «Что остаётся за глазами пользователя» в конце «Проверки» называет, что человеку смотреть при живом входе с телефона (попадает ли палец в ссылку и в поле кода, не мешает ли десятистрочная ссылка добраться до поля, доносит ли клавиатура код, хватает ли терпения дороге со вторым фактором). Перебор обходов в «Приёмке» больше не изображает, что headless снимает барьер целиком: за ним числа, за человеком читаемость. Сценарий проверки получил пятым шагом меру человека
- проверил снятие замечаний 4, 5, 6 живьём (коммиты 93f67028, 1a8fcfd7). Замечание 6 закрыто по существу: «Что остаётся за глазами пользователя» называет настоящую работу для человека, барьер «глаза» в «Приёмке» больше не выдаёт headless-замер за полное снятие барьера, сценарий получил явный пятый шаг человека. Наследство упавшего захода (признак жизни по открытой вкладке уведомлений) снято чисто: notifications.go и server.go не отличаются от состояния до этой правки (сверил git diff f32e5f79 HEAD, в server.go только чужой несвязанный маршрут из параллельного коммита), в clientlogin.go следов notif/tab нет
- замечание 4 снято не до конца: колонка разрыва и итоговая проверка url.Parse ловят прежние случаи (соседнюю ссылку, строку-хэш с непустой строкой после), но остаётся дыра ровно там, где ссылка укладывается в колонку без короткого хвоста. Тогда последний настоящий обрывок той же ширины, что и остальные, и код (loginLinkOf, ветка 'len(piece) <= torn' без хвостовой ветки) продолжает собирать дальше без всякого требования пустой строки после. Живым тестом (loginLinkOf на панели: два полных обрывка ровно по 40 знаков плюс третья чужая строка той же ширины из URL-знаков без пустой строки между ними) подтвердил: чужая строка приклеилась к ссылке. loginLinkCheck это не ловит. Схема одна, хост живой, длина в пределах, и склеенная ссылка выглядит синтаксически валидной. Нужно требовать пустую строку после ЛЮБОГО последнего обрывка, а не только короче колонки, либо запоминать колонку по количеству переносов и проверять, что дальше идёт пустая строка независимо от ширины хвоста: исправлено, склейка теперь требует пустой строки за собой при любой ширине последнего обрывка. Обрывок короче колонки приклеивается, только когда следом стоит пустая строка. Склейка обрывков полной ширины, за которой пустой строки не видно, не отдаётся вовсе: первый кусок в таком случае молчит вместо того, чтобы сойти за целую ссылку, и поллинг подъёма спрашивает панель снова через полсекунды. Стенд TestLoginLinkOfGlue держит пять видов панели таблицей, в том числе чужую строку той же ширины без пустой строки за ней, и краснеет на прежнем коде
- замечание 5 снято не до конца: живая проверка возврата дольше срока (TestClientLoginSurvivesMobileOAuth) и учёт после рестарта (TestClientLoginAdoptedAfterRestart) настоящие, срок от Alive и повышение до получаса закрывают race с живым возвратом. Но само узнавание после рестарта (loginRecover) опознаёт сессию только по имени login-N, возрасту и тому, что в панели нашлась хоть какая-то ссылка. Ничего, что привязывало бы сессию к самому дашборду, тут нет. Живым тестом (tmux ls отдаёт login-1 с настоящей ссылкой входа и login-2 младше с посторонней https-ссылкой в панели, обе моложе TTL) подтвердил: loginRecover взял login-2 (чужую, с посторонним адресом) как живой вход и снял login-1 (настоящую сессию с ссылкой авторизации) как сироту. Сортировка по времени рождения отдаёт предпочтение более новой сессии независимо от того, дашборд ли её поднял. На живой машине это не только теоретический сосед: login-N мог остаться от упавшего захода и потом наполниться посторонним текстом, а туда же после узнавания уйдёт код входа нажатиями. Нужна привязка сильнее имени и содержимого панели: метка процесса, файл-маркер рядом с сессией или хотя бы отказ от узнавания при двух живых login-N разом вместо выбора одной по времени: исправлено, узнавание опирается на след дашборда вместо вида панели. Поднятая сессия сразу метится переменной окружения tmux DEVKIT_LOGIN_OWNER, значение это путь конфига экземпляра. Метка ставится тем же подъёмом и живёт ровно столько, сколько сессия. Перезапуск службы она переживает, а боевой экземпляр от POC на одном tmux-сервере отличает. Непомеченную сессию loginRecover не усыновляет и не снимает: чужое дело чужое. Не пометившаяся сессия снимается сразу на подъёме, ничьих не остаётся. Стенд TestClientLoginStrangerNotAdopted ставит рядом свою login-1 и постороннюю login-2 помоложе со ссылкой в панели и краснеет на прежнем коде. Живьём на 7131: посторонняя login-2 поднята рукой, после перезапуска службы заход получил свою login-1 с ссылкой в 450 знаков, посторонний адрес в ответ не попал, login-2 осталась нетронутой
