# DK-584: Снимок квоты встаёт без разбивки по моделям и молчит об этом

Цель: [tasks/DK-584.md](tasks/DK-584.md)

## Что происходит

Съёмщик панели `/usage` уносит кадр без разбивки по моделям. В снимке стоит
одно `week_all`, а строка `Current week (Fable)` в панели есть: пользователь
показал её снимком экрана 2026-08-29 в 13:29, там 9% потрачено. Пересъёмка в ту
же минуту дала снимок опять без неё.

Разбор DK-574 записал в файл задачи, что клиент перестал рисовать разбивку и
тянет её отдельным запросом. Первая половина вывода неверна, и её надо снять:
рисует.

Панель приезжает не одним кадром. Сначала общий недельный бакет, разбивка
позже. Съёмщик это учитывает выдержкой `usagePartialGrace` в три секунды, и
выдержки не хватает. Кадр уносится неполным, отказа нет, кадр экрана при удаче
не сохраняется, жаловаться нечему.

## Чего хотим

Снимок несёт разбивку по моделям, когда панель её показывает. Неполный снимок
называет себя неполным.

## Развилки

Чем мерить ожидание. Выдержка временем это гадание. Панель сама говорит, что
разбивка обещана либо недоступна («Per-model breakdown unavailable»), и на этом
можно строить ожидание вместо счёта секунд.

Что делать с честно неполным снимком. Отказ клиента про частоту обращений это
законный случай, разбивки в кадре нет вовсе. Такой снимок надо принимать, но
помечать, и `pick` должен говорить «разбивки нет» отличимо от «её не рисовали».

Сколько ждать сверх. Съёмка сейчас занимает 25 секунд потолком. Ожидание
разбивки этот потолок поднимет, и надо решить, насколько.

Первая развилка закрыта словом самой панели. Пока клиент тянет цифры, внизу
висит `Refreshing`. Запрос сел, строка пропала, и на её месте оказывается либо
разбивка по моделям, либо `Per-model breakdown unavailable`. Ожидание кончается
на этом. Досчитанным кадр считается с двух подряд. `capture-pane` снимает экран
в любой момент и может застать панель на середине перерисовки. Заодно нашлась
третья строка, `Showing last-known usage`. Ею панель подписывает цифры прошлого
раза. Свежий момент снятия над старыми цифрами это молчащая ложь, поэтому такой
кадр в снимок не идёт вовсе.

Вторая развилка закрыта пометкой в самом снимке. Строка
`partial = week_max: панель отказала по частоте обращений` называет бакет и
причину словами панели. Та же причина едет в хвост вердикта `pick`. Пометки нет,
значит панель досчитала и бакета у подписки правда не оказалось.

Третья развилка: потолок поднят с двадцати пяти секунд до сорока пяти. Двадцать
пять отмеряли отрисовку. Теперь ждать приходится и запрос за цифрами. Клиент
даёт запросу пять секунд и повторяет после обновления токена, так что сверх
отрисовки заложены две попытки с запасом на медленную сеть. Без снимка корректор
выключен целиком, поэтому, дойдя до потолка с разобранным общим бакетом, съёмщик
пишет снимок с пометкой.

## DoD

- Снимок на машине пользователя несёт `week_max`, когда панель показывает
  разбивку.
- Неполный снимок помечен, и `agentctl pick` называет причину словами.
- Вывод про пропавшую разбивку снят из файла DK-574 припиской.
- Стенд на кадре с поздней разбивкой падает на прежнем коде.

## Ранг

`25+7+2+5+3 = 42`, P2.

- Серьёзность 25: корректор ставки теряет страховку от перекоса, обход есть.
- Ценность 7: пользователь заметил сам и вернулся к теме дважды.
- Неопределённость 2: подход понятен, открыт признак ожидания.
- Поправка на баг 5: чинили это место вчера и починили наполовину.
- Рычаг 3: снимок кормит выбор модели и корректор ставки.

## Приёмка

- вид: agent

## Ход работы

- Разработка: opus/medium по вердикту pick, 2026-08-29.
- Признак ожидания вытащен из самого клиента 2.1.251: строки `Refreshing`,
  `Per-model breakdown unavailable`, `Showing last-known usage` найдены в
  бинаре, там же видно, что рисует их одна и та же панель по состоянию запроса.
- Кадр `testdata/usage-panel-settled.txt` собран из настоящего кадра
  `usage-panel-partial.txt` снятием строк про пересчёт: живого кадра подписки
  без дорогого бакета под рукой нет, а проверять надо ровно этот случай.

## Сценарий проверки

1. `go test -run 'Panel|Pane|Usage|Quota' ./tools/agentctl`: зелено.
2. `agentctl quota refresh` собранным бинарём: снимок встаёт, а когда панель
   разбивку не показала, в файле стоит строка `partial = week_max: ...`.
3. `agentctl pick DK-584`: хвост причины называет, почему `week_max` в снимке
   нет, а не только то, что его нет.

## Выкат

- 2026-08-29 слито: cfa10fcc, 79c78ff4

## Проверка
