# DK-603: regcheck засчитывает падение сборки базы как красноту теста

## Что происходит

Базовый прогон под --inline переносит в копию main только тестовую часть
файла. Новые тесты зовут продакшн-символы, которых в базе нет (новая функция,
импорт, изменившаяся арность). База с новыми тестами не собирается с ошибками
E0425, E0433, E0061. regcheck засчитывает это как «тест краснеет на main» и
отчитывается успехом. На прогоне XR-202 так прошли ворота, и только ревьювер
своим прогоном выяснило, что один из двух новых тестов зелёный на старом коде:
ложная краснота заменила собой проверку.

### Разбор 2026-09-16

Актуальность: предмет на месте. Второй прогон в `tools/regcheck/check.go`
судит исход одним значением ошибки exec, то есть кодом выхода. Любой
ненулевой код печатается как «тест краснеет на базе». Разбора вывода
компилятора там нет. README в абзаце «Дописывание тестовой части в
конец базового файла» это признаёт и предлагает смотреть глазами. Правок
regcheck после записи черновика две, обе по DK-968 про временные деревья.
Исхода прогона они не касаются. Теста на падение сборки в `check_test.go` нет, все сценарии
гоняют `sh probe_test.sh` и grep.

Дыра не только у `--inline`. Тест, перенесённый флагом `--tests` целиком, тоже
зовёт новые символы и роняет сборку базы тем же исходом.

Дубли: на доске и в архиве строки с этим предметом нет. В накопителе лежат
DK-591 и DK-607. У первого команда тестов не нашлась в PATH, и пустой вывод
сошёл за красноту. У второго присваивание среды после `--` ушло в exec, и
команда не запустилась. Корень у трёх черновиков один. regcheck читает исход
прогона одним кодом выхода и не различает «тест упал», «не собралось» и «не
запустилось».

Стыки: `shipctl merge` (advice.go, regcheckGate) читает журнал `.devkit/log`.
Зелёным он считает только код 0. Отказ regcheck и сегодня выходит ненулевым
кодом, третий исход «не доказано» контракт ворот не меняет.

Второй вариант гипотезы (переносить на базу новые продакшн-символы) не
проходит. Инструменту пришлось бы отличать новое объявление от исправленной
логики в произвольном языке. Логику regcheck обязан оставить старой, а единого
критерия под такое разделение нет.

Условия: доступов, секретов, невозвратных шагов и чужих команд не нашла.
Компилятор в тесте не нужен. Сборку моделирует скрипт-заглушка с выводом
`error[E0425]` и кодом выхода 101.

## Чего хотим

Падение сборки базы должно считаться неубедительным исходом (отказ ворот).
Другой путь: regcheck переносит на базу и новые продакшн-символы, на которые
ссылаются тесты.

## Развилки

- «кейсы»: какие пользовательские кейсы у DK-603? рекомендую сбалансированный: узкий теряет кейс (4), ту же дыру у --tests, которая чинится тем же местом кода, а широкий тянет кейс (5) из соседних черновиков DK-591 и DK-607, про них отдельный вопрос
  - решает: человек
  - рекомендация: сбалансированный состав, 4 кейса: (1) инлайновый файл, новые тесты зовут новые продакшн-символы, база с новыми тестами не собирается, regcheck отказывает с причиной вместо успеха; (2) тест на базе собирается и краснеет, успех как сейчас; (3) тест на базе собирается и зелёный, отказ как сейчас; (4) тест перенесён флагом --tests целиком и роняет сборку базы новым символом, тот же отказ
  - вариант: узкий состав, 2 кейса: (1) инлайновый файл, база с новыми тестами не собирается, отказ вместо успеха; (2) тест на базе собирается и краснеет, успех как сейчас
  - вариант: широкий состав, 5 кейсов: (1) база с новыми тестами не собирается, отказ вместо успеха; (2) тест собирается и краснеет, успех; (3) тест собирается и зелёный, отказ; (4) тест через --tests роняет сборку базы, отказ; (5) команда тестов не нашлась или не запустилась (DK-591, DK-607), отказ с причиной «не запустилось»
  - решено человеком 2026-09-16: сбалансированный состав, 4 кейса: (1) инлайновый файл, новые тесты зовут новые продакшн-символы, база с новыми тестами не собирается, regcheck отказывает с причиной вместо успеха; (2) тест на базе собирается и краснеет, успех как сейчас; (3) тест на базе собирается и зелёный, отказ как сейчас; (4) тест перенесён флагом --tests целиком и роняет сборку базы новым символом, тот же отказ
- «различение»: чем отличать падение сборки и незапуск команды от падения теста?
  - решает: исполнитель
  - рекомендация: судить по выводу известных раннеров: у cargo это «error: could not compile» и «error[E», у go test это «[build failed]» и «setup failed»; незапуск это ошибка exec без кода выхода (ENOENT); неизвестный раннер без маркера падения теста считать неубедительным и печатать выжимку
- «след»: нужен ли воротам merge отдельный след третьего исхода?
  - решает: исполнитель
  - рекомендация: отказ выходит ненулевым кодом, как и нынешний отказ «зелёный на старом»; regcheckGate в shipctl читает только код 0 как зелёный, и контракт журнала .devkit/log не меняется
- «исход»: что regcheck делает, когда тест на базе не собирается? рекомендую отказ: перенос символов требует отличать новое объявление от исправленной логики в произвольном языке, единого критерия под это нет, а исправленную логику regcheck обязан оставить старой
  - решает: человек
  - рекомендация: отказ ненулевым кодом с причиной «база не собралась» и подсказкой выхода: тест без новых символов либо запись в файле задачи, что regcheck здесь не применим
  - вариант: переносить на базу и новые продакшн-символы, на которые ссылаются тесты
  - решено человеком 2026-09-16: отказ ненулевым кодом с причиной «база не собралась» и подсказкой выхода: тест без новых символов либо запись в файле задачи, что regcheck здесь не применим

## DoD

- Базовый прогон, который не собрался или не запустился, regcheck не
  засчитывает краснотой. Утилита отказывает ненулевым кодом и называет
  причину: сборка или запуск. Рядом выжимка вывода и подсказка, что делать
  дальше. Выходов два: тест без новых символов либо запись в файле задачи,
  что regcheck здесь не применим. Так же отказ выходит у теста, перенесённого
  флагом `--tests` целиком, когда он роняет сборку базы.
- Тесты в `check_test.go` с заглушкой компилятора покрывают четыре кейса из
  развилки «кейсы». Тест на падение сборки падает на старом коде и проходит на
  новом. На старом коде утилита отчитывалась успехом.
- Коммит README с ветки `dk-603-readme` едет в задачу: «тестовый регион» в
  README стал «тестовой частью» файла.
- README: абзац «смотреть глазами» заменён описанием отказа и его причин.

## Ранг

`50+7+2+5+4 = 68`, P1.

- Серьёзность 50: конвейер работает, но ведёт не туда. Ворота XR-202 прошли
  на ложной красноте. Один из двух новых тестов оказался зелёным на старом
  коде, и поймал это ревьювер ручным прогоном.
- Ценность 7: regcheck гоняется на каждой правке с тестами, много раз в день.
  Честность его вердикта решает, чему верят ворота.
- Неопределённость 2: подход выбран, отказ вместо успеха. Открыты детали, чем
  отличать падение сборки от падения теста у разных раннеров.
- Поправка на баг +5: главная функция утилиты это доказать красноту, а она
  отчитывается успехом без доказательства.
- Рычаг 4: тестовые ворота. От них дешевле и надёжнее каждая следующая задача.

## Ход работы

- Из черновика DK-603 «regcheck засчитывает падение сборки базы как красноту теста»: записан 2026-08-29, оформлен 2026-09-16.
- Разработка: субагент fable/medium по вердикту pick (маппинг opus, корректор: профицит week_all, week_max; квота: week_all 22% профицит, week_max 34% профицит, снимок 11м назад), 2026-09-16 12:04-12:04.
