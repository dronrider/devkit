# DK-606: LLD: приёмы прозы по типу страницы, разметка и счёт переживают правку скиллом prose

## Что происходит

29 августа пользователь показал снимок раздела README до и после правки
скиллом prose. Три буллета с жирными лид-инами стали сплошными абзацами. Счёт
в прозе сбит, стоит «вторая беда», следом «третья», а первая нигде не названа.
Времена смешаны. Абзац открывается словом «раньше», глаголы стоят в настоящем,
хвост абзаца снова в прошедшем. Второй случай пришёл от коллеги, там правка
срезала таблицы. Замер корпуса эталонов объясняет оба. В kit/skills/prose/corpus
жанр readme это 15 фрагментов и 3 строки перечисления на 204 строки. Таблиц и
подзаголовков нет ни в одном из четырёх жанров, 0 на 1024 строки.

Скилл сам говорит, что примеры держат форму сильнее запретов. В эталонах одна
сплошная проза, и агент приводит к ней чужой текст. Сносится при этом
навигация. README и справочник читают по диагонали, таблицу читают по столбцу,
а после правки читателю приходится читать абзац целиком. Заодно после правки
нарушены связность, счёт перечисления и время глаголов. Сторож
hooks/check-prose.py и тринадцать пунктов вычитки этого не видят. Они меряют
частоты внутри фразы, а сломана страница. Оба случая нашёл человек глазами, и
другого способа найти такое сейчас нет.

### Что нашёл грумминг

Замер корпуса подтверждён: таблиц и подзаголовков 0 на 1024 строки, в жанре
readme три строки нумерованного перечисления. Разбор скилла дал шесть находок.

1. Единица стиля в скилле это фраза. Пять примет горячего списка и двенадцать
   пунктов вычитки говорят про предложение и абзац, про страницу ни одного.
2. Формат корпуса исключает структуру сам: «тело фрагмента, один или два
   абзаца». Источники это реплики чата и issue трекеров. Расширение корпуса
   без правки формата даст те же абзацы.
3. Жанр выбирается точкой вызова, а не текстом. Под `readme` попадают входная
   страница, справочник флагов и таблица команд, набор закрыт четырьмя
   файлами, а полный перечень типов документов заранее не известен.
4. Режима правки готового текста нет. Выборка берётся до первой фразы. У
   proofread раздел «Что не трогает» разметку и структуру не называет.
5. `hooks/check-prose.py` выкидывает таблицы и заголовки до счёта (строки 49 и
   83), сбитый счёт и спор времён он не увидит.
6. Вся выборка это сплошная проза, а примеры держат форму сильнее запретов.
   Оговорка словами проигрывает примерам.

Пользователь на грумминге: типы текстов разные, весь их набор неизвестен,
элементы стиля надо скорректировать на каждый тип; корпус расширять добычей
из интернета с выбором по типу документа. Решено вести дизайном.

### Свежие правки скиллом с 27 августа

Разобраны коммиты devkit с 2026-08-27 по 2026-08-30, где звались prose или
proofread либо правилась проза: README утилит, RULES, скиллы, LLD, файлы
задач (DK-525, DK-522, DK-523, DK-524, DK-548, DK-530, DK-366, DK-583,
DK-584, DK-605, DK-581, DK-585, DK-577, DK-428), больше тридцати коммитов.

- Ни в одном коммите строки списков, таблиц и заголовков в минусе не
  превышают плюса, лид-ины `- **` не снимались нигде. Правки шли внутри
  абзацев, структура прибавлялась (agentctl/README.md, proofread/SKILL.md).
- Оба случая потери структуры лежат вне гита devkit. Снимок 29 августа это
  README чужого проекта автотестов (раздел «Проблема, которую это решает»,
  сессия 2cc76dac, скриншот в загрузках дашборда): три буллета с лид-инами
  стали двумя абзацами, счёт «вторая беда, третья» без первой, «это» трижды в
  соседних фразах, «раньше» при глаголах в настоящем. Тире сняты правильно,
  перестройкой. Срезанные таблицы у коллеги известны со слов, диффа нет.
- Один дефект по существу внутри devkit: `8421ff09` (DK-428), правка двух
  строк ради порога сторожа «а не». «Парсер читает грамматику, а не имена»
  стало «Парсер знает грамматику, список имён живёт в таблице» (одушевление
  вопреки ядру), «конечный источник максимума, а не ближайший сосед» стало
  «задача, чей ранг стал максимумом, даже если она держится через цепочку».
  Утверждение переписано ради метрики, а не ради читателя.

Следствие для дизайна. Тексты devkit, куда скилл заходил, почти сплошь
абзацные, и там ему нечего сносить; ломается чужой текст с живой структурой,
то есть ровно тот случай, под который корпус эталонов не собран. Второй класс
дефекта, подгонка смысла под порог сторожа, к структуре не относится, но
показывает тот же механизм: правило фразы исполняется в ущерб странице. Обе
находки идут в развилку про инвариант правки готового текста.

## Чего хотим

После правки скиллом prose разметка исходного текста остаётся на месте,
меняются формулировки внутри пунктов. Сбитый счёт и спор времён ловит сквозная
проверка после переписывания, и находка приходит в тот же заход.

## Развилки

Правится корпусом (фрагменты readme с живым перечислением и таблицей) или
оговоркой в SKILL.md о том, что разметка не предмет мимикрии? Второй вопрос про
режим захода. Скилл написан под письмо с нуля, выборка берётся до первой фразы,
а оба случая это переписывание готового текста со своей неприкосновенной
структурой. Нужен ли этому режиму свой раздел. Третий вопрос про раскладку
приёмов по типу документа. Хватает ли одного слоя про страницу поверх общего
слоя про фразу или приёмы делятся по жанрам.

- Ключ раскладки приёмов: имя типа документа или режим чтения (по диагонали,
  по столбцу, подряд, по шагам), в который новый тип попадает без правки
  скилла.
- Где живёт инвариант правки готового текста «структура исходника это данные,
  а не стиль»: в prose, в proofread или общим правилом.
- Как корпус везёт список и таблицу: поле формы у фрагмента, отдельный жанр
  или правка формата, и как отбирается добыча из интернета под тип.

Развилки закрыты документом `docs/lld/DK-606-prose-structure.md`, решения
1-4. Там же названы четыре слоя правки прозы и нарезка работы шестью строками.

## DoD

- В `docs/lld/DK-606-prose-structure.md` приняты решения по трём развилкам с
  отвергнутыми вариантами, пользователь согласовал документ.
- Названы слои: приёмы фразы (как сейчас), приёмы страницы по ключу
  раскладки, инвариант правки готового текста и проверка страницы после
  переписывания (счёт, времена, сохранность разметки).
- Работа по корпусу, скиллам и сторожу нарезана строками доски со ссылкой на
  решения документа.

## Ранг

`25+6+3+0+4 = 38`, P2.

- Серьёзность 25: снесённую структуру ловит человек глазами, обход дешёвый
  (откатить правку).
- Ценность 6: правка долгоживущего текста идёт каждый день, страдает
  навигация README и справочников.
- Неопределённость 3: подход понятен, три развилки открыты, потому LLD.
- Поправка на баг 0.
- Рычаг 4: снимает просмотр глазами с каждой правки текста агентом.

## Приёмка

- вид: user
- барьер «согласие»: решения LLD меняют скиллы prose и proofread, корпус
  эталонов и хуки записи
  - обхода нет по определению: субагент-критик даёт находки, согласие с
    решениями даёт пользователь

## Ход работы

- Из черновика DK-606 «списки, таблицы и счёт переживают правку скиллом prose»: записан 2026-08-30, оформлен 2026-08-30.
- Разработка: субагент opus/xhigh по вердикту pick (квота: week_all 72%, week_max в снимке нет, снимок 22м назад, сдвига нет), 2026-08-30 00:46-00:46.
- Написан `docs/lld/DK-606-prose-structure.md` в ветке dk-606, 2026-08-30:
  четыре решения с отвергнутыми вариантами, слои, нарезка шестью строками,
  заголовки решений в `docs/map.md`.
- Вычитка: 1 файл, 4 правки, 3 пометки, 2026-08-30.

## Ревью

- Решение 4, механика хука: обе опоры ложны. «Рядом с check-prose.py встаёт check-structure.py на том же событии записи. До записи файл на диске это «до»» не сходится с фактом: check-prose.py висит на PostToolUse (devkitctl.py:265, матчер Edit|Write|NotebookEdit), к моменту хука на диске уже «после», а hookio.parse_write (hookio.py:143-147) собирает только new_string, content и new_source, то есть записанный фрагмент, и old_string не читает вовсе. Считать разметку фрагмента против целого файла значит давать убыль всех счётчиков на каждой мелкой правке. Сторона «до» в событии есть, но в tool_response.originalFile (hooks/testdata/claude-code/write-edit.json), и берётся она своим разбором, а не write_event. Вторая опора: «ручной запуск сравнивает рабочую копию с HEAD, тем же порядком, что у check-symbols.py» неверна, у check-symbols.py ручной режим это скан файла целиком (check-symbols.py:108-110), слова git в нём нет, а дифф по staged делает pre-commit:21-27, и это не сравнение с HEAD. Назвать источник обеих сторон явно.: исправлено, исправлено: решение 4 переписано по HOOK_LAYOUT и hookio.claude_code_write, стороны берутся из события (originalFile и применённая правка), write_event отвергнут явно, ручной режим это --cached против индекса с шагом в pre-commit
- Решение 4 и строка нарезки про хук не доводят сторож до пользователя. Новый рубеж не встаёт от того, что файл лежит в hooks/: по hooks/README.md:545-558 категория заводится тройкой (имя скрипта в кортеже POST_SCRIPTS, строка в HOOK_LAYOUT, запись у доктора), раскладку кладёт devkitctl doctor --fix, и без этой тройки хук не сработает ни у кого, включая автора. Отдельно про конфиг: полноту kit/prose.toml доктор спрашивает у самого check-prose.py --config (devkitctl.py:1952-1972), поэтому новая секция под режим check-structure.py доктору не видна и ей нужен свой канал. Ни решение, ни строка нарезки этого не называют.: исправлено, исправлено: в решении 4 названа тройка (POST_SCRIPTS, HOOK_LAYOUT, hook_gaps) с раскладкой doctor --fix, у check-structure.py свой --config и вторая проверка в devkitctl по образцу check_prose_config, всё это в строке нарезки про хук
- Решение 4 противоречит себе на счёте и временах и не закрывает случай, с которого задача началась. Отвергая «ловить структуру вычиткой», документ пишет: «Оба случая нашёл человек глазами, и вычитка при переписывании README не звалась». Тем же абзацем счёт перечисления и время глаголов отданы proofread. Вычитку долгоживущего текста перед коммитом обязывает уже ядро (RULES.core.md:75, RULES.md:69 третьим удержанием), и в разобранном случае это не помогло: заход шёл со скиллом prose, вычитка не звалась. «Чего хотим» файла задачи требует находку «в тот же заход». Кто делает проход счёта и времён в заходе prose, документ не называет: решение 2 упоминает раздел «Правка готового текста» только ссылкой «проверка после (решение 4)», а по своему же правилу («Читает текст субагент со свежим контекстом, не тот, что писал текст») пишущий сделать проход сам не может. Строка нарезки про prose вызова вычитки тоже не содержит.: исправлено, исправлено: в решении 4 абзац «Кто зовёт проход в заходе prose»: раздел «Правка готового текста» кончается вызовом proofread последним шагом того же захода, исходник субагент берёт из git diff HEAD; вызов вписан в строку нарезки про prose и в канал выборки в решении 2
- Число пунктов вычитки устарело, и опора взята не из репозитория. Документ четырежды говорит «двенадцать пунктов вычитки» (строки 35, 73, 235 и «Двенадцать пунктов остаются про фразу, нумерация их не меняется» в строке 240). В kit/skills/proofread/SKILL.md пунктов тринадцать с коммита b730ca54 (DK-366, блатная и уголовная лексика), это же число стоит во фронтматтере. Двенадцать осталось только в установленной копии ~/.claude/skills/proofread/SKILL.md, на которую документ и ссылается в «Живом материале» (строка 37) и которая от репозитория отстала. Ссылаться надо на коммитимый kit/skills/proofread/SKILL.md, как на него ссылается RULES.md:69, и поправить число во всех четырёх местах.: исправлено, исправлено: тринадцать пунктов во всех местах, ссылка в «Живом материале» на kit/skills/proofread/SKILL.md с оговоркой про отставшую установленную копию
- Решение 3, добыча из интернета: команда fetch <url> упирается в разрешения, и документ этого случая не закрывает. При сборке корпуса сеть была отбита два захода подряд, оба раза записано в DK-522: «Сходить в трекеры за новым материалом. Сеть закрыта разрешениями второй заход подряд, curl и WebFetch отбиты» (docs/tasks/archive/2026/DK-522.md:128-129) и «Сети в заходе не было: gh, curl, WebFetch и WebSearch отбиты» (:561-562). Страницы третьего и четвёртого источника пользователь тогда передал текстами. В такой раскладке fetch <url> молчит, а по ядру («случай, когда фича бездействует из-за нехватки данных или окружения, продумывается отдельно, молчание неотличимо от штатной работы») это надо назвать: либо вход текстом или файлом рядом с url, либо внятный отказ.: исправлено, исправлено: у fetch два входа, url либо --file с --url для поля источника, без сети и без файла отказ с подсказкой и кодом 2; случай DK-522 назван в «Живом материале» и в «Нештате»
- Решение 3 не называет два теста корпуса, которые добыча задевает. kit/skills/prose/prose_test.py:708-727 запирает ровно четыре вида источника фрагмента (журнал сессии, трекер, страница проекта, файл задачи), а соседний тест :729-747 требует поле «обезличен: да» у всего с источником «страница проекта». Блок, добытый командой fetch со страницы, ложится именно в этот вид и обязан нести обезличивание, иначе оба теста краснеют. Обезличивание документ упоминает, про сами тесты и про то, какой вид источника получает добыча, не говорит. Заодно уточнить пятое сито: prosecheck считает плотность трёх метрик по порогам kit/prose.toml с минимумом слов, и на коротких ячейках таблицы такой счёт даёт шум, а не суждение.: исправлено, исправлено: добыча ложится в вид «страница проекта» с шапкой от команды, оба теста prose_test.py названы и остаются, обезличен: да ставится после обезличивания до попадания в корпус; prosecheck по таблице не голосует, у списка пункты склеиваются и меряются при min_words
- Решение 3: «Раздел «Формат корпуса» переписывается с «один или два абзаца» на «блок как он стоит в источнике», разбор parse_genre под это уже готов» верно не до конца. HEAD_RE = re.compile(r"^##\s+(.+)$") (kit/skills/prose/prose.py:695) режет фрагмент на любой строке, начинающейся с «## », поэтому блок с подзаголовком внутри развалится на два кривых фрагмента с чужой шапкой. Для таблиц и списков разбор действительно чист, для «блока как он стоит в источнике» нет. Либо сузить формулировку до списков и таблиц, либо назвать правку разделителя.: исправлено, исправлено: формулировка формата сужена до абзацев, списка, таблицы и шагов без подзаголовков внутри, HEAD_RE и причина названы, разделитель не меняется
- Решение 2: у пометки в находке сторожа нет места. Правило «Когда так не выходит, находка остаётся с пометкой, а утверждение не трогается» перенесено из proofread «без изменений», но контексты разные. У вычитки пометке есть выход, раздел «Выход» велит вернуть короткий список помеченного и не исправленного. Находка check-prose.py приходит посреди чужого захода, выхода у неё нет, и документ не говорит, куда пометка ложится и кто её увидит. В режиме warn это значит, что агент молча идёт дальше, а по ядру такое молчание неотличимо от штатной работы. Назвать место записи (ход работы файла задачи, отдельная строка отчёта или что-то ещё).: исправлено, исправлено: в решении 2 абзац о месте пометки: строка «- Сторож прозы: ...» в «Ход работы» файла задачи по образцу следа вычитки, вне задачи в отчёт захода; текст находки велит её записать, строка нарезки про check-prose.py дополнена
- Нарезка, рычаг: проза противоречит таблице и калибровке RANKING.md. Проза говорит «Рычаг 4 у скиллов и хука, они снимают просмотр глазами с каждой правки, 3 у корпуса и текста находки», а в таблице у строки prose рычаг 3 (36 = 25+6+2+0+3), у строки корпуса 4 (36 = 25+6+1+0+4): два числа переставлены против собственного объяснения. Отдельно калибровка RANKING.md:209-211: «4 только у того, что делает дешевле каждую следующую задачу», «Ускорение своего потока по одной теме это 2», «Правка текста скилла или правил без новой механики это 1». Строки prose, proofread и текста находки check-prose.py это правка текста, у них стоит 3-4. Либо пересчитать, либо назвать, почему калибровка тут не действует.: исправлено, исправлено: рычаги пересчитаны по калибровке RANKING.md (хук 4, механика корпуса и добычи 2, тексты скиллов и подсказки 1), проза и таблица нарезки сведены, ценность добычи 5, инвариант зависимости соблюдён
- Нештат, «Чужой проект без хуков devkit. Хук не сработает ... Оба случая августа пришли оттуда» неверно, и на этой посылке держится половина решения 4. Сторож прозы висит в settings.json харнеса машинно (~/.claude/settings.json, PostToolUse, матчер Edit|Write|NotebookEdit), команда это абсолютный путь в дерево devkit, конфиг берётся оттуда же (DEFAULT_CONFIG = os.path.join(ROOT, 'kit', 'prose.toml'), check-prose.py:45), а is_prose (:342-350) отсеивает только по суффиксу и по каталогам testdata и corpus. Никакой привязки к проекту нет: хук уже сейчас срабатывает на каждой записи .md в любом проекте машины, включая тот README из снимка 29 августа (сессия там своя, машина эта же). Значит check-structure.py на том же событии ловил бы оба августовских случая сам, а не «остаётся ручной запуск». Посылку поправить и перечитать, что после этого остаётся у ручного режима.
- Решение 4, сторона «после» собирается лишним и более хрупким путём. Абзац сам говорит «К этому моменту файл на диске уже «после»», а следом велит складывать её заново: «сторона «после» это тот же текст с применённой правкой (old_string на new_string у Edit, content у Write)». Читать filePath с диска и проще, и точнее. Восстановление правкой ломается на replace_all: поле replaceAll в событии есть (hooks/testdata/claude-code/write-edit.json, ключи filePath, oldString, newString, originalFile, structuredPatch, userModified, replaceAll), и одна подстановка при нём даёт не тот текст. Заодно имена полей в событии camelCase (oldString, newString), а не old_string. Назвать источником «после» файл на диске по filePath, а восстановление снять.
- Решение 4, третий элемент тройки для пост-скрипта не работает так, как обещано. Своё сообщение «убыль разметки при записи не ловится» доктор не скажет: в hook_gaps все недостающие пост-скрипты схлопываются в один пробел с общим текстом (devkitctl.py:1299-1302, комментарий «три одинаковые строки про них это один пробел: хуки харнеса не подключены»), а посписочные сообщения лежат в PRE_READ_GAPS (:205) и читаются только для PRE_READ_SCRIPTS (:1339). Тройка из hooks/README.md:554-556 переписана верно по букве, но для POST_SCRIPTS третий её элемент инертен. Либо сказать, что сообщение общее и своего у хука не будет, либо назвать правку hook_gaps как часть строки нарезки.

## Сценарий проверки

1. Прочитать `docs/lld/DK-606-prose-structure.md`, раздел «Задача и рамка»:
   три допущения приняты без вопроса, и с ними надо согласиться или
   поправить.
2. Решение 1: ключ приёмов страницы это режим чтения из четырёх, тип
   документа открытый словарь поверх. Согласие означает, что новый тип
   документа попадает в раскладку строкой таблицы «тип, режим», без правки
   приёмов.
3. Решение 2: формулировка инварианта в два предложения, владелец proofread,
   повтор в выборке prose и в тексте находки check-prose.py. Согласие
   означает, что в ядро правил инвариант не идёт.
4. Решение 3: поля «форма» и «тип» у фрагмента, ключ `sample --form`, добыча
   командой `fetch` по типу и форме через те же сита. Согласие означает, что
   отдельного жанра под таблицы не будет.
5. Решение 4: разметку считает хук `check-structure.py` по диффу записи в
   режиме `warn`, счёт и времена читает проход страницы в proofread.
6. Раздел «Нарезка»: шесть строк с ценой и рангом. Согласие означает, что
   диспетчер заводит их на доске как есть, правка состава или порядка
   возвращает задачу в работу.
