# DK-609: Снятый шаг уходит из плана, а расхождение с делом ловит сторож

## Что происходит

`PLAN_RULE` в `hooks/session-task.py` описывает ведение плана вперёд. Завести
этапы, пометить текущий, закрыть сделанные, писать файл целиком. Исхода
«работа сменилась, шаг больше не нужен» в правиле нет.

Отсюда живой случай 2026-08-30. В кольце диспетчерской сессии висели два шага
многочасовой давности, оба отменены сменой работы. Закрывать их неверно, они
не были сделаны. Правило не разрешает их снимать. Заметил это пользователь
глазами, спросив, что за пункты висят в кольце.

Три дыры, все в самом правиле.

Нет исхода «снято». Отменённый шаг некуда деть.

Нет момента пересмотра. Сказано, когда план завести, и не сказано, когда
перечитывать. У сессии, которая ведёт разговор, работа приходит репликами
человека непрерывно, и план устаревает за полчаса.

Нет сторожа. Расхождение плана с делом не проявляется ничем. Сравнить план
с работой машине нечем, но простые признаки есть: пункт в работе много часов,
все пункты закрыты при живой сессии, план не менялся, пока лента идёт.

Смежное: DK-580 про то, что правило велит массив, а сессии пишут объект.

Разбор 2026-09-09 сверил запись с кодом, и адрес дыры изменился. `PLAN_RULE` в
`hooks/session-task.py:253` про ведение плана больше ничего не рассказывает.
После DK-613 это две строки, которые вызывают `agentctl plan` и отсылают за
порядком в скилл `work-plan`. Значит правится теперь скилл, а не строка хука.

Из трёх дыр записи две скилл закрывает наполовину, третьей нет вовсе.

Про смену работы в «Ритме» сказано пунктом 4: поменялся состав работы, и план
кладётся заново тем же `set`. Совет рабочий ровно до первого захода, где часть
пунктов уже закрыта. `planParse` в `tools/agentctl/plan.go:183` собирает список
целиком из `pending`, состояний прежнего плана `set` не смотрит. Сессия,
снявшая один отменённый пункт, теряет отметки на остальных, и кольцо в шапке
откатывается к нулю посреди работы. Отдельного исхода «снято» у пункта нет.
Состояний в файле три: `pending`, `in_progress`, `completed`.

Момент пересмотра назван только один, в конце. Пункт 5 просит закрыть
сделанное перед возвратом или концом хода. Про то, что план надо перечитать,
когда работа сменилась посреди захода, не сказано ни там, ни в правиле 4
скилла `board-chat`.

Сторожа нет, и это подтвердилось поиском. Каталог `~/.devkit/plans` читают
дашборд (`tools/dashboard/sessions.go`) и доктор, но доктор смотрит на возраст
файла и удаляет планы мёртвых сессий старше месяца
(`tools/devkitctl/devkitctl.py:394`). Расхождение живого плана с делом не
проверяет никто.

Соседи по правилу плана разобраны в других записях и к этой не относятся:
DK-881 (порог повода, стоит строкой в Backlog) прямо оставляет отменённую работу этой записи,
DK-580 про формат файла, DK-880 про сессию без задачи.

## Чего хотим

Скилл `work-plan` получает исход снятого шага, а `agentctl plan` перестаёт
ронять отметки. Отменённый пункт просто не попадает в переложенный план, `set`
переносит состояния совпавших по тексту пунктов, и состояний в файле остаётся
три. Дашборд не трогается. Разбор дашборда считает чужое слово состояния жалобой
(`mark` в `tools/dashboard/sessions.go:2880`), и новое состояние стоило бы
правки в трёх местах вместо одного.

Момент пересмотра в «Ритме» это событие, а не часы. Реплика человека,
сменившая предмет захода, перекладывает план тем же ходом.

Расхождение ловит сторож на конце хода, хуком по образцу `agent-watch.py`.
Считает он три признака черновика с порогами из конфига: идущий пункт старше
порога, все пункты закрыты при живой сессии, план не менялся заданное число
ходов. Сторож сдаёт находку решением `block`, как сдача фоновых работ. Ход не
кончается, пока сессия не переложила план. Строка без блокировки теряется
среди необязательных. Расхождение чинит та же сессия, которой его завели, и
человеку не нужно ловить его глазами через час.

## Границы

Правило плана разбирают ещё три записи, и к этой они не относятся: DK-881 про
порог повода стоит строкой в Backlog, DK-580 про формат файла, DK-880 про
сессию без задачи. Разбор дашбордом чужого состояния не трогается, потому что
нового состояния не заводится.

## Развилки

- «исход снятого»: Чем в плане выражается отменённый шаг?
  - решает: человек
  - рекомендация: снятый пункт просто не попадает в переложенный план, а set сохраняет отметки совпавших по тексту: состояний в файле остаётся три, дашборд не трогается, кольцо не откатывается к нулю
  - вариант: новое состояние «снято» у пункта: правятся утилита, скилл и разбор дашборда, где чужое слово состояния считается жалобой (sessions.go:2880)
  - вариант: только скилл: текст велит при перекладке перечислять закрытые пункты заново, утилита не трогается
  - решено человеком 2026-09-09: снятый пункт просто не попадает в переложенный план, а set сохраняет отметки совпавших по тексту: состояний в файле остаётся три, дашборд не трогается, кольцо не откатывается к нулю
- «момент пересмотра»: Когда скилл велит перечитать план?
  - решает: человек
  - рекомендация: пересмотр привязан к смене работы: реплика человека, сменившая предмет захода, перекладывает план тем же ходом
  - вариант: концом каждого хода: пункт 5 «Ритма» растёт с «закрыть сделанное» до «сверить план с работой»
  - вариант: по времени: пункт, идущий дольше часа, сессия перечитывает
  - решено человеком 2026-09-09: пересмотр привязан к смене работы: реплика человека, сменившая предмет захода, перекладывает план тем же ходом
- «сторож расхождения»: Входит ли сторож расхождения плана с делом в эту задачу?
  - решает: человек
  - рекомендация: сторож отдельной строкой с зависимостью от этой: у него свой предмет (где живёт, чем сигналит, какие признаки считает), и он тянет работу из S в M
  - вариант: тем же заходом: признаки из черновика встают сторожком вместе с правкой скилла
  - вариант: сторожа не заводим: хватает исхода и момента пересмотра
  - решено человеком 2026-09-09: тем же заходом: признаки из черновика встают сторожком вместе с правкой скилла
- «место сторожа»: Где живёт проверка расхождения плана с делом?
  - решает: человек
  - рекомендация: конец хода, хуком по образцу agent-watch.py: расхождение чинит та же сессия тем же заходом, а не человек глазами через час
  - вариант: дашборд: пометка в строке разговора и в кольце, чинит человек репликой
  - вариант: devkitctl doctor: находка задним числом, смотрится редко
  - решено человеком 2026-09-09: конец хода, хуком по образцу agent-watch.py: расхождение чинит та же сессия тем же заходом, а не человек глазами через час
- «признаки расхождения»: Какие признаки сторож считает?
  - решает: человек
  - рекомендация: все три признака черновика, пороги ключами конфига: идущий пункт старше порога, все пункты закрыты при живой сессии, план не менялся N ходов подряд
  - вариант: один признак для начала: идущий пункт старше порога, остальные добираются по находкам
  - вариант: без времени вовсе: только закрытый целиком план при живой сессии, у порогов по часам слишком много ложных срабатываний на долгих прогонах
  - решено человеком 2026-09-09: все три признака черновика, пороги ключами конфига: идущий пункт старше порога, все пункты закрыты при живой сессии, план не менялся N ходов подряд
- «голос сторожа»: Чем сторож говорит о расхождении?
  - решает: человек
  - рекомендация: сдача решением block, как у agent-watch: ход не кончается, пока сессия не переложила план, иначе строка теряется как всякая необязательная
  - вариант: строкой в ход без блокировки: сессия видит, но вольна не чинить
  - вариант: молча в журнал, а наружу через дашборд
  - решено человеком 2026-09-09: сдача решением block, как у agent-watch: ход не кончается, пока сессия не переложила план, иначе строка теряется как всякая необязательная
- «счёт ходов»: Чем сторож помнит, сколько ходов план стоит без правки?
  - решает: исполнитель
  - рекомендация: признак «план не менялся N ходов» считается по реестру сторожка ~/.devkit/runs, где ходы уже считаются: своего журнала рядом с планом не заводить
  - вариант: время правки файла плана против времени последней записи транскрипта: ходов не считаем вовсе

## DoD

- Скилл `work-plan` в «Ритме» называет исход снятого шага и момент пересмотра
  по смене работы.
- `agentctl plan set` переносит состояния совпавших по тексту пунктов, и
  перекладка плана не роняет отметки закрытых.
- Хук конца хода считает три признака расхождения с порогами из конфига и
  сдаёт находку сессии решением `block`.
- Тесты на перекладку и на каждый признак сторожа, прогон стенда `obeycheck`
  по скиллу `prompt-test` на правленом тексте скилла.

## Ранг

`25+6+2+0+2 = 35`, P2.

- Серьёзность 25. Кольцо врёт про живой заход, но конвейер не встаёт: обход
  дешёвый, спросить сессию репликой, что она делает.
- Ценность 6. План ведёт каждый заход длиннее хода, и честное кольцо это
  ежедневная операция диспетчера, а не редкая.
- Неопределённость 2. Место, признаки, голос и исход названы ответами
  грумминга, открытым остался счёт ходов, и он оставлен исполнителю
  рекомендацией.
- Поправка на баг 0. Ронять отметки `set` умел всегда, ничего не ломалось.
- Рычаг 2. Ускоряет свой поток по одной теме: диспетчеру видно состояние
  захода без опроса.

Цена M: утилита с тестами, хук с тестами, текст скилла и прогон стенда
`obeycheck` по скиллу `prompt-test`.

Приёмка агентская. Ни один из шести барьеров не встал. Снятый пункт и
сохранённые отметки проверяются парой вызовов `agentctl plan` с чтением файла,
сторож на синтетическом плане и подменённом событии конца хода, а текст скилла
прогоном стенда.

## Ход работы

- Из черновика DK-609 «Правило плана не знает, что делать с отменённой работой»: записан 2026-08-30, оформлен 2026-09-09.
- Вычитка: 1 файл, 15 правок, 0 пометок, 2026-09-09.
