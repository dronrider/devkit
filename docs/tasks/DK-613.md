# DK-613: Скилл work-plan: план работ сессии ведёт одно правило и утилита

## Что происходит

Правило «веди план работ файлом ~/.devkit/plans/<ID сессии>.json» живёт в пяти копиях. Оно стоит в четырёх kit/agents/exec-*.md и в инструкции задания дашборда (planRule в POC-дереве, tools/dashboard/chats.go). Читает файлы только кольцо прогресса дашборда, в самом devkit читателей нет. Разбор дашборда терпит три формата (массив, объекты с полями stages/steps/items, состояние done наравне с completed). Агенты пишут файл кто как. Замена на TodoWrite проверена 30.08 и не подходит. У сессий на Fable инструмента в наборе нет вовсе (1 транскрипт из 588 за неделю), а у GLM есть. Файлов планов за ту же неделю 153.

Правило есть, а текста, который его задаёт, в правилах devkit нет. Человек, читающий RULES, о плане не узнаёт. Агент получает его приписками, которые панель вырезает из ленты (cutOrderRules) и корпус прозы отсеивает (TAIL_MARKS в prose.py). Пять копий расходятся, формат плывёт, запасной адрес chat-N.json при пустом CLAUDE_CODE_SESSION_ID подпирает GLM-контур.

## Чего хотим

Есть скилл work-plan: один формат файла, адрес по ID сессии из окружения или события хука, у субагента <ID>-sub.json, чужой не перезаписывать, ритм ведения. Утилита пишет и показывает план (show печатает план живой сессии в терминале), формат держат её тесты. Вход в скилл одной строкой в RULES.core.md, exec-* и board-chat отсылают к нему, копий текста нет. Дашборд остаётся одним из читателей, а разбор трёх форматов у него уходит.

## Развилки

- «место утилиты»: утилита это подкоманда taskctl plan или своя по правилу раскладки README «Раскладка»?
  - решает: человек
  - решено человеком 2026-09-06: подкоманда `agentctl plan`. agentctl уже ведёт агентскую сессию (pick, stage, budget) и пишет в ~/.devkit. Своя утилита стоила бы нового бинаря в PATH и правки правила раскладки README
- «чистка планов»: чистка планов кончившихся сессий идёт по возрасту доктором или при снятии сессии?
  - решает: человек
  - решено человеком 2026-09-06: по возрасту, находкой `devkitctl doctor` с уборкой на --fix. Чистка при снятии сессии отвергнута. Сессия умирает и без снятия (обрыв терминала, стоп сторожка), и ловить её смерть негде

## DoD

- В kit/skills есть скилл work-plan: формат файла плана, адрес по ID сессии, файл субагента, ритм ведения; вход в него одной строкой в RULES.core.md.
- Утилита пишет и читает план: команда show печатает план живой сессии в терминале, формат держат её тесты.
- Из kit/agents/exec-*.md текст правила снят, стоит отсылка к скиллу; копий правила в devkit нет (греп по «Веди план работ»).
- Стенд prompt-test: сессия на обеих раскладках ведёт план по скиллу без инструкции к реплике.

## Ранг

`25+6+2+0+4 = 37`, P2.

- Серьёзность 25: правило в пяти копиях, формат плывёт, инструкции вырезаются из ленты; обход есть, инструкции работают.
- Ценность 7: план виден снаружи любой сессии, это наблюдаемость, на которой стоит DK-614; правило читается в правилах.
- Неопределённость 2: подход выбран (скилл, утилита, строка ядра), открыты место утилиты и чистка старых планов.
- Баг 0: новая раскладка, не регресс.
- Рычаг 4: скилл читает каждая сессия и каждый субагент, вход для board-chat и exec-*.

## Ход работы

- Вычитка: 1 файл, 3 правки, 1 пометка, 2026-08-30.

- Из черновика DK-613 «Скилл work-plan: план работ сессии ведёт одно правило и утилита»: записан 2026-08-30, оформлен 2026-08-30.
- Уточнение: вопрос: Где живёт утилита плана работ?; Кто убирает планы кончившихся сессий?, 2026-09-06 12:33-12:33.
- Разработка: субагент opus/medium по вердикту pick (маппинг sonnet, корректор: профицит week_all, week_max; квота: week_all 39% профицит, week_max 37% профицит (панель разбивку не дала, цифра из кеша клиента возрастом 3ч 2м назад), снимок 19м назад), 2026-09-06 12:33-12:33.
- Снаружи: блок: вопрос: Где живёт утилита плана работ?; Кто убирает планы кончившихся сессий?, 2026-09-06 12:40-13:00.
- Вычитка: 5 файлов, 6 правок, 2026-09-06.
- Разработка: субагент opus/medium, дерево devkit-dk-613, ветка dk-613; шесть коммитов, полный прогон tools/devkitctl/parallel.py зелёный (18 компонентов), 2026-09-06.
- Стенд prompt-test: сценарий 27-work-plan, конец субагент, ярус mini, k 3. Обе раскладки 3/3 зелёные, план кладут все шесть сессий. Пара разводит скилл и строку ядра, а команды плана называет `kit/agents/exec-*.md`, который стенд раскладке не подчиняет. Развести его парой нечем, отсюда вердикт «зелёный на обеих», 2026-09-06.
- Стенд, первый заход: с отсылкой «веди план по скиллу work-plan» без названных команд обе раскладки были красные, плана не клала ни одна сессия. Правило переписано командами в ядре и в определениях исполнителей, после чего прогон позеленел, 2026-09-06.
- Ревью, отработка: два блокирующих замечания закрыты. Потолок ядра вернулся на 5500, правило плана в нём стало одной строкой (ядро 5499 символов). Чужое состояние пункта плана дашборд читает жалобой, регрессионный тест падает на коде до правки. Прогоны rules_test.py, пакета tools/dashboard и check-skills.py зелёные, 2026-09-06.

## Ревью

Уровень 3 до 06be8a0: неопределённость 3 (agentctl pick --role review: sonnet/high); триаж: правка меняет RULES.core.md, читаемое каждой сессией и от которого зависит DoD фичи (да), тронуты hooks из critical_paths (да) -> два да, третий уровень сразу

- [возврат: правила, 2026-09-06] блокирует: потолок ядра tools/devkitctl/rules_test.py::test_core_budgets поднят с 5500 до 5700 (RULES.core.md сейчас 5670 символов), а решение DK-173.2 «Бюджет контекста: ядро не растёт» (docs/lld/DK-173-agent-prose.md, раздел «Решение 2») именно этот сторож и ставило воротами против роста резидента. Довод коммита («план кладут только названные прямо команды», rules_test.py:1923) не подтверждён собственным прогоном задачи: раздел «Проверка» файла задачи показывает пару dk613-with/dk613-without зелёной 3/3 на обеих сторонах с пометкой «текст ничего не меняет: агент сделал шаг и без него», то есть добавленный абзац RULES.core.md на поведение сессии в проверенном прогоне не повлиял (команды уже лежат в kit/agents/exec-*.md и в PLAN_RULE hooks/session-task.py, которые пара не варьирует). Раз абзац не доказал вклад отдельно от exec-*.md, поднимать общий потолок ядра под него нельзя без отдельного решения человека: либо сжать RULES.core.md на компенсирующий объём (уложиться в 5500), либо перепроверить необходимость абзаца стендом, который умеет варьировать exec-*.md, либо явно завести развилку «поднять потолок ядра» с решением человека, как заведены «место утилиты» и «чистка планов» в этой же задаче.: исправлено, правило плана в ядре сжато до одной строки «План: agentctl plan set|step|done, скилл work-plan», ядро 5499 символов, потолок в rules_test.py вернулся на 5500
- [возврат: реализация, 2026-09-06] блокирует: tools/dashboard/sessions.go, planFileItem.mark() больше не переводит старое состояние "done" в "completed" и не жалуется на него (readPlanFile молча ставит pending для непустого текста с незнакомым state, жалоба bad=true выставляется только на нечитаемый JSON). На этой машине живьём в ~/.devkit/plans 55 из 432 файлов содержат хотя бы один пункт {"text":...,"state":"done"} (пример: ~/.devkit/plans/166e2b4c-a997-4743-8f7d-0906dc24e49d.json, все четыре пункта done) - после правки такие пункты в кольце покажутся ждущими без всякой жалобы человеку, тихая порча уже случившегося прогресса. По собственному принципу файла ("другие состояния в файл попадают только правкой руками", sessions.go, комментарий mark()) чужеродное состояние следовало бы заводить той же жалобой, что и объект-план (34 таких файла на машине уже показывают жалобу корректно). Нужно либо считать непустой незнакомый state жалобой наравне с нечитаемым файлом, либо вернуть перевод done -> completed как единственное явно разрешённое legacy-исключение с комментарием почему.: исправлено, чужое непустое состояние пункта (done и любое другое) идёт жалобой bad=true наравне с планом-объектом, перевод done -> completed не вернулся; тест TestPlanFileShape падает на коде до правки

## Сценарий проверки

1. Свежая сессия ведёт план утилитой и он виден снаружи:

   ```bash
   agentctl plan set "разведка
   правка
   коммит"
   agentctl plan step разведка
   agentctl plan show
   cat ~/.devkit/plans/$CLAUDE_CODE_SESSION_ID.json
   ```

   `show` печатает три пункта, первый со значком `[>]`, в файле массив из трёх
   объектов `{"text", "state"}`.

2. Субагент без метки план внешней сессии не портит. В сессии с
   `CLAUDE_CODE_CHILD_SESSION=1` команда `agentctl plan set "работа"` отказывает
   и зовёт `--label`, а с `--label dk613` кладёт `<ID сессии>-sub-dk613.json`.

3. Копий правила в devkit нет. `grep -rn "Веди план работ" ~/projects/devkit
   --exclude-dir=.git` находит только сторож `check-skills.py`, отрез корпуса
   `prose.py` и их тесты, ни одного определения исполнителя и ни одного заказа
   дашборда.

4. Сторож копий работает. Вписать строку «Веди план работ файлом» в
   `kit/agents/exec-low.md`, прогнать `python3 kit/skills/check-skills.py`,
   получить находку «exec-low: несёт копию правила плана», строку убрать.

5. Доктор видит старые планы. Положить в `~/.devkit/plans` файл с mtime
   месячной давности, прогнать `devkitctl doctor`, увидеть находку про планы
   кончившихся сессий, прогнать `devkitctl doctor --fix`, увидеть, что файл убран,
   а свежие планы на месте.

6. Полный прогон тестов devkit зелёный:

   ```bash
   GOWORK=off python3 tools/devkitctl/parallel.py
   ```

## Проверка

- Стенд не зачтён: 2026-09-06 16:14, дерево f67ec3a11, предмет 44d82853, база пусто, ярус mini, k 3, сценарии 27-work-plan, ворота закрыты.

```console
$ obeycheck --task DK-613 --end субагент --agent-def exec-medium --only 27-work-plan -k 3 --base пусто /tmp/dk613-with /tmp/dk613-without
сценарий                                  dk613-with  dk613-without  вердикт
план работ сессии на многошаговой работе         3/3            3/3  зелёный на обеих

итог: сценариев 1, не зачтено 1, база пусто
  27-work-plan: зелёный на обеих, текст ничего не меняет: агент сделал шаг и без него
```
