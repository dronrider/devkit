# DK-614: Скилл board-chat: правила разговора диспетчера живут в devkit

## Что происходит

Дашборд (POC-дерево devkit-poc-chat, tools/dashboard/chats.go:1420-1540) приклеивает к заданию подъёма и резюма чата четыре правила. Это план (planRule), ротация исполнителя (rotateRule), отзывчивость (paceRule) и канал доставки (channelRule). Тот же текст панель вырезает из ленты регулярками (sessions.go cutOrderRules), а prose.py отсеивает его из корпуса (TAIL_MARKS). Разобрано в разговоре 30.08. Правила это поведение сессии, не задание, и носитель у них по правилам devkit другой. Проверено документацией. SessionStart зовётся после каждой компакции и его additionalContext попадает в свежий контекст. Межсессионные сообщения через UserPromptSubmit не проходят. Наборы хуков в домах ~/.claude и ~/.devkit/claude-glm совпадают.

Правила живут в трёх местах (задание, вырезка ленты, отсев корпуса) и ни в одном из них как правила devkit. Сессия из терминала их не получает вовсе. Инструкция повторяется на каждом резюме и жжёт контекст. При пустом CLAUDE_CODE_SESSION_ID нужен запасной адрес плана. Требование пользователя - решение обязано переживать компакцию и работать на GLM. Без этого диспетчер в чате ломается.

## Чего хотим

Скилл board-chat даёт разговор с человеком по задаче, отзывчивость (долгие дела субагенту), отсылки к work-plan и к правилу срока жизни исполнителя (DK-503), соглашение о подписи реплики человека в межсессионном канале без упоминания дашборда. Хук SessionStart с source=compact восстанавливает скилл в контексте. Задание дашборда сводится к «Поговорим про DK-N», инструкции, cutOrderRules и TAIL_MARKS уходят. Доктор проверяет, что хуки во втором доме не отстали.

## Границы

- Порядок дорог доставки реплики (send-keys раньше сокета) в DK-480.
- Сам файл плана и его утилита в DK-613, две меры срока жизни в DK-615.

## Развилки

Хук после компакции вставляет тело скилла целиком или фразу «перечитай board-chat»? Решает стенд prompt-test в обеих раскладках (длинный чат, компакция, реплика панели, ответ человеку текстом в ленту). Начать стоит с фразы, поднять до тела при потере.

## DoD

- В kit/skills есть скилл board-chat: разговор по задаче, отзывчивость (долгие дела субагенту), отсылки к work-plan и к сроку жизни исполнителя, соглашение о подписи реплики человека в межсессионном канале без имени приложения.
- Хук SessionStart с source=compact возвращает скилл в контекст; что именно вставляется (фраза или тело), выбрано по стенду.
- Стенд prompt-test в раскладках claude-code и glm-code: длинный чат, компакция, реплика панели, ответ человеку текстом в ленту и на «вы».
- devkitctl doctor даёт находку, когда набор хуков второго дома отстал от первого.

## Ранг

`25+7+3+0+4 = 39`, P2.

- Серьёзность 25: правила разговора живут в задании дашборда, сессия из терминала их не получает, инструкция повторяется на каждом резюме; обход есть, инструкции работают.
- Ценность 7: диспетчер в чате держит правила после компакции, ноль приписок в ленте, один текст правил.
- Неопределённость 3: подход выбран, развилки в деталях (фраза или тело после компакции, послушание GLM), снимаются стендом.
- Баг 0.
- Рычаг 4: правила разговора для всех поднятых работ, снятие приписок из дашборда стоит на этом.

## Ход работы

- Вычитка: 1 файл, 5 правок, 1 пометка, 2026-08-30.

- Из черновика DK-614 «Скилл board-chat: правила разговора диспетчера живут в devkit»: записан 2026-08-30, оформлен 2026-08-30.
