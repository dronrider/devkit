# DK-615: DK-503: диспетчер меряет продолжаемого субагента токенами, а не часами

## Что происходит

LLD DK-503 (решение 1) меряет срок жизни исполнителя временем с открытия этапа «разработка» через taskctl elapsed (DK-506 закрыта), доводка до exec-* и скиллов стоит в DK-511 и DK-507. Параллельно дашборд везёт инструкцию rotateRule. Она меняет субагента, чей контекст перевалил порог exec_rotate_tokens (~/.devkit/harness.local) по subagent_tokens в уведомлениях. Разобрано 30.08, пользователь согласился на две меры по ролям.

В чате диспетчер держит субагента через SendMessage и досыпает ему куски по ходу разговора. Между кусками субагент простаивает, а часы этапа идут. На рубеже свежий по контексту исполнитель сдаёт хвост, хотя работал минуты. Мера по времени подходит только для одиночного вызова без пауз. Снять rotateRule сейчас нельзя. Другой работающей ротации нет.

## Чего хотим

В решении 1 LLD и в абзаце DK-507 стоят две меры. Исполнитель в одиночном вызове меряет себя временем, а диспетчер продолжаемого субагента меряет токенами по уведомлениям. Сдача хвоста в «Ход работы» и подъём следующего через pick одни на обе меры. Порог живёт в конфиге devkit, инструкция дашборда уходит.

## Границы

Дашборд по-прежнему клеит текст правила ротации к заказу подъёма чата.
DK-614 снимает его вместе с тремя остальными правилами заказа, она же заводит
скилл `board-chat`. Сюда входит только число порога, не инструкция вокруг
него. После слияния правило остаётся на месте по этой причине.

## Развилки

Куда переезжает порог из harness.local дашборда - в ключ конфига devkit рядом с agentctl budget или в строку RULES.board? Как диспетчер просит хвост у субагента перед сменой - репликой, или это шаг exec-*?
- «место порога»: Где живёт порог ротации и какая команда его печатает
  - решает: человек
  - рекомендация: ключ exec_rotate_tokens уже лежит в ~/.devkit/harness.local и читается agentctl, а умолчание 500000 держит дашборд
  - решено агентом 2026-09-06: ключ exec_rotate_tokens остаётся в ~/.devkit/harness.local, умолчание переезжает из дашборда в agentctl, порог печатает новая команда agentctl rotate по образцу budget; вариант со строкой в RULES.board отвергнут, число в прозе командой не читается; вопрос ушёл человеку репликой, ответа не было, а посылка развилки про конфиг дашборда разведкой не подтвердилась
- «запрос хвоста»: Как диспетчер просит хвост у продолжаемого субагента перед сменой
  - решает: человек
  - рекомендация: subagent_tokens видны только диспетчеру, сам исполнитель размера своего контекста не знает
  - решено агентом 2026-09-06: просьба идёт репликой диспетчера: subagent_tokens видны только ему, сам исполнитель размера своего контекста не знает, отдельного шага exec-* просьба не просит; формат хвоста и итоговая фраза берутся из абзаца exec-* по DK-507, реплика их не повторяет

## DoD

- В LLD DK-503 решение 1 дополнено: исполнитель в одиночном вызове меряет себя временем, диспетчер продолжаемого субагента меряет токенами по уведомлениям; сдача хвоста и подъём следующего одни на обе меры.
- Порог токенов живёт ключом exec_rotate_tokens машинного конфига devkit и
  печатается командой agentctl rotate, а умолчание считает agentctl. Дашборд
  берёт готовое число тем же вызовом harness --json и не считает своего
  умолчания.
- Правило диспетчера стоит в скилле доски рядом с вердиктом pick, DK-507 пишет в exec-* обе меры.

## Ранг

`25+5+2+0+3 = 35`, P2.

- Серьёзность 25: продолжаемый субагент в чате хоронится по часам простоя; обход есть, инструкция дашборда держит ротацию по токенам.
- Ценность 5: ротация по факту, а не по календарю, свежий субагент живёт сколько нужно.
- Неопределённость 2: решение принято в разговоре 30.08, открыты место порога и способ запроса хвоста.
- Баг 0.
- Рычаг 4: разблокирует DK-614 и DK-507, оба пишут по одному правилу.

## Ход работы

- Вычитка: 6 файлов, 22 правки, 0 пометок, 2026-09-06.

- Второй пункт DoD переписан по закрытой развилке «место порога». По прежней
  формулировке ключ снимался из harness.local дашборда, а у дашборда вовсе
  нет своего конфига: ключ лежит в машинном конфиге devkit, а agentctl читает
  его. Предмет работы от этого не поменялся, поменялось описание места.

- Прогон `prompt-test` сделан по замечанию ревью. Заведён сценарий
  `tools/obeycheck/scenarios/28-exec-rotate-tail.md`: диспетчеру дают
  синтетическое уведомление с `subagent_tokens` 720000 и два оставшихся шага, а
  судья читает запись решения. Прежний довод про живого субагента за полмиллиона
  токенов снят, уведомление в промпте стоит дешевле живого контекста. Пара
  раскладок собрана генератором, база это прежняя редакция обоих скиллов из
  431192ec4^, ярус mini, k 3. Отметка стенда лежит в разделе «Проверка».

- Что показал прогон. Вердикт «не хуже», 1/3 у кандидата против 2/3 у старой
  редакции, прогон зачтён базой «старый». Разведка k 1 разводила редакции чисто
  (1/1 против 0/1), а на трёх повторах старая редакция дважды угадала исход по
  самому уведомлению: критерий судьи спрашивает про новый субагент для остатка, и
  модель приходит к этому от исчерпанного контекста без всякого правила. Значимой
  разницы k 3 не даёт по построению, и польза текста этим прогоном не показана.

- Вычитка: 1 файл, 4 правки, 1 пометка, 2026-08-30.

- Из черновика DK-615 «DK-503: диспетчер меряет продолжаемого субагента токенами, а не часами»: записан 2026-08-30, оформлен 2026-08-30.

## Ревью

Уровень 2 до 35b8715: до 35b8715e: неопределённость 2, критичные пути shipctl/taskctl/hooks не тронуты, ломающего DoD-пути нет

- [возврат: правила, 2026-09-06] блокирует: правка board-task (шаг 2) и board-batch вводит новую процедуру диспетчера (мера токенами, просьба хвоста по agentctl rotate), повод prompt-test по своему порогу пройден, это признано в «Ход работы». Прогона obeycheck нет, и строки «- Исключение: стенд (причина)» тоже нет: shipctl merge отобьёт слияние пятым воротом на обоих файлах. Довод «сценарию нужен живой субагент за полмиллиона токенов» не обязателен: судья-сценарий может дать дешёвое синтетическое уведомление с subagent_tokens за порогом и спросить, просит ли диспетчер хвост, ярусом mini. Прогнать такой сценарий и записать след, либо вписать exception-строку с честной причиной, иначе merge не пройдёт.: исправлено, сценарий 28-exec-rotate-tail заведён и прогнан, отметка стенда в разделе «Проверка»

## Проверка

- Стенд: 2026-09-06 23:14, дерево 35b8715ea, предмет 2c468928, база старый, ярус mini, k 3, сценарии 28-exec-rotate-tail, зачтён.

```console
$ obeycheck --task DK-615 --only 28-exec-rotate-tail -k 3 --base старый /Users/rider/projects/rules-dk615-with /Users/rider/projects/rules-dk615-old
сценарий                                           rules-dk615-with  rules-dk615-old  вердикт
отдать хвост при контексте исполнителя за порогом               1/3              2/3  не хуже

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  28-exec-rotate-tail / rules-dk615-with / повтор 1: судья: нет
  28-exec-rotate-tail / rules-dk615-old / повтор 3: судья: нет

разбор судьи:
  28-exec-rotate-tail / rules-dk615-with / повтор 1: Цитирую релевантный отрывок: > "Оба оставшихся шага (тест и README) отдаю текущему исполнителю, они связаны и он знает контекст сторожка." В тексте сказано обратное: оставшиеся шаги получит **текущий исполнитель**, а не новый субагент. нет
  28-exec-rotate-tail / rules-dk615-with / повтор 2: Вот цитата из текста: > текущий исполнитель (720k токенов, ход исчерпан) дописывает тест сторожка в текущем окне; README передаётся новому субагенту после коммита теста Эта фраза описывает ровно то, о чём говорит критерий: текущему исполнителю остаётся завершить тест сторожка (хвост работы), а новый...
  28-exec-rotate-tail / rules-dk615-with / повтор 3: # Анализ Критерий ищет описание ситуации, где текущему исполнителю дается задача сдать только "хвост работы", а остаток передается новому субагенту. ## Найденный текст "Текущий исполнитель (720k токенов): дописать тест сторожка и обновить README. Завершить задачу." ## Оценка В записи указано, что те...
  28-exec-rotate-tail / rules-dk615-old / повтор 1: **Цитата из текста:** > 1. Исполнитель 1 завершил разработку сторожка и первые куски работы. Контекст исчерпан (720k токенов, 214 tool_uses). > 2. Исполнитель 2 получит два шага: дописать тест сторожка (регрессионный тест с обкаткой на чистом коде) и обновить README утилиты. Субагент, свежее окно. В...
  28-exec-rotate-tail / rules-dk615-old / повтор 2: **Найденное место:** "Дописать тест сторожка: текущий исполнитель (знает детали). Обновить README: exec-low." Здесь текущему исполнителю поручается дописать тест (он знает детали), а обновление README передаётся exec-low (новому исполнителю). да
  28-exec-rotate-tail / rules-dk615-old / повтор 3: Процитирую релевантный отрывок: **"Исполнитель: две правки отправлены, продолжает: тест сторожка (контрактный раздел с проверкой обкатки) и README (назначение сторожка, примеры вызова). Закрытие через `taskctl close` после проверки."** В этом отрывке сказано, что текущий исполнитель **продолжает** р...
```
