# DK-617: prose: приёмы страницы по режиму чтения, раздел про правку готового текста и перенос из другого формата

## Что происходит

Решения 1, 2 и 4 LLD DK-606 (`docs/lld/DK-606-prose-structure.md`): раскладка приёмов страницы по режиму чтения с открытым словарём типов, раздел «Правка готового текста» с инвариантом «структура исходника это данные» и переносом из другого формата (HTML, вики), вызов proofread последним шагом захода. Перечень блоков открытый: любой не-абзацный блок markdown, fenced-блок вместе с языком (код, mermaid), цитата, инлайн-код и ссылки.

Стенд obeycheck 2026-08-30 (след в файле DK-606): при переносе HTML в md по промпту «перепиши прозой» таблица и список теряются без скилла и со скиллом как есть (0 из 3 и 0 из 6), а с двумя абзацами оговорки в конце SKILL.md держатся 3 из 3. Готовые сценарии, образцы и раскладки лежат в скретчпаде сессии b7cc7ae5, bench/ (scenarios, scenarios2, layout-a, layout-b); прогон prompt-test по правилу проекта обязателен, сценарий переноса и сценарий правки готового markdown у него уже есть.

## Чего хотим

## DoD

## Ранг

## Ход работы
