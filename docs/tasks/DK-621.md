# DK-621: hooks/check-structure.py: сторож блоков markdown по сторонам события записи и --cached

## Что происходит

Решение 4 LLD DK-606 (`docs/lld/DK-606-prose-structure.md`): хук на PostToolUse по сторонам события (`tool_response.originalFile` и файл по `filePath`), счёт блоков по видам markdown, режим `--cached` для pre-commit, пара регистрации POST_SCRIPTS и HOOK_LAYOUT, свой `--config` для доктора, секция в prose.toml с режимом warn. По стенду DK-606 оба живых случая это Write нового файла, где сторона «до» пуста; хук страховка для Edit, не главная опора.

## Чего хотим

## DoD

## Ранг

## Ход работы
