# DK-624: Скилл board-debt: долг у диффа идёт следующей задачей исполнителя

## Что происходит

Долговые строки на доске всегда проигрывают по рангу живым задачам и лежат
месяцами. Наглядный запас на 2026-08-30: 72 стенда дашборда без драйвера, 34
места с выводом подпроцесса на экране, невычитанный README, непокрытая отметка
работы в двух утилитах.

Замысел пользователя. Скилл ищет долг в момент, когда определяется объём работ
задачи, и гасит его тем же заходом. Исполнитель уже сидит в этих файлах и
держит их в голове, поэтому цена погашения самая низкая из возможных.

Решения пользователя по четырём местам, где скилл мог бы разъехаться.

Потолок вариативен. Долг ценой S берётся молча, больший спрашивается у
пользователя. Место вопроса это груминг, там человека и так спрашивают.
Нюанс, который придётся снять правкой правил: сейчас строки на доску без
груминга не заводятся.

Отдельный коммит не обязателен. В subject допустимо назвать несколько задач по
стилю conventional commits, история от этого станет прозрачнее. Проверено
2026-08-30: хук `check-commit.py` требует хотя бы один ID, а не ровно один;
поиск коммитов задачи идёт `git log --grep=<ID>` и найдёт коммит по обоим
номерам. Ограничение остаётся от правила одной строки: два номера и внятная
суть съедают её быстро.

Учёт через метку. У задачи заводится метка технического долга, и по ней видно
весь запас этой категории. Метка ставится при заведении, точка входа тот же
груминг. Формально это задачи, не влияющие на пользователя или влияющие слабо.

Отбор идёт двумя ступенями. Кандидатов набирают по близости к правимому коду.
Правится лента, значит берётся долг ленты, а не самый дешёвый долг из другого
угла. Дальше из кандидатов выбирают по связке ранга и размера. К рангу
прибавляется балл за размер, он поднимает мелкий долг, но не на порядок:
разницу в ранге эта надбавка перебить не должна. Уточнение пользователя
2026-08-30, оно снимает моё возражение о том, что у долга ранг низок по
построению и потому всегда проигрывает.

Своей арифметики скиллу не нужно, и это находка пользователя. Надбавка за
дешевизну уже принята правилом: DK-429 закрыта, в RANKING.md записано S +2,
M +1, L и XL ноль, а сумма мягких слагаемых с бонусом меньше шага серьёзности,
поэтому надбавка полосу не перебивает. Механическая часть висит открытой
строкой DK-428: `taskctl` пока не считает бонус из колонки цены и не печатает
его шестым слагаемым. Значит порядок такой. Сперва DK-428, дальше скилл берёт
готовый ранг и своей формулы не заводит.

К разбору. Величину надбавки за размер назвать числом и проверить на живом
запасе долга, чтобы она не стала решающей. Мера близости тоже требует
определения: общий файл, общий каталог, общая подсистема.

Смежное. Узор «долг копится молча» собирают DK-580, DK-590, DK-600, DK-609,
DK-611, DK-623. Цели у узора пока нет, черновик цели про доставку правки это
DK-610 и он про другое.

Разбор 2026-09-11, критик свежим контекстом по коду и правилам.

- Коммит с двумя ID ломает shipctl. Владельцем коммита считается первый
  ID в subject (`tools/shipctl/ops.go`, `ownsSubject`; README shipctl,
  «Владельцем коммита этот поиск считает задачу, чей ID стоит в subject
  первым»), и так с DK-288 от 2026-08-17, раньше отметки «проверено
  2026-08-30». Долг вторым номером не попадает ни в поезд, ни в откат.
  Долг первым номером отнимает коммит у основной задачи, а строку долга в
  Backlog делает stray, и merge с ship отказывают. Ворота `taskctl gate`
  при этом читают `--grep`, то есть у двух утилит две семантики «коммит
  задачи».
- Ворота merge считаются от одного ID: regcheck (только у bug), тест в
  диффе, сценарий проверки, след ревью, стенд промптов. Долг в чужой ветке
  своих ворот не проходит. Запас долга это в основном баги (DK-031, DK-076,
  DK-077, DK-024, DK-027, DK-035), под task-веткой regcheck молчит.
- Хвост приёмки у долга не дешевеет. `close` сверяет прогонявшего сценарий
  с исполнителем (DK-642), нужны раздел «Проверка» и отметка smoke. На
  S-правку ложатся своя строка In progress, сценарий, чужой прогон, smoke и
  след ревью, и черновик этого не считает.
- Гашение внутри чужой ветки производит состояние DK-623 (сделанное в
  чужой ветке не отличить от нетронутого) на каждой задаче. DK-623 это
  предпосылка, а не смежное.
- Близость считать не по чему. Поля с файлами нет ни у строки, ни в файле
  задачи, суффиксов у строки четыре и все про другое. В docs/map.md
  двенадцать компонентов, kit/ среди них нет, а дашборд один компонент, и
  главный пример долга (72 стенда) такой мерой не различается.
- Арифметика ранга. Мягкие слагаемые дают 25, шаг серьёзности 25, с
  бонусом 27, и RANKING.md говорит про «меньше двух шагов», а не одного.
  На живом беклоге из 158 строк серьёзность 0 у 25 с потолком ранга 11,
  у 106 строк ранг выше 34, DK-590 бонус поднял с 33 на 34. Готовый ранг
  долг не поднимает, поднимать его может только фильтр близости.
- Метка суффиксом дороже, чем выглядит. Регулярки `internal/accept`
  привязаны к концу строки с фиксированным порядком хвостов, и новый
  суффикс после `[приёмка: ...]` тихо вернёт вид agent у user-задачи.
  Учёт без метки уже есть: `taskctl list backlog | grep '(0+'`.
- Снялось проверкой. DK-428 закрыта 2026-08-31, бонус печатается шестым
  слагаемым, пункт «величину надбавки назвать числом» закрыт. Нюанс про
  строки без груминга снят раньше записи: с 2026-08-17 задача руками
  проходит те же ворота с изъятием для очевидных находок (RULES.board.md,
  «Трекинг задач» п. 5; board-task, «Что идёт строкой сразу»). Запрета на
  попутные правки в правилах нет, `check-commit.py` требует хотя бы один ID:
  эти две половины черновика верны.

## Чего хотим

Предмет после разбора 2026-09-11 лежит в развилке «предмет»: долг едет не
тем же заходом, а следующей задачей того же исполнителя, а подбирается при
сдаче основной задачи по пересечению файлов диффа со строками долга.

## Границы

- В чужую ветку и в коммит с двумя ID долг не едет (развилка «маршрут»).
- Правила заведения строки не трогаются (развилка «нюанс»).
- Своей арифметики ранга скилл не заводит, готовый ранг с бонусом берётся
  как есть, а поднимает долг только фильтр близости.
- DK-623 (сделанное в чужой ветке не отличить от нетронутого) остаётся
  отдельной строкой: при маршруте «следующей задачей» скилл её состояния не
  производит.

## Развилки

- «момент»: В черновике место вопроса про долг это груминг основной задачи, но предпосылка скилла в том, что исполнитель уже сидит в правимых файлах, а на груминге файлы ещё не известны. Где скилл подбирает долг и спрашивает про M+: при взятии в работу или на груминге?
  - решает: человек
  - рекомендация: взятие в работу: исполнитель уже читает код и знает файлы, вопрос про долг M+ уходит блоком decide --chat с парковкой, груминг только ставит метку долга
  - вариант: груминг основной задачи, как записано
  - решено человеком 2026-09-11: взятие в работу: исполнитель уже читает код и знает файлы, вопрос про долг M+ уходит блоком decide --chat с парковкой, груминг только ставит метку долга
- «нюанс»: Черновик отмечает нюанс: строки на доску без груминга не заводятся, и его придётся снять правкой правил. С тех пор в RULES.board.md п. 5 задача руками проходит те же ворота с изъятием для очевидных находок. Хватает ли этого, или правило про заведение строки долга всё же правится?
  - решает: человек
  - рекомендация: правки правил не нужно: скилл берёт только стоящие строки доски, а долг, найденный по ходу работы, идёт строкой руками с изъятием для очевидных находок или черновиком, как сейчас
  - вариант: снять правило и разрешить строку долга без груминга
  - решено человеком 2026-09-11: правки правил не нужно: скилл берёт только стоящие строки доски, а долг, найденный по ходу работы, идёт строкой руками с изъятием для очевидных находок или черновиком, как сейчас
- «разметка»: Метка долга ставится на груминге, а в беклоге уже 158 строк, из них 25 с серьёзностью 0 и ещё несколько с трением вроде DK-590. Кто размечает стоящий беклог: исполнитель по критерию, вы по списку, или задним числом не размечать?
  - решает: человек
  - рекомендация: исполнитель размечает беклог сам по критерию серьёзности 0 плюс строки узора DK-590, DK-609, DK-611, DK-623, а список кладёт в файл задачи на приёмку
  - вариант: список утверждается вами до простановки
  - вариант: задним числом не размечать, метка только новым строкам
  - решено человеком 2026-09-11: исполнитель размечает беклог сам по критерию серьёзности 0 плюс строки узора DK-590, DK-609, DK-611, DK-623, а список кладёт в файл задачи на приёмку
- «метка»: Где живёт метка долга: суффикс строки, поле файла задачи или производное от ранга?
  - решает: исполнитель
  - рекомендация: суффикс заголовка строки по образцу [приёмка: ...] (DK-292.3), флаг у taskctl add и фильтр у list; отдельного поля и раздела в файле не заводить
- «близость»: Мера близости долга к правимому коду: общий файл, общий каталог или компонент карты?
  - решает: исполнитель
  - рекомендация: компонент из docs/map.md (tools/<имя>, kit/skills/<имя>, hooks): у строки доски поля с файлами нет, и компонент это единственная мера, которую исполнитель сверяет по заголовку и файлу долга без чтения всего запаса; общий файл и общий каталог остаются уточнением внутри компонента
  - решено исполнителем 2026-09-11: пересмотр после «маршрут 1»: мера это файлы диффа основной задачи против файлов и каталогов, названных в заголовке и файле строки долга; компонент карты не различает (дашборд один компонент), а диффа у исполнителя к моменту выбора уже есть
- «маршрут»: Критик показал, что коммит с двумя ID и долг в чужой ветке ломают поезд, откат и ворота merge, а хвост приёмки долга не дешевеет. Каким маршрутом долг едет попутно?
  - решает: человек
  - рекомендация: долг идёт следующей задачей того же исполнителя: своя ветка, свои ворота и свой хвост приёмки, а экономия это тот же контекст и те же файлы в голове; в чужую ветку и в чужой коммит долг не едет
  - вариант: своей строкой и своим коммитом в той же ветке: скилл двигает строку долга в In progress и ведёт её через полный хвост приёмки
  - вариант: замысел снять: черновик удалить, долг гасит очередь по рангу
  - решено человеком 2026-09-11: долг идёт следующей задачей того же исполнителя: своя ветка, свои ворота и свой хвост приёмки, а экономия это тот же контекст и те же файлы в голове; в чужую ветку и в чужой коммит долг не едет
- «предмет»: После ответа «маршрут 1» предмет звучит иначе, чем в черновике: не гашение тем же заходом, а очередь следующей задачей того же исполнителя. Из этого сдвигается и момент: дифф, по которому считается близость, есть только при сдаче, а не при взятии в работу. Так ли звучит предмет и конец работы?
  - решает: человек
  - рекомендация: скилл board-debt зовётся исполнителем при сдаче основной задачи (перевод в Check), когда дифф уже есть: из строк с меткой долга берутся те, чьи файлы пересекаются с диффом, S ставится следующей задачей без вопроса, M и выше спрашивается блоком decide --chat; дальше долг идёт обычным маршрутом board-task и board-ship со своей веткой, воротами и приёмкой, а экономия это тот же исполнитель с тем же контекстом; конец работы: скилл, метка долга у строки (флаг add, фильтр list), разметка стоящего беклога списком в файле задачи, дока и прогон стенда prompt-test
  - вариант: момент оставить взятием в работу, как отвечено: долг подбирается до диффа по файлам, которые исполнитель прочитал
  - решено человеком 2026-09-11: скилл board-debt зовётся исполнителем при сдаче основной задачи (перевод в Check), когда дифф уже есть: из строк с меткой долга берутся те, чьи файлы пересекаются с диффом, S ставится следующей задачей без вопроса, M и выше спрашивается блоком decide --chat; дальше долг идёт обычным маршрутом board-task и board-ship со своей веткой, воротами и приёмкой, а экономия это тот же исполнитель с тем же контекстом; конец работы: скилл, метка долга у строки (флаг add, фильтр list), разметка стоящего беклога списком в файле задачи, дока и прогон стенда prompt-test

## DoD

- Скилл `board-debt` в `kit/skills` и шаг его вызова в `board-task` при
  переводе основной задачи в Check: из строк с меткой долга берутся те, чьи
  файлы пересекаются с диффом сданной задачи; S ставится следующей задачей
  того же исполнителя без вопроса, M и выше уходит человеку блоком
  `decide --chat`. Долг идёт обычным маршрутом со своей веткой, воротами и
  приёмкой, в чужую ветку и чужой коммит не едет.
- Метка долга у строки доски: флаг у `taskctl add`, фильтр у `list`,
  разбор вида приёмки (`internal/accept`) и хвостов строки (`dep.go`)
  метку переживает, `lint` ловит сломанный порядок хвостов. Всё с тестами.
- Стоящий беклог размечен по критерию: серьёзность 0 плюс DK-590, DK-609,
  DK-611, DK-623. Список размеченных строк лежит в файле задачи.
- Дока: README `taskctl` (метка), `RULES.board.md` (долг попутно), README
  скилла с первым запуском. Стенд `prompt-test` прогнан, след в файле задачи.

## Ранг

`25+6+2+0+4 = 37`, P2.

- Серьёзность 25: долг «лежит месяцами» при дешёвом обходе, ручном взятии
  строки по рангу.
- Ценность 6: сдача задачи идёт каждый день, и с каждой сдачей запас долга
  (72 стенда, 34 места вывода) тает без отдельного захода на строку.
- Неопределённость 2: подход выбран развилками «маршрут», «момент»,
  «предмет»; открыты место метки в строке и мера пересечения файлов.
- Поправка на баг 0: ничего не сломано.
- Рычаг 4: каждая долговая строка после этого дешевле, а метка даёт учёт
  запаса; «всё, что делает следующую задачу дешевле».
- Цена L: метка тянет `accept`, `dep`, `lint` и `list` в `taskctl`, поверх
  скилл, разметка трёх десятков строк, дока и стенд; в один контекст это не
  укладывается.

## Приёмка

- вид: mixed
- барьер «согласие»:

## Ход работы

- Из черновика DK-624 «Скилл board-debt: технический долг гасится попутно основной задаче»: записан 2026-08-30, оформлен 2026-09-11.
