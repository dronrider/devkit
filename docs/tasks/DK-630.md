# DK-630: Настройку доставки реплики раскладывает доктор

## Что происходит

Реплика из одной сессии в другую едет сокетом. Работает это, пока в настройках
харнеса стоит ключ `crossSessionInbound: accept`. Без него входящее сообщение
упирается в класс разрешений и до сессии не доходит.

Ключ поставили рукой на витке 6 цели DK-397, прямо в живых настройках машины.
В коде ветки его нет. Поиск по `*.go`, `*.py`, `*.js` и `*.json` на
2026-08-30 находит имя ключа только в файле цели `docs/tasks/DK-397.md` и в
`docs/lld/DK-459-poc-mode.md`. Ни установщик, ни `devkitctl doctor` о нём не
знают.

На чужой машине доставка после слияния молча мертва. Человек нажимает отправку,
реплика уходит в сокет и пропадает, а ошибки на экране нет. Правило «фича
доезжает до пользователя» нарушено дважды: раскладка ключа лежит на человеке,
а его отсутствие ничем не видно.

Строка просится в цель DK-610 «правка доезжает до того, чем пользуются». Там же
разбирается общий случай, когда фича работает только на машине автора.

## Чего хотим

`devkitctl doctor` находит машину без `crossSessionInbound: accept` и называет
находку словами про доставку реплик. `doctor --fix` ставит ключ сам. Сессия, у
которой ключа нет, говорит об этом при первой же попытке доставки.

## Границы

Остальные настройки харнеса сюда не входят. Битый `settings.json` целиком это
черновик DK-467, здесь чинится один ключ. Механика доставки не трогается. Предмет
задачи это раскладка ключа и видимость его отсутствия.

## Развилки

- Ставить ключ доктором или установщиком. Доктор чинит и уже стоящую машину,
  установщик только новую. Похоже, нужен доктор, а установщик зовёт его же.
- Чем показать отсутствие ключа человеку у экрана. Кандидаты это отказ ручки
  доставки с внятной причиной и находка доктора на панели дашборда.
- «Кто кладёт ключ»: Ставить ключ доктором или установщиком
  - решает: исполнитель
  - рекомендация: доктор, он чинит и уже стоящую машину
  - вариант: доктор
  - вариант: установщик
  - решено агентом 2026-09-17: Доктор. Он чинит и уже стоящую машину, а установщик кладёт только новую; доктора зовёт и сам установщик.
- «Чем видно отсутствие ключа»: Чем показать отсутствие ключа человеку у экрана
  - решает: исполнитель
  - рекомендация: находка доктора плюс причина в ответе ручки доставки
  - вариант: находка доктора и причина в ответе ручки доставки
  - вариант: жёсткий отказ ручки доставки до записи в сокет
  - решено агентом 2026-09-17: Находка доктора плюс причина в ответе ручки доставки. Жёсткий отказ до записи в сокет отвергнут: без ключа доставка встаёт только при расхождении класса разрешений, а сессии того же класса реплику берут, и отказ сломал бы работающее.

## DoD

На машине без ключа `devkitctl doctor` даёт находку, а `doctor --fix` её
закрывает. Прогон на подставном доме показан выводом в разделе «Проверка».
Попытка доставить реплику без ключа кончается видимым отказом.

## Ранг

`50+2+1+0+2 = 55`, P1.

- Серьёзность 50: главная функция POC на чужой машине не работает, и обход это
  ручная правка настроек харнеса по подсказке из файла цели.
- Ценность 2: у самого автора всё уже стоит, польза достаётся второй машине.
- Неопределённость 1: место правки известно, открыт выбор между доктором и
  установщиком.
- Поправка на баг 0: раскладку не ломали, её не написали.
- Рычаг 2: снимает ручной шаг с переноса чат-контура на новую машину.

## Ход работы

- Разработка: субагент opus/medium по вердикту pick (маппинг sonnet, корректор: профицит week_all, week_max; квота: week_all 28% профицит, week_max 44% профицит, снимок 16м назад), 2026-09-17 00:27-00:27.
- Ключ кладёт доктор рядом с ретрай-вотчдогом. Тот же файл настроек, тот же
  рубеж worktree, та же additive-правка. Своей раскладки установщику не завели,
  он зовёт того же доктора.
- Отсутствие ключа видно и вторым местом, в самой отправке. Ручка `say`
  отвечает причиной полем `stuck`, панель рисует её пузырём «не доставлено».
  Жёсткий отказ до записи в сокет отвергнут. Он сломал бы работающую доставку
  сессии того же класса разрешений.
- Стенд `peerInboundHeld` зовёт функцию, которой в базе нет. Он живёт отдельным
  файлом, а краснота на старом коде показана стендом ручки доставки.

## Сценарий проверки

1. Подставной дом без ключа. Доктор даёт находку словами про доставку реплик,
   `--fix` вписывает ключ, а в настройках дома остаётся `accept`. Копия дерева
   без `.git` нужна рубежу worktree. С ветки задачи машинное хозяйство не
   раскладывается, и `--fix` там молчит. Шаг идёт одной строкой: переменные
   между шагами обкатки не живут.

```sh
H=$(mktemp -d); T=$(mktemp -d)/tree; mkdir -p "$T"; cp -R . "$T" && rm -rf "$T/.git" && HOME=$H python3 "$T/tools/devkitctl/devkitctl.py" doctor -C "$T" | grep crossSessionInbound && HOME=$H python3 "$T/tools/devkitctl/devkitctl.py" doctor --fix -C "$T" | grep crossSessionInbound && python3 -c "import json,os; print(json.load(open(os.path.join('$H','.claude','settings.json')))['crossSessionInbound'])"
```

2. Отправка без ключа кончается видимой причиной, а с ключом молчит.

```sh
cd tools/dashboard && go test -run TestChatSayNamesClosedInbound ./...
```

## Проверка


- Обкатка: 2026-09-17 01:37, свежее дерево 61e6d2a43154, сценарий 7bc536ce, временный HOME, утилит дерева 11, шагов 2, все зелёные.

Шаг 1 (зелёный):

```console
$ H=$(mktemp -d); T=$(mktemp -d)/tree; mkdir -p "$T"; cp -R . "$T" && rm -rf "$T/.git" && HOME=$H python3 "$T/tools/devkitctl/devkitctl.py" doctor -C "$T" | grep crossSessionInbound && HOME=$H python3 "$T/tools/devkitctl/devkitctl.py" doctor --fix -C "$T" | grep crossSessionInbound && python3 -c "import json,os; print(json.load(open(os.path.join('$H','.claude','settings.json')))['crossSessionInbound'])"
находок: 36
машина: в /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/tmp.LYML3p8zgP/.claude/settings.json нет ключа crossSessionInbound: реплика из дашборда приходит живой сессии межсессионным сообщением, и без «accept» харнес придерживает её на расхождении класса разрешений, пока человек не ответит в чужом окне; вписать: devkitctl doctor --fix
находок: 3
починено: вписан ключ crossSessionInbound=accept в /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/tmp.LYML3p8zgP/.claude/settings.json: реплика дашборда доходит до живой сессии, не упираясь в класс разрешений отправителя
accept
```

Шаг 2 (зелёный):

```console
$ cd tools/dashboard && go test -run TestChatSayNamesClosedInbound ./...
ok  	github.com/dronrider/devkit/tools/dashboard	0.667s
```
