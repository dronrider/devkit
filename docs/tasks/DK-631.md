# DK-631: Тесты привязки и гигиена реестра

## Что происходит

Реестр сессий связывает сессию с задачей, деревом и окном tmux. На нём стоит
адресация реплик и подсчёт занятости деревьев. Ветка POC цели DK-397 переписала
его целиком, и три долга по этой части остались незакрытыми. Собраны они в одну
строку, потому что чинятся в одних и тех же файлах и проверяются одним
прогоном.

Первый долг. После `go test` в утилите taskctl список чатов дашборда показывает
чипы вымышленных задач из фикстур (XR-005) с настоящими деревьями и именами
tmux. Записан черновиком DK-573 2026-08-28. Разбор DK-581 нашёл другую течь,
подпроцесс делегирования наследовал `DEVKIT_TASK` и `DEVKIT_TMUX` раздавшей
сессии, и закрыл её коммитом в main. Догадка про сами прогоны тестов после
этого не подтверждена и не опровергнута. Проверять её надо чистым прогоном на
свободной машине и сверкой реестра до и после.

Первый долг подтверждён живым случаем 9 сентября 2026, и не тестами, а рукой
агента. Сессия груминга DK-880 разбирала хук `hooks/session-task.py` и прогнала
его из своего окна с выдуманным событием: `session_id` равен `test-880`,
транскрипт `/tmp/t.jsonl`. Имя окна хук взял из окружения, и в живом реестре
легла строка «сессия test-880 ... tmux task-DK-880». Дальше хозяином окна по
реестру стал `test-880`, дашборд отказал в клавишах словами «занято
разговором test-880», ответ человека в 11:58 ушёл в ящик `.in`, а признак
ожидания снялся. Чат груминга замер на вопросе, хотя окно и процесс жили.
Прогон хука руками из живого окна это тот же путь в реестр, что и у тестов,
и отсев ему нужен один.

Второй долг. Отметку живости `touchWork` держат три утилиты. Тест на неё есть
только у taskctl (`tools/taskctl/touch_test.go`). В `tools/shipctl/touch.go` и
`tools/agentctl/touch.go` тестов нет вовсе, а зовутся обе на каждой команде
через `main.go`.

Третий долг. `internal/works` читает вывод `tmux ls -F`, и разделителем полей
там стоял таб. Под launchd и cron окружение бедное, локали нет, tmux считает
таб непечатным знаком и подменяет подчёркиванием. Вся строка приезжает одним
полем, имена сессий выходят вида `chat-1_1_1787148735`. Найдено 2026-08-19 на
POC, записано черновиком DK-456. Разделитель в ветке уже сменён на `|`
(`internal/works/works.go:62`), парсер понимает оба формата, а теста на бедное
окружение нет. `TestParseSessions` кормит парсер только табами.

## Чего хотим

Прогон тестов не оставляет следов в реестре сессий машины. `touchWork` в
shipctl и agentctl покрыт тестами. Парсер `ParseSessions` проверен строкой с
подчёркиваниями и ловит возврат к табу.

## Границы

Разбор диффа реестра идёт строкой DK-626, здесь только тесты и гигиена
прогона. Течь наследования переменных закрыта DK-581 и заново не чинится.

## Развилки

- Куда уводить прогон, если он и правда пишет в живой реестр. Кандидаты это
  подставной дом на весь прогон и метка тестовой записи с отсевом на чтении.
- Проверять ли заодно стенды дашборда. Они зовут те же утилиты, и мусор от них
  выглядел бы так же.

Разобрано чистым прогоном на свободной машине (раздел «Сценарий проверки»).
taskctl уже снимает ID сессии в `TestMain` (`notify_test.go`) и живой реестр
не трогает. У dashboard та же подмена стоит с DK-397 (`TestMain`,
`tools/dashboard/tasks_test.go`). Повторная проверка не нужна. У shipctl
подмены не было вовсе. `goRunShip` в `args_test.go` гоняет собранный shipctl
отдельным подпроцессом и наследует окружение целиком. `TestStartFlagAnywhere`
и `TestStartExtraPositionalRejects` дописывали в живой реестр строки «shipctl
start» по фикстурным ID XR-999 и XR-001. При этом команда тут же падает на
«нет на доске». Найдено сверкой реестра до и после прогона с подставленным ID
живой сессии. Решение то же, что у taskctl и dashboard. `TestMain` shipctl
снимает `CLAUDE_CODE_SESSION_ID` (`notify_test.go`). У agentctl подмены не
было. Разговора по живому ID не завела ни одна из существующих проверок.
Каждая уже ставила свой временный дом отдельно. `TestMain` добавлен туда же
ради того же рубежа на будущее (`touch_test.go`).

## DoD

Прогон `go test ./...` на свободной машине оставляет реестр сессий таким же,
каким он был до прогона, и это показано сверкой в разделе «Проверка». Тесты
`touchWork` для shipctl и agentctl лежат в дереве. `TestParseSessions` разбирает
строку `chat-1_1_1787148735` и краснеет при возврате разделителя к табу,
краснота показана прогоном `regcheck`.

## Ранг

`25+2+1+5+2 = 35`, P2.

- Серьёзность 25: конвейер работает, а человек чистит чужие чипы из списка
  чатов руками.
- Ценность 2: заметно тому, кто держит дашборд открытым и гоняет тесты в том же
  дне.
- Неопределённость 1: два долга из трёх имеют готовый рецепт, у первого открыт
  способ увода прогона.
- Поправка на баг 5: мусор в реестре и слипшийся разбор `tmux ls` это дефекты.
- Рычаг 2: следующая правка реестра получает тесты, на которые можно опереться.

## Ход работы

- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 9%, снимок 8м назад, сдвига нет), 2026-09-18 17:56-17:57.
- Сторож прозы: docs/tasks/DK-631.md, средняя длина предложения 102,5 при пороге 23, оставлено: строка это команда shell в ```sh, а не проза, счётчик предложений принял `&&`-цепочку за одно предложение; `rehearse` гоняет каждую строку блока отдельным процессом, и зависимые команды обязаны стоять в одной строке.
- Сторож прозы: docs/tasks/DK-631.md, предложений длиннее 25 слов 50,0 при пороге 30, оставлено: та же команда shell, дробить её на команды короче нельзя, не сломав шаг.
- Сторож прозы: docs/tasks/DK-631.md, довод в той же фразе 50,0 при пороге 48, оставлено: тот же случай, метрика посчитана по командам shell, а не по прозе.

## Сценарий проверки

Шаги идут во временном HOME обкатки, его ставит `taskctl rehearse`. Доска
тестов не касается. Проверяется только реестр сессий. `rehearse` гоняет
каждую строку блока отдельным процессом, поэтому шаг с несколькими
зависимыми командами написан одной строкой через `&&`.

```sh
go -C internal test ./works/... -run 'TestParseSessions$|TestParseSessionsUnderscoredLine|TestSessionsPoorLocaleMangling' -v
```

Ожидание: три строки `--- PASS`. `TestParseSessionsUnderscoredLine` разбирает
слипшуюся строку бедного окружения. `TestSessionsPoorLocaleMangling` гоняет
`Sessions()` через подставной tmux, который мнёт табуляцию в подчёркивания
так же, как настоящий без локали.

```sh
python3 -c 'data = open("internal/works/works.go").read().replace(chr(34)+"|"+chr(34), chr(34)+chr(9)+chr(34)); open("/tmp/dk631-buggy-works.go","w").write(data)' && BLOB=$(git hash-object -w /tmp/dk631-buggy-works.go) && IDX=$(mktemp) && git read-tree --index-output="$IDX" HEAD && GIT_INDEX_FILE="$IDX" git update-index --cacheinfo 100644,"$BLOB",internal/works/works.go && TREE=$(GIT_INDEX_FILE="$IDX" git write-tree) && BUGGY=$(git commit-tree "$TREE" -p HEAD -m "regcheck: временный откат разделителя на таб") && rm -f "$IDX" && regcheck -C internal --base "$BUGGY" --tests internal/works/works_test.go -- go test ./works/... -run 'TestParseSessionsUnderscoredLine|TestSessionsPoorLocaleMangling'
```

Ожидание: `регрессия закрыта`. База тут не коммит из истории. Разделитель
поменяли до этой задачи. Старого кода в доступной истории для этой формы
`Sessions()` уже нет. Вместо него используется временный коммит рядом с HEAD.
Его единственная правка возвращает разделитель обратно на таб. `git
commit-tree` строит коммит без единого прикосновения к рабочему дереву. После
прогона коммит остаётся висячим. Собственный чекаут `rehearse` его не помнит.

```sh
go -C tools/shipctl test ./... -run TestTouchWork -v
go -C tools/agentctl test ./... -run 'TestTouchStage|TestTouchWorkGoal|TestTouchWorkWithoutGoal' -v
```

Ожидание: все `--- PASS`. Среди них новые `TestTouchWorkMarksTask`,
`TestTouchWorkFindsIDPastFlags` у shipctl и `TestTouchWorkGoalKey`,
`TestTouchWorkGoalKeyWithEquals` у agentctl. До этой задачи `touchWork` там
тестов не имел вовсе.

```sh
mkdir -p "$HOME/.devkit" && printf 'seed-line-should-not-move\n' > "$HOME/.devkit/sessions.log" && before=$(shasum -a 256 "$HOME/.devkit/sessions.log" | awk '{print $1}') && CLAUDE_CODE_SESSION_ID=test-live-session-0001 go -C tools/shipctl test ./... -run 'TestStartFlagAnywhere|TestStartExtraPositionalRejects' -v && after=$(shasum -a 256 "$HOME/.devkit/sessions.log" | awk '{print $1}') && echo "before=$before after=$after" && ( [ "$before" = "$after" ] && echo "реестр не тронут" || echo "реестр тронут" ) && cat "$HOME/.devkit/sessions.log"
```

Ожидание: `реестр не тронут`. Хеши до и после совпадают. В хвосте
`sessions.log` остаётся ровно `seed-line-should-not-move` без строк «shipctl
start». С ID живой сессии в окружении, как у прогона из настоящего разговора,
эти же тесты до починки `TestMain` дописывали в файл строки «задача
XR-999»/«задача XR-001». Подпроцесс `goRunShip` наследовал окружение целиком.
`sessions.Touch` срабатывал раньше отказа «нет на доске».

## Из черновика DK-573 «Прогоны тестов пишут в живой реестр сессий машины» (записан 2026-08-28)

### Черновик

#### Ситуация

После `go test` в утилите taskctl список чатов дашборда показывает чипы
вымышленных задач из фикстур (XR-005) с настоящими деревьями и именами
tmux. То есть стенды пишут не в свой дом, а в общий реестр, и мусор потом
висит в списке у человека.

Чинить у источника: либо прогон уводится в подставной дом, либо тестовая
запись метится и отсеивается на чтении. Проверить заодно, не пишут ли в
живой реестр стенды дашборда.

#### Осложнение

#### Вопрос

#### Гипотеза

### Приписка 2026-08-29: источник найден, и он другой

Разбор DK-581 нашёл настоящую причину. Подпроцесс делегирования наследовал
переменные заказа раздавшей сессии, `DEVKIT_TASK` и `DEVKIT_TMUX`, и вставал в
реестр её задачей. Разговор диспетчера был заказан с задачей XR-005, поэтому
каждая работа второй подписки писала в реестр строку «задача XR-005, tmux
chat-XR-005-1», а дерево в строке стояло то, где работал делегат.

Течь закрыта коммитом DK-581 в main: `run` ставит делегату свою задачу и пустой
tmux.

Гипотеза про прогоны тестов при этом не подтверждена и не опровергнута.
Проверять её надо отдельно, чистым прогоном на свободной машине и сверкой
реестра до и после.

## Из черновика DK-456 «tmux ls -F с табуляцией слипается в бедном окружении launchd» (записан 2026-08-19)

### Черновик

#### Ситуация

Ситуация. internal/works читает tmux ls -F с табуляцией разделителем. tmux без UTF-8 локали в окружении (нет LANG, штатно под launchd и cron) считает табуляцию непечатным знаком и подменяет подчёркиванием: вся строка приезжает одним полем, имена сессий выходят вида chat-1_1_1787148735. Воспроизводится ровно: env -i tmux ls даёт подчёркивания, с LANG=en_US.UTF-8 возвращаются табуляции. Найдено 2026-08-19 на POC цели DK-397: демон дашборда под launchd не видел ни одной tmux-сессии и падал на подъёме чата с duplicate session.

Осложнение. Пакет общий: тот же разбор гоняет планировщик слота taskctl и всё, что считает занятость деревьев, то есть у всякого, кто зовёт их из launchd или cron, занятость сейчас считается неверно и молча. В POC-ветке poc-chat разделитель уже сменён на печатный | (парсер понимает оба формата), правка лежит в e87bcaea и ждёт переноса в ядро своим порядком, с тестом на бедное окружение.

Вопрос. Переносить сменой разделителя или добавлять LANG в окружение вызова tmux?

Гипотеза. Разделитель | дешевле и не зависит от наличия локалей на машине; тест кормит парсер строкой с подчёркиваниями и ловит регресс.

#### Осложнение

#### Вопрос

#### Гипотеза

## Проверка


- Обкатка: 2026-09-18 18:45, свежее дерево 653fc4a680a6, сценарий 8a0b80d4, временный HOME, утилит дерева 11, шагов 5, все зелёные.

Шаг 1 (зелёный):

```console
$ go -C internal test ./works/... -run 'TestParseSessions$|TestParseSessionsUnderscoredLine|TestSessionsPoorLocaleMangling' -v
=== RUN   TestParseSessions
--- PASS: TestParseSessions (0.00s)
=== RUN   TestParseSessionsUnderscoredLine
--- PASS: TestParseSessionsUnderscoredLine (0.00s)
=== RUN   TestSessionsPoorLocaleMangling
--- PASS: TestSessionsPoorLocaleMangling (0.34s)
PASS
ok  	github.com/dronrider/devkit/internal/works	0.925s
```

Шаг 2 (зелёный):

```console
$ python3 -c 'data = open("internal/works/works.go").read().replace(chr(34)+"|"+chr(34), chr(34)+chr(9)+chr(34)); open("/tmp/dk631-buggy-works.go","w").write(data)' && BLOB=$(git hash-object -w /tmp/dk631-buggy-works.go) && IDX=$(mktemp) && git read-tree --index-output="$IDX" HEAD && GIT_INDEX_FILE="$IDX" git update-index --cacheinfo 100644,"$BLOB",internal/works/works.go && TREE=$(GIT_INDEX_FILE="$IDX" git write-tree) && BUGGY=$(git commit-tree "$TREE" -p HEAD -m "regcheck: временный откат разделителя на таб") && rm -f "$IDX" && regcheck -C internal --base "$BUGGY" --tests internal/works/works_test.go -- go test ./works/... -run 'TestParseSessionsUnderscoredLine|TestSessionsPoorLocaleMangling'
тест краснеет на a2ef96091e6dec6553526906f10b8a1c4bd68836 и проходит на текущем коде, регрессия закрыта (тесты: internal/works/works_test.go)
```

Шаг 3 (зелёный):

```console
$ go -C tools/shipctl test ./... -run TestTouchWork -v
=== RUN   TestTouchWorkMarksTask
--- PASS: TestTouchWorkMarksTask (0.00s)
=== RUN   TestTouchWorkFindsIDPastFlags
--- PASS: TestTouchWorkFindsIDPastFlags (0.00s)
=== RUN   TestTouchWorkIgnoresReadCommands
--- PASS: TestTouchWorkIgnoresReadCommands (0.00s)
=== RUN   TestTouchWorkWithoutSessionIDWritesNothing
--- PASS: TestTouchWorkWithoutSessionIDWritesNothing (0.00s)
PASS
ok  	github.com/dronrider/devkit/tools/shipctl	0.381s
```

Шаг 4 (зелёный):

```console
$ go -C tools/agentctl test ./... -run 'TestTouchStage|TestTouchWorkGoal|TestTouchWorkWithoutGoal' -v
=== RUN   TestTouchStageMarksWork
--- PASS: TestTouchStageMarksWork (0.00s)
=== RUN   TestTouchStageShowIsNotWork
--- PASS: TestTouchStageShowIsNotWork (0.00s)
=== RUN   TestTouchWorkGoalKey
--- PASS: TestTouchWorkGoalKey (0.00s)
=== RUN   TestTouchWorkGoalKeyWithEquals
--- PASS: TestTouchWorkGoalKeyWithEquals (0.00s)
=== RUN   TestTouchWorkWithoutGoalWritesNothing
--- PASS: TestTouchWorkWithoutGoalWritesNothing (0.00s)
=== RUN   TestTouchWorkGoalIgnoresOtherCommands
--- PASS: TestTouchWorkGoalIgnoresOtherCommands (0.00s)
PASS
ok  	github.com/dronrider/devkit/tools/agentctl	0.382s
```

Шаг 5 (зелёный):

```console
$ mkdir -p "$HOME/.devkit" && printf 'seed-line-should-not-move\n' > "$HOME/.devkit/sessions.log" && before=$(shasum -a 256 "$HOME/.devkit/sessions.log" | awk '{print $1}') && CLAUDE_CODE_SESSION_ID=test-live-session-0001 go -C tools/shipctl test ./... -run 'TestStartFlagAnywhere|TestStartExtraPositionalRejects' -v && after=$(shasum -a 256 "$HOME/.devkit/sessions.log" | awk '{print $1}') && echo "before=$before after=$after" && ( [ "$before" = "$after" ] && echo "реестр не тронут" || echo "реестр тронут" ) && cat "$HOME/.devkit/sessions.log"
=== RUN   TestStartFlagAnywhere
--- PASS: TestStartFlagAnywhere (1.42s)
=== RUN   TestStartExtraPositionalRejects
--- PASS: TestStartExtraPositionalRejects (0.32s)
PASS
ok  	github.com/dronrider/devkit/tools/shipctl	2.082s
before=3dc1204310aeb2bf6a85f6f789144c42107a9e45285d81034b0f1e5d7963a2ef after=3dc1204310aeb2bf6a85f6f789144c42107a9e45285d81034b0f1e5d7963a2ef
реестр не тронут
seed-line-should-not-move
```
