# DK-633: снимок квоты обеих подписок обновляется сам

## Что происходит

Остаток подписок на экране застыл, и бед под этим три, сошедшихся в один день.
Пока их не починили, человек за своим лимитом ходит в два разных кабинета
руками, на каждую задачу.

Первая пришла с DK-605. Снимок первой подписки читает кеш клиента
(`~/.claude.json`, ключ `cachedUsageUtilization`) вместо разбора панели
`/usage`. Кеш этот клиент обновляет, только когда сам ходит за расходом, а
съёмщик выбирал дорогу по наличию кеша, а не по его возрасту. Кеш застыл на
17:39, и снимок вместе с ним.

Вторая живёт в съёмщике второй подписки, `kit/harness/snap/glm-code.sh`.
Пятичасовое окно, по которому не потрачено ни кредита, z.ai присылает без
времени сброса: часы окна пускает первая трата. Съёмщик считал это поломкой и
отказывал целиком, вместе со вторым окном, поэтому снимок второй подписки стоял
двадцать часов.

Третья это откат снимка назад. Кеш клиента переписывает всякая живая сессия
своей копией в памяти, и долго висящее окно кладёт туда цифры четырёхчасовой
давности поверх двадцатиминутных. На экране у человека стояло 82% в 21:19, а в
файле через сорок минут оказалось 79% в 17:39. Дашборд всё это время показывал
содержимое файла слово в слово.

## Чего хотим

`agentctl quota refresh` даёт свежий снимок обеих подписок, а на экране
дашборда стоят те же цифры и то же время, что в файлах снимков.

## DoD

Сверх общего: живой прогон обеих подписок и сверка с ручкой `/api/quota`.

## Ранг

`50+8+1+5+3 = 67`, P1.

- Серьёзность 50: основной сценарий сломан, обход есть и болезненный, человек
  ходит за остатком в два кабинета руками.
- Ценность 8: остаток кормит корректор вердикта и через него выбор модели на
  каждой задаче.
- Неопределённость 1: подход понятен, открыты детали.
- Поправка на баг 5.
- Рычаг 3: чинит наблюдаемость, которой пользуется каждая следующая задача.

## Приёмка

- вид: mixed
- барьер «глаза»: остаток обеих подписок стоит на экране дашборда свежим
  - утверждённый макет: не годится, эталона нет, предмет это свежесть цифры,
    а не вид экрана
  - headless-браузер с замером: не нужен, экран берёт цифру и возраст из
    снимка, а механику снимка держат стенды на выбор дороги, заимствование
    разбивки и повтор отказа панели
  - тест по разметке и стилям: годится для пометки возраста, остальное держат
    стенды нетронутого окна и запрета отката

## Ход работы

- Разработка: opus/high, 2026-08-30.
- Снаружи: проверка после выката, 2026-08-30 23:19-10:47.
- Доработка по находке приёмки: fable, 2026-08-31.
- Вычитка: 3 файла, 9 правок, 1 пометка, 2026-08-31.
- Снаружи: проверка после выката, 2026-08-31 11:29-16:01.

## Ревью

- самообновления снимка второй подписки нет: без захода glm-сессии снимок стоит часами (01:03 -> 10:46 обновила только рука при живом дашборде), а заголовок обещает «обновляется сам»; нужен периодический съём (таймер launchd или опрос дашборда), находка приёмки глазами: исправлено
- тик считает оставленные сторожем отката цифры за «снято»: в 15:59 панель не далась, кеш был старее диска, файл claude-code остался на 15:13, а журнал сказал «снято 2, отказов 0»; при недающейся панели снимок стареет бесконечно и след врёт; несвежий исход должен считаться отказом с ретраем следующим тиком, а занятая цифра week_max со временем сброса в прошлом видна как протухшая, не как живой процент

## Сценарий проверки

1. `DEVKIT_HARNESS=glm-code agentctl quota refresh` печатает оба окна второй
   подписки, и `taken` в `~/.devkit/quota/glm-code.local` равен текущей минуте.
2. `agentctl quota refresh` при кеше клиента старше сорока пяти минут говорит
   про смену дороги, снимает панель и кладёт свежий `taken` в
   `~/.devkit/quota/claude-code.local`.
3. Ручка `/api/quota` дашборда отдаёт те же проценты и то же время, что стоят в
   обоих файлах.
4. Самообновление: при закрытых сессиях `taken` в обоих файлах
   `~/.devkit/quota/*.local` не старше часа, снимок свежеет без руки. В
   `~/.devkit/goal-watch.log` лежит строка со счётом исходов последнего съёма.

## Выкат

- smoke прогнан, 2026-08-31
- 2026-08-31 слито: abe3213f, 25d969ab, d9a8bd77, bba7b38b
- smoke прогнан, 2026-08-31

## Проверка

Шаг 1, вторая подписка снимается, хотя пятичасовое окно нетронуто:

```text
снимок /Users/rider/.devkit/quota/glm-code.local (харнес glm-code)
снят 2026-08-30T22:34, возраст 0м
window5h_all: потрачено 0%, сброс 2026-08-31T03:34, pace 1.0, норма
week_all: потрачено 17%, сброс 2026-09-05T20:52, pace 1.0, норма
```

Прежний съёмщик на том же ответе уходил с ненулевым кодом и уносил снимок
целиком, вместе с недельным окном, где данные были в порядке:

```text
taken = 2026-08-30T22:26
у окна window5h_all нет времени сброса
```

Шаг 2, кеш клиента протух, съёмщик ушёл на панель и занял у кеша разбивку:

```text
кеш клиента снят 4ч 55м назад при пороге 45м и обновится, только когда клиент сам сходит за расходом; снимаем панелью /usage
бакет week_max панель не показала, взят из кеша клиента возрастом 4ч 55м назад
снимок /Users/rider/.devkit/quota/claude-code.local (харнес claude-code)
снят 2026-08-30T22:34, возраст 0м
week_all: потрачено 84%, сброс 2026-08-31T15:00, pace 1.6, норма
week_max: потрачено 17%, сброс 2026-08-31T14:59, pace 8.5, профицит (панель разбивку не дала, цифра из кеша клиента возрастом 4ч 55м назад)
```

Шаг 3, ручка дашборда отдаёт то же, что лежит в файлах:

```text
claude-code снят 2026-08-30T22:34 возраст 1м свеж
    week_all 84% сброс 2026-08-31T15:00
    week_max 17% сброс 2026-08-31T14:59
glm-code снят 2026-08-30T22:34 возраст 1м свеж
    window5h_all 0% сброс 2026-08-31T03:34
    week_all 17% сброс 2026-09-05T20:52
```

Сторож отката сработал на живом прогоне: панель в тот заход не далась, в снимок
пошёл кеш семнадцати часов, и назад его не пустили.

```text
снятое (2026-08-30T17:39) старше снимка на диске (2026-08-30T22:29): источник расхода откатился назад, цифры на диске оставлены прежними
```

Показ дашборда правки не потребовал. Ручка `/api/quota` читала файл и раньше,
а между снимком экрана и разбором успел откатиться назад сам файл.

Шаг 4 после выката, снимки свежеют без руки. К слиянию оба протухали
(10:31 и 10:46 при времени 11:29). Снимок первой подписки переснял демон
дашборда после перезапуска выкатом, второй дождался своего протухания и
переснялся тиком сторожка, обе живые сессии в это время квоту не трогали.

```text
taken = 2026-08-31T11:31 (claude-code, демон дашборда)
taken = 2026-08-31T11:32 (glm-code, тик сторожка)
2026-08-31T11:32:56 снимок квоты: подписок 2: снято 1, свежих 1, отказов 0
```

Последняя строка лежит в `~/.devkit/goal-watch.log`. Ручка `/api/quota` после
того тика отдала те же `taken` 11:31 и 11:32 с возрастом 2м и 1м, оба снимка
без пометки протухания.

## Решения

Носитель периодического съёма. Кандидатов было три: свой launchd-агент со
StartInterval, опрос из живого дашборда и тик сторожка `ru.devkit.goal-watch`.
Свой агент означал бы третий plist ради работы, которой хватает пары строк в
готовом расписании. Один дашборд не годится, его демон бывает выключен, а
launchd живёт всегда. Взят тик сторожка, и тик демона дашборда переведён на
тот же режим. Оба зовут `agentctl quota refresh --all --if-stale`. Флаг
`--all` обходит обе подписки, флаг `--if-stale` держит частоту: панель и
кабинет z.ai дёргаются только по протуханию снимка при пороге в 45 минут.
Перечень подписок, порог и замок живут в agentctl одной копией, носители его
только запускают. Раскладку обоих носителей и так делает `devkitctl doctor
--fix`, нового механизма установки задача не заводит.

Разбивка по моделям при уходе на панель. Общие цифры берутся у панели, они
свежие, а бакет модели, которого панель не показала, занимается у кеша с
пометкой возраста. Заимствование не выдумывает число. Свою разбивку клиент
просит тем же запросом, что и общие цифры, и при отказе рисует её из того же
кеша. В снимок едет ровно то, что в эту минуту стоит у человека на
экране. Выбросить бакет было бы хуже. За разбивкой человек шёл бы в кабинет
подписки, ради чего задача и заводилась.

Поэтому в снимке две пометки. `# partial` означает «бакета нет, и вот почему».
`# borrowed` означает «бакет есть, но цифра не своя». Первая правка их сливала.
Снимок печатал под цифрой строку о том, что цифры нет, а корректор терял возраст
занятой цифры вовсе. Он читает пометку только там, где бакета не нашёл. Обе
пометки едут комментарием. В формате снимка бакетом считается всё, что не
`taken`, и сторонний читатель принял бы новый ключ за бакет.

Нетронутое окно второй подписки. Подписка не назначает ему времени сброса, и
сброс считается от длины окна. Бакет с нулём и сроком через пять часов даёт pace
1.0 и «норму». Про окно это правда. Пустой срок прочитался бы просроченным окном
и тянул бы вердикт вниз. От «данных нет» такое окно отличается тем, что строка в
снимке есть. Отсутствие данных это отсутствие строки. Потраченному окну без
срока сброса верить нечему, и оно по-прежнему получает отказ. Ноль без срока
объясняется неначатым окном, а трата без срока это поломка ответа.
