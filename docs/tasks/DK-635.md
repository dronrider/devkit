# DK-635: Потолок стенных секунд в тестах parallel валит слияние под нагрузкой

## Что происходит

Тест test_term_proof_component_is_killed_harder (tools/devkitctl/parallel_test.py) меряет стенные секунды с потолком 10, а подопытный подпроцесс спит 30. На машине с load average под 70 (полный параллельный прогон плюс соседние деревья) выходит 11-13 секунд, и любой shipctl merge падает, хотя настоящий инвариант «компонент умер по KILL, до метки не дожил» держится. Поодиночке тест проходит за 4-5 секунд. Лечить поднятием потолка до 20 или замером не стенными секундами. Всплыло на слиянии DK-428, три захода подряд. Повторилось 2026-08-31 на слиянии цели DK-397. Вместе с ним лёг соседний test_first_failure_stops_the_rest, потолок там тот же. Вышло 13.9 и 11.9 секунды при load average около 9. Значит потолка не хватает уже на обычной рабочей нагрузке, не только на полном прогоне. Поодиночке компонент зелёный за 25 секунд.

## Чего хотим

## DoD

Тесты держат инвариант, ради которого написаны. Компонент умер по KILL и до
метки `done` не дожил, стоп по первому провалу не ждёт упрямого подпроцесса.
Замер, зависящий от нагрузки машины, из проверки уходит либо получает запас,
которого хватает при load average около 70. Прогон компонента devkitctl под
соседним параллельным прогоном зелёный три захода подряд, и `shipctl merge`
на загруженной машине не отбивается.

## Ранг

`50+6+1+5+4 = 66`, P1.

- Серьёзность 50: слияние падает на обычной рабочей нагрузке, три захода подряд
  на DK-428 и повтор на цели DK-397; обход есть, но болезненный, повторять
  merge и ждать, пока машина освободится.
- Ценность 6: конвейер перестаёт отбивать готовые правки по погоде на машине.
- Неопределённость 1: лечение названо в самой находке, поднять потолок или уйти
  от стенных секунд к проверке факта смерти.
- Поправка на баг +5: тест краснеет там, где проверяемое поведение держится.
- Рычаг 4: зелёный прогон слияния, от него дешевеет каждая следующая задача.

Цена S, два теста в одном файле. Вид приёмки агентский. Нагрузка на своей
машине воспроизводится параллельным прогоном, ни один из шести барьеров не
назван.

## Ход работы

- Из черновика DK-635 «devkitctl parallel: потолок теста убийства компонента валит merge»: записан 2026-08-31, оформлен 2026-09-01.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 0% профицит, снимок 3ч 48м назад, протух, сдвига нет), 2026-09-03 04:55-04:56.
- Правка: `test_first_failure_stops_the_rest` и
  `test_term_proof_component_is_killed_harder` в
  `tools/devkitctl/parallel_test.py` перестали мерить стенные секунды
  (`assertLess(wall, 10, ...)`). Вместо этого они проверяют факт смерти.
  Долгий или упрямый компонент метит свой конец файлом `done`. Стенд после
  `run_all` смотрит на отсутствие метки. Тот же приём уже стоял у соседнего
  `test_kill_reaches_the_subprocesses`. `run_all` синхронный, треды джойнятся
  до возврата. Поэтому проверка метки верна независимо от того, сколько
  секунд ушло на убийство. Секунды растягиваются под нагрузкой соседних
  прогонов. Факт «не дожил до метки» не растягивается. `long.sh` в
  `test_first_failure_stops_the_rest` стал писать `done` перед выходом: метка
  была нужна, а её не было. Докстринг файла и комментарии обоих тестов
  переписаны под новый приём, находка DK-635 в них названа явно.
- Исключение: regcheck (баг живёт в самом тестовом файле, `parallel.py`
  правкой не тронут; механизм regcheck переносит тестовые файлы на старый
  код; production-код по обе стороны одинаковый, и разницу между старым
  тестом на секундах и новым на факте так не поймать; краснота старого
  варианта и зелёность нового показаны отдельным прогоном под нагрузкой,
  вывод ниже).
- Краснота и зелёность под нагрузкой (демонстрация DoD, машина исполнителя,
  8 ядер darwin/arm64). Старый вариант теста прогонялся под искусственной
  нагрузкой (несколько десятков фоновых busy-loop) и под соседними полными
  прогонами `parallel.py`. Load average доходил до 196. Пад на этой машине
  поймать не удалось, но запас таял на глазах: комбинированное время обоих
  тестов дошло до 10.286s и 9.428s при потолке 10s на каждый тест по
  отдельности, а изолированный прогон без нагрузки держит 4 до 6s. Реальные
  красные прогоны с этой же парой тестов уже есть в «Что происходит»: 13.9s и
  11.9s на слиянии DK-397 при load average около 9, три пада подряд на
  DK-428. Новый вариант держит инвариант под той же нагрузкой, три подряд
  захода, load average от 177 до 186:

  ```
  === NEW attempt 1, load averages: 177.07 162.18 109.45 ===
  test_first_failure_stops_the_rest (parallel_test.RunAllTest) ... ok
  test_term_proof_component_is_killed_harder (parallel_test.RunAllTest) ... ok
  Ran 2 tests in 6.125s
  OK
  rc=0
  === NEW attempt 2, load averages: 183.77 164.10 110.75 ===
  test_first_failure_stops_the_rest (parallel_test.RunAllTest) ... ok
  test_term_proof_component_is_killed_harder (parallel_test.RunAllTest) ... ok
  Ran 2 tests in 6.191s
  OK
  rc=0
  === NEW attempt 3, load averages: 186.51 165.33 111.81 ===
  test_first_failure_stops_the_rest (parallel_test.RunAllTest) ... ok
  test_term_proof_component_is_killed_harder (parallel_test.RunAllTest) ... ok
  Ran 2 tests in 5.909s
  OK
  rc=0
  ```

## Ревью

- Вердикт: без замечаний. Уровень 2, неопределённость 1. Триаж дал одно «да». Тест держит доверие к DoD других задач через ворота shipctl merge. tools/devkitctl не входит в critical_paths. Путь от симптома до правки пройден по звеньям. У long.sh метка пишется в том же процессе, который ждёт communicate(). Гонки там нет. У stubborn.sh sleep(0.5) переиспользует приём соседнего test_kill_reaches_the_subprocesses. SIGKILL по группе процессов не растягивается нагрузкой, снятый замер растягивался. Мутация подтвердила инвариант. Откат эскалации KILL до чистого TERM красит test_term_proof_component_is_killed_harder, 30.8s. Отключение killpg целиком красит test_first_failure_stops_the_rest, 30.4s. Оба падают на assertFalse(marker.exists()). Полный прогон parallel_test зелёный, 14.17s, 18 тестов. Regcheck-исключение стоит в формате shipctl README, раздел «Слияние». Причина честная. Production-код не менялся, новый факт-тест не покраснеет ни на каком parallel.py, регчек тут бессилен по конструкции. Сценарий agent, ожидание проверяется кодом возврата шага (test "$ok" -eq 3). Отметка обкатки сверена. Отпечаток f0950385 совпал с пересчётом sha256 раздела. HEAD 2b1f49ce трогает только файл задачи, отметка держится. Шесть docs-коммитов это связная история починки сценария под rehearse, без противоречивых абзацев. Символы чистые (check-symbols.py). Коммиты однострочные, с ID DK-635, без трейлеров.

## Сценарий проверки

Агентский. До правки `test_first_failure_stops_the_rest` и
`test_term_proof_component_is_killed_harder` мерили стенные секунды с
потолком 10. Нагрузка растягивала секунды, и `shipctl merge` краснел (см.
«Что происходит» и запись про 10.286s и 9.428s в «Ход работы»). Воспроизвести
это можно только на старом тексте теста: фикс убирает саму секундомерную
проверку. После правки оба теста смотрят на факт (отсутствие метки `done` у
убитого компонента) и не зависят от того, сколько секунд ушло на смерть
подпроцесса. Шаг ниже гоняет их три раза подряд под искусственной нагрузкой
и ждёт нулевого кода возврата на каждом заходе.

```sh
cd tools/devkitctl; for i in $(seq 1 12); do ( while true; do :; done ) & done; spin_pids="$(jobs -p)"; ( sleep 90; kill $spin_pids >/dev/null 2>&1 ) & sleep 4; ok=0; for i in 1 2 3; do python3 -m unittest parallel_test.RunAllTest.test_first_failure_stops_the_rest parallel_test.RunAllTest.test_term_proof_component_is_killed_harder && ok=$((ok+1)); done; kill $spin_pids >/dev/null 2>&1; echo "green_runs=$ok"; test "$ok" -eq 3
```

Шаг это одна физическая строка, не блок. `taskctl rehearse` режет блок по
переводам строки. Каждая строка идёт своим отдельным прогоном без общего
состояния (`tools/taskctl/README.md`, раздел «Команды», описание `rehearse`).
Поэтому фоновые
busy-loop, переменная `spin_pids` и завершающий `kill` живут в одной строке.
На нескольких строках `kill` одного прогона не достаёт до busy-loop другого
прогона. Контрольные циклы тогда висят до предела в десять минут: так и было
на первой редакции сценария, шаг с `for` под busy-loop убило по таймауту. Число
busy-loop небольшое, двенадцать. Под управлением `taskctl rehearse` (свежее
дерево, сборка утилит) лишняя нагрузка от самого шага и так замедляет
харнесс сильнее целевых тестов. Сторожевой `kill` через 90s фоном подчищает
busy-loop, если сам шаг оборвётся раньше своего конца.

Ожидание: в выводе строка `green_runs=3`, код возврата шага 0. Три подряд
зелёных захода под нагрузкой из двенадцати фоновых busy-loop.

## Проверка


- Обкатка: 2026-09-03 06:14, свежее дерево 1705b82d2596, сценарий f0950385, временный HOME, утилит дерева 10, шагов 1, все зелёные.

Шаг 1 (зелёный):

```console
$ cd tools/devkitctl; for i in $(seq 1 12); do ( while true; do :; done ) & done; spin_pids="$(jobs -p)"; ( sleep 90; kill $spin_pids >/dev/null 2>&1 ) & sleep 4; ok=0; for i in 1 2 3; do python3 -m unittest parallel_test.RunAllTest.test_first_failure_stops_the_rest parallel_test.RunAllTest.test_term_proof_component_is_killed_harder && ok=$((ok+1)); done; kill $spin_pids >/dev/null 2>&1; echo "green_runs=$ok"; test "$ok" -eq 3
..
----------------------------------------------------------------------
Ran 2 tests in 5.389s

OK
..
----------------------------------------------------------------------
Ran 2 tests in 5.372s

OK
..
----------------------------------------------------------------------
Ran 2 tests in 5.561s

OK
green_runs=3
```
