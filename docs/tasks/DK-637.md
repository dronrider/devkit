# DK-637: shipctl: ветка, вобравшая main слиянием, сливается без ребейза

## Что происходит

Как воспроизвести. Завести длинную ветку, дать main уйти вперёд по тем же
файлам, догнать ветку не ребейзом, а `git merge main`, развести конфликты один
раз и позвать `shipctl merge <ID>`.

Что ожидалось. Ветка уже потомок main, fast-forward у неё есть, слияние идёт
дальше к тестам и выкату.

Что вышло. `shipctl merge` зовёт `git rebase main` безусловно. На ветке, где
main уже предок, ребейз не бездействует. Он проигрывает коммиты ветки заново
поверх main и выбрасывает merge-коммиты вместе с разведёнными в них
конфликтами. Конфликты возвращаются по одному на коммит, ребейз падает,
`shipctl` его абортит и отказывает.

Нашлось 2026-08-30 на ветке цели DK-397. Там 346 коммитов, сшивка с main
сделана слиянием, и ребейз в клоне-песочнице падает на пятом коммите из 346
конфликтом в `.gitignore`. Развести те же конфликты 346 раз нечем. Обход
остаётся один, сплющить историю недели работы в один коммит.

## Чего хотим

`shipctl merge` спрашивает `git merge-base --is-ancestor main HEAD` и зовёт
ребейз только тогда, когда main ветке не предок. Ветка, догнанная слиянием,
сливается штатно и приезжает в main со своей историей.

## DoD

Сверх общего. Ветка цели DK-397 проходит `shipctl merge` без сплющивания.

## Ранг

`50+2+0+5+4 = 61`, P1.

- Серьёзность 50. Длинная ветка не сливается вовсе, обход есть, но платит
  историей всей работы над веткой.
- Ценность 2. Починка ради статус-кво, нового пользователь не увидит.
- Неопределённость 0. Причина найдена прогоном, место в коде названо.
- Поправка на баг 5.
- Рычаг 4. Сшивка слиянием становится законной дорогой, и следующая длинная
  ветка проходит слияние сама.

## Приёмка

- вид: agent
- барьеров нет, исход виден прогоном: ветка либо сливается, либо нет

## Ход работы

- Разработка: opus/medium, 2026-08-31, правка по ходу цели DK-397 (скилл live-core).

## Ревью

Не звалось. Правка в пять строк по названной причине, ревьювера не назначали.

## Сценарий проверки

1. `cd tools/shipctl && GOWORK=off go test -count=1 -run TestMergeSewnBranchKeepsHistory ./...`,
   ждём PASS.
2. `GOWORK=off regcheck -C tools/shipctl -- go test -count=1 -run TestMergeSewnBranchKeepsHistory ./...`,
   ждём «тест краснеет на HEAD и проходит на текущем коде».

## Выкат

- smoke прогнан, 2026-08-31

## Проверка

Оба шага прогнаны из главного дерева 2026-08-31.

1. `GOWORK=off go test -count=1 -run TestMergeSewnBranchKeepsHistory ./...`:
   `ok github.com/dronrider/devkit/tools/shipctl 1.614s`.
2. `regcheck -C tools/shipctl --base 897751fd^ -- go test -count=1 -run
   TestMergeSewnBranchKeepsHistory ./...`: «тест краснеет на 897751fd^ и
   проходит на текущем коде, регрессия закрыта». База взята фикс^: после
   слияния дифф с main пуст, и regcheck без базы отказывает.
