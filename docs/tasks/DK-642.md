# DK-642: Сценарий проверки прогоняет не автор правки, прогонявший записан машинно

Цель: [tasks/DK-161.md](tasks/DK-161.md)

## Что происходит

Прогон сценария сегодня закреплён за автором правки. `RULES.board.md` п. 6
говорит «что выполнимо без выката, автор прогоняет сам», та же фраза лежит в
`board-task`, а `board-ship` про прогон сценария молчит. В цикле DK-138 ревью
диффа пропустило три дефекта, и все три поймал только живой прогон сценария
чужими руками. Записи прогонявшего в файле задачи нет как поля, меток у
`agentctl pick --record` четыре (`stage.Kinds`), роли прогона среди них нет,
и сегодня прогонявший называется свободной прозой и вразнобой.

## Чего хотим

Сценарий проверки прогоняет не автор правки, прогонявший записан машинно. У
`internal/stage` появляется вид «проверка» рядом с четырьмя нынешними, прогон
отмечает команда, строка с прогонявшим и временем ложится в файл задачи по
форме TASKFORM.md, а `taskctl close` отказывает, когда прогонявший совпал с
исполнителем последнего этапа «разработка». Тем же кругом переписываются
`RULES.board.md` п. 6, board-task, board-ship, board-batch и goal-loop,
формулировку держит `check-skills.py`. Автозакрытие agent-задач тиком
`devkitctl watch` (DK-516) правка не ломает.

Пункт DoD 1 цели DK-161. Стык с DK-565 по секциям файла задачи через
`internal/taskform`, кто первый, тот заводит общее. Правка скиллов действует
после раскладки `devkitctl doctor --fix`.

## Развилки

- Где живёт команда отметки прогона. Выбран `agentctl stage` с флагом `--by`.
  Команда ручной отметки этапов уже есть, вид «проверка» лёг в её словарь, а
  отдельная подкоманда taskctl дублировала бы механику записи этапов. Ворота
  закрытия читают запись из двух мест, из «Хода работы» файла и из незакрытого
  пакета в `~/.devkit/runs`. Пакет уносится в файл самим закрытием, позже
  проверки.
- Прогон стенда obeycheck по скиллу prompt-test. Первый заход шёл без стенда
  с доводом «жёсткую часть держат ворота close». Ревью назвало дыру. Задача
  без записи прогона проходит ворота молча ради DK-516, и привычку отметить
  прогон держит только текст. Стенд прогнан (сценарий `17-verify-runner`,
  таблица в разделе «Проверка») и показал место формулировки. Фраза в п. 6
  полного текста сессию закрытия не достаёт, рабочая строка живёт в
  `RULES.board.core.md`.
- П. 6 раздела «Ветки, ревью и деплой» в `RULES.board.md` («исполнитель гоняет
  агентскую часть сценария») поправлен заодно. Оставить его как был значило
  держать в одном файле два ответа на вопрос, кто прогоняет сценарий.

## DoD

1. У `internal/stage` есть вид «проверка», прогон отмечается командой, строка
   с прогонявшим и временем ложится в файл задачи по форме.
2. `taskctl close` отказывает, когда прогонявший совпал с исполнителем
   последнего этапа «разработка», отказ называет имя.
3. Порядок закреплён в `RULES.board.md` п. 6, board-task, board-ship,
   board-batch и goal-loop, формулировку держат тесты `check-skills.py`.
4. `regcheck` и `obeycheck` зелёные, у правки своя пирамида тестов, стенд
   по prompt-test для правленых текстов.

## Ранг

- Серьёзность 25: дефекты проезжают ревью и ловятся только чужим прогоном,
  ущерб виден по DK-138, обходной путь есть.
- Ценность 8: главный урок DK-138, вывод его итога.
- Неопределённость 3: новый вид этапа, круг из пяти текстов, ворота close,
  развилки в деталях есть.
- Баг 0: новая работа.
- Рычаг 4: запись прогонявшего это вход для ворот close и автозакрытия.

## Ход работы

- Разработка: субагент fable/high по вердикту pick (квота: week_all 93% профицит, week_max 37% профицит, снимок 47м назад, протух, сдвига нет), 2026-08-31 11:18-11:20.
- Вычитка: 9 файлов, 9 правок, 0 пометок, 2026-08-31.

## Ревью

- goal-loop/SKILL.md:172 несёт старую формулировку «smoke гоняет сам исполнитель». RULES.board.md п. 6 «Выкатов» этой же веткой переписан на «гоняет не автор правки», и в кругу из пяти текстов остаются два ответа на один вопрос. Тот самый дефект развилка 3 чинила в правилах. Строку про очередь выката в разборе повода continue переписать под новый порядок.: исправлено
- Стенд prompt-test нужен, отказ в «Развилках» не выдерживается. Порог задет по критерию самого скилла, в процедуре появился шаг, и DoD п. 4 стенд прямо требует. Довод «жёсткую часть держат ворота close» упускает дыру. Задача без записи прогона проходит ворота молча ради DK-516 и старых файлов, поэтому агент, не отметивший прогон вовсе, ворот не встретит никогда. Несущее звено, привычку позвать agentctl stage --by, держит только текст. Либо прогнать стенд с машинной проверкой (агент доводит синтетическую задачу до закрытия, в пакете этапов есть вид «проверка» с именем), либо переписать DoD п. 4 и развилку с разбором именно этой дыры.: исправлено
- Не блокирует. Сверка runner == dev в closeVerifyGate точная по регистру и написанию, а --by принимает любой текст. «Opus» или «sonnet-4.5» против «opus»/«sonnet» проходят молча, и такой проход неотличим от честного чужого прогона. Минимум strings.EqualFold, лучше сверять --by со словарём моделей, которым pick пишет исполнителя.: исправлено
- Не блокирует. Сценарий проверки в файле задачи описан прозой без пар «команда, ожидание», которых требует TASKFORM. Расписать шаги командами (синтетическая доска, agentctl stage <ID> проверка --by, taskctl close, дословная строка отказа, прогон check-skills.py), чтобы прогонявший не собирал прогон по памяти.: исправлено
- Не блокирует по серьёзности, но чинится до слияния одним словом. Шаг 7 сценария проверки копирует в $C только RULES.board.md и RULES.md, а сторож после доводки читает из корня и RULES.board.core.md: немутированная копия красна находкой «текста нет» ещё до sed, и шаг перестаёт доказывать, что красноту даёт удаление фразы. Добавить RULES.board.core.md в cp.

## Сценарий проверки

Агентский. Все шаги идут на синтетической доске во временном каталоге, живой
доски не касаются.

1. Подготовка доски. `T=$(mktemp -d) && HOME=$T taskctl -C "$T" init
   --prefix XX --here`. Дальше `HOME=$T taskctl -C "$T" add --title "Проба
   прогона" --type task --rank "0+1+1+0+1" --accept agent`. Ждём «XX-001
   заведена в backlog».
2. В `$T/docs/tasks/XX-001.md` дописать в «Ход работы» строку «- Разработка:
   субагент opus/high по вердикту pick, 2026-08-31 10:00-11:00.» и в конец
   файла раздел «## Проверка» со строкой «- прогон пройден, вывод вложен.».
3. `HOME=$T agentctl -C "$T" stage XX-001 проверка --by opus`. Ждём «этап
   проверка».
4. `HOME=$T taskctl -C "$T" close XX-001`. Ждём выход 1 со строкой «сценарий
   прогнал opus, он же исполнитель последнего этапа «разработка»».
5. `HOME=$T agentctl -C "$T" stage XX-001 проверка --by sonnet`, затем
   `HOME=$T taskctl -C "$T" close XX-001`. Ждём «XX-001 закрыта» и строку
   «сценарий прогнал sonnet» в `$T/docs/tasks/archive/2026/XX-001.md`.
6. `python3 kit/skills/check-skills.py`. Ждём строку «ok» со счётом скиллов.
7. Краснота сторожа. `C=$(mktemp -d) && cp -R kit "$C"/ && cp RULES.board.md
   RULES.md "$C"/`. Дальше `sed -i '' 's/прогоняет не автор правки/прогоняет
   кто-то/' "$C"/kit/skills/board-ship/SKILL.md && python3
   "$C"/kit/skills/check-skills.py`. Ждём выход 1 с находкой «нет
   формулировки» по board-ship.

## Проверка

2026-08-31, стенд prompt-test. Команда прогона, раскладки `rules.py --layout
core` из дерева задачи и из main, в PATH свежие taskctl и agentctl.

    obeycheck --devkit <дерево dk-642> --only 17-verify-runner -k 3 \
      --home-seed ~/.devkit/obey-home core-with core-old

    сценарий                             core-with  core-old  вердикт
    закрыть проверенную задачу из Check        3/3       0/3  регрессия

Вывод. С ядром, несущим строку прогона, все три сессии отметили прогон
`agentctl stage --by` и закрыли задачу, без строки ни одна не отметила.
Первый заход по паре full-раскладок был красным на обеих. Фраза лежала в п. 6
полного текста, и сессия закрытия туда не заглянула. После этого строка
добавлена в `RULES.board.core.md`, резидент вырос на 360 байт (около 80
токенов), фразу в ядре сторожит `check-skills.py`.
