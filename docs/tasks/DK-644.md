# DK-644: Журнал цели несёт время витка, итог называет, где время ушло

Цель: [tasks/DK-161.md](tasks/DK-161.md)

## Что происходит

Времени в журнале цели не пишет никто. Формат строки витка это «дата, что
сделано, маркер», пишется она руками, а машинная метка в файле цели одна,
строка снимка квоты с минутной точностью, и несёт она момент съёма панели
`/usage`. В DK-138 на девять витков пришлось пять снимков, и на вопрос
«куда ушёл день» ответа в файле цели нет. Посекундные метки пишет только
оболочка `goal-run.py` в `.devkit/goal-<ID>.log`, цели в живом чате они не
достаются.

## Чего хотим

Журнал цели несёт время витка, итог называет, где время ушло. Строка витка
получает начало и конец, стоп получает длительность, времена пишет команда,
сводку итога собирает из этапов задач цели (`internal/stage` уже держит
времена и виды, DK-503). Гейт бюджета (`agentctl spend` читает только строки
с префиксом «снимок ») и детектор воронки в `goal-run.py` от новой строки не
ломаются. Формат держат юниты goal-loop.

Пункт DoD 4 цели DK-161. Ранг ниже остальных задач цели, при нехватке
week_max откладывается первой.

## DoD

1. Строка витка получает начало и конец, стоп длительность, времена пишет
   команда.
2. Сводка итога собирается из этапов задач цели и называет разбивку по видам.
3. Гейт бюджета и детектор воронки работают на файле с новыми строками как
   раньше.
4. `regcheck` и `obeycheck` зелёные, у новых команд своя пирамида тестов,
   формат строки витка держат юниты goal-loop.

## Ранг

- Серьёзность 25: расход времени цикла нечитаем, разбор «куда ушёл день»
  невозможен по журналу, работе это не мешает.
- Ценность 6: наблюдаемость времени, четвёртый пункт DoD цели.
- Неопределённость 2: источники времён готовы (`internal/stage`), открыты
  детали формата.
- Баг 0: новая работа.
- Рычаг 3: новый формат строки витка это вход DK-145 (прогресс цели).

## Ход работы

- Разработка: субагент opus/medium по вердикту pick (квота: week_all 5%, week_max 37% протух (панель разбивку не дала, цифра из кеша клиента возрастом 5ч 29м назад), снимок 19м назад, сдвига нет), 2026-08-31 16:19-16:19.

## Ревью

- Тело kit/skills/goal-loop/SKILL.md выросло с 15970 до 16799 символов, порог weigh (SKILL_BODY_LIMIT) 16000: на main правки нет, находка доктора новая, её принесла именно эта правка ("тело скилла goal-loop: 16 799 символов, порог 16 000 символов; резать скилл надвое"). Ужать вставку шагов 5-6: в шаге 5 явно избыточны предложения "Формат это «часы витка, что сделано, маркер»." (дублирует пример строки) и "Руками времена не ставить, у витка в живом чате они те же." (хвост после запятой лишний, тест ждёт только фразу «времена не ставить»); в шаге 6 предложение "Видно и сколько цикл занял, и на чём он стоял." дублирует предыдущее "Разбивка идёт по видам этапов и по самим задачам." и снимается целиком. Разбор начала и конца витка, перехода через полночь и семантики стоп-маркера уже подробно расписан в tools/agentctl/README.md, поэтому в скилле стоит оставить только команду, обязательные флаги, фразы «времена не ставить» и «по видам этапов» (их держат тесты SkillRecordTests) и один пример строки, а детали убрать ссылкой на README. Прямого прироста от вставки 1308 байт (829 символов), а порог превышен на 799 символов: точечной подрезки двух названных предложений (не больше 300-400 символов) не хватит, нужна более плотная правка обоих шагов с запасом, как просит соседняя задача DK-211 «взять запас под следующие правки».

## Сценарий проверки

Агентский. На подставном файле цели команда дописывает строку витка с
временами, сводка печатает разбивку по видам этапов, `agentctl spend` читает
снимки квоты как раньше.

## Проверка

Подставная цель SC-100 с двумя задачами, у каждой свой «Ход работы»; в
«Журнале» один снимок квоты.

```text
$ agentctl lap --goal docs/tasks/SC-100.md --marker continue \
    --note "нарезка: 2 задачи SC-101, SC-102, порядок по dep"
- 2026-08-31 09:05-16:39, нарезка: 2 задачи SC-101, SC-102, порядок по dep; continue
виток занял 7ч 34м

$ agentctl lap --goal docs/tasks/SC-100.md --marker done --start "2026-08-31 14:20" \
    --note "SC-101 и SC-102 закрыты, DoD цели сошёлся"
- 2026-08-31 14:20-16:39, SC-101 и SC-102 закрыты, DoD цели сошёлся, цикл 7ч 34м, витков 2; done
виток занял 2ч 19м

$ agentctl tally --goal docs/tasks/SC-100.md
время задач цели: всего 4ч 00м, этапов 4, задач 2
- разработка: 3ч 15м, этапов 2
- ревью: 30м, этапов 1
- снаружи: 15м, этапов 1
- уточнение: 0м, этапов 0
- дольше прочих: SC-101 3ч 00м, SC-102 1ч 00м

$ agentctl spend --goal docs/tasks/SC-100.md
gate: ok
week_all: потрачено 5 из 25; точек расхода 2; снимок 39м назад
```

Детектор воронки оболочки на строках витка не сбивается, это держат юниты
`kit/skills/goal-loop/goal_run_test.py`.
