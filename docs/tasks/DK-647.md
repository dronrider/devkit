# DK-647: Молчащий из-за квоты чат доносит человеку причину и срок сброса

## Что происходит

Материал собран 31.08 разбором транскриптов обеих harness на машине, порядка 480 сессий за 10 дней. Примеры повторимы по файлам:

- Чат «Груминг DK-640 с вопросами в чате», сессия 22fb4dce в ~/.claude/projects/-Users-rider-projects-devkit/. Ход закрыт в 12:04, в 12:09 сообщение с панели принято очередью (queue-operation dequeue), после него в транскрипте нет ни одной записи: модель не запустилась, недельная квота исчерпана. Единственный след на всей машине это статус-строка окна tmux task-DK-640: «Weekly limit reached, Retrying in 1h (3pm), attempt 1/300». Второе сообщение от 12:22 лежит в очереди за первым.
- Та же harness, сессия 5782ec20: замолчала так же в 11:51, конец транскрипта queue-operation, ошибочных записей ноль.
- 29.08, claude-glm, сессия d351231e: «Login expired, Please run /login» приехало одной синтетической строкой после 3.5 минут видимой тишины, ретраи невидимы (cost-state: 768 с API-времени против 0.8 с реальной генерации), оживление только ручным resume через 3 ч 36 м; вторая ошибка приехала вовсе без пользовательского хода.
- 29.08, сессия 893a9c4c: спавн ревьювера DK-581, промпт доставлен, ни ответа, ни ошибки, ноль токенов; причина зафиксирована только родительской сессией (процесс завершён, exit 144).
- 22.08, вторая harness: 8 сессий за 18 минут умерли на «OAuth session expired and could not be refreshed», ни одна не возобновилась, доска наплодила новые.
- Реестр ~/.devkit/sessions.log пишет только старты, завершений не фиксирует, на 26-29.08 в нём дыра.

Человек пишет в чат с панели и не получает ни ответа, ни ошибки, ни срока: сообщение лежит в очереди часами, харнес заявляет 300 почасовых ретраев, работа стоит, а со стороны доски сессия неотличима от живой, но задумавшейся. За 10 дней насчитано минимум 11 полностью немых смертей (спавны ревьюверов, генератор заголовков, проверки канала), оживление каждый раз ручное, resume с реплеем того же сообщения.

### Разбор 31.08

Половина вопроса снялась чтением кода. Дашборд уже снимает пейн окна (tools/dashboard/tmux.go) и держит у чата состояния live, idle, «молчит», Login и три рода клина (tools/dashboard/chats.go). Тик демона ходит раз в 10 минут, тик сторожка раз в 5. Детектор Login ловит истёкший вход по служебной строке в транскрипте. Случай d351231e он бы закрыл, строка туда приехала.

Дыра в квоте. Уперевшаяся сессия не пишет в транскрипт ничего, сокет при этом отвечает. Готовые детекторы считают её живой. Строку ретрая «Retrying in 1h (3pm), attempt 1/300» никто не разбирает, греп по дереву пуст. Срок сброса квоты уже лежит в снимке ~/.devkit/quota (DK-633), его можно сцепить со строкой экрана. Расширять sessions.log завершениями не нужно, сессия в ретрае жива.

Вторая половина закрыта ответом автора 31.08. Писать человеку в чат, что лимит подписки исчерпан, и называть дату сброса, чтобы было видно, сколько ждать. Причина и срок дублируются в живом статусе, сообщение остаётся в очереди.

## Чего хотим

Тишина чата видна снаружи: в панели у молчащего чата стоят причина и срок (квота до 15:00, вход истёк), сообщение человека не сгорает бесследно.

## Границы

Немые смерти спавнов вне задачи: ревьюверы и генератор заголовков человеку в
чат не пишут, их причина живёт в exit-коде родителя, отдельная строка позже.
Истёкший вход трогать не нужно, его ловит готовый детектор Login по
транскрипту. Реестр sessions.log завершениями не расширяется, сессия в
ретрае жива.

## Развилки

Где сторожу брать статус тишины: строка ретрая живёт только на экране tmux-окна, в транскрипте и реестре следа нет. Парсить экран живого процесса, расширять sessions.log завершениями или поднимать статус из самой панели? И что показывать человеку: поле в живом статусе, ответ-статус в чат вместо тишины, будильник на срок ретрая?

## DoD

Молчащий от квоты чат отвечает человеку в панели. На отправленное сообщение
приходит статус вместо тишины: лимит подписки исчерпан, сброс в такое-то
время, сообщение осталось в очереди. Та же причина и срок стоят в живом
статусе чата.

## Ранг

`50+8+2+0+4 = 64`, P1.

- Серьёзность 50: человек пишет в чат с панели и не получает ни ответа, ни
  ошибки, ни срока. Обход болезненный, смотреть экран окна руками и оживлять
  сессию resume.
- Ценность 8: ответ автора закрывает вопрос, ради него задача и затевается,
  «чтобы он знал, сколько ждать».
- Неопределённость 2: источник статуса снят разбором, кадр пейна плюс снимок
  квоты DK-633. Открыты детали: порог молчания, сцепка строки экрана со
  снимком, форма ответа в чат.
- Поправка на баг 0: наблюдаемости не было никогда, это новое поведение.
- Рычаг 4: за 10 дней на машине 11 немых смертей, каждая стоила часов простоя
  и ручного оживления.

## Приёмка

- вид: mixed
- барьер «событие»: живой формат строки ретрая подтверждается только на
  настоящей немой парковке. Синтетика закрывает механику
  - игрушечные часы: не годится, подмена времени не порождает саму строку
    ретрая харнеса на экране
  - синтетические входящие: не годится, строка ретрая не входящее сообщение,
    а собственный вывод чужого процесса в его pty, входящих там нет
  - стенд с подменой события: не годится закрыть барьер целиком. Закрывает
    механику (разбор строки, сцепка со снимком квоты DK-633, вынос причины и
    срока в панель тестами `quotawait_test.go` и стендом
    `testdata/poc_quotawait.mjs`), но точный текст, которым харнес пишет о
    себе на живом ретрае, тестами не подтверждается

## Ход работы

- Из черновика DK-647 «чаты молчат при истечении подписки и квоты, след только на экране окна»: записан 2026-08-31, оформлен 2026-08-31.
- 2026-08-31, отложен: вопрос автору: что показывать при молчании из-за квоты (причина и срок в панели, ответ-статус в чат, будильник на срок ретрая) и отрезать ли немые смерти спавнов границей; разбор источника статуса 31.08 уже дописан в файл, вернуться с ответом
- Вычитка: 6 файлов, 28 правок, 2 пометки, 2026-09-03.

## Сценарий проверки

Первая часть гоняется командами (агентская часть смешанной приёмки), вторая
глазами в панели дашборда (пользовательская часть, барьер «событие»).

1. Разбор строки ретрая, сцепка со снимком `~/.devkit/quota` и вынос причины
   со сроком в список разговоров, в живой статус открытой панели и в ответ
   ручки `/say`:

   ```sh
   cd $(git rev-parse --show-toplevel)/tools/dashboard && GOWORK=off go test ./... -run Quota -v
   ```

   Ожидание: строка `PASS`, в выводе видны все подтесты, среди них
   `TestQuotaRetryStuck`, `TestChatListShowsQuotaWait`,
   `TestChatListQuotaWaitUnknownResetHonest`,
   `TestChatSayQuotaWaitTellsStatusInsteadOfSilence` и
   `TestStaticChatQuotaWaitTellsThePanel` (стенд `testdata/poc_quotawait.mjs`
   в node, живая разметка `static/app.js`).

2. - Исключение: обкатка (нужен браузер и живой дашборд, шаг только смотрит).
   Открыть дашборд, зайти в любой проект и открыть панель разговоров.
   Щёлкнуть по заголовку текущего чата в шапке панели (стрелка рядом с
   именем разговора раскрывает список). У обычных живых чатов чипа причины
   рядом со словом состояния быть не должно. Список не путает занятого
   агента с ожиданием сброса подписки.

3. - Исключение: обкатка (живой формат строки ретрая харнеса подтверждает
   только настоящий случай, барьер «событие» не снят). Дождаться очередного
   молчания чата от исчерпанной недельной подписки. Признак тот же, что
   нашёл разбор 31.08, это статус-строка ретрая на экране tmux-окна.
   Проверить три вещи. В выпадающем списке разговоров у этого чата стоит чип
   «лимит подписки исчерпан, сброс ЧЧ:ММ». Открытая панель показывает ту же
   строку живым статусом над лентой, поле ввода не заперто. Реплика,
   отправленная в этот чат, возвращает пузырь «не доставлено: лимит подписки
   исчерпан, сброс ЧЧ:ММ». Та же строка встаёт пометкой в ленте вместо
   часовой тишины. Разошёлся реальный текст экрана с разобранным («limit
   reached» и «retrying»)? Это узкая находка на `quotaRetryStuck` в
   `tools/dashboard/quotawait.go`, на всю задачу она не тянет.

## Проверка


- Обкатка: 2026-09-03 20:59, свежее дерево a016f728a608, сценарий 8ea88ca8, временный HOME, утилит дерева 10, шагов 1, все зелёные.

Шаг 1 (зелёный):

```console
$ cd $(git rev-parse --show-toplevel)/tools/dashboard && GOWORK=off go test ./... -run Quota -v
=== RUN   TestQuotaTwoHarnesses
--- PASS: TestQuotaTwoHarnesses (0.01s)
=== RUN   TestQuotaStale
--- PASS: TestQuotaStale (0.00s)
=== RUN   TestQuotaNoTaken
--- PASS: TestQuotaNoTaken (0.00s)
=== RUN   TestQuotaEmpties
--- PASS: TestQuotaEmpties (0.01s)
=== RUN   TestQuotaBrokenLines
--- PASS: TestQuotaBrokenLines (0.00s)
=== RUN   TestQuotaPartialNoteIgnored
--- PASS: TestQuotaPartialNoteIgnored (0.00s)
=== RUN   TestQuotaBucketWithoutReset
--- PASS: TestQuotaBucketWithoutReset (0.00s)
=== RUN   TestQuotaNeedsLogin
--- PASS: TestQuotaNeedsLogin (0.00s)
=== RUN   TestQuotaNoHarnessNamesInCode
--- PASS: TestQuotaNoHarnessNamesInCode (0.00s)
=== RUN   TestQuotaRefreshDirNotCwd
--- PASS: TestQuotaRefreshDirNotCwd (0.02s)
=== RUN   TestQuotaRefreshDirSkipsUntrusted
--- PASS: TestQuotaRefreshDirSkipsUntrusted (0.02s)
=== RUN   TestQuotaFailReachesPlate
--- PASS: TestQuotaFailReachesPlate (0.02s)
=== RUN   TestQuotaFailLoggedOnce
--- PASS: TestQuotaFailLoggedOnce (0.02s)
=== RUN   TestQuotaFailPanelVsClient
--- PASS: TestQuotaFailPanelVsClient (0.00s)
=== RUN   TestQuotaFailWords
--- PASS: TestQuotaFailWords (0.00s)
=== RUN   TestStaticQuotaFailLine
    quota_test.go:499: poc_quotafail: ok
--- PASS: TestStaticQuotaFailLine (0.15s)
=== RUN   TestQuotaFailSaidShort
--- PASS: TestQuotaFailSaidShort (0.03s)
=== RUN   TestQuotaAgePerHarnessWithoutSpread
--- PASS: TestQuotaAgePerHarnessWithoutSpread (0.01s)
=== RUN   TestQuotaRefreshRunCoversAllSubscriptions
--- PASS: TestQuotaRefreshRunCoversAllSubscriptions (0.01s)
=== RUN   TestQuotaBucketExpired
--- PASS: TestQuotaBucketExpired (0.00s)
=== RUN   TestQuotaStaleThresholdConfigured
--- PASS: TestQuotaStaleThresholdConfigured (0.00s)
=== RUN   TestQuotaRetryStuck
=== RUN   TestQuotaRetryStuck/строка_ретрая_недельного_лимита
=== RUN   TestQuotaRetryStuck/обычный_вывод_агента
=== RUN   TestQuotaRetryStuck/пусто
=== RUN   TestQuotaRetryStuck/одно_из_двух_слов_без_второго
=== RUN   TestQuotaRetryStuck/перенос_строки_посреди_фразы
=== RUN   TestQuotaRetryStuck/регистр_не_важен
--- PASS: TestQuotaRetryStuck (0.00s)
    --- PASS: TestQuotaRetryStuck/строка_ретрая_недельного_лимита (0.00s)
    --- PASS: TestQuotaRetryStuck/обычный_вывод_агента (0.00s)
    --- PASS: TestQuotaRetryStuck/пусто (0.00s)
    --- PASS: TestQuotaRetryStuck/одно_из_двух_слов_без_второго (0.00s)
    --- PASS: TestQuotaRetryStuck/перенос_строки_посреди_фразы (0.00s)
    --- PASS: TestQuotaRetryStuck/регистр_не_важен (0.00s)
=== RUN   TestQuotaRetryWeekly
--- PASS: TestQuotaRetryWeekly (0.00s)
=== RUN   TestQuotaResetHuman
--- PASS: TestQuotaResetHuman (0.00s)
=== RUN   TestQuotaWaitNote
--- PASS: TestQuotaWaitNote (0.00s)
=== RUN   TestChatListShowsQuotaWait
--- PASS: TestChatListShowsQuotaWait (0.07s)
=== RUN   TestChatListNoQuotaOnOrdinaryPane
--- PASS: TestChatListNoQuotaOnOrdinaryPane (0.07s)
=== RUN   TestChatListQuotaWaitUnknownResetHonest
--- PASS: TestChatListQuotaWaitUnknownResetHonest (0.07s)
=== RUN   TestChatSayQuotaWaitTellsStatusInsteadOfSilence
--- PASS: TestChatSayQuotaWaitTellsStatusInsteadOfSilence (0.32s)
=== RUN   TestStaticChatQuotaWaitTellsThePanel
    quotawait_test.go:183: ретрай кончившейся подписки виден живым статусом и чипом списка, поле ввода не гасится
--- PASS: TestStaticChatQuotaWaitTellsThePanel (0.06s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	1.683s
```
