# DK-656: Список чатов: текущий сверху, порядок замер, уборка переключает панель

## Что происходит

Список чатов сейчас отображается в порядке активности в них. Чаты с самыми свежими сообщениями будут выше.

Проблематика состоит из трёх частей.
Часто нужно открыть список чатов чтобы отправить текущий чат в архив. Но из-за того что записи сортируются по активности строки в списке чатов могут убежать и так можно поместить в архив не ту запись что нужно.
Помимо этого запись текущего чата может быть где-то ниже и до неё придётся долистывать.
Ну и третья проблема в том, что после закрытия чата диалог сам не закрывается и приходится лезть в список ещё раз чтобы переключиться на следующий актуальный чат.

Возможно проблема усугубляется тем что в сортировке списка чатов есть баг, который отбрасывает свежие записи вниз. Я наблюдал сегодня как такое случилось и свежая запись переехала в блок Сегодня, при этом выше была запись сильно старше.

### Что нашёл разбор

Читал `tools/dashboard/static/app.js` и `tools/dashboard/chats.go`, все три части подтвердились.

Строки правда едут под пальцем, и причина не в сортировке. Уборка в архив зовёт `done`, тот перерисовывает список и следом идёт за свежим перечнем на сервер (`refresh`), поэтому после каждого нажатия порядок пересчитывается заново. Убирают пачкой, и соседняя строка успевает встать на место убранной.

Текущий чат в списке помечен подсветкой строки (класс `on` в `chatOption`), но стоит на своём месте по времени, и в длинном списке уезжает за экран.

Панель после уборки остаётся на убранном разговоре: `done` трогает только список.

Свежая запись под старой это не баг сортировки. `chatGroups` кладёт первой группой все живые сессии, без подписи, а дальше идут группы по дням. Живой разговор трёхдневной давности стоит выше сегодняшнего мёртвого, и заголовок «сегодня» встаёт под ним. Сама сортировка (`sortEntries` в `chats.go`) идёт строго по времени последней содержательной реплики, времена сведены к одному формату.

### Развилки, снятые груммингом

Автор ответил пачкой 2026-09-01, все четыре ответа рекомендованными вариантами.

- Группа живых остаётся, но получает подпись: без неё порядок читается как поломка.
- Порядок строк замирает на время, пока список открыт. Убранная строка гаснет на месте, новый порядок берётся при следующем открытии.
- Текущий чат идёт отдельной группой сверху, с подписью, ниже обычные группы.
- Убрали текущий чат, панель переключается на следующий разговор из оставшихся, а список остаётся открытым.

## Чего хотим

Открытый список начинается с текущего разговора, и до него не надо долистывать. Пока список открыт, строки стоят на месте, и уборка пачкой не промахивается мимо соседа. Убранный текущий чат уводит панель на следующий разговор, второго захода в список для этого не нужно.

## Границы

Три положения кнопки архива, поиск по списку и окно догрузки («показать раньше») остаются как есть. Подтверждение уборки живой сессии тоже не трогается.

## DoD

В открытом списке чатов первая строка это текущий разговор, под ней подписанная группа живых, дальше группы по дням. Пока список открыт, строки не переставляются: уборка гасит строку на месте, а порядок пересчитывается только при следующем открытии. Уборка текущего чата переводит панель на следующий разговор из оставшихся. Поведение закрыто тестами модулей фронта, README дашборда рассказывает про порядок групп.

## Ранг

`25+6+1+0+2 = 34`, P2.

- Серьёзность 25: заметное трение в ежедневной работе, обход дешёвый и очевидный, долистать список и найти подсвеченную строку («до неё придётся долистывать»).
- Ценность 6: ощутимое улучшение потока, о котором просят, но главную ценность дашборда оно не двигает («часто нужно открыть список чатов»).
- Неопределённость 1: подход выбран ответами автора, места правки названы разбором (`chatGroups`, `chatOption`, `done`), правка живёт в одном файле фронта.
- Поправка на баг 0: прежнее поведение работало как задумано, наблюдение про сортировку разбор снял.
- Рычаг 2: небольшая своя польза, разговоров в день проходит десяток и каждый дешевеет.

## Приёмка

- вид: mixed
- барьер «глаза»: порядок строк и подписи групп видно только на экране.
  Тест по разметке берёт порядок и состав групп, вид подписей смотрит человек;
  перебор обходов пишет исполнитель.
  - утверждённый макет: не годится, макета под этот список на старте работы не было
  - headless-браузер с замером: не годится. Спор тут про текст подписи и про то, какая строка первая. Ширины и раскладка к делу не относятся
  - тест по разметке и стилям: годится частично. Порядок строк и состав групп проверяет тест. Слова подписи и опрятность вида остаются человеку

## Ход работы

- Из черновика DK-656 «[dashbord] Показывать в списке чатов первым текущий чат»: записан 2026-08-31, оформлен 2026-09-01.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 27%, снимок 44м назад, сдвига нет), 2026-09-01 15:38-15:38.

## Ревью

- Сценарий проверки состоит из одних тестов ветки. Шаги 1 и 2 это `go test -run 'TestStaticChatListGroups'` и `-run 'TestStaticChatArchiveCurrentSwitchesPanel'`. Оба гоняют `testdata/poc_chatgroup.mjs` и `testdata/poc_chatnext.mjs` против static/app.js прямо в дереве, через синтетический DOM (poc_dom.mjs). Это те же тесты из коммита 3d667f32 в chat_test.go. Они зелёные уже в обкатке до слияния, запись «Проверка» снята на дереве 88bb86351f68, раньше финального докс-коммита. По правилу ACCEPTANCE.md «Самодостаточность агентской части» тест, который гоняется по файлу в дереве, о выкаченном не говорит ничего. Агентской частью такой тест не становится. В этом же проекте есть прецедент. DK-693 получил ровно такое замечание. Исправление добавило шаг 3, он поднимает дашборд (`go run . smoke`) и проверяет поведение через живую ручку сервера, а не повтором go test. Для DK-656 обход исполним. В репозитории уже есть headless-chrome стенд против поднятого дашборда (board_test.go, testdata/board_narrow.js). Дашборд можно поднять и через настоящий браузер открыть список чатов, проверить группу «текущий разговор»/«живые» и переключение панели после уборки на реально отданной сервером странице. Это и есть шаг, который доказывает что-то про выкаченное. Сценарий переписывается: нужен хотя бы один шаг после выката или слияния, не повторяющий тесты ветки.: исправлено, Добавлен шаг 3: TestDashboardSmokeChatGroupOrder (tools/dashboard/chat_smoke_test.go) поднимает httptest-сервер настоящими обработчиками newTestEnv, пишет синтетические сессии и открывает отданную сервером страницу настоящим headless chrome. Список открывается настоящим нажатием кнопки шапки панели, уборка текущего разговора настоящим нажатием кнопки архива строки; порядок и подписи групп и переключение панели читаются из настоящего DOM. Обкатка taskctl rehearse зелёная под временным HOME (запись «Проверка»).

## Сценарий проверки

1. Группировка списка и заморозка порядка при уборке. Стенд
   `testdata/poc_chatgroup.mjs`.

   ```sh
   cd tools/dashboard && GOWORK=off go test ./... -run 'TestStaticChatListGroups' -v
   ```

   Ожидание. Строка `--- PASS: TestStaticChatListGroups`, а в логе видна
   `poc_chatgroup: ok, текущий разговор и живые стоят подписанными группами
   сверху, а уборка гасит строку на месте до следующего открытия`.

2. Переключение панели после уборки текущего разговора. Стенд
   `testdata/poc_chatnext.mjs`.

   ```sh
   cd tools/dashboard && GOWORK=off go test ./... -run 'TestStaticChatArchiveCurrentSwitchesPanel' -v
   ```

   Ожидание. Строка `--- PASS: TestStaticChatArchiveCurrentSwitchesPanel`, а
   в логе видна `poc_chatnext: ok, уборка текущего разговора переводит
   панель на следующий без повторного захода в список`.

3. Живой сервер настоящим браузером (агентская часть). Тест поднимает
   httptest-сервер реальными обработчиками (`newTestEnv`), пишет синтетические
   сессии (одну живую трёхдневной давности, несколько мёртвых сегодняшних) и
   открывает отданную сервером страницу настоящим headless chrome. Список
   чатов открывается настоящим нажатием кнопки шапки панели, уборка текущего
   разговора настоящим нажатием кнопки архива строки. Порядок и подписи групп
   и переключение панели читаются из настоящего DOM.

   ```sh
   cd tools/dashboard && GOWORK=off go test ./... -run 'TestDashboardSmokeChatGroupOrder' -v
   ```

   Ожидание. Строка `--- PASS: TestDashboardSmokeChatGroupOrder`, а в логе
   видна `smoke: живой сервер ..., группы списка и переключение панели
   подтверждены настоящим DOM headless-chrome`. Headless chrome ищется
   `findChrome()` (PATH, кэш playwright, переменная `DASHBOARD_CHROME`).
   Браузера на машине нет, шаг пропускается строкой `--- SKIP`. Пропуск это
   повод доставить headless chrome на машину прогона, зелёным этот шаг
   считает только настоящий `PASS`.

4. Глазами. Открыть дашборд, зайти в проект с несколькими разговорами и
   открыть список чатов кнопкой в шапке панели. Первой строкой стоит открытый
   сейчас разговор под подписью «текущий разговор». Группа «сегодня» стоит на
   своём месте и не уезжает вниз под более старой живой сессией. Убрать
   открытый разговор в архив прямо из строки списка. Панель переключается на
   следующий разговор из оставшихся без повторного захода в список.

## Проверка


- Обкатка: 2026-09-01 17:42, свежее дерево d098c4b744c6, сценарий 2c1d777e, временный HOME, шагов 3, все зелёные.

Шаг 1 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test ./... -run 'TestStaticChatListGroups' -v
=== RUN   TestStaticChatListGroups
    chat_test.go:2930: poc_chatgroup: ok, текущий разговор и живые стоят подписанными группами сверху, а уборка гасит строку на месте до следующего открытия
--- PASS: TestStaticChatListGroups (0.08s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	0.622s
```

Шаг 2 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test ./... -run 'TestStaticChatArchiveCurrentSwitchesPanel' -v
=== RUN   TestStaticChatArchiveCurrentSwitchesPanel
    chat_test.go:2947: poc_chatnext: ok, уборка текущего разговора переводит панель на следующий без повторного захода в список
--- PASS: TestStaticChatArchiveCurrentSwitchesPanel (0.07s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	0.544s
```

Шаг 3 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test ./... -run 'TestDashboardSmokeChatGroupOrder' -v
=== RUN   TestDashboardSmokeChatGroupOrder
    chat_smoke_test.go:281: smoke: живой сервер http://127.0.0.1:62379/__smoke__#demo/chat/dead2, группы списка и переключение панели подтверждены настоящим DOM headless-chrome
--- PASS: TestDashboardSmokeChatGroupOrder (0.35s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	0.841s
```
