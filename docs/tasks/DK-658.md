# DK-658: Разбор в RULES.md называет, чему в комментарии не место

## Что происходит

Соседняя команда держит в AGENTS.md добавки к правилу комментариев, пример
принесён пользователем 2026-08-31 и разобран в сессии. Добавки запрещают в
комментарии историю события, процесс с планами и голый номер тикета, каждому
запрету дана замена. Наше правило про комментарии стоит строкой в ядре и
абзацем разбора в RULES.md, в пункте про тикеты и авторов.

Наш разбор отвечает только на вопрос «писать ли». В написанный комментарий
проходят история падения, планы доработки и оценки автора. Контур вычитки
комментарии не видит, вход proofread это документы. Планы оседают в коде
вместо доски, история дублирует гит, а оценку читателю сверить не с чем.

## Чего хотим

У пункта про комментарии в RULES.md стоит типология содержимого с заменами.
История переписывается фактом настоящего времени, план уезжает черновиком на
доску, ссылка на тикет остаётся только источником решения или ограничения.
Ядро не меняется. Запрет оценок отдельно не заводится, он уже стоит пунктом 7
вычитки proofread.

## Границы

Сторож комментариев в диффе не заводится. На грумминге 2026-08-31 решено
обкатать правило и вернуться к сторожу по событию. Повод для возврата тоже
назван. Если после правки в дифф продолжают проходить комментарии с историей
или планами, пишется новый черновик с живыми примерами.

## Развилки

Нужен ли сторож комментариев в диффе расширением proofread, или хватает
правила в момент письма.

## DoD

В пункте про комментарии в RULES.md стоит типология содержимого с заменами.
История события переписывается фактом настоящего времени. План уезжает
черновиком на доску. Ссылка на тикет остаётся только источником решения или
ограничения. Ядро не трогается.

## Ранг

`25+5+1+0+1 = 32`, P2.

- Серьёзность 25: трение с дешёвым обходом, мусор в комментариях чистится на
  ревью руками, а планы оседают в коде вместо доски.
- Ценность 5: правило действует на каждое письмо кода агентом, операция идёт
  много раз в день.
- Неопределённость 1: подход выбран на грумминге, типология с заменами в
  RULES.md, осталось сделать.
- Поправка на баг 0: доработка правила, не регресс.
- Рычаг 1: правка текста правил без новой механики.

## Ход работы

- Из черновика DK-658 «разбор в RULES.md называет, чему в комментарии не место»: записан 2026-08-31, оформлен 2026-08-31.
- Вычитка: 1 файл, 6 правок, 2 пометки, 2026-09-01.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 25%, снимок 36м назад, сдвига нет), 2026-09-01 11:42-11:42.

## Сценарий проверки

1. Типология с заменами (история, план, ссылка на тикет) стоит в пункте про
   комментарии раздела «Код и тексты»:
   ```sh
   grep -c "Из написанного убрать три вещи, у каждой есть замена" RULES.md
   ```
   Ожидание: `1`.
2. Ядро правил не тронуто, разбор типологии в нём не дублируется:
   ```sh
   ! grep -q "Из написанного убрать три вещи" RULES.core.md
   ```
   Ожидание: код возврата `0` (совпадений в ядре нет).
3. Правленый текст не выходит за раскладку en/ru:
   ```sh
   python3 hooks/check-symbols.py RULES.md
   ```
   Ожидание: пустой вывод, код возврата `0`.

## Проверка


- Обкатка: 2026-09-01 11:46, свежее дерево f4127ed88b18, сценарий 374fb98e, временный HOME, шагов 3, все зелёные.

Шаг 1 (зелёный):

```console
$ grep -c "Из написанного убрать три вещи, у каждой есть замена" RULES.md
1
```

Шаг 2 (зелёный):

```console
$ ! grep -q "Из написанного убрать три вещи" RULES.core.md

```

Шаг 3 (зелёный):

```console
$ python3 hooks/check-symbols.py RULES.md

```

2026-09-01, obeycheck --only 18-comment-typology -k 3 ../rules-with ../rules-without

    сценарий                                            rules-with  rules-without  вердикт
    комментарий workaround без истории, плана и тикета         3/3            3/3  держится

Вывод: обе раскладки зелёные на всех трёх повторах. Sonnet и без нового
текста уже избегает истории, плана и голого тикета в спровоцированном
комментарии. Регрессии правка не даёт, разницу в поведении сильной модели
сценарий не показывает. Ценность правки в письменном стандарте для ревью и
для более слабых моделей, а не в машинно замеренном сдвиге поведения sonnet.
