# DK-664: Сторожок цели идёт с голым PATH launchd, и разлив очереди падает

## Что происходит

~/.devkit/goal-watch.log на 2026-08-31 циклически пишет «taskctl не найден в
PATH» по трём корням: devkit, it-road-course, xr-proxy.

Надзор крутится вхолостую: парковки и переходы цели никто не будит, и цель
стоит, пока в лог не заглянут руками. Молчание не отличить от штатной работы.

## Чего хотим

Сторож находит taskctl из любого окружения запуска, а падение такого рода
видно снаружи, не только в логе.

## Развилки

Откуда сторожу брать PATH: зашить каталог бинарей при установке или тянуть
профиль пользователя при запуске.

Решение: зашить. `doctor --fix` считает PATH агента по машине (наличием
каталогов) тем же сборщиком, что у дашборда, `dashboard.agent_path`, и кладёт
готовую строку в `EnvironmentVariables` plist. Профиль пользователя (`.zshrc`
и подобные) launchd не читает вовсе, у него своя, отдельная от логина среда
запуска: тянуть его смысла нет. Зашитый срез вместо живого PATH сессии не
плывёт от прогона к прогону, и `doctor --fix` не переписывает агента заново
на каждом вызове (тот же довод уже стоит в docstring `dashboard.agent_path`
про дашборд).

## DoD

Launchd-агент сторожка получает PATH тем же сборщиком, что агент дашборда, и
разлив очереди из-под него доходит до конца. Доктор считает агента без PATH
находкой и переписывает plist по `--fix`, как переписывает показывающего не
туда. Тест держит каталог бинарей в PATH агента, краснота сверена прогоном
regcheck. Снаружи это видно журналом сторожка. Строки про ненайденный taskctl
уходят, на их месте печатается отчёт разлива.

## Ранг

`50+8+1+5+4 = 68`, P1.

- Серьёзность 50: надзор цели крутится вхолостую, парковки и переходы никто не
  будит, а очередь слияния стоит часами; обход есть, но ручной, звать разлив и
  читать журнал глазами.
- Ценность 8: на этом сторожке держатся автономный цикл цели и разлив очереди,
  и сейчас он молчит про собственную бесполезность.
- Неопределённость 1: подход взят у соседнего агента, осталось позвать тот же
  сборщик PATH.
- Поправка на баг +5: сторожок работал, пока его дети не пошли за утилитами.
- Рычаг 4: чинится носитель автономного цикла, от него дешевеет каждая
  следующая цель и каждая пачка.

Цена S, это раздел в plist, вызов готового сборщика и тест рядом. Вид приёмки
агентский. Агент подделывает дом, зовёт проверку носителя и читает plist, ни
один из шести барьеров не назван.

## Ход работы

- Из черновика DK-664 «надзор цели падает на «taskctl не найден в PATH» по всем корням»: записан 2026-08-31, оформлен 2026-09-01.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 0% профицит, снимок 3ч 48м назад, протух, сдвига нет), 2026-09-03 04:55-04:56.
- 2026-09-03: `plist_text` в tools/devkitctl/watch.py несёт раздел
  `EnvironmentVariables`/`PATH`. Собран он тем же сборщиком, что у
  launchd-агента дашборда, `dashboard.agent_path`, по бинарю dashboard из
  PATH. `check()` получил ключ `which` (по умолчанию `shutil.which`) и новую
  находку. Бинаря devkit нет в PATH, значит собирать PATH агента неоткуда.
  Класть plist `--fix` тогда не берётся. Старый plist без такого PATH это
  находка тем же путём, каким находкой считается plist, показывающий на
  чужой чекаут: обе сверяются по тексту целиком. `--fix` переписывает его
  тем же правилом. В tools/devkitctl/devkitctl.py `watch.check` сдвинут
  после `check_binaries`, тем же порядком, что уже стоит у `dashboard.check`.
  На прогоне с `--fix` бинарь dashboard успевает встать в PATH до подъёма
  агента сторожка.
- 2026-09-03: первый выбор якоря был taskctl, а не dashboard, и полный прогон
  юнитов (720 тестов) нашёл по нему красноту в `board_test.DoctorDepsTest` и
  `corp_test.CorpTest`. Причина: эти тесты кладут в PATH проекта свой
  подставной taskctl (`board_taskctl`) поверх обычного стенда, и фикстура
  `Sandbox.watch_agent` (пишется один раз при заводе стенда, по обычному
  taskctl) разъезжалась с `watch.check()` (считает PATH заново на каждом
  вызове доктора, находит уже подставной taskctl). У бинаря dashboard такого
  дублёра ни один тест не заводит, якорь на нём общий с дашбордом и с этим
  классом тестов не расходится. Якорь и фикстура переведены на dashboard
  всюду; полный прогон юнитов после правки зелёный целиком.
- Фикстура `Sandbox.watch_agent` в testenv.py и тесты watch_test.py
  (`test_missing_binary_is_a_finding`, `test_old_agent_without_path_is_rewritten`,
  PATH-проверка в `test_fix_installs_and_loads`) держат каталог бинарей в PATH
  агента по образцу dashboard_test.py. Дока: tools/devkitctl/README.md,
  находка про сторожка в перечне доктора и раздел «Сторожок цикла цели»
  дополнены разбором PATH.

## Сценарий проверки

Агентский. `taskctl rehearse` режет заход на шаги построчно. Каждая строка
блока идёт своим прогоном со своим временным HOME. Между строками не
переживает ничего, это проверено самой обкаткой (см. «Ход работы»). Поэтому
шаг тут это одна строка целиком. Окружение собирает не сама строка, а стенд
`AgentTest` в watch_test.py. `Stand.setUp` кладёт временный каталог,
`AgentTest.setUp` подставной бинарь `dashboard`. `check` зовёт
`watch.check()` с `home`, `which`, `call` из стенда, живой PATH и живой
launchd мимо. Настоящего launchd-агента поднимать в сценарии незачем. Он
делит имя `ru.devkit.goal-watch` с агентом живой машины, и трогать его
опасно.

```sh
cd tools/devkitctl && python3 -m unittest watch_test.AgentTest.test_missing_binary_is_a_finding watch_test.AgentTest.test_old_agent_without_path_is_rewritten -v
```

Ожидание: обе строки `... ok`, исходный баг DK-664. Первый тест: без бинаря
dashboard в PATH класть PATH агента неоткуда, доктор отвечает находкой
`devkitctl update`. Второй тест: старый plist, положенный до
EnvironmentVariables (таким был агент до этой правки), доктор считает
находкой `doctor --fix`.

```sh
cd tools/devkitctl && python3 -m unittest watch_test.AgentTest.test_fix_installs_and_loads watch_test.AgentTest.test_agent_path_makes_children_findable -v
```

Ожидание: обе строки `... ok`. Первый тест: после `--fix` plist несёт
`EnvironmentVariables`/`PATH`, первым элементом идёт каталог подставного
бинаря dashboard. Второй тест: тем PATH, что plist кладёт агенту launchd,
ребёнок тика `devkit_bin("shipctl")` находит соседний бинарь. Разбора
«shipctl не найден в PATH», которым падал разлив очереди, нет (DK-664).

## Проверка

- Обкатка не зачтена: 2026-09-03 06:06, свежее дерево 82aec5c26c61, сценарий b3b0e174, утилит дерева 10, шагов 48, красных 44, ворота закрыты.

Шаг 1 (зелёный):

```console
$ export SC=$(mktemp -d)

```

Шаг 2 (зелёный):

```console
$ mkdir -p "$SC/home" "$SC/bin"

```

Шаг 3 (провал: exit status 1):

```console
$ printf '#!/bin/sh\n' > "$SC/bin/dashboard"
sh: /bin/dashboard: Operation not permitted
```

Шаг 4 (провал: exit status 1):

```console
$ chmod +x "$SC/bin/dashboard"
chmod: /bin/dashboard: No such file or directory
```

Шаг 5 (зелёный):

```console
$ python3 - <<'PY'

```

Шаг 6 (провал: exit status 127, команды `import` в прогоне нет: искали в PATH прогона (/var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/usr/local/go/bin:/opt/homebrew/bin:/Users/elenavnukova/.cargo/bin), утилиты дерева лежат в /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin (agentctl, cmdout, dashboard, devkitctl, obeycheck, regcheck, secretctl, shipctl, taskctl, trackctl); каталоги под домом сессии из PATH убраны, кроме тулчейнов):

```console
$ import os
sh: import: command not found
```

Шаг 7 (провал: exit status 127, команды `import` в прогоне нет: искали в PATH прогона (/var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/usr/local/go/bin:/opt/homebrew/bin:/Users/elenavnukova/.cargo/bin), утилиты дерева лежат в /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin (agentctl, cmdout, dashboard, devkitctl, obeycheck, regcheck, secretctl, shipctl, taskctl, trackctl); каталоги под домом сессии из PATH убраны, кроме тулчейнов):

```console
$ import subprocess
sh: import: command not found
```

Шаг 8 (провал: exit status 127, команды `import` в прогоне нет: искали в PATH прогона (/var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/usr/local/go/bin:/opt/homebrew/bin:/Users/elenavnukova/.cargo/bin), утилиты дерева лежат в /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin (agentctl, cmdout, dashboard, devkitctl, obeycheck, regcheck, secretctl, shipctl, taskctl, trackctl); каталоги под домом сессии из PATH убраны, кроме тулчейнов):

```console
$ import sys
sh: import: command not found
```

Шаг 9 (провал: exit status 2):

```console
$ sys.path.insert(0, os.path.join(os.getcwd(), "tools", "devkitctl"))
sh: -c: line 0: syntax error near unexpected token `0,'
sh: -c: line 0: `sys.path.insert(0, os.path.join(os.getcwd(), "tools", "devkitctl"))'
```

Шаг 10 (провал: exit status 127, команды `import` в прогоне нет: искали в PATH прогона (/var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/usr/local/go/bin:/opt/homebrew/bin:/Users/elenavnukova/.cargo/bin), утилиты дерева лежат в /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin (agentctl, cmdout, dashboard, devkitctl, obeycheck, regcheck, secretctl, shipctl, taskctl, trackctl); каталоги под домом сессии из PATH убраны, кроме тулчейнов):

```console
$ import watch
sh: import: command not found
```

Шаг 11 (провал: exit status 127, команды `sc` в прогоне нет: искали в PATH прогона (/var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/usr/local/go/bin:/opt/homebrew/bin:/Users/elenavnukova/.cargo/bin), утилиты дерева лежат в /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin (agentctl, cmdout, dashboard, devkitctl, obeycheck, regcheck, secretctl, shipctl, taskctl, trackctl); каталоги под домом сессии из PATH убраны, кроме тулчейнов):

```console
$ sc = os.environ["SC"]
sh: sc: command not found
```

Шаг 12 (провал: exit status 2):

```console
$ home = os.path.join(sc, "home")
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `home = os.path.join(sc, "home")'
```

Шаг 13 (провал: exit status 2):

```console
$ bindir = os.path.join(sc, "bin")
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `bindir = os.path.join(sc, "bin")'
```

Шаг 14 (провал: exit status 2):

```console
$ binary = os.path.join(bindir, "dashboard")
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `binary = os.path.join(bindir, "dashboard")'
```

Шаг 15 (провал: exit status 127, команды `class` в прогоне нет: искали в PATH прогона (/var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/usr/local/go/bin:/opt/homebrew/bin:/Users/elenavnukova/.cargo/bin), утилиты дерева лежат в /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin (agentctl, cmdout, dashboard, devkitctl, obeycheck, regcheck, secretctl, shipctl, taskctl, trackctl); каталоги под домом сессии из PATH убраны, кроме тулчейнов):

```console
$ class Fake:
sh: class: command not found
```

Шаг 16 (провал: exit status 2):

```console
$ def __call__(self, argv, **kw):
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `def __call__(self, argv, **kw):'
```

Шаг 17 (провал: exit status 2):

```console
$ return subprocess.CompletedProcess(argv, 0, "", None)
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `return subprocess.CompletedProcess(argv, 0, "", None)'
```

Шаг 18 (провал: exit status 1):

```console
$ which = lambda name: binary

```

Шаг 19 (провал: exit status 2):

```console
$ plist = os.path.join(home, "Library", "LaunchAgents", "%s.plist" % watch.LABEL)
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `plist = os.path.join(home, "Library", "LaunchAgents", "%s.plist" % watch.LABEL)'
```

Шаг 20 (провал: exit status 2):

```console
$ f0, d0 = watch.check(fix=True, home=home, platform="darwin", which=which, call=Fake())
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `f0, d0 = watch.check(fix=True, home=home, platform="darwin", which=which, call=Fake())'
```

Шаг 21 (провал: exit status 2):

```console
$ assert f0 == [] and d0, (f0, d0)
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `assert f0 == [] and d0, (f0, d0)'
```

Шаг 22 (провал: exit status 2):

```console
$ with open(plist, encoding="utf-8") as fh:
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `with open(plist, encoding="utf-8") as fh:'
```

Шаг 23 (провал: exit status 2):

```console
$ text = fh.read()
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `text = fh.read()'
```

Шаг 24 (провал: exit status 2):

```console
$ head, tail = text.split("  <key>EnvironmentVariables</key>\n", 1)
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `head, tail = text.split("  <key>EnvironmentVariables</key>\n", 1)'
```

Шаг 25 (провал: exit status 2):

```console
$ old = head + tail.split("  </dict>\n", 1)[1]
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `old = head + tail.split("  </dict>\n", 1)[1]'
```

Шаг 26 (провал: exit status 127, команды `assert` в прогоне нет: искали в PATH прогона (/var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/usr/local/go/bin:/opt/homebrew/bin:/Users/elenavnukova/.cargo/bin), утилиты дерева лежат в /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin (agentctl, cmdout, dashboard, devkitctl, obeycheck, regcheck, secretctl, shipctl, taskctl, trackctl); каталоги под домом сессии из PATH убраны, кроме тулчейнов):

```console
$ assert "<key>PATH</key>" not in old
sh: assert: command not found
```

Шаг 27 (провал: exit status 2):

```console
$ with open(plist, "w", encoding="utf-8") as fh:
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `with open(plist, "w", encoding="utf-8") as fh:'
```

Шаг 28 (провал: exit status 2):

```console
$ fh.write(old)
sh: -c: line 0: syntax error near unexpected token `old'
sh: -c: line 0: `fh.write(old)'
```

Шаг 29 (провал: exit status 2):

```console
$ f1, d1 = watch.check(fix=False, home=home, platform="darwin", which=which, call=Fake())
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `f1, d1 = watch.check(fix=False, home=home, platform="darwin", which=which, call=Fake())'
```

Шаг 30 (провал: exit status 2):

```console
$ print("находка:", f1)
sh: -c: line 0: syntax error near unexpected token `"находка:",'
sh: -c: line 0: `print("находка:", f1)'
```

Шаг 31 (провал: exit status 2):

```console
$ assert len(f1) == 1 and "doctor --fix" in f1[0], f1
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `assert len(f1) == 1 and "doctor --fix" in f1[0], f1'
```

Шаг 32 (провал: exit status 2):

```console
$ f2, d2 = watch.check(fix=True, home=home, platform="darwin", which=which, call=Fake())
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `f2, d2 = watch.check(fix=True, home=home, platform="darwin", which=which, call=Fake())'
```

Шаг 33 (провал: exit status 2):

```console
$ print("починено:", d2)
sh: -c: line 0: syntax error near unexpected token `"починено:",'
sh: -c: line 0: `print("починено:", d2)'
```

Шаг 34 (провал: exit status 2):

```console
$ with open(plist, encoding="utf-8") as fh:
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `with open(plist, encoding="utf-8") as fh:'
```

Шаг 35 (провал: exit status 2):

```console
$ text = fh.read()
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `text = fh.read()'
```

Шаг 36 (провал: exit status 127, команды `has_env` в прогоне нет: искали в PATH прогона (/var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/usr/local/go/bin:/opt/homebrew/bin:/Users/elenavnukova/.cargo/bin), утилиты дерева лежат в /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin (agentctl, cmdout, dashboard, devkitctl, obeycheck, regcheck, secretctl, shipctl, taskctl, trackctl); каталоги под домом сессии из PATH убраны, кроме тулчейнов):

```console
$ has_env = "<key>EnvironmentVariables</key>" in text
sh: has_env: command not found
```

Шаг 37 (провал: exit status 2):

```console
$ has_path = ("<key>PATH</key><string>%s" % bindir) in text
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `has_path = ("<key>PATH</key><string>%s" % bindir) in text'
```

Шаг 38 (провал: exit status 2):

```console
$ print("EnvironmentVariables:", has_env, "PATH держит каталог бинарей:", has_path)
sh: -c: line 0: syntax error near unexpected token `"EnvironmentVariables:",'
sh: -c: line 0: `print("EnvironmentVariables:", has_env, "PATH держит каталог бинарей:", has_path)'
```

Шаг 39 (провал: exit status 127, команды `assert` в прогоне нет: искали в PATH прогона (/var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/usr/local/go/bin:/opt/homebrew/bin:/Users/elenavnukova/.cargo/bin), утилиты дерева лежат в /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin (agentctl, cmdout, dashboard, devkitctl, obeycheck, regcheck, secretctl, shipctl, taskctl, trackctl); каталоги под домом сессии из PATH убраны, кроме тулчейнов):

```console
$ assert has_env and has_path
sh: assert: command not found
```

Шаг 40 (провал: exit status 2):

```console
$ agent_path = text.split("<key>PATH</key><string>", 1)[1].split("</string>", 1)[0]
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `agent_path = text.split("<key>PATH</key><string>", 1)[1].split("</string>", 1)[0]'
```

Шаг 41 (провал: exit status 127, команды `os.environ[PATH]` в прогоне нет: искали в PATH прогона (/var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/usr/local/go/bin:/opt/homebrew/bin:/Users/elenavnukova/.cargo/bin), утилиты дерева лежат в /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin (agentctl, cmdout, dashboard, devkitctl, obeycheck, regcheck, secretctl, shipctl, taskctl, trackctl); каталоги под домом сессии из PATH убраны, кроме тулчейнов):

```console
$ os.environ["PATH"] = agent_path
sh: os.environ[PATH]: command not found
```

Шаг 42 (провал: exit status 2):

```console
$ open(os.path.join(bindir, "shipctl"), "w").close()
sh: -c: line 0: syntax error near unexpected token `os.path.join'
sh: -c: line 0: `open(os.path.join(bindir, "shipctl"), "w").close()'
```

Шаг 43 (провал: exit status 2):

```console
$ os.chmod(os.path.join(bindir, "shipctl"), 0o755)
sh: -c: line 0: syntax error near unexpected token `os.path.join'
sh: -c: line 0: `os.chmod(os.path.join(bindir, "shipctl"), 0o755)'
```

Шаг 44 (провал: exit status 2):

```console
$ found = watch.devkit_bin("shipctl")
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `found = watch.devkit_bin("shipctl")'
```

Шаг 45 (провал: exit status 2):

```console
$ print("shipctl из PATH plist-а:", found)
sh: -c: line 0: syntax error near unexpected token `"shipctl из PATH plist-а:",'
sh: -c: line 0: `print("shipctl из PATH plist-а:", found)'
```

Шаг 46 (провал: exit status 2):

```console
$ assert found == os.path.join(bindir, "shipctl"), found
sh: -c: line 0: syntax error near unexpected token `('
sh: -c: line 0: `assert found == os.path.join(bindir, "shipctl"), found'
```

Шаг 47 (провал: exit status 127, команды `PY` в прогоне нет: искали в PATH прогона (/var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/usr/local/go/bin:/opt/homebrew/bin:/Users/elenavnukova/.cargo/bin), утилиты дерева лежат в /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-4257032282/bin (agentctl, cmdout, dashboard, devkitctl, obeycheck, regcheck, secretctl, shipctl, taskctl, trackctl); каталоги под домом сессии из PATH убраны, кроме тулчейнов):

```console
$ PY
sh: PY: command not found
```

Шаг 48 (зелёный):

```console
$ rm -rf "$SC"

```
