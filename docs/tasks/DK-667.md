# DK-667: Ворот тестов shipctl не знает раскладку Cargo и Gradle: интеграционный тест в tests/ и юнит в src/test невидимы, и слияние Rust-задачи с честными тестами отбивается с предложением пометить правку как бестестовую

## Что происходит

## Чего хотим

## DoD

## Ранг

## Ход работы

Вторая сессия (проверка прогона коммита):

1. Полный прогон локальных тестов: `python3 tools/devkitctl/parallel.py`
   - Все 17 компонентов прошли успешно (OK, exit code 0)
   - shipctl: 257.1s (включает advice_test.go с исправлением)
   - taskctl: 83.6s
   - trackctl: 10.9s
   - devkitctl Python-часть: 184.5s
   - hooks: 184.2s
   - goal-loop: 102.5s
   - skills, prose, doctor, check-skills: все ok
   - Общее время: 4 минуты 17 секунд

Прошлая сессия:
- Правка в tools/shipctl/advice.go: расширена функция isTestFile, добавлена inTestDir
- Ворот тестов shipctl теперь распознаёт:
  - Rust интеграционные тесты в tests/ по расширению .rs и каталогу
  - JVM юнит-тесты в src/test/ по расширениям .kt/.java и пути src/test
- Регрессионный тест добавлен в tools/shipctl/readiness_test.go
- Все Go-утилиты прогнаны и зелёные

## Ревью

- tools/shipctl/advice.go:371-388 inTestDir: .rs-файл ловится по любому предку-каталогу "tests" без разбора вложенности. Вспомогательные модули Cargo без собственных тестов ловятся тоже: tests/common/mod.rs, tests/fixtures/data.rs, tests/helpers/mock_server.rs. Пробный тест на все три дал true. Это тот самый случай заглушки стенда, который комментарий к функции обещает не считать тестом. Исключение в коде реализовано только для расширений .sh и .json. Ветка, тронувшая лишь общий тестовый хелпер без единого настоящего теста, пройдёт ворот
- tools/shipctl/README.md:445-450 не обновлён под правку. Текст всё ещё говорит, что предикат теста "опирается на имя файла, а не на расширение", и перечисляет только старые суффиксы (_test.go, _test.py, _test.sh, test_*.py, test_*.sh). Advice.go этой же веткой добавил распознавание по каталогу и расширению: .rs в tests/, .kt и .java в src/test/. Дока теперь противоречит коду, который она описывает
- docs/tasks/DK-667.md не по форме TASKFORM.md. Разделы автора "Что происходит", "Чего хотим", "DoD", "Ранг" заведены пустыми. Нет разбора корня, нет DoD, нет расшифровки ранга, хотя строка доски уже несёт формулу 68 (50+7+0+5+4, S+2). Разделов "Приёмка", "Сценарий проверки", "Ревью", "Выкат", "Проверка" в файле не было вовсе. Без "Приёмки" не определить вид приёмки и не проверить перебор обходов. Без "Сценария проверки" не проверить, воспроизводит ли первый шаг исходный баг
