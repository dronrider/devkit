# DK-689: README taskctl не говорит, что обкатка режет блок сценария построчно

## Что происходит

Раздел про `rehearse` в README taskctl читается так, будто в блок `sh` кладут
скрипт: там сказано «шаги берутся из блоков раздела», а про то, что блок
режется по строкам, не сказано нигде. На деле `scenarioSteps` в
`tools/taskctl/rehearse.go` берёт шагом каждую непустую строку блока и гоняет
её своим прогоном в свежем дереве. Скрипт из десяти строк превращается в
десять шагов, переменные между ними не живут, и обкатка краснеет на пустом
месте.

Найдено на DK-471: сценарий из двух многострочных блоков дал 22 шага и 11
красных, а понять причину получилось только чтением `rehearse.go`.

## Чего хотим

Читатель README знает про построчную нарезку до первого прогона, а не после
красной обкатки.

## DoD

Абзац про `rehearse` в `tools/taskctl/README.md` говорит, что шагом становится
каждая непустая строка блока, а многострочный скрипт надо сводить в одну
строку. Рядом стоит рабочий пример строки со сборкой бинаря и песочной доской.
Отказ команды при красном прогоне называет число шагов, и по числу видно
нарезку, это стоит упомянуть.

## Ранг

`25+3+0+5+1 = 34`, P2.

- Серьёзность 25: заметное трение, обход есть (прочитать код), но нашли его
  тремя красными прогонами.
- Ценность 3: ждёт всякий, кто первый раз пишет сценарий с многошаговой
  командой.
- Неопределённость 0: правится дока, предмет назван.
- Поправка на баг 5: дока расходится с поведением.
- Рычаг 1: чужих задач не держит.

## Ход работы

- Заведена по ходу DK-471, 2026-09-01.
- Разработка: субагент haiku/low по вердикту pick (квота: week_all 30%, снимок 30м назад, сдвига нет), 2026-09-19 21:32-21:32.
- Исключение: стенд (в TASKFORM.md одна справочная фраза про нарезку блока, процедура агента не меняется; из скилла board-task правка снята, тело скилла упиралось в порог веса).
- Сторож прозы: docs/tasks/DK-689.md, средняя длина предложения 30,7 при пороге 23, предложений длиннее 25 слов 66,7 при пороге 30, довод в той же фразе 83,3 при пороге 48, двоеточие в середине фразы 21,7 при пороге 19, хвост «..., а не Y» 10,9 при пороге 5, оставлено: замечания ревью держат в одной фразе место, симптом и правку, разбор надвое множит фразы без потери длины и путает, к какому месту относится совет.
- Сторож прозы: docs/tasks/DK-689.md, средняя длина предложения 30,5 при пороге 23, предложений длиннее 25 слов 100,0 при пороге 30, довод в той же фразе 100,0 при пороге 48, хвост «..., а не Y» 24,6 при пороге 5, оставлено: то же замечание ревью держит место, живой пример ошибки и правку в одной фразе, короткая разбивка не сокращает предложения, а множит их.

## Из черновика DK-1051 «rehearse гоняет блок sh построчно, а TASKFORM об этом молчит» (записан 2026-09-18)

### Черновик

#### Ситуация

2026-09-18, DK-986 и DK-631. `taskctl rehearse` исполняет каждую строку блока `sh` раздела «Сценарий проверки» отдельным `sh -c`. Продолжение строки обратным слешем и переменная, заведённая строкой выше, ломают шаг. Оба исполнителя написали многострочные шаги, получили красную обкатку и переписали шаги в одну строку через `&&`.

#### Осложнение

Каждый новый исполнитель тратит на это лишний заход обкатки. В TASKFORM.md про одну команду на строку не сказано, отказ обкатки причину тоже не называет.

#### Вопрос

#### Гипотеза

TASKFORM.md называет правило одной команды на строку, а красная обкатка на строке с обратным слешем подсказывает причину.

## Ревью

Уровень 2 до ec783aa: неопределённость 0, тронут tools/taskctl (критичный путь), автор младшего яруса

- [возврат: постановка, 2026-09-19] блокирует: пример в tools/taskctl/README.md (`go build ./cmd/foo && ./foo run-test`) не рабочий: каталога `cmd/foo` в репозитории нет, и песочной доски пример не касается вовсе, а DoD просит рядом с абзацем рабочий пример строки со сборкой бинаря и песочной доской. Заменить на исполняемую строку по конвенции задач (сборка бинаря во временный путь и прогон на песочной доске, как в архивных сценариях DK-003, DK-011).: исправлено, пример заменён на рабочий: сборка taskctl и проверка помощи
- [возврат: постановка, 2026-09-19] блокирует: третье предложение DoD ("отказ называет число шагов, и по числу видна нарезка, это стоит упомянуть") не отражено нигде в правке: ни README.md, ни TASKFORM.md, ни SKILL.md не говорят, что "шагов упало N из M" в отказе обкатки сам по себе выдаёт построчную нарезку. Добавить фразу рядом с абзацем про построчность.: исправлено, добавлена фраза про число упавших шагов в отказе обкатки
- [возврат: реализация, 2026-09-19] блокирует: подсказка в rehearse.go ловит только `HasSuffix(cmd, "\\")`, `HasPrefix(cmd, "|")`, `HasPrefix(cmd, "&&")`, и не срабатывает на зеркальном варианте продолжения строки: строке, заканчивающейся на `|` или `&&` без слеша. Проверено живым прогоном rehearse на синтетической доске: блок `echo начало |`/`cat` и блок `echo начало &&`/`echo конец` оба красные ("syntax error: unexpected end of file") и без подсказки. Добавить проверку хвоста `|` и `&&` (после TrimSpace) к условию.: исправлено, добавлены проверки хвоста | и && после TrimSpace
- [возврат: правила, 2026-09-19] блокирует: текст подсказки "...многострочную команду соедините в одну через &&" обращается на "вы", RULES.core.md это запрещает. Вставка "; по подсказке: ..." вдобавок читается как отсылка к чужой подсказке, а не как сама подсказка: в проекте принята прямая форма "подсказка: ..." (tools/taskctl/gate.go:329). Переписать безлично, например "; подсказка: строка блока это отдельный шаг, многострочную команду нужно свести в одну через &&".: исправлено, подсказка переписана безлично, используется форма подсказка:
- [возврат: реализация, 2026-09-19] не блокирует: подсказка не ловит и третий реальный случай. Переменная, заведённая строкой выше (шаг вида `VAR=...` без команды), тоже ломает следующий шаг, только тихой пустой подстановкой, а не явным синтаксисом, и подсказка её не видит. Именно этот случай вчера сдвинул живой тег deployed (черновик DK-1061). Дёшево добавляется проверкой, что предыдущий шаг целиком совпадает с `^[A-Za-z_][A-Za-z0-9_]*=.*$`. Решить явно: добавить тут же или оформить отдельной задачей.: исправлено, добавлена проверка присваивания переменной: VAR=value и VAR=()
- [возврат: правила, 2026-09-19] не блокирует: комментарий "// Проверяем, есть ли в упавших шагах признаки попытки многострочности" в rehearse.go пересказывает следующую строку кода (что), а не поясняет неочевидное почему, против правила "комментариев по умолчанию не писать". Соседние файлы пакета (accept.go) комментируют мотивацию, а не механику.: исправлено, комментарий переписан, объясняет мотивацию: почему нужна подсказка
- [возврат: реализация, 2026-09-19] блокирует: isVariableAssignment в rehearse.go (второй круг, коммит 8adddd5c) ловит ложно легальную строку вида `VAR=1 команда аргумент`: проверка требует только отсутствия пробела до знака равенства, а не того, что после значения ничего больше нет. Плюс сам цикл по |/&&/||/присваиванию в hasMultilineHints смотрит на все шаги подряд, а не только на упавший, поэтому подсказка цепляется за любую подходящую строку сценария независимо от того, где реально красно. Проверено живым прогоном: сценарий из двух шагов `VAR=1 echo ok` (зелёный) и `false` (красный по своей причине) выдаёт подсказку про построчную нарезку, хотя обкатка тут ни при чём. Нужно и сузить isVariableAssignment до строк вида ^[A-Za-z_][A-Za-z0-9_]*=\S*$ целиком, без хвоста-команды, и привязать проверку служебных символов/присваивания к упавшему шагу (или его соседям), а не ко всему списку.: исправлено, признак присваивания требует, чтобы после значения ничего не стояло: значение с пробелом идёт за присваивание только в кавычках или в подстановке команды, случай VAR=1 команда аргумент покрыт тестом
- Вердикт: без замечаний до ba47f65. третий круг: замечание 7 закрыто коммитом ba47f65e, живой прогон подтвердил, что VAR=1 echo ok с соседним красным шагом подсказки больше не даёт, а D=$(mktemp -d), VAR="раз два" и прежние случаи с | и && по-прежнему дают; ложных срабатываний на export PATH=/x:$PATH команда, VAR=1 VAR2=2 команда, сравнении == и test "$HOME" = "" нет; точечные тесты и regcheck с базой 43901d4b зелёные

## Сценарий проверки

Вид приёмки agent.

```sh
regcheck -C tools/taskctl --base 43901d4ba552e03f6b095b839a35134c535c7b7a --tests tools/taskctl/rehearse_test.go -- go test -run 'Rehearse' ./...
grep -q "отдельный шаг" tools/taskctl/README.md
go -C tools/taskctl test -run 'TestRehearseHintsOnMultilineScripts' -count=1 ./...
```

## Проверка


- Обкатка: 2026-09-19 23:01, свежее дерево 267634bbdd7e, сценарий e0b4ee2c, временный HOME, утилит дерева 11, шагов 3, все зелёные.

Шаг 1 (зелёный):

```console
$ regcheck -C tools/taskctl --base 43901d4ba552e03f6b095b839a35134c535c7b7a --tests tools/taskctl/rehearse_test.go -- go test -run 'Rehearse' ./...
тест краснеет на 43901d4ba552e03f6b095b839a35134c535c7b7a и проходит на текущем коде, регрессия закрыта (тесты: tools/taskctl/rehearse_test.go)
```

Шаг 2 (зелёный):

```console
$ grep -q "отдельный шаг" tools/taskctl/README.md

```

Шаг 3 (зелёный):

```console
$ go -C tools/taskctl test -run 'TestRehearseHintsOnMultilineScripts' -count=1 ./...
ok  	github.com/dronrider/devkit/tools/taskctl	7.684s
```
