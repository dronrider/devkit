# DK-695: Вопрос ждущей сессии доезжает до панели текстом и вариантами

## Что происходит

01.09 с 13:54 до 14:21 клиенты chat-13 и chat-14 ждали ответа человека. Снимок панели на каждый запрос писал в dashboard.log «не разобрать: человеку показывать нечего». Вопрос агента пришёл структурным блоком, но виджет из него не собрался, и человек ответил вслепую строкой «пункт 1».

Канал вопроса это единственный штатный выход из парковки. Пока виджет не собирается, человек не видит текста вопроса и вариантов, отвечает наугад или не отвечает вовсе. Задача всё это время стоит припаркованной.

### Что нашёл разбор

Симптом живой и не разовый. В `~/.devkit/dashboard.log` таких строк восемь (число уточнено разбором 03.09, черновик называл девять), 31.08 по клиентам chat-DK-161-1 и chat-12, 01.09 по chat-13 и chat-14, последняя в 14:21. Пишет их `askQuietLog` (`tools/dashboard/chats.go:2489`), и зовётся она только тогда, когда провалились обе дороги ручки `handleChatAsk`. Первая это вопрос агента из признака ожидания, который кладёт `taskctl ask`, и там `agentAsk` ищет признак по сессии разговора или по её родителю (`tools/dashboard/waiting.go:114`). Вторая это разбор снимка панели клиента через `tmuxAskOf`, и молчит она, когда в снимке не нашлось ни одного варианта.

### Развилки

Чья из двух дорог отказала. Признака могло не быть вовсе (вопрос задан панелью клиента, а не инструментом ожидания), он мог лечь с чужой сессией и не сойтись ни с sid разговора, ни с родителем в реестре, либо разбор снимка не узнал блок вариантов. Дороги две, а строка в журнале одна на обе, и по ней случай не разложить.

Чем мерить «панель недавно ответила». Взято совпадение отметки события в
журнале уведомителя. Строка, на которой стоит липкость `chatStuck`, снимается в
момент ответа панели и сверяется при следующем опросе. Пока она та же, журнал
про сессию молчит, и клиент идёт тем же ходом.

Отвергнуты четыре мерила.

- Окно `askQuietWindow`. Оно заведено под подавление повторной строки, а
  липкость на живом случае 01.09 продержалась 5м50с. Первая версия правки на
  этом и споткнулась (замечание ревью).
- Свой порог, заведомо больший пяти минут. Число опять берётся с потолка, а у
  липкости срока нет вовсе.
- Признак живого виджета в снимке. В эту минуту снимок как раз пуст, и
  спрашивать его не о чем.
- Конец хода по транскрипту. Запись транскрипта двигают и вставшие в очередь
  реплики, и `chatStuck` этому уже не верит.

Чем опознавать саму запись журнала. Взят счёт повторов строки: отметка это
число вхождений плюс сама строка. Время `hooks/notify.py` пишет до секунды,
своего номера у записи нет, и два вопроса разрешения в одну секунду дают две
дословно одинаковых строки. Приём взят у `fresh` в
`kit/skills/board-task/task-run.py`, где секундная точность харнеса ломает
память по строке так же (DK-740).

Отвергнуты три способа.

- Голый текст строки. Близнец в ту же секунду не отличается от отмеченной
  записи, и новый вопрос прячется за старым ответом (третье замечание ревью).
- Смещение в файле. Журнал уведомителя режется по размеру, и после реза
  смещение указывает в середину чужой строки.
- Порядковый номер строки. Рез сдвигает нумерацию молча и на любое число.

Заводить у записи свой номер значит править `hooks/notify.py` и формат журнала,
который читают ещё несколько мест. Ради одной строки дашборда это дорого, и
такая правка была бы отдельной задачей.

### Что дал разбор 03.09

Отказала не дорога, а сама строка. Строк по четырём названным клиентам восемь,
и шесть из них стоят в пределах секунды после строки «ответ на вопрос клиента
chat-13 в devkit: пункт 1 (Починить сейчас (Recommended))». Две оставшиеся стоят
особняком, через 5м41с и 5м50с после такой же строки ответа. Текст варианта
берётся из разобранного виджета (`ask.Options[body.Option-1].Text`,
`tools/dashboard/chats.go`). Значит, в момент ответа виджет был разобран и
человек нажал кнопку панели. Строка «пункт 1» в журнале это выбранный вариант.

Секундой позже панель переспрашивает вопрос, отвеченного виджета в снимке уже
нет, а `chatStuck` держит своё «клиент стоит на вопросе» до конца хода. Мера у
него это последнее событие сессии в журнале уведомителя, и после разрешения оно
не меняется. Отсюда и жалоба на работающий разбор. Две строки особняком это та
же тревога, придержанная окном подавления `askQuietWindow`: 14:00:22 по chat-14
идёт через 5м41с после ответа в 13:54:41, 14:21:34 по chat-13 через 5м50с после
ответа в 14:15:44.

Симптом жив и после 01.09. За 03.09 таких строк девятнадцать. У chat-33 с
19:21:17 до 19:30:48 сто сорок семь раз подряд сработал рубеж эха. Строка про
молчащий виджет в это же время сообщала, что разбор нужно чинить. Разбор при
этом отработал верно. Агент напечатал в своё окно снимок панели соседнего чата
(`tmux capture-pane -t "=chat-34:"`, транскрипт d82b5433), и разбор поднял чужой
виджет как свой, а рубеж эха его отбил.

Осталась и настоящая дыра на дороге признака. `handedAsks` пропускал признак с
пустой сессией первой же проверкой, а `taskctl ask` кладёт такой признак всякий
раз, когда сессия не назвалась ни ключом, ни окружением, ни реестром. Вопрос
такого захода до панели не доезжал ни текстом, ни вариантами, хотя ответ он
принял бы. Безадресную строку входа он и ждёт.

## Чего хотим

Вопрос ждущей сессии виден на панели текстом и вариантами всегда, сколько бы клиентов ни смотрело.

## DoD

- Вопрос ждущей сессии приходит на панель текстом и вариантами. Случай 01.09 по chat-13 и chat-14 разобран до причины, причина названа в файле задачи.
- Строка журнала называет, какая из двух дорог отказала: признака ожидания не нашлось или разбор снимка не собрал вариантов. По журналу случай раскладывается без второго захода.
- Регрессионный тест падает на старом коде и проходит на исправленном, краснота проверена `regcheck`. Вход тот же, на котором ручка сейчас отвечает «клиент ни о чём не спрашивает».
- README дашборда говорит, откуда панель берёт вопрос и что делать, когда виджет не собрался.

## Ранг

`50+8+3+5+3 = 69`, P1.

- Серьёзность 50. Основной сценарий сломан, обход болезненный, человек отвечает вслепую строкой «пункт 1» или идёт руками в tmux.
- Ценность 8. Вопрос и ответ это единственный штатный выход из парковки, и без него конвейер стоит.
- Неопределённость 3. Общий подход понятен, а какая из двух дорог отказала, разбором не выяснено.
- Поправка на баг 5. Виджет собирался раньше, снимок панели теперь его не узнаёт.
- Рычаг 3. Каждая следующая парковка выходит из простоя сама, без ручного захода в терминал.

## Приёмка

- вид: mixed
- барьер «глаза»: как виджет вопроса встал на панели живого чата, видно только
  на экране; обе дороги ручки и строка журнала закрываются тестами
  - утверждённый макет: не годится, макета блока вопроса нет, да и предмет тут
    не вёрстка, а сам факт того, что вопрос доехал
  - headless-браузер с замером: не годится, живая панель собирается из tmux и
    признака ожидания, и поднять эту связку в headless нечем
  - тест по разметке и стилям: годится наполовину. Стенд
    `testdata/poc_agentask.mjs` собирает блок вопроса из настоящего `app.js` и
    меряет текст, варианты кнопками и дорогу ответа. Связки
    «taskctl ask -> ручка -> браузер» он не держит, и она остаётся человеку

## Ход работы

- Из черновика DK-695 «Вопрос агента не доезжает до панели, снимок пишет «показывать нечего»»: записан 2026-09-01, оформлен 2026-09-01.
- Разбор случая 01.09 шёл по `~/.devkit/dashboard.log`, `~/.devkit/notify.log`, реестру чатов и транскриптам сессий chat-33 и chat-35, 2026-09-03.
- Краснота: `regcheck -C tools/dashboard --base main --tests tools/dashboard/askroad_test.go`, вердикт «тест краснеет на main и проходит на текущем коде», 2026-09-03.
- Вычитка: 2 файла, 13 правок, 0 пометок, 2026-09-03.
- Отработка ревью: блокирующее замечание про два срока у `askQuietWindow` закрыто отметкой события журнала, числа строк перепроверены грепом по `~/.devkit/dashboard.log`, 2026-09-03.
- Краснота правки по ревью: `regcheck -C tools/dashboard --base af75ca2d2 --tests tools/dashboard/askroad_test.go`, вердикт «тест краснеет на af75ca2d2 и проходит на текущем коде», 2026-09-03.
- Отработка третьего замечания: запись журнала опознаётся счётом повторов, приём взят у `fresh` в `kit/skills/board-task/task-run.py` (DK-740), 2026-09-04.
- Краснота правки по третьему замечанию: `regcheck -C tools/dashboard --base 2c489dfef --tests tools/dashboard/askroad_test.go`, вердикт «тест краснеет на 2c489dfef и проходит на текущем коде», 2026-09-04.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 37%, week_max 18% (панель разбивку не дала, цифра из кеша клиента возрастом 5ч 22м назад), снимок 3м назад, сдвига нет), 2026-09-03 22:40-23:15.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 37%, week_max 18% (панель разбивку не дала, цифра из кеша клиента возрастом 5ч 22м назад), снимок 38м назад, сдвига нет), 2026-09-03 23:15-09:45.

## Ревью

Уровень 3 до af75ca2: механизм ask/parking критичен для DoD (единственный штатный выход из парковки), дифф трогает обе дороги вопроса; разбор исполнителя неожиданный, диспетчер просил проверить строже обычного

- блокирует: askQuietWindow (5 минут) в tools/dashboard/chats.go служит двум ролям. Он гасит повторную строку журнала askQuietLog. Он же отделяет свежий ответ панели от настоящей поломки разбора в askPaneWhy. Живой случай 01.09 показывает, что chatStuck остаётся липким дольше пяти минут. Девятая строка chat-13 стоит через 5м49с после ответа, сам файл задачи называет её на самой границе окна. Прогнал проверку на дереве, временный тест не закоммичен. Ответ панели лёг в 13:54:40, следующий опрос пришёл через 5м49с с тем же снимком «Работаю дальше». Дорога снимка панели ответила «вариантов не собралось, разбор надо чинить». Работающая дорога снова названа отказавшей. Ровно это DK-695 должна была прекратить. askDone стоит развязать от askQuietWindow. Гасить его не по времени, а по следующему событию notify.log, тому же, что снимает chatStuck. Либо завести для «недавно отвечен» свой порог, заведомо больше пяти минут: исправлено, свежесть ответа панели меряется отметкой события журнала уведомителя, а не окном askQuietWindow: отметка снимается в момент ответа и сверяется при опросе; тесты TestAskQuietAfterPanelAnswerOutlivesWindow (5м50с, дольше окна) и TestAskQuietBlamesParseAfterNewPrompt, краснота regcheck --base af75ca2d2; отвергнутые мерила названы в Развилках
- не блокирует: README и вступление файла задачи говорят категоричнее, чем сам разбор 03.09. README пишет «Каждая стояла в журнале следом за строкой ответа». Вступление называет число девять. Разбор 03.09 честнее, он называет девятую строку исключением, 5м49с, не следом. Проверил grep по ~/.devkit/dashboard.log за chat-DK-161-1, chat-12, chat-13, chat-14 на 31.08-01.09. Нашёл 8 строк «похоже ждёт ответа», не 9. Из них 6 стоят 0-1с после ответа, 2 стоят особняком, 5м41с и 5м49с. Диагноз число не меняет, поэтому не блокирует. README стоит поправить на менее категоричную формулировку: исправлено, числа перепроверены грепом: восемь строк по четырём названным клиентам, шесть в пределах секунды, две через 5м41с и 5м50с; поправлены вводный раздел, разбор 03.09 и README, формулировка доки смягчена до проверенного
- блокирует: chatStuckMark опознаёт событие по сырому тексту строки notify.log. У hooks/notify.py секундная точность времени и нет id события. Две разные записи permission_prompt одной сессии могут выйти байт-в-байт одинаковыми. Так бывает при повторе той же команды в ту же секунду, и при возврате к тому же поводу после промежуточного другого. Проверил прогоном на дереве, временный тест не закоммичен. После ответа панели в notify.log дописана вторая строка permissionNotify(sid), та же секунда, тот же текст, но это новое, ещё не отвеченное событие. askPaneWhy всё равно говорит «клиент в ходу после ответа». Новый настоящий вопрос молча спрятан за старым ответом. Это ровно случай «строка та же по тексту, но событие новое», названный в постановке этого круга. Развилки его не разбирают. Событие стоит метить порядковым номером строки или смещением в файле: исправлено, запись опознаётся счётом повторов строки плюс самой строкой (notifyMark), приём взят у fresh в kit/skills/board-task/task-run.py (DK-740); смещение и номер строки отвергнуты из-за реза журнала по размеру, отвергнутое названо в Развилках; тесты TestAskQuietBlamesParseAfterTwinPrompt, TestNotifyMarkCountsTwins и TestAskQuietSilentWithoutNotifyLog, краснота regcheck --base 2c489dfef
- не блокирует: «усушка всегда идёт в безопасную сторону» верно не всегда. Есть составной случай. Усушка убирает ровно одно старое вхождение той же строки. Одновременно добавляется новое вхождение. Счёт может вернуться к прежнему значению. Проверил прогоном на дереве, временный тест не закоммичен. Окно 200 строк (tailDefault). При ответе в окне 2 вхождения T, на позициях 10 и 150 из 150 строк. Дальше 100 строк-наполнителей и новое T на позиции 251. Итоговое окно [52..251] теряет позицию 10, но держит 150 и 251. Счёт снова 2, отметка совпала, новый вопрос спрятан. Комбинация редкая, нужно точное совпадение числа выпавших и добавленных повторов одной строки. TestNotifyMarkCountsTwins её не проверяет, он показывает только безопасный случай: усушка убирает постороннюю строку. Эта находка и предыдущая (что решаем сейчас) родом из одного корня. Отметка о нём уже уходит отдельным черновиком. Точечную правку этого хвоста сейчас не прошу: отклонено, неблокирующее наблюдение ревьювера по прямому вопросу диспетчера. Третьего круга не открываю. Корневая причина, секундная точность журналов и отсутствие id события, уже уходит отдельным черновиком. Точечная правка этого хвоста сейчас не запрошена
- Вердикт: без замечаний. круг 3 (сверка решения человека) до db9f7e853: замечание 3 сверено с диффом, chatStuckMark опознаёт запись счётом повторов (notifyMark), byte-идентичный близнец больше не прячет новый вопрос. Прогнал сам TestAskQuietBlamesParseAfterTwinPrompt и TestNotifyMarkCountsTwins, зелёные, и свой ручной прогон близнеца в notify.log подтвердил явление. Прежние TestAskQuietAfterPanelAnswerOutlivesWindow и TestAskQuietBlamesParseAfterNewPrompt не сломаны. regcheck --base 2c489dfef подтверждён независимо. Найдено неблокирующее замечание 4 (усушка не всегда безопасна при составном совпадении счёта) и закрыто без правки: корень уходит отдельным черновиком диспетчера

## Сценарий проверки

Первые два шага гоняются командами (агентская часть смешанной приёмки), третий
и четвёртый идут на живом дашборде после выката (пользовательская часть, барьер
«глаза»).

1. Обе дороги вопроса и строка журнала: признак с чужой сессией, просроченный
   признак, отбитый рубежом эха вопрос, свежий ответ панели (в том числе через
   5м50с, дольше окна подавления), новый вопрос клиента после ответа, близнец
   записи журнала в ту же секунду, пропавший журнал уведомителя и безадресный
   признак своей задачи.

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/dashboard" test . -count=1 -run "TestAskQuiet|TestChatAskSign|TestNotifyMark" -v
   ```

   Ожидание: строка `PASS` и в выводе двенадцать подтестов, среди них
   `TestAskQuietNamesSignRoad`, `TestAskQuietNamesEchoRoad`,
   `TestAskQuietAfterPanelAnswerOutlivesWindow`,
   `TestAskQuietBlamesParseAfterTwinPrompt`, `TestNotifyMarkCountsTwins` и
   `TestChatAskSignWithoutSessionReachesTaskChat`.

2. Панельная часть: блок вопроса агента собирается из настоящего `app.js` с
   текстом, вариантами кнопками и ответом репликой разговора.

   ```sh
   node "$(git rev-parse --show-toplevel)/tools/dashboard/testdata/poc_agentask.mjs" "$(git rev-parse --show-toplevel)/tools/dashboard/static/app.js"
   ```

   Ожидание: строка «poc_agentask: блок с вопросом без живой tmux, варианты
   кнопками, пачка шагами и одной репликой, ответ во вход задачи» и код
   возврата 0.

3. - Исключение: обкатка (живая связка «taskctl ask -> ручка -> браузер»
   поднимается только на настоящем дашборде, барьер «глаза» не снят). Открыть
   панель чата, который ведёт задачу, и попросить агента спросить человека
   командой `taskctl ask <ID> --question "..." --wait 300`. Над полем ввода
   встаёт блок с текстом вопроса и кнопкой на каждый вариант. Нажать вариант.
   Ответ остаётся виден и после закрытия окна: в `~/.devkit/dashboard.log`
   ложится строка «ответ человека сессии <sid> ушёл во вход разговора
   task-<ID>», и заход продолжается с ответом на руках.

4. - Исключение: обкатка (нужен настоящий случай, когда клиент стоит на вопросе
   в своём окне, а панель вопроса не показывает; момент неизвестен). Дождаться
   такого случая и прочитать хвост журнала дашборда.

   ```text
   grep "похоже ждёт ответа" ~/.devkit/dashboard.log | tail -3
   ```

   Ожидание: в строке названы обе дороги порознь. Дорога признака ожидания
   называет одно из трёх («вопросов за разговором нет», «признак лежит, но не за
   этим разговором», «живого признака нет, лежит просроченный»), дорога снимка
   панели тоже («вариантов не собралось, разбор надо чинить», «вопрос собрался,
   но повторяет ленту разговора», «вариантов нет, а панель ответила виджету N
   назад»). Случай раскладывается по этой строке, второго захода в транскрипты
   и реестр не нужно.

## Выкат

- 2026-09-04 слито: 10f1af33f, a1e5528ea, 29fb6e6d6, 0f83fb4a9, db119028a, 0bb2fc971, e32965294, 352671e67, f1354a283, eb450934f, ba45da475, 3596c3cf2, 3ab129d23, 0961af761, 8f99edc4b, 4dae5d8b4

## Проверка


- Обкатка: 2026-09-04 06:06, свежее дерево 4e2964af1425, сценарий 682be864, временный HOME, утилит дерева 10, шагов 2, все зелёные.

Шаг 1 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/dashboard" test . -count=1 -run "TestAskQuiet|TestChatAskSign|TestNotifyMark" -v
=== RUN   TestAskQuietNamesSignRoad
--- PASS: TestAskQuietNamesSignRoad (0.05s)
=== RUN   TestAskQuietNamesStaleSign
--- PASS: TestAskQuietNamesStaleSign (0.04s)
=== RUN   TestAskQuietNamesEchoRoad
--- PASS: TestAskQuietNamesEchoRoad (0.04s)
=== RUN   TestAskQuietAfterPanelAnswer
--- PASS: TestAskQuietAfterPanelAnswer (0.07s)
=== RUN   TestChatAskSignWithoutSessionReachesTaskChat
--- PASS: TestChatAskSignWithoutSessionReachesTaskChat (0.04s)
=== RUN   TestChatAskSignWithoutSessionStaysInItsTask
--- PASS: TestChatAskSignWithoutSessionStaysInItsTask (0.04s)
=== RUN   TestAskQuietAfterPanelAnswerOutlivesWindow
--- PASS: TestAskQuietAfterPanelAnswerOutlivesWindow (0.08s)
=== RUN   TestAskQuietBlamesParseAfterNewPrompt
--- PASS: TestAskQuietBlamesParseAfterNewPrompt (0.09s)
=== RUN   TestAskQuietBlamesParseAfterTwinPrompt
--- PASS: TestAskQuietBlamesParseAfterTwinPrompt (0.10s)
=== RUN   TestNotifyMarkCountsTwins
--- PASS: TestNotifyMarkCountsTwins (0.00s)
=== RUN   TestAskQuietSilentWithoutNotifyLog
--- PASS: TestAskQuietSilentWithoutNotifyLog (0.04s)
=== RUN   TestAskQuietGoesToLog
--- PASS: TestAskQuietGoesToLog (0.08s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	1.451s
```

Шаг 2 (зелёный):

```console
$ node "$(git rev-parse --show-toplevel)/tools/dashboard/testdata/poc_agentask.mjs" "$(git rev-parse --show-toplevel)/tools/dashboard/static/app.js"
poc_agentask: блок с вопросом без живой tmux, варианты кнопками, пачка шагами и одной репликой, ответ во вход задачи
```
