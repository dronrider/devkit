# DK-715: Вопрос агента через AskUserQuestion живёт в панели до ответа

## Что происходит

Груминг DK-706 2026-09-02 дважды звал `taskctl ask DK-706 --wait 480` с пачкой
из трёх вопросов с вариантами. Панель рисует виджет вопроса по признаку
ожидания, файлу `task-<ID>.ask` со сроком. Команда `ask` кладёт файл на входе
и снимает на любом выходе, включая выход по сроку. Чат открыли в 09:59, а
второе ожидание кончилось в 09:15. От вопросов остались реплика агента прозой и
строка причины в разделе «Грумминг» черновика. Уведомление ушло с одной первой
фразой и ссылкой на VS Code, к чату оно не ведёт.

Виджет привязан к живому заходу, и восемь минут это окно, в которое человек
должен успеть заглянуть в чат. Не успел, и вариантов больше нигде нет. У строки
доски остаётся причина парковки в 160 символов, у черновика ничего. Файл исхода
снесён как мусор без читателя. Ответить остаётся текстом по памяти о том, что
спрашивали. Черновик ждёт повторного груминга и повторного вопроса.

Случай 2026-09-04 по цели DK-713 показал вторую половину беды.
Исполнитель DK-716 спросил через `taskctl ask`, восемь минут виджет стоял в
панели, потом строка ушла в blocked с обрезанной причиной. Диспетчер цели,
получив отчёт, повторил тот же вопрос диалогом AskUserQuestion в своём
tmux-окне chat-DK-713-1. Панель терминальный диалог не показывает, реплика
человека «Работа остановилась?» встала в очередь за диалогом, и цель простояла
четыре часа. Ночью до этого то же самое было с DK-695.

## Чего хотим

Ожидание внутри команды было единственным поводом для `taskctl ask`.
Headless-проход умирал с концом хода, и команде приходилось сидеть в Bash и
опрашивать вход. DK-724 сделала сессии живыми, и вместе с ожиданием ушла
причина существования команды. Текст в чат агент кладёт и сам, а от вопроса
нужны три вещи, которых проза не даёт: структура для панели (кнопки по
вариантам, рекомендация, множественный выбор), живучесть до ответа, а не до
конца хода или снимка экрана tmux, и адрес ответа, чтобы панель знала, что
реплика это ответ и куда его везти. Транскрипт носителем не годится. Висящий
AskUserQuestion диспетчера в его транскрипт не записан вовсе.

Агент везде спрашивает одним штатным инструментом, AskUserQuestion, как в
обычном терминале. В сессии, поднятой панелью (tmux chat-*, goal-*, task-*),
хук PreToolUse перехватывает вызов. Он перекладывает вопросы в файл признака
`.devkit/chat/<разговор>.ask` в том же виде, что читает панель, без срока.
Дальше хук паркует строку задачи причиной «вопрос: ...», зовёт уведомление и
отбивает диалог с подсказкой закончить ход текстом вопроса и ждать реплику.
Хуки PreToolUse срабатывают и у субагентов, так что исполнитель накрывается
тем же хуком без правки exec-агентов. Панель рисует блок из файла, пока файл
лежит. Ответ уходит обычной дорогой (сокет живой сессии, вход припаркованной
строки с пробуждением тиком) и снимает файл. В обычном терминале хук молчит.

`taskctl ask` уходит из скиллов, определений агентов и README как команда
агента. От него остаётся писатель признака, которого зовёт хук. Формат живёт в
go-пакете `chat`, и дублировать его на python незачем. Ключ `--wait` и опрос
входа уходят вместе с ним.

Приписка 2026-09-03 из LLD DK-756 (ревью чужой задачи): сценарий проектируется
под вопрос последней репликой хода со структурным блоком и парковкой строки,
без ожидания внутри команды. Гипотеза выше эту приписку доводит до конца.
Блок рождается из штатного инструмента, а не из отдельной команды.

## Границы

Список ждущих задач с переходом к вопросу остаётся за DK-696. Сводка решений
человека по ходу разговора (сворачивание отвеченных блоков) в задачу не
входит, блок после ответа остаётся в ленте как реплика.

## Развилки

Срок в самом `ask` остаётся, он про потолок хода Bash и занятый слот. Открыто
другое. Первое, откуда рисовать блок. Вызов `taskctl ask` лежит в транскрипте
с пачкой JSON в аргументах, ответ приходит записью вывода того же вызова по
ToolID. Второй вариант это признак ожидания с продлением жизни файла до ответа.
Второе, вопросы розданной работы задаёт субагент. Его вызовы лежат в своём
транскрипте, в ленту родителя они не попадают, и для них нужен второй источник.
Третье, чем панель узнаёт, живо ли ожидание. После отправки она говорит «ответ
заберёт заход» либо «ответ разбудит задачу тиком», а черновику поднимает
повторный груминг с уточнением.

## DoD

- Вызов AskUserQuestion в сессии, поднятой панелью, кладёт вопросы файлом
  признака без срока, паркует строку задачи, зовёт уведомление и отбивается
  подсказкой закончить ход текстом; в обычном терминале диалог работает как
  раньше. Вызов из субагента-исполнителя идёт той же дорогой.
- Панель рисует блок вопроса из признака без срока, сколько бы времени ни
  прошло, и без обратного отсчёта; ответ с панели снимает признак и едет
  сокетом живой сессии либо во вход припаркованной строки с пробуждением
  тиком.
- `taskctl ask` как команда агента убран из board-task, board-groom,
  goal-loop, exec-* и README; остаётся внутренний писатель признака для хука,
  `--wait` и опрос входа удалены вместе с тестами на них.
- Утренний случай воспроизведён стендом: вопрос диспетчера цели из tmux-чата
  виден в панели блоком, ответ кнопкой доходит в живую сессию, цель не встаёт.
- Регрессионные тесты на хук, на признак без срока и на снятие по ответу;
  README дашборда и hooks/README называют новую дорогу вопроса.

## Ранг

`50+8+2+0+4 = 64`, P1.

- Серьёзность 50: конвейер встаёт на человека там, где человек вопроса не
  видит; обход есть, руками в tmux, и он болезненный (цель DK-713 стояла
  четыре часа 2026-09-04, ночью то же с DK-695).
- Ценность 8: вопрос и ответ это единственный штатный выход из парковки и
  единственный канал развилки диспетчера, с ним каждый виток цели автономнее.
- Неопределённость 2: подход выбран (хук, признак без срока, панель), открыты
  детали: как хук называет разговор у чата цели и у субагента, чем снимается
  признак при уходе строки командой доски.
- Поправка на баг 0: механика меняется по решению DK-756.4, а не чинится
  регресс.
- Рычаг 4: одна дорога вопроса на все чаты и субагентов, `taskctl ask`
  уходит из четырёх скиллов и четырёх определений агентов.

Цена M: хук с тестами, три места в дашборде, вычистка скиллов и README,
стенд утреннего случая.

## Приёмка

- вид: mixed
- барьер «глаза»: блок вопроса рисуется в панели без обратного отсчёта
  - headless-браузер с замером: годится, `TestStaticAgentAskWidget` и
    `TestStaticChatPanelMeasured` меряют разметку и раскладку блока тем же
    приёмом, что у остального экрана;
  - утверждённый макет: не годится, макета для этого блока нет;
  - тест по разметке и стилям: частично годится, покрыт тем же замером

## Ход работы

- Из черновика DK-715 «Вопрос агента живёт в ленте чата до ответа, а не пока идёт ожидание»: записан 2026-09-02, оформлен 2026-09-04.
- Разработка: sonnet/high по вердикту pick, 2026-09-04.
- Вычитка: 14 файлов, 31 правка, 2 пометки, 2026-09-04.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 43%, снимок 43м назад, сдвига нет), 2026-09-04 11:44-13:52.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 46%, снимок 33м назад, сдвига нет), 2026-09-04 13:52-14:46.
- Снаружи: проверка после выката, 2026-09-04 14:46-14:49.
- Проверка: сценарий прогнал opus, 2026-09-04 14:49-15:22.
- Уточнение: вопрос: Тестовый вопрос DK-715: каким способом вы отвечаете на него?, 2026-09-04 15:22-15:22.

## Ревью

Уровень 3 до d291c23: два да триажа по critical_paths: hooks и tools/taskctl тронуты, путь вопроса человеку критичен для DoD

- блокирует: tools/dashboard/drafts.go:34-42 groomPrompt всё ещё велит грумеру звать `taskctl ask <ID> --question "..." --wait 480` и ждать ответа не кончая ход ("вопросом заход не кончай"); это ровно старая дорога, которую DK-715 убирает, флаг --wait у команды снят этим же диффом (tools/taskctl/ask.go), и живой заказ грумминга будет либо падать на неизвестном флаге, либо не заканчивать ход рубежом хука. Тест tools/dashboard/drafts_test.go:TestDraftWaitingFromAsk (не тронут диффом) до сих пор проверяет именно эту старую формулировку, поэтому регресс не поймался. Чинить groomPrompt на заказ через AskUserQuestion, как в board-groom/SKILL.md п.16, и обновить тест: исправлено, groomPrompt зовёт AskUserQuestion, тест TestDraftWaitingFromAsk переведён на новую формулировку, regcheck подтвердил красноту на старом тексте, 8c84e5686
- блокирует: tools/taskctl/main.go встроенная справка ("Менять доску", строки ~100 и ~114-124) для `ask` и `draft ask` не обновлена: всё ещё показывает `[--wait N]` и описывает старое поведение ("ход зовётся с явным сроком... срок по умолчанию 480 секунд... --wait 0 паркует сразу"), хотя флаг --wait снят этим же диффом из askFlags. taskctl без аргументов или на ошибке разбора аргументов покажет агенту/человеку неверную подсказку про несуществующий флаг. DoD прямо требует "ключ --wait ... удалены"; TestUsageMatchesFlagSet в main_test.go уже ловит такое рассогласование для add/set, но на ask не распространён. Поправить текст справки под фактические флаги (--question, --session): исправлено, справка ask и draft ask поправлена под askFlags, TestUsageMatchesFlagSet расширен на ask и draft ask с обратной проверкой, regcheck подтвердил красноту на HEAD, dc650d7a9
- блокирует: tools/taskctl/README.md:79,84 (краткий список команд в начале файла) всё ещё показывает `ask <ID> [--question "..."] [--wait N] [--session SID]` и `draft ask <ID> [--question "..."] [--wait N]`, с описанием старого поведения ("не дождавшись, команда паркует задачу сама"), хотя флаг снят и раздел «Вопрос человеку посреди захода» ниже в этом же файле уже переписан верно. Пропуск моего первого круга: читал только развёрнутый раздел, не начальный список команд. Поправить синопсис под фактические флаги (--question, --session), как в main.go usageText: исправлено, синопсис README поправлен под askFlags (--question, --session), проверено check-symbols.py, c0d6189df

## Сценарий проверки

1. Признак без срока живёт до ответа, а не до часов, и снимается ответом:

```sh
GOWORK=off go -C internal/chat test -run 'TestAskForeverRoundTrip|TestAskLiveUntilExpiry' -v ./...
```

Ожидание: `PASS`, `TestAskForeverRoundTrip` подтверждает, что первой строкой
признака лежит метка `-`, а не штамп, и что `Live` не гаснет и через десять
лет.

2. Писатель `taskctl ask` всегда паркует сразу, без `--wait`, признак остаётся
   лежать до ответа:

```sh
GOWORK=off go -C tools/taskctl test -run TestAsk -v ./...
```

Ожидание: `PASS`, `TestAskWritesTheForeverStamp` и `TestAskParksAtOnce`
подтверждают признак без срока и парковку одним вызовом.

3. Хук `ask-panel.py` перехватывает AskUserQuestion только в сессии, поднятой
   панелью, и молчит в обычном терминале:

```sh
python3 -m unittest discover -s hooks -p "ask_panel_test.py" -v
```

Ожидание: `OK`, 11 тестов, среди них `test_plain_terminal_passes` (диалог не
тронут) и `test_panel_session_blocks_and_calls_writer` (хук зовёт писатель и
отбивает исходный вызов).

4. Утренний случай DK-713: вопрос диспетчера цели из живого tmux-чата виден в
   панели блоком, ответ кнопкой доходит клавишами в ту же сессию, цель
   возвращается в In progress тем же ходом, а не тиком:

```sh
GOWORK=off go -C tools/dashboard test -run TestMorningCaseGoalAskReachesLiveSession -v ./...
```

Ожидание: `PASS`, ответ ушёл `send-keys`, `moved.log` несёт
`move DK-713 in-progress --push`, признак снят.

5. Живой терминал получает ответ клавишами, а не файлом, который читать
   больше некому; без терминала (делегат, парковка) ответ остаётся файлом со
   страховкой сторожка:

```sh
GOWORK=off go -C tools/dashboard test -run 'TestChatSayAnsweredByKeys|TestChatSayGoesToInputWhileAskWaits|TestChatSaySettlesHandedAsk' -v ./...
```

Ожидание: `PASS` на всех четырёх тестах.

6. Страховка тика ловит признак без срока, брошенный писателем между записью и
   парковкой (страховка меряет возрастом файла, а не сроком, которого нет), и
   не трогает признак при живой сессии:

```sh
python3 -m unittest discover -s tools/devkitctl -p "watch_test.py" -k ParkStaleTest -v
```

Ожидание: `OK`, среди тестов `test_forever_ask_is_parked_once_orphaned` и
`test_forever_ask_with_live_session_keeps_the_row`.

7. Доктор находит неподключённый хук `ask-panel.py` и раскладывает его тем же
   `--fix`, чистый проект после `new` тоже не горит находкой:

```sh
python3 -m unittest discover -s tools/devkitctl -p "devkitctl_test.py" -k test_hooks_are_laid_out_once -k test_fresh_project_is_clean -v
```

Ожидание: `OK`, находка про `ask-panel.py` на PreToolUse видна до `--fix` и
пропадает после.

## Выкат

- 2026-09-04 слито: 561982868, c0d6189df, c15077a7d, a81f9f5b6, dc650d7a9, 8c84e5686, 5662027d9, cab8f4e65, d291c23f0, 7423b4e37, bd38a0001, 8e15e18c0, c613723d3, cdfd639e4, be15a9ba0, ca86c8480

## Проверка

Агентская часть сценария прогнана целиком на слитом main (5302371de),
семь пунктов из семи сошлись с ожиданием, расхождений нет.

Пункт 1, признак без срока:

```
=== RUN   TestAskForeverRoundTrip
--- PASS: TestAskForeverRoundTrip (0.00s)
=== RUN   TestAskLiveUntilExpiry
--- PASS: TestAskLiveUntilExpiry (0.00s)
PASS
ok  	github.com/dronrider/devkit/internal/chat	0.596s
```

Пункт 2, писатель ask: `PASS`, 12 тестов, среди них
`TestAskWritesTheForeverStamp` и `TestAskParksAtOnce`, оба ожидаемые именем.

Пункт 3, хук `ask-panel.py`: `OK`, ровно 11 тестов, в списке
`test_plain_terminal_passes` и `test_panel_session_blocks_and_calls_writer`.

Пункт 4, утренний случай DK-713:

```
=== RUN   TestMorningCaseGoalAskReachesLiveSession
--- PASS: TestMorningCaseGoalAskReachesLiveSession (0.37s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	0.922s
```

Тело теста сверено с прозой ожидания: ответ уходит `send-keys`, в `moved.log`
ложится `move DK-713 in-progress` с `--push`, а `chat.ReadAsk` после ответа
признака не находит.

Пункт 5, живой терминал: `PASS` на четырёх тестах,
`TestChatSayGoesToInputWhileAskWaits`, `TestChatSayAnsweredByKeysDropsTheAsk`,
`TestChatSayAnsweredByKeysUnparksTheRow`, `TestChatSaySettlesHandedAsk`.
Третье имя сценария ловит префиксом два теста, отсюда и четвёртый.

Пункт 6, страховка тика: `OK`, 10 тестов `ParkStaleTest`, среди них
`test_forever_ask_is_parked_once_orphaned` и
`test_forever_ask_with_live_session_keeps_the_row`.

Пункт 7, доктор: `OK`, 2 теста. Находка про неподключённый хук проверяется
внутри `test_hooks_are_laid_out_once` до `--fix` и раскладка после него.

За пользователем остаётся живая половина барьера «глаза»: вопрос из настоящего
tmux-чата и вид блока на экране панели, включая ответ кнопкой. Прогоном это не
имитируется, машинно её держат замеры разметки `TestStaticAgentAskWidget` и
`TestStaticChatPanelMeasured`.
