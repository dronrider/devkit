# DK-716: Работа по задаче из любого чата даёт строке «Стоп» и вход в живой чат

## Что происходит

2026-09-02, DK-704 в in-progress. Работу по ней ведёт доводящий чат dff98764, поднятый с дашборда под именем chat-<ID>-<n>. Чат привязан к задаче по факту работы, в реестре сессий лежит запись «работа, повод taskctl move DK-704». Файла этапа в ~/.devkit/runs у задачи нет, agentctl stage чат не звал.

Признак «строка присвоена» считает leadsTask в tools/dashboard/registry.go. При непустом имени tmux решает только имя. Ведут task-* и goal-*, всё остальное считается разговором (решение DK-460, чтобы чат про задачу не выглядел вторым исполнителем). Такая работа помечается Talk, фильтры run и run_busy в tools/dashboard/tasks.go её пропускают, и у строки пустой run. Чип этапа по тому же пустому run гаснет в rowStage. У сессии без имени tmux (терминал, окно vscode) ведущей считается почти любая привязка, кроме «рука».

Для доски запись реестра сворачивается правилом «последняя выигрывает», а список чатов задачи берёт множество Touched. Чат dff98764 сначала двигал DK-684, потом DK-704, и для доски он работа только по DK-704.

На форме DK-704 стоят рядом чип «активна» и жёлтая кнопка «Продолжить», в списке у строки нет ни кружка работы, ни «Стоп». По нажатию «Продолжить» тот же чат находится по реестру, и в его живой ход попадает вводная «Продолжай работу по DK-704». Кнопка «Выполнить» у такой строки доступна, и с неё поднимается второй исполнитель поверх идущей работы. Иконка чата открывает список чатов, до разговора с идущим ходом ещё один клик. Человек не может по экрану сказать, работают ли по задаче и где.

### Проверено на груминге

2026-09-02, чтением кода. `leadsTask` (tools/dashboard/registry.go:173) при
непустом имени tmux решает только по префиксам `task-` и `goal-`, а без имени
считает работой всё, кроме привязки рукой. Строка собирается тем же признаком.
За неё отвечают `run` и `run_busy` в tools/dashboard/tasks.go, чип этапа
гаснет в `rowStage` по тому же пустому `run`. Ни `Stage`, ни `Record` в
internal/stage/stage.go поля сессии не несут, дописывать его придётся вместе
с форматом записи.

## Чего хотим

Задача, над которой идёт работа в любом чате (дашборд, терминал, окно vscode), в списке и на форме показывает «Стоп» и чип этапа, кнопок «Выполнить» и «Продолжить» у неё нет. Иконка чата открывает ту сессию, где ход идёт сейчас, при нескольких самую свежую по реплике. Когда все рабочие сессии остановились или строка ушла из in-progress, кнопки возвращаются.

## Границы

Защита DK-460 остаётся. Чат, который задачу только читал, записей «работа» не
набирает и вторым исполнителем не становится. Индикатор внутри панели чата
правит DK-701, здесь трогаются строка задачи и её кнопки.

DK-723 эта правка не снимает. Там свой предмет, склейка перезапусков конвейера
в одну запись списка чатов. Строки списка ключует `chatEntries` по ID сессии, и
эту дорогу тут никто не трогал. Вклад косвенный. Список рабочих задач сессии
теперь считает `sessions.Works`, и группировать перезапуски есть по чему, не
разбирая имя окна.

## Развилки

Развилки сняты в разговоре 2026-09-02, исполнителю остаётся механика.

1. Одиночного «ведущего» в модели нет. У задачи множество рабочих сессий. Рабочей сессию делает запись реестра «заказ», «дерево» или «работа», запись «рука» оставляет разговором, «снята» снимает. Имя tmux в критерии не участвует. Дашбордный чат, который только читал задачу, записей «работа» не набирает и рабочим не становится, защита DK-460 остаётся.

   Решение исполнителя 2026-09-04: слов в критерии два, «дерево» и «работа».
   «Заказ» из него убран. Разбор такой. Слово это пишет хук старта всякому
   подъёму дашборда. `launchEnv` ставит `DEVKIT_TASK` одинаково конвейеру,
   циклу цели, разбору черновика и чату по строке. Возьми критерий
   «заказ», и рабочей становилась бы любая строка, по которой человек открыл
   чат почитать. Кнопка запуска у неё уезжала бы в тот же чат вместо подъёма
   исполнителя по вердикту pick. Это противоречит «Границам» этой же задачи и
   сносит защиту DK-460 вместе с её регрессионным тестом. Конвейер и цикл цели
   строку от этого не теряют. Их работа видна списком tmux по собственному
   имени сессии, и до критерия дело не доходит. Остаётся узкая дыра. Сессия
   конвейера, чьё окно уже умерло, а транскрипт ещё свеж, строку до первой
   команды доски не присваивает. Цена дыры меньше цены отобранной кнопки
   запуска, и правило board-task эту дыру закрывает само, обязывая взявшего
   открыть этап.
2. Состояние строки это свёртка по множеству. Ход идёт, если идёт хоть у одной. Ждёт человека, если ждёт хоть одна и никто не работает. Иначе простаивает.
3. Привязку снимает конец работы по задаче. Следующая задача той же сессии прежнюю привязку оставляет. Закрытие строки, перевод её из in-progress и «отпустить» рукой кладут «снята». Пока этого не было, сессия работает по всем своим задачам, и каждая строка её показывает.
4. Взятие задачи в любом чате это действие. Правило board-task обязывает открыть этап agentctl stage <ID> разработка, тот кладёт запись «работа» в реестр. В запись этапа добавляется ID сессии, сейчас в Record его нет. Страховкой остаётся кнопка привязки на форме.
5. «Стоп» у конвейера убивает tmux, как сейчас. «Стоп» у чата прерывает текущий ход, кладёт «снята», а в чат приходит сообщение, что работа по задаче остановлена. Разговор остаётся живым. При нескольких рабочих сессиях «Стоп» показывает список.
6. Доска не опрашивается по таймеру, обновляется по фокусу и событию уведомителя. Начало и конец хода уведомитель видит (UserPromptSubmit, Stop). Перерисовку кнопки после смены состояния проверить живым прогоном.

Развилки доработки после провала приёмки, сняты исполнителем 2026-09-05.

7. Что значит остановить работу, когда ход не идёт, а фоновая работа жива.
   Решение такое. Стоп это состояние работы, и одним нажатием оно не кончается.
   Ход прерывается сразу, а при живой фоновой работе на окно встаёт заказ
   дожима. Привязка держится, строка стоит под «Стопом», сторож разговоров
   прерывает всякий поднявшийся ход теми же двумя Escape. Заказ кончается
   тишиной. Нового хода нет, фоновая работа встала, и в реестр идёт «снята».
   Слова человека в тот же разговор снимают заказ раньше, срок заказа четверть
   часа от первого нажатия.

   Живой фоновую работу держат три мерки, и хватает любой. Две первые те же,
   что у кольца пульса: журнал субагента без метки возврата писался последние
   две минуты, либо ответа на его вызов в транскрипте нет вовсе и журнал моложе
   получаса. Третья мерка про долгий инструмент. Субагент в сборке или в
   прогоне тестов журнала не трогает минутами, а вызов в хвосте его журнала
   стоит без ответа, и хвост этот разбирается тем же способом, каким меряется
   ход разговора. Мерка одна на оба места, стоп и состояние строки. Без третьей
   мерки полный прогон пакета дашборда, а он идёт больше двух минут, выглядел
   бы вставшей работой, и стоп снимал бы привязку посреди неё, то есть
   возвращал бы расхождение приёмки (замечание ревью 2).

   Отвергнуто четыре дороги. Убить сессию разговора, как убивают конвейер. Это
   снято прежним разбором задачи, у разговора память человека, и следующая
   реплика обязана попасть в неё же. Считать работу оконченной концом хода. Это
   и была прежняя дорога, строка тогда показывала свободной занятую задачу.
   Ждать конца фоновой работы, ничего не прерывая. Вернувшийся субагент поднимет
   агента, тот раздаст новую работу, и остановка не наступит никогда. Написать
   агенту реплику «остановись». Реплика поднимает новый ход и встаёт в очередь
   клиента за той самой работой, которую просят прекратить.

8. Как взятие становится видно в строке, пока человек смотрит на список.
   Решение такое. Круг обновления заводит и живая сессия проекта, у которой
   строки ещё нет, а доска перечитывается по возврату на вкладку наравне с
   фокусом окна.

   Отвергнуто три дороги. Постоянный опрос доски по таймеру ест батарею телефона
   ради экрана, на котором ничего не движется. Событие уведомителя на запись
   «работа» запускало бы python-хук по десятку раз за вечер, а лента получила бы
   десяток карточек, которых человек не просил. Серверный канал состояния доски
   дороже предмета, круг решает тот же случай тремя строками клиента.

9. Дыра стопа конвейера. Её нет, и это проверено живым прогоном 2026-09-05.
   Конвейер снимают убийством tmux-сессии, клиент умирает вместе с ней, и
   боковой журнал субагента встаёт в ту же секунду. Последняя запись 14:59:42,
   убийство 14:59:43, дальше журнал не рос. Правка стопа конвейера не касается.

## DoD

- Рабочей сессию задачи делает запись реестра «дерево» или «работа», имя tmux в
  критерии не участвует, а «рука» оставляет сессию разговором. «Заказ» в
  критерий не входит, разбор в развилке 1. Рабочих сессий у задачи может быть
  несколько.
- Состояние строки собирается свёрткой по этому множеству: ход идёт, ждёт
  человека, простаивает.
- Пока по строке идёт работа, в списке и на форме стоят чип этапа и «Стоп», а
  кнопок «Выполнить» и «Продолжить» нет. Остановились все рабочие сессии или
  строка ушла из in-progress, кнопки вернулись.
- «Стоп» у чата прерывает текущий ход, кладёт в реестр «снята», а в чат
  приходит сообщение, что работа по задаче остановлена; разговор остаётся
  живым. При нескольких рабочих сессиях «Стоп» показывает список. «Стоп»
  конвейера убивает tmux, как убивал.
- Конец работы над задачей отмечают и утилиты доски: `taskctl close` и
  `taskctl move` в любой статус кроме In progress кладут «снята» с названной
  задачей, соседних задач той же сессии запись не трогает.
- Иконка чата открывает сессию с идущим ходом, при нескольких самую свежую по
  реплике.
- В записи этапа появляется ID сессии, и `agentctl stage <ID> разработка`
  кладёт запись «работа» в реестр.
- Тесты на критерий рабочей сессии и на свёртку состояния; перерисовка кнопки
  после смены состояния проверена живым прогоном дашборда.
- «Стоп» у разговора, чей ход уже кончился, а фоновые субагенты живы, привязку
  не снимает. Она держится до тишины, а всякий поднявшийся ход прерывается тем
  же стопом. Строка при этом стоит под «Стопом» и говорит подсказкой, что
  остановка идёт.
- Строка получает «Стоп» без перезагрузки страницы и тогда, когда работа
  открылась в живом разговоре при открытом списке задач. Доска перечитывается
  по возврату на вкладку наравне с фокусом окна.
- Правило рабочей сессии и свёртка записаны в docs/lld/DK-430-task-chat.md,
  поведение кнопок строки в tools/dashboard/README.md.

## Ранг

`50+8+3+0+2 = 63`, в строке доски 71 по подтяжке от цели DK-713, P1.

- Серьёзность 50: с кнопки «Выполнить» у занятой строки поднимается второй
  исполнитель поверх идущей работы, а обход только глазами по списку чатов
  (ответ пользователя 2026-09-02).
- Ценность 8: доска перестаёт показывать неверное состояние работы по задаче.
  Это прямая ценность экрана, ради которого дашборд заводился.
- Неопределённость 3: развилки сняты разговором, но правка едет разом в
  сервере и в клиенте, а перерисовку кнопки видно только на живом прогоне.
- Поправка на баг 0: критерий `leadsTask` написан так сознательно (защита от
  второго исполнителя), и работа тут переопределяет модель без починки
  регресса.
- Рычаг 2: единый критерий работы снимает разъезд, которым этот признак уже
  ломался, и удешевляет соседние задачи цели.

Цена L: правится критерий в реестре, сборка строки, формат записи этапа,
`agentctl stage`, панель чата и кнопки экрана, и всё это с тестами.

## Приёмка

- вид: mixed
- барьер «глаза»: возврат кнопок и чипа после остановки работы виден только
  на экране дашборда, утверждённого макета под него нет.
  - обход «стендом на настоящем app.js»: не годится. Стенд poc_rowstop.mjs
  кормит рендер готовыми полями строки и проверяет разметку кнопок, подсказки
  и списка выбора. Перерисовку по живому событию он не поднимает: событий
  уведомителя в поддельном DOM нет, а доска по таймеру себя не опрашивает.
  - обход «сквозным прогоном dashboard smoke»: не годится. Прогон бьёт по
  ручкам и сверяет ответы, экрана в нём нет вовсе, а предмет барьера это
  именно перерисованная кнопка у человека перед глазами.
  - обход «сверкой следов в журналах»: закрывает половину. Строка «снята» в
  реестре, строка о прерванном ходе в журнале дашборда и пометка в ленте
  разговора машинно проверяются шагами 1-4 и вложены в сценарий. Не
  закрывают они одного: увидел ли человек смену кнопки без перезагрузки
  страницы. Этот остаток и уходит человеку шагом 5.

  Приёмка 2026-09-05 провалена, и предмет живого шага сузился. Проверять теперь
  надо стоп в ту минуту, когда ход уже кончился, а фоновая работа жива, и
  смену кнопки на живой странице при взятии строки из чата. Оба места
  закрываются командами наполовину. Заказ дожима, повторные Escape и запись
  «снята» по тишине машинно проверяются шагами 1-3, а круг обновления и
  подсказка кнопки стендом poc_rowwake.mjs на настоящем app.js. Прежнего они не
  закрывают. Увидел ли человек смену кнопки своими глазами, и дошёл ли до него
  дожим второго хода. Этот остаток уходит человеку шагом 6.

## Ход работы

- Из черновика DK-716 «Работа по задаче из любого чата даёт строке стоп и вход в живой чат»: записан 2026-09-02, оформлен 2026-09-02.
- Вычитка: 1 файл, 11 правок, 0 пометок, 2026-09-02.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 40%, week_max 28% (панель разбивку не дала, цифра из кеша клиента возрастом 3ч 4м назад), снимок 46м назад, протух, сдвига нет), 2026-09-04 06:21-06:47.
- Уточнение: вопрос: DK-716, критерий рабочей сессии: слово «заказ» в него входит или нет? DoD называет три слова источника (заказ, дерево, работа). Но кнопка чата дашборда поднимает разговор о задаче тем же «заказом»: launchEnv ставит DEVKIT_TASK всякому подъёму, и хук старта пишет «заказ» и конвейеру, и чату chat-<ID>-<n>, и грумингу черновика. По букве DoD всякий открытый по строке чат делает её рабочей: строка теряет «Выполнить» и получает «Стоп», а нажатие пуска уводит вводную в этот чат вместо подъёма исполнителя по вердикту pick. «Границы» задачи говорят обратное, и на этом же случае стоит регрессионный тест защиты DK-460. Я взял пока вариант 1 и на нём всё зелено. Скажи, если нужен другой. 1. Критерий это «дерево» и «работа». Конвейер и цикл цели присваивают строку своей живой tmux-сессией, как присваивали (список tmux, мимо критерия), а чат становится рабочим, открыв этап agentctl stage или двинув строку командой доски. Защита DK-460 и её тест остаются как есть. Дыра: сессия конвейера, чьё окно уже умерло, а транскрипт ещё свеж, строку до первой команды доски не присваивает. 2. Критерий это три слова из DoD, тест DK-460 переписать. Всякий чат, поднятый по строке, сразу считается работой по ней. 3. Три слова, но подъём разговора перестаёт писать «заказ»: в реестр едет новое слово источника (скажем, «беседа»), и «заказ» остаётся за подъёмом работы. Правка шире: формат записи, хук старта и читатели слова (chatReply меряет по «заказу» кончившийся разговор)., 2026-09-04 06:47-06:55.
- Вычитка: 4 файла, 29 правок, 0 пометок, 2026-09-04.
- Снаружи: блок: вопрос: DK-716, критерий рабочей сессии: слово «заказ» в него входит или нет? DoD называет три слова..., 2026-09-04 06:55-14:51.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 47%, week_max 29% (панель разбивку не дала, цифра из кеша клиента возрастом 6ч 12м назад), снимок 1м назад, сдвига нет), 2026-09-04 15:37-16:09.
- Снаружи: проверка после выката, 2026-09-04 16:09-16:11.
- Проверка: сценарий прогнал sonnet, 2026-09-04 16:11-14:37.
- Вычитка: 3 файла, 27 правок, 0 пометок, 2026-09-05.

## Ревью

Уровень 3 до 9f0b644: неопределённость 3 по вердикту pick (opus/high), триаж даёт «да» первым вопросом: дифф это сам путь DoD, стоп работы и привязка строки, и правка идёт после провала приёмки на этом же пути

- Вердикт: без замечаний. критерий Works/Off/Release в internal/sessions прогнан (3 теста), формат этапа с 4м полем и обратная совместимость в internal/stage (2), agentctl stage помечает работу (2), taskctl close/move помечают снята и разбор статуса по первому слову за ID (2), полный пакет tools/dashboard (state/chat/run/stop marks, 409 список, 404 чужая сессия, соседние задачи не снимает) выборочно (22) и целиком (126с), стенд poc_rowstop.mjs на настоящем app.js, доки LLD DK-430 решение 8 и README dashboard/taskctl/agentctl сверены с кодом; мелкий комментарий в sessions.go поправлен самим коммитом 449075c
- [возврат: реализация, 2026-09-05] блокирует: subBusyOf (stopwait.go:41) считает фоновую работу живой только по свежести бокового журнала (subFresh, две минуты), а кольцо пульса считает её идущей ещё и по отсутствию ответа на вызов до subStale, тридцати минут (sessions.go:3128-3133, разбор при subClosed на 3059: «субагент, который думает третью минуту, в журнал не пишет, а работать не перестал»). Субагент на долгом инструменте (сборка, go test на две минуты) журнал не трогает, и стоп со строки посчитает работу вставшей: привязка снимется сразу, вернётся расхождение 2 приёмки, а board.go:346-352 тем же мерилом погасит «Стоп» и вернёт «Выполнить» посреди работы. Комментарий stopwait.go:31-34 и LLD DK-430 решение 9 при этом говорят, что мерка та же, что у кольца, а она не та. Чем проверить: боковой журнал без Ended, без tool_result на его вызов в транскрипте, mtime три минуты назад, стоп обязан оставить привязку
- [возврат: правила, 2026-09-05] блокирует: шаг 7 сценария не пройти как написано. Слова, которые велено дать чату («Отдай субагенту Explore задание ... а сам сразу закончи ход одной строкой»), не заставляют сессию присвоить строку, а подъём чата кладёт в реестр «заказ», который критерием рабочей сессии не считается (развилка 1). Строка «Стоп» не получит, ожидание «дождаться, когда сессия откроет этап» не наступит, а стоп со строки ответит 404 «работа не идёт». Свой же живой разбор 2026-09-05 запись «работа» писал рукой. Чем чинить: дописать в слова взятие задачи (agentctl stage <ID> разработка перед работой субагенту) либо сказать прямо, что запись кладут рукой по формату agentctl stage
- [возврат: реализация, 2026-09-05] не блокирует: заказ переживает свой объявленный срок. stopWaitOne на каждом дожиме переписывает StopAt = now (stopwait.go:152), и четверть часа отсчитывается не от нажатия, а от последнего прерванного хода: пока ходы поднимаются, заказ живёт сколько угодно, а README dashboard и LLD решение 9 говорят «срок заказа четверть часа». Либо держать время первого нажатия отдельным полем и мерить TTL по нему, либо поправить оба текста под то, что делает код
- [возврат: реализация, 2026-09-05] не блокирует: поднявшимся ходом сторож считает любую новую запись транскрипта (turn = last.After(since), stopwait.go:139), а снимает заказ только реплика через панель (handleChatSay). Отсюда два следствия. Ход, который человек поднял нарочно мимо панели, гасится молча: «Продолжить» со страницы, не успевшей перечитать доску, идёт в handleTaskContinue -> peerSay без stopWaitOff, и набор прямо в окне tmux тоже. И запись о прерывании, которую клиент дописывает сразу после Escape, даёт на первом же заходе сторожа ложную строку «ход разговора поднялся снова и прерван дожимом», а шаг 7 сценария грепает ровно её как доказательство дожима. Снимать заказ в общей точке доставки реплики человека, а не в одной ручке
- [возврат: правила, 2026-09-05] не блокирует: ветка срока в stopWaitOne (stopwait.go:157-162) без стенда. Девять тестов закрывают дожим, тишину, закрытое окно, реплику человека и подъём чата, а «фоновая работа не встала за четверть часа» это единственная защита от строки, вечно стоящей под «Стопом», и проверяется она только глазами
- [возврат: реализация, 2026-09-05] не блокирует: subBusyOf ходит в каталог боковых журналов на каждую работу при каждой сборке доски (ReadDir плюс чтение меты и Stat на журнал, sessions.go:2609), кеша у него нет, а круг обновления теперь ходит каждые три секунды у всякой живой сессии проекта. Рядом sessionBusy для того же держит кеш по mtime и размеру (sessions.go:2459), стоит взять его же
- [возврат: реализация, 2026-09-05] не блокирует: stopWaitSet ищет корень проекта заново по имени (projectPathOf), хотя зовущий держит found.Path. Не найдя проекта, заказ ложится без StopPath, и сторож на первом же заходе кончает его словами «фоновая работа встала», то есть тихо возвращает прежнее поведение стопа. Передать путь параметром

## Сценарий проверки

Первые шесть шагов гоняются командами (агентская часть смешанной приёмки),
седьмой идёт на живом дашборде после выката (пользовательская часть, барьер
«глаза»). Прогоняет сценарий не автор правки.

1. Критерий рабочей сессии и свёртка состояния строки. Работой сессию делает
   слово источника, а не имя окна; подъём разговора и привязка рукой работой не
   считаются; именная отвязка снимает одну задачу, а не всю сессию; состояние
   строки собирается по множеству сессий. На коде до правки шаги красные.
   Критерий там читает префикс имени tmux.

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/internal" test ./sessions/... -count=1 -run "TestWorks" -v
   ```

   Ожидание: строка `PASS`, три пройденных теста среди них
   `TestWorksStandsOnSourceNotWindow`, `TestWorksIgnoresOrderAndHand` и
   `TestWorksKeepsEveryTaskUntilRelease`, код возврата 0.

2. Строка доски и «Стоп» у работы, идущей в окне разговора. Свёртка состояния,
   разговор с идущим ходом у строки, прерывание хода двумя Escape, запись
   «снята» в реестре, строка о причине в ленте разговора и список при
   нескольких рабочих сессиях.

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/dashboard" test . -count=1 -run "TestStateMarks|TestRunMarksSpread|TestChatMarks|TestStoppable|TestRunStop" -v
   ```

   Ожидание: строка `PASS`, двадцать два пройденных теста среди них
   `TestStateMarksFoldsOverWorkSessions`, `TestChatMarksPicksLiveTurn`,
   `TestStoppableChatsPicksOurLiveWindows`, `TestRunStopInterruptsChatWork`,
   `TestRunStopListsSeveralChatWorks` и `TestRunStopRejectsForeignSession`, код
   возврата 0. Прежние тесты стопа конвейера входят в тот же набор нарочно.
   Под образец попадают и они, а стоп конвейера правка трогать не должна.

3. Отметка работы и её конец утилитами доски. Открытый этап называет сессию
   работой по задаче, ID разговора встаёт в запись этапа, закрытие строки и
   перевод её из In progress кладут именную «снята».

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/agentctl" test . -count=1 -run "TestTouchStage" -v
   ```

   Ожидание: строка `PASS`, два пройденных теста, `TestTouchStageMarksWork` и
   `TestTouchStageShowIsNotWork`, код возврата 0.

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" test . -count=1 -run "TestTouch" -v
   ```

   Ожидание: строка `PASS`, два пройденных теста, `TestTouchDoneOnCloseAndMoveOut`
   и `TestTouchWorkWritesRelease`, код возврата 0.

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/internal" test ./stage/... -count=1 -run "TestOpenKeepsSession|TestParseStageWithoutSession" -v
   ```

   Ожидание: строка `PASS`, два пройденных теста, код возврата 0.

4. Панельная часть на настоящем `static/app.js`. У строки, за которой работает
   окно разговора, стоит красный «Стоп» со своим исходом в подсказке. Иконка
   чата ведёт в разговор с идущим ходом. При нескольких рабочих сессиях стоп
   спрашивает, в какой прервать ход, и уносит выбранную в ручку.

   ```sh
   node "$(git rev-parse --show-toplevel)/tools/dashboard/testdata/poc_rowstop.mjs" "$(git rev-parse --show-toplevel)/tools/dashboard/static/app.js"
   ```

   Ожидание: строка `poc_rowstop: ok` и код возврата 0.

5. Дожим стопа и живость фоновой работы. Боковой журнал субагента считается
   работой, строка с ним не гаснет, стоп при живой фоновой работе привязку не
   снимает, поднявшийся ход прерывается дожимом, а после тишины заказ кончается
   записью «снята». На коде до правки шаги красные. Стоп снимал привязку сразу
   и отвечал «работа снята».

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/dashboard" test . -count=1 -run "TestSubBusyOfSeesBackgroundWork|TestRowStaysBusyWhileSubagentWorks|TestRunStopWaitsForBackgroundWork|TestStopWaitPressesRisenTurn|TestStopWaitReleasesWhenWorkStands|TestStopWaitEndsWithWindow|TestStopWaitOffOnHumanReply|TestStopWaitDropsOnRaise|TestChatStopSaysBackgroundWork" -v
   ```

   Ожидание: строка `PASS`, девять пройденных тестов, код возврата 0.

6. Обновление строки на настоящем `static/app.js`. Круг обновления заводит
   живая сессия, у которой строки ещё нет; взятая ею строка показывает «Стоп»
   ближайшим заходом круга; доска перечитывается по возврату на вкладку;
   подсказка «Стопа» во время дожима говорит про фоновых субагентов.

   ```sh
   node "$(git rev-parse --show-toplevel)/tools/dashboard/testdata/poc_rowwake.mjs" "$(git rev-parse --show-toplevel)/tools/dashboard/static/app.js"
   ```

   Ожидание: строка `poc_rowwake: ok` и код возврата 0.

7. - Исключение: обкатка (живая связка «фоновый субагент -> строка доски ->
   кнопка» поднимается только на настоящем дашборде с живым клиентом, барьер
   «глаза» не снят). Прежний живой шаг этот случай пропускал. Он проверял стоп
   в идущем ходе, а провалилась приёмка на стопе после конца хода.

   Открыть панель и поднять чат по задаче из In progress. Подъём кладёт в реестр
   «заказ», а рабочей строку делает первая команда доски (развилка 1), и потому
   взятие задачи стоит первым в словах чату.

   ```text
   Открой этап командой agentctl stage <ID> разработка. Потом отдай субагенту
   Explore задание перечислить все функции в каждом .go файле каталога
   tools/dashboard, а сам сразу закончи ход одной короткой строкой, результата
   не жди.
   ```

   Не уходя со списка задач, дождаться, когда строка сама, без перезагрузки,
   сменит кнопку запуска на «Стоп». Дальше дождаться конца хода (уведомление
   `turn_done`) и в эту минуту нажать «Стоп» со строки. Ход уже кончился, а
   боковой журнал субагента ещё пишется. Дальше не трогать ничего и смотреть,
   что делает строка, пока субагент дорабатывает и поднимает агента снова.

   ```text
   grep "стоп дожимается" ~/.devkit/dashboard.log
   grep "прерван дожимом" ~/.devkit/dashboard.log
   grep "источник снята" ~/.devkit/sessions.log
   ```

   Ожидание: ответ кнопки говорит, что ход прерван, а по задаче ещё работают
   фоновые субагенты. Строка остаётся под «Стопом», и подсказка кнопки называет
   дожим. В журнале дашборда стоит строка «фоновая работа жива, стоп
   дожимается», а на возврате субагента строка «ход разговора chat-<ID>-<n>
   поднялся снова и прерван дожимом». Когда боковые журналы затихли, в реестре
   появляется строка с задачей и словом источника «снята», строка доски
   возвращает кнопку запуска без обновления страницы, а в ленте разговора стоят
   обе строки, про дожим и про остановленную работу. Сама сессия жива и
   следующую реплику берёт.

## Выкат

- 2026-09-04 слито: e63d2b3f0, 51977ca94, c54383ba5, 60a4905cb, e312c5fec, 12386b22c, a676b78ba, 07f618645, ffa764c8d, 99c4c0fb6, 01e1e147b, cef00f37d, dde40076e, 9848f5a3c
- smoke прогнан, 2026-09-04

## Проверка

### Приёмка провалена, 2026-09-05

Человек прогнал живой шаг на задаче XR-034 и нашёл два расхождения. Разбор по
журналам подтвердил оба, придумывать причины не пришлось.

Что делал человек: зашёл в новый чат по задаче и написал «Выполни XR-034»,
закрыл окно (проверял с телефона), в списке задач увидел прежнюю кнопку
запуска вместо «Стоп», обновил страницу, «Стоп» появился, нажал его, работа в
чате продолжилась, и остановилась только от кнопки стоп в самом чате.

Хронология по журналам:

```console
14:29:04 чат поднят, ход начат
14:29:37 запись «работа» (taskctl file)
14:30:00 запись «работа» (taskctl move)
14:30:22 ход кончен
14:30:33 стоп со строки: ход прерван, привязка снята
14:30:23, 14:30:55, 14:31:27, 14:31:50 события subagent_stop
14:32:15 ход начат снова
```

Расхождение 1. Строка не сменила кнопку без перезагрузки. DoD требует смены на
живой странице, и обход этого места был назван в «Приёмке» как то, что стендом
не закрывается.

Расхождение 2, тяжелее. Стоп прерывает ход клиента двумя Escape, а работа с
субагентами этим не останавливается: они доработали своё, вернули результаты, и
агент продолжил через полторы минуты. Привязка при этом уже снята, то есть
строка доски считает работу оконченной, пока та идёт. Второй стоп человека, из
самого чата, попал в живой ход и сработал.


### Живой разбор доработки, 2026-09-05

Оба расхождения воспроизведены живьём, на работающем дашборде и настоящем
клиенте. Чат поднят ручкой панели в дереве задачи, работа ему дана с фоновым
субагентом, стоп нажат ручкой строки. Запись «работа» в реестре написана рукой
по формату `agentctl stage`, чтобы прогон не трогал этапы живой задачи.

```console
14:54:11 реплика: отдай работу субагенту и закончи ход
14:54:26 последняя запись транскрипта разговора
14:54:27 уведомление turn_done: ход кончился
14:54:33 стоп со строки, ответ 200 «ход прерван, работа снята»
14:54:33 в реестре строка «источник снята»
14:54:33 ... 14:57:51 боковой журнал субагента пишется дальше, шагом в секунды
```

Escape прерывать было нечего, транскрипт разговора после стопа не двинулся ни
разу. Работа при этом шла. Боковой журнал субагента рос ещё три с лишним
минуты. Привязка снялась в ту же секунду, и строка доски показывала задачу
свободной посреди работы. Ход, который субагент поднимает своим возвратом,
виден по часам в первом прогоне того же дня. Ход кончился в 14:47:27, события
subagent_stop пришли в 14:47:29 и 14:47:30, новый ход кончился в 14:47:36.

Стоп конвейера проверен тем же прогоном. Сессия чата снята `tmux kill-session`
в 14:59:43, последняя запись бокового журнала субагента стоит на 14:59:42, и
дальше он не рос ни разу за две минуты наблюдения. Убийство сессии уносит
фоновую работу с собой, дыры у стопа конвейера нет.

Расхождение 1 разобрано по коду клиента и подтверждено стендом. Круг обновления
доски заводили строки с признаком работы, а разговор до первой команды доски
строки не присваивает. Круг, который заметил бы взятие, не заводился. Заводил
его тот самый признак, который взятие и добавляет. Стенд `poc_rowwake.mjs` на
коде main падает первой же проверкой, «круга обновления при живом разговоре
нет».

### Прогон другой моделью (sonnet, 2026-09-04)

- 2026-09-04 16:12, дерево main c0d157080e73, чекаут чистый, агентская часть сценария (шаги 1-4), все зелёные.

Шаг 1 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/internal" test ./sessions/... -count=1 -run "TestWorks" -v
=== RUN   TestWorksStandsOnSourceNotWindow
--- PASS: TestWorksStandsOnSourceNotWindow (0.00s)
=== RUN   TestWorksIgnoresOrderAndHand
--- PASS: TestWorksIgnoresOrderAndHand (0.00s)
=== RUN   TestWorksKeepsEveryTaskUntilRelease
--- PASS: TestWorksKeepsEveryTaskUntilRelease (0.00s)
PASS
ok  	github.com/dronrider/devkit/internal/sessions	0.421s
```

Три теста, все PASS, ровно те, что названы ожиданием.

Шаг 2 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/dashboard" test . -count=1 -run "TestStateMarks|TestRunMarksSpread|TestChatMarks|TestStoppable|TestRunStop" -v
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	2.390s
```

Двадцать два теста, все PASS, среди них все названные ожиданием
(`TestStateMarksFoldsOverWorkSessions`, `TestChatMarksPicksLiveTurn`,
`TestStoppableChatsPicksOurLiveWindows`, `TestRunStopInterruptsChatWork`,
`TestRunStopListsSeveralChatWorks`, `TestRunStopRejectsForeignSession`).

Шаг 3 (зелёный, три подкоманды):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/agentctl" test . -count=1 -run "TestTouchStage" -v
=== RUN   TestTouchStageMarksWork
--- PASS: TestTouchStageMarksWork (0.00s)
=== RUN   TestTouchStageShowIsNotWork
--- PASS: TestTouchStageShowIsNotWork (0.00s)
PASS
ok  	github.com/dronrider/devkit/tools/agentctl	0.535s

$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" test . -count=1 -run "TestTouch" -v
=== RUN   TestTouchDoneOnCloseAndMoveOut
--- PASS: TestTouchDoneOnCloseAndMoveOut (0.00s)
=== RUN   TestTouchWorkWritesRelease
--- PASS: TestTouchWorkWritesRelease (0.00s)
PASS
ok  	github.com/dronrider/devkit/tools/taskctl	0.506s

$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/internal" test ./stage/... -count=1 -run "TestOpenKeepsSession|TestParseStageWithoutSession" -v
=== RUN   TestOpenKeepsSession
--- PASS: TestOpenKeepsSession (0.00s)
=== RUN   TestParseStageWithoutSession
--- PASS: TestParseStageWithoutSession (0.00s)
PASS
ok  	github.com/dronrider/devkit/internal/stage	0.454s
```

Каждая подкоманда дала ровно два пройденных теста, как названо ожиданием.

Шаг 4 (зелёный):

```console
$ node "$(git rev-parse --show-toplevel)/tools/dashboard/testdata/poc_rowstop.mjs" "$(git rev-parse --show-toplevel)/tools/dashboard/static/app.js"
poc_rowstop: ok
```

Код возврата всех команд 0, строки и счёт тестов совпали с ожиданием сценария
дословно. Шаг 5 (пометка «- Исключение: обкатка», барьер «глаза») остаётся
человеку.

- Обкатка: 2026-09-05 15:38, свежее дерево 5f3df972fb64, сценарий 1b67472f, временный HOME, утилит дерева 11, шагов 8, все зелёные.

Шаг 1 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/internal" test ./sessions/... -count=1 -run "TestWorks" -v
=== RUN   TestWorksStandsOnSourceNotWindow
--- PASS: TestWorksStandsOnSourceNotWindow (0.00s)
=== RUN   TestWorksIgnoresOrderAndHand
--- PASS: TestWorksIgnoresOrderAndHand (0.00s)
=== RUN   TestWorksKeepsEveryTaskUntilRelease
--- PASS: TestWorksKeepsEveryTaskUntilRelease (0.00s)
PASS
ok  	github.com/dronrider/devkit/internal/sessions	0.432s
```

Шаг 2 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/dashboard" test . -count=1 -run "TestStateMarks|TestRunMarksSpread|TestChatMarks|TestStoppable|TestRunStop" -v
=== RUN   TestStateMarksFoldsOverWorkSessions
--- PASS: TestStateMarksFoldsOverWorkSessions (0.00s)
=== RUN   TestRunMarksSpreadOverEveryRow
--- PASS: TestRunMarksSpreadOverEveryRow (0.00s)
=== RUN   TestChatMarksPicksLiveTurn
--- PASS: TestChatMarksPicksLiveTurn (0.00s)
=== RUN   TestStoppableChatsPicksOurLiveWindows
--- PASS: TestStoppableChatsPicksOurLiveWindows (0.00s)
=== RUN   TestRunStopInterruptsChatWork
--- PASS: TestRunStopInterruptsChatWork (0.52s)
=== RUN   TestRunStopListsSeveralChatWorks
--- PASS: TestRunStopListsSeveralChatWorks (0.48s)
=== RUN   TestRunStopRejectsForeignSession
--- PASS: TestRunStopRejectsForeignSession (0.05s)
=== RUN   TestRunStopWithoutAnyWork
--- PASS: TestRunStopWithoutAnyWork (0.04s)
=== RUN   TestRunStopGoal
--- PASS: TestRunStopGoal (0.11s)
=== RUN   TestRunStopGoalSayFailureNamed
--- PASS: TestRunStopGoalSayFailureNamed (0.10s)
=== RUN   TestRunStopNotifies
--- PASS: TestRunStopNotifies (0.11s)
=== RUN   TestRunStopWithoutNotifierNamed
--- PASS: TestRunStopWithoutNotifierNamed (0.06s)
=== RUN   TestRunStopTask
--- PASS: TestRunStopTask (0.05s)
=== RUN   TestRunStopForeignPrefixUntouched
--- PASS: TestRunStopForeignPrefixUntouched (0.05s)
=== RUN   TestRunStopGoalLedOutside
--- PASS: TestRunStopGoalLedOutside (0.05s)
=== RUN   TestRunStopNotRunning
--- PASS: TestRunStopNotRunning (0.05s)
=== RUN   TestRunStopForeignOriginLogged
--- PASS: TestRunStopForeignOriginLogged (0.00s)
=== RUN   TestRunStopNotRunningLogged
--- PASS: TestRunStopNotRunningLogged (0.59s)
=== RUN   TestRunStopInteractiveSessionLogged
--- PASS: TestRunStopInteractiveSessionLogged (0.08s)
=== RUN   TestRunStopProjectNotFoundLogged
--- PASS: TestRunStopProjectNotFoundLogged (0.02s)
=== RUN   TestRunStopNoTmuxLogged
--- PASS: TestRunStopNoTmuxLogged (0.00s)
=== RUN   TestRunStopInteractiveSession
--- PASS: TestRunStopInteractiveSession (0.05s)
=== RUN   TestRunStopWaitsForBackgroundWork
--- PASS: TestRunStopWaitsForBackgroundWork (0.52s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	3.470s
```

Шаг 3 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/agentctl" test . -count=1 -run "TestTouchStage" -v
=== RUN   TestTouchStageMarksWork
--- PASS: TestTouchStageMarksWork (0.00s)
=== RUN   TestTouchStageShowIsNotWork
--- PASS: TestTouchStageShowIsNotWork (0.00s)
PASS
ok  	github.com/dronrider/devkit/tools/agentctl	0.510s
```

Шаг 4 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" test . -count=1 -run "TestTouch" -v
=== RUN   TestTouchDoneOnCloseAndMoveOut
--- PASS: TestTouchDoneOnCloseAndMoveOut (0.00s)
=== RUN   TestTouchWorkWritesRelease
--- PASS: TestTouchWorkWritesRelease (0.00s)
PASS
ok  	github.com/dronrider/devkit/tools/taskctl	0.492s
```

Шаг 5 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/internal" test ./stage/... -count=1 -run "TestOpenKeepsSession|TestParseStageWithoutSession" -v
=== RUN   TestOpenKeepsSession
--- PASS: TestOpenKeepsSession (0.00s)
=== RUN   TestParseStageWithoutSession
--- PASS: TestParseStageWithoutSession (0.00s)
PASS
ok  	github.com/dronrider/devkit/internal/stage	0.358s
```

Шаг 6 (зелёный):

```console
$ node "$(git rev-parse --show-toplevel)/tools/dashboard/testdata/poc_rowstop.mjs" "$(git rev-parse --show-toplevel)/tools/dashboard/static/app.js"
poc_rowstop: ok
```

Шаг 7 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/dashboard" test . -count=1 -run "TestSubBusyOfSeesBackgroundWork|TestRowStaysBusyWhileSubagentWorks|TestRunStopWaitsForBackgroundWork|TestStopWaitPressesRisenTurn|TestStopWaitReleasesWhenWorkStands|TestStopWaitEndsWithWindow|TestStopWaitOffOnHumanReply|TestStopWaitDropsOnRaise|TestChatStopSaysBackgroundWork" -v
=== RUN   TestSubBusyOfSeesBackgroundWork
--- PASS: TestSubBusyOfSeesBackgroundWork (0.01s)
=== RUN   TestRowStaysBusyWhileSubagentWorks
--- PASS: TestRowStaysBusyWhileSubagentWorks (0.08s)
=== RUN   TestRunStopWaitsForBackgroundWork
--- PASS: TestRunStopWaitsForBackgroundWork (0.54s)
=== RUN   TestStopWaitPressesRisenTurn
--- PASS: TestStopWaitPressesRisenTurn (0.91s)
=== RUN   TestStopWaitReleasesWhenWorkStands
--- PASS: TestStopWaitReleasesWhenWorkStands (0.54s)
=== RUN   TestStopWaitEndsWithWindow
--- PASS: TestStopWaitEndsWithWindow (0.49s)
=== RUN   TestStopWaitOffOnHumanReply
--- PASS: TestStopWaitOffOnHumanReply (0.52s)
=== RUN   TestStopWaitDropsOnRaise
--- PASS: TestStopWaitDropsOnRaise (0.47s)
=== RUN   TestChatStopSaysBackgroundWork
--- PASS: TestChatStopSaysBackgroundWork (0.46s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	4.503s
```

Шаг 8 (зелёный):

```console
$ node "$(git rev-parse --show-toplevel)/tools/dashboard/testdata/poc_rowwake.mjs" "$(git rev-parse --show-toplevel)/tools/dashboard/static/app.js"
poc_rowwake: ok
```
