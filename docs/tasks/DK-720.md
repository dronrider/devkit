# DK-720: Слияние и отметка выката не спотыкаются о чужое незакоммиченное

## Что происходит

2026-09-02, четыре отказа за день. shipctl merge и shipctl smoke спрашивают
чистоту командой git status --porcelain --untracked-files=no по всему рабочему
каталогу (tools/shipctl/ops.go:138-143). В devkit по одной доске идут
несколько сессий сразу, и в основном чекауте почти всегда лежит чужое
незакоммиченное. Слияние DK-684 отбилось правкой docs/tasks/DK-711.md от
соседней сессии. Слияние DK-704 отбилось той же правкой. Отметка smoke за
DK-684 отбилась правками docs/TASKS.md и docs/tasks/DK-713.md от сессии,
заводившей DK-716. Ни одна из этих правок к отбитой задаче отношения не имела.

DK-684 и DK-704 запарковались вопросом к человеку и простояли на блокере
полтора часа каждая. Прогон smoke ждал две с половиной минуты и сдался.
Агент в такой ситуации бессилен по правилам. Чужую правку он трогать не
вправе, а ждать ему нечего, потому что сессия-соседка про него не знает.
Работа нескольких рук по одной доске это штатный режим devkit, и ворот,
который на нём спотыкается, гасит автономность на ровном месте.

## Чего хотим

Слияние и отметка выката проходят, когда чужие правки в общем чекауте не
пересекаются с файлами задачи.

## Границы

Предмет это предусловие чистоты у merge и smoke. Диагностика самого отказа
(чей это дифф и по какой задаче) остаётся за DK-674, отказ на грязном дереве
задачи в worktree (ops.go:723) и проверка перед переключением готового дерева
(worktree.go:278) не трогаются.

## Развилки

Чем считать множество файлов задачи, с которым сверяется чужое незакоммиченное.
Годятся дифф диапазона слияния, дифф ветки от точки ветвления, список путей из
последнего прогона тестов. Разница видна на слиянии, которое тянет за собой
чужой коммит: диапазон шире ветки, и на нём сузить получится меньше.

Направление снято ответом человека 2026-09-02. Проверка сужается на месте,
общий чекаут остаётся точкой слияния. Накрываются merge и smoke, а ship, push
и revert требуют чистоты как прежде: revert коммитит без путей и чужое из
индекса уехало бы в его коммит. Изъятия по путям для доски и файлов задач не
заводится, они проходят общим сужением.

## DoD

- `shipctl merge` и `shipctl smoke` проходят на чекауте, где лежит чужое
  незакоммиченное, не пересекающееся с файлами задачи.
- Пересечение по-прежнему отбивается, и отказ называет пересёкшиеся пути, а не
  весь `git status`.
- `shipctl ship`, `shipctl push` и `shipctl revert` требуют чистоты как прежде,
  и у revert это записано причиной рядом с кодом.
- Тесты на временном репозитории с синтетической доской: слияние с чужой
  правкой мимо файлов задачи проходит, слияние с правкой по своему файлу
  отбивается, обкатка ведёт себя так же. Краснота регрессии сверена `regcheck`.
- Поведение описано в `tools/shipctl/README.md` там же, где сейчас сказано про
  предусловие чистого дерева.

## Ранг

`50+8+2+0+4 = 64`, P1.

`50+8+2+0+4 = 64`, P1.

- Серьёзность 50: слияние и отметка выката встают, обход есть, но болезненный.
  За 2026-09-02 четыре отказа, DK-684 и DK-704 простояли на блокере по полтора
  часа каждая, разбирал их человек.
- Ценность 8: несколько сессий по одной доске это штатный режим devkit, а
  слияние и обкатка идут у каждой задачи.
- Неопределённость 2: направление и состав зовущих названы ответом, открыто
  одно, чем считать множество файлов задачи.
- Баг 0: предусловие задумано таким, оно слишком широкое, а не сломано.
- Рычаг 4: возвращает автономность конвейеру. Каждое следующее слияние в пачке
  и в цикле цели обходится без захода человека.

## Приёмка

- вид: agent
- барьеров не названо: сценарий гоняется на временном репозитории с
  синтетической доской, итог каждого шага это код возврата и строка вывода,
  прод не трогается.

## Ход работы

- Из черновика DK-720 «Предусловие чистого дерева у shipctl ловит чужие правки в чекауте»: записан 2026-09-02, оформлен 2026-09-02.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 8%, снимок 26м назад, сдвига нет), 2026-09-02 16:07-16:07.
- Ревью: субагент sonnet/high по вердикту pick --role review, две находки (самодостаточность сценария, след regcheck в «Ход работы»).
- Краснота регрессии сверена командой `cd tools/shipctl && GOWORK=off regcheck --base main -- go test ./... -run 'TestMergeIgnoresForeignDirty|TestMergeRefusedOnOwnFileDirty|TestSmokeIgnoresForeignDirty|TestSmokeRefusedOnOwnFileDirty'`, тест краснеет на main и проходит на текущем коде, регрессия закрыта.
- Сценарий проверки переписан на агентский шаг после слияния (`docs/tasks/DK-720-check.sh`, собранный бинарь shipctl против синтетического репозитория), обе находки ревью закрыты.

## Ревью

- Сценарий проверки и раздел «Проверка» состоят из одного и того же прогона go test с теми же четырьмя тестами ветки (TestMergeIgnoresForeignDirty, TestMergeRefusedOnOwnFileDirty, TestSmokeIgnoresForeignDirty, TestSmokeRefusedOnOwnFileDirty), без отдельного шага после выката/слияния. По ACCEPTANCE.md («Самодостаточность агентской части») повтор тестов ветки в приёмку не входит: они зелёные ещё до слияния, и taskctl fail на них не отличить от штатной работы. Обход исполним (собрать бинарь shipctl и прогнать реальные команды merge/smoke через subprocess на синтетическом репозитории после слияния в main, как это сделано для DK-711 в дашборде, шаг «после выката, не повтор тестов ветки»), поэтому вид agent понижать не нужно, а сценарий переписывается добавлением такого шага.: исправлено, сценарий переписан на docs/tasks/DK-720-check.sh: собранный бинарь shipctl против синтетического репозитория после слияния, не повтор тестов ветки
- DoD требует, чтобы краснота регрессии была сверена regcheck, и TASKFORM.md явно указывает записывать след прогона regcheck в разделе «Ход работы», но такой строки в файле задачи нет. Сам прогон regcheck -C tools/shipctl --base main -- go test ./... -run TestMergeIgnoresForeignDirty подтверждает красноту на main и зелень на текущем коде, то есть по существу требование выполнено, но след прогона нужно дописать в «Ход работы».: исправлено, след прогона regcheck дописан в «Ход работы»

## Сценарий проверки

Агентский, после слияния в main: собранный бинарь `shipctl` против отдельного
синтетического репозитория с доской, не повтор тестов ветки (ACCEPTANCE.md,
«Самодостаточность агентской части»). Чужой файл соседней задачи коммитится,
потом правится мимо коммита; своя ветка отдельно. Слияние и smoke проходят на
чужой правке и отбиваются на правке своего файла, отказ называет только его.

```sh
sh docs/tasks/DK-720-check.sh
```

Ждём четыре строки `ok` (по одной на шаг) и `--- готово` в конце, без слова
`ПРОВАЛ`.

## Выкат

- 2026-09-02 слито: 7037fbea, 09f5d569, 06b93186, 2218e896, 853e2ce2, 0381b5be, 396f008a

## Проверка


- Обкатка: 2026-09-02 17:46, свежее дерево 90795c4062ba, сценарий b591d710, временный HOME, утилит дерева 10, шагов 1, все зелёные.

Шаг 1 (зелёный):

```console
$ sh docs/tasks/DK-720-check.sh
--- 1. merge: чужая незакоммиченная правка мимо диапазона слияния не отбивает
ok
--- 2. merge: незакоммиченное по своему файлу отбивает и называет только его
ok
--- 3. smoke: чужая незакоммиченная правка мимо файлов задачи не отбивает
ok
--- 4. smoke: незакоммиченное по своему файлу отбивает и называет только его
ok
--- готово, дерево: /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/tmp.OsG5agxbVF
```
