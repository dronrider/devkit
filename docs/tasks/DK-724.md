# DK-724: Конвейер задачи идёт одной живой сессией вместо череды проходов

## Что происходит

2026-09-02, разбор после DK-720 и DK-722. Витки цели без человека поднимает
goal-run.py, конвейер задачи с дашборда поднимает task-run.py. Обе обёртки на
каждый виток или проход запускают отдельный клиент с флагом -p в tmux-сессии
goal-<ID> или task-<ID>. Ход такого клиента кончается вместе с процессом.
Дизайн DK-079 выбрал это сознательно, «сессии короткие по построению».

Конец хода это выход процесса, и вместе с ним гибнет всё, чего голова не
дождалась. По DK-720 шесть проходов подряд убили слияние с полным прогоном
тестов на середине. Слилось только из живого чата, полтора часа простоя и
квота вхолостую (DK-722). Фоновых субагентов сессия добивает через десять минут
после хода. Поэтому скиллы доски зовут исполнителя и ревьювера синхронно, и
голова стоит всё время их работы. Каждый проход это новая сессия со своим ID.
По DK-720 в реестре семь записей с одним заголовком. Реплика человека доходит
только до живого прохода (DK-723). Каждый проход заново читает состояние с
диска и заново ориентируется, разгон сгорает на каждом витке.

Два довода дизайна практика уже сняла. Раздел «Цикл в чате» скилла goal-loop
ведёт витки одной длинной сессией, и контекст не раздувается, тяжёлую работу
несут субагенты. Панель дашборда поднимает разговор по задаче интерактивным
клиентом в tmux, chat-<ID>-<n>. Headless-ход кончается раньше, чем
человек прочитает реплику.

## Чего хотим

На цель и на задачу с дашборда поднимается одна интерактивная сессия в tmux.
Витки и проходы приходят в неё репликами. Долгая команда и фоновый субагент
доживают до конца, уведомления доходят до головы, у задачи в списке чатов одна
запись без склейки, реплика человека попадает в живую работу всегда.

## Границы

Дорогу обкатывает конвейер задачи, `task-run.py`. Цикл цели (`goal-run.py`)
едет второй строкой. Развилки к тому времени сняты, и заводит её исполнитель
сам, повесив зависимость через `taskctl dep add` (ответ пользователя
2026-09-02).

Безголовый проход остаётся запасным входом (ответ пользователя 2026-09-02).
Машина без tmux, расписание и подъём после ребута идут им же.

DK-722 с доски не снимается. Её вторая половина это рубеж синхронности на
долгую команду, и живой сессии он тоже нужен. DK-723 стоит до слияния DK-724 и
снимается тогда, когда проходов не станет.

Проходов не стало внутри одного прогона. Этим снят случай, ради которого DK-723
заводилась, вечер DK-720 с семью записями на одну задачу. Целиком строка не
закрывается, и снимать её без остатка не надо (замечание ревью 2). Остановы у
живой головы те же, что были. Потолок проходов, воронка, вышедший клиент,
перезагрузка машины. После любого из них кнопка поднимает новую сессию со своим
ID под тем же именем окна. В реестре ложится вторая запись, и список чатов
(`chatEntries`, ключ строки это ID сессии) покажет её отдельной строкой. Склейку
списка по имени окна DK-724 не пишет, `chats.go` не тронут ни строкой. Записей
на задачу теперь единицы вместо дюжины, и серьёзность строки от этого падает.
Переписать её стоит под оставшийся предмет, склейку перезапусков конвейера.

Носитель разговора цели трогает черновик DK-439, и канал заказа витка зависит
от того же черновика.

## Развилки

Охват и судьба соседних строк сняты разговором 2026-09-02, механика осталась
исполнителю.

Проходы поднимаются в одном месте, `task-run.py`. Оболочка зовёт клиента
строкой `list(self.client) + ["-p", order]`, ждёт выхода процесса и по статусу
строки решает, поднимать ли следующий. Дашборд к этому месту приводит двумя
дорогами, кнопкой экрана (`runs.go`) и подъёмом прогона после выката
(`checkrun.go`), а обе сходятся в `startTaskSession`. Она поднимает
tmux-сессию `task-<ID>`, где команда окна это оболочка, а за нею клиент без
заказа. Слова заказов сочиняет `runPrompt`, оболочка их не пересказывает.

Реплике человека мешает доехать то же самое. Панель адресует разговор окну
(DK-430.2), а в окне стоит печатный процесс. Он читает заказ из аргумента и в
терминал не смотрит. Реплика уходит в панель и остаётся там до конца прохода.
Каждый проход при этом новая сессия со своим ID и своей записью в реестре
чатов, откуда и растёт DK-723.

Живая сессия выглядит так. Оболочка поднимает интерактивного клиента в своей же
панели, отдав ему терминал окна, и остаётся его родителем. Первый заказ едет
первым аргументом, следующие приходят клавиатурой окна через `tmux send-keys`,
той же дорогой, какой панель подаёт реплику человека. Процесс не выходит между
проходами. Долгая команда и фоновый субагент доживают до конца, а метка
печатного режима с клиента снимается. Рубеж синхронности отбивал бы живому окну
как раз то, ради чего оно заведено.

**Конец прохода узнаётся отметкой хука.** Кода возврата у живой головы нет.
Транскрипт растёт и на середине долгого хода. Типов записей в нём полтора
десятка, от `bridge-session` до `atis-latch`, и разбор их зависит от версии
харнеса. Панель терминала читается глазами, но не разбором. Харнес говорит про
конец хода прямо, событием `Stop`, и его ловит новый хук `hooks/turn-mark.py`.
Строки ложатся в `~/.devkit/turns.log` рядом с реестром чатов. Событий у хука
четыре: конец хода, его провал ошибкой API, вопрос сессии и приход реплики.
Своя сессия узнаётся по реестру чатов, где имя окна пишет только клиент со
своим терминалом (DK-673). Отметки соседних сессий в счёт не идут.
Отметка «начат» держит оболочку от заказа поверх чужого хода.

**Своя отметка узнаётся по себе самой.** Оба журнала
общие на машину, и хук режет их по размеру. `append_capped` переписывает файл
последними пятьюстами строками. Оболочка, читавшая журнал по смещению, после
такого реза перечитывала его с начала. Своя прошлая отметка возвращалась и шла
за конец текущего прохода (замечание ревью 1). Теперь оболочка отбирает из
журнала свои строки и помнит прочитанные. Отметки узнаются по началу ID сессии,
записи реестра по имени окна. Чужих строк на машине тысячи, своих за весь
конвейер набирается десяток, и памяти на них уходит меньше килобайта. Строка без
перевода строки на конце пропускается. Рез это перезапись файла целиком, и
чтение попадает на её середину. По той же причине память не сжимается до того,
что лежит в файле. Чтение посреди реза видит пустую середину, а сжатая по нему
память вернула бы прочитанное второй раз.

Отвергнуто три дороги. Первая это порядковый номер прохода в самой отметке.
У хука нет своего состояния, счётчик ему брать неоткуда, а счёт строк того же
общего журнала зависел бы от чужих сессий. Вторая это отметка строго новее
последней принятой. Харнес пишет время до секунды, и «начат» короткого хода
попадает в ту же секунду, что предыдущий «кончен». Такая отметка терялась бы
целиком. Третья это отдельный журнал на задачу вместо общего. Задачу хук знает
не в каждой сессии (`DEVKIT_TASK` ставит подъёмщик, у разговора человека его
нет). Тот же рез случается и внутри файла на задачу. Общий журнал стоит рядом
с реестром чатов и журналом уведомителя и читается тем же разбором.

**Заказ идёт клавиатурой окна.** Межсессионный канал живой голове не годится.
У оболочки нет своей сессии, а значит нет и сокета, а `taskctl ask` работает в
обратную сторону. Подача через `tmux send-keys` уже написана и обкатана для
реплики человека (`chats.go`), и оболочка повторяет её дословно, вместе с
паузой перед переводом строки и скобками вставки для многострочного текста.

**Вставшая сессия зовёт человека тем же журналом.** Запрос разрешения приходит
событием `Notification`, и та же отметка кладёт его в журнал словом «ждёт» с
типом вопроса. Оболочка пишет строку в `.devkit/log` и зовёт уведомителя
поводом `wait_human` в тот же момент. Отдельно ловится молчание. Полчаса без
единой отметки значат неподключённый хук либо замёрзшее окно, и это тоже
уходит человеку.

**После ребута работу поднимает та же кнопка.** Своего замка оболочка не
заводит и не заводила. Состояние задачи лежит на доске и в дереве задачи, а
живой сессии после перезагрузки нет ни в tmux, ни в реестре. Кнопка экрана
поднимает окно заново, оболочка читает строку доски и идёт дальше. Замок,
названный в развилке, стоит у цикла цели, и переезд `goal-run.py` разбирается
своей строкой.

Панельная часть тут не тронута ни строкой. Список чатов, лента и адресация
реплики остались как были, а одна запись на задачу получилась оттого, что
сессия стала одна. Из go-файлов правились только слова комментариев, которые
называли конвейер печатным.

## DoD

- Конвейер задачи ведёт одна интерактивная сессия в tmux. Проходы приходят в
  неё репликами, и нового клиента на каждый из них никто не поднимает.
- Долгая команда и фоновый субагент доживают до конца, уведомление доходит до
  головы. Слияние с полным прогоном тестов, на котором встали шесть проходов
  DK-720, из такой сессии доезжает с первого раза.
- Обёртка знает конец прохода и решает, звать ли следующий. Способ назван в
  шапке `task-run.py` словами.
- Сессия, вставшая на неизвестном запросе разрешения, видна снаружи. Обёртка
  пишет это в журнал и зовёт уведомителя в тот же момент.
- После перезагрузки машины работа поднимается той же командой. Сессии нет,
  замок снят, состояние прочитано с диска.
- В реестре `~/.devkit/sessions.log` на задачу приходится одна запись.
- Безголовый вход остаётся рабочим. Без tmux или по флагу конвейер идёт
  проходами, как шёл.
- Регрессионный тест на живучесть долгого шага падает на сегодняшнем коде и
  проходит на исправленном, краснота сверена прогоном `regcheck`.
- Устройство записано в шапке `task-run.py` и в скилле `board-task`, а
  расхождение с решением DK-079 дописано в
  `docs/lld/DK-079-autonomous-loop.md`.

## Ранг

`50+8+3+0+4 = 65`, P1.

- Серьёзность 50. Конвейер работает, но встаёт на человека там, где обещана
  автономия. 2026-09-02 шесть проходов подряд умерли на слиянии DK-720, задача
  простояла полтора часа, слилось только из живого чата. Обход есть и он
  болезненный, долгие шаги гонятся руками.
- Ценность 8. Автономный конвейер это то, ради чего оболочки заводились, и
  живая голова возвращает им обещанное. Работа не гибнет, разгон не сгорает на
  каждом проходе, реплика человека попадает в живую работу.
- Неопределённость 3. Дорога названа, но четыре развилки механики открыты, и
  снимает их исполнитель (ответ пользователя 2026-09-02). Задача пойдёт с
  диспетчером в цикле.
- Поправка на баг 0. Проходы это исходный выбор дизайна DK-079, а не регресс.
- Рычаг 4. Дешевле становится каждая следующая задача с экрана и каждый виток
  цели, а предмет DK-723 снимается целиком.

## Проверено на груминге

2026-09-02, чтением кода и скиллов.

- Предмет на месте. `turn_cmd` в `kit/skills/goal-loop/goal-run.py:310` собирает
  виток как `["-p", "--session-id", sid, prompt]`, а `task-run.py:201` зовёт
  клиента как `list(self.client) + ["-p", order]`.
- Оба довода дизайна сняты практикой. Раздел «Цикл в чате»
  (`kit/skills/goal-loop/SKILL.md:245`) прямо запрещает уводить витки в фон и
  ведёт их живой сессией. Дашборд поднимает разговор задачи интерактивным
  клиентом (`tools/dashboard/chats.go:1812`) и подаёт в него реплики через
  `tmux send-keys` (`chats.go:1915`). Headless-ход кончается раньше
  человека.
- Материал для второй развилки уже лежит в коде. Подача реплики в живой tmux
  написана и обкатана там же, `chats.go:1401` про имена и `chats.go:1419` про
  подачу в идущий процесс.
- Дубля нет. DK-722 и DK-723 лечат следствия проходов и остаются по ответу
  пользователя, черновик DK-439 про носитель разговора цели, а не про носитель
  работы.

## Приёмка

- вид: agent

Итог проверяется с машины агента: журнал обёртки, файл вывода долгого шага,
число записей в реестре сессий и статус строки после прохода. Ни один из шести
барьеров не назван.

## Ход работы

- Из черновика DK-724 «Цикл цели и конвейер задачи идут одной живой сессией, а не проходами -p»: записан 2026-09-02, оформлен 2026-09-02.
- Вычитка: 1 файл, 17 правок, 0 пометок, 2026-09-02.
- Вычитка: 7 файлов, 47 правок, 1 пометка, 2026-09-03.
- Краснота проверена тремя прогонами `regcheck`, 2026-09-03. Стенд живой головы:
  `regcheck --tests kit/skills/board-task/task_run_test.py -- python3
  kit/skills/board-task/task_run_test.py TestLiveHead.test_one_process_serves_every_pass
  TestLiveHead.test_long_step_lives_through_the_pass
  TestLiveHead.test_live_head_drops_the_headless_mark
  TestLiveHead.test_registry_holds_one_record_for_the_task`. Отметка хода:
  `regcheck --tests hooks/turn_mark_test.py -- python3 hooks/turn_mark_test.py`.
  Находка доктора: `regcheck --tests
  tools/devkitctl/devkitctl_test.py,tools/devkitctl/testenv.py -- python3
  tools/devkitctl/devkitctl_test.py ProjectFindingsTest.test_5d_turn_mark_events`.
  Все три ответили «тест краснеет на HEAD и проходит на текущем коде».
- Замечание ревью 1 закрыто, 2026-09-03. Отметка узнаётся по себе самой,
  оболочка помнит прочитанные свои строки и не обрабатывает их повторно.
  Краснота:
  `regcheck --tests kit/skills/board-task/task_run_test.py -- python3
  kit/skills/board-task/task_run_test.py
  TestLiveHead.test_trimmed_journal_does_not_repeat_a_turn`, три прогона подряд
  ответили «тест краснеет на HEAD и проходит на текущем коде». Тест пропажи
  журнала (`test_lost_journal_calls_the_human`) регрессионным не считается.
  Вызов человека при молчании конвейера работал и до правки, тест это сторож
  на случай, когда журнала нет вовсе.
- Репетиция сценария исполнителем, 2026-09-03: шаги 1-3 гонялись в рабочем
  дереве (`Ran 23 tests`, `Ran 9 tests`, `Ran 17 tests`, все `OK`), шаги 4 и 5
  не гонялись, они спрашивают разложенные хуки и живую службу после выката.
  Прогон сценария остаётся за проверяющим.
- Вычитка: 1 файл, 6 правок, 0 пометок, 2026-09-03.

## Ревью

Уровень 3 до 2098af7: неопределённость 2 (agentctl pick), плюс триаж: дифф трогает hooks/turn-mark.py (critical_paths) и ломает путь, от которого зависит DoD (отметка хода единственный сигнал конвейера) - два «да»
- блокирует: ~/.devkit/turns.log общий на всю машину. append_capped (hookio.py) режет его на 100КБ до последних 500 строк. Pipeline.tail() (task-run.py:311) на такой усушке сбрасывает своё смещение в 0. Дальше он перечитывает файл с начала. Своя уже прочитанная отметка часто ещё лежит в оставленном хвосте: оболочка опрашивает журнал раз в WATCH_STEP=3с и почти всегда успевает её увидеть до того, как она устареет. marks() и wait_turn() (task-run.py:404,428) тогда видят эту отметку второй раз. Старый «кончен» или «упал» принимается за конец текущего прохода. Оболочка либо шлёт следующий заказ поверх ещё идущего хода, либо копит ложные «вхолостую» и не вовремя останавливает здоровый конвейер. Уведомитель молчит про истинную причину. Проверено воспроизведением: снял Pipeline.tail() отдельно, дописал 700+ строк чужих сессий до превышения LOG_LIMIT после уже прочитанной своей отметки. На следующем tail() своя отметка «кончен» вернулась второй раз. Тестов на ротацию журнала нет ни в TestLiveHead, ни в turn_mark_test.py. Нужна метка «уже видел», по содержимому строки или отдельному счётчику. Она должна пережить сброс смещения на усохшем файле, либо стоит вовсе отказаться от сырого offset.: исправлено
- не блокирует: DK-723 своим DoD хотел склейку списка чатов по любому числу сессий одного tmux-имени, после реестра в семь записей на проход. DK-724 такую склейку не пишет, chats.go не тронут вовсе. Многосессийность снимается только для непрерывного прогона: test_registry_holds_one_record_for_the_task и test_one_process_serves_every_pass гоняют один run() без перезапуска. PASS_LIMIT=6, IDLE_LIMIT=3, падение головы и ребут остаются рабочими остановами и у живой головы. После такого стопа кнопка поднимает новую tmux-сессию с новым ID под тем же именем task-<ID>. В sessions.log снова ляжет несколько записей на задачу. chatEntries (ключ - ID сессии) покажет их разными строками списка, тем самым старым способом. Проблема DK-723 решена для рутинного случая, ради которого заводилась (DK-720). Для случая рестарта она не закрыта буквально. Это стоит учесть, когда строка DK-723 будет сниматься с доски, а не считать её закрытой без остатка.: отклонено, склейка списка чатов это DoD самой DK-723, и в этой задаче chats.go не трогается; вердикт исполнителя записан в «Границы»: рутинный случай DK-720 снят, случай перезапуска остался, строку с доски без остатка не снимать

## Сценарий проверки

1. Юниты оболочки конвейера. Живая голова служит все проходы одним процессом,
   долгий шаг доживает до конца, метка печатного режима с живого клиента снята и
   печатному проходу оставлена, реестр чатов держит одну запись на задачу,
   запасной вход берётся флагом и отсутствием окна, вопрос сессии и молчание
   зовут человека, реплика человека держит заказ, вышедший клиент останавливает
   конвейер, рез журнала не даёт второго конца прохода, а пропавший журнал не
   оставляет конвейер молчать.
   ```sh
   cd "$(git rev-parse --show-toplevel)/kit/skills/board-task" && GOWORK=off python3 task_run_test.py
   ```
   Ожидание: последние строки вывода это `Ran 23 tests` и `OK`, код возврата 0.
2. Юниты отметки хода. Конец хода, его провал и вопрос сессии ложатся в журнал
   своими словами, конец субагента отметкой не считается, сессия подпроцесса
   делегирования молчит, кривое событие сессию не роняет.
   ```sh
   cd "$(git rev-parse --show-toplevel)/hooks" && python3 turn_mark_test.py
   ```
   Ожидание: последние строки вывода это `Ran 9 tests` и `OK`, код возврата 0.
3. Юниты доктора про раскладку хука. Отметка хода ложится `--fix` на четыре
   события, повторный проход её не дублирует, а пропажа любого события это
   находка со словами о том, что без неё ломается.
   ```sh
   cd "$(git rev-parse --show-toplevel)/tools/devkitctl" && GOWORK=off python3 devkitctl_test.py ProjectFindingsTest
   ```
   Ожидание: последние строки вывода это `Ran 17 tests` и `OK`, код возврата 0.
4. Шаг идёт после выката, на машине с разложенными хуками. Первая команда
   спрашивает настройки харнеса, вторая смотрит журнал отметок, который к этому
   времени уже наполнили живые сессии машины, включая ходы самого прогоняющего.
   Обкатка в свежем дереве настроек машины не трогает.
   ```
   python3 -c 'import json,os;d=json.load(open(os.path.expanduser("~/.claude/settings.json")));print(sorted(e for e,g in d["hooks"].items() if any("turn-mark.py" in h["command"] for b in g for h in b["hooks"])))'
   tail -n 3 ~/.devkit/turns.log
   ```
   Ожидание: первая команда печатает `['Notification', 'Stop', 'StopFailure',
   'UserPromptSubmit']`, вторая три строки вида `<время> сессия <ID> ход кончен
   повод - дерево <путь>`, и обе уходят кодом 0.
   - Исключение: обкатка (шаг спрашивает разложенные доктором настройки машины,
     а правка доезжает до них выкатом)
5. Живой конвейер задачи после выката. Задача поднимается с экрана дашборда
   кнопкой «Выполнить» (годится любая строка Backlog, тут `<ID>`), и через
   несколько минут работы спрашиваются три вещи: сколько окон конвейера у задачи,
   сколько записей в реестре чатов и что оболочка написала в журнал проекта.
   ```
   tmux ls | grep -c '^task-<ID>:'
   grep -c ' tmux task-<ID> ' ~/.devkit/sessions.log
   grep 'task-run' "$(git rev-parse --show-toplevel)/.devkit/log" | tail -n 3
   ```
   Ожидание: первая команда печатает `1`, вторая `1` (а не по записи на проход,
   как было до правки), третья показывает строки вида «конец прохода живой
   головы task-<ID> (кончен), <ID> в in-progress, проход 1 из 6, N с». Все три
   уходят кодом 0.
   - Исключение: обкатка (шаг поднимает работу живой службой дашборда, а в
     свежем дереве ни доски с живой задачей, ни tmux-сессий нет)

## Проверка


- Обкатка: 2026-09-03 15:21, свежее дерево 7a3176341aae, сценарий 5afdf4bd, временный HOME, утилит дерева 10, шагов 3, все зелёные.

Шаг 1 (зелёный):

```console
$ cd "$(git rev-parse --show-toplevel)/kit/skills/board-task" && GOWORK=off python3 task_run_test.py
----------------------------------------------------------------------
Ran 23 tests in 68.878s

OK
```

Шаг 2 (зелёный):

```console
$ cd "$(git rev-parse --show-toplevel)/hooks" && python3 turn_mark_test.py
.........
----------------------------------------------------------------------
Ran 9 tests in 0.198s

OK
```

Шаг 3 (зелёный):

```console
$ cd "$(git rev-parse --show-toplevel)/tools/devkitctl" && GOWORK=off python3 devkitctl_test.py ProjectFindingsTest
.................
----------------------------------------------------------------------
Ran 17 tests in 26.093s

OK
```
