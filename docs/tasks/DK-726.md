# DK-726: Поиск чата достаёт разговор из архива и из-за окна списка

Цель: [tasks/DK-713.md](tasks/DK-713.md)

## Что происходит

2026-09-02, разведка цели DK-713 чтением кода. Список разговоров панели
приезжает окном в трое суток (`chatWindowDays`, tools/dashboard/chats.go:655),
глубже ведёт кнопка «показать раньше» ступенями 7, 30 и весь список машины.
Поиск окна не знает нарочно: набранный запрос догружает список целиком,
`chatLoadWindow(project, st, 0)` в static/app.js.

С архивом вышло иначе. Фильтр стоит раньше поиска. Строка
`list = chatArchShown(st.chats, chatArchMode())` режет набор до того, как по
нему пройдёт запрос. Убранный в архив разговор не находится ничем, пока
человек не проведёт кнопку архива по кругу. Положение кнопки лежит в
localStorage и переживает перезагрузку, умолчание «архивные скрыты»
держится месяцами. Счёта спрятанного тоже нет. Список не говорит, сколько
разговоров он не показал, и пустая выдача читается как «такого разговора не
было».

Цель DK-713 держит в DoD по одному чату в каждом из шести барьерных состояний,
и два из них (архивный, старше окна) начинаются с того, что разговор надо
найти.

## Чего хотим

Поиск в списке разговоров идёт по всей машине и мимо положения кнопки архива:
набранный запрос достаёт и убранный разговор, и разговор глубже окна. Архивная
находка стоит в выдаче со своим клеймом «в архиве». Так человек видит, откуда
взялась строка. Выдача, оставшаяся пустой при непустом архиве, говорит словами,
сколько разговоров скрыто положением кнопки и окном списка.

## Границы

Ступени окна и само положение кнопки архива остаются как есть. Задача меняет
только выдачу поиска и слова под ней. Ввод в найденный разговор
и подъём его резюмом идут своими строками цели (DK-727, DK-728).

## Развилки

- Архивная находка идёт вперемешку с живыми или отдельной группой в конце
  выдачи. Решено: вперемешку, своим местом среди дней. Строка уже помечена
  клеймом «в архиве» и притемнена рамкой `chatOption`, второй группы ради
  одной находки заводить незачем, а порядок по свежести читается привычнее.
- Число скрытых считает клиент по своему набору или сервер в ответе списка.
  Решено: клиент, по уже загруженному `st.chats`. Сообщение стоит только в
  пустой выдаче без запроса (положение кнопки спрятало весь загруженный
  набор), а разговоры глубже окна клиенту в этот момент попросту не видны.
  Досчитывать их отдельным запросом ради одной строки текста не стоит того,
  и кнопка «показать раньше» рядом всё равно ведёт за окно.

## DoD

Запрос в поиске находит убранный в архив разговор при умолчании кнопки, тот же
поиск находит разговор старше трёх суток, а найденная архивная строка помечена
в выдаче.

## Ранг

`25+6+2+0+2 = 35`, в строке доски 71 по подтяжке от цели DK-713, P1.

- Серьёзность 25: разговор из списка не пропал, до него два нажатия кнопкой
  архива и «показать раньше», обход дешёвый и очевидный.
- Ценность 6: поиск разговора идёт в панели каждый день, и досягаемостью
  чата открывается вся цель DK-713.
- Неопределённость 2: место известно до строки (`chatArchShown` в
  static/app.js), открыт только вид выдачи.
- Поправка на баг 0: фильтр поставлен раньше поиска намеренно, задача меняет
  решение, а не чинит поломку.
- Рычаг 2: своя польза по теме чата, проверка остальных строк цели становится
  дешевле.

## Ход работы

- Вычитка: 4 правки, 1 пометка, 2026-09-02.
- Вычитка: 2 файла, 3 правки, 0 пометок, 2026-09-03.
- regcheck: `regcheck -C tools/dashboard --tests tools/dashboard/testdata/poc_chatfind.mjs
  -- node testdata/poc_chatfind.mjs static/app.js` ответил «тест краснеет на
  HEAD и проходит на текущем коде, регрессия закрыта», 2026-09-03. Утилита
  зовётся с `GOWORK=off`, иначе go подхватывает чужой `~/go.work`.
- regcheck: `regcheck -C tools/dashboard --tests tools/dashboard/chatfind_test.go,tools/dashboard/chat_smoke_test.go
  -- go test -run 'TestDashboardSmokeChatFindArchived|TestChatFindBeyondWindow' -count=1 .`
  ответил тем же вердиктом, 2026-09-03.
- Пока правился testdata/poc_chatarch.mjs (блок про поиск и положение кнопки),
  найдено: стенд не зовётся ни одним go test. Он падает уже на HEAD, до моей
  правки. Первый блок ждёт две строки списка при умолчании кнопки, а получает
  три. Адрес «board» ни с чем не совпал, и chatState завела текущим
  разговором первую строку набора. Пришпиленная строка остаётся в списке вне
  зависимости от архива. Находка не по предмету задачи, блок правки её не
  касается, и строка сценария на нём не стоит.
- Сценарий переписан по разбору ревью, 2026-09-03. Шаги 1-3 это тесты ветки,
  сами по себе про выкаченное они ничего не доказывают. Добавлены шаги 4-6
  против живой поднятой службы. Версия в `/healthz`, ручка списка на
  настоящем реестре, стенд против реально отданного `/assets/app.js`.
- Исключение: обкатка (шаг 4, живая проверка после выката, нужна настоящая
  поднятая служба, у обкатки в чистом дереве её нет).
- Исключение: обкатка (шаг 5, живая проверка после выката, нужны настоящая
  служба и настоящий реестр диалогов машины, у обкатки нет ни того, ни
  другого).
- Исключение: обкатка (шаг 6, живая проверка после выката, нужна настоящая
  служба, отдающая статику браузеру, у обкатки такой службы нет).
- Репетиция шагов 4-6 прошла на живой службе машины 2026-09-03. Это не факт
  официального прогона. Код там ещё до слияния. Шаг 4, `curl -s
  http://127.0.0.1:7112/healthz`, ответ `{"errors":[],"ok":true,"projects":4,
  "uptime_s":3181,"version":"dashboard v0.12.0-4424-g0c6be583-dirty
  (0c6be583649c)"}`, формат и поле `version` подтверждены. Шаг 5, та же связка
  входа и запроса списка, нашлись 77 архивных записей. Число само по себе про
  содержимое машины сейчас, не про правку. Шаг 6, стенд против реально
  отданного `/assets/app.js`. Ответ красный. Служба ещё раздаёт старый код,
  строка `поиск не нашёл архивную запись при спрятанном архиве`. Это и
  ожидалось до выката.
- Вычитка: 1 файл, 1 правка, 0 пометок, 2026-09-03.

## Ревью

- Вердикт: без замечаний. уровень 3 (триаж: правка трогает путь отображения списка чатов панели chatDropOpen/chatArchShown, от него зависит DoD всей панели и досягаемость чатов для сиблингов цели DK-713). Прочитан дифф целиком. Проверено: все четыре сочетания архив/живой x внутри окна/за окном достигаются поиском (стенд poc_chatfind.mjs и TestChatFindBeyondWindow), посторонний запрос не тащит архив (тест в стенде), соседнее поведение кнопки архива, клеймо и гашение архивной строки, выбор текущего разговора и пустая выдача не сломаны (чтение диффа и README). Свои прогоны: GOWORK=off go test -count=1 ./... в tools/dashboard (один неродственный сбой TestDashboardSmokeChatGroupOrder из-за перехода через полночь во время прогона, к правке не относится, чтение chatGroups подтверждает нетронутость), GOWORK=off go test -run 'TestDashboardSmokeChatFindArchived|TestChatFindBeyondWindow' -count=1 . зелёный, node testdata/poc_chatfind.mjs static/app.js зелёный. Оба regcheck из хода работы перепрогнаны самостоятельно и подтверждают красноту на main и зелень на текущем коде. Сценарий проверки: шаги 1-3 прогнаны, зелёные; шаги 4-6 корректно поставлены как обкатка недоступна тремя отдельными пометками с честными причинами, репетиция в «Ход работы» явно помечена как не факт официального прогона. Символы чистые (check-symbols.py по всем тронутым файлам). Дока (README) отражает новое поведение поиска.

## Сценарий проверки

1. Регрессия. Стенд поиска на настоящем static/app.js. Запрос находит
   архивную запись при умолчании кнопки, а пустая выдача называет число
   скрытых. Шаг гоняется на файле ветки, про выкаченное он ничего не
   говорит.
   ```sh
   cd tools/dashboard && node testdata/poc_chatfind.mjs static/app.js
   ```
   Ожидание: строка `ok:` и код возврата `0`.
2. Регрессия. Настоящий headless chrome бьёт по httptest-серверу самой
   ветки, не по выкаченной службе, и набирает запрос в настоящем поле поиска
   настоящего DOM.
   ```sh
   cd tools/dashboard && GOWORK=off go test -run TestDashboardSmokeChatFindArchived -count=1 .
   ```
   Ожидание: `ok` и код возврата `0`. Шаг требует установленного chrome, без
   него go помечает прогон пропуском.
3. Регрессия. Разговор старше окна доезжает до выдачи тем же запросом.
   ```sh
   cd tools/dashboard && GOWORK=off go test -run TestChatFindBeyondWindow -count=1 .
   ```
   Ожидание: `ok` и код возврата `0`.
4. Живая проверка после выката. Не рехёрсится, нужна настоящая машина с
   поднятой службой. Служба называет свежую версию.
   ```
   curl -s http://127.0.0.1:7112/healthz
   ```
   Ожидание: поле `ok` равно `true`. Короткий hash в поле `version` входит в
   диапазон коммитов раздела «Выкат» файла задачи (раздел появится после
   слияния) либо стоит позже последнего из них. Порт запасной, действующий
   называет `~/.devkit/dashboard.local`.
5. Живая проверка после выката. Не рехёрсится, нужны настоящая служба и
   настоящий реестр диалогов. Ручка списка, которой пользуется поиск при
   наборе текста (`chatLoadWindow(project, st, 0)` в static/app.js), отдаёт
   архивные записи живой машины.
   ```
   TOKEN=$(dashboard secret)
   curl -s -c /tmp/dk726.jar -X POST http://127.0.0.1:7112/api/login \
     -H 'Content-Type: application/json' -d "{\"token\":\"$TOKEN\"}"
   curl -s -b /tmp/dk726.jar \
     'http://127.0.0.1:7112/api/projects/devkit/chats?all=1&days=0' \
     | python3 -c 'import json,sys
   d = json.load(sys.stdin)
   print(len([c for c in d["chats"] if c.get("archived")]))'
   ```
   Ожидание: число больше нуля.
6. Живая проверка после выката. Не рехёрсится, нужна настоящая служба. Стенд
   гоняется не на исходнике из дерева, а на статике. Ту же статику службу
   спрашивают напрямую, ту, что реально отдана браузеру.
   ```
   TOKEN=$(dashboard secret)
   curl -s -c /tmp/dk726.jar -X POST http://127.0.0.1:7112/api/login \
     -H 'Content-Type: application/json' -d "{\"token\":\"$TOKEN\"}"
   curl -s -b /tmp/dk726.jar http://127.0.0.1:7112/assets/app.js -o /tmp/dk726-app.js
   cd "$(git rev-parse --show-toplevel)/tools/dashboard"
   node testdata/poc_chatfind.mjs /tmp/dk726-app.js
   ```
   Ожидание: строка `ok:` и код возврата `0`. До выката шаг красный по той же
   причине, что и шаг 1. Отданная браузеру статика ещё режет список раньше
   запроса.

## Выкат

- 2026-09-03 слито: 630f429f, 824ff9db, 73b840d0

## Проверка


- Обкатка: 2026-09-03 00:52, свежее дерево 269b2e7df634, сценарий 976882e5, временный HOME, утилит дерева 10, шагов 3, все зелёные.

Шаг 1 (зелёный):

```console
$ cd tools/dashboard && node testdata/poc_chatfind.mjs static/app.js
ok: поиск находит архивную запись при спрятанной кнопке, метит её клеймом «в архиве», и пустая выдача называет число скрытых архивом разговоров
```

Шаг 2 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test -run TestDashboardSmokeChatFindArchived -count=1 .
ok  	github.com/dronrider/devkit/tools/dashboard	0.767s
```

Шаг 3 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test -run TestChatFindBeyondWindow -count=1 .
ok  	github.com/dronrider/devkit/tools/dashboard	0.448s
```
