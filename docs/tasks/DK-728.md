# DK-728: Молчаливая смерть поднятой сессии видна в ленте разговора

Цель: [tasks/DK-713.md](tasks/DK-713.md)

## Что происходит

2026-09-02, разведка цели DK-713 чтением кода. Дашборд поднимает сессии
командой `tmux new-session -d` в пяти местах tools/dashboard/chats.go (новый
чат, резюм чата, реплика в незачатый разговор, продолжение работы по задаче,
новый чат задачи) и ещё раз в clientlogin.go. Удачей считается код возврата
tmux, а он говорит только о том, что сессия создана. Клиент, умерший через
секунду (нет входа, кончилась квота, не то дерево, чужие флаги), уносит сессию
с собой, и об этом не узнаёт никто. Ручка уже ответила `200`, в журнале стоит
«чат поднят».

Панель в это время говорит: «работа поднята, сессия N вот-вот назовётся в
реестре: разговор откроется сам, ждать нажатий не надо». Ожидание идёт
`chatSewLoop` пять минут шагом в две секунды, до него `chatWait` минуту, и
кончается всё молчанием, потому что цикл просто возвращает `false`. Слова на
экране остаются прежними, транскрипта у мёртвого спавна нет, а пустая запись
списка доживает до уборки часом (`chatBlankLife`).

Так же молчит и смерть уже начавшей ход сессии. Запись реестра стоит, лента
замирает на последней реплике, и человеку нечем отличить её от думающего
агента.

## Чего хотим

Смерть подъёма перестаёт быть немой. После запуска сервер убеждается, что
tmux-сессия жива и клиент в ней дошёл до реестра. Пропажа имени раньше срока
это событие с причиной, а причина берётся у самого терминала (`tmux
capture-pane`, дорога уже проложена в clientlogin.go). Событие доезжает до
панели. В ленте разговора встаёт строка о том, что сессия умерла через столько
секунд, с последними строками её терминала, и ожидание подъёма гаснет вместе с
ней. Смерть сессии, уже начавшей ход, говорит о себе тем же порядком, а не
тишиной в ленте.

## Границы

Подъём упавшей сессии заново это DK-510, здесь смерть только называется
человеку. Клиент, вставший на вопросе доверия каталогу или на входе, смертью не
считается. Он жив, и про его случай панель уже говорит словами. Разговор, чьё
имя tmux отдали другому, едет строкой DK-673.

## Развилки

- Смерть замеряет сторожок сервера или опрос панели, которая и так ждёт
  подъёма.
- Через сколько секунд молчания подъём считается мёртвым, чтобы не обгонять
  медленный старт клиента.
- Сколько строк терминала показывать человеку и чем защититься от секрета,
  попавшего в снимок панели.

## DoD

Подъём чата моделью, которой нет, кончается в ленте строкой с причиной и снятым
ожиданием, а не бесконечным «вот-вот назовётся»; смерть сессии после первого
хода видна в ленте тем же порядком.

## Ранг

`50+8+3+5+2 = 68`, в строке доски 71 по подтяжке от цели DK-713, P1.

- Серьёзность 50: человек поднял работу, она не идёт, а панель говорит, что
  вот-вот пойдёт; конвейер стоит вхолостую, и обход у человека только
  операционный, лезть в tmux руками с машины.
- Ценность 8: каждый подъём получает видимый исход, и виток цикла перестаёт
  ждать сессию, которой нет.
- Неопределённость 3: мест подъёма шесть, кто замеряет смерть и чем она
  отличается от клиента, вставшего на вопросе, решается по ходу.
- Поправка на баг +5: панель обещает подъём, которого не будет, это дефект, а
  не новая работа.
- Рычаг 2: своя польза по теме чата, разбор любой следующей поломки подъёма
  перестаёт идти вслепую.

## Ход работы

- Вычитка: 7 правок, 1 пометка, 2026-09-02.

## Сценарий проверки

1. Стенд с подменённым tmux: сессия создаётся и умирает сразу, лента разговора
   получает строку о смерти с хвостом терминала.
   ```sh
   cd tools/dashboard && GOWORK=off go test -run TestChatSpawnDeathSaid -count=1 .
   ```
   Ожидание: `ok` и код возврата `0`.
2. Живой подъём смертью не считается: сессия, которая называется в реестре с
   задержкой, доезжает до ленты как прежде.
   ```sh
   cd tools/dashboard && GOWORK=off go test -run TestChatSpawnSlowStartAlive -count=1 .
   ```
   Ожидание: `ok` и код возврата `0`.
3. Шаг после выката, не повтор тестов ветки. Сквозной прогон поднимает сервер с
   настоящими ручками, страницу открывает настоящий headless chrome, водитель
   заказывает чат клиентом, который падает сразу, и читает ленту через
   настоящий DOM: строка о смерти стоит, ожидание подъёма снято.
   ```sh
   cd tools/dashboard && GOWORK=off go test -run TestDashboardSmokeSpawnDeath -count=1 .
   ```
   Ожидание: `ok` и код возврата `0`. Шаг требует установленного chrome, без
   него go помечает прогон пропуском.
