# DK-740: Цикл цели останавливается молча, когда диспетчер кончает ход текстом

Цель: [tasks/DK-713.md](tasks/DK-713.md)

## Что происходит

Скилл goal-loop говорит прямо: отчёт витка хода не кончает, текст в чат без вызова инструмента останавливает цикл, и ход отдаётся человеку только на стоп-маркере. Правило верное, но держится оно на одной внимательности диспетчера, и этого мало. На цели DK-713 я оборвал цикл дважды за ночь: отчитался о сделанном и не взял следующую работу тем же ходом.

Ситуация снаружи выглядит одинаково при живой работе и при вставшем цикле: в чате лежит осмысленный отчёт, доска в порядке, прод цел. Человек замечает простой сам, спустя часы.

Сторожок devkitctl watch тут не помог. На парковке DK-727 уведомитель позвал один раз, в момент самой парковки, а дальше четыре с половиной часа тишины при цели в работе и незакрытых задачах в составе.

Разобрать надо, чем ловить обрыв. Похоже, у devkit есть машинный признак: цель стоит в работе, состав не закрыт, а последняя запись журнала цикла старше порога. Отсюда либо повторный зов сторожка, либо хук конца хода, который не даёт кончить молчанием, пока у цели есть готовая работа.

## Чего хотим

Сторожок меряет движение цикла двумя журналами и считает цикл живым по любому
из них. Первый это журнал цикла `.devkit/goal-<ID>.log`, куда строку хода пишет
каждый виток. Второй это журнал запусков `.devkit/log`, из которого выброшены
строки расписания. Разлив и снимок квоты гонит сам тик сторожка, подхват
катится хуком по часам сессии, и по этим строкам журнал выглядит живым при
мёртвом цикле.

Зов по вставшему циклу повторяется через тот же порог, пока цикл стоит, и со
второго раза называет свой номер. Отметка `stopped` перестаёт быть вечной
глушилкой и становится временем последнего зова, рядом с ней счёт зовов
`shouts`. Движение снимает обе.

## DoD

Оборванный цикл виден снаружи без человека: при цели в работе и незакрытом составе простой дольше порога поднимает зов уведомителя, и зов повторяется, а не звучит однажды. Проверяется прогоном с искусственно состаренной записью журнала цикла.

## Ранг

`50+9+2+5+4 = 70`, P1.

## Ход работы

- Из черновика DK-740 «Цикл цели останавливается молча, когда диспетчер кончает ход текстом»: записан 2026-09-03, оформлен 2026-09-03.
- Вычитка: 3 файла, 8 правок, 0 пометок, 2026-09-03.
- Краснота: `regcheck -C tools/devkitctl --tests tools/devkitctl/watch_test.py -- python3 watch_test.py`, «тест краснеет на HEAD и проходит на текущем коде», 2026-09-03.

## Сценарий проверки

1. Юниты сторожка. Тик собственного расписания за движение цикла не сходит,
   строка хода витка сходит, долгий виток с вызовами утилит зова не поднимает,
   а стоящий цикл получает повторный зов с номером.
   ```sh
   cd "$(git rev-parse --show-toplevel)/tools/devkitctl" && GOWORK=off python3 watch_test.py 2>&1 | tail -n 3
   ```
   Ожидание: строки `Ran 93 tests`, `OK` и код возврата 0.
2. Живой запуск на синтетическом доме. Стенд кладёт во временный каталог реестр
   целей, доску с целью в работе, состаренный журнал цикла и журнал запусков,
   полный свежих строк расписания, потом гоняет `devkitctl watch` тремя тиками
   и читает `notify.log` того же дома. Настоящий дом и настоящая доска не
   трогаются.
   ```sh
   cd "$(git rev-parse --show-toplevel)" && python3 tools/devkitctl/testdata/poc_goalwatch.py
   ```
   Ожидание: строка «poc_goalwatch: ok, тик 1 позвал, тик 2 смолчал до порога,
   тик 3 позвал 2-й раз; уведомитель отработал 2 раза» и код возврата 0.
3. Шаг идёт после выката, на поставленном бинаре. Сторожок по расписанию гоняет
   ту же команду, и молчание тика на живой машине это признак, что выкат доехал.
   Обкатка в свежем дереве поставленного бинаря не проверяет.
   ```
   devkitctl watch --idle 45 | tail -n 1
   ```
   Ожидание: строка вида «целей под надзором N, вставших 0» и код возврата 0.
   - Исключение: обкатка (шаг спрашивает поставленный бинарь, а его правка
     доезжает выкатом)
4. Живая проверка после выката, на настоящем доме. Первая команда показывает
   отметки сторожка в записи цели под надзором, вторая его собственный журнал.
   ```
   grep -H '' ~/.devkit/goals/*.watch
   tail -n 3 ~/.devkit/goal-watch.log
   ```
   Ожидание: у идущей цели в записи нет строк `stopped` и `shouts`, а последняя
   строка журнала сторожка содержит «вставших 0». Если цикл в этот момент стоит,
   в записи стоят обе строки, и повторный зов приходит баннером не чаще порога.
   - Исключение: обкатка (свежее дерево реестра целей машины не заводит)

## Проверка

- Обкатка: 2026-09-03 11:08, свежее дерево cb8947dde08b, сценарий 09178314, временный HOME, утилит дерева 10, шагов 2, все зелёные.

Шаг 1 (зелёный):

```console
$ cd "$(git rev-parse --show-toplevel)/tools/devkitctl" && GOWORK=off python3 watch_test.py 2>&1 | tail -n 3
Ran 93 tests in 3.337s

OK
```

Шаг 2 (зелёный):

```console
$ cd "$(git rev-parse --show-toplevel)" && python3 tools/devkitctl/testdata/poc_goalwatch.py
poc_goalwatch: ok, тик 1 позвал, тик 2 смолчал до порога, тик 3 позвал 2-й раз; уведомитель отработал 2 раза
```
