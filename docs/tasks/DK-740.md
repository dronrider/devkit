# DK-740: Цикл цели останавливается молча, когда диспетчер кончает ход текстом

Цель: [tasks/DK-713.md](tasks/DK-713.md)

## Что происходит

Скилл goal-loop говорит прямо: отчёт витка хода не кончает, текст в чат без вызова инструмента останавливает цикл, и ход отдаётся человеку только на стоп-маркере. Правило верное, но держится оно на одной внимательности диспетчера, и этого мало. На цели DK-713 я оборвал цикл дважды за ночь: отчитался о сделанном и не взял следующую работу тем же ходом.

Ситуация снаружи выглядит одинаково при живой работе и при вставшем цикле: в чате лежит осмысленный отчёт, доска в порядке, прод цел. Человек замечает простой сам, спустя часы.

Сторожок devkitctl watch тут не помог. На парковке DK-727 уведомитель позвал один раз, в момент самой парковки, а дальше четыре с половиной часа тишины при цели в работе и незакрытых задачах в составе.

Разобрать надо, чем ловить обрыв. Похоже, у devkit есть машинный признак: цель стоит в работе, состав не закрыт, а последняя запись журнала цикла старше порога. Отсюда либо повторный зов сторожка, либо хук конца хода, который не даёт кончить молчанием, пока у цели есть готовая работа.

## Чего хотим

Сторожок меряет движение цикла двумя журналами и считает цикл живым по любому
из них. Первый это журнал цикла `.devkit/goal-<ID>.log`, куда строку хода пишет
каждый виток. Второй это журнал запусков `.devkit/log`, из которого выброшены
строки расписания. Разлив и снимок квоты гонит сам тик сторожка, подхват
катится хуком по часам сессии, и по этим строкам журнал выглядит живым при
мёртвом цикле.

Зов по вставшему циклу повторяется через тот же порог, пока цикл стоит, и со
второго раза называет свой номер. Отметка `stopped` перестаёт быть вечной
глушилкой и становится временем последнего зова, рядом с ней счёт зовов
`shouts`. Движение снимает обе.

## Развилки

Чем сторожку отличить свой служебный вызов от боевого. Первая правка резала
строки журнала по паре «утилита, команда». Ревью нашло дыру. Флагов журнал не
хранит, `logCmd` берёт `args[0]`, и выкат поезда `shipctl ship <ID>` пишется той
же парой, что служебный `shipctl -C <корень> ship --drain`. То же у
`agentctl quota refresh` витка против `agentctl quota refresh --all --if-stale`
сторожка. Настоящее движение цикла пропадало из счёта, и на конце витка это
давало ложный зов.

Выбрано время. Каждый служебный вызов засекает своё окно и кладёт его строкой
«начало, конец, корень» в `~/.devkit/watch.ticks`. Строка журнала внутри окна
принадлежит сторожку. Окно длится секунды против пяти минут между тиками.
Знание тут точное, окно засёк сам сторожок. Соседних утилит правка не касается.

Второй круг ревью нашёл в окне две дыры, и обе оказались про одно. Окно писалось
одной строкой в конце прогона, поэтому упавший тик не оставлял его вовсе, а
строки, которые он уже успел положить в журнал запусков, сходили за движение.
И окно было одно на весь дом, поэтому минута, потраченная разливом одного
проекта, гасила боевой выкат соседнего. Обе закрыты переносом записи на
отдельный вызов. Окно пишется сразу как вызов вернулся, в том числе после
падения, и несёт корень, к которому вызов относится. Снимок квоты корня не
имеет и накрывает любой: он живёт на уровне машины.

Звёздочка снята тем же кругом. `agentctl` кладёт строку журнала в тот проект, из
которого запущен сторожок, и окно съёма квоты теперь называет этот проект.
Повисший до потолка съём соседей больше не задевает. Окна на весь дом в файле не
остаётся ни одного, а звёздочка в разборе понимается только у старых строк, до
их вымывания хвостом.

- Отвергнуто: держать одно окно на прогон и обернуть запись в `try/finally`.
  Падение так закрывается, а чужой корень нет: правая граница всё равно уезжает
  на всё время обхода. Убитый по `SIGKILL` прогон `finally` не исполняет.
- Отвергнуто: записывать окно открытым до вызова и закрывать после. Файл окон
  копил бы незакрытые строки после каждого падения, а незакрытое окно без
  правой границы режет всё, что легло позже.
- Отвергнуто: считать окно по журналу самого сторожка. Он пишет не каждый
  вызов, а только заметное, и по нему граница вызова не восстанавливается.

Потолок ожидания подпроцесса взят той же правкой. Без потолка сторожок висит
вместе с подпроцессом до убийства launchd: `git push` внутри shipctl на этой
машине уже вешался на неинтерактивном keychain.

Третий круг ревью нашёл в потолке две беды. `subprocess.run(timeout=...)` шлёт
SIGKILL, и разлив очереди под ним не успевает отметить провал на доске. Само
число в пятнадцать минут не опиралось ни на какой замер. Закрыто мягким снятием
и настройкой.

Снятие идёт двумя шагами: SIGTERM, грация, SIGKILL для не вышедшего. Грация
полминуты. Отметка провала это вызов `taskctl fail` и зов уведомителя, секунды,
и полминуты кроют их с запасом, оставаясь на два порядка ниже потолка.

Умолчание потолка 35 минут, правится ключом `timeout` в `~/.devkit/watch.local`
рядом с порогом простоя. Число взято из замера трёх точек. Настоящая команда
выката devkit (`devkitctl build` из `.devkit/deploy.local`) на тёплом кэше идёт
8,9 секунды. Самый тяжёлый прогон на этой машине, полный `parallel.py -j 4` под
нагрузкой, занял 7 минут 6 секунд. У самого shipctl есть предел на команду
выката, ключ `deploy_timeout` в `.devkit/deploy.local`, умолчание 30 минут.
Потолок сторожка обязан стоять выше этого предела. Иначе сторожок убивает
shipctl раньше, чем тот увидит своё время, упадёт по-своему и отметит провал.
Тридцать пять минут дают shipctl его тридцать плюс запас на слияние, теги и
перевод строк доски.

Тут же находка мимо задачи: обработчика SIGTERM у shipctl нет, и мягкое снятие
даёт ему выйти чисто только после того, как он такой обработчик заведёт. Пока
работает порядок пределов: свой предел выката shipctl видит первым и падает сам.
Отдельной строкой это стоит завести.

- Отвергнуто: оставить `subprocess.run(timeout=...)`. Он шлёт SIGKILL сразу, и
  разлив теряет отметку провала.
- Отвергнуто: считать потолок от порога простоя (например, порог минус минута).
  Числа про разное. Порог меряет молчание цикла, потолок терпение к
  подпроцессу, и связывать их значит ломать одно правкой другого.

- Отвергнуто: писать флаги вызова в журнал запусков. Это правка `logCmd` в
  шести утилитах, разъезд формата журнала с его читателями (`trackctl facts`,
  `devkitctl stats`) и полный вызов с путями в файле, который лежит в каждом
  проекте. Цена не по задаче.
- Отвергнуто: метить свои вызовы переменной окружения и не писать такую строку
  в журнал. Утилиты пишут строку сами, значит правка снова уезжает в шесть
  соседних утилит, а вызов из-под сторожка перестал бы попадать в журнал
  вовсе, и `devkitctl stats` потерял бы часть картины.
- Отвергнуто: угадывать тик по расписанию launchd (раз в пять минут от
  установки). Расписание сдвигается спящей машиной, ручным прогоном и правкой
  plist, и такое угадывание режет боевые строки ровно там, где сторожок и
  ошибался бы.
- Оставлено по паре: `catchup` обеих утилит. Его катает хук по часам сессии, а
  не тик сторожка, и у витка такой команды нет ни с какими флагами.

## DoD

Оборванный цикл виден снаружи без человека: при цели в работе и незакрытом составе простой дольше порога поднимает зов уведомителя, и зов повторяется, а не звучит однажды. Проверяется прогоном с искусственно состаренной записью журнала цикла.

## Ранг

`50+9+2+5+4 = 70`, P1.

## Ход работы

- Из черновика DK-740 «Цикл цели останавливается молча, когда диспетчер кончает ход текстом»: записан 2026-09-03, оформлен 2026-09-03.
- Вычитка: 3 файла, 8 правок, 0 пометок, 2026-09-03.
- Краснота: `regcheck -C tools/devkitctl --tests tools/devkitctl/watch_test.py -- python3 watch_test.py`, «тест краснеет на HEAD и проходит на текущем коде», 2026-09-03.
- Краснота третьего круга: `regcheck --base 2296d63b --tests tools/devkitctl/watch_test.py -C tools/devkitctl -- python3 -m unittest watch_test.SoftKillTest watch_test.ConfTest`, «тест краснеет на 2296d63b и проходит на текущем коде», 2026-09-03.
- Краснота второго круга: `regcheck --base 61cc1202 --tests tools/devkitctl/watch_test.py,tools/devkitctl/testdata/poc_goalwatch.py -C tools/devkitctl -- python3 -m unittest watch_test.TimedTest watch_test.LookTest.test_window_of_another_root_does_not_hide_movement watch_test.RunTest.test_dead_tick_keeps_windows_of_done_calls watch_test.RunTest.test_each_root_gets_its_own_window`, «тест краснеет на 61cc1202 и проходит на текущем коде», 2026-09-03.
- Краснота после ревью: `regcheck --base 70275cf4 --tests tools/devkitctl/watch_test.py -C tools/devkitctl -- python3 -m unittest watch_test.LookTest.test_real_ship_counts_as_movement watch_test.LookTest.test_real_quota_refresh_counts_as_movement`, «тест краснеет на 70275cf4 и проходит на текущем коде», 2026-09-03.
- Разработка: субагент opus/medium по вердикту pick (квота: week_all 22%, week_max 7% (панель разбивку не дала, цифра из кеша клиента возрастом 16ч 57м назад), снимок 39м назад, сдвига нет), 2026-09-03 10:51-11:09.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 23%, week_max 7% (панель разбивку не дала, цифра из кеша клиента возрастом 17ч 43м назад), снимок 11м назад, сдвига нет), 2026-09-03 11:09-13:05.

## Ревью

Уровень 3 до 70275cf: неопределённость 2 (вердикт pick), триаж: диффа ломает не чужой DoD, а надёжность самого сторожка devkitctl watch, от которого зависит незамеченный простой любой цели; критичные пути shipctl/taskctl/hooks дифф не трогает
- блокирует: фильтр IDLE_CALLS в last_run_stamp (tools/devkitctl/watch.py:116-117, 238-249) режет строки по паре (утилита, подкоманда) без учёта флагов, а журнал запусков и не хранит флаги (logCmd = args[0] в tools/shipctl/main.go:175, tools/agentctl/main.go:193). Значит настоящий 'shipctl ship <ID>' боевого выката (board-batch шаг 10, board-ship: 'shipctl ship везёт накопленное одним выкатом') логируется той же парой (shipctl, ship), что и служебный тик сторожка 'shipctl -C root ship --drain' (watch.py:725), и фильтруется точно так же. Так же 'agentctl quota refresh' живого гейта на протухшем снимке (kit/skills/goal-loop/SKILL.md:53) неотличим от снимка сторожка 'agentctl quota refresh --all --if-stale' (watch.py:685). Оба случая живого движения могут пропасть из last_run_stamp, а тестов, что настоящий ship/quota-вызов цикла всё же считается движением, в диффе нет (только обратное: test_watch_own_tick_is_not_movement, test_scheduled_catchup_is_not_movement). Раз движение оценивается максимумом трёх источников, шанс ложного простоя невелик, но он не нулевой и не проверен: конец витка, где из журнала запусков в окне простоя видны только реальные shipctl ship/agentctl quota refresh без свежей строки в goal-<ID>.log, даст ложный зов на живом цикле, то есть ровно тот шум, который отключает сторожок насовсем. Нужен либо признак, различающий --drain/--if-stale от боевого вызова (например, отдельная метка тика в самом сторожке вместо угадывания по чужому логу), либо тест, доказывающий, что боевой ship/quota не тонет: исправлено
- блокирует: write_tick (watch.py:249-260) стоит одной строкой в самом конце run() (watch.py:1064), после quota_snap, всего цикла по entries(home), wake/park_stale/close_agent и цикла drain/check_run по каждому корню. Ни try/finally, ни другой страховки нет. Один упавший или зависший тик (subprocess.run в drain/quota_snap без timeout, а git push внутри shipctl в этом проекте уже вешался на неинтерактивном keychain) теряет своё окно насовсем: собственные shipctl --drain/agentctl quota строки, которые он уже успел дописать в .devkit/log, ни один следующий тик своим окном не накроет и распознает их как реальное движение. Это тот же исход, что до фикса, только не всегда, а именно тогда, когда сторожку хуже всего. Теста на этот путь нет: исправлено
- блокирует: окно тика одно на весь run() и общее на все корни (ticks строится в run() один раз до цикла for path in entries(home) и передаётся в look() для каждой записи без разбора по root, watch.py:1004-1005, 1020). Правая граница закрытого окна это now + всё время работы run(), включая drain и check_run по каждому корню под надзором (watch.py:1064): если разлив хоть одного корня реально катит выкат минуту-другую, следующий тик запишет широкое окно на весь дом. Своим own_tick следующий тик проверяет по этому окну строки любого корня, и боевой shipctl ship/agentctl quota refresh другого, не связанного с медленным разливом, корня, случайно попавший в этот же промежуток по времени, будет принят за собственный тик и вычеркнут из движения. Это ровно тот ложный простой на живом цикле, которого боится задача. Теста на несколько корней под надзором с медленным разливом одного из них нет, только сквозной test_own_drain_does_not_look_like_movement на одном корне: исправлено
- блокирует: Timed.__call__ (watch.py:305-312) ставит subprocess-таймаут CALL_TIMEOUT=15 минут на каждый служебный вызов, в том числе drain() -> 'shipctl -C root ship --drain' (watch.py:783-784), который может катить настоящий выкат. По истечении таймаута Python сам убивает дочерний процесс через process.kill(), это SIGKILL, а не SIGTERM: проверил эмпирически (обработчик SIGTERM в дочернем процессе ни разу не получает сигнал при истечении timeout= у subprocess.run). Docstring drain() (watch.py:809-811) прямо возлагает сигнал провала деплоя на сам shipctl: 'уведомление на провал деплоя шлёт сам shipctl через признак провала и taskctl fail'. SIGKILL этому шлёт мимо: shipctl не успевает дописать признак провала и позвать taskctl fail, а сторожок на код 124 пишет только строку в свой журнал (log_line), в board/taskctl fail она не идёт. Значит настоящий выкат, который честно не уложился в 15 минут (или деплой, зависший так, что shipctl не успевает отреагировать на SIGTERM, которого тут и не бывает), будет прибит без обычного пути уведомления о провале, и никто не узнает того, что раньше узнавал. Число 15 минут не привязано ни к одному измеренному прогону деплоя в этом доме, и порог не настраивается (в отличие от idle из watch.local). Теста на последствия кода 124 для видимости провала (board/taskctl fail) нет, только на факт, что сам код 124 возвращается и окно записывается (TimedTest): исправлено
- Вердикт: без замечаний. круг 4 до eda339af: сверка решения человека (мягкое снятие плюс потолок в конфиг) с исполнением. Проверено: run_soft (SIGTERM, грация 30с, SIGKILL) корректна для обычного случая и для гонки сигнал/communicate по трём тестам SoftKillTest; порядок пределов 35м > deploy_timeout 30м у shipctl обоснован тремя измерениями (8.9с тёплый выкат, 7м06с тяжёлый parallel.py -j4, 30м предел shipctl) и разобран построчно по shipctl/ops.go, internal/deployconf, internal/gitrun; потолок читается из watch.local тем же приёмом, что idle (ConfTest, 4 теста); звёздочка квоты снята through here(), окно съёма квоты теперь несёт корень запуска сторожка, а не весь дом (test_sweep_takes_the_timeout_from_the_file и обновлённые RunTest). Полный watch_test.py 111 тестов зелёный, poc_goalwatch.py зелёный. regcheck --base 9d381faf на SoftKillTest+ConfTest подтвердил красноту на базе и зелень на текущем коде. Согласен с находкой мимо задачи (у shipctl нет обработчика SIGTERM, связка держится на порядке чисел, а не на устройстве) и с тем, что она едет отдельной строкой, не DK-740 (нашёл черновик DK-749, описание точное). Main зелёный, DK-747 слита. Строка уровня стоит первой непустой строкой раздела Ревью

## Сценарий проверки

1. Юниты сторожка. Вызов из окна собственного тика за движение цикла не сходит,
   боевой `shipctl ship` и `agentctl quota` вне окна сходят, окно соседнего
   корня движения не гасит, упавший тик окна сделанных вызовов оставляет,
   повисший вызов снимается потолком, строка хода витка сходит, долгий виток с
   вызовами утилит зова не поднимает, стоящий цикл получает повторный зов с
   номером, подпроцесс с обработчиком SIGTERM выходит своим кодом и успевает
   отметиться, глухой к SIGTERM снимается жёстко, а потолок читается из
   `watch.local` и без файла берётся умолчанием.
   ```sh
   cd "$(git rev-parse --show-toplevel)/tools/devkitctl" && GOWORK=off python3 watch_test.py 2>&1 | tail -n 3
   ```
   Ожидание: строки `Ran 111 tests`, `OK` и код возврата 0.
2. Живой запуск на синтетическом доме. Стенд кладёт во временный каталог реестр
   целей, доску с целью в работе, состаренный журнал цикла и журнал запусков,
   полный свежих строк расписания, потом гоняет `devkitctl watch` пятью тиками
   и читает `notify.log` того же дома. Настоящий дом и настоящая доска не
   трогаются.
   ```sh
   cd "$(git rev-parse --show-toplevel)" && python3 tools/devkitctl/testdata/poc_goalwatch.py
   ```
   Ожидание: строка «poc_goalwatch: ok, тик 1 позвал, тик 2 смолчал до порога,
   тик 3 позвал 2-й раз, тик 4 принял боевой выкат за движение, тик 5 не
   поддался окну чужого корня; уведомитель отработал 2 раза, окна своих вызовов
   записаны» и код возврата 0.
3. Шаг идёт после выката, на поставленном бинаре. Сторожок по расписанию гоняет
   ту же команду, и молчание тика на живой машине это признак, что выкат доехал.
   Обкатка в свежем дереве поставленного бинаря не проверяет.
   ```
   devkitctl watch --idle 45 | tail -n 1
   ```
   Ожидание: строка вида «целей под надзором N, вставших 0» и код возврата 0.
   - Исключение: обкатка (шаг спрашивает поставленный бинарь, а его правка
     доезжает выкатом)
4. Живая проверка после выката, на настоящем доме. Первая команда показывает
   отметки сторожка в записи цели под надзором, вторая его собственный журнал.
   ```
   grep -H '' ~/.devkit/goals/*.watch
   tail -n 3 ~/.devkit/goal-watch.log
   ```
   Ожидание: у идущей цели в записи нет строк `stopped` и `shouts`, а последняя
   строка журнала сторожка содержит «вставших 0». Если цикл в этот момент стоит,
   в записи стоят обе строки, и повторный зов приходит баннером не чаще порога.
   - Исключение: обкатка (свежее дерево реестра целей машины не заводит)

## Выкат

- 2026-09-03 слито: 6238e1c5, 3376b99b, 02f84695, 10beb42e, 939463a4, 9c8667a9, 4465248b, cbde45ea, acdd5e21, 07e814c4, 60f9ac0a
- smoke прогнан, 2026-09-03

## Проверка


- Обкатка: 2026-09-03 12:53, свежее дерево eda339aff2d0, сценарий 61aa319a, временный HOME, утилит дерева 10, шагов 2, все зелёные.

Шаг 1 (зелёный):

```console
$ cd "$(git rev-parse --show-toplevel)/tools/devkitctl" && GOWORK=off python3 watch_test.py 2>&1 | tail -n 3
Ran 111 tests in 4.910s

OK
```

Шаг 2 (зелёный):

```console
$ cd "$(git rev-parse --show-toplevel)" && python3 tools/devkitctl/testdata/poc_goalwatch.py
poc_goalwatch: ok, тик 1 позвал, тик 2 смолчал до порога, тик 3 позвал 2-й раз, тик 4 принял боевой выкат за движение, тик 5 не поддался окну чужого корня; уведомитель отработал 2 раза, окна своих вызовов записаны
```

- Агентская часть на выкаченном коде: 2026-09-03 13:08, main на 02f84695, шагов 4, все зелёные.

Шаг 1 (зелёный):

```console
$ cd "$(git rev-parse --show-toplevel)/tools/devkitctl" && GOWORK=off python3 watch_test.py 2>&1 | tail -n 3
Ran 111 tests in 5.808s

OK
```

Шаг 2 (зелёный):

```console
$ cd "$(git rev-parse --show-toplevel)" && python3 tools/devkitctl/testdata/poc_goalwatch.py
poc_goalwatch: ok, тик 1 позвал, тик 2 смолчал до порога, тик 3 позвал 2-й раз, тик 4 принял боевой выкат за движение, тик 5 не поддался окну чужого корня; уведомитель отработал 2 раза, окна своих вызовов записаны
```

Шаг 3 (зелёный, с оговоркой):

```console
$ devkitctl watch --idle 45 | tail -n 1
корень p641.AmJPIv: SC-001: подъём не нужен, выкат за пользователем (autonomous = false в .devkit/deploy.local): проверяющего поднимает человек
$ echo "exit=$?"
exit=0
```

Строка «целей под надзором N, вставших 0» на стдаут не попала: `log_line` на
watch.py:1177 пишет её только в файл журнала, а на стдаут (watch.py:1178-1179)
выводит лишь ветку «целей под надзором нет» при пустом реестре. Так было с
DK-153, правки DK-740 это место не касаются. Подстановка подтверждена шагом 4:
та же строка «целей под надзором 1, вставших 0» лежит в `goal-watch.log` этого
самого прогона.

Шаг 4 (зелёный):

```console
$ grep -H '' ~/.devkit/goals/*.watch
/Users/rider/.devkit/goals/DK-713-Users-rider-projects-devkit.watch:goal = DK-713
/Users/rider/.devkit/goals/DK-713-Users-rider-projects-devkit.watch:root = /Users/rider/projects/devkit
/Users/rider/.devkit/goals/DK-713-Users-rider-projects-devkit.watch:file = /Users/rider/projects/devkit/docs/tasks/DK-713.md
/Users/rider/.devkit/goals/DK-713-Users-rider-projects-devkit.watch:seen = 2026-09-03T01:10:16
$ tail -n 3 ~/.devkit/goal-watch.log
2026-09-03T13:06:35 целей под надзором 1, вставших 0
2026-09-03T13:06:45 корень board: разлив упал с кодом 1: ошибка: в рабочем дереве незакоммиченное, сначала закоммить: M docs/TASKS.md M docs/tasks/SC-001.md path: /private/tmp/claude-501/-Users-rider-projects-devkit/5782ec20-4d5c-488f-9fa0-6bf69cacf8c5/scratchpad/board/.devkit/cmdout/20260903T130645-git-status/out
2026-09-03T13:06:46 целей под надзором 1, вставших 0
```

В записи DK-713 нет строк `stopped` и `shouts`, последняя строка журнала
сторожка содержит «вставших 0».
