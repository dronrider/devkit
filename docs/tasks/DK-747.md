# DK-747: Круг самопроверки не знает ворота уровня ревью и валит прогон на main

Цель: [tasks/DK-713.md](tasks/DK-713.md)

## Что происходит

Свежий ворот слияния требует в разделе «Ревью» строку уровня, которую ставит taskctl review level. Круг самопроверки devkitctl гоняет задачу SC-001 обычным конвейером и до слияния доходит без этой строки, поэтому шаг «слияние с выкатом» падает, а вместе с ним три теста SelfcheckTest.

Падает это на чистом main, без всякой правки, и значит стоит у всех: пока прогон красный, ни одно слияние в devkit не проходит. Проверил отдельным деревом на main, воспроизводится.

Чинить надо круг, а не ворот: ворот правильный, а фикстура просто отстала от него на полчаса. Похоже, шагу круга не хватает вызова taskctl review level между ревью и слиянием, ровно как это теперь делает живой ревьювер.

## Чего хотим

## DoD

Прогон tools/devkitctl на чистом main зелёный: три теста SelfcheckTest (живой круг, коммит файла задачи, молчаливый выкат) проходят, а шаг слияния внутри круга проходит ворот уровня ревью. Проверяется прогоном suite девкита после выката.

## Ранг

`50+8+1+5+4 = 68`, P1.

## Ход работы

- Из черновика DK-747 «Круг самопроверки не знает ворота уровня ревью и валит прогон на main»: записан 2026-09-03, оформлен 2026-09-03.
- Разработка: sonnet/high по вердикту pick, 2026-09-03 11:00-11:35.
- Дыра нашлась в круге. `circle()` (tools/devkitctl/selfcheck.py) вела задачу SC-001 от ветки прямо к `shipctl merge`. Строку уровня ревью она не заводила. reviewLevelGate (tools/shipctl/advice.go) спрашивает эту строку в разделе «Ревью» docs/tasks/SC-001.md.
- Между шагами «обкатка сценария» и «слияние с выкатом» добавлен шаг «уровень ревью». Он зовёт `taskctl review level SC-001 0 "..."` в дереве задачи. Уровень нулевой, с причиной: синтетическая задача круга, критичности нет. Шкала уровней такой выбор позволяет (kit/skills/review/SKILL.md).
- Второй такой отставшей фикстуры не нашлось. Grep по дереву за вызовами `shipctl` `start`/`merge` вне самого shipctl и его тестов указывает только на selfcheck.py. Соседние стенды (devkitctl_test.py, obeycheck, скиллы) до `shipctl merge` не доезжают.
- Красноту показал `regcheck -C tools/devkitctl --tests tools/devkitctl/selfcheck_test.py -- python3 -m unittest selfcheck_test -v`. Тест красный на HEAD, до правки, и зелёный на текущем коде. Регрессия закрыта. До правки то же самое видно и вручную, через `git stash`. Три теста SelfcheckTest падают на строке «нет строки уровня ревью в разделе «Ревью» docs/tasks/SC-001.md».
- Полный прогон `python3 tools/devkitctl/parallel.py -j 4` (GOWORK=off) прошёл зелёным целиком, 18 из 18 компонентов за 5м12с. Второй ворот регрессия не задела. Вторую отставшую фикстуру такой прогон бы тоже поймал.

## Ревью

Уровень 3 до 9079e11: неопределённость 3 из вердикта pick (effort high, tier base): правка чинит ворот слияния, обязательный для всего проекта
- Вердикт: без замечаний. живой путь: суита tools/devkitctl/suite.py 721 тест зелёный (GOWORK=off), в т.ч. три ранее красных SelfcheckTest; regcheck -C tools/devkitctl --tests selfcheck_test.py подтвердил красноту на main и зелень на фиксе; порядок шагов круга и уборка места сверены по коду и по тесту test_live_circle_passes_and_cleans_up; грепом по вызовам shipctl start/merge вне tools/shipctl и docs/tasks/*-check.sh проверено, что второй отставшей фикстуры нет; сужение сценария до suite.py оправдано диапазоном диффа (только tools/devkitctl) и совпадает с DoD, полный parallel.py -j4 прогнан исполнителем отдельно

## Сценарий проверки

Агентский. Обкатка гоняет прогон в свежем дереве на HEAD с временным HOME.
Дыра сидела в сюите devkitctl (компонент `devkitctl` раннера parallel.py),
поэтому сценарий целится в её собственный раннер, а не в весь parallel.py:
рядом с ним стоят go:dashboard и hooks, а их таймингом бой отдельный (машина
под нагрузкой), к DK-747 они отношения не имеют.

1. Сюита devkitctl:

   ```sh
   GOWORK=off python3 tools/devkitctl/suite.py; echo "exit=$?"
   ```

   Ожидание: `exit=0` и строка `OK` в конце. Внутри сюиты идут все шесть
   тестов класса SelfcheckTest, в том числе три ранее красных:
   `test_live_circle_passes_and_cleans_up`, `test_board_step_commits_task_file`,
   `test_silent_deploy_fails_merge_step`. Стена в порядке двух-трёх минут.

## Проверка


- Обкатка: 2026-09-03 11:47, свежее дерево 0221ad634b75, сценарий 8fa4b171, временный HOME, утилит дерева 10, шагов 1, все зелёные.

Шаг 1 (зелёный):

```console
$ GOWORK=off python3 tools/devkitctl/suite.py; echo "exit=$?"
Ran 721 tests in 2m16s across 134 classes
OK
exit=0
```
