# DK-751: Модель из вердикта pick доезжает до субагента сессии glm-code

## Что происходит

agentctl pick в сессии glm-code выдаёт модели glm-именами. Это glm-5.3 и
glm-5.3-flash. Параметр model инструмента Agent принимает только алиасы первой
лестницы, sonnet, opus, haiku, fable.

Диспетчер переводил glm-5.3 в fable, набралось 8 вызовов. Транскрипты xr-proxy
лежат в сессиях 241619c6, 2304fe5d, 405d126a. Переменной
ANTHROPIC_DEFAULT_FABLE_MODEL у клиента glm-профиля нет. Алиас уходил на провод
как claude-fable-5(-1). Сервер z.ai обслуживал его glm-5.3-flash. Это видно по
полю model в subagents/agent-*.jsonl.

Проба 2026-09-03 подтверждает лекарство. С переменной
ANTHROPIC_DEFAULT_FABLE_MODEL=glm-5.3 клиент разворачивает fable в glm-5.3.
Подмена sonnet через ANTHROPIC_DEFAULT_SONNET_MODEL работает, в транскриптах
стоит resolvedModel glm-5.3.

Сверка груминга 2026-09-03 сошлась с текстом. В env каталога glm-профиля из
подстановок алиасов стоят OPUS, SONNET и HAIKU, переменной FABLE нет. Лестница
glm-code в agentctl harness держит mini на glm-5.3-flash, base на glm-5.3,
pro на opus.

Ревью заказывали на самой сильной модели. Все восемь исполняла лёгкая. Панель
и логи молчали о подмене.

## Чего хотим

Модель из первой строки вердикта pick доезжает до исполнителя на любой ступени.
В транскрипте субагента стоит эта же модель.

## Границы

- Сервер z.ai обслуживает незнакомый алиас claude-fable-5 лёгкой моделью, его
  поведение не чинится. Задача правит дорогу от вердикта до клиента.

## Развилки

Чинить инструкции диспетчерских скиллов, переменную ANTHROPIC_DEFAULT_FABLE_MODEL
в env профиля или обе меры разом. Правило для инструкций расшифровывается легко.
glm-5.3 значит вызов без параметра модели. glm-5.3-flash значит haiku. Чужой via
значит agentctl run. Отдельный вопрос, как разносятся установленные копии
скиллов.

## DoD

- Субагент в сессии glm-code работает на модели из первой строки вердикта
  pick. Поле model в subagents/agent-*.jsonl это glm-имя из вердикта.
- Выбранная мера описана в доке профиля второй подписки. Env профиля и
  установленные копии диспетчерских скиллов не расходятся в правиле про
  модели.

## Ранг

`50+8+3+5+4 = 70`, P1.

- Серьёзность 50: заказанное ревью на сильной модели молча исполняется лёгкой.
- Ценность 8: дорога модели из вердикта работает на каждом витке конвейера.
- Неопределённость 3: открыты выбор меры и разноска правки в копии скиллов.
- Поправка на баг +5: вердикт называет модель, до субагента доходит другая.
- Рычаг 4: дорога модели из вердикта держит каждую задачу доски.

## Ход работы

- Из черновика DK-751 «ревью, заказанное на fable, молча исполняется flash»: записан 2026-09-03, оформлен 2026-09-03.
- Разработка: подпроцесс claude-code:opus/high по вердикту pick (квота: ступень pro уезжает в claude-code, остаток чужой подписки мерить нечем, вердикт без корректора), 2026-09-03 14:05-14:06.
- Замечание ревью 1: диспетчер, отдельная находка пустого лёгкого якоря, докстринг и тест болванки без лёгкой модели, regcheck закрыт, 2026-09-03.

## Ревью

Уровень 3 до 861404b: неопределённость 1, дифф перекладывает общий код env-раскладки в tools/devkitctl, на нём дорога моделей всех профилей доски.

- не блокирует: пустой ANTHROPIC_SMALL_FAST_MODEL не поднимает подстановку haiku и находки не даёт (ключа нет в ALT_SUB_KEYS, а докстринг alias_folding ссылается на находку про пустые ключи, которой для него нет), mini-ступень уезжает голым алиасом и держится только на запасном поведении сервера; добавить ключ в проверку пустых или отдельной находкой, проверить на болванке без лёгкой модели: исправлено, отдельной находкой пустого лёгкого якоря с тестом на болванке без лёгкой модели
- мелочь, мёртвый параметр devkit у alias_folding, его не передаёт ни один вызов: исправлено, правку в ветке сделал ревьювер

## Сценарий проверки

1. Регрессия на слитом main. Модуль утилиты держит раскладку и сворачивание
   алиасов, тест болванки без лёгкой модели краснел на коде до правки, что
   подтвердил regcheck в ветке.
   ```sh
   cd "$(git rev-parse --show-toplevel)" && python3 tools/devkitctl/devkitctl_test.py
   ```
   Ожидание: строка `OK` и 147 прогнанных тестов, код возврата 0.
2. Доктор на живой машине бинарём из PATH. Машинный env второй подписки
   раскладан этой же правкой, и находок про подстановки быть не должно.
   ```
   devkitctl doctor | grep -c 'подстановк\|лёгкой модели'
   python3 -c 'import json,os; e=json.load(open(os.path.expanduser("~/.devkit/claude-glm/settings.json")))["env"]; [print(k,"=",e.get(k)) for k in ("ANTHROPIC_MODEL","ANTHROPIC_SMALL_FAST_MODEL","ANTHROPIC_DEFAULT_SONNET_MODEL","ANTHROPIC_DEFAULT_OPUS_MODEL","ANTHROPIC_DEFAULT_FABLE_MODEL","ANTHROPIC_DEFAULT_HAIKU_MODEL")]'
   ```
   Ожидание: счётчик 0, SONNET, OPUS и FABLE равны glm-5.3, SMALL_FAST и
   HAIKU равны glm-5.3-flash. Шаг живой, обкатка в чистом дереве его не
   берёт, поэтому блок без языка.
3. Живая проба дороги субагента. Сессия второй подписки поднимает субагента
   с моделью fable, боковой журнал сессии хранит поле модели его реплик.
   ```
   j=$(ls -t <каталог подписки>/projects/<проект>/subagents/*.jsonl | head -1)
   grep -o '"model":"[^"]*"' "$j" | sort | uniq -c
   ```
   Ожидание: свежий журнал субагента, поднятого с параметром fable, несёт
   "model":"glm-5.3" и не несёт "model":"glm-5.3-flash". Субагента поднимает
   диспетчер живой сессии подписки тем же вызовом, каким его звала проба
   исполнителя. До правки тот же вызов обслуживался лёгкой моделью. Шаг
   живой, блок без языка.

## Выкат

- 2026-09-03 слито: 187be0cb, cb364a8d, 9d9d7e50, 5f0537d6, 29878c1a, 2ef62f2c, 8082d7ba
