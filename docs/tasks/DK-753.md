# DK-753: Строка уровня ревью в корп-контуре несёт sha боковой доски, а не кода

## Что происходит

`taskctl review level` записал в файл задачи строку «Уровень 3 до a343fb4», и
a343fb4 это вершина репозитория доски. Дерево ревью при этом было клоном кода
с совсем другой вершиной. Поймано 2026-09-03 на задаче проекта cap_autotests,
где доска лежит боковой директорией корп-контура.

Sha в этой строке нужен второму кругу ревью, он строит от него дифф до текущего
HEAD кода. От вершины доски такой дифф не строится. Ревьювер второго круга тогда
либо перечитывает весь дифф ветки, либо берёт границу на глаз. В корп-контуре
доска отделена от кода всегда, и строка бесполезна на каждой задаче.

## Чего хотим

Строка уровня несёт вершину дерева, по которому шло ревью, и второй круг строит
от неё дифф.

## Границы

Домашний проект правка не трогает: там корень доски и дерево ревью это один
репозиторий, а у дерева задачи своя доска и своя вершина, и sha уже верен.
Вершину кода команда ищет только тогда, когда корень пришёл редиректом.

## Развилки

Откуда брать вершину кода, из привязки `tracker.local` по ключу repo, из
рабочей директории вызова или новым ключом команды?

Вершину даёт рабочая директория вызова. Ревьювер стоит в том дереве, по
которому читает дифф, другого свидетеля у команды нет. Флаг `-C` двигает
вместе с корнем и источник вершины, так что запись остаётся верной и на
линкованном дереве ветки, и во втором клоне. Ключ repo такого свидетеля не
заменяет, он называет один клон контура и на дереве ветки разойдётся с ним.
Новый ключ команды отвергнут. Его пришлось бы передавать в каждом вызове, а
забытый ключ вернул бы прежнюю ошибку молча, теперь ещё и с видом осознанного
выбора.

Привязка остаётся запасным ходом на один случай. Вызов из самой боковой доски
редиректа не видит, и вершину там даёт ключ repo. Сдвиг на несколько коммитов
дифф второго круга переживёт, а от вершины доски он не строится вовсе.
Директория без git и домашний проект остались прежними, корень доски лежит там
в одном репозитории с кодом.

## DoD

- `taskctl review level` в корп-контуре пишет в строку уровня вершину дерева
  кода, а не вершину боковой доски;
- шапка файла замечаний `<ID>.review.md` несёт тот же sha, потому что читает
  строку уровня;
- регрессионный тест падает на старом коде и проходит на исправленном,
  краснота сверена `regcheck`;
- выбранный источник вершины назван в README taskctl, в разделе про
  `review level`.

## Ранг

`25+4+3+5+2 = 39`, P2.

- Серьёзность 25: строка уровня в корп-контуре бесполезна на каждой задаче, но
  обход дешёвый и очевидный, второй круг берёт границу руками, как и вышло
  на чужом ревью 2026-09-05.
- Ценность 4: ревью идёт в корп-контуре регулярно, а не разово, хотя контур
  один.
- Неопределённость 3: подход выбран (строка несёт вершину дерева ревью), а
  источник вершины открыт тремя вариантами вопроса черновика.
- Поправка на баг +5: команда пишет заведомо неверные данные.
- Рычаг 2: дешевле становится ревью корп-контура, одна тема потока.

Цена S: правка в одну функцию плюс тест на готовом стенде `corpClone`.
Приёмка агентская, барьеров нет: и клон, и боковая доска поднимаются в
синтетическом стенде.

## Разбор 2026-09-05

Случай повторился на чужом ревью, и его записью стал черновик DK-801: в строке
уровня оказалась вершина боковой доски, и ревьювер правил sha на нужный руками.
Предмет тот же, поэтому DK-801 приписан сюда.

Проверено по коду. `headSha` (tools/taskctl/review.go:281) зовёт
`git rev-parse HEAD` от корня доски, а корень в корп-контуре приходит
редиректом `devkit.local` и указывает на боковую директорию (ops.go:33,
corp.go:23). Вершина кода в него не попадает ни при какой команде.

Шапка файла замечаний своего источника не имеет: `reviewHeadFromTask`
(tools/taskctl/reviewdraft.go:304) читает sha из той же строки уровня, а
`trackctl review` кладёт в файл задачи только ссылку на MR, без пути клона.
Значит «взять из шапки» это не источник, а тот же неверный sha вторым кругом.

Машинная опора у первого варианта уже есть: привязка `.devkit/tracker.local`
несёт ключ `repo` с путём клона, его разбирает `corpTrackerRepo`. Стенд для
теста тоже готов, `corpClone` в tools/taskctl/corp_test.go поднимает клон с
редиректом и боковую доску.

## Ход работы

- Из черновика DK-753 «Строка уровня ревью несёт sha доски, когда код в другом репозитории»: записан 2026-09-03, оформлен 2026-09-05.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 2% профицит, week_max 2% профицит (панель разбивку не дала, цифра из кеша клиента возрастом 2ч 18м назад), снимок 45м назад, протух, сдвига нет), 2026-09-05 13:08-13:09.
- Краснота: regcheck на tools/taskctl/reviewcorp_test.go, тесты падают на main
  и проходят на правке, 2026-09-06.
- Отработка ревью: правка foreign.md это одна фраза условия, порог
  prompt-test она не берёт, стенд не гонялся, 2026-09-06.

## Ревью

Уровень 3 до 948df77: неопределённость 3, тронут tools/taskctl из критичных путей

- [возврат: правила, 2026-09-06] не блокирует: относительный repo в tracker.local тестом не закрыт. TestReviewLevelCorpFromBoardDir кладёт в привязку абсолютный путь клона, а README trackctl показывает форму «repo = ../corp-repo», и ветка filepath.Join(root, repo) в reviewCodeDir мутацию переживёт. Живьём относительный путь прогнал, вершина выходит верной, дыра только в тесте. Исход: случай с относительным repo в тот же тест: исправлено, случай с относительным repo добавлен подтестом
- [возврат: постановка, 2026-09-06] не блокирует: у чужого ревью, ради которого сюда приписан DK-801, вершину даёт HEAD дерева вызова, а шаг 2 порядка в kit/skills/review/foreign.md зовёт review level сразу после trackctl review, когда ветка MR в дереве может быть не выкачена. Строка тогда несёт вершину main клона: дифф от неё хотя бы строится, но это снова не то дерево, о котором говорит DK-801. Условие «ветка MR выкачена в дереве вызова» не названо ни в README taskctl, ни в foreign.md. Исход: строка условия шагом 2 foreign.md либо отдельная задача: исправлено, условие про выкаченную ветку MR встало в README taskctl и в шаг 2 foreign.md
- Вердикт: без замечаний. круг 2: оба неблокирующих закрыты. Мутация reviewCodeDir (снял filepath.Join(root, repo)) валит подтест «относительный» и щадит «абсолютный», GOWORK=off go test пакета taskctl зелёный за 61s, check-symbols по четырём файлам чист, фразы README и foreign.md стоят по месту

## Сценарий проверки

Все шаги агентские, стенд синтетический. Обкатка гоняет каждую строку блока
отдельной командой и держит один временный HOME на прогон, поэтому стенд живёт
файлами под `$HOME/stand`, а не переменными. На коде до правки красен четвёртый
шаг: в строке уровня там вершина боковой доски.

1. Регрессионные тесты выбора дерева. Вершина берётся из рабочей директории
   вызова и из её поддиректории, при вызове из самой доски её даёт привязка,
   домашний проект остаётся при вершине своего репозитория, шапка файла
   замечаний идёт за строкой уровня.

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" test . -count=1 -run "TestReviewLevelCorp|TestReviewLevelHome|TestReviewDraftHeadFollowsLevel" -v
   ```

   Ожидание: строка `PASS`, пять пройденных тестов среди них
   `TestReviewLevelCorpTakesCodeHead`, `TestReviewLevelCorpFromBoardDir` и
   `TestReviewDraftHeadFollowsLevel`, код возврата 0.

2. Стенд корп-контура и утилита из дерева. Клон кода с редиректом
   `devkit.local`, боковая доска со своей историей и строкой XR-005 в работе.

   ```sh
   mkdir -p "$HOME/stand/proj" "$HOME/stand/proj-local/docs/tasks"
   git -C "$HOME/stand/proj" init -q -b main && git -C "$HOME/stand/proj" config user.email t@t && git -C "$HOME/stand/proj" config user.name t && git -C "$HOME/stand/proj" config devkit.local ../proj-local
   echo код > "$HOME/stand/proj/code.txt" && git -C "$HOME/stand/proj" add . && git -C "$HOME/stand/proj" commit -q -m код
   printf '# Стенд: доска (префикс XR)\n\n## In progress\n\n| ID | Задача | Тип | P | R | Цена | Ссылка |\n|--------|--------|-----|---|---|------|--------|\n| XR-005 | Задача в работе | task | P2 | 30 (25+2+1+0+2) | - | [tasks/XR-005.md](tasks/XR-005.md) |\n\n## Check (готово, ждёт проверки пользователем)\n\n| ID | Задача | Тип | P | R | Цена | Ссылка |\n|--------|--------|-----|---|---|------|--------|\n\n## Backlog\n\n| ID | Задача | Тип | P | R | Цена | Ссылка |\n|--------|--------|-----|---|---|------|--------|\n\n## Blocked\n\nНет.\n' > "$HOME/stand/proj-local/docs/TASKS.md"
   git -C "$HOME/stand/proj-local" init -q -b main && git -C "$HOME/stand/proj-local" config user.email t@t && git -C "$HOME/stand/proj-local" config user.name t && git -C "$HOME/stand/proj-local" add . && git -C "$HOME/stand/proj-local" commit -q -m доска
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" build -o "$HOME/taskctl" .
   ```

   Ожидание: команды молчат, код возврата 0 у каждой. Вершины клона и доски
   после этого разные, репозитория два.

3. Запись уровня и черновик замечания из дерева кода.

   ```sh
   "$HOME/taskctl" -C "$HOME/stand/proj" review level XR-005 2 "тронут разбор диффа"
   "$HOME/taskctl" -C "$HOME/stand/proj" review draft XR-005 "гонка в close"
   ```

   Ожидание: первая печатает `XR-005: уровень ревью 2 до <sha> записан, файл
   задачи создан`, вторая `XR-005: замечание 1 записано черновиком в
   docs/tasks/XR-005.review.md`, код возврата 0.

4. Сверка записанного sha с вершинами обоих репозиториев. Тут и виден предмет
   задачи.

   ```sh
   code=$(git -C "$HOME/stand/proj" rev-parse --short=7 HEAD); board=$(git -C "$HOME/stand/proj-local" rev-parse --short=7 HEAD); task="$HOME/stand/proj-local/docs/tasks/XR-005.md"; grep -q "Уровень 2 до $code:" "$task" && ! grep -q "$board" "$task" && grep -q "ревью до: $code" "${task%.md}.review.md" && echo "вершина кода $code, вершина доски $board, строка уровня и шапка несут код"
   ```

   Ожидание: строка `вершина кода <sha>, вершина доски <sha>, строка уровня и
   шапка несут код`, код возврата 0. Любое из трёх условий валит шаг на своём
   `grep`.

## Из черновика DK-801 «review level у чужого ревью пишет sha боковой доски, а не дерева MR» (записан 2026-09-05)

### Черновик

#### Ситуация

#### Осложнение

#### Вопрос

#### Гипотеза

### Ситуация

Строка уровня в файле замечаний несёт sha дерева, по которому шло ревью. headSha в tools/taskctl/review.go берёт git rev-parse HEAD от корня доски. У задачи доски корень и дерево ревью совпадают. У чужого ревью в корп-контуре корень это боковая доска ../<проект>-local со своим git, а дерево ревью это клон MR.

### Осложнение

Коллега получил в строке уровня HEAD боковой доски и правил sha на 32699e1 руками, 2026-09-05. Второй круг сверяет исправления диффом от sha из строки уровня, и с чужим sha он читает не тот дифф.

### Гипотеза

У строки с пометкой «Ревью <ключ>:» брать sha из дерева MR: ключом --tree у review level или из шапки файла замечаний, куда trackctl review кладёт ссылку на MR и клон. Тест на боковой доске с отдельным клоном.

## Проверка

- Обкатка: 2026-09-06 10:41, свежее дерево 8270423c1740, сценарий e34d6bec, временный HOME, утилит дерева 11, шагов 10, все зелёные.

Шаг 1 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" test . -count=1 -run "TestReviewLevelCorp|TestReviewLevelHome|TestReviewDraftHeadFollowsLevel" -v
=== RUN   TestReviewLevelCorpTakesCodeHead
--- PASS: TestReviewLevelCorpTakesCodeHead (1.28s)
=== RUN   TestReviewLevelCorpFromSubdir
--- PASS: TestReviewLevelCorpFromSubdir (0.82s)
=== RUN   TestReviewLevelCorpFromBoardDir
=== RUN   TestReviewLevelCorpFromBoardDir/абсолютный
=== RUN   TestReviewLevelCorpFromBoardDir/относительный
--- PASS: TestReviewLevelCorpFromBoardDir (0.94s)
    --- PASS: TestReviewLevelCorpFromBoardDir/абсолютный (0.45s)
    --- PASS: TestReviewLevelCorpFromBoardDir/относительный (0.48s)
=== RUN   TestReviewLevelHomeKeepsBoardHead
--- PASS: TestReviewLevelHomeKeepsBoardHead (0.36s)
=== RUN   TestReviewDraftHeadFollowsLevel
--- PASS: TestReviewDraftHeadFollowsLevel (0.51s)
PASS
ok  	github.com/dronrider/devkit/tools/taskctl	4.320s
```

Шаг 2 (зелёный):

```console
$ mkdir -p "$HOME/stand/proj" "$HOME/stand/proj-local/docs/tasks"

```

Шаг 3 (зелёный):

```console
$ git -C "$HOME/stand/proj" init -q -b main && git -C "$HOME/stand/proj" config user.email t@t && git -C "$HOME/stand/proj" config user.name t && git -C "$HOME/stand/proj" config devkit.local ../proj-local

```

Шаг 4 (зелёный):

```console
$ echo код > "$HOME/stand/proj/code.txt" && git -C "$HOME/stand/proj" add . && git -C "$HOME/stand/proj" commit -q -m код

```

Шаг 5 (зелёный):

```console
$ printf '# Стенд: доска (префикс XR)\n\n## In progress\n\n| ID | Задача | Тип | P | R | Цена | Ссылка |\n|--------|--------|-----|---|---|------|--------|\n| XR-005 | Задача в работе | task | P2 | 30 (25+2+1+0+2) | - | [tasks/XR-005.md](tasks/XR-005.md) |\n\n## Check (готово, ждёт проверки пользователем)\n\n| ID | Задача | Тип | P | R | Цена | Ссылка |\n|--------|--------|-----|---|---|------|--------|\n\n## Backlog\n\n| ID | Задача | Тип | P | R | Цена | Ссылка |\n|--------|--------|-----|---|---|------|--------|\n\n## Blocked\n\nНет.\n' > "$HOME/stand/proj-local/docs/TASKS.md"

```

Шаг 6 (зелёный):

```console
$ git -C "$HOME/stand/proj-local" init -q -b main && git -C "$HOME/stand/proj-local" config user.email t@t && git -C "$HOME/stand/proj-local" config user.name t && git -C "$HOME/stand/proj-local" add . && git -C "$HOME/stand/proj-local" commit -q -m доска

```

Шаг 7 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" build -o "$HOME/taskctl" .

```

Шаг 8 (зелёный):

```console
$ "$HOME/taskctl" -C "$HOME/stand/proj" review level XR-005 2 "тронут разбор диффа"
XR-005: уровень ревью 2 до b73ba26 записан, файл задачи создан
```

Шаг 9 (зелёный):

```console
$ "$HOME/taskctl" -C "$HOME/stand/proj" review draft XR-005 "гонка в close"
XR-005: замечание 1 записано черновиком в docs/tasks/XR-005.review.md
```

Шаг 10 (зелёный):

```console
$ code=$(git -C "$HOME/stand/proj" rev-parse --short=7 HEAD); board=$(git -C "$HOME/stand/proj-local" rev-parse --short=7 HEAD); task="$HOME/stand/proj-local/docs/tasks/XR-005.md"; grep -q "Уровень 2 до $code:" "$task" && ! grep -q "$board" "$task" && grep -q "ревью до: $code" "${task%.md}.review.md" && echo "вершина кода $code, вершина доски $board, строка уровня и шапка несут код"
вершина кода b73ba26, вершина доски 2cd4c04, строка уровня и шапка несут код
```
