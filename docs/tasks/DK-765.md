# DK-765: Живая голова конвейера встаёт на вопросе разрешения и молчит

Цель: [tasks/DK-713.md](tasks/DK-713.md)

## Что происходит

Конвейер DK-724, поднятый слиянием, простоял двенадцать минут на вопросе клиента «Do you want to proceed?» и никого не позвал. Команда была обычной, поиск по коду через cd и grep, а классификатор её не пропустил: путь после cd он не определяет, а поверх стоит запрет на чтение, и решение остаётся за человеком.

Права машинного контура тут ни при чём, их разложила DK-739. Это другой класс: команда законная, но её форма классификатору непонятна.

Хуже всего молчание. Сессия жива, окно на месте, смертью её никто не считает, отметки «ждёт» в журнале ходов нет, строки в .devkit/log нет, уведомитель не сработал. Снаружи вставший конвейер неотличим от работающего, и нашёлся он только тем, что прогон сценария полез смотреть tmux руками.

Разобрать надо оба конца. Первый: вопрос разрешения обязан становиться видимым, как стала видимой смерть сессии в DK-728, иначе живая голова меняет одну немую беду на другую. Второй: команды с cd в начале стоит либо готовить так, чтобы классификатор их понимал, либо звать из корня без cd.

## Чего хотим

Оболочка конвейера узнаёт вопрос сессии двумя журналами вместо одного. Первый
остался прежним, это отметка «ждёт» хука `turn-mark.py`. Второй это строка того
же повода в журнале уведомителя `~/.devkit/notify.log`, и держится он на другом
хуке. Вопрос, пришедший любым из двух, зовёт человека тем же зовом
`wait_human`: строка в журнал проекта, строка в ленту разговора, баннер.

Второй канал нужен. Первый молчит целым классом случаев. Отметки хода пишет
хук, а в настройки харнеса он ложится доктором. Сессия, поднятая до этого, не
пишет ни одной отметки за всю жизнь. Получас молчания стоял запасом на такую
тишину, а двенадцать минут простоя в него не попадают.

Один вопрос приходит обоими каналами разом, и повторный зов о нём придержан на
две минуты.

## DoD

Конвейер, ставший на вопросе разрешения, виден человеку: строка в ленте разговора и зов уведомителя, не позже минуты ожидания. Проверяется прогоном, где живой голове подсовывается команда, которую классификатор не пропускает.

## Приёмка

- вид: agent
- барьеров нет: живой клиент поднимается в tmux с машины агента, вопрос
  разрешения виден строкой журнала, зов человека тоже строкой журнала

## Ранг

`50+9+2+5+3 = 69`, P1.

## Ход работы

- Разработка: opus/medium по вердикту pick, 2026-09-03.
- Разбор живьём. Журнал отметок `~/.devkit/turns.log` заведён 18:37:01, а
  конвейер DK-724 встал на вопросе в 18:32. Настройки харнеса с хуком отметки
  записаны в 18:33:53, то есть после простоя. Уведомитель на том же событии
  сработал. Строка повода `permission_prompt` по сессии 4c676ae8 лежит в
  `~/.devkit/notify.log` временем 18:32:13.
- Проба живьём. Клиент, поднятый в tmux с командой `cd ~/projects/devkit && grep
  -rn ...`, встал на вопросе разрешения через десять секунд после начала хода.
  Обе строки, отметка и уведомление, легли в журналы одним временем 18:54:16.
  Событие `Notification` на запрос разрешения приходит, хук его пишет, а
  оболочка читает его тоже верно.
- Отработка ревью. Придержание повторного зова держится за время события из
  строки журнала, окно пять секунд. Конец и начало хода его снимают. Запасной
  канал читается раньше отметок. Конец хода в той же пачке обрывал разбор
  возвратом, и строка уведомителя доставалась следующему проходу.
- Из черновика DK-765 «Живая голова конвейера встаёт на вопросе разрешения и молчит»: записан 2026-09-03, оформлен 2026-09-03.
- Вычитка: 4 файла, 9 правок, 0 пометок, 2026-09-03.

## Ревью

Уровень 3 до 6a53453: неопределённость 2 от pick, триаж: дифф трогает сам путь DoD (обнаружение и зов на вопросе сессии), да; критичные пути и горячий путь нет

- блокирует: kit/skills/board-task/task-run.py, stuck() (около строки 540), STUCK_QUIET придерживает повторный зов по ключу why (одна из трёх причин STANDING), а не по конкретному вопросу. said_stuck не сбрасывается на возобновлении хода. Второй, действительно другой вопрос той же причины в пределах 120 с молча теряется навсегда: строка notify.log уже вычитана fresh() как увиденная, повторно stuck() её не увидит. Проверил мутацией: снятие проверки удваивает зов на одновременном вопросе (это ожидаемо), но обратный случай, разнесённый по времени, не покрыт ни одним тестом. Нужен ключ дедупа по факту самого вопроса (сессия+причина+окно вокруг конкретного срабатывания, либо сброс said_stuck на TURN_STARTED), а не глобальный по причине: исправлено
- Вердикт: без замечаний. круг 2 до badb0f75: fixed 1 сверено дифф+тестами (redness на 7af8f26a подтверждена изоляцией обоих новых тестов), TestLiveHead 17/17 зелёных, check-symbols чисто

## Сценарий проверки

1. Стенд живой головы. Вопрос, о котором есть только строка уведомителя и ни
   одной отметки хода, зовёт человека. Один вопрос, пришедший обоими каналами,
   зовёт его один раз, а второй вопрос той же причины зовёт заново, и в том же
   проходе, и в следующем.
   ```sh
   cd "$(git rev-parse --show-toplevel)" && python3 kit/skills/board-task/task_run_test.py TestLiveHead
   ```
   Ожидание: строки `Ran 17 tests`, `OK` и код возврата 0.
2. Живой клиент на команде, которую классификатор не пропускает. Окно tmux
   поднимает клиента с поиском по коду через `cd` и `grep`, и он встаёт на
   вопросе разрешения. Через полторы минуты журнал уведомителя показывает повод
   `permission_prompt` этой сессии, то есть канал, по которому оболочка зовёт
   человека, работает и без отметки хода. Окно снимается последней командой.
   ```
   tmux new-session -d -s dk765 -c "$(git rev-parse --show-toplevel)" claude 'Выполни ровно одну команду Bash: cd ~/projects/devkit && grep -rn ЛистПробы . Ничего больше не делай.'
   sleep 90
   grep -c permission_prompt ~/.devkit/notify.log
   tmux kill-session -t dk765
   ```
   Ожидание: третья команда печатает число на единицу больше, чем то же число до
   шага, и последние строки журнала называют сессию окна поводом
   `permission_prompt`. Код возврата 0 у всех четырёх команд.
   - Исключение: обкатка (шаг поднимает живого клиента и ждёт его вопроса,
     свежее дерево такого окна не заводит)
3. Конвейер целиком, на живой задаче. Кнопка «Выполнить» на экране дашборда
   поднимает оболочку, сессия встаёт на вопросе разрешения, и журнал проекта
   получает строку зова.
   ```
   grep 'ждёт человека' "$(git rev-parse --show-toplevel)/.devkit/log"
   ```
   Ожидание: строка вида «живая голова task-<ID> ждёт человека:
   permission_prompt, <ID>» временем не позже минуты от вопроса, и баннер
   уведомителя с тем же текстом на экране.
   - Исключение: обкатка (строка появляется только после того, как конвейер
     задачи встал на вопросе живьём)

## Выкат

- 2026-09-03 слито: 108aef7f, 2eb74785, d3175742, 368aaa42, 60755848, 83e71e34, ee222edc, a301f145, 2ec59c20, aa42b7ed

## Проверка


- Обкатка: 2026-09-03 19:42, свежее дерево 85d30230fabe, сценарий 73f9b97d, временный HOME, утилит дерева 10, шагов 1, все зелёные.

Шаг 1 (зелёный):

```console
$ cd "$(git rev-parse --show-toplevel)" && python3 kit/skills/board-task/task_run_test.py TestLiveHead
----------------------------------------------------------------------
Ran 17 tests in 43.959s

OK
```
