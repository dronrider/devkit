# DK-781: Разлив поезда проверяет состав до выката и доводит перевод повтором

## Что происходит

Разлив поезда 2026-09-03 около 22:10 с составом DK-758 и DK-770. Выкат прошёл, потом `taskctl move` отбил перевод по DK-770, у которой нет раздела «Сценарий проверки». Ship напечатал «выкат прошёл, но доска не переведена». Повторный `shipctl ship` ответил «поезд пуст: после точки последнего выката нет слитых задач». Обе строки остались в In progress, в «Выкате» их файлов только строка «слито», отметки выката нет.

Точка выката сдвинута, а строки не переведены, и никакая повторная команда их уже не подхватывает. Код на проде без Check и без сценария, очередь непроверенного выката про него не знает. Одна чужая задача без сценария роняет перевод всего состава. Переводить пришлось руками.

### Что нашёл разбор

Ворота перевода в Check живут в `taskctl` (`checkGate`, tools/taskctl/gate.go), а `ship` спрашивал их последним шагом разлива, когда деплой прошёл и тег `deployed` уже сдвинут (`cmdShip`, tools/shipctl/ops.go). Первый отказ `taskctl move` выходил из цикла перевода ошибкой, и доводить перевод было нечем. Состав поезда считается по коммитам после точки выката (`trainTasks`), а точка уехала за них, поэтому повторный `ship` честно отвечал «поезд пуст».

У слияния свой ворот сценария (`scenarioGate`), но DK-770 приехала на main прямым коммитом мимо shipctl и через него не проходила, а состав поезда её взял. Да и ворот слияния уже, чем ворота перевода. Обкатку агентского сценария и перебор обходов у mixed и user он не спрашивает вовсе.

## Чего хотим

Ship либо отказывает до выката, называя строку без сценария, либо повторный ship доводит перевод остальных строк. Ни одна слитая задача не остаётся на проде в In progress без следа выката.

## Развилки

Проверять состав перед выкатом теми же воротами, что и `move check`, чтобы отказ случился до сдвига точки. Либо не сдвигать точку, пока перевод не прошёл, и дать повторному `ship` довести перевод. Либо переводить состав построчно и оставлять в поезде только отбитую строку.

## DoD

- Состав поезда проверяется воротами `move check` до выката: строка без раздела «Сценарий проверки» или без перебора обходов называется в отказе, точка выката не двигается.
- Если перевод отбит после выката, повторный `shipctl ship` доводит перевод остальных строк, а отбитую называет и оставляет в поезде.
- Регрессионный тест воспроизводит разлив 2026-09-03: состав из строки со сценарием и строки без него, после ship ни одна слитая строка не остаётся в In progress без следа выката. Тест краснеет на старом коде по `regcheck`.
- README shipctl, раздел про поезд, описывает порядок: ворота состава, выкат, перевод, повтор.

## Ранг

`50+6+3+5+4 = 68`, P1.

## Ход работы

- Из черновика DK-781 «поезд сдвинул точку выката, а отбитые строки остались в In progress»: записан 2026-09-03, оформлен 2026-09-03.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 36%, week_max 18% (панель разбивку не дала, цифра из кеша клиента возрастом 4ч 37м назад), снимок 28м назад, сдвига нет), 2026-09-03 22:20-22:20.
- Разлив: ворота состава спрашиваются сухим прогоном `taskctl move --dry-run` до выката, перевод после выката идёт построчно, отбитая строка получает пометку в «Выкате» и доводится следующим `ship`.

## Сценарий проверки

Агентский, гоняется из дерева задачи.

1. `GOWORK=off go test ./...` в `tools/shipctl` и в `tools/taskctl`. Ждём `ok` в обоих пакетах.
2. `GOWORK=off regcheck -C tools/shipctl -- env GOWORK=off go test -run 'TestTrainGateBeforeDeploy|TestTrainRepeatFinishesRefusedMove' ./...`. Ждём «тест краснеет на HEAD и проходит на текущем коде» (после слияния база называется явно, `--base <коммит правки>^`).
3. `taskctl move <ID строки без раздела «Сценарий проверки»> check --dry-run`. Ждём отказ с названной строкой и причиной. Доска после сухого прогона не меняется, `git status` чистый.
4. `taskctl move DK-781 check --dry-run` на готовой строке. Ждём «ворота перевода в check пройдены, строка не двигалась».
5. `shipctl status` на живом проекте. Ждём прежний вывод, порядок отчёта правка не трогает.
