# DK-796: Калитка пуша не признаёт доску в подкаталоге корп-контура

## Что происходит

Замечено при подключении корп-проекта authn 2026-09-04. Боковые директории
контура лежат подкаталогами одного репозитория, `/Users/vea/projects/corp-local`
держит `authn`, `cap_autotests` и `cap_ci_templates`, доска каждого из них это
`<проект>/docs/TASKS.md`.

Калитку пуша считает `boardOnly` (`tools/shipctl/ops.go:1667`), и пути она
сверяет от корня репозитория, `f != "docs/TASKS.md"` и
`strings.HasPrefix(f, "docs/tasks/")`. Путь `authn/docs/TASKS.md` под них не
подходит. Отказ поймала попытка запушить коммит
`chore(authn): доска проекта с префиксом AU и привязкой к тикетам CAP`, тем же
порядком отобьётся и правка файла задачи в `cap_autotests/docs/tasks/CA-001.md`.

Правило «коммит доски пушится сразу и без просьбы» в контурной раскладке не
работает ни на одном коммите. Доска общая, и отставший remote оборачивается
конфликтами ровно там, где рук больше одной. Каждый пуш доски приходится
просить у пользователя, хотя remote тут личный приватный.

## Чего хотим

Коммит доски в контурной раскладке пушится сам, как в домашней. Тест на
диапазон с путями вида `<проект>/docs/TASKS.md` рядом с `pre_push_test.py`.

## Границы

Послабление включается признаком корп-контура, и домашние репозитории калитка
судит прежним правилом: стенд `tools/obeycheck/testdata/project/docs/TASKS.md`
за доску не сходит. Раскладку боковых директорий задача не трогает, чинится
только разбор путей на стороне ворот пуша.

## Развилки

Считать пути доски от корня репозитория или от корня боковой директории,
который знает редирект `devkit.local`? Второе требует передать калитке корень
доски, а `pre-push` зовёт `shipctl push --check-only` из корня репозитория,
где боковых директорий может быть несколько.

- «серьёзность»: насколько мешает: обход дешёвый (25) или болезненный (50)?
  - решает: человек
  - рекомендация: 50: в контурной раскладке правило пуша доски не работает ни на одном коммите, обход это просить человека каждый раз
  - вариант: 25, обход дешёвый: доска корп-проекта правится редко, пуш руками терпим, ранг 43 и полоса P2
  - решено человеком 2026-09-09: 50: в контурной раскладке правило пуша доски не работает ни на одном коммите, обход это просить человека каждый раз
- «область»: доской считается любой вложенный docs/TASKS.md в любом репозитории или только доска боковой директории контура?
  - решает: человек
  - рекомендация: послабление только боковой директории контура: в самом devkit лежит стенд tools/obeycheck/testdata/project/docs/TASKS.md, и правило по суффиксу пропустило бы его правку за доску
  - вариант: правило по суффиксу пути, одинаковое всем репозиториям: дешевле в коде, но правка стенда с доской внутри проекта сходит за доску
  - решено человеком 2026-09-09: послабление только боковой директории контура: в самом devkit лежит стенд tools/obeycheck/testdata/project/docs/TASKS.md, и правило по суффиксу пропустило бы его правку за доску
- «границы»: чинить только калитку shipctl или заодно boardOnlyFiles в taskctl (mainahead) и shell-ветку хука pre-push без shipctl в PATH?
  - решает: человек
  - рекомендация: чинить все три места одной задачей: разъехавшийся критерий доски вернёт тот же отказ с другой стороны
  - вариант: править один boardOnly в shipctl, остальные два места отдельными строками
  - решено человеком 2026-09-09: чинить все три места одной задачей: разъехавшийся критерий доски вернёт тот же отказ с другой стороны
- «механика»: как калитка узнаёт корень доски в репозитории с несколькими проектами?
  - решает: исполнитель
  - рекомендация: корень доски искать подъёмом от каталога правленого файла в диффе: pre-push зовётся из корня репозитория, где досок несколько, и один корень от cwd там не находится

## DoD

Коммит доски в контурной раскладке пушится сам, как в домашней. Калитка считает
путь доски от корня доски проекта, а не от корня репозитория. Включает
послабление признак корп-контура. Критерий доски лежит копиями в трёх местах, и
правятся все три: `boardOnly` в `tools/shipctl/ops.go`, `boardOnlyFiles` в
`tools/taskctl/mainahead.go`, shell-ветка `hooks/pre-push` на случай, когда
shipctl не в PATH. Тесты берут диапазон с путями вида `<проект>/docs/TASKS.md`.
Место им рядом с `hooks/pre_push_test.py` и `tools/shipctl/push_test.go`.
Краснота сверяется прогоном regcheck. Снаружи работа видна на временном
репозитории. Коммит доски подкаталога проходит `shipctl push --check-only`.
Правка стенда с доской внутри обычного проекта отбивается по-прежнему.

## Ранг

`50+7+2+5+3 = 67`, P1.

- Серьёзность 50. Правило «коммит доски пушится сразу» в контурной раскладке не
  работает ни на одном коммите. Обход тут один, просить пуш у человека каждый
  раз (ответ на развилку «серьёзность»).
- Ценность 7. Корп-работа перестаёт спотыкаться на каждом коммите доски.
  Отставший remote общей доски оборачивается конфликтами там, где рук больше
  одной.
- Неопределённость 2. Подход выбран ответом про область. Открытой осталась одна
  деталь, как калитка узнаёт корень доски при вызове из корня репозитория с
  несколькими проектами.
- Поправка на баг 5. Раскладку с доской в подкаталоге кладёт само подключение, а
  калитка за ней не поспевает.
- Рычаг 3. Ручной пуш доски уходит во всех проектах контура. Заодно сходится
  критерий доски, разъехавшийся по трём местам.

## Границы

Послабление включается признаком корп-контура. Домашние репозитории калитка
судит прежним правилом, и стенд `tools/obeycheck/testdata/project/docs/TASKS.md`
за доску не сходит. Раскладку боковых директорий задача не трогает. Чинится
разбор путей на стороне ворот пуша.

## Развилки

Считать пути доски от корня репозитория или от корня боковой директории,
который знает редирект `devkit.local`? Второе требует передать калитке корень
доски, а `pre-push` зовёт `shipctl push --check-only` из корня репозитория,
где боковых директорий может быть несколько.

- «серьёзность»: насколько мешает: обход дешёвый (25) или болезненный (50)?
  - решает: человек
  - рекомендация: 50: в контурной раскладке правило пуша доски не работает ни на одном коммите, обход это просить человека каждый раз
  - вариант: 25, обход дешёвый: доска корп-проекта правится редко, пуш руками терпим, ранг 43 и полоса P2
  - решено человеком 2026-09-09: 50: в контурной раскладке правило пуша доски не работает ни на одном коммите, обход это просить человека каждый раз
- «область»: доской считается любой вложенный docs/TASKS.md в любом репозитории или только доска боковой директории контура?
  - решает: человек
  - рекомендация: послабление только боковой директории контура: в самом devkit лежит стенд tools/obeycheck/testdata/project/docs/TASKS.md, и правило по суффиксу пропустило бы его правку за доску
  - вариант: правило по суффиксу пути, одинаковое всем репозиториям: дешевле в коде, но правка стенда с доской внутри проекта сходит за доску
  - решено человеком 2026-09-09: послабление только боковой директории контура: в самом devkit лежит стенд tools/obeycheck/testdata/project/docs/TASKS.md, и правило по суффиксу пропустило бы его правку за доску
- «границы»: чинить только калитку shipctl или заодно boardOnlyFiles в taskctl (mainahead) и shell-ветку хука pre-push без shipctl в PATH?
  - решает: человек
  - рекомендация: чинить все три места одной задачей: разъехавшийся критерий доски вернёт тот же отказ с другой стороны
  - вариант: править один boardOnly в shipctl, остальные два места отдельными строками
  - решено человеком 2026-09-09: чинить все три места одной задачей: разъехавшийся критерий доски вернёт тот же отказ с другой стороны
- «механика»: как калитка узнаёт корень доски в репозитории с несколькими проектами?
  - решает: исполнитель
  - рекомендация: корень доски искать подъёмом от каталога правленого файла в диффе: pre-push зовётся из корня репозитория, где досок несколько, и один корень от cwd там не находится

## DoD

Коммит доски в контурной раскладке пушится сам, как в домашней: калитка считает
путь доски от корня доски проекта, а не от корня репозитория, и послабление это
включает признаком корп-контура. Правится оно в трёх местах, где критерий доски
лежит копиями: `boardOnly` в `tools/shipctl/ops.go`, `boardOnlyFiles` в
`tools/taskctl/mainahead.go` и shell-ветка `hooks/pre-push` на случай, когда
shipctl не в PATH. Тесты берут диапазон с путями вида `<проект>/docs/TASKS.md`
рядом с `hooks/pre_push_test.py` и `tools/shipctl/push_test.go`, краснота
сверяется прогоном regcheck. Снаружи это видно прогоном на временном
репозитории: коммит доски подкаталога проходит `shipctl push --check-only`, а
правка стенда с доской внутри обычного проекта отбивается по-прежнему.

## Ранг

`50+7+2+5+3 = 67`, P1.

`50+7+2+5+3 = 67`, P1.

- Серьёзность 50: правило «коммит доски пушится сразу» в контурной раскладке не
  работает ни на одном коммите, и обход тут это просить пуш у человека каждый
  раз (ответ на развилку «серьёзность»).
- Ценность 7: корп-работа перестаёт спотыкаться на каждом коммите доски, а
  отставший remote общей доски оборачивается конфликтами ровно там, где рук
  больше одной.
- Неопределённость 2: подход выбран ответом про область, открытой осталась одна
  деталь, как калитка узнаёт корень доски, когда её зовут из корня репозитория
  с несколькими проектами.
- Поправка на баг 5: раскладку с доской в подкаталоге подключение делает само
  («доска ложится подкаталогом репозитория, он один на весь контур»), а калитка
  за ней не поспевает.
- Рычаг 3: снимает ручной пуш доски во всех проектах контура и сводит критерий
  доски, разъехавшийся по трём местам.

## Ход работы

- Из черновика DK-796 «в общем на контур репозитории доски пуш доски отбивается всегда»: записан 2026-09-04, оформлен 2026-09-09.
