# DK-803: Сторожок печатает на стыке фаз остаток окна и действие

Цель: [tasks/DK-565.md](tasks/DK-565.md)

## Что происходит

Переходы задачи между фазами видны по `taskctl move` и по журналу сессии.
Объём контекста сессии тоже виден, `devkitctl weigh` разбирает журналы по
факту. Что делать на стыке фазы, сессия решает по памяти. По журналам видно,
что дефолт это дотянуть до автосжатия.

Хвост задачи попадает на самый занятый контекст. Закрытие, коммит доски и
вычитка идут последними, и там же чаще всего теряются шаги. Субагент под
следующую фазу поднимается поздно, вводная ему пишется хуже.

Правило в тексте держалось бы на памяти той же сессии. Ни один хук в `hooks/`
сейчас не читает usage журнала и не привязан к `taskctl move`.

## Чего хотим

Стык фаз считает сторожок. Хук берёт остаток окна из журнала сессии и оценку
следующей фазы по прошлым задачам той же цены. Малый остаток даёт строку в
ленту сессии с остатком и с действием. Действий четыре, и порядок между ними
называет ядро правил: продолжить, поднять субагента со свежим окном, передать
работу файлом задачи, сжать контекст. Сжатие стоит последним.

## Развилки

Постановка оставила исполнителю четыре развилки. Я решил их по ходу
разработки.

Источник числа. Взят журнал сессии. Событие хука несёт `transcript_path`. У
последнего хода ассистента в нём лежит `usage`. Сумма свежего входа с
записью и чтением кеша даёт размер контекста на этот момент. Счётчик харнеса
отвергнут. В событие хука он не приходит, статусная строка живёт другим
контуром, а `devkitctl weigh` меряет резидент печатным прогоном.

Адресат подсказки. Сама сессия, через канал добавки контекста
`hookio.Context` на `PostToolUse`. Сессия выбирает шаг на стыке, и подсказка
нужна ей в тот же ход. Человек через дашборд как единственный адресат
отвергнут. Он видит строку с опозданием и не успевает вмешаться в ход. Запись
в `~/.devkit/phase.log` ложится на каждом переходе, дашборд её подберёт
отдельной задачей.

Порог остатка. Порог это третья четверть роста контекста за ту же фазу у
прошлых задач той же цены, оценка в подсказке это медиана. Остаток считается
до черты харнеса, десятая часть окна остаётся ему на ответ и автосжатие.
Фиксированный процент окна как единственный порог отвергнут. У разработки и
проверки разный размер, и единый процент либо не срабатывает на разработке,
либо срабатывает слишком часто на проверке. Доля окна осталась умолчанием на
пустой журнал (0,35 и 0,15), подсказка называет её умолчанием.

Оценка следующей фазы. Рост берётся из журнала самого сторожка. Строка
ложится на переход, рост это разница занятости окна между двумя переходами
одной задачи в одной сессии. Чтение транскриптов прошлых задач по реестру
чатов на каждом переходе отвергнуто. У субагента транскрипт хозяина, записи
этапов `agentctl` держат время без токенов, а разбор десятка журналов на
каждом `taskctl move` лишний. Оговорка записана в README хуков. Фаза,
отданная субагенту, растит окно диспетчера только отчётом, и медиана по
журналу это смесь обоих режимов.

Стык без хука. Переходы «постановка сдана» и «код готов» командой доски не
отмечены. Там сторожок молчит, и абзац ядра держит ряд действий. Стенд гонял
такой стык.

## DoD

- Хук после `taskctl move DK-X check` на подставном журнале с занятым окном
  печатает подсказку с остатком и с действием.
- На пустом журнале хук молчит.
- Порог остатка и оценка следующей фазы считаются из данных прошлых задач той
  же цены.
- Ряд из четырёх действий записан в ядре правил одним абзацем.
- Тесты по стандарту, сценарий стенда на абзац ядра по скиллу prompt-test.

## Ранг

`50+8+3+0+3 = 64`, P1.

- Серьёзность 50: работа на исходе окна теряет шаги, обход это ручное слежение
  за контекстом.
- Ценность 8: прямой вклад в вектор цели, стык фаз перестаёт держаться на
  памяти.
- Неопределённость 3: открыты источник числа (журнал против счётчика харнеса),
  адресат подсказки и порог.
- Поправка на баг 0.
- Рычаг 3: подсказку получают все задачи доски, не только задачи цели.

## Ход работы

- Вычитка: 0 правок, 0 пометок, 2026-09-05.
- Разработка: субагент fable/high по вердикту pick (маппинг opus, корректор: профицит week_all, week_max; квота: week_all 11% профицит, week_max 2% профицит (панель разбивку не дала, цифра из кеша клиента возрастом 5ч 23м назад), снимок 1м назад), 2026-09-05 15:29-15:30.
- Ядро правил ужато под порог 5 500 символов из `devkitctl doctor`. Пункт про `cd` остался без примеров, его разбор перенесён в RULES.md, раздел «Чтение контекста», из «Тестов» и «Мимикрии» сняты пояснительные скобки. Итог 5 500 символов.
- Резидент devkit на main над потолком 6 500 токенов (6 598). Карман ядра в порог влез, лишнее лежит в листинге скиллов, это другая задача.
- Вычитка: 5 файлов, 17 правок, 1 пометка, 2026-09-05.
- Отработка ревью: ветка перебазирована на main 45cb35a7, ядро 5 442 символа с возвращённым путём check-symbols.py, краснота фикстуры архива подтверждена `regcheck -- python3 -m unittest phase_budget_test`, 2026-09-05.

## Ревью

Уровень 3 до 73d738f: неопределённость 3, тронут hooks из critical_paths, хук висит на каждом PostToolUse Bash

- [возврат: реализация, 2026-09-05] блокирует: на `taskctl close` цена читается из архива. У живого docs/TASKS-archive.md шесть колонок, цены среди них нет. В шестую попадает ссылка на файл задачи. Запись с полем «закрыл проверка» уходит с ценой вида [tasks/archive/2026/DK-99.md](...). По цене M её потом никто не найдёт. Порог фазы «проверка» навсегда сидит на умолчании 0,15 окна. Это тот самый стык, который называет DoD. Воспроизвёл событием с `taskctl close` на дереве с живой шапкой архива, поле «цена» видно в ~/.devkit/phase.log.: исправлено, цена на закрытии берётся из прошлой записи той же задачи в журнале, колонку цены называет шапка таблицы, у архива её нет и строка даёт «-»
- [возврат: правила, 2026-09-05] блокирует: фикстура ARCHIVE в hooks/phase_budget_test.py выдумана по форме доски. В ней семь колонок и цена в шестой. У живого архива колонок шесть, цены нет вовсе. Поэтому test_archive_row_is_closed зелёный на сломанном коде. Ожидание надо снимать с docs/TASKS-archive.md, как снят транскрипт.: исправлено, фикстура ARCHIVE снята с живого docs/TASKS-archive.md, regcheck подтвердил красноту теста на HEAD
- [возврат: правила, 2026-09-05] не блокирует: в ядре, раздел «Символы», команда ужата до `hooks/check-symbols.py <файлы>`. Ядро резидентно в каждом проекте. Относительный путь там ни во что не разрешается. Пункт как раз про проекты, где хуки devkit не подключены. Прежняя строка давала python3 ~/projects/devkit/hooks/check-symbols.py. Абсолютный путь надо вернуть. Заодно вёрстка. Строка 6 ядра вышла в 95 символов, код-спан разорван переносом, абзац «Мимикрия» свёрстан рвано.: исправлено, в ядро возвращён путь python3 ~/projects/devkit/hooks/check-symbols.py, строка 6 и абзац «Мимикрия» свёрстаны заново
- [возврат: правила, 2026-09-05] не блокирует: третья четверть в тестах неотличима от максимума. У TestEstimate четыре значения. На четырёх (3*n)//4 равно n-1. Мутация upper = values[-1] проходит весь набор зелёной. Порог это главная формула сторожка. Нужен набор из пяти значений.: исправлено, в TestEstimate пять значений, третья четверть 40 тыс при максимуме 70 тыс
- [возврат: реализация, 2026-09-05] не блокирует: сжатие между двумя переходами одной сессии даёт used меньше before. max(used - before, 0) пишет «рост 0». Ноль тут не замер, а выдумка. Он тянет медиану и порог вниз. Вниз уезжают самые длинные фазы, они же и доезжают до автосжатия. Лучше писать «рост -», как при смене сессии. Такая запись в оценку не попадёт.: исправлено, сжатие между переходами пишет «рост -», в оценку такая запись не идёт, тест добавлен
- [возврат: правила, 2026-09-05] не блокирует: ядро вышло ровно в 5500 символов при пороге 5500 из rules_test.test_core_budgets. Запаса нет. Следующая правка ядра свалит тест раньше, чем её посмотрят по существу. Стоит вернуть полсотни символов. Часть ужатого всё равно просится обратно, замечание 3.: исправлено, ядро 5 442 символа, запас 58: ужаты интро про роль, «в новое окно» в ряду и скобка корп-контура
- [возврат: правила, 2026-09-05] не блокирует: проверка сценария 23-phase-junction засчитывает любую правку docs/tasks/OB-001.md против сида. Сессия могла написать доку сама в том же окне и дописать строку в «Ход работы». Она выйдет зелёной, хотя выбрала «продолжить». Критерий стоит сузить до записи именно о передаче.: исправлено, передача засчитывается только при нетронутом README против сида, правленый README с правленым файлом задачи красный
- [возврат: правила, 2026-09-05] не блокирует: ветка отстала от main. Merge-base e4596435, main 45cb35a7. Дерево несёт docs/map.md с конфликтными маркерами в первых девяти строках. В main этот файл уже починен. В дифф задачи он не попал. До слияния нужен ребейз.: исправлено, ветка перебазирована на main 45cb35a7, docs/map.md без маркеров
- [возврат: правила, 2026-09-05] блокирует: шаг 4 сценария проверки красный. Команда grep -c 'сжать контекст' по ядру даёт 0, а ожидание в сценарии 1. После вычитки ряд разошёлся на отдельные предложения, и фраза стала «Сжать контекст» с большой буквы. Шаг сценария под новую редакцию не переписали. Прогонять его будет проверяющий, и он упрётся в красное на живом ядре.: исправлено, шаг 4 ищет «Сжать контекст, когда не подошло остальное» с ожиданием 1
- [возврат: правила, 2026-09-05] блокирует: шаг 4 сценария проверки снова красный. Новая команда grep -c 'Сжать контекст, когда не подошло остальное' по ядру даёт 0. Фраза разорвана переносом между строками 29 и 30 ядра, а grep построчный. Прогнал руками на живом файле. Либо искать кусок в одну строку, либо сшивать строки перед поиском. Заодно строка 110 ядра вышла в 95 символов после правки корп-контура.: исправлено, шаг 4 ищет «Сжать контекст», кусок влезает в одну строку ядра, ожидание 1 проверено grep; строка 110 переверстана, строк длиннее 80 символов в ядре нет
- [возврат: правила, 2026-09-05] блокирует: след стенда на редакции, которая едет в main, порог просадки не берёт. Таблица 2/3 против 0/3 при k=3 даёт по точному критерию Фишера 0,20. Порог из DK-805.3 это не чаще раза из двадцати. При k=3 в порог укладывается только 3/3 против 0/3, и то ровно в 0,05. Прежняя редакция давала 3/3, нынешняя 2/3, то есть решение ослабло. Абзац сидит в резиденте каждой сессии, и держится он на этом следе. Нужен прогон побольше на этой же редакции: при k=5 порог берёт 4/5 против 0/5 и лучше.: исправлено, k=5 mini дал 3/5 против 1/5 (Фишер 0,26), k=5 base 5/5 против 3/5 (0,22), объявленный заранее решающий прогон k=10 mini дал 8/10 против 2/10, Фишер 0,012, порог взят; все таблицы в «Проверке»
- [возврат: правила, 2026-09-05] не блокирует: ярус стенда в следе не записан ни у одного из трёх прогонов. Прогоны в файле задачи сравниваются между собой. Без яруса непонятно, чем объясняется просадка с 3/3 до 2/3. Текстом ядра, сужением критерия по замечанию 7 или другим ярусом. Ярус стоит вписать в каждую строку следа.: исправлено, ярус вписан в каждую строку следа, у прежних прогонов это умолчание mini
- [возврат: реализация, 2026-09-05] не блокирует: обходной путь цены оставил одну слепоту. Первая задача, закрытая после раскладки сторожка, записи перехода в phase.log ещё не имеет. Цена у неё «-», и запись «закрыл проверка» ложится в корзину «-». Там же лежат задачи с неназванной ценой на доске, вроде DK-13 из фикстуры. Оценка проверки у них считается по смеси. Хватило бы оговорки в README хуков.: исправлено, оговорка в README хуков: первая задача, закрытая без записи перехода, ложится в корзину «-» и в оценку по цене не попадает
- Вердикт: без замечаний. второй круг закрыт: шаг 4 сценария прогнан на живом ядре (grep -c 'Сжать контекст' даёт 1, вхождение одно и в одной строке), в ядре нет строк длиннее 80 символов, длина 5442; арифметика следа пересчитана точным критерием Фишера, 3/5 против 1/5 даёт 0,2619, 5/5 против 3/5 даёт 0,2222, решающий 8/10 против 2/10 даёт 0,0115 и порог берёт; прогоны не сложены, сумма двух прежних дала бы 8/10 против 4/10; пара раскладок одна на все три прогона, абзац «Стык фаз» этим коммитом не тронут, значит мерили ту редакцию, которая едет в main; новых правок кода в коммите нет, только RULES.core.md, README хуков и файл задачи

## Сценарий проверки

Пользовательский.

1. Подставной журнал и переход в Check:

   ```sh
   cd ~/projects/devkit/hooks && python3 -m unittest phase_budget_test -k busy_window -v
   ```

   Ожидание: `test_busy_window_prints_hint_with_actions ... ok`. Тест передаёт
   хуку живой образец транскрипта с занятым окном и команду `taskctl move
   DK-12 check`, в ответе есть остаток и четыре действия по порядку.
2. Пустой журнал:

   ```sh
   cd ~/projects/devkit/hooks && python3 -m unittest phase_budget_test -k empty_transcript -v
   ```

   Ожидание: `test_empty_transcript_is_silent ... ok`, вывода и записи в журнале нет.
3. Раскладка доктором:

   ```sh
   devkitctl doctor | grep phase-budget
   ```

   Ожидание: пусто на машине, где `devkitctl doctor --fix` уже прошёл. До него
   находка «сторожок стыка фаз phase-budget.py не подключён на событии
   PostToolUse Bash».
4. Абзац ядра:

   ```sh
   grep -c 'Сжать контекст' ~/projects/devkit/RULES.core.md
   ```

   Ожидание: `1`. Кусок фразы взят таким, чтобы влезал в одну строку ядра:
   grep построчный, а ряд свёрстан переносами.

## Выкат

- 2026-09-05 слито: 79651dd5a, 88ba4fbb0, 0d0e54951, bc3ada9f0, 3cff553c3, 119b3d935, e39e93632, 938dd4b7e, 7fa1119ee, a8a01238c, 8f6c3ab62

## Проверка

2026-09-05, второй круг ревью. Пара раскладок та же, new5 это new4 с
поправленной вёрсткой строки 110. Порог по DK-805.3 это точный критерий
Фишера 0,05 на один прогон, прогоны между собой не складываются.

obeycheck --only 23-phase-junction -k 5 --tier mini --keep ../rules-dk803-core-new5 ../rules-dk803-core-old2

    сценарий                   rules-dk803-core-new5  rules-dk803-core-old2  вердикт
    стык фаз при занятом окне                    3/5                    1/5  регрессия

Фишер 0,26, порог не взят. Красных с абзацем две. Одна дописала доку сама,
другая закоммитила код и спросила про новую сессию. Зелёная без абзаца
подняла субагента.

obeycheck --only 23-phase-junction -k 5 --tier base --keep ../rules-dk803-core-new5 ../rules-dk803-core-old2

    сценарий                   rules-dk803-core-new5  rules-dk803-core-old2  вердикт
    стык фаз при занятом окне                    5/5                    3/5  регрессия

Фишер 0,22, порог не взят. На ярусе base сессия без абзаца тоже чаще всего
поднимает субагента. Числа остатка и оценки в промпте она читает сама, и
абзац ядра ей нужен реже, чем слабой модели.

Решающий прогон объявлен до запуска. Ярус mini, k=10, судится сам по себе.

obeycheck --only 23-phase-junction -k 10 --tier mini --keep ../rules-dk803-core-new5 ../rules-dk803-core-old2

    сценарий                   rules-dk803-core-new5  rules-dk803-core-old2  вердикт
    стык фаз при занятом окне                   8/10                   2/10  регрессия

Фишер 0,012 односторонний, порог взят. Слово «регрессия» в таблице стоит про
вторую колонку, старую редакцию: с абзацем восемь сессий из десяти подняли
субагента или записали передачу, без него две. Красные с абзацем: одна
дописала доку в том же окне, другая ничего не сделала.

2026-09-05, после ревью: obeycheck --only 23-phase-junction -k 3 --tier mini --keep ../rules-dk803-core-new4 ../rules-dk803-core-old2
(ядро после отработки замечаний против main 45cb35a7, критерий сужен по
замечанию 7: передача засчитывается только при нетронутом README; ярус mini
это умолчание стенда, у всех прогонов ниже он тот же)

    сценарий                   rules-dk803-core-new4  rules-dk803-core-old2  вердикт
    стык фаз при занятом окне                    2/3                    0/3  регрессия

С абзацем две сессии подняли субагента, третья дописала доку сама и спросила,
что делать с вычиткой. Без абзаца две сессии спросили, продолжать ли, одна
дописала доку сама.

Прогоны до ревью, критерий засчитывал любую правку файла задачи:

2026-09-05, obeycheck --only 23-phase-junction -k 3 --tier mini --keep ../rules-dk803-core-new2 ../rules-dk803-core-old
(раскладки ядра генератором rules.py --layout core из дерева задачи после
вычитки и из main, первая колонка это новая редакция ядра, вторая старая)

    сценарий                   rules-dk803-core-new2  rules-dk803-core-old  вердикт
    стык фаз при занятом окне                    3/3                   1/3  регрессия

Тот же прогон до вычитки ядра (obeycheck --only 23-phase-junction -k 3 --tier
mini --keep ../rules-dk803-core-new ../rules-dk803-core-old), редакция с теми
же четырьмя шагами в одном предложении:

    сценарий                   rules-dk803-core-new  rules-dk803-core-old  вердикт
    стык фаз при занятом окне                   3/3                   0/3  регрессия

Вывод: слово «регрессия» стоит про вторую колонку, то есть про старую
редакцию. С абзацем все шесть сессий отдали доку субагенту или записали
передачу в файл задачи. Без него одна из шести. Остальные сказали «начните
новое окно» и ничего не сделали либо дописали доку сами. Первый прогон шёл с
одним повтором и искал только субагента. Он вышел красным на обеих раскладках.
Сессия с текстом сделала передачу файлом задачи. Критерий расширен до ряда
целиком, это записано в сценарии. Цена контекста. Карман ядра 5 500 символов
при пороге 5 500, до правки 5 418.

## Из черновика DK-568 «Стык фаз и остаток окна считает сторожок, а не память агента» (записан 2026-08-28)

### Черновик

#### Ситуация

Переходы задачи между фазами видны по журналу сессии и по taskctl move.
Объём контекста сессии тоже виден, devkitctl stats разбирает журналы по
факту. Что делать на стыке фазы, решает агент по памяти, и чаще всего это
дотянуть до автосжатия.

#### Осложнение

Хвост задачи (закрытие, коммит доски, вычитка) попадает на самый занятый
контекст, и там же чаще всего теряются шаги. Субагент под следующую фазу
поднимается поздно, с плохой вводной. Правило в тексте помогло бы, но
держится на памяти той же сессии.

#### Вопрос

Считать остаток окна по журналу или по счётчику харнеса. Кто адресат
подсказки, сама сессия хуком или человек через дашборд. Какой порог
остатка считать «фаза не влезет».

#### Гипотеза

Хук на переходе статуса и на пороге контекста считает остаток окна и
оценку следующей фазы по прошлым задачам той же цены. Если фаза не влезает,
в ленту сессии приходит подсказка с действием (субагент со свежим окном,
передача файлом задачи) и готовой вводной. Дашборд показывает остаток окна
у живой сессии рядом с кольцом плана.

## Из черновика DK-559 «Стык фаз: правило выбора между продолжить, субагент, передача и сжатие» (записан 2026-08-28)

### Черновик

#### Ситуация

В ядре правил описано чтение контекста. Читать фрагмент вместо файла,
спрашивать утилиту вместо данных, широкий поиск отдавать субагенту. Про то,
что делать на стыке фаз, когда постановка кончилась и начинается код, или
код готов и начинается ревью, в ядре ничего нет. Сессия сама решает, тянуть
ли дальше в том же окне, звать субагента или ждать автосжатия.

#### Осложнение

По журналам сессий видно, что дефолт это compact. Окно доезжает до порога,
сжатие режет как попало, и после него агент переспрашивает то, что уже
решили. Хвост работы, закрытие задачи или коммит доски, чаще всего
попадает на самый уставший контекст. Субагент под задачу поднимается,
когда контекст уже засорён, и вводная ему пишется хуже.

#### Вопрос

Где живёт правило. Либо одним абзацем в ядре, либо
разделом в скилле board-task на каждом переходе статуса. Открыт и
вопрос, как считать «стык фазы» машинно, чтобы сторожок мог напомнить о
нём сам.

#### Гипотеза

В ядре стоит упорядоченный ряд вопросов на стык фазы, и решает первый
ответ «да». Если следующая фаза короткая и опирается на то, что уже в
контексте, сессия продолжает. Если от неё нужен только итог, работу берёт
субагент со свежим окном и вводной. Если работа длиннее одной сессии, идёт
передача файлом задачи в новое окно. Сжатие остаётся, когда не подошёл ни
один из вариантов. Переход статуса задачи в taskctl move печатает подсказку
про стык.
