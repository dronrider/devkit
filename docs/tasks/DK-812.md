# DK-812: review clean записывает чистый вердикт второго круга

## Что происходит

taskctl review clean отбивает повторный вызов словами «чистый вердикт в ревью уже записан». У задачи, вернувшейся из Check с проваленной приёмкой, второй круг ревью законный и свой вердикт записать обязан: правка после провала это новый код, и ревьювер судит его заново.

Флага обхода у подкоманды нет, поэтому ревьювер на DK-716 дописал строку вердикта в раздел руками по образцу из examples.md. Строку lint принял, но приём ненадёжный: руками легко разойтись с форматом, который ворот слияния разбирает машинно.

Разобрать надо, чем считать круг. Похоже, вердиктов в разделе может быть столько, сколько было кругов, и каждый со своим коммитом, а отбивать стоит только повтор в пределах одного круга.

Случай частый: любой возврат с красного слияния или из Check после чистого первого круга. 2026-09-18 на DK-457 ревьювер записал чистый второй круг обходом, `review add` плюс `review resolve`. Такое «замечание» `review stats` считает исправленным, и счёт возвратов врёт. По разделу «Ревью» не видно, до какого коммита код проверен. Скилл review (раздел «Второй круг») и examples.md обещают «тот же review clean с новым sha», а команда отказывает.

## Чего хотим

Чистый вердикт второго круга записывается той же командой, и по разделу «Ревью» видно, до какого коммита код проверен. Повтор вердикта на том же коде отбивается, как и раньше.

## Границы

Отказ второго `clean` это защита от дубля, её ввела DK-471 и держит тест `TestReviewCleanAfterResolved`. Защита остаётся, меняется её признак: дублем считается вердикт на том же коде. Ворота слияния и рубеж пуша строку вердикта не читают, им хватает строки уровня, поэтому правка живёт в taskctl. Условий, посильных только человеку, обход не нашёл: искал в LLD DK-460, `tools/taskctl/review.go`, `tools/shipctl/advice.go` и `hooks/check-review.py`.

## Развилки

- «вердикт»: как записывать чистый вердикт второго круга?
  - решает: исполнитель
  - рекомендация: новая строка вердикта со своим sha, прежняя остаётся: так обещают скилл review и examples.md, история кругов видна в review show
  - вариант: заменить sha в прежней строке по образцу review level, след первого круга теряется
  - вариант: явная подкоманда круга, новая сущность, которой в коде нет
  - источник: код tools/taskctl/review.go (cmdReviewClean), дока kit/skills/review/SKILL.md, раздел «Второй круг»
  - решено агентом 2026-09-19: новая строка вердикта со своим sha, прежняя остаётся: реализовано и задокументировано (tools/taskctl/review.go, cmdReviewClean), история кругов видна в review show, второй вариант (замена sha в прежней строке) терял бы след первого круга, третий (отдельная подкоманда) не нужен - review clean уже несёт нужный смысл

## DoD

- `taskctl review clean` принимает повторный вердикт, когда после прежнего вердикта в ветке появились коммиты с кодом, и пишет новую строку со своим sha. Прежняя строка остаётся на месте.
- Повтор на том же коде отбивается прежними словами.
- `review show` печатает оба вердикта, `review stats` чистые вердикты в счёт замечаний не берёт.
- Раздел с двумя вердиктами ворота слияния и `taskctl lint` читают закрытым ревью.
- Тесты кладут второй круг после правки кода, повтор без правки и раздел с замечанием между вердиктами. Регрессионный тест краснеет на старом коде, это проверено прогоном regcheck.
- Скилл review, examples.md и board-ship сверены с поведением команды.

## Ранг

`25+6+1+5+3 = 40`, P2.

`25+6+1+5+3 = 40`.

- Серьёзность 25: ручная работа с дешёвым обходом, ревьювер пишет итог круга обходной записью.
- Ценность 6: обход засоряет `review stats` фиктивным исправленным замечанием, а раздел «Ревью» перестаёт говорить, до какого коммита код проверен.
- Неопределённость 1: рекомендация одна, новая строка вердикта со сверкой sha, её уже обещают скилл review и examples.md.
- Поправка на баг 5: написанная процедура второго круга не исполняется.
- Рычаг 3: упираются на каждом возврате после чистого круга, за пачку 2026-09-18 дважды (DK-457, DK-464).

Цена S: одна функция и тесты, по объёму как сама DK-471.

## Ход работы

- Из черновика DK-812 «Второй чистый вердикт ревью не записать, хотя круг законный»: записан 2026-09-05, оформлен 2026-09-19.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 24%, снимок 6м назад, сдвига нет), 2026-09-19 17:15-17:15.

## Сценарий проверки

Все шаги агентские, стенд синтетический во временном каталоге. Обкатка
гоняет каждую строку блока отдельной командой в общем свежем дереве.

1. Сборка `taskctl` из правки и синтетическая доска с одной задачей XR-812
   в работе.

   ```sh
   mkdir -p stand/proj/docs/tasks
   git -C stand/proj init -q -b main
   git -C stand/proj config user.email t@t
   git -C stand/proj config user.name t
   printf '# Стенд: доска (префикс XR)\n\n## In progress\n\n| ID | Задача | Тип | P | R | Цена | Ссылка |\n|--------|--------|-----|---|---|------|--------|\n| XR-812 | Задача в работе | task | P2 | 30 (25+2+1+0+2) | - | [tasks/XR-812.md](tasks/XR-812.md) |\n\n## Check (готово, ждёт проверки пользователем)\n\n| ID | Задача | Тип | P | R | Цена | Ссылка |\n|--------|--------|-----|---|---|------|--------|\n\n## Backlog\n\n| ID | Задача | Тип | P | R | Цена | Ссылка |\n|--------|--------|-----|---|---|------|--------|\n\n## Blocked\n\nНет.\n' > stand/proj/docs/TASKS.md
   printf '# Тест: задачи (префикс XR), архив\n' > stand/proj/docs/TASKS-archive.md
   echo код > stand/proj/code.txt
   git -C stand/proj add .
   git -C stand/proj commit -q -m код
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" build -o stand/taskctl .
   ```

   Ожидание: команды молчат, код возврата 0 у каждой.

2. Первый круг ревью кончается чисто. Слияние возвращает провал,
   исполнитель довозит правку новым коммитом кода, и второй круг тоже
   кончается чисто. Такой ход раньше был исходной поломкой: до правки
   второй `review clean` отбивал этот законный вызов.

   ```sh
   stand/taskctl -C stand/proj review level XR-812 1 "неопределённость 0, не критично"
   stand/taskctl -C stand/proj review clean XR-812 "первый круг чист"
   echo v2 > stand/proj/code.txt
   git -C stand/proj add code.txt
   git -C stand/proj commit -q -m "fix: XR-812 правка после красного слияния"
   out=$(stand/taskctl -C stand/proj review clean XR-812 "второй круг чист") && echo "$out" | grep -q "вердикт без замечаний до" && echo "второй круг записан"
   ```

   Ожидание: последняя строка печатает «второй круг записан», код
   возврата 0. На старом коде эта строка красная: команда отбивает вызов
   словами «уже записан» вместо новой строки со своим sha.

3. По разделу видно оба круга. Обе строки вердикта остаются на месте,
   `review show` печатает два чистых исхода, `lint` доски молчит.

   ```sh
   [ "$(grep -c 'Вердикт: без замечаний' stand/proj/docs/tasks/XR-812.md)" = "2" ] && echo "обе строки на месте"
   [ "$(stand/taskctl -C stand/proj review show XR-812 | grep -c '\[чисто\]')" = "2" ] && echo "оба исхода чисты"
   stand/taskctl -C stand/proj lint
   ```

   Ожидание: первые две строки печатают своё сообщение, `lint` печатает
   «доска и архив в порядке», код возврата 0 у каждой.

4. Повтор без новой правки кода остаётся дублем: защита DK-471 держит.

   ```sh
   out=$(stand/taskctl -C stand/proj review clean XR-812 "третий круг" 2>&1); echo "$out" | grep -q "уже записан на этом коде" && echo "дубль отбит"
   ```

   Ожидание: строка «дубль отбит», код возврата 0.

## Из черновика DK-1049 «review clean отказывает второму кругу после чистого первого» (записан 2026-09-18)

### Черновик

#### Ситуация

2026-09-18, DK-457. Первый круг ревью кончился `taskctl review clean`. Слияние отбил красный тест, исполнитель положил новый коммит с кодом, ревьювер пошёл вторым кругом и замечаний не нашёл. Повторный `taskctl review clean DK-457 "..."` отказал словами «чистый вердикт уже записан». Ревьювер записал итог круга обходом: `review add` с префиксом «ревью:» и тут же `review resolve ... fixed`. На DK-894 в тот же день второй круг записался штатно, там первый круг кончился замечаниями, а не вердиктом.

#### Осложнение

Чистый вердикт стоит на sha первого круга, а код после него менялся. По разделу «Ревью» не видно, что второй круг был и что он чистый, видно закрытое «замечание», которого никто не делал. Текст board-ship, внесённый DK-472 («чистый проход второго круга записывает тот же review clean, с новым sha»), для этого случая неверен. Случай частый: любой возврат с красного слияния после чистого ревью.

#### Вопрос

Разрешить повторный `review clean` с новым sha (новая строка вердикта или замена sha в прежней) либо завести круг явной подкомандой?

#### Гипотеза

Чистый второй круг после чистого первого записывается той же командой, и по разделу «Ревью» видно, до какого коммита код проверен.
