# DK-818: Тесты taskctl пишут записи этапов в боевой ~/.devkit/runs

## Что происходит

5 сентября 2026 разбирал тормоза дашборда. В ~/.devkit/runs лежал 18601 файл
*.run. Из них 18548 носят имена вида
XR-004-var-folders-...-T-TestMoveQuestionCeiling...-001.run. Поле root у них
смотрит во временный каталог go-теста, и на диске его уже нет. Писатель берёт
дом через stage.Home(), а это os.UserHomeDir(). Общий setup(t) в тестах taskctl
подменяет корень проекта, а HOME не трогает. Каждый cmdMove внутри теста кладёт
запись в боевой каталог машины. Больше всех дают TestMoveQuestionCeiling (1937
файлов), TestMoveQuestionCeilingLinkForms (771) и TestPush (517). Мусор я
удалил руками в той же сессии.

За день прогонов набегает больше тысячи файлов. stage.List читает каталог
целиком на каждый запрос пульса и доски дашборда. Через две недели после чистки
ручки снова отвечают по секунде, а демон держит 60-85% CPU.

## Чего хотим

Прогон go test в tools/taskctl не оставляет ни одного файла в ~/.devkit/runs.
Есть тест, который это проверяет.

## Границы

Чинится сюита taskctl, она даёт почти весь мусор. Остальные сюиты дом уже
подменяют, и переписывать их задача не берётся. Сторож пишется так, чтобы
ловить любую сюиту, а не только taskctl.

Записи вида SC-001 и OB-001 в том же каталоге оставляет боевой бинарь на
временных стендах сценариев проверки и obeycheck, это другой предмет.
Медленное чтение каталога разбирает DK-819, здесь речь только про то, кто
каталог наполняет.

## Развилки

Подменять HOME в общей setup(t) тестов taskctl или дать stage.Home() шов для
тестов? Нужен ли сторож, который падает, когда прогон оставил след в настоящем
доме?

Разбор 5 сентября 2026 обе развилки закрыл. Подмена HOME через t.Setenv в
проекте уже принята: так делают сюиты agentctl, dashboard, shipctl, trackctl и
secretctl, да и внутри taskctl семь файлов подменяют дом поштучно
(gate_test.go, elapsed_test.go, notify_test.go и другие). Шов в stage.Home()
завёл бы второй способ там, где работает первый, поэтому подмена уезжает в
общий setup(t). Сторож нужен, он же стоит в гипотезе.

## DoD

- Общий setup(t) в tools/taskctl подменяет HOME временным каталогом, и записи
  этапов из тестов уходят туда.
- Прогон `go test ./...` в tools/taskctl не меняет числа файлов в настоящем
  ~/.devkit/runs. Проверяется счётом до и после прогона.
- Сторож падает, когда прогон оставил в настоящем доме хоть один файл.
- Мусор, набежавший в ~/.devkit/runs после ручной чистки, убран.

## Ранг

`25+4+1+5+4 = 39`, P2.

- Серьёзность 25: дашборд тормозит, но обход дешёвый и очевидный, каталог
  чистится одной командой.
- Ценность 4: чистка перестаёт быть регулярным ручным делом, а боевой каталог
  перестаёт держать фикстурные записи вперемешку с настоящими.
- Неопределённость 1: подход задан практикой проекта, открыто устройство
  сторожа.
- Поправка на баг +5: тесты пишут в дом машины, чего от них никто не ждёт.
- Рычаг 4: сторож защищает и те сюиты, которые начнут писать записи потом, а
  чистый каталог ускоряет каждый прогон дашборда.

## Ход работы

- Из черновика DK-818 «тесты taskctl льют записи этапов в настоящий ~/.devkit/runs»: записан 2026-09-05, оформлен 2026-09-05.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 18% профицит, снимок 45м назад, протух, сдвига нет), 2026-09-05 18:42-18:42.

## Ревью

Уровень 3 до 7b9b808: критичный путь tools/taskctl (2 да триажа), неопределённость 1; ревью прошло свежим контекстом, два замечания закрыты

- [возврат: реализация, 2026-09-06] блокирует: DoD «Мусор, набежавший после ручной чистки, убран» не выполнен. В реальном $HOME/.devkit/runs сейчас 689 файлов маски *var-folders*-T-Test*-NNN.run (тот самый паттерн бага, например TestMoveQuestionCeiling - 89 шт, TestDepAddGuardsInProgressAndCheck, TestMoveCheckGate*), даты 2026-09-05 18:33 - 2026-09-06 11:10, то есть уже после ручной чистки 5 сентября. Проверить: ls -1 $HOME/.devkit/runs | grep -c var-folders. Сделать повторную ручную чистку перед закрытием задачи (лучше после мержа, иначе другие деревья на неисправленном main продолжат сорить).: исправлено, убрал 689 фикстурных записей из боевого ~/.devkit/runs, осталось 56 боевых, проверка ls -1 $HOME/.devkit/runs | grep -c var-folders даёт 0
- [возврат: правила, 2026-09-06] не блокирует: Guard (internal/runsguard/runsguard.go) сверяет реальный $HOME/.devkit/runs без привязки к тому, кто именно пишет файл. Под shipctl merge риска нет (freshtree уже подменяет HOME), но при обычном ad hoc go test ./... на реальном HOME (109-117с окно, ровно тот сценарий, из-за которого завели задачу) параллельная боевая запись с другой живой сессии машины (обычный taskctl move по несвязанной задаче) будет ложно записана в вину сюите. Стоит явно назвать это ограничение в doc-комментарии Guard, чтобы не тратить время на разбор ложного красного.: исправлено, каркас риска описан в doc-комментарии Guard (internal/runsguard/runsguard.go), коммит 41ccda321, правка ревью

## Сценарий проверки

Команды звать из корня чекаута. `taskctl rehearse` подменяет `HOME` на время
сценария сам (`internal/freshtree`). Каталог `$HOME/.devkit/runs` ниже это
изолированный каталог обкатки, отдельный от боевого дома машины.

1. Счёт файлов в `$HOME/.devkit/runs` до и после прогона сюиты
   `tools/taskctl`. Так воспроизводится исходная поломка: `go test ./...`
   пакета оставлял в этом каталоге сотни фикстурных `.run`.

   ```sh
   before=$(ls -1 "$HOME/.devkit/runs" 2>/dev/null | wc -l); GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" test ./... -count=1; after=$(ls -1 "$HOME/.devkit/runs" 2>/dev/null | wc -l); echo "before=$before after=$after"
   ```

   Ожидание: строка `PASS`, а `before` и `after` равны.

2. Регрессия самого бага. `setup(t)` подменяет `HOME` на временный каталог, и
   move в Blocked пишет запись этапа только туда.

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" test ./... -run TestSetupIsolatesRealHome -v
   ```

   Ожидание: строка `PASS`, в выводе `TestSetupIsolatesRealHome`.

3. Сторож `internal/runsguard` сам по себе. Пять случаев: чистый прогон,
   отсутствующий каталог, новый файл валит зелёный прогон, новый файл на
   красном прогоне сохраняет его код, старые файлы остаются вне счёта.

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/internal" test ./runsguard/... -v
   ```

   Ожидание: строка `PASS`, пять подтестов, среди них
   `TestGuardNewFileFailsGreenRun` и `TestGuardNewFileKeepsFailingCode`.

## Выкат

- 2026-09-06 слито: 23ce26a6b, 2b77e4034, 02ba6cee2, 9a76ac7e5, 72c86614f, 825a8ffb6, 2000a8a43, 05cba0db7

## Проверка

Прогнал сценарий 2026-09-06. Машина живая, в ~/.devkit/runs лежит 54 файла от других сессий, но прогон сюиты taskctl не добавил новых, счёт совпал до и после. Все три шага зелёные, регрессия поймана, сторож работает.

Шаг 1 (зелёный):

```console
$ before=$(ls -1 "$HOME/.devkit/runs" 2>/dev/null | wc -l); GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" test ./... -count=1; after=$(ls -1 "$HOME/.devkit/runs" 2>/dev/null | wc -l); echo "before=$before after=$after"
ok  	github.com/dronrider/devkit/tools/taskctl	116.260s
before=      54 after=      54
```

Шаг 2 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" test ./... -run TestSetupIsolatesRealHome -v
=== RUN   TestSetupIsolatesRealHome
--- PASS: TestSetupIsolatesRealHome (0.09s)
PASS
ok  	github.com/dronrider/devkit/tools/taskctl	0.813s
```

Шаг 3 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/internal" test ./runsguard/... -v
=== RUN   TestGuardCleanRunKeepsCode
--- PASS: TestGuardCleanRunKeepsCode (0.00s)
=== RUN   TestGuardMissingDirIsClean
--- PASS: TestGuardMissingDirIsClean (0.00s)
=== RUN   TestGuardNewFileFailsGreenRun
--- PASS: TestGuardNewFileFailsGreenRun (0.00s)
=== RUN   TestGuardNewFileKeepsFailingCode
--- PASS: TestGuardNewFileKeepsFailingCode (0.00s)
=== RUN   TestGuardOnlyCountsNewNames
--- PASS: TestGuardOnlyCountsNewNames (0.00s)
PASS
ok  	github.com/dronrider/devkit/internal/runsguard	0.482s
```
