# DK-822: Дашборд поднимается одной строкой установки devkit

## Что происходит

Захотел дать коллеге попробовать дашборд на его маке и сверил 2026-09-05 по
исходникам, что даёт строка из `CONNECT.md` («клон плюс `devkitctl update
--pin`»). Дашборда после неё нет вовсе, и причина не в доводке, а в релизе:
`--pin` переводит клон на новейший тег и дальше работает его кодом, а в
тарболле `v0.12.0` от 2026-08-07 бинаря `dashboard` нет, как нет в `devkitctl`
тега и проверки `dashboard.check`. Владелец живёт со сборкой из `main`
через `devkitctl build`, поэтому пробела не видел.

Даже на теге с дашбордом строка не доводит его до входа. Конфиг
`~/.devkit/dashboard.local` никто не заводит, умолчания для `root` нет, и
экран отвечает «нет ни одной строки root»; доктор на первом прогоне про это
молчит нарочно (конфиг ещё не родился), а `--fix` строку `root` не пишет.
Токен рождается при первом старте демона, но нигде не печатается: про
`dashboard secret` и адрес `http://localhost:7112/login` новичку узнать
неоткуда. На чистом маке нет brew, а это единственный пакетник devkit, и
tmux с terminal-notifier уходят в «осталось сделать» без слова о том, что
ставить первым.

## Чего хотим

Коллега на чистом маке вставляет одну строку из `CONNECT.md` и в конце вывода
видит адрес входа и токен. Открывает адрес, вставляет токен и видит доску
devkit из своего клона: он лежит в корне, у него есть `docs/TASKS.md`, витрина
бесплатная. Свой проект добавляется `devkitctl new`, дашборд подхватывает его
без рестарта.

Для этого в devkit меняется четыре места:

- `dashboard.check` с `--fix` до взвода агента заводит `~/.devkit/dashboard.local`
  правами 600 со строкой `root`, равной родителю чекаута devkit. Клон по
  `CONNECT.md` лежит в `~/projects`, корень напрашивается сам; клон в другом
  месте даёт другой корень, а не пустой список. Существующий конфиг не
  трогается.
- Хвост `update` и `doctor --fix` печатает адрес входа. Токен печатается,
  когда родился на этом же прогоне: машина та же, и лишний шаг новичку это
  лишний вопрос. На обновлениях одна строка «дашборд поднят, адрес». Браузер
  сам не открывается: `update` гоняется и при обновлениях.
- «Осталось сделать» называет brew с командой установки, а не только
  пакеты, которых им нечем поставить. Сам brew не ставится: это тот же
  `curl | sh`, от которого devkit уходит.
- `CONNECT.md`, раздел «Один раз на машину», и README дашборда описывают
  этот ход: что печатает строка, куда идти, что заработает без Claude Code и
  tmux (доска, чаты), а что нет (запуск задач, разговор с агентом).

## Границы

- Релизный тег ставит человек, автоматика его не двигает (README, «Версии и
  доставка»). Задача готовит код, а тег с `tools/dashboard` в тарболле
  выпускается после её слияния; без тега строка для коллеги по-прежнему
  ставит `v0.12.0` без дашборда.
- Xcode Command Line Tools на чистом маке ставит сам первый `git`, строку
  после этого повторяют. Обходить это devkit не берётся.
- Второй сценарий пробы, показать коллеге дашборд владельца через
  браузерный вход, не годится: токен один на весь дашборд, отдать его
  значит отдать запуск задач за квоту владельца.
- Claude Code доктор по-прежнему не ставит; проверять его наличие и называть
  в «осталось сделать» можно, но это не предмет строки.

## Развилки

- Сценарий проверки гоняется на машине с живым дашбордом владельца: тот же
  порт 7112 и та же метка launchd `ru.devkit.dashboard`. Второй демон на том
  же порту не встанет, а `launchctl bootstrap` с той же меткой столкнётся с
  живым агентом. Исполнителю решить, как стенд разводит их: переопределение
  порта и метки в конфиге временного дома либо проверка раскладки без
  реального подъёма демона (плист и конфиг на диске, демон поднимает
  `dashboard serve` руками на свободном порту). Выбор записать здесь.
- «стенд проверки живости дашборда»: как сценарий проверки разводит стенд с живым дашбордом владельца на порту 7112 и меткой launchd
  - решает: исполнитель
  - рекомендация: раскладка без реального подъёма демона: launchd под временным HOME не трогаем (DK-588 own_home), plist и конфиг сверяем на диске, /healthz проверяем ручным dashboard serve на свободном порту
  - вариант: переопределить порт и метку в конфиге временного дома, поднять демон launchd по-настоящему
  - вариант: раскладка без подъёма демона: plist и конфиг на диске, dashboard serve руками на свободном порту
  - оставлена агентом 2026-09-18: own_home (DK-588) под временным HOME не трогает реальный launchd в принципе: доктор под подставным домом никогда не поднимет настоящий сервер и не подтвердит /healthz, так что первый вариант несовместим с уже существующей защитой. Раскладка (root, права 600 конфига) проверяется агентским сценарием под временным HOME без запуска демона, живость /healthz тот же сценарий проверяет отдельным ручным подъёмом dashboard serve на свободном порту temp HOME (без launchd и без риска для порта 7112 живого дашборда), а печать адреса и токена в хвосте doctor/update проверяется юнит-тестом dashboard_test.py приёмом machine=self.home, тем же, что у AgentTest и ProbeTest

## DoD

- В свежем доме (временный `HOME`, чекаут devkit внутри) `doctor --fix` кладёт
  `dashboard.local` с `root` родителя чекаута и правами 600, `/healthz`
  отвечает без ошибок конфига и `projects` не меньше 1.
- Хвост `update`/`doctor --fix` содержит адрес входа всегда. Значение токена
  при его рождении печатается только человеку за терминалом вне агентской
  сессии (замечание ревью); в остальных случаях, включая вывод не в
  терминал, вместо значения называются файл, ключ и команда `dashboard
  secret`. Проверяется тестом стенда devkitctl.
- «Осталось сделать» без brew на машине называет brew с командой установки.
- `CONNECT.md` и README дашборда описывают первый запуск шагами.

## Ранг

25+5+1+0+2 = 33 (M+1 = 34), P2

- Серьёзность 25: ручная работа с дешёвым обходом, владелец ставит дашборд
  руками, коллеге можно дать инструкцию из шести шагов.
- Ценность 5: каждая новая машина devkit получает дашборд без рук, но
  операция редкая, не ежедневная.
- Неопределённость 1: подход выбран (корень от чекаута, печать токена, brew
  не ставим), открыты детали раскладки в `update` на теге.
- Поправка на баг 0: новой работы больше, чем дефекта.
- Рычаг 2: ускоряет подключение машин к дашборду, дешевле каждую задачу не
  делает.

## Ход работы

- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 13%, снимок 13м назад, сдвига нет), 2026-09-18 20:17-20:17.
- Сторож прозы: CONNECT.md, хвост «..., а не Y» 4,1 при пороге 2, оставлено: хвосты называют настоящую вторую возможность (два пакета вместо только brew, серверный слой вместо доводки доктора), а не пустое отрицание, перестройка меняла бы утверждение.
- Сторож прозы: tools/dashboard/README.md, хвост «..., а не Y» 7,9 при пороге 5, оставлено: тот же случай, часть хвостов старый текст файла, задетый правкой соседней фразы, часть новых называет настоящую альтернативу (серверный слой против слоя доктора).
- Сторож прозы: docs/tasks/DK-822.md, средняя длина предложения 67,6 при пороге 23, предложений длиннее 25 слов 80,0 при пороге 30, довод в той же фразе 50,0 при пороге 48, оставлено: развилка «стенд проверки живости дашборда» в разделе «Развилки» записана строкой ответа `taskctl decide --leave`, это техническое обоснование команды, а не свободная проза, дробить его на фразы средствами вычитки нечем.
- Сторож прозы: docs/tasks/DK-822.md, средняя длина предложения 38,5 при пороге 23, предложений длиннее 25 слов 75,0 при пороге 30, оставлено: строка исхода замечания в разделе «Ревью» склеена командой `taskctl review resolve --reason` из исходной формулировки замечания и причины закрытия, это машинная запись, а не свободная проза.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 14%, снимок 14м назад, сдвига нет), 2026-09-18 21:03-22:10.

## Ревью

Уровень 3 до f028f12: триаж дал два да триажа, потому что правка меняет семантику пробы живости и рискует сломать существующие конфиги, а печать токена в хвосте доктора работает с секретом; критичные пути tools/shipctl, taskctl и hooks не задеты

- не блокирует: login_line печатает токен безусловно на первом взводе агента, не только когда doctor/update запускает человек за клавиатурой на своей машине; агентская сессия, которая первой поднимет дашборд (doctor --fix по любому другому поводу), увидит секрет в собственном выводе Bash и в своём контексте. Развилки задачи разбирают только стенд проверки, этот случай не назван; стоило бы либо явно принять риск строкой в файле задачи, либо сузить условие печати (например тегом «интерактивный запуск»).: исправлено, login_line: адрес всегда, значение токена только isatty и не CLAUDECODE=1 (агентская сессия), иначе называются файл/ключ/команда dashboard secret; тесты на три ветки в dashboard_test.py (LoginLineTest, InstallLoginTest, AgenticSessionTest), CONNECT.md и README дашборда поправлены, вычитаны proofread
- Вердикт: без замечаний. второй круг проверен диффом b737e5d2..dk-822, а не со слов: значение токена больше не уходит наружу ни одной другой веткой check/probe/находок, 45 тестов dashboard_test.py зелёные, мутация трёх веток видимости токена и мутация agentic_session пойманы тестами и откачены, дока и DoD сверены с кодом, команда dashboard secret существует, готового живого детектора по всем профилям kit/harness нет, только схемная валидация и разбор по имени, генерализация несоразмерна одному хардкоду при одном живом сигнале сейчас

## Сценарий проверки

Шаги гоняются с временным домом, чтобы не трогать живой дашборд владельца и
его launchd-агента на порту 7112 (решение развилки выше): каждый шаг сам
заводит клон и дом, `doctor --fix` под таким домом launchd не трогает вовсе
(DK-588, `own_home`), а живость проверяется отдельным ручным подъёмом
`dashboard serve` на свободном порту, без участия launchd. Адрес и токен в
хвосте `doctor --fix`, а также текст находки про установку `brew`, проверяет
юнит-тест `tools/devkitctl/dashboard_test.py` (классы `LoginLineTest`,
`InstallLoginTest`) и `tools/devkitctl/devkitctl_test.py`
(`PackagesTest.test_3_no_package_manager`), уже зелёный на ветке.

```sh
home=$(mktemp -d) && git clone -q $(pwd) "$home/projects/devkit" && HOME="$home" python3 "$home/projects/devkit/tools/devkitctl/devkitctl.py" doctor --fix >/dev/null 2>&1 ; grep -c '^root = ' "$home/.devkit/dashboard.local" && stat -f %Lp "$home/.devkit/dashboard.local" ; rm -rf "$home"
```

Ожидание: `1` и `600`.

```sh
home=$(mktemp -d) && git clone -q $(pwd) "$home/projects/devkit" && HOME="$home" python3 "$home/projects/devkit/tools/devkitctl/devkitctl.py" doctor --fix >/dev/null 2>&1 ; port=$(python3 -c "import socket;s=socket.socket();s.bind(('127.0.0.1',0));print(s.getsockname()[1]);s.close()") && printf 'port = %s\n' "$port" >> "$home/.devkit/dashboard.local" ; HOME="$home" dashboard serve >"$home/serve.out" 2>&1 & pid=$! ; out=$(curl -fsS --retry 20 --retry-delay 1 --retry-connrefused "http://127.0.0.1:$port/healthz") ; kill "$pid" 2>/dev/null ; rm -rf "$home" ; echo "$out" ; echo "$out" | grep -q '"errors":\[\]' && echo "$out" | grep -q '"projects":1'
```

Ожидание: печатается JSON вида `{"ok":true,...,"projects":1,"errors":[]}`
(доска клона devkit, найденная по строке `root`, которую завёл `doctor --fix`),
команда завершается нулём. Демон дашборда временного дома этим же шагом
снимается (`kill`), а домашний каталог удаляется.

## Выкат

- 2026-09-18 слито: 28f7b3ef, b00acf49, b1525d21, af36d81a, bf6f6a22, d54ccfb1, 52784502, dd0e50c0, ebed7217, 0f6335cf, 53e8d120, ab1596b7, 3cf00a83, b520401c
- smoke прогнан, 2026-09-18

## Проверка


- Обкатка: 2026-09-18 22:10, свежее дерево da8e5d53c346, сценарий 7c1088e6, временный HOME, утилит дерева 11, шагов 2, все зелёные.

Шаг 1 (зелёный):

```console
$ home=$(mktemp -d) && git clone -q $(pwd) "$home/projects/devkit" && HOME="$home" python3 "$home/projects/devkit/tools/devkitctl/devkitctl.py" doctor --fix >/dev/null 2>&1 ; grep -c '^root = ' "$home/.devkit/dashboard.local" && stat -f %Lp "$home/.devkit/dashboard.local" ; rm -rf "$home"
1
600
```

Шаг 2 (зелёный):

```console
$ home=$(mktemp -d) && git clone -q $(pwd) "$home/projects/devkit" && HOME="$home" python3 "$home/projects/devkit/tools/devkitctl/devkitctl.py" doctor --fix >/dev/null 2>&1 ; port=$(python3 -c "import socket;s=socket.socket();s.bind(('127.0.0.1',0));print(s.getsockname()[1]);s.close()") && printf 'port = %s\n' "$port" >> "$home/.devkit/dashboard.local" ; HOME="$home" dashboard serve >"$home/serve.out" 2>&1 & pid=$! ; out=$(curl -fsS --retry 20 --retry-delay 1 --retry-connrefused "http://127.0.0.1:$port/healthz") ; kill "$pid" 2>/dev/null ; rm -rf "$home" ; echo "$out" ; echo "$out" | grep -q '"errors":\[\]' && echo "$out" | grep -q '"projects":1'
{"errors":[],"ok":true,"projects":1,"taskctl_live":0,"taskctl_waiting":0,"uptime_s":0,"version":"dashboard dev (unknown)"}
```
