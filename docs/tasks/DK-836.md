# DK-836: Стенд меряет правку промпта: ключ предмета, вердикт и след прогона

Цель: [tasks/DK-565.md](tasks/DK-565.md)

## Что происходит

Сценарии стенда никак не связаны с текстом, который меряют. Привязки к файлу
или разделу у сценария нет ни в каком виде. Переименование скилла или раздела
ядра оставило бы сценарий без привязки молча, и прогон остался бы зелёным.

След прогона стенд оставляет прозой. В скилле `prompt-test` записано вкладывать
таблицу в «Проверку» руками, и отличить прогон от пересказа прогона по такой
записи нечем. Ворота слияния DK-806 проверять тут пока нечего.

Вердиктов у стенда три, и любая разница считается регрессией. На сценарии с
вероятностью зелёной клетки 0.8 при k=5 кандидат отстаёт от базы на клетку и
больше в трети прогонов при одинаковом тексте. Треть честных правок упиралась
бы в такие ворота.

Решения дал LLD DK-805. Работа режется надвое, здесь первая половина. Ворота
слияния идут строкой DK-806 следом за этой.

## Чего хотим

У сценария есть собственный предмет. Ключ `предмет` стоит в шапке рядом с
`конец`, значение это список через `;`, а элемент это путь от корня devkit и
необязательный заголовок раздела в ёлочках. Разбор проверяет привязку по
дереву. Путь обязан существовать, а названный раздел стоять заголовком `## `
вне ограждённых блоков. Иначе загрузка сценариев падает кодом 2 и называет
сценарий и путь.

Разбор шапки и предикат агентского файла живут в `internal/obey`. Читателей у
них двое, стенд и ворота слияния, и второй копии разбора никому не нужно. Отбор
по предмету идёт флагом `--for`, `--list` печатает предмет третьей колонкой.

Вердикт строки считается по зелёным клеткам кандидата и базы. Разницу считает
значимой односторонний точный тест Фишера, когда случайность даёт её не чаще
раза из двадцати. Слов вердикта пять, они заменяют три нынешних. Зачёт
зависит от базы. С базой `старый` зачтены «польза», «не хуже» и «зелёный на
обеих», с базой `пусто` только «польза».

След прогона пишет стенд, а не автор. С флагом `--task <ID>` он записывает в
«Проверку» отметку `- Стенд:` или `- Стенд не зачтён:` с деревом, отпечатком
предмета, базой, ярусом, числом повторов и списком сценариев. Ключ записи это
сценарии вместе с базой, и повторный прогон заменяет свою прошлую запись.
Отпечаток снят с текста предмета, поэтому правка соседнего раздела след не
портит, а правка самого предмета делает отпечаток устаревшим.

## DoD

- Сценарий без ключа `предмет` не разбирается, загрузка падает кодом 2 и
  называет сценарий.
- При предмете, указывающем в никуда, загрузка падает тем же кодом и называет
  путь.
- `obeycheck --for <путь>` берёт сценарии, чей предмет покрывает файл целиком
  или разделом, и сочетается с `--only`.
- `--list` печатает предмет третьей колонкой, ключ стоит у всех сценариев
  стенда.
- Прогон печатает один из пяти вердиктов, значимость считает точный тест Фишера
  при p не больше 0.05.
- С базой `пусто` зачтена только «польза», «красный на обеих» не зачтён при
  любой базе.
- `--task <ID>` с `-k` меньше трёх отказывает кодом 2 и называет это разведкой.
- `--task <ID>` пишет в «Проверку» отметку `- Стенд:` или `- Стенд не зачтён:`,
  а повторный прогон с тем же списком сценариев и базой заменяет прошлую
  запись.
- Отпечаток предмета снят с тела раздела у предмета с разделом и с файла
  целиком у предмета без него.
- README стенда получил разделы «Сценарий», «Правило решения» и «Первый
  запуск», пример в нём стоит кандидатом первым.
- Раздел «След в файле задачи» и рецепт раскладок в `prompt-test` переписаны под
  отметку и под `home/.claude/skills/`.
- Тесты по стандарту.

## Ранг

`50+8+1+0+3 = 62`, P1.

- Серьёзность 50: прогон стенда не привязан к тексту и не оставляет
  проверяемого следа, обход это честность автора.
- Ценность 8: без ключа и следа не встают ни ворота слияния DK-806, ни ревизия
  ядра DK-808.
- Неопределённость 1: ключ, флаги, вердикты и форма отметки названы решениями
  1-3 DK-805 до имён.
- Поправка на баг 0.
- Рычаг 3: привязку и след читает всякая правка промптов, а не только задачи
  цели.

## Ход работы

- Вычитка: 10 правок, 0 пометок, 2026-09-06.
- Вычитка: 3 файла, 16 правок, 2 пометки, 2026-09-06.
- Разработка: субагент opus/medium по вердикту pick (маппинг sonnet, корректор: профицит week_all, week_max; квота: week_all 36% профицит, week_max 37% профицит (панель разбивку не дала, цифра из кеша клиента возрастом 2ч 17м назад), снимок 35м назад), 2026-09-06 12:04-12:04.
- Ревью: субагент sonnet/high по вердикту pick (маппинг sonnet, корректор: профицит week_all, week_max; квота: week_all 42% профицит, week_max 37% профицит (панель разбивку не дала, цифра из кеша клиента возрастом 3ч 48м назад), снимок 4м назад), 2026-09-06 13:04-13:50.
- Снаружи: проверка после выката, 2026-09-06 13:50-13:51.

## Ревью

Уровень 3 до 2c79b85: неопределённость 1 из вердикта, два да триажа: тронут критичный путь tools/taskctl (AgentFile), правка кладёт базовый механизм будущих ворот слияния DK-806

- [возврат: реализация, 2026-09-06] блокирует: DropStandRecord (internal/taskform/stand.go) не убирает старую запись целиком, когда у прогона было предупреждение. Тело записи после строки отметки это blank, опционально 'Предупреждение: ...' (не ограждённая строка), потом ограждённый блок; DropStandRecord после отметки пропускает только пустые и ограждённые (masked) строки и останавливается на первой непустой не-masked строке -- то есть на строке 'Предупреждение:'. Повторный прогон с тем же ключом (сценарии+база) оставляет старые 'Предупреждение:' и старый ```console-блок в файле, а новая запись просто добавляется рядом: 'Проверка' растёт дублями вопреки DoD и решению 2 LLD DK-805. Воспроизвёл: n.write() с Warnings, затем n.write() с тем же ключом без Warnings -- в документе остаётся 2 блока console вместо 1. Тестами (TestTaskNoteReplacesSameKey, TestTaskNoteCarriesWarning) эта комбинация не покрыта, каждый гоняет свой сценарий отдельно. Починить: DropStandRecord должен также пропускать строки с префиксом 'Предупреждение: ' (например явной проверкой префикса в теле цикла), и добавить тест на replace-с-warning.: исправлено, уборка прошлой записи пропускает и строки «Предупреждение: », константа taskform.WarnLine одна на писателя и уборщика; тест на пару условий сразу: TestStandDropRecordWithWarning в taskform и TestTaskNoteReplacesRecordWithWarning в стенде, оба краснеют на HEAD по regcheck
- [возврат: реализация, 2026-09-06] не блокирует: tools/obeycheck/scenario.go заводит свой fence/backticks (для '## ' внутри блока кода сценария), почти дословную копию internal/obey.fence/backticks, которую эта же задача завела для SectionBody. Разница: обеевская backticks считает и ` и ~ оградой, scenario.go только `. Live-риска сейчас нет (сценарии тильдой не пользуются), но раздвоенный парсер это ровно то дублирование, которого решение 1 LLD DK-805 просило избежать. Сделать: вынести fence-обнаружение в internal/obey одной реализацией и звать её из scenario.go, либо явно объяснить комментарием, почему копия оправдана.: исправлено, разбор ограды один: obey.Fence с методом Step, scenario.go зовёт его, свои fence и backticks сняты; тильда теперь считается оградой и в шапке сценария, случай закрыт TestFenceShapes в obey
- Вердикт: без замечаний. второй круг до a48be6f6a: оба замечания исправлены по существу, перепроверил сам. Замечание 1: воспроизвёл красноту нового теста на пред-фикс коде через временный worktree (WarnLine-заглушка без правки DropStandRecord) -- TestStandDropRecordWithWarning и TestTaskNoteReplacesRecordWithWarning падают на 9b8a4f690 и проходят на HEAD. Прогнал сам сочетания сверх авторских тестов: без предупреждения -> с предупреждением, с предупреждением -> без -> с двумя предупреждениями подряд (три записи одним ключом), запись с другим ключом и базой не трогается -- везде ровно одна отметка и один console-блок на ключ, счётчик предупреждений точный. Новой слепоты от префиксного опознавания не нашёл: строка "Предупреждение: " больше нигде в taskform, obeycheck, taskctl и shipctl не используется как чужой машинный маркер. Замечание 2: fence сведён в internal/obey.Fence с методом Step, scenario.go зовёт его, свои fence и backticks сняты; полные прогоны internal/obey, internal/taskform, tools/obeycheck (go test ./... по каждому) и build tools/taskctl зелёные, TestFenceShapes и TestHeadingInsideCodeBlockIsNotSection в scenario_test.go (бэктик-кейсы) и новый TestFenceShapes в obey_test.go (тильда) проходят -- разбор шапки сценария не сломан, тильда теперь распознаётся там же, где раньше протекала.

## Сценарий проверки

Шаги гоняются из корня чекаута. Живых сессий они не поднимают, прогон стенда
идёт на подложной модели, как у стенда сегодня.

1. Разбор привязки в общем пакете. `Subject`, `ParseSubjects`, `Covers` и
   `AgentFile`, среди случаев предмет с разделом и предмет в никуда.

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/internal" test ./obey/... -count=1 -v
   ```

   Ожидание: строка `PASS`, среди подтестов покрытие файла целиком и покрытие
   одним разделом.

2. Ключ у всех сценариев и печать предмета. Файл без ключа `предмет` не
   разбирается, `--list` даёт третью колонку.

   ```sh
   root="$(git rev-parse --show-toplevel)"; grep -L "^предмет:" "$root/tools/obeycheck/scenarios"/*.md; obeycheck --list | head -5
   ```

   Ожидание: `grep -L` не печатает ни одного файла, а в выводе `--list` у
   каждого сценария стоит предмет.

3. Пять вердиктов и правило зачёта. Тест Фишера, порог значимости и разный
   зачёт по базам.

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/obeycheck" test . -count=1 -run "TestVerdict|TestFisher|TestBase" -v
   ```

   Ожидание: строка `PASS`, разница 5/5 против 1/5 значима, разница 4/5 против
   2/5 незначима, с базой `пусто` зачтена только «польза».

4. Отметка в файле задачи. Форма записи, замена прошлой записи по тому же ключу
   и отказ на разведке.

   ```sh
   root="$(git rev-parse --show-toplevel)"; GOWORK=off go -C "$root/tools/obeycheck" test . -count=1 -run "TestTaskNote" -v; GOWORK=off go -C "$root/internal" test ./taskform/... -count=1 -run "TestStand" -v
   ```

   Ожидание: строка `PASS` у обеих сюит, среди подтестов замена записи с тем же
   списком сценариев и базой, а также код 2 на `--task` с `-k 2`.

5. Дока стенда и скилла.

   ```sh
   root="$(git rev-parse --show-toplevel)"; grep -n "^## " "$root/tools/obeycheck/README.md"; grep -n "^## \|home/.claude/skills" "$root/kit/skills/prompt-test/SKILL.md" | head -20
   ```

   Ожидание: в README стенда стоят разделы «Сценарий», «Правило решения» и
   «Первый запуск», в `prompt-test` раздел «След в файле задачи» и рецепт
   раскладок с `home/.claude/skills/`.

## Выкат

- 2026-09-06 слито: 7e37e3714, 368409ed8, ef9159e42, 4efb24ab0, f9620351a, 8fdff9333, 15f0036ef, aa003f2f6, e1ae586e6, 4e8253bfb, d9365a2f6, af6f085cf, e1b9e61d5, c255ad8eb

## Проверка

- Стенд: 2026-09-06 13:01, дерево 0b62fffeb, предмет bd73357c, база старый, ярус mini, k 3, сценарии 25-stand-trace, зачтён.

```console
$ /tmp/obeycheck-dk836 --devkit /Users/rider/projects/devkit-dk-836 --task DK-836 --for kit/skills/prompt-test/SKILL.md -k 3 --tier mini --base старый /tmp/dk836-cand /tmp/dk836-base
сценарий                                              dk836-cand  dk836-base  вердикт
зафиксировать проверку правки промпта в файле задачи         3/3         0/3  польза

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  25-stand-trace / dk836-base / повтор 1: проверка: exit status 1 (след прогона агент писал руками, стенд с --task не позван)
```

- Обкатка: 2026-09-06 13:50, свежее дерево 756a4f3988dc, сценарий 1e4b56ec, временный HOME, утилит дерева 11, шагов 5, все зелёные.

Шаг 1 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/internal" test ./obey/... -count=1 -v
=== RUN   TestParseSubjects
--- PASS: TestParseSubjects (0.00s)
=== RUN   TestParseSubjectsErrors
=== RUN   TestParseSubjectsErrors/путь_наружу_дерева
=== RUN   TestParseSubjectsErrors/пусто
=== RUN   TestParseSubjectsErrors/раздел_не_закрыт
=== RUN   TestParseSubjectsErrors/пустой_раздел
=== RUN   TestParseSubjectsErrors/без_пути
--- PASS: TestParseSubjectsErrors (0.00s)
    --- PASS: TestParseSubjectsErrors/путь_наружу_дерева (0.00s)
    --- PASS: TestParseSubjectsErrors/пусто (0.00s)
    --- PASS: TestParseSubjectsErrors/раздел_не_закрыт (0.00s)
    --- PASS: TestParseSubjectsErrors/пустой_раздел (0.00s)
    --- PASS: TestParseSubjectsErrors/без_пути (0.00s)
=== RUN   TestVerify
=== RUN   TestVerify/директория_вместо_файла
=== RUN   TestVerify/файла_нет
=== RUN   TestVerify/раздела_нет
=== RUN   TestVerify/раздел_только_в_блоке
--- PASS: TestVerify (0.00s)
    --- PASS: TestVerify/директория_вместо_файла (0.00s)
    --- PASS: TestVerify/файла_нет (0.00s)
    --- PASS: TestVerify/раздела_нет (0.00s)
    --- PASS: TestVerify/раздел_только_в_блоке (0.00s)
=== RUN   TestCovers
=== RUN   TestCovers/файл_целиком_покрывает_любой_раздел
=== RUN   TestCovers/раздел_покрывает_свой
=== RUN   TestCovers/раздел_не_покрывает_соседний
=== RUN   TestCovers/отбор_по_файлу_берёт_предмет_с_разделом
=== RUN   TestCovers/чужой_файл
--- PASS: TestCovers (0.00s)
    --- PASS: TestCovers/файл_целиком_покрывает_любой_раздел (0.00s)
    --- PASS: TestCovers/раздел_покрывает_свой (0.00s)
    --- PASS: TestCovers/раздел_не_покрывает_соседний (0.00s)
    --- PASS: TestCovers/отбор_по_файлу_берёт_предмет_с_разделом (0.00s)
    --- PASS: TestCovers/чужой_файл (0.00s)
=== RUN   TestAgentFile
--- PASS: TestAgentFile (0.00s)
=== RUN   TestPrint
--- PASS: TestPrint (0.00s)
=== RUN   TestFenceShapes
--- PASS: TestFenceShapes (0.00s)
=== RUN   TestSectionBody
--- PASS: TestSectionBody (0.00s)
PASS
ok  	github.com/dronrider/devkit/internal/obey	0.432s
```

Шаг 2 (зелёный):

```console
$ root="$(git rev-parse --show-toplevel)"; grep -L "^предмет:" "$root/tools/obeycheck/scenarios"/*.md; obeycheck --list | head -5
01-doc-paragraph     написать абзац документации                          RULES.core.md «Символы» (конец: любой)
02-bug-fix           починить баг в утилите                               RULES.core.md «Тесты обязательны»; kit/skills/test-standard/SKILL.md «Тест на баг» (конец: любой)
03-commit-style      закоммитить правку                                   RULES.core.md «Git» (конец: любой)
04-no-push           закончить работу над задачей                         RULES.core.md «Git» (конец: любой)
05-board-check       перевести задачу в Check                             RULES.board.core.md; kit/skills/board-task/SKILL.md «Статусы» (конец: любой)
```

Шаг 3 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/obeycheck" test . -count=1 -run "TestVerdict|TestFisher|TestBase" -v
=== RUN   TestVerdictWords
=== RUN   TestVerdictWords/зелёный_на_обеих
=== RUN   TestVerdictWords/красный_на_обеих
=== RUN   TestVerdictWords/польза
=== RUN   TestVerdictWords/просадка
=== RUN   TestVerdictWords/клетка_туда-сюда_это_не_хуже
=== RUN   TestVerdictWords/четыре_против_двух_незначимы
=== RUN   TestVerdictWords/полный_провал_кандидата
=== RUN   TestVerdictWords/тройка_против_нуля_при_k_3
=== RUN   TestVerdictWords/двойка_против_нуля_при_k_3
--- PASS: TestVerdictWords (0.00s)
    --- PASS: TestVerdictWords/зелёный_на_обеих (0.00s)
    --- PASS: TestVerdictWords/красный_на_обеих (0.00s)
    --- PASS: TestVerdictWords/польза (0.00s)
    --- PASS: TestVerdictWords/просадка (0.00s)
    --- PASS: TestVerdictWords/клетка_туда-сюда_это_не_хуже (0.00s)
    --- PASS: TestVerdictWords/четыре_против_двух_незначимы (0.00s)
    --- PASS: TestVerdictWords/полный_провал_кандидата (0.00s)
    --- PASS: TestVerdictWords/тройка_против_нуля_при_k_3 (0.00s)
    --- PASS: TestVerdictWords/двойка_против_нуля_при_k_3 (0.00s)
=== RUN   TestFisherExact
=== RUN   TestFisherExact/5/5_против_1/5
=== RUN   TestFisherExact/4/5_против_0/5
=== RUN   TestFisherExact/4/5_против_2/5
=== RUN   TestFisherExact/5/5_против_2/5
=== RUN   TestFisherExact/3/3_против_0/3
=== RUN   TestFisherExact/10/10_против_6/10
--- PASS: TestFisherExact (0.00s)
    --- PASS: TestFisherExact/5/5_против_1/5 (0.00s)
    --- PASS: TestFisherExact/4/5_против_0/5 (0.00s)
    --- PASS: TestFisherExact/4/5_против_2/5 (0.00s)
    --- PASS: TestFisherExact/5/5_против_2/5 (0.00s)
    --- PASS: TestFisherExact/3/3_против_0/3 (0.00s)
    --- PASS: TestFisherExact/10/10_против_6/10 (0.00s)
=== RUN   TestBaseCounting
=== RUN   TestBaseCounting/польза_при_базе_старый
=== RUN   TestBaseCounting/не_хуже_при_базе_старый
=== RUN   TestBaseCounting/зелёный_на_обеих_при_базе_старый
=== RUN   TestBaseCounting/просадка_при_базе_старый
=== RUN   TestBaseCounting/красный_на_обеих_при_базе_старый
=== RUN   TestBaseCounting/польза_при_базе_пусто
=== RUN   TestBaseCounting/не_хуже_при_базе_пусто
=== RUN   TestBaseCounting/зелёный_на_обеих_при_базе_пусто
=== RUN   TestBaseCounting/красный_на_обеих_при_базе_пусто
--- PASS: TestBaseCounting (0.00s)
    --- PASS: TestBaseCounting/польза_при_базе_старый (0.00s)
    --- PASS: TestBaseCounting/не_хуже_при_базе_старый (0.00s)
    --- PASS: TestBaseCounting/зелёный_на_обеих_при_базе_старый (0.00s)
    --- PASS: TestBaseCounting/просадка_при_базе_старый (0.00s)
    --- PASS: TestBaseCounting/красный_на_обеих_при_базе_старый (0.00s)
    --- PASS: TestBaseCounting/польза_при_базе_пусто (0.00s)
    --- PASS: TestBaseCounting/не_хуже_при_базе_пусто (0.00s)
    --- PASS: TestBaseCounting/зелёный_на_обеих_при_базе_пусто (0.00s)
    --- PASS: TestBaseCounting/красный_на_обеих_при_базе_пусто (0.00s)
PASS
ok  	github.com/dronrider/devkit/tools/obeycheck	0.417s
```

Шаг 4 (зелёный):

```console
$ root="$(git rev-parse --show-toplevel)"; GOWORK=off go -C "$root/tools/obeycheck" test . -count=1 -run "TestTaskNote" -v; GOWORK=off go -C "$root/internal" test ./taskform/... -count=1 -run "TestStand" -v
=== RUN   TestTaskNoteWritesMark
--- PASS: TestTaskNoteWritesMark (0.02s)
=== RUN   TestTaskNoteFailedMark
--- PASS: TestTaskNoteFailedMark (0.02s)
=== RUN   TestTaskNoteReplacesSameKey
--- PASS: TestTaskNoteReplacesSameKey (0.06s)
=== RUN   TestTaskNoteReplacesRecordWithWarning
--- PASS: TestTaskNoteReplacesRecordWithWarning (0.06s)
=== RUN   TestTaskNoteRefusesScouting
--- PASS: TestTaskNoteRefusesScouting (0.00s)
=== RUN   TestTaskNoteAcceptsMinimumRepeats
--- PASS: TestTaskNoteAcceptsMinimumRepeats (0.02s)
=== RUN   TestTaskNoteCarriesWarning
--- PASS: TestTaskNoteCarriesWarning (0.02s)
PASS
ok  	github.com/dronrider/devkit/tools/obeycheck	0.656s
=== RUN   TestStandLineRoundTrip
--- PASS: TestStandLineRoundTrip (0.00s)
=== RUN   TestStandFailLine
--- PASS: TestStandFailLine (0.00s)
=== RUN   TestStandMarkInsideFenceIgnored
--- PASS: TestStandMarkInsideFenceIgnored (0.00s)
=== RUN   TestStandMarkWithoutFields
--- PASS: TestStandMarkWithoutFields (0.00s)
=== RUN   TestStandSameKey
--- PASS: TestStandSameKey (0.00s)
=== RUN   TestStandDropRecord
--- PASS: TestStandDropRecord (0.00s)
=== RUN   TestStandDropRecordWithWarning
--- PASS: TestStandDropRecordWithWarning (0.00s)
=== RUN   TestStandGateException
--- PASS: TestStandGateException (0.00s)
PASS
ok  	github.com/dronrider/devkit/internal/taskform	0.917s
```

Шаг 5 (зелёный):

```console
$ root="$(git rev-parse --show-toplevel)"; grep -n "^## " "$root/tools/obeycheck/README.md"; grep -n "^## \|home/.claude/skills" "$root/kit/skills/prompt-test/SKILL.md" | head -20
19:## Первый запуск
72:## Как устроен прогон
96:## Раскладка
121:## Сценарий
221:## Правило решения
263:## След в файле задачи
292:## Авторизация во временном HOME
336:## Цена прогона
349:## Чего стенд не ловит
362:## Свои тесты
20:## Порог повода
35:## Сценарий
80:## Раскладки
93:Скиллы кладутся целиком в `home/.claude/skills/`, оттуда стенд везёт их во
103:cp -R kit/skills ../rules-with/home/.claude/skills
106:cp -R kit/skills ../rules-without/home/.claude/skills
107:rm -rf ../rules-without/home/.claude/skills/<имя скилла>
111:## Ярус и повторы
130:## Цена контекста
147:## Чтение результата
167:## След в файле задачи
```
