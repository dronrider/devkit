# DK-847: Список чатов панели держит только разговоры человека

## Что происходит

Человек заводит разговоры кнопкой панели, а в списке рядом с ними стоят
задания, которые раздавал конвейер: прогоны сценариев проверки, груминги,
подъёмы исполнителей. Плюс записи стендов, которые панель вообще не поднимала,
а просто нашла транскриптом в каталоге проекта. Замер на живой машине: в списке
devkit 92 записи, в xr-proxy 92, машинных вне архива 38, из них 21 это прогоны
стенда послушания за один день.

Архив тут не лечит: убранное руками нарастает заново за неделю. Склейка заходов
DK-723 тоже не про это, она сводит перезапуски одного окна, а разные задания
идут разными окнами и остаются разными строками по замыслу.

Механизм в панели уже есть, его надо распространить. Память диалога держит поле
`hidden`, и список по нему запись прячет (chats.go, отсев рядом с
titleSession). Служебные вызовы заголовков узнаются по метке первой реплики
([devkit-title], [devkit-probe]), а рабочий каталог им дают вне всех проектов,
чтобы транскрипт не попал ни в один список (titleDir).

Предлагаемое устройство:

- в списке остаются разговоры, которые человек завёл сам: новый чат, кнопка
  выполнения задачи, продолжение работы по задаче, разбор черновика. Вопросы
  в них задаёт диспетчер, ответ человек пишет ему же, и что там делали
  субагенты, человеку видеть незачем;
- сессии, поднятые без нажатия человека, в список не попадают вовсе,
  переключателя для них нет: прогон проверки из `shipctl` и тика `devkitctl
  watch` (`dashboard check`), проходы цикла цели (runs.go), делегирование
  `agentctl run`. Признак `hidden` ставит поднимающий в момент подъёма, а не
  разбор промпта постфактум;
- скрытие касается только списка. Вопрос такой сессии ложится признаком
  ожидания через хук ask-panel.py, и до него ведут полка ждущих (DK-696) и
  строка задачи на доске (живой статус строки и есть её чат, DK-430.6). Поиск
  такую запись тоже достаёт. Иначе спрятанный прогон повиснет молча с
  вопросом, а это ровно то, против чего затевалась цель DK-713;
- стенды (obeycheck, prompt-test) поднимаются рабочим каталогом вне проектов
  тем же приёмом, что служебные вызовы заголовков, и транскрипта в проекте не
  оставляют;
- накопившееся метится разом, разбором первой реплики: это разовая уборка, а не
  постоянный признак.

Проверять глазами: в списке остаются только заведённые человеком, а прогон
проверки с вопросом открывается с полки ждущих и со строки задачи.

Разбор 6 сентября сверил замер с машиной. В каталоге транскриптов devkit лежит
496 записей, за трое суток набежало 94. Человек завёл из них десяток. Рядом с
ними стоят прогоны сценариев проверки, груминги, стенды цели и подъёмы
исполнителей, а прогон DK-716 попал в список дважды.

Отсев по `hidden` стоит на сервере (chats.go, строка 859). Помеченная запись до
вкладки не доезжает, и поиск её не достанет, потому что отбор `chatDropOpen`
работает по тому, что сервер отдал. Кода, который ставит `hidden`, в дашборде
нет вовсе. Поле описано и читается, а пишут его руками.

Стенд послушания поднимается временным домом и своего следа в проекте не
оставляет. А прогоны судьи prompt-test 30 августа легли в каталог проекта
шестью записями.

## Чего хотим

## Развилки

- «делегирование»: сессии agentctl run (делегирование на GLM) прячем из списка или они остаются единственным окном в эту работу?
  - решает: человек
  - рекомендация: прятать вместе с остальными машинными: работа GLM видна строкой задачи и полкой ждущих
  - решено человеком 2026-09-06: прятать как остальные машинные: работа GLM ищется строкой задачи и полкой ждущих
- «поиск»: сейчас сервер отсеивает скрытые до клиента, и поиск их не увидит: доставать скрытые поиском в этой задаче или отдать это DK-832?
  - решает: человек
  - рекомендация: в этой задаче сервер отдаёт скрытые по запросу поиска, отдельный экран выдачи не рисуем
  - решено человеком 2026-09-06: скрытые достаются поиском в этой же задаче, отдельного экрана выдачи не рисуем
- «уборка»: накопившиеся 38 машинных записей метим разом разбором первой реплики или оставляем, пусть уедут окном свежести?
  - решает: человек
  - рекомендация: старое пометить архивом, а не hidden: ошибка разбора первой реплики тогда обратима вкладкой архива
  - решено человеком 2026-09-07: накопившееся уходит в архив, список на уборку сперва показывается человеку и согласуется

## DoD

- признак «поднято без человека» ставит сам поднимающий в момент подъёма:
  конвейер задачи, прогон проверки из `shipctl` и тика `devkitctl watch`,
  проходы цикла цели, делегирование `agentctl run`;
- список панели показывает разговоры, которые человек завёл сам, а
  переключателя на остальные в шапке нет;
- сервер отдаёт скрытые записи по запросу поиска, и найденная открывается
  лентой;
- вопрос спрятанной сессии виден полкой ждущих и строкой задачи на доске;
- прогоны судьи `prompt-test` поднимаются рабочим каталогом вне проектов;
- накопившееся уходит в архив, а не под признак: разбор первой реплики
  печатает список кандидатов, человек его смотрит и соглашается, и только
  после этого записи метятся архивом;
- тесты держат отсев списка, выдачу поиска и метку на каждой дороге подъёма;
- README дашборда называет признак и перечисляет дороги.

## Ранг

`25+7+2+0+2 = 36`, P2.

- серьёзность 25: список работает, обход у человека есть и он дешёвый, но
  трение заметное. За трое суток 94 записи, человеческих десяток, а убранное
  руками нарастает заново за неделю;
- ценность 7: панель это рабочее место, и чистый список ускоряет каждый заход
  к ней;
- неопределённость 2: устройство выбрано целиком, открыты детали дорог подъёма
  и стыка поиска с сервером;
- поправка на баг 0: список ведёт себя так с рождения, это доработка;
- рычаг 2: расчищенный список делает полку ждущих и строку задачи главной
  дорогой к прогону, и на это опирается DK-832.

Цена M: правка идёт по четырём точкам подъёма, полю памяти диалога, отдаче
списка и отбору поиска, плюс тесты и дока.

Приёмка смешанная, барьер «глаза». Механику отсева и поиска закрывают тесты по
разметке, а на живой машине человек сам смотрит, не спрятался ли нужный ему
разговор.

## Приёмка

- вид: mixed
- барьер «глаза»:

## Ход работы

- Из черновика DK-847 «Список чатов панели держит только разговоры человека»: записан 2026-09-06, оформлен 2026-09-06.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 13%, снимок 5м назад, сдвига нет), 2026-09-08 19:14-19:14.
