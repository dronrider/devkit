# DK-850: Экран архивной задачи в дашборде: тот же состав, что у живой, только без правки

## Что происходит

Закрыл задачу и открыл её экран в дашборде, чтобы посмотреть, какие задачи она
держала, и взять их в работу. Зависимостей на экране нет. Кроме них у архивной
строки пропало почти всё: тип, приоритет, цена, связи с LLD, план агента,
полоса действий. Остались файл постановки, дата закрытия и кнопка разговора.

Где теряется, по разведке:

- `tools/dashboard/tasks.go`, `handleTask`: у закрытой строки ранний выход с
  пустыми `after` и `blocks`, `taskDeps` и `taskLinks` не зовутся вовсе.
- `tools/dashboard/goal.go`, `archiveRows`: из шести колонок архива читаются
  две (заголовок и дата), тип и приоритет выбрасываются, дальше в строку
  подставляются `task` и `-`.
- `tools/dashboard/static/app.js`, `renderTask`, ветка `row.closed`: собирает
  страницу с `has: {file, read, chat}`, карточка зависимостей и связи не
  рисуются, `return` обрывает план агента и журнал витка.
- `tools/taskctl/dep.go`, `cmdDepList` и `json.go`, `cmdDepListJSON`: отказ
  «нет на доске» для любого ID вне доски, хотя `depSides` уже считает, кого
  держит архивная строка (маркеры `[после X]` живут на доске и целят в архив).
- `tools/taskctl/ops.go`, `cmdClose`: суффикс `[после ...]` в архивную строку не
  переносится и вычищается из чужих строк. Направление «от кого зависела»
  после закрытия не восстановимо из текстов доски и архива.
- `tools/taskctl/view.go`, `cmdShow`: у архива две строки без зависимостей и
  без пути файла постановки, `show --json` без `blocks` и `file`.

## Чего хотим

Экран архивной задачи показывает всё, что показывает экран живой: строку с
типом, приоритетом, ценой и датой закрытия, зависимости в обе стороны, связи,
файл постановки, план агента и журнал, полосу действий без правящих кнопок.
Разница одна: править архивную строку нельзя, и экран говорит об этом словами
там, где у живой стоит карандаш.

## Развилки

- «направление после»: хранит ли close суффикс [после ...] в архивной строке или экран честно пишет, что направление неизвестно?
  - решает: исполнитель
  - рекомендация: не хранить, экран говорит словами, что закрытие снимает маркер
  - решено исполнителем 2026-09-07: не хранить. Нужда после закрытия это «кого держала», и её доска знает по маркерам на живых строках. «После кого шла» это история, работы за ней нет. Суффикс в архивной строке это маркер ожидания там, где никто не ждёт, и вычищать его пришлось бы всем читателям заголовка архива (состав цели, поиск, show, человек с файлом). Старые закрытия остались бы без него при любом исходе, и слова «неизвестно» экрану нужны в обоих случаях. TestCloseStripsDeps остаётся как есть

## DoD

- `taskctl dep list <ID>` и `--json` отвечают по архивному ID: кого держит
  (строки доски с маркером на этот ID) и после кого шла, если известно.
- `taskctl show <ID>` для архива печатает зависимости и путь файла постановки,
  `show --json` отдаёт `blocks` и `file`.
- Ручка `GET /api/projects/{p}/tasks/{id}` для закрытой строки отдаёт `after`,
  `blocks`, `links` и полную строку архива (тип, P, дата, ссылка).
- Ветка `row.closed` в `renderTask` рисует карточку зависимостей, связи, план
  агента и журнал, полоса действий без кнопок правки, на месте карандаша
  пояснение «архив, правки нет».
- Направление «после кого» у архивной строки: либо `close` сохраняет суффикс в
  архивной строке, либо экран честно пишет, что для старых закрытий оно
  неизвестно. Развилку решает исполнитель и записывает исход здесь.
- Тесты: `runs_test.go` (экран закрытой задачи с `after`/`blocks`),
  `tasks_test.go` (разбор ветки `row.closed`), `dep_test.go` и `json_test.go`
  (dep list по архивному ID), `ops_test.go` (show для архива). Тест бага
  красный на старом коде через regcheck.
- README дашборда и taskctl: абзац про экран архивной задачи и dep list по
  архиву.

## Ранг

- Серьёзность 25: заметное трение, обход дешёвый: грепнуть доску по
  `[после <ID>]` или позвать `taskctl dep list` без ID.
- Ценность 6: операция после каждого закрытия, просят вслух.
- Неопределённость 3: подход ясен, развилка про хранение «после» в архиве.
- Баг +5: экран обещает задачу, а показывает обрубок.
- Рычаг 4: следующая работа берётся сразу с экрана закрытой, без обхода.

## Ход работы

- Разработка: субагент fable/high по вердикту pick (маппинг opus, корректор: профицит week_all, week_max; квота: week_all 76% профицит, week_max 37% профицит (панель разбивку не дала, цифра из кеша клиента возрастом 23ч 39м назад), снимок 30м назад), 2026-09-07 09:21-09:21.
- Разведка подтвердилась по коду во всех шести местах. Правка пошла двумя
  коммитами. В taskctl `dep list`, `show` и их `--json` отвечают по архивному
  ID, появились помощники `Archive.find`, `archiveTaskFile` и слово
  `archiveAfterNote`. В дашборде `archiveRows` читает тип, P и ссылку,
  `taskRowOf` собирает из них строку, `handleTask` у закрытой строки зовёт
  `taskDeps` и `taskLinks`, а `taskDeps` несёт `after_note`. Ветка
  `row.closed` в `renderTask` рисует чипы, карточку зависимостей чтением,
  связи, план агента, журнал у цели и `penOff` вместо карандаша. `cmdClose`
  не тронут, `TestCloseStripsDeps` остался.
- Тесты `TestDepListArchived`, `TestDepListJSONArchived`,
  `TestShowJSONArchiveDepsAndFile`, `TestShowArchiveDepsAndFile` в taskctl,
  `TestTaskClosedRowFullScreen` и `TestStaticTaskClosedScreen` в дашборде.
  Краснота на старом коде проверена `regcheck` по каждому пакету, полные
  прогоны `GOWORK=off go test .` обоих пакетов зелёные.
- Цена и ранг на экране закрытой задачи не показываются. В архиве этих колонок
  нет, а подставлять значения живой строки значило бы выдумывать.

## Ревью

Уровень 3 до 1e90102: неопределённость 3, тронут tools/taskctl и путь DoD в dashboard

- [возврат: реализация, 2026-09-07] не блокирует: ошибка dep list роняет весь экран закрытой задачи. taskDeps в handleTask зовётся до ветки row.Closed, и отказ уходит ответом 502. Со старым taskctl рядом с бинарём дашборда экран закрытой задачи не откроется вовсе. Прежде он открывался обрубком. Проверено в main: dep list DK-615 --json отвечает «DK-615 нет на доске». Предлагаю у закрытой строки отказ taskDeps в ответ не поднимать. Экран отдаётся без зависимостей, причина едет словами в after_note: исправлено, отказ taskDeps у закрытой строки не поднимается, экран без зависимостей с причиной в after_note, тест TestTaskClosedRowSurvivesDepListRefusal, коммит 374b8dd10
- [возврат: реализация, 2026-09-07] не блокирует: закрытая цель на экране не помечена целью. Чип «цель» в ветке row.closed не ставится, у живой цели он есть. closedGoal уже посчитан, журнал витка по нему рисуется. Состав экрана расходится с заказом «тот же, что у живой». Проверить на закрытой цели #devkit/DK-109: исправлено, чип «цель» в ветке row.closed, проверка в TestStaticTaskClosedScreen, коммит 374b8dd10

## Сценарий проверки

Вид агентский, команды печатают проверяемые строки.

1. Утилита отвечает по архивному ID. На пустой доске заводится строка XR-001,
   в архив кладётся XR-009, XR-001 ставится после XR-009.

```sh
T=$(mktemp -d) && cd "$T" && git init -q . && taskctl init --prefix XR --name demo >/dev/null
taskctl add --id XR-001 --title "Живая" --rank 25+0+0+0+0 --accept agent >/dev/null
printf '| XR-009 | Закрытая | bug | P1 | 2026-08-13 | - |\n' >> docs/TASKS-archive.md
mkdir -p docs/tasks/archive/2026 && printf '# XR-009\n' > docs/tasks/archive/2026/XR-009.md
taskctl dep add XR-001 XR-009 >/dev/null
taskctl dep list XR-009
taskctl dep list XR-009 --json
taskctl show XR-009
taskctl show XR-009 --json
```

   Ожидание. `dep list` печатает `XR-009 после: неизвестно (...)` и
   `XR-009 держит: XR-001`. В `--json` есть `"blocks":["XR-001"]`,
   `"archived":true` и `"after_note"`, ключа `after` нет. `show` печатает
   `держит: XR-001` и `файл задачи: docs/tasks/archive/2026/XR-009.md`, в
   `--json` есть `"blocks"` и `"file"`.

2. Ручка и экран дашборда. Тесты пакетов гоняют настоящий taskctl на
   фикстурной доске и проверяют ответ `GET /tasks/{id}` по закрытой строке и
   ветку `row.closed` в `static/app.js`.

```sh
cd ~/projects/devkit/tools/taskctl && GOWORK=off go test -run 'TestDepListArchived|TestDepListJSONArchived|TestShowJSONArchiveDepsAndFile|TestShowArchiveDepsAndFile|TestCloseStripsDeps' .
cd ~/projects/devkit/tools/dashboard && GOWORK=off go test -run 'TestTaskClosedRowFullScreen|TestStaticTaskClosedScreen|TestRunStartClosedRowNamesArchive' .
```

   Ожидание. Обе команды печатают `ok`.

3. Глазами, по желанию: открыть в дашборде экран любой закрытой задачи
   (`#devkit/DK-615`). На экране чип «закрыта <дата>», чип приоритета,
   карточка зависимостей без кнопок «Добавить» и «Снять», связи, файл
   постановки, карандаш погашен с подсказкой «Задача в архиве, правки нет».
