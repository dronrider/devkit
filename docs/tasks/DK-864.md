# DK-864: Вопрос человеку приходит текстом в чате, а не виджетом AskUserQuestion

## Что происходит

Разобрано с пользователем 7 сентября в разговоре chat-8. Сейчас вопрос
ходит двумя дорогами. В терминале это диалог AskUserQuestion. В сессии панели
хук hooks/ask-panel.py перехватывает тот же вызов и переливает его в признак
.ask через taskctl ask. Панель рисует блок с кнопками. Рекомендация по дороге
теряется. У AskUserQuestion нет поля рекомендации, есть только приписка
«(Recommended)» в подписи варианта, которую агент ставит, когда вспомнит.
Источник вопроса богаче виджета. В разделе «Развилки» записи лежат вопрос,
рекомендация и довод, их пишет taskctl decide --ask --hint до того, как вопрос
задан.

Пять претензий пользователя к виджету. Рекомендация варианта часто не
показывается, хотя в тексте рядом она есть. Читать неудобно, виджет не похож
на остальную ленту. Вопросы разбрасываются по табам, тексты объёмные. Форма
мешает смотреть чат. Работа с чатом заперта до ответа. Слова пользователя:
«совершенно неудобный, даже раздражающий».

### Проверка при разборе

Предмет на месте 8 сентября. Хук hooks/ask-panel.py лежит и раскладывается
доктором (tools/devkitctl/devkitctl.py, строка PreToolUse с матчером
AskUserQuestion). Вызов виджета предписан агентам в десяти местах: четыре
скилла (interview, board-groom, board-task, review), четыре определения
exec-агентов и два сценария стенда, 12 и 13.

Разбор поправил две вещи из гипотезы. Снимок tmux разбирается не только под
виджет вопроса. Файл tools/dashboard/clientlogin.go читает тем же parseTmuxAsk
вопрос клиента про доверие каталогу и вход по /login, и эта половина остаётся.
У развилки в internal/taskform/forks.go сейчас два поля, вопрос и рекомендация,
а вариантов нет вовсе. Нумерованный список брать неоткуда. Его либо заводят
полем записи, либо собирают из текста, и выбор остаётся исполнителю.

Печать открытого края стоит. При ключе --open decideShow
(tools/taskctl/decide.go) печатает незакрытые развилки строками с
рекомендацией. Работа сводится ко второму формату вывода.

## Чего хотим

Вопрос в чате это печать открытого края перечня развилок записи. Блок
печатает утилита (taskctl decide <ID> --open --chat или соседний ключ), агент
вставляет его в реплику как есть. Формат один на все сессии. У развилки имя,
у варианта номер, рекомендованный вариант идёт первым и помечен словом. Довод
одной фразой, четыре вопроса на пачку, подробности живут в файле записи. Ответ
человека одной строкой, «выкат 2, правило 1» или словами. Отдельно стоит «по
рекомендации», оно закрывает пачку рекомендованным. В сессии панели та же
команда кладёт признак .ask и паркует строку. Кнопки панели остаются и шлют в
чат ту же строку ответа. Хук ask-panel.py уходит целиком, а разбор снимка tmux
урезается до вопросов самого клиента. Вызов AskUserQuestion уходит из всех
входов: interview, board-task, board-groom, review, определения exec-агентов,
сценарии obeycheck 12 и 13.

## Границы

Механику виджета строили две недавние задачи, DK-652 и DK-715. DK-864
разворачивает их решение назад к тексту. Живучесть вопроса до ответа и адрес
ответа, ради которых заводили признак ожидания, остаются, уходит только виджет
как носитель.

Смежный черновик DK-839 (заказ конвейера перекрывает открытый вопрос из
Backlog) разбирается отдельно, его предмет это парковка строки и чтение
признака оболочкой. Обе задачи трогают один код (`tools/taskctl/ask.go`,
`hooks/ask-panel.py`), поэтому порядок между ними назначает диспетчер.

Откуда берутся пронумерованные варианты, разбор оставил исполнителю. Варианты
заведены полем записи. У развилки появилась подстрока `вариант: <ответ>`, по
строке на вариант, кроме рекомендованного. Рекомендация лежит своей подстрокой
и идёт в блок первым номером. Сборка вариантов из текста делила бы вопрос по
слову «или», и номер, которым отвечает человек, зависел бы от формулировки.
Форма подстроки описана в TASKFORM.md.

Решение развилки «панель» уточнено развилкой «галочки»: блока с кнопками не
остаётся вовсе, вместо него в реплике появляются галочки. Строку варианта
панель узнаёт по номеру в начале строки, поэтому в терминале и в уведомлении
тот же вопрос приходит слово в слово, только без галочек.

## Развилки

Развилки разобраны с пользователем в чате, ответ «по рекомендации» по всем
четырём. Перечень ниже, ответы уже стоят.

- «печать»: кто собирает текст вопроса в чат: утилита или агент по шаблону скилла?
  - решает: человек
  - рекомендация: утилита печатает блок из перечня развилок, агент вставляет как есть
  - решено человеком 2026-09-08: утилита, ответ «по рекомендации» 8 сентября
- «ответ»: какой формы ответ человека?
  - решает: человек
  - рекомендация: имя развилки и номер варианта, словами тоже можно, «по рекомендации» закрывает пачку
  - решено человеком 2026-09-08: имя и номер, слова, «по рекомендации»; ответ «по рекомендации» 8 сентября
- «панель»: что делает блок с кнопками на панели дашборда?
  - решает: человек
  - рекомендация: остаётся, кнопка шлёт в чат строку ответа того же формата
  - решено человеком 2026-09-08: остаётся, кнопка шлёт строку ответа; ответ «по рекомендации» 8 сентября
- «терминал»: где остаётся диалог AskUserQuestion?
  - решает: человек
  - рекомендация: нигде, все входы спрашивают текстом
  - решено человеком 2026-09-08: нигде; ответ «по рекомендации» 8 сентября
- «галочки»: Как человек отвечает на вопрос в панели: галочками, собирающими текст ответа в поле ввода, или прежней кнопкой, шлющей ответ сразу?
  - решает: человек
  - рекомендация: галочки в блоке вопроса вместо табов, отметка собирает строку ответа в поле ввода, отправляет обычная кнопка чата
  - решено человеком 2026-09-08: галочки слева от строк вариантов в самой реплике, без блока, табов и своей кнопки; отметка собирает строку ответа в поле ввода, отправляет обычная кнопка чата

## DoD

Вопрос человеку приходит текстом в ленте чата, одинаково в терминале, в окне
панели и у субагента. Блок печатает утилита из перечня развилок записи: имя
развилки, пронумерованные варианты, рекомендованный первым и назван словом,
довод одной фразой, четыре развилки на пачку. Ответ человека принимается в
трёх видах: имя развилки с номером, слова своей речью и «по рекомендации»,
закрывающее пачку. Панель по признаку ожидания ставит галочку слева от каждой
строки варианта прямо в реплике, и добавляет она только галочку: ни рамки, ни
шапки, ни табов, ни своей кнопки отправки. Отметка дописывает строку ответа в
поле ввода, снятая отметка её оттуда убирает, а отправляет обычная кнопка
чата. Вызова AskUserQuestion не остаётся
ни в одном скилле, определении субагента и сценарии стенда. Хук ask-panel.py
удалён вместе со своим тестом, а от разбора снимка tmux остаётся половина под
вопросы самого клиента.

## Ранг

`25+7+3+0+3 = 38`, P2.

- серьёзность 25: вопрос до человека доходит и ответ доезжает, а обход
  дешёвый, агент ставит в подписи варианта приписку «(Recommended)», когда
  вспомнит. Форма мешает работать с чатом, и часть разбора теряется по дороге;
- ценность 7: спрашивают человека в каждом груминге, постановке и ревью, то
  есть по нескольку раз в день, и рекомендация с доводом доезжают до него
  целиком;
- неопределённость 3: все четыре развилки закрыты человеком 8 сентября, но
  правку везут через три границы разом (сервер дашборда, его фронт, хуки и
  утилита). Вариантов у развилки в форме записи нет, и разбор не решил, откуда
  берётся их нумерация;
- поправка на баг 0: механику меняем сознательно, сломанного тут нет;
- рычаг 3: разговор с человеком идёт из каждого скилла постановки, а снятие
  разбора снимка tmux убирает хрупкое место, но следующую задачу это дешевле
  не делает.

Цена L: правка идёт по четырём слоям сразу, от печати блока в taskctl и
признака ожидания до панели с её фронтом, скиллов, определений субагентов и
сценариев стенда. Замер разбора назвал объём: около шестисот строк разбора
снимка в tools/dashboard/tmux.go с их тестами, из которых часть останется под
вопросы самого клиента, 247 строк хука, десяток README и два сценария стенда.

## Приёмка

- вид: mixed
- барьер «согласие»: новая форма вопроса принимается человеком, задача затеяна
  из-за неё, и «читается лучше виджета» это его слово, а не проверка факта
  - субагент-критик: не годится, находки по формату он даёт, а согласия нет

Барьер накрывает один шаг из сценария, остальное проверяется с машины: печать
блока из перечня развилок, разбор трёх видов ответа, отсутствие вызовов виджета
в дереве и прогон сценариев стенда 12 и 13.

## Ход работы

- Из черновика DK-864 «Вопрос человеку задаётся текстом в чате, а не виджетом AskUserQuestion»: записан 2026-09-08, оформлен 2026-09-08.
- Вычитка: 1 файл, 12 правок, 2 пометки, 2026-09-08.
- Уточнение: вопрос: Как человек отвечает на вопрос в панели дашборда?, 2026-09-08 09:37-10:45.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 7%, week_max 8% (панель разбивку не дала, цифра из кеша клиента возрастом 4ч 32м назад), снимок 43м назад, сдвига нет), 2026-09-08 10:45-10:45.
- Вычитка: 7 файлов, 22 правки, 0 пометок, 2026-09-08.
- Вычитка: 1 файл, 8 правок, 2 пометки, 2026-09-08.
- Вычитка: 13 файлов, 8 правок, 0 пометок, 2026-09-08.

## Ревью

Уровень 3 до d948520: стартовый 2 от диспетчера, триаж: дифф трогает critical_paths tools/taskctl и hooks -> да, до d948520f6

- [возврат: правила, 2026-09-08] блокирует: правка kit/skills/review/SKILL.md раздула тело скилла до 16162 символов при пороге 16000, python3 -m unittest tools.devkitctl.weigh_test.MeasureTest.test_14_live_skills_are_under_the_limit падает (доктор требует резать скилл надвое); поправить текст этого скилла (или другого раздела review) так, чтобы тело влезло в порог, и перепрогнать тест - исправлено: текст скилла ужат многословием (абзацы про taskctl decide --chat и ещё десяток фраз по всему файлу), тело стало 15793 символа, test_14_live_skills_are_under_the_limit и весь weigh_test зелёные: исправлено
- Вердикт: без замечаний. круг 2 до 53028bcd9: fixed 1 сверено прогоном weigh_test.MeasureTest (37 тестов зелёные) и devkitctl_test.HarnessHooksTest.test_gone_hook_is_dropped, тело review/SKILL.md 27294 байт; порядок decide --ask/--chat/--answer цел в tools/taskctl/README.md; остальной дифф раунда (docs/tasks/DK-864.md, hooks/README.md, tools/dashboard/README.md, asksay_test.go, devkitctl.py/_test.py) только проза и легитимный фикс доктора для снятого хука, новых находок нет

## Сценарий проверки

Вид приёмки mixed. Шаг 6 накрыт барьером «согласие». Новую форму вопроса
принимает человек глазами, и «читается лучше виджета» это его слово. Остальные
пять шагов машинные, и гоняет их обкатка.

1. Блок вопроса собирается из перечня развилок записи: имя развилки, номера у
   вариантов, рекомендованный первым и назван словом, четыре развилки на пачку.

```sh
GOWORK=off go -C tools/taskctl test -run 'TestChatBlock|TestChatPanelWritesAsk' -v ./...
```

Ожидание: `PASS`, четыре теста. `TestChatBlockNumbersOptions` держит номера и
слово рекомендации, `TestChatBlockHoldsPackLimit` потолок пачки,
`TestChatBlockWithoutForks` молчание при пустом крае, `TestChatPanelWritesAsk`
признак ожидания в сессии панели.

2. Ответ человека разбирается в трёх видах: имя развилки с номером, «по
   рекомендации» на всю пачку и свободные слова, которые остаются агенту.

```sh
GOWORK=off go -C tools/taskctl test -run TestAnswer -v ./...
```

Ожидание: `PASS`, шесть тестов. `TestAnswerClosesByNameAndNumber`,
`TestAnswerByRecommendation` и `TestAnswerLeavesWordsToAgent` держат три вида,
`TestAnswerRefusesUnknownNumber` отказ на номер за пределами списка.

3. Вызова виджета не осталось ни в одном скилле, определении субагента и
   сценарии стенда, а хук `ask-panel.py` удалён вместе со своим тестом.

```sh
grep -rl "AskUserQuestion" kit hooks/*.py tools/obeycheck/scenarios 2>/dev/null; echo "мест с вызовом виджета: $(grep -rl "AskUserQuestion" kit hooks/*.py 2>/dev/null | wc -l | tr -d ' ')"; ls hooks/ask-panel.py hooks/ask_panel_test.py 2>&1 | head -2
```

Ожидание: список пуст, кроме двух сценариев стенда, где слово стоит в самой
проверке. Строка «мест с вызовом виджета: 0» и два отказа `No such file or
directory` на хук и его тест.

4. От разбора снимка панели tmux осталась половина под вопросы самого клиента,
   и она работает: доверие каталогу с номерами и без них, вход по `/login`,
   рубеж от чужих нумерованных списков.

```sh
GOWORK=off go -C tools/dashboard test -run 'TestParseTmuxAsk|TestTmuxAnswer|TestClientLogin' -v ./...
```

Ожидание: `PASS`. `TestParseTmuxAskBareTrust` и `TestTmuxAnswerBareTrustKeys`
держат вопрос доверия у свежего клиента, `TestParseTmuxAskNotWidget` рубеж от
эха реплики и списков агента.

5. Панель ставит галочку слева от строки варианта прямо в реплике, а прежнего
   блока с кнопками и табами в статике не осталось.

```sh
GOWORK=off go -C tools/dashboard test -run 'TestStaticAskPicksInReply|TestStaticAgentAskWidgetGone' -v ./...
```

Ожидание: `PASS` на обоих. Стенд галочек проходит по `static/app.js`, а
`TestStaticAgentAskWidgetGone` не находит там ни `paintAgentAsk`, ни
`askStepShell`, ни `ask.steps`.

6. Барьер «согласие». Шаг человеческий, и обкатка его не гоняет. Человек
   поднимает разговор по записи с открытой человеческой развилкой, зовёт печать
   блока и смотрит на реплику глазами.

```
taskctl decide <ID> --chat
```

Ожидание: блок читается как обычная реплика ленты, рекомендация видна словом
при первом номере, галочки стоят слева от строк вариантов. Дальше человек
говорит, лучше ли это виджета.

## Проверка

- Стенд: 2026-09-08 16:07, дерево 3fcb82ec6, предмет a1ae1ee1, база старый, ярус mini, k 3, сценарии 12-ask-human, 13-groom-ask, зачтён.

```console
$ obeycheck --task DK-864 --only 12-ask-human,13-groom-ask -k 3 --base старый /Users/rider/projects/rules-864-with /Users/rider/projects/rules-864-old
сценарий                                rules-864-with  rules-864-old  вердикт
спросить человека посреди работы                   1/3            0/3  не хуже
спросить человека на разборе черновика             3/3            0/3  польза

итог: сценариев 2, все зачтены, база старый

первый красный прогон каждого угла:
  12-ask-human / rules-864-with / повтор 2: проверка: exit status 1 (вопрос мимо записи: человеческой развилки в docs/tasks/OB-002.md нет)
  12-ask-human / rules-864-old / повтор 1: проверка: exit status 1 (вопрос задан виджетом: AskUserQuestion звался)
  13-groom-ask / rules-864-old / повтор 1: проверка: exit status 1 (вопрос на разборе задан виджетом: AskUserQuestion звался)
```

- Обкатка: 2026-09-08 16:28, свежее дерево 21fb9a1bbe83, сценарий 276b38c8, временный HOME, утилит дерева 11, шагов 5, все зелёные.

Шаг 1 (зелёный):

```console
$ GOWORK=off go -C tools/taskctl test -run 'TestChatBlock|TestChatPanelWritesAsk' -v ./...
=== RUN   TestChatBlockNumbersOptions
--- PASS: TestChatBlockNumbersOptions (0.01s)
=== RUN   TestChatBlockHoldsPackLimit
--- PASS: TestChatBlockHoldsPackLimit (0.01s)
=== RUN   TestChatBlockWithoutForks
--- PASS: TestChatBlockWithoutForks (0.00s)
=== RUN   TestChatPanelWritesAsk
--- PASS: TestChatPanelWritesAsk (0.01s)
PASS
ok  	github.com/dronrider/devkit/tools/taskctl	0.523s
```

Шаг 2 (зелёный):

```console
$ GOWORK=off go -C tools/taskctl test -run TestAnswer -v ./...
=== RUN   TestAnswerClosesByNameAndNumber
--- PASS: TestAnswerClosesByNameAndNumber (0.00s)
=== RUN   TestAnswerByRecommendation
--- PASS: TestAnswerByRecommendation (0.00s)
=== RUN   TestAnswerLeavesWordsToAgent
--- PASS: TestAnswerLeavesWordsToAgent (0.00s)
=== RUN   TestAnswerRefusesUnknownNumber
--- PASS: TestAnswerRefusesUnknownNumber (0.00s)
=== RUN   TestAnswerHoldsRecommendationNextToWords
--- PASS: TestAnswerHoldsRecommendationNextToWords (0.00s)
=== RUN   TestAnswerNamesLongestFork
--- PASS: TestAnswerNamesLongestFork (0.00s)
PASS
ok  	github.com/dronrider/devkit/tools/taskctl	0.488s
```

Шаг 3 (зелёный):

```console
$ grep -rl "AskUserQuestion" kit hooks/*.py tools/obeycheck/scenarios 2>/dev/null; echo "мест с вызовом виджета: $(grep -rl "AskUserQuestion" kit hooks/*.py 2>/dev/null | wc -l | tr -d ' ')"; ls hooks/ask-panel.py hooks/ask_panel_test.py 2>&1 | head -2
tools/obeycheck/scenarios/13-groom-ask.md
tools/obeycheck/scenarios/12-ask-human.md
мест с вызовом виджета: 0
ls: hooks/ask-panel.py: No such file or directory
ls: hooks/ask_panel_test.py: No such file or directory
```

Шаг 4 (зелёный):

```console
$ GOWORK=off go -C tools/dashboard test -run 'TestParseTmuxAsk|TestTmuxAnswer|TestClientLogin' -v ./...
=== RUN   TestClientLoginStartGivesLink
--- PASS: TestClientLoginStartGivesLink (0.31s)
=== RUN   TestClientLoginCodeAccepted
--- PASS: TestClientLoginCodeAccepted (0.32s)
=== RUN   TestClientLoginTrustAskedAndPassed
--- PASS: TestClientLoginTrustAskedAndPassed (0.37s)
=== RUN   TestClientLoginNoKeysIntoBlankPane
--- PASS: TestClientLoginNoKeysIntoBlankPane (0.29s)
=== RUN   TestClientLoginCodeRejected
--- PASS: TestClientLoginCodeRejected (0.38s)
=== RUN   TestClientLoginLinkMissingSaysWords
--- PASS: TestClientLoginLinkMissingSaysWords (0.13s)
=== RUN   TestClientLoginGoneMidWay
--- PASS: TestClientLoginGoneMidWay (0.30s)
=== RUN   TestClientLoginEmptyCode
--- PASS: TestClientLoginEmptyCode (0.23s)
=== RUN   TestClientLoginWrappedLinkJoins
--- PASS: TestClientLoginWrappedLinkJoins (0.23s)
=== RUN   TestClientLoginHardWrappedLinkJoins
--- PASS: TestClientLoginHardWrappedLinkJoins (0.23s)
=== RUN   TestClientLoginSweepDropsExpired
--- PASS: TestClientLoginSweepDropsExpired (0.47s)
=== RUN   TestClientLoginSiblingLinkNotGlued
--- PASS: TestClientLoginSiblingLinkNotGlued (0.23s)
=== RUN   TestClientLoginHashAfterLinkNotGlued
--- PASS: TestClientLoginHashAfterLinkNotGlued (2.13s)
=== RUN   TestClientLoginSurvivesMobileOAuth
--- PASS: TestClientLoginSurvivesMobileOAuth (0.34s)
=== RUN   TestClientLoginAliveFromLastTouch
--- PASS: TestClientLoginAliveFromLastTouch (0.41s)
=== RUN   TestClientLoginAdoptedAfterRestart
--- PASS: TestClientLoginAdoptedAfterRestart (0.42s)
=== RUN   TestClientLoginOrphanSweptAfterRestart
--- PASS: TestClientLoginOrphanSweptAfterRestart (0.27s)
=== RUN   TestClientLoginLinkRefusalSaysFrame
--- PASS: TestClientLoginLinkRefusalSaysFrame (2.11s)
=== RUN   TestClientLoginLinkRefusalSurvivesASlowTmuxCall
--- PASS: TestClientLoginLinkRefusalSurvivesASlowTmuxCall (2.10s)
=== RUN   TestClientLoginStrangerNotAdopted
--- PASS: TestClientLoginStrangerNotAdopted (0.33s)
=== RUN   TestClientLoginCodeRefusedToStranger
--- PASS: TestClientLoginCodeRefusedToStranger (0.45s)
=== RUN   TestClientLoginCodeErrorNotSuccess
--- PASS: TestClientLoginCodeErrorNotSuccess (0.32s)
=== RUN   TestClientLoginCodeStuckNotSuccess
--- PASS: TestClientLoginCodeStuckNotSuccess (0.30s)
=== RUN   TestClientLoginLocalWayNoCode
--- PASS: TestClientLoginLocalWayNoCode (0.20s)
=== RUN   TestClientLoginProxiedWayKeepsCode
--- PASS: TestClientLoginProxiedWayKeepsCode (0.18s)
=== RUN   TestParseTmuxAsk
--- PASS: TestParseTmuxAsk (0.00s)
=== RUN   TestTmuxAnswerBlindWidgetRefuses
--- PASS: TestTmuxAnswerBlindWidgetRefuses (0.00s)
=== RUN   TestParseTmuxAskNotWidget
=== RUN   TestParseTmuxAskNotWidget/эхо_реплики_человека
=== RUN   TestParseTmuxAskNotWidget/ответ_агента_списком
=== RUN   TestParseTmuxAskNotWidget/список_задач_в_ответе_агента
=== RUN   TestParseTmuxAskNotWidget/тот_же_список_без_подсказки_навигации
--- PASS: TestParseTmuxAskNotWidget (0.00s)
    --- PASS: TestParseTmuxAskNotWidget/эхо_реплики_человека (0.00s)
    --- PASS: TestParseTmuxAskNotWidget/ответ_агента_списком (0.00s)
    --- PASS: TestParseTmuxAskNotWidget/список_задач_в_ответе_агента (0.00s)
    --- PASS: TestParseTmuxAskNotWidget/тот_же_список_без_подсказки_навигации (0.00s)
=== RUN   TestParseTmuxAskStillWidget
--- PASS: TestParseTmuxAskStillWidget (0.00s)
=== RUN   TestParseTmuxAskPrintedAboveWidget
--- PASS: TestParseTmuxAskPrintedAboveWidget (0.00s)
=== RUN   TestParseTmuxAskBareTrust
--- PASS: TestParseTmuxAskBareTrust (0.00s)
=== RUN   TestParseTmuxAskBareNotWidget
=== RUN   TestParseTmuxAskBareNotWidget/тот_же_вопрос_без_подсказки
=== RUN   TestParseTmuxAskBareNotWidget/реплика_человека_и_текст_под_ней
--- PASS: TestParseTmuxAskBareNotWidget (0.00s)
    --- PASS: TestParseTmuxAskBareNotWidget/тот_же_вопрос_без_подсказки (0.00s)
    --- PASS: TestParseTmuxAskBareNotWidget/реплика_человека_и_текст_под_ней (0.00s)
=== RUN   TestTmuxAnswerBareTrustKeys
=== RUN   TestTmuxAnswerBareTrustKeys/доверяю
=== RUN   TestTmuxAnswerBareTrustKeys/не_доверяю
--- PASS: TestTmuxAnswerBareTrustKeys (0.03s)
    --- PASS: TestTmuxAnswerBareTrustKeys/доверяю (0.02s)
    --- PASS: TestTmuxAnswerBareTrustKeys/не_доверяю (0.01s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	13.600s
```

Шаг 5 (зелёный):

```console
$ GOWORK=off go -C tools/dashboard test -run 'TestStaticAskPicksInReply|TestStaticAgentAskWidgetGone' -v ./...
=== RUN   TestStaticAskPicksInReply
    agentask_test.go:185: poc_askpick: галочка при каждой строке варианта, строка ответа в поле ввода, снятая отметка её уносит, прежнего блока с кнопками нет
--- PASS: TestStaticAskPicksInReply (0.09s)
=== RUN   TestStaticAgentAskWidgetGone
--- PASS: TestStaticAgentAskWidgetGone (0.00s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	0.473s
```
