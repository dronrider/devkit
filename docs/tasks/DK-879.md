# DK-879: Служебный чат подписан задачей и ролью без haiku, чип не дублирует номер, полоса чипов уже

## Что происходит

Дашборд называет чаты лестницей titleFor в tools/dashboard/chats.go. Сначала берётся запись summary транскрипта, потом сохранённый заголовок в ~/.devkit/chats, потом эвристика titleTrim. Фоном идёт заказ у haiku отдельным процессом `claude -p --model haiku` из каталога ~/.devkit/titles.

Замер сделан 2026-09-09 по этой машине. Транскриптов с записью summary ноль из 3373, с записью ai-title 1743. Заказов haiku по dashboard.log с 1 сентября 308. Ещё 17 транскриптов заказа лежат в ~/.claude/projects под именами тестовых каталогов. Вызова titleAsk из go-тестов при разборе 2026-09-22 не нашлось, транскрипты под каталогами проб оставили ручные пробы дашборда, а не `go test`.

Claude Code сам пишет запись `{"type":"ai-title","aiTitle":"..."}` сразу после первой реплики человека, до первого ответа модели. У интерактивных сессий она есть почти всегда. На подписке glm-code она тоже есть, и генерирует её endpoint подписки. У сессий печатного режима `-p` записи нет. Команда `/rename` и флаг `claude --name` пишут запись `custom-title`. Флаг проверен и в печатном режиме. Шапку читает readSessionHead в sessions.go, и чтение обрывается после Branch и First. До ai-title оно не доходит.

У других агентов та же развилка. OpenCode и Goose называют сессию своей дешёвой моделью на первой реплике. Codex CLI и Gemini CLI показывают первую реплику. Второй процесс поверх харнеса ради заголовка не поднимает никто.

### Проверка на разборе

Сверил 2026-09-22 на этой машине, клиент 2.1.263. В каталоге транскриптов devkit 548 файлов, записей `summary` ноль, `ai-title` в 429, `custom-title` в одном (проба `claude --name`). В dashboard.log с 10 августа 518 записей «назван haiku», среди них и чаты, поднятые самим дашбордом: заказ «Проведи груминг DK-920» haiku переименовал в «Проведи груминг черновика задачи DK-920», а прогон Check в «Запуск агентской проверки выкаченного кода». Флаг `-n, --name <name>` в справке клиента есть. Дашборд ни `ai-title`, ни `custom-title` не читает и `--name` не ставит (grep по tools/dashboard пустой).

Дополнение от 2026-09-22. У сессий, которые дашборд поднимает сам, предмет известен до первого токена: груминг, выполнение и продолжение задачи, цель, прогон Check. Имя таким чатам считается из ID и роли заказа, а не спрашивается у модели: тогда все заходы одной задачи подписаны одинаково, заголовок короткий и умещается в строку списка на смартфоне, а квота на украшение не тратится. Чат, поднятый рукой из терминала словами заказа («Выполни DK-5»), флага не получает: узнавать его по тексту значит подписывать именем работы любой разговор со случайно совпавшими словами, и такой чат остаётся с именем из транскрипта.

Замечания пользователя 2026-09-22 по примерам. Номер задачи в заголовке дублирует чип задачи той же строки, чип на смартфоне громоздкий и отнимает место у заголовка, а заголовок из одного слова роли без номера непонятен. Есть чаты с номером в заголовке и без чипа. Замер по живому списку из 79 чатов: у 14 строк номер в заголовке и чип, у 7 номер без чипа. Чип собирает ownTasks из касаний реестра (задачи, которых сессия касалась командами доски) плюс ID из имени дерева, а привязка запуска, которой дашборд сам поднял сессию под задачу (bindTask, tmux task-DK-972 и chat-DK-909-1), в чипы не едет: у сессии цели DK-909 и у мёртвой сессии выполнения DK-972 чипа нет при заголовке с номером.

Заголовок появляется с задержкой на подъём отдельного клиента, а заказов на заход списка по два. Каждый заказ жжёт токены подписки по умолчанию на украшение списка. Механика привязана к одному поставщику, и чат на glm-code называется haiku с claude-code. Заказы из тестов тратят квоту на каждом прогоне.

Обход 2026-09-22 нашёл три сборщика команды клиента, а не один: chatCmd для чатов, groomCmd для груминга и профиль `[head]` харнеса (internal/taskhead, kit/harness/*.toml) для конвейера задачи, прогона Check и второго круга ревью; флаг ложится во все три, а у профиля это новый ключ TOML. Чтение шапки транскрипта (readSessionHead) обрывается на первой реплике человека, а запись ai-title лежит после первого ответа модели, и дочитывать надо до неё. Порядок лестницы после правки: custom-title, ai-title, summary, эвристика первой реплики.

## Чего хотим

Чат в списке дашборда получает заголовок через секунды после первой реплики без отдельного процесса и без токенов дашборда. Сессии, поднятые дашбордом с известным предметом, названы до первого токена. Тем же именем они видны в окне терминала и в списке `claude --resume`. Заголовок чата второй подписки пишет её же модель. Тесты дашборда в живого клиента не ходят, и сторож этого стоит в самих тестах.

## Границы

Совместимость `--name` с `--resume` проверяется пробой на клиенте до правки продолжения: у резюма имя уже есть в транскрипте, и флаг там либо лишний, либо переписывает custom-title. Имя чата и заголовок строки доски это разные поля, регулярки по «Цель:» в app.js смотрят на строку доски и правкой не трогаются.

## Развилки

- «кейсы»: какие пользовательские кейсы у DK-879? рекомендую сбалансированный: (7) широкого подписывает именем работы любой разговор со случайно совпавшими словами, (8) ломает единообразие имени, ради которого задача заведена
  - решает: человек
  - рекомендация: сбалансированный состав, 6 кейсов: (1) чат груминга с экрана черновика назван «DK-N груминг» до первого токена; (2) конвейер задачи (выполнение, продолжение, закрытие, прогон Check) назван «DK-N работа» либо «DK-N проверка»; (3) чат цели назван «DK-N цель»; (4) свободный чат подписан ai-title из транскрипта сразу после первой реплики, без haiku; (5) чат, переименованный человеком через /rename, держит custom-title старше остального; (6) то же имя видно в claude --resume и в окне терминала
  - вариант: узкий состав, 4 кейса: (1) чат груминга назван «DK-N груминг»; (2) конвейер задачи назван «DK-N работа» либо «DK-N проверка»; (3) чат цели назван «DK-N цель»; (4) свободный чат подписан ai-title без haiku
  - вариант: широкий состав, 8 кейсов: (1) чат груминга назван «DK-N груминг»; (2) конвейер задачи назван «DK-N работа» либо «DK-N проверка»; (3) чат цели назван «DK-N цель»; (4) свободный чат подписан ai-title без haiku; (5) /rename держит custom-title старше остального; (6) то же имя в claude --resume и терминале; (7) чат, поднятый рукой словами заказа «Выполни DK-5», подписан как работа по совпадению текста; (8) повторный заход той же задачи получает суффикс номера, «DK-N работа 2»
  - решено человеком 2026-09-22: сбалансированный состав, 6 кейсов: (1) чат груминга с экрана черновика назван «DK-N груминг» до первого токена; (2) конвейер задачи (выполнение, продолжение, закрытие, прогон Check) назван «DK-N работа» либо «DK-N проверка»; (3) чат цели назван «DK-N цель»; (4) свободный чат подписан ai-title из транскрипта сразу после первой реплики, без haiku; (5) чат, переименованный человеком через /rename, держит custom-title старше остального; (6) то же имя видно в claude --resume и в окне терминала
- «имя»: какой вид у детерминированного имени чата?
  - решает: человек
  - рекомендация: «DK-1120 груминг», «DK-1120 работа», «DK-1120 проверка», «DK-1120 цель»: ID первым, чтобы список сортировался и искался по номеру, роль одним словом, на смартфоне умещается целиком
  - вариант: роль первой: «груминг DK-1120», «работа DK-1120»
  - вариант: с заголовком строки: «DK-1120 груминг: копирование команд из ленты», хвост обрезает список
  - решено человеком 2026-09-22: «DK-1120 груминг», «DK-1120 работа», «DK-1120 проверка», «DK-1120 цель»: ID первым, чтобы список сортировался и искался по номеру, роль одним словом, на смартфоне умещается целиком
- «haiku»: что делать с заказом заголовка у haiku?
  - решает: человек
  - рекомендация: снять целиком: заказ, каталог ~/.devkit/titles, метку служебной сессии и фильтр titleSession; чаты без ai-title это печатный режим, которого в списке чатов и так нет, а мёртвый фильтр следующий читатель примет за живую механику
  - источник: код, tools/dashboard/chats.go, titleSession и titleAsk
  - вариант: оставить за ключом конфига, по умолчанию выключен
  - решено человеком 2026-09-22: снять целиком: заказ, каталог ~/.devkit/titles, метку служебной сессии и фильтр titleSession; чаты без ai-title это печатный режим, которого в списке чатов и так нет, а мёртвый фильтр следующий читатель примет за живую механику
- «повторы»: нужен ли суффикс номера захода у повторной работы над той же задачей?
  - решает: исполнитель
  - рекомендация: имя одно на все заходы задачи, без суффикса номера: список различает заходы временем и состоянием, поиск по чатам идёт по полю tasks и ID, а единообразие имени это цель задачи
  - источник: возражение, обход 2026-09-22: поиск по заголовку перестаёт различать заходы
- «ручной заказ»: узнавать ли заказ по тексту первой реплики у сессий, поднятых не дашбордом?
  - решает: исполнитель
  - рекомендация: имя считается только сессии, которую дашборд поднял сам: у неё предмет известен из ручки, а не из текста; чат, поднятый рукой словами «Выполни DK-5», остаётся с ai-title
  - источник: возражение, обход 2026-09-22: сопоставление текста, а не факт подъёма
- «чип»: откуда чип задачи у чата, который дашборд поднял сам, но который ещё не трогал доску?
  - решает: исполнитель
  - рекомендация: в чипы строки первой идёт задача привязки запуска (bindTask), а касания реестра дописываются следом: чат, поднятый дашбордом под задачу, несёт её чип с первой секунды, до всякой команды доски
- «имя и чип»: как разложить номер задачи и роль между заголовком чата и чипом, чтобы номер не дублировался и заголовок оставался понятным?
  - решает: человек
  - рекомендация: заголовок «DK-1120 груминг», а чип с тем же ID строка не рисует: номер стоит один раз, в заголовке, где он читается и ищется, и то же имя видно в claude --resume; чип остаётся у чатов, чей заголовок номера не несёт
  - вариант: заголовок из роли и заголовка строки доски: «Груминг: копирование команд и ссылок в ленте», номер только чипом; заголовок понятен без номера, но длиннее и требует строки доски у сессии
  - вариант: заголовок только роль, «Груминг», номер чипом
  - решено человеком 2026-09-22: заголовок «DK-1120 груминг», а чип с тем же ID строка не рисует: номер стоит один раз, в заголовке, где он читается и ищется, и то же имя видно в claude --resume; чип остаётся у чатов, чей заголовок номера не несёт
- «полоса чипов»: где решать громоздкость полосы чипов строки чата на смартфоне?
  - решает: человек
  - рекомендация: отдельным черновиком: полоса чипов строки чата (проект, состояние, модель, подписка, заходы, задачи) громоздка на смартфоне сама по себе, и её уплотнение не зависит от именования; здесь только снимается дубль номера
  - вариант: в этой задаче: заодно уплотнить полосу чипов на смартфоне
  - решено человеком 2026-09-22: в этой задаче: заодно уплотнить полосу чипов на смартфоне

## DoD

Чат, поднятый дашбордом с известным предметом (груминг, выполнение, продолжение и закрытие задачи, прогон Check, цель), стоит в списке чатов под коротким именем из ID и роли с первой секунды, и то же имя видно в `claude --resume`. Свободный чат подписан записью custom-title или ai-title из транскрипта без отдельного процесса. Заказ у haiku, каталог ~/.devkit/titles, метка служебной сессии и фильтр titleSession сняты из кода, в dashboard.log записей «назван haiku» после выката нет. Тесты дашборда клиента не поднимают, и это проверяет сам тест. Чип задачи у строки, чей заголовок начинается с того же номера, не рисуется, а чат, поднятый дашбордом под задачу, несёт чип задачи до первой команды доски. Полоса чипов строки чата на смартфоне уплотнена так, что заголовок и полоса умещаются в две строки экрана шириной 360 px. README дашборда описывает лестницу заголовка и состав полосы чипов заново.

## Ранг

`25+6+1+0+2 = 34`, P2.

- Серьёзность 25: заметное трение, служебные чаты подписаны разнобойно и не умещаются в строку на смартфоне («сократить их заголовки, чтобы они умещались даже на смартфоне»), обход есть, открыть чат и прочитать.
- Ценность 6: ощутимое улучшение, которое просят, единообразные короткие имена, чип без дубля и полоса, умещающаяся на смартфоне, плюс «небольшая экономия на подписке» словами автора; на 8-10 не тянет, заказ на чат это один вызов haiku.
- Неопределённость 1: подход ясен после обхода, три сборщика команды названы, чтение записи транскрипта это дочитать шапку.
- Поправка на баг 0: дашборд не читал summary никогда после смены формата харнеса, но заказ у haiku работал, поведение не ломалось.
- Рычаг 2: своя польза, каждый чат конвейера обходится без лишнего процесса и токенов.
- Цена M: правка в трёх сборщиках, чтение шапки, снятие заказа, чипы и полоса строки с тестами и README укладываются в одну сессию.
- Приёмка mixed, барьер «глаза»: имена и чипы агент сверяет по ответу ручки списка, а умещение полосы на смартфоне видно только глазами на телефоне.

## Приёмка

- вид: mixed
- барьер «глаза»: умещение заголовка и полосы чипов в две строки на экране 360 px видно только на телефоне, а ответ ручки и DOM о переносе строк не говорят; имена и чипы сверяются ответом ручки списка агентом.

## Ход работы

- Из черновика DK-879 «заголовок чата берётся из транскрипта харнеса, а не заказом у haiku»: записан 2026-09-09, оформлен 2026-09-22.

## Из черновика DK-833 «Дашборд заказывает заголовок чата у haiku, хотя он есть в транскрипте» (записан 2026-09-06)

### Черновик

#### Ситуация

6 сентября 2026, на разборе того, почему разговор не ищется по заголовку.
readSessionHead (tools/dashboard/sessions.go:441) берёт заголовок харнеса из
записи с полем type = "summary". Claude Code 2.1.261 такой записи больше не
пишет. В каталоге ~/.claude/projects/-Users-rider-projects-devkit записей
type = "summary" ноль на 500 транскриптов. Строка вида
{"type":"ai-title","aiTitle":"..."} лежит в 361 файле. В транскрипте ed82119c
харнес назвал разговор «Ранкинг для черновиков в devkit». Дашборд этого имени
не видит.

#### Осложнение

head.Summary остаётся пустым всегда. titleFor (chats.go:3645) уходит по
лестнице дальше. Сперва идёт эвристика первого предложения на месте, потом
фоновый заказ имени у haiku (titleOrder, не больше titleAskLimit заказов на
заход списка). Готовое имя лежит в том же файле даром. Панель платит за него
квотой. Имя появляется к следующему открытию списка. На ed82119c обе версии
видны рядом. Харнес назвал разговор «Ранкинг для черновиков в devkit», haiku
назвал «Добавить ранжирование черновиков по приоритетам».

#### Вопрос

Читать ai-title вместо summary или наравне с ним. Кто старше, если в файле
окажутся оба. Ключ формата у харнеса свой и меняется без предупреждения.
Отсюда второй вопрос. Ставить ли сторожа, который заметит следующую смену.

#### Гипотеза

Заголовок разговора берётся из транскрипта сразу. Он совпадает с тем именем,
которое показывает сам клиент. Haiku зовётся только там, где имени в файле нет.
