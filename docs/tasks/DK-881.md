# DK-881: Правило плана называет порог повода и не просит плана на один ответ

## Что происходит

`PLAN_RULE` в `hooks/session-task.py:252` велит вести план командой
`agentctl plan` и порога не называет. Так же безусловно звучит правило 4
скилла `board-chat`. Порог написан рядом, в скилле `work-plan`, раздел «Порог
повода». Он требует плана там, где работа длиннее одного хода, и оставляет без
плана один вопрос с одним ответом. В контекст старта едет строка хука, порог
остаётся в скилле.

Разговор на две реплики получает веление завести план. Дальше сессия заводит
план на один ответ либо отбрасывает правило целиком как неприменимое.
Отброшенное правило не возвращается к моменту, когда работа выросла, и кольцо
в шапке стоит молчащим. Ревью DK-614 возвращало `PLAN_RULE` в компактную фразу
за это же.

## Чего хотим

План заводится в работе длиннее одного хода. Разговор на одну реплику идёт без
плана. Проверка прогоном стенда `obeycheck` по скиллу `prompt-test`.

## Границы

Правило плана разбирается ещё тремя записями накопителя. Сюда они не
съезжают: DK-580 про формат файла, DK-609 про отменённую работу, DK-880 про
сессию без задачи.

## Развилки

Правится строка хука одна или вместе с правилом 4 скилла. Оговорка вроде
«когда есть потребность» отдаёт решение той же сессии, которой план вести
лень. Машинный порог «длиннее одного хода» проверяется прогоном.

- «адреса правила»: Куда, кроме строки PLAN_RULE в хуке, едет порог повода?
  - решает: человек
  - рекомендация: строка PLAN_RULE в хуке и правило 4 скилла board-chat, а ядро и определения exec-*/review-* не трогаем: у субагента работа заведомо длиннее хода, а ядро упёрлось в потолок 5500 знаков
  - вариант: хук и board-chat
  - вариант: только PLAN_RULE в хуке, скилл остаётся безусловным
  - вариант: хук, board-chat и строка ядра RULES.core.md с компенсацией потолка
  - решено человеком 2026-09-09: строка PLAN_RULE в хуке и правило 4 скилла board-chat, а ядро и определения exec-*/review-* не трогаем: у субагента работа заведомо длиннее хода, а ядро упёрлось в потолок 5500 знаков
- «формулировка порога»: Каким текстом порог встаёт в правило?
  - решает: человек
  - рекомендация: порог берётся дословно из скилла work-plan, раздел «Порог повода»: один текст в двух местах не разъедется
  - вариант: дословно из work-plan: план ведётся в работе длиннее одного хода, один вопрос с одним ответом плана не просит
  - вариант: по моменту: заводи план, как только видно, что работа не кончится этим ходом
  - вариант: обе половины: порог work-plan плюс оговорка про работу, выросшую посреди разговора
  - решено человеком 2026-09-09: порог берётся дословно из скилла work-plan, раздел «Порог повода»: один текст в двух местах не разъедется
- «доказательство порога»: Чем прогон покажет, что порог сработал?
  - решает: человек
  - рекомендация: к сценарию 27-work-plan добавляется обратный: один вопрос с одним ответом, файла плана после захода нет; пара показывает обе стороны порога
  - вариант: пара сценариев: 27 остаётся зелёным, новый обратный требует пустого каталога планов
  - вариант: только обратный сценарий, 27 уже стоит
  - вариант: без нового сценария, хватит пары with/without по prompt-test на правленом тексте
  - решено человеком 2026-09-09: к сценарию 27-work-plan добавляется обратный: один вопрос с одним ответом, файла плана после захода нет; пара показывает обе стороны порога

## DoD

- `PLAN_RULE` в `hooks/session-task.py` и правило 4 скилла `board-chat` несут
  порог дословно из `work-plan`. План ведётся в работе длиннее одного хода.
  Один вопрос с одним ответом плана не просит.
- Строка ядра `RULES.core.md` и определения `kit/agents/exec-*.md` с
  `review-*.md` остаются как есть. У субагента работа заведомо длиннее хода, а
  ядро упёрлось в потолок 5500 знаков.
- Рядом со сценарием `27-work-plan` в стенде `obeycheck` стоит обратный.
  Сессия получает один вопрос и отвечает одним ходом, а каталог планов после
  захода пуст.
- Обе стороны порога прогнаны по скиллу `prompt-test`. Вывод прогона лежит в
  разделе «Проверка» файла задачи.

## Ранг

`25+4+1+0+3 = 33`, P2.

- Серьёзность 25. Трение заметное, обход дешёвый: порог лежит в скилле
  `work-plan`, и сессия решает по нему сама. Черновик: разговор на две реплики
  либо заводит план на один ответ, либо отбрасывает правило целиком.
- Ценность 4. Правило перестаёт спорить с порогом. Кольцо молчит только там,
  где работы и правда на один ход.
- Неопределённость 1. Подход выбран, развилки груминга закрыты человеком.
- Поправка на баг 0. Порога в правиле не было изначально, ломаться было нечему.
- Рычаг 3. По плану человек следит за каждой сессией задачи, и порог чинит
  картину целиком.

## Приёмка

- вид: agent
- барьеров не нашёл. Прогон стенда даёт код возврата и каталог планов, пустой
  или нет. Согласие с формулировкой человек дал развилками груминга.

## Ход работы

- Из черновика DK-881 «правило плана едет в контекст без порога повода»: записан 2026-09-09, оформлен 2026-09-09.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 40%, снимок 10м назад, сдвига нет), 2026-09-17 17:43-17:46.
- Скилл board-chat из формулировки DoD к моменту разработки уже переименован в chat (DK-1018). Правило про план в нём стоит пунктом 1, не 4. Правка легла в этот пункт.

## Ревью

Уровень 3 до 24bb86d: неопределённость 3 (sonnet/high), critical_paths: тронут hooks/session-task.py -> два да триажа

- [возврат: правила, 2026-09-17] блокирует: сценарии tools/obeycheck/scenarios/32-chat-no-task-long-task.md и 41-chat-no-task-second-turn.md встраивают в «Промпт» буквальный текст PLAN_RULE из hooks/session-task.py, а он в этой правке изменился (добавлена фраза про порог). Оба файла остались со старой редакцией без порога. Прецедент DK-1018 (коммит ea2be850) синхронизировал ровно эти два файла при прошлой правке текста правила тем же заходом. Сейчас стенд обкатывает по этим сценариям текст, которого сессия в проде уже не видит, особенно чувствителен 41-chat-no-task-second-turn: он про ту же границу «план ко второй настоящей работе», что и порог DK-881. Чем проверить: git diff показывает, что PLAN_RULE менялся, а Промпт в 32/41 нет; поправка это дописать в оба Промпта то же предложение, что легло в PLAN_RULE.: исправлено, дописал фразу порога в Промпт 32-chat-no-task-long-task.md и 41-chat-no-task-second-turn.md, коммит 4bc91a8b
- [возврат: постановка, 2026-09-17] не блокирует: DoD называет цель «правило 4 скилла board-chat», но к моменту разработки скилл уже переименован в chat (DK-1018), а правило про план там теперь под номером 1. Правка корректно нашла актуальное место (kit/skills/chat/SKILL.md, пункт 1) и сценарий проверки грепает верный файл, но в «Ход работы» этот дрейф наименования не назван. Стоит одной строкой отметить, что правится текущий пункт 1 skill chat, а не пункт 4 board-chat из формулировки DoD, чтобы при чтении задачи позже не читалось как несоответствие.: исправлено, Ход работы получил строку про переименование board-chat -> chat (DK-1018), правило теперь пунктом 1
- Вердикт: без замечаний. круг 2 до 56b4f0f0: оба замечания сверены по диффу 24bb86d0..HEAD, порог дословно лёг в Промпт 32 и 41, дрейф board-chat->chat назван в Ходе работы, unittest hooks зелёный (39 тестов), grep/test -f из Сценария проверки повторены

## Сценарий проверки

Стенд `obeycheck` гоняется руками по скиллу `prompt-test`: раскладка с новой
редакцией правила против раскладки со старой, сценарий
27-work-plan-single-turn. Таблицы прогонов лежат в «Проверке». Обкатка живые
сессии не поднимает, машинная часть ниже гоняется в чистом дереве.

```sh
python3 -m unittest discover -s hooks -p 'session_task_test.py'
grep -q "один вопрос с одним ответом" hooks/session-task.py
grep -q "один вопрос с одним ответом" kit/skills/chat/SKILL.md
test -f tools/obeycheck/scenarios/27-work-plan-single-turn.md
```

## Выкат

- 2026-09-19 слито: fc911059b, 940a7eb80, ecbfc7a7b, cfc6ed281, 41139cb40, d3c749679

## Проверка


- Стенд: 2026-09-17 18:13, дерево 32f72af9, предмет bd045560, база старый, ярус mini, k 3, сценарии 27-work-plan-single-turn, зачтён.

```console
$ obeycheck --task DK-881 --tier mini --only 27-work-plan-single-turn -k 3 --base старый /private/tmp/claude-501/-Users-rider-projects-devkit/e45bfe00-cb80-4f3e-a74d-341cb2e2fa09/scratchpad/dk881-rules-with /private/tmp/claude-501/-Users-rider-projects-devkit/e45bfe00-cb80-4f3e-a74d-341cb2e2fa09/scratchpad/dk881-rules-without
сценарий                                      dk881-rules-with  dk881-rules-without  вердикт
один вопрос с одним ответом плана не заводит               3/3                  3/3  зелёный на обеих

итог: сценариев 1, все зачтены, база старый
```

Хук `hooks/session-task.py` не входит в раскладку стенда. Его текст стенд
берёт из чекаута, раскладка `rules-with`/`rules-without` его не варьирует.
Прежнюю редакцию `PLAN_RULE` (без порога) проверил отдельный экземпляр
сценария вне `scenarios/`, отличавшийся от боевого только этой фразой
промпта, на раскладке `rules-without` с обеих сторон.

- Контрольный стенд: 2026-09-17 18:16, прежняя редакция PLAN_RULE без порога,
  ярус mini, k 3, сценарий 27-work-plan-single-turn (копия вне доски), зачтён.

```console
$ obeycheck --scenarios <scratch>/dk881-scenarios-old --tier mini --only 27-work-plan-single-turn -k 3 --base старый <scratch>/dk881-rules-without <scratch>/dk881-rules-without
сценарий                                      dk881-rules-without  dk881-rules-without  вердикт
один вопрос с одним ответом плана не заводит                  3/3                   3/3  зелёный на обеих

итог: сценариев 1, все зачтены, база старый
```

Обе редакции `PLAN_RULE`, и с порогом, и без него, дали план пустым на
разговоре из одного вопроса. Модель на ярусе mini план на короткий разговор
не заводит и без явного текста. Правка фиксирует это поведение фразой,
прогон его не создаёт. Позитивную сторону порога несёт сценарий 27-work-plan,
эта правка его не трогала.

- Обкатка: 2026-09-20 00:01, свежее дерево c961bce2d662, сценарий 42110d14, временный HOME, утилит дерева 11, шагов 4, все зелёные.

Шаг 1 (зелёный):

```console
$ python3 -m unittest discover -s hooks -p 'session_task_test.py'
.......................................
----------------------------------------------------------------------
Ran 39 tests in 3.531s

OK
```

Шаг 2 (зелёный):

```console
$ grep -q "один вопрос с одним ответом" hooks/session-task.py

```

Шаг 3 (зелёный):

```console
$ grep -q "один вопрос с одним ответом" kit/skills/chat/SKILL.md

```

Шаг 4 (зелёный):

```console
$ test -f tools/obeycheck/scenarios/27-work-plan-single-turn.md

```
