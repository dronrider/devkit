# DK-882: decide дописывает вариант ответа в заведённую развилку

## Что происходит

Варианты ответа перечень развилок получил позже самой команды. Записи, чей
вопрос заведён раньше, остались с одной рекомендацией. Блок `decide --chat`
печатает такой развилке единственный пункт, и выбирать человеку не из чего.
Дописать вариант было нечем: ключ `--option` работал только при `--ask`, а
повторное `--ask` на занятое имя отбивается своим отказом. Поймано грумингом
DK-796, где три человеческие развилки завелись до правки DK-864.

## Чего хотим

Вопрос, заведённый без вариантов, доводится до выбора там же, где он лежит, без
второго имени под тем же вопросом.

## DoD

Команда `taskctl decide <ID> «имя» --option "..."` дописывает вариант в
заведённую развилку. Вариант встаёт после рекомендации и прежних вариантов, но
раньше следов решения и передачи. Повтор записанного варианта пачку не отбивает,
команда называет его в ответе и второй раз в файл не кладёт. Решённая развилка
вариант не принимает. Тесты лежат рядом с остальными про `decide` и про формы
развилок. Абзац в README taskctl, раздел «Развилки записи».

## Сценарий проверки

Прогон идёт на временном стенде, живую доску он не трогает.

```sh
GOWORK=off go test -C /Users/rider/projects/devkit/internal/taskform ./...
GOWORK=off go test -C /Users/rider/projects/devkit/tools/taskctl -run 'Decide|Chat|Fork' ./...
```

Оба прогона зелёные. Дальше боевая команда на записи DK-796, где правка и
понадобилась.

```sh
taskctl decide DK-796 --open
taskctl decide DK-796 --chat
```

Развилка «серьёзность» несёт строку «вариант: 25, обход дешёвый», а блок вопроса
печатает под ней два пункта, рекомендованный первым номером.

## Ранг

`25+5+0+0+2 = 32`, P2.

- Серьёзность 25. Вопрос без выбора мешает разговору постановки, обход есть и он
  дешёвый: назвать варианты текстом реплики мимо перечня.
- Ценность 5. Перечень развилок перестаёт расходиться с тем, что видит человек в
  блоке вопроса.
- Неопределённость 0. Место правки и форма строки известны, ключ уже есть у
  заведения.
- Поправка на баг 0. Дописывания не было с самого начала, ломаться было нечему.
- Рычаг 2. Чинит все записи, заведённые до появления вариантов, а не только
  DK-796.

## Ход работы

- 2026-09-09: правка сделана по ходу груминга DK-796, строка заведена следом.

