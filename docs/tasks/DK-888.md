# DK-888: go:dashboard тесты красные на main: talk_state пуст у чата без хода

## Что происходит

Находка при слиянии DK-880. Ветка DK-880 не трогает `tools/dashboard`, а
`shipctl merge` всё равно отказал: полный прогон `tools/devkitctl/parallel.py`
на свежем ребейзе на main красный по компоненту `go:dashboard`. Проверено
отдельно, вне ветки DK-880, прямо на HEAD main (`0c4119b0`), `git status` в
основном дереве чист:

```
$ cd tools/dashboard && GOWORK=off go test ./... -run \
  'TestRunStopInterruptsChatWork|TestBoardRowTalkStateWithoutWork|TestRowStaysBusyWhileSubagentWorks' -v
=== RUN   TestRunStopInterruptsChatWork
    runchat_test.go:119: строку не ведёт разговор с ходом: run="" busy=false
--- FAIL: TestRunStopInterruptsChatWork (0.01s)
=== RUN   TestBoardRowTalkStateWithoutWork
    runchat_test.go:329: строка молчит о живом разговоре: talk_state="" talk_chat=""
--- FAIL: TestBoardRowTalkStateWithoutWork (0.01s)
=== RUN   TestRowStaysBusyWhileSubagentWorks
    stopwait_test.go:153: строка с живой фоновой работой простаивает: state="" busy=false
    stopwait_test.go:156: признак работы строки "", ожидал "chat"
--- FAIL: TestRowStaysBusyWhileSubagentWorks (0.01s)
```

`go clean -testcache` перед прогоном находку не снимает. Последний коммит,
тронувший `runchat_test.go` и `stopwait_test.go`, это `cf75bbdc` (DK-716,
«замечания 13 и 14 закрыты, шаг сценария на шесть состояний окна»,
2026-09-06). Тесты ждут, что строка без активного хода, но с живым чатом или
фоновым субагентом, несёт признак `talk_state`/`busy`, а код отдаёт пустые
значения. Открытой задачи про эту красноту на доске не было.

Причина оказалась не в правке, а в календаре. `cf75bbdc` тут ни при чём. Те же
три теста красны и на коммите, который их завёл. Стенды дашборда морозят часы
сервера фикстурой, у половины из них это 2026-08-10. Срок куки входа сервер
считает своими часами, `s.now()` плюс тридцать дней `cookieAge`. Банка клиента
(`net/http/cookiejar`) судит о сроке настоящими часами машины, а куку с
прошедшим сроком выбрасывает молча. Пока разница держалась внутри тридцати
дней, вход стенда работал. 2026-09-10 она их перешагнула, и всякий вызов
`loggedClient` после заморозки часов стал давать клиента без куки. Сервер
отвечал таким запросам 401, `boardRows` разбирал ответ об ошибке в пустые
строки, и краснели разом все тесты, которые сначала поднимают стенд, потом
морозят время, а потом спрашивают доску. Названными тремя список не
исчерпывался. Полный прогон давал двенадцать провалов той же природы.

Чинится в `loggedClient`. Кука кладётся в банку отдельным ходом со снятым
сроком, и вход стенда перестаёт зависеть от того, какое сегодня число. Сам
срок куки проверяет `auth_test.go`, там часы стенду не мешают.

## Чего хотим

`go test ./...` в `tools/dashboard` зелёный на main, и `shipctl merge` любой
задачи, не трогающей dashboard, снова проходит полный прогон без отказа по
чужому компоненту.

## DoD

- Три названных теста (и остальной пакет `tools/dashboard`) проходят на main.
- Причина красноты названа: где и почему признак `talk_state`/`busy` перестал
  доезжать до строки.
- Тест бага воспроизводит исходную красноту на коде до правки.

## Ранг

`50+2+3+5+1 = 61`, P1.

- Серьёзность 50 (Critical). Красный прогон блокирует слияние любой ветки в
  devkit через общий `parallel.py`, а не только конкретный компонент; обход
  есть (ручное вмешательство в проверку), но болезненный и рискованный.
- Ценность 2. Чистая техника, основная польза учтена строкой серьёзности.
- Неопределённость 3. Причина расхождения с логикой DK-716 не разобрана,
  общий подход к починке пока не выбран.
- Поправка на баг +5. Регресс, тесты были зелёными после DK-716.
- Рычаг 1. Зелёный билд восстанавливает уже сама серьёзность.

## Ход работы

- Найдена при слиянии DK-880 субагентом-исполнителем DK-880, заведена
  диспетчером строкой в Backlog, 2026-09-09.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 23%, week_max 12% (панель разбивку не дала, цифра из кеша клиента возрастом 7ч 4м назад), снимок 37м назад, сдвига нет), 2026-09-10 00:49-00:49.

## Ревью

Уровень 3 до f956ac0: неопределённость 3 из вердикта pick, глубокое ревью

- [возврат: правила, 2026-09-10] не блокирует: runs_test.go:runsEnvWithLog (строки 930-934) и chat_smoke_test.go:smokeWrap (строка 237) собирают логин и куку вручную, в обход исправленного loggedClient, и не снимают Expires у куки. Сейчас ловушка спит: ни один их тест не морозит e.s.now при входе. Но стоит появиться там тесту с заморозкой времени, вернётся тот же баг DK-888 (401 после cookiejar молча выбросит просроченную по реальным часам куку). Стоит перевести оба места на e.loggedClient либо повторить приём (снять Expires/RawExpires/MaxAge перед jar.SetCookies).: исправлено, runsEnvWithLog переведён на e.loggedClient, smokeWrap снимает срок у куки перед отдачей браузеру

## Сценарий проверки

Вид агентский, барьеров нет. Прогон идёт в каталоге `tools/dashboard` на свежем
main. Каждая строка блока это свой шаг. Первый шаг берёт три теста, названных
находкой поимённо. Второй берёт тест бага, который морозит часы стенда далеко в
прошлом. Третий гоняет весь пакет. Провалов той же природы было двенадцать.

```sh
cd tools/dashboard && GOWORK=off go test ./... -count=1 -run 'TestRunStopInterruptsChatWork|TestBoardRowTalkStateWithoutWork|TestRowStaysBusyWhileSubagentWorks'
cd tools/dashboard && GOWORK=off go test ./... -count=1 -run TestLoggedClientKeepsCookieUnderFrozenClock
cd tools/dashboard && GOWORK=off go test ./... -count=1
```

Ожидание. Все три шага выходят нулём и печатают `ok` с именем пакета
`github.com/dronrider/devkit/tools/dashboard`.

## Выкат

- 2026-09-10 слито: 39e59f4f, 05765565, 6cf7cb01

## Проверка

- Обкатка: 2026-09-10 10:39, свежее дерево 2150acc75675, сценарий 35a0d93d, временный HOME, утилит дерева 11, шагов 3, все зелёные.

Шаг 1 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test ./... -count=1 -run 'TestRunStopInterruptsChatWork|TestBoardRowTalkStateWithoutWork|TestRowStaysBusyWhileSubagentWorks'
ok  	github.com/dronrider/devkit/tools/dashboard	1.664s
```

Шаг 2 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test ./... -count=1 -run TestLoggedClientKeepsCookieUnderFrozenClock
ok  	github.com/dronrider/devkit/tools/dashboard	0.481s
```

Шаг 3 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test ./... -count=1
ok  	github.com/dronrider/devkit/tools/dashboard	157.587s
```

- Прогон сценария: 2026-09-10 10:43, дерево 01c04b04, шаги 1-3 зелёные, прогонял субагент sonnet.

```console
$ cd tools/dashboard && GOWORK=off go test ./... -count=1 -run 'TestRunStopInterruptsChatWork|TestBoardRowTalkStateWithoutWork|TestRowStaysBusyWhileSubagentWorks'
ok  	github.com/dronrider/devkit/tools/dashboard	1.595s
$ cd tools/dashboard && GOWORK=off go test ./... -count=1 -run TestLoggedClientKeepsCookieUnderFrozenClock
ok  	github.com/dronrider/devkit/tools/dashboard	0.515s
$ cd tools/dashboard && GOWORK=off go test ./... -count=1
ok  	github.com/dronrider/devkit/tools/dashboard	156.297s
```
