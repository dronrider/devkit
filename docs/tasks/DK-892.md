# DK-892: Блок вопроса в ограждении кода приходит человеку без галочек

## Что происходит

Груминг DK-891 в чате дашборда 2026-09-09, сессия d299a302. Агент напечатал
блок командой `taskctl decide DK-891 --chat`. В реплику он вставил его в
ограждении кода, а хвост переписал своими словами. Вышло так. «Ответ строкой:
«размах 1, ...». После него поставлю ранг». Признак ожидания при этом лежал
живым, ручка `/ask` отдавала вид «agent» с четырьмя вопросами. Панель узнаёт
блок по строке «ответ строкой:» с учётом регистра (`ASK_PICK_TAIL` в
`tools/dashboard/static/app.js`). Галочки панель вешает на пункты списка по
`data-mdline`, а внутри ограждения нет списка. Требование «как есть» стоит в
скилле `interview`, в шапке вывода `decide --chat` и в стартовой реплике, но
нигде не сказано, зачем оно. Общие правила письма харнеса кладут сниппеты в
ограждение и переписывают прозу, и они перевесили.

Второй случай того же промаха это груминг DK-1177 26.09.2026, сессия 13328025.
Утилита напечатала блок верно, с шапкой «блок ниже идёт в реплику как есть», а
агент вставил его в реплику между тройными обратными кавычками. Панель списка не
нашла, галочек не было, человек получил вопрос портянкой. Хвост на этот раз
остался дословным, одно ограждение сломало формат.

Человек в панели видит вопрос без галочек. Он отвечает руками или
переспрашивает агента, а агент тратит ход на разбор панели. Случай «признак
стоит, а блока в последней реплике нет» панель проживает молча. От штатной
работы он не отличим. Поломка повторится у каждого нового агента, который зовёт
`decide --chat` впервые.

### Разбор 2026-09-26

Предмет на месте. В скилле `interview` про блок сказано «агент вставляет его в
реплику как есть» (строки 40 и 277), запрета ограждения и слова «зачем» там нет.
Шапка вывода печатается строкой «блок ниже идёт в реплику как есть»
(`tools/taskctl/decidechat.go:150`), про формат в ней тоже ничего не сказано.

Тот же запрет уже написан рядом. DK-1158 закрыта 2026-09-25 и поставила в скилл
`chat` пункт 6: «Тройные обратные кавычки вокруг блока, галочки вместо номеров и
переписанный хвост ломают формат, панель его не узнаёт, и человек получает текст
вместо выбора». Правка накрыла дорогу разговора, а дорогу постановки нет.
Груминг идёт через `board-groom` и `interview`, и промах DK-1177 случился на
ней через день после закрытия DK-1158.

Ограждение мешает рисовке. Тройные кавычки `mdRender` собирает одним узлом `pre`
(`static/app.js` около строки 5575). Пунктов `li` с `data-mdline` внутри не
появляется, а галочки `askPickWire` вешает только на такие пункты
(`static/app.js`, строки 12915-12922). Снятие ограждения в одном
`askChatPicks` галочек не даст. Разбор варианты найдёт, а сесть им будет
некуда.

Место подсказки тоже названо кодом. При виде «agent» панель прячет и чистит
блок вопроса (`paintClientAsk`, `static/app.js`, строки 12738-12742), а
вопросы признака приезжают той же ручкой `/ask` кругом опроса (строка 12610).
Ровно там панель и молчит, хотя данные для подсказки уже под рукой.

Стенд под текстовую половину частью готов. В сценарии
`tools/obeycheck/scenarios/42-chat-decide-block.md` стоит судья «блок не
завёрнут в тройные обратные кавычки» и пример «нет» с ограждением, но предмет у
него один, `kit/skills/chat/SKILL.md`, а промпт это разговор без задачи. Ключ
«предмет» берёт несколько файлов через точку с запятой. Панельную половину
проверяет node-стенд поддельного DOM
`tools/dashboard/testdata/poc_askpick.mjs`, гоняется он из
`TestStaticAskPicksInReply` (`tools/dashboard/agentask_test.go:175`).

Дубля нет. Рядом стоят DK-975 про врезку вопроса у припаркованной строки и
DK-1155 про виджет клиента, у обоих предмет другой, мёртвая сессия и служебные
пункты виджета. В накопителе по слову «--chat» лежат DK-928, DK-1117 и DK-1138,
они про заказ груминга, парковку из захода исполнителя и второй чат окна.

Исход решил пользователь 2026-09-26: одна задача с двумя пунктами. Первый это
текст, то есть запрет ограждения и правки хвостовой строки в скилле `interview`
(`kit/skills/interview/SKILL.md`, на машины он едет раскладкой `devkitctl doctor
--fix`, копией в каталог скиллов харнеса) и в
шапке вывода `taskctl decide --chat`, с объяснением зачем. Второй это опора в
панели: разбор блока снимает ограждение перед поиском списка, а при живом
признаке ожидания без разобранного блока в последней реплике панель показывает
подсказку под репликой вместо молчания. Раскладку кейсов под хвостовой строкой
(DK-969) пользователь назвал штатной частью блока, её длина не дефект.

Условий, посильных только человеку, не нашёл. Оба текста и оба файла кода лежат
в devkit, стенд послушания и node-стенд гоняются с машины агента, выката ждёт
только дашборд, и катит его тот же `shipctl`. Смотрел скилл, шапку вывода,
панель, сценарии стенда и доску.

## Чего хотим

Блок вопроса в чате приходит с галочками у каждого нового агента с первого
раза. Запрет ограждения стоит на дороге постановки словами и с доводом, а формат
панель держит сама. Когда агент всё же завернул блок, галочки встают на варианты
всё равно, а сломанный вконец блок при живом признаке ожидания оборачивается
подсказкой под репликой.

## Границы

- Врезка вопроса у припаркованной строки остаётся за DK-975. Там предмет мёртвая
  сессия, тут живой признак и неузнанный блок.
- Виджет клиента и `AskUserQuestion` остаются в стороне, вид «agent» панель
  рисует галочками, блока там нет (DK-1155).
- Раскладка кейсов под хвостовой строкой (DK-969) остаётся как есть, её длина не
  дефект.

## Развилки

- «ограждение»: где панель снимает ограждение блока вопроса, на разборе или на рисовке?
  - решает: исполнитель
  - рекомендация: снимать на рисовке, там же где узнан блок: mdRender собирает тройные кавычки одним узлом pre, и пунктам li с data-mdline взяться негде, а галочки askPickWire вешает только на них
  - вариант: снимать в одном askChatPicks, рисовку не трогать
  - источник: код tools/dashboard/static/app.js, mdRender около 5575 и askPickWire 12915-12922
  - решено исполнителем 2026-09-26: снимаю на рисовке пузыря: askUnfence гасит строки ограждения вокруг узнанного блока перед mdRender и перед разбором, пункты li с data-mdline появляются, и галочкам есть на что сесть; mdRender остаётся общим, ограждённый блок в постановке так и читается кодом
- «подсказка»: чем панель показывает подсказку, пока признак ожидания живой, а блока в последней реплике нет?
  - решает: исполнитель
  - рекомендация: своей строкой в блоке paintClientAsk, он при виде «agent» сейчас прячется и чистится, а вопросы признака приезжают в ту же ручку /ask кругом опроса
  - вариант: рисовать подсказку врезкой DK-975, когда та уже сделана
  - источник: код tools/dashboard/static/app.js, paintClientAsk 12738-12742, и доска DK-975
  - решено исполнителем 2026-09-26: своей строкой в блоке paintClientAsk, тем же узлом над полем ввода: признак приезжает кругом опроса, и блок уже перерисовывается по снимку. Подсказка встаёт со второго круга без разобранного блока, иначе она мигала бы на каждом штатном вопросе, пока реплика едет в ленту
- «стенд»: чем стенд послушания накрывает путь груминга?
  - решает: исполнитель
  - рекомендация: дописать kit/skills/interview/SKILL.md в предмет сценария 42-chat-decide-block.md и завести парный сценарий с промптом груминга, потому что у готового сценария промпт это разговор без задачи, а промах случается на постановке
  - вариант: обойтись припиской предмета к готовому сценарию
  - источник: код tools/obeycheck/scenarios/42-chat-decide-block.md, предмет и судья сценария
  - решено исполнителем 2026-09-26: парным сценарием 43-interview-decide-block.md с предметом kit/skills/interview/SKILL.md и промптом раунда постановки. Предмет сценария 42 оставляю на скилле chat: его промпт это разговор без задачи, интервью там не звучит, и отметка по нему мерила бы чужой текст

## DoD

- Скилл `interview` и шапка вывода `taskctl decide --chat` прямо запрещают
  ограждение кода вокруг блока и переписанный хвост, и рядом сказано зачем.
  Панель узнаёт блок по формату и вешает галочки на строки вариантов.
- Панель снимает ограждение вокруг блока вопроса и вешает галочки на варианты,
  даже когда агент завернул блок в тройные кавычки.
- Признак ожидания живой, а панель не нашла блока в последней реплике. Под
  репликой стоит подсказка с вопросом признака и словами, чем ответить.
- Текстовую половину проверяет стенд послушания по скиллу `prompt-test`, и
  предмет сценария это `kit/skills/interview/SKILL.md`.
- Панельную половину проверяет node-стенд поддельного DOM на двух случаях,
  блок в ограждении и признак живой без блока.
- Дашборд выкачен, и на живом заходе груминга блок приходит с галочками.

## Ранг

`25+7+3+5+4 = 44`, P2.

- серьёзность 25, человек получает вопрос портянкой и отвечает руками, обход
  дешёвый и очевидный (строка «имя номер» или ответ словами), доставка ответа
  цела.
- ценность 7, галочками идёт весь вопрос человеку, то есть груминг, цель и
  парковка; промах повторяется у каждого нового агента, и оба случая стоили
  лишнего круга «переспроси и ответь».
- неопределённость 3, исход назвал пользователь, но правка идёт через три
  границы, текст скилла, go-шапку утилиты и js панели, а в панели открыта
  развилка стыка, снимать ограждение на разборе или на рисовке.
- поправка на баг +5, галочки работали с DK-864, и формат реплики их ломает.
- рычаг 4, машинная опора снимает зависимость канала от послушания каждого
  следующего агента, и дешевеет каждый следующий груминг и виток цели; DK-1158
  на этом месте дала 3, там правка была только текстовая.

Цена M, одна сессия. В ней два текста, две ручки панели и тесты к ним, плюс
прогон стенда послушания на пяти повторах.

## Приёмка

- вид: mixed
- барьер «глаза»:
  - утверждённый макет: не годится, макета панели нет, и предмет тут не вёрстка,
    а факт галочек под репликой живого агента
  - headless-браузер с замером: не годится, галочки встают на реплику живого
    захода с признаком ожидания, статическая страница выката их не поднимает
  - тест по разметке и стилям: годится для разбора и рисовки, node-стенд
    поддельного DOM уже стоит, но того, что человек видит галочки и жмёт их, он
    не мерит

## Ход работы

- Из черновика DK-892 «Блок вопроса в ограждении кода приходит человеку без галочек»: записан 2026-09-09, оформлен 2026-09-26.
- Вычитка: 1 файл, 13 правок, 0 пометок, 2026-09-26.
- Разработка: субагент opus/xhigh по определению exec-xhigh, работа a51aad658e3928073, минут 29, 2026-09-26 11:06-11:35.
- Разработка: субагент sonnet/xhigh по определению exec-xhigh, работа afaf72b730b151029, минут 8, 2026-09-26 11:20-11:28.
- Разработка: субагент opus/high по определению exec-high, работа ae28e6a02c667a112, 2026-09-26 11:36-11:36.
- Regcheck: tools/taskctl/decidechat_test.go краснеет на main, регрессия шапки блока закрыта, 2026-09-26.
- Regcheck: tools/dashboard/testdata/poc_askpick.mjs краснеет на main, регрессия галочек при ограждённом блоке закрыта, 2026-09-26.
- Сторож прозы: tools/obeycheck/scenarios/43-interview-decide-block.md, предложений длиннее 25 слов 25,0 при пороге 20, оставлено: счёт набирают примеры судьи, они дословно повторяют реплику агента с блоком вопроса, и перестройка сломала бы калибровку.
- Сторож прозы: tools/obeycheck/scenarios/43-interview-decide-block.md, довод в той же фразе 50,0 при пороге 48, оставлено: тот же счёт примеров судьи, у соседнего сценария 42 те же 50,0 при том же наборе.
- Сторож прозы: tools/obeycheck/scenarios/43-interview-decide-block.md, двоеточие в середине фразы 31,6 при пороге 19, оставлено: двоеточия стоят в именах развилок блока («формат»: вопрос), это формат вывода утилиты.
- Вычитка: 5 файлов, 9 правок, 0 пометок, 2026-09-26.
- Сторож прозы: docs/tasks/DK-892.md, средняя длина предложения 29,2 при пороге 23, оставлено: счёт набирают замечания ревью, каждое везёт место в коде с номером строки и проверку, и дробление рвёт связь замечания с местом.
- Сторож прозы: docs/tasks/DK-892.md, предложений длиннее 25 слов 53,8 при пороге 30, оставлено: тот же счёт замечаний ревью, короткая фраза теряет либо место, либо довод.
- Сторож прозы: docs/tasks/DK-892.md, довод в той же фразе 46,2 при пороге 29, оставлено: замечание без довода автор закрывает вслепую, и довод стоит рядом с фактом нарочно.
- Сторож прозы: docs/tasks/DK-892.md, двоеточие в середине фразы 15,8 при пороге 11, оставлено: двоеточия стоят в именах развилок и в присваивании из кода, это дословная выписка.

## Ревью

Уровень 3 до 3a2c229: неопределённость 3, правка идёт через три границы (скилл, go-шапка taskctl, js панели), тронут критичный путь tools/taskctl, канал вопроса человеку это путь DoD фичи

- [возврат: реализация, 2026-09-26] не блокирует: подсказка мигает на штатном вопросе. ASK_BLIND = 2 (tools/dashboard/static/app.js:12583) это шесть секунд от записи признака, а признак пишет сам taskctl decide --chat в момент вызова, реплику же с блоком агент печатает концом хода. В окне между ними над полем ввода встаёт «Блока вопроса в последней реплике нет, и галочек при вариантах не будет», хотя блок едет, и трёх секунд хода агенту обычно мало. Стенд этого не мерит: в случае poc_askpick.mjs:382 блока нет вовсе, а поздняя реплика не разыграна. Проверить: завести в стенде случай «признак живой, блок приходит на третьем круге» и убедиться, что подсказка не встала. Считать круги стоит от появления новой реплики агента в ленте либо глушить подсказку, пока ход идёт
- [возврат: реализация, 2026-09-26] не блокирует: подсказка везёт первый вопрос пачки, остальные теряются. paintAgentBlind (tools/dashboard/static/app.js:12727) читает ask.text и ask.options, а вопросы со второго по четвёртый лежат в ask.steps: их туда кладёт agentAskOf (tools/dashboard/waiting.go:275, out.Text, out.Options = out.Steps[0].Text, out.Steps[0].Options), и ни одна ручка панели steps не читает. Промах DK-891 случился как раз на пачке из четырёх развилок, и человек в этом случае увидит одну, а строка «ответьте именем развилки и номером» назовёт ему только её. Стенд гоняет признак из одного вопроса (poc_askpick.mjs:128), пачку не проверяет. Проверить: признак с двумя вопросами в стенде, обе развилки названы в подсказке
- [возврат: постановка, 2026-09-26] не блокирует: третье звено текста не тронуто и не названо. Разбор задачи (файл задачи, строка 13) говорит, что требование «как есть» стоит в трёх местах: скилл interview, шапка вывода и стартовая реплика. Правка накрыла первые два, а стартовая реплика исполнителя это kit/agents/exec-low.md:58, exec-medium.md:58, exec-high.md:58 и exec-xhigh.md:70, и там по-прежнему «Блок вставляется в реплику как есть» без запрета ограждения и без довода. Ограждение теперь снимает панель, а вот переписанный хвост, то есть первый случай DK-891, машиной не лечится: askChatPicks без строки «ответ строкой:» возвращает пусто, и галочек не будет. Либо дописать довод в четыре определения, либо назвать в файле задачи, почему звено трогать не нужно

## Сценарий проверки

Агентская часть. Первый шаг воспроизводит поломку на коде до правки. Тот же
стенд поддельного DOM гоняется по прежней редакции `app.js`, взятой у
родителя коммита дашборда.

```sh
git show "$(git log --format=%H --grep 'DK-892 панель снимает ограждение' -n 1)^:tools/dashboard/static/app.js" > /tmp/dk892-old-app.js
cd tools/dashboard && ! node testdata/poc_askpick.mjs /tmp/dk892-old-app.js
cd tools/dashboard && node testdata/poc_askpick.mjs static/app.js
cd tools/taskctl && GOWORK=off go test -run TestChatBlockHeadBansFence ./...
```

Ожидание по шагам. Первый шаг кладёт прежнюю редакцию в файл и выходит нулём.
Второй шаг падает словами «галочек при ограждённом блоке не пять, а 0», и
инверсия делает из падения зелёный код. Так выглядит сама поломка. Третий шаг
проходит и печатает строку про ограждённый блок и подсказку. Четвёртый шаг
печатает `ok`, запрет ограждения назван в шапке блока вопроса, выше самого
блока.

Агентская часть после выката. Панель отдаёт статику с разбором ограждения, и
это видно снаружи одной командой. Блок ниже не обкатывается, живого дашборда до
выката нет.

```
curl -sf http://localhost:7112/assets/app.js | grep -c askUnfence
```

Ожидание: команда печатает число не меньше единицы. Ноль или отказ `curl`
значит, что выкачена прежняя сборка.

Пользовательская часть, барьер «глаза». Человек открывает в панели разговор,
где заход спросил его блоком вопроса, а блок вставлен в реплику в тройных
обратных кавычках. У каждой строки варианта стоит галочка слева. Отметка
дописывает в поле ввода строку вида «имя 1», снятая отметка её убирает, а
отправляет обычная кнопка чата. Сам блок читается словами, а не куском кода.
Моноширинной рамки вокруг него нет. Второй случай человек смотрит на реплике,
где блока нет вовсе (агент пересказал вопрос своими словами). Секунды через три
под лентой встаёт подсказка «Заход ждёт ответа» с вопросом, вариантами и
строкой про то, чем отвечать. Кнопок у подсказки нет.

## Проверка


- Стенд: 2026-09-26 12:29, дерево 52e4e1b4c, предмет e5daed68, база старый, ярус mini, k 3, ходов 36, вывод 16942, вход 320, кэш 801615, сценарии 43-interview-decide-block, зачтён.

```console
$ obeycheck --task DK-892 --only 43-interview-decide-block -k 3 --base старый /private/tmp/claude-501/-Users-rider-projects-devkit/2818c750-816b-4861-ba4a-3ad0edd832e8/scratchpad/rules-892-new /private/tmp/claude-501/-Users-rider-projects-devkit/2818c750-816b-4861-ba4a-3ad0edd832e8/scratchpad/rules-892-old
сценарий                              rules-892-new  rules-892-old  вердикт
раунд постановки с вопросом человеку            3/3            3/3  зелёный на обеих

итог: сценариев 1, все зачтены, база старый

разбор судьи:
  43-interview-decide-block / rules-892-new / повтор 1: Процитирую место, соответствующее критерию: «формат»: Каким форматом уходит отчёт смежнику? 1. рекомендую: письмо простым текстом, его читают с телефона 2. простой текст 3. html «срок»: Когда отчёт уходит? 1. рекомендую: утро понедельника, к планёрке 2. утро понедельника 3. вечер пятницы ответ строк...
  43-interview-decide-block / rules-892-new / повтор 2: Цитирую соответствующий фрагмент: ``` «формат»: Каким форматом уходит отчёт смежнику? 1. рекомендую: письмо простым текстом, его читают с телефона 2. простой текст 3. html «срок»: Когда отчёт уходит? 1. рекомендую: утро понедельника, к планёрке 2. утро понедельника 3. вечер пятницы ответ строкой: «и...
  43-interview-decide-block / rules-892-new / повтор 3: **Цитата из текста:** > «формат»: Каким форматом уходит отчёт смежнику? > 1. рекомендую: письмо простым текстом, его читают с телефона > 2. простой текст > 3. html > > «срок»: Когда отчёт уходит? > 1. рекомендую: утро понедельника, к планёрке > 2. утро понедельника > 3. вечер пятницы > > ответ строк...
  43-interview-decide-block / rules-892-old / повтор 1: Цитирую соответствующую часть: «формат»: Каким форматом уходит отчёт смежнику? 1. рекомендую: письмо простым текстом, его читают с телефона 2. простой текст 3. html «срок»: Когда отчёт уходит? 1. рекомендую: утро понедельника, к планёрке 2. утро понедельника 3. вечер пятницы ответ строкой: «имя 1, и...
  43-interview-decide-block / rules-892-old / повтор 2: Процитирую: «формат»: Каким форматом уходит отчёт смежнику? 1. рекомендую: письмо простым текстом, его читают с телефона 2. простой текст 3. html «срок»: Когда отчёт уходит? 1. рекомендую: утро понедельника, к планёрке 2. утро понедельника 3. вечер пятницы ответ строкой: «имя 1, имя 2», своими слова...
  43-interview-decide-block / rules-892-old / повтор 3: Цитирую соответствующий фрагмент: «формат»: Каким форматом уходит отчёт смежнику? 1. рекомендую: письмо простым текстом, его читают с телефона 2. простой текст 3. html «срок»: Когда отчёт уходит? 1. рекомендую: утро понедельника, к планёрке 2. утро понедельника 3. вечер пятницы ответ строкой: «имя 1...

# символы разбора судьи и заметок приведены к клавиатурным, дословный ответ судьи лежит в каталоге прогона (judge/answers) при --keep или своём --work, а без них каталог прогона временный и сносится вместе с ответом
```

- Обкатка: 2026-09-26 12:32, свежее дерево 2480e311e86c, сценарий a15c7407, временный HOME, утилит дерева 11, шагов 4, все зелёные.

Шаг 1 (зелёный):

```console
$ git show "$(git log --format=%H --grep 'DK-892 панель снимает ограждение' -n 1)^:tools/dashboard/static/app.js" > /tmp/dk892-old-app.js

```

Шаг 2 (зелёный):

```console
$ cd tools/dashboard && ! node testdata/poc_askpick.mjs /tmp/dk892-old-app.js
галочек при ограждённом блоке не пять, а 0:  это начало чата    September 26         Спросил по развилкам записи.   «печать»: кто собирает текст вопроса в чат?
1. рекомендую: утилита печатает блок из перечня развилок
2. агент собирает текст по шаблону скилла

«ответ»: какой формы ответ человека?
1. рекомендую: имя развилки и номер варианта
2. только словами
3. кнопкой панели

ответ строкой: «имя 1, имя 2», своими словами или «по рекомендации»   агент, 10:52 AM  
```

Шаг 3 (зелёный):

```console
$ cd tools/dashboard && node testdata/poc_askpick.mjs static/app.js
poc_askpick: галочка при каждой строке варианта, строка ответа в поле ввода, снятая отметка её уносит, ограждённый блок отмечается наравне с открытым, а живой признак без блока оборачивается подсказкой
```

Шаг 4 (зелёный):

```console
$ cd tools/taskctl && GOWORK=off go test -run TestChatBlockHeadBansFence ./...
ok  	github.com/dronrider/devkit/tools/taskctl	0.481s
```
