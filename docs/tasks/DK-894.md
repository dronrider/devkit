# DK-894: shipctl выбирает команду выката по путям диффа задачи

## Что происходит

В xr-proxy файл `.devkit/deploy.local` держит ключи `deploy` (xr-server на оба VPS) и `deploy_xr_hub`, а комментарий говорит, что роутеры, хаб, relay и APK «катаются по своим шагам из хэндоффа», и на такие merge надо передавать `--deploy` руками. Ключа под APK нет, релиз идёт скриптом `scripts/release-apk.sh`. В сессии XR-294 (2026-09-09, чат «Проблема с маршрутизацией трафика в xr-proxy») правка легла в `xr-core`, то есть в приложение, а пользователь попросил «задеплоить всё, обновить роутеры и перевыпустить приложения». Агент сам решал, какой компонент задет, и какой скрипт под него звать, читая комментарии конфига.

Автономия по пункту 8 правил доски кончается там, где кончается дефолтный ключ. Компонент не из дефолта агент катит либо по памяти, либо не катит совсем и отдаёт пользователю список команд. В XR-294 релиз APK ушёл пользователю с формулировкой «за владельцем», и пользователь вернул его вопросом «почему ты сам не сделал». Подсказка «что дальше» у `shipctl status` не называет компонентов диффа, это то же требование, что стояло в DK-191 про ветку автономии.

### Что снял разбор

Обвязка `internal/deployconf` знает четыре ключа: `deploy`, `test`,
`autonomous` и `deploy_timeout`. Остальные строки файла разбор пропускает
молча. `deploy_xr_hub` для утилиты не существует, его подставляют в `deploy`
руками перед хабовым слиянием. Формат плоский, «ключ = значение» через
`internal/kvconf`, секций в нём нет. Точка внутри имени ключа проходит без
правки разбора.

Команды компонентов в xr-proxy уже написаны и лежат скриптами:
`deploy-server.sh`, `deploy-router.sh`, `deploy-hub.sh`, `release-apk.sh`,
`release-xr-setup.sh`, `release-xr-share.sh`. Раскладке остаётся сопоставить
пути диффа со скриптом. Сочинять команды выката не придётся.
Условия, посильного только человеку, разбор тут не нашёл.

У `shipctl ship` есть тег `deployed`, и диапазон для правки, легшей мимо ветки,
берётся оттуда же, откуда состав поезда.

Соседний черновик DK-895 держит вторую половину той же сессии XR-294: текст
правил автономии и сторожа стенда. Здесь остаётся утилита, и связь между
строками ставится `taskctl dep add`.

## Чего хотим

`shipctl status` и `shipctl merge` печатают, какие компоненты задел дифф задачи и каким ключом каждый катится. При autonomous=true merge катит их все сам, а компонент без ключа это отказ с названным путём и подсказкой, какой ключ завести. Явное «задеплой» пользователя в проекте с раскладкой компонентов сводится к одной команде без чтения комментариев.

## Развилки

Сопоставление путей и ключей класть в `deploy.local` (например `deploy.xr-core = ...` или отдельная секция «компоненты») или выводить из структуры монорепозитория по имени крейта? Что делать, когда дифф задел два компонента с разными ключами, катить оба по очереди или отказывать? Как быть с багфиксом прямо в main без ветки, у которого нет merge, а выкат нужен.

- «раскладка»: Откуда shipctl берёт сопоставление путей диффа и команды выката?
  - решает: человек
  - рекомендация: пара ключей в deploy.local на компонент: команда выката и пути, которые его задевают
  - вариант: пути коммитятся отдельным файлом в .devkit, команды остаются в гитигнорнутом deploy.local
  - вариант: раскладка выводится из структуры монорепозитория по имени крейта
  - решено человеком 2026-09-10: пара ключей в deploy.local на компонент: команда выката и пути, которые его задевают
- «двое»: Дифф задачи задел два компонента с разными командами, что делает merge при autonomous=true?
  - решает: человек
  - рекомендация: катить оба по очереди в порядке из конфига, первый провал останавливает остальные
  - вариант: отказать и отдать выбор человеку
  - вариант: катить только тот компонент, чьих путей в диффе больше
  - решено человеком 2026-09-10: катить оба по очереди в порядке из конфига, первый провал останавливает остальные
- «безветки»: Багфикс лёг прямо в main без ветки и без merge, чем тогда выкатывается его компонент?
  - решает: человек
  - рекомендация: ship считает компоненты по диффу от тега deployed до main, отдельной команды не заводим
  - вариант: отдельная команда shipctl deploy с явным диапазоном коммитов
  - вариант: случай не покрывать, правка мимо ветки остаётся ручным выкатом
  - решено человеком 2026-09-10: ship считает компоненты по диффу от тега deployed до main, отдельной команды не заводим
- «конец»: Чем работа кончается: боевой раскладкой xr-proxy или механикой на синтетическом стенде?
  - решает: человек
  - рекомендация: работа кончается ключами компонентов в xr-proxy и выкатом оттуда одной командой
  - вариант: хватит синтетического репозитория с командами-заглушками в тестах, боевую раскладку заводит отдельная задача
  - решено человеком 2026-09-10: работа кончается ключами компонентов в xr-proxy и выкатом оттуда одной командой

## DoD

- Обвязка `.devkit/deploy.local` знает компоненты: у каждого своя команда
  выката и свои пути, которые его задевают. Разбор компонентных ключей лежит в
  `internal/deployconf` вместе с остальными.
- `shipctl status` по задаче печатает, какие компоненты задел дифф ветки и
  какой командой каждый катится.
- `shipctl merge` при `autonomous = true` катит задетые компоненты по очереди
  в порядке из конфига. Первый провал останавливает остальные, и это видно в
  отказе.
- Путь диффа, не попавший ни в один компонент, это отказ с названным путём и
  подсказкой, какой ключ завести.
- `shipctl ship` считает компоненты по диффу от тега `deployed` до `main`.
  Правка, легшая мимо ветки, выкатывается тем же способом.
- Тесты идут на синтетическом репозитории с командами-заглушками. Прогон
  проверяет печать компонентов, порядок выката, остановку на провале и отказ
  без ключа.
- Раздел про компоненты стоит в `tools/shipctl/README.md`, а образец ключей в
  `CONNECT.md` рядом с остальной раскладкой обвязки.
- Раскладка компонентов заведена в проекте, где их несколько, и выкат оттуда
  проходит одной командой. Проектных имён при этом нет ни в коде, ни в доке
  devkit. Они живут в гитигнорнутом конфиге самого проекта.

## Ранг

`25+8+1+0+5 = 39`, с надбавкой за цену 40, P2.

- Серьёзность 25: основной выкат из дефолтного ключа работает. Компонент мимо
  дефолта катится передачей `--deploy` руками, обход дешёвый и очевидный.
- Ценность 8: пользователь просил «задеплоить всё» и получил список команд
  вместо выката. Автономный выкат всех задетых компонентов закрывает исходную
  цель конвейера.
- Неопределённость 1: четыре развилки закрыты ответом человека, подход выбран,
  осталось сделать.
- Поправка на баг 0: механики выбора компонента не было вовсе, это новая
  работа.
- Рычаг 5: снимает ручной шаг с каждого недефолтного выката и держит DK-895.
  Правила автономии там обещают отказ утилиты на компоненте без ключа, а
  обещать его можно только после этой строки.

Цена M: правка обвязки, три точки в shipctl, тесты на синтетике, дока и
боевая раскладка.

## Приёмка

- вид: agent

Шаги сценария проверяются с машины агента. Печать компонентов читается из
вывода команды, выкат кончается кодом возврата и проверкой службы.
Необратимый релиз наружу гоняется сперва сухим прогоном.

## Ход работы

- Из черновика DK-894 «shipctl выбирает ключ выката по путям диффа задачи»: записан 2026-09-10, оформлен 2026-09-10.
- Вычитка: 1 файл, 9 правок, 0 пометок, 2026-09-10.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 4%, снимок 1м назад, сдвига нет), 2026-09-18 15:31-15:31.
- Вычитка: 2 файла, 1 правка, 1 пометка, 2026-09-18.

## Ревью

Уровень 3 до fe6ba07: critical_paths совпал по tools/shipctl (2 да триажа), плюс дифф меняет путь выката, от которого зависит DoD фичи: до fe6ba07f

- блокирует: README.md, CONNECT.md и оба теста (components_test.go, deployconf_test.go) берут в примеры xr-core, xr-hub, apk. Это реальные имена частей xr-proxy: xr-core и xr-hub её члены Cargo workspace, apk её релизный скрипт (docs/lld/DK-194-project-map.md, постановка DK-894). DoD прямо запрещает проектные имена в коде и доке devkit. Заменить на нейтральные имена вроде svc-a/svc-b или web/worker во всех четырёх файлах.: исправлено, заменены на web/worker/mobile во всех четырёх файлах (README.md, CONNECT.md, components_test.go, deployconf_test.go) плюс комментарии в internal/deployconf/deployconf.go; проверено грепом по всему диффу ветки
- [возврат: правила, 2026-09-18] не блокирует: Match отдаёт первый по конфигу компонент, если путь попадает под paths сразу двух компонентов (перекрывающиеся paths), молча и без предупреждения; поведение детерминированное, но не описано в README и не покрыто тестом. Стоит либо задокументировать порядок разрешения конфликта в README, либо добавить тест на этот случай.: исправлено, README.md, раздел «Раскладка компонентов»: добавлена фраза про first-by-config; тест TestMatchOverlappingPathsPicksFirstByConfig в internal/deployconf/deployconf_test.go
- [возврат: правила, 2026-09-18] не блокирует: pathUnder в internal/deployconf/deployconf.go корректно отличает tools/ship от tools/shipctl (сравнение по границе / или точному совпадению), но регрессионного теста на такую коллизию префиксов нет ни в deployconf_test.go, ни в components_test.go. Стоит добавить кейс в TestMatch.: исправлено, тест TestMatchPathBoundaryNotPrefixCollision в internal/deployconf/deployconf_test.go, кейс tools/ship против tools/shipctl

## Сценарий проверки

Раскладка и команды-заглушки живут внутри самих тестов,
`tools/shipctl/components_test.go` и `internal/deployconf/deployconf_test.go`.
Каждый тест сам заводит синтетический репозиторий и убирает его за собой.
Шаги ниже гоняют эти тесты с машины агента.

1. `status` по задаче в работе печатает задетые раскладкой компоненты вместе
   с их командой, а путь мимо раскладки называет информационной строкой, не
   отказом.

```sh
cd tools/shipctl && go test -run 'TestStatusPrintsComponents|TestStatusComponentsMissingPath' -v .
```

Ожидание: `--- PASS: TestStatusPrintsComponents` и `--- PASS:
TestStatusComponentsMissingPath`. Первый тест находит строку `компоненты:
web (touch ...)`, второй находит `infra/loader.py` рядом со строкой «не
задет ни одним компонентом» и подсказкой `deploy.<имя>`.

2. `merge` при `autonomous = true` катит задетые дифф-веткой компоненты по
   очереди, в порядке ключей `deploy.<имя>` конфига.

```sh
cd tools/shipctl && go test -run 'TestMergeComponentsHappyPath' -v .
```

Ожидание: `--- PASS: TestMergeComponentsHappyPath`. Оба маркера компонентов
(`web.marker`, `worker.marker`) появляются на диске, отчёт называет «выкат
компонентов прошёл: web, worker» в порядке конфига, звонок стенда несёт
«move XR-001 check».

3. Второй по конфигу компонент падает. Первый к этому моменту уже выкачен и
   не перезапускается. Отказ называет упавший компонент и то, что уже
   уехало.

```sh
cd tools/shipctl && go test -run 'TestMergeComponentsStopsOnFirstFailure' -v .
```

Ожидание: `--- PASS: TestMergeComponentsStopsOnFirstFailure`. Отказ несёт
`worker`, строку «уже выкачены: web» и «In progress», маркер первого
компонента на диске есть, маркера второго нет.

4. Путь диффа мимо раскладки это отказ `merge`, до тестов и ребейза. Он
   называет путь и подсказывает, какую пару ключей завести.

```sh
cd tools/shipctl && go test -run 'TestMergeRefusesUnmappedPath' -v .
```

Ожидание: `--- PASS: TestMergeRefusesUnmappedPath`. Отказ несёт
`infra/loader.py` и `deploy.<имя>`, доска не тронута.

5. `ship` считает задетые компоненты по диапазону от тега `deployed` до
   `main`. Два поездных слияния в разные компоненты выкатываются одним
   `ship` по очереди, обе задачи переводятся в Check разом.

```sh
cd tools/shipctl && go test -run 'TestShipRunsComponentsForTrain' -v .
```

Ожидание: `--- PASS: TestShipRunsComponentsForTrain`. Отчёт несёт «поезд
выкачен по компонентам», оба маркера на диске, звонки стенда несут «move
XR-001 check» и «move XR-003 check».

6. Раскладка компонентов заводится в проекте с несколькими частями выката, в
   его собственном гитигнорнутом `.devkit/deploy.local`. Пара ключей
   `deploy.<имя>` и `deploy.<имя>.paths` на каждую часть. Первым идёт сухой
   прогон: `shipctl status` из корня этого проекта печатает задетые
   компоненты и их команды, ничего не запуская. Настоящий выкат (`shipctl
   merge` при `autonomous = true` либо `shipctl ship`) идёт только после
   того, как сухой прогон подтвердил верное сопоставление путей. Он
   заканчивается кодом возврата команды вместе с проверкой самой службы.
   Шаг идёт вне синтетического стенда, без проектных имён и адресов, и
   своего блока `sh` здесь не несёт. Команда та же, что в шагах 1 и 2, но на
   боевом дереве вместо тестового.

## Проверка


- Обкатка: 2026-09-18 16:48, свежее дерево 73e1b9607517, сценарий ae5f4468, временный HOME, утилит дерева 11, шагов 5, все зелёные.

Шаг 1 (зелёный):

```console
$ cd tools/shipctl && go test -run 'TestStatusPrintsComponents|TestStatusComponentsMissingPath' -v .
=== RUN   TestStatusPrintsComponents
--- PASS: TestStatusPrintsComponents (0.33s)
=== RUN   TestStatusComponentsMissingPath
--- PASS: TestStatusComponentsMissingPath (0.32s)
PASS
ok  	github.com/dronrider/devkit/tools/shipctl	1.162s
```

Шаг 2 (зелёный):

```console
$ cd tools/shipctl && go test -run 'TestMergeComponentsHappyPath' -v .
=== RUN   TestMergeComponentsHappyPath
--- PASS: TestMergeComponentsHappyPath (2.90s)
PASS
ok  	github.com/dronrider/devkit/tools/shipctl	3.793s
```

Шаг 3 (зелёный):

```console
$ cd tools/shipctl && go test -run 'TestMergeComponentsStopsOnFirstFailure' -v .
=== RUN   TestMergeComponentsStopsOnFirstFailure
--- PASS: TestMergeComponentsStopsOnFirstFailure (0.96s)
PASS
ok  	github.com/dronrider/devkit/tools/shipctl	1.816s
```

Шаг 4 (зелёный):

```console
$ cd tools/shipctl && go test -run 'TestMergeRefusesUnmappedPath' -v .
=== RUN   TestMergeRefusesUnmappedPath
--- PASS: TestMergeRefusesUnmappedPath (0.47s)
PASS
ok  	github.com/dronrider/devkit/tools/shipctl	0.917s
```

Шаг 5 (зелёный):

```console
$ cd tools/shipctl && go test -run 'TestShipRunsComponentsForTrain' -v .
=== RUN   TestShipRunsComponentsForTrain
--- PASS: TestShipRunsComponentsForTrain (3.69s)
PASS
ok  	github.com/dronrider/devkit/tools/shipctl	4.558s
```
