# DK-899: Оболочка снимает голову, которая ждёт машинного события

## Что происходит

2026-09-10, конвейер DK-893 живой головой из дашборда. Исполнитель дошёл до
слияния, и `shipctl merge` отбился дважды: в 09:41 из-за незакоммиченной
записи ревью в дереве задачи, в 09:42 из-за грязной доски в основном дереве,
где соседняя сессия оформляла DK-895. Исполнитель взвёл фоновое ожидание
чистой доски и каждый ход заканчивал коротким отчётом «слияние ждёт соседа».
Оболочка `task-run.py` на каждый такой конец хода через десять секунд подала
заказ «Продолжай выполнение DK-893» (09:42:41, 09:43:47, 09:44:40). В ленте
чата заказ выглядит репликой человека, он идёт в окно тем же `tmux send-keys`,
что и реплика из панели. В 09:45:13 сработала воронка «три прохода подряд
вхолостую», оболочка вышла с кодом 1 и сняла голову вместе с окном tmux. Через
полминуты сосед запушил, и доска стала чистой. Строки в `.devkit/log`
с 81144 по 81195, транскрипт сессии 88a28c88.

Тот же рисунок в ту же ночь у DK-510. В 01:43 исполнитель после второго круга
ревью позвал `shipctl ship` фоновой командой и ход закончил словами «сторож на
конец merge взведён, вернусь с итогом». Три прохода по двадцать секунд, в
01:44:56 воронка, окно снято. Слияния в журнале после этого нет, коммиты
DK-510 лежат только в ветке dk-510. Строки в `.devkit/log` с 80077 по 80094,
сессия 3d1c4c04.

Обе задачи остались в in-progress без слияния, окно закрыто, и с экрана это
неотличимо от падения чата. Два случая за одну ночь на четырёх задачах в
работе. Исполнитель работал по правилам. Коммитить чужую
правку доски нельзя, ждать соседа было по делу. Воронка заведена под голову,
которая вылетает на подъёме без логина или квоты, а порог у неё только
временной, полторы минуты на проход. Отчёт об ожидании укладывается в
него так же, как пустой выход. Заказ на продолжение один на все исходы и
причину прошлого прохода не везёт. Поэтому на отбитое слияние он отвечает так
же, как на уснувшую голову. Тот же промах уже снял субагентов в DK-864.

Парковка по DK-400 с причиной «окружение:» здесь не спасает. Для оболочки это
тот же стоп, окно закрывается, а пробуждение идёт тиком сторожка раз в пять
минут и возвращает строку без живой сессии. Ради трёх минут ожидания чужого
коммита это дорого.

### Проверка

Разбор 2026-09-10 сверил предмет с кодом. Воронка живёт в `task-run.py`
(`IDLE_SECONDS = 90`, `IDLE_LIMIT = 3`). Проход в счёт не идёт только в двух
случаях: сессия оставила живых субагентов либо задача с приёмкой mixed или
user стоит в check. Ход, который кончился отчётом об ожидании, ни под один из
них не подходит и считается холостым наравне с головой, вылетевшей на
подъёме. Команды ожидания у `agentctl` нет, журнала ходов
`~/.devkit/turns.log` на диске тоже нет.

## Чего хотим

Исполнитель говорит оболочке, сколько ждать и почему. Оболочка на такой
проход заказа не подаёт и в воронку его не считает. По истечении срока она
подаёт заказ либо просыпается раньше по уведомлению фоновой команды, как
сейчас просыпается по концу субагента и по реплике человека. Окно с ждущей
головой живёт до конца ожидания.

## Границы

Заказ на продолжение причину прошлого прохода по-прежнему не везёт, на это
заводится своя строка. Машинные разряды парковки, обход ждущих из `close` и
`merge` и подъём упавшей сессии сюда не входят. Они лежат в цели про
непрерывный конвейер (черновик DK-902).

## Развилки

Форма отметки от исполнителя: отдельная команда (`agentctl wait --seconds N
--reason ...`) либо отметка хода со словом «ждёт» и сроком в журнале
`~/.devkit/turns.log`, который оболочка читает. Считать ли проход с такой
отметкой в потолок проходов. Везти ли в заказе причину прошлого исхода.

- «место»: Оформлять DK-899 строкой задачи сейчас или ждать, пока цель DK-902 заведут и нарежут?
  - решает: человек
  - рекомендация: строкой сейчас, узко: ход с живым ожиданием не холостой. Голову оболочка снимает уже сегодня, а цель ещё черновик, и в состав цели строка попадёт нарезкой
  - вариант: оформить строкой сейчас, цель возьмёт её в состав нарезкой
  - вариант: отложить до заведения цели DK-902, задачу нарежет цель
  - решено человеком 2026-09-10: строкой сейчас, узко: ход с живым ожиданием не холостой. Голову оболочка снимает уже сегодня, а цель ещё черновик, и в состав цели строка попадёт нарезкой
- «форма»: Чем исполнитель говорит оболочке, что ждёт машинного события?
  - решает: человек
  - рекомендация: команда agentctl wait с условием и сроком: условие машинное, читается с диска, и одинаково годится голове окна, субагенту и headless, а оболочке не приходится разбирать слова хода
  - вариант: команда agentctl wait <условие> --until <срок>
  - вариант: отметка конца хода со словом ожидания и сроком в журнале ходов, оболочка её читает
  - решено человеком 2026-09-10: команда agentctl wait с условием и сроком: условие машинное, читается с диска, и одинаково годится голове окна, субагенту и headless, а оболочке не приходится разбирать слова хода
- «заказ»: Чинить ли этой же задачей заказ на продолжение, который не везёт причину прошлого прохода?
  - решает: человек
  - рекомендация: не чинить: предмет строки это ожидание, а заказ с причиной трогает всю ленту заказов и тянет на свою строку
  - вариант: нет, причина прошлого прохода едет отдельной строкой
  - вариант: да, тем же диффом
  - решено человеком 2026-09-10: не чинить: предмет строки это ожидание, а заказ с причиной трогает всю ленту заказов и тянет на свою строку
- «потолок»: Считается ли проход с отметкой ожидания в потолок проходов PASS_LIMIT?
  - решает: исполнитель
  - рекомендация: проход ожидания не считать ни в воронку, ни в потолок проходов: иначе шесть коротких ожиданий снимут окно тем же способом
  - решено исполнителем 2026-09-10: не считать ни туда, ни туда. Потолок стоит от головы, которая жжёт бюджет, а ожидание бюджета не тратит. Сессия стоит в окне и ничего не просит. Потолок считается вычетом, `n - holds >= passes`, а число ожиданий за цикл держит срок каждого из них

## DoD

- Исполнитель отмечает машинное ожидание командой `agentctl wait` с условием и
  сроком, и ход после этого кончается штатно.
- Оболочка `task-run.py` читает отметку, заказа на такой проход не подаёт и в
  воронку с потолком проходов его не считает. Окно живёт до конца срока.
- Срок вышел или событие пришло раньше, и оболочка подаёт заказ так же, как
  сейчас на конец субагента.
- Ожидание видно снаружи. В `.devkit/log` и в чате стоит строка с условием и
  сроком вместо «проход прошёл вхолостую».
- Тест на синтетическом стенде: голова с отметкой ожидания живёт три прохода
  подряд, она же без отметки снимается воронкой на третьем.
- Правило «ход с живым ожиданием не холостой» стоит в промптах исполнителя, а
  прогон стенда obeycheck по скиллу `prompt-test` лежит в файле задачи.

## Ранг

`50+7+3+5+4 = 69`, P1.

- Серьёзность 50: конвейер бросает задачу на слиянии, обход есть, но
  болезненный, работу до утра поднимает человек руками (два случая за ночь,
  DK-893 и DK-510).
- Ценность 7: ночная работа доходит до слияния сама, ради этого конвейер и
  заведён.
- Неопределённость 3: подход выбран человеком, а детали открыты. Правка идёт
  через границу языков, отметку пишет go-утилита, а читает python-оболочка.
- Баг +5: голова, работавшая по делу, снимается вместе с окном, и работа
  теряется.
- Рычаг 4: ночные стопы снимаются у всякой задачи под оболочкой, и это первый
  пункт цели про непрерывный конвейер.

Цена M: подкоманда `agentctl` с тестами, правка воронки и правило в промптах
исполнителя ложатся в одну сессию.

## Приёмка

- вид: agent
- барьер «глаза»: ожидание видно снаружи строкой в `.devkit/log` и ответом
  самой команды в ленте чата
  - шаг 1 сценария печатает строку команды, шаг 4 держит строки оболочки в
    журнале стенда. На живое окно смотреть не надо

## Ход работы

- Из черновика DK-899 «Оболочка конвейера снимает голову, которая ждёт чужого коммита»: записан 2026-09-10, оформлен 2026-09-10.
- Вычитка: 1 файл, 5 правок, 1 пометка, 2026-09-10.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 28%, week_max 18% (панель разбивку не дала, цифра из кеша клиента возрастом 5ч 27м назад), снимок 4м назад, сдвига нет), 2026-09-10 14:03-14:03.
- Первый прогон стенда взял все сценарии с предметом board-task (`--for`).
  Прогон вышел не зачтённым из-за 17-verify-runner, красного на обеих
  раскладках и не связанного с этой правкой. Запись снята, оставлен прогон по
  тронутому разделу.
- Вычитка: 4 файла, 23 правки, 1 пометка, 2026-09-10.

## Сценарий проверки

1. Команда кладёт отметку и говорит конвейеру, что из неё следует.

```sh
cd tools/agentctl && GOWORK=off go build -o /tmp/agentctl-dk899 . && /tmp/agentctl-dk899 -C .. wait DK-899 срок --until 1m --note "проба сценария"
```

Первая строка называет условие и срок, вторая путь отметки и то, что оболочка
не подаст заказ на этот проход.

2. Отметка легла записью на задачу.

```sh
cat ~/.devkit/waits/DK-899.json
```

В записи стоят поля `task`, `kind`, `until` и `note` с теми же значениями.

3. Отметка без срока не кладётся.

```sh
/tmp/agentctl-dk899 -C . wait DK-899 срок ; echo "код $?"
```

Команда отказывает словами «жду срок ожидания» и выходит кодом 1.

4. Стенд оболочки. Голова с отметкой живёт три прохода, без отметки воронка
   снимает её.

```sh
python3 kit/skills/board-task/task_run_test.py
```

Стенд печатает «Ran 54 tests» и «OK».

5. Тест краснеет на старом коде.

```sh
regcheck --base main --tests kit/skills/board-task/task_run_test.py -- python3 kit/skills/board-task/task_run_test.py -k wait
```

Утилита печатает «тест краснеет на main и проходит на текущем коде, регрессия закрыта».

6. Уборка за сценарием.

```sh
rm -f /tmp/agentctl-dk899 ~/.devkit/waits/DK-899.json ; echo "код $?"
```

Уборка выходит кодом 0.

## Проверка

- Стенд: 2026-09-10 15:21, дерево c6d1b505, предмет ecb9778f, база старый, ярус mini, k 3, сценарии 31-wait-mark, зачтён.

```console
$ obeycheck --task DK-899 --only 31-wait-mark -k 3 --base старый /private/tmp/claude-501/-Users-rider-projects-devkit/bf22c800-fca2-4d38-a1bd-b39e1709321b/scratchpad/rules-new /private/tmp/claude-501/-Users-rider-projects-devkit/bf22c800-fca2-4d38-a1bd-b39e1709321b/scratchpad/rules-old
сценарий                                      rules-new  rules-old  вердикт
отметить машинное ожидание перед концом хода        3/3        0/3  польза

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  31-wait-mark / rules-old / повтор 1: проверка: exit status 1 (отметки ожидания нет: оболочка сочтёт проход холостым)
```
