# DK-900: Субагент затирает план родителя общей меткой agentctl plan

## Что происходит

Ситуация. План работ сессии живёт файлом под меткой, метку берут из
DEVKIT_PLAN_LABEL либо из аргумента --label (tools/agentctl/plan.go:35, 71).
Признак дочерней сессии DEVKIT_PLAN_CHILD у механики есть.

Осложнение. При работе над DK-888 исполнитель вёл план под меткой задачи, а
поднятый им субагент-ревьювер позвал plan set --label DK-888 со своей стороны и
переписал файл целиком. Исполнитель восстанавливал этапы руками, повторными
plan done. Метка совпала, потому что она называет задачу, а задача у них одна.

Двенадцатого сентября повторилось на DK-936, и путь метки там другой. Субагент
вычитки унаследовал ID сессии исполнителя, метку взял из окружения и позвал
plan set без всякого --label, а план захода переписался целиком. Значит
дочерней сессии мало запретить явную метку, у неё и наследованная зовёт тот
же файл.

Вопрос. Должна ли метка различать заходы внутри одной задачи, или дочерней
сессии писать в общий план вообще незачем.

Гипотеза. Дочерняя сессия узнаётся признаком DEVKIT_PLAN_CHILD, и явный --label
у неё либо уходит в свою ветку файла, либо отклоняется с подсказкой.

### Приписка 2026-09-17

Третий случай на пачке мелких задач. Исполнитель DK-467 позвал `plan done` без `--label`, и пункт «спавн исполнителей» закрылся в плане диспетчера. Обход тот же: субагент дальше звал план только с `--label DK-467`.

### Приписка 2026-09-17, разнобой записи и чтения

Четвёртый случай той же механики, и субагент тут ни при чём. В сессии груминга
DK-1029 `plan set` записал план в `~/.devkit/plans/<sid>.json`, а `plan show`
следом напечатал ветку субагента из `<sid>-sub-DK-1029.json`. Запись и чтение
в одном окружении разошлись по файлам, и `--sid <sid>` картины не поменял.
Файл плана самой сессии при этом цел, диспетчер просто не видит своих пунктов.

В окружении сессии стоит `CLAUDE_CODE_CHILD_SESSION=1`, который клиент ставит
всем сессиям подряд (DK-852). Похоже, `show` читает признак дочерней сессии, а
`set` его не смотрит, и обход «звать план только с --label» от этого случая не
спасает: метки у команды не было вовсе.

### Разбор 2026-09-17

Гипотеза выше протухла. Переменной DEVKIT_PLAN_CHILD в коде нет и не было, а
ворота по CLAUDE_CODE_CHILD_SESSION сняла DK-852: признака дочерней сессии в
механике не осталось вовсе (tools/agentctl/plan.go:32-42). Предмет при этом
живой, и проверен он на временном HOME. План из трёх пунктов с начатым первым
исчез после `plan set` с тем же sid и без метки, а на его месте легли два чужих
пункта.

Различителя захода в окружении нет ни одного. Субагент живёт внутри процесса
головы. CLAUDE_PID, сокет обмена и CLAUDE_CODE_SESSION_ID у них совпадают до
символа. Свой идентификатор у захода один, toolUseId в сайдкаре
`~/.claude/projects/<проект>/<sid>/subagents/`. Пути к сайдкару субагенту не
дают. Хука SubagentStart, который прочитал бы сайдкар и положил метку, в
настройках нет.

Метку DEVKIT_PLAN_LABEL сегодня только читают. Присвоения ей нет нигде в
дереве, а харнес не даёт положить пару окружения при подъёме субагента. Правило
«поднявший кладёт её парой окружения» (kit/skills/work-plan/SKILL.md:105,
tools/agentctl/README.md:1887) не опирается ни на какую автоматику. Метку
называет себе сам субагент флагом `--label`, как велит его определение
(kit/agents/exec-*.md:81, kit/agents/review-*.md:67). На метке задачи он и
сходится с родителем.

Маску имени `<sid>-sub-<метка>.json` разбирают своим кодом три места:
planSubFiles (tools/agentctl/plan.go:256), subPlanFiles дашборда
(tools/dashboard/sessions.go:2729) и label_plans сторожка
(hooks/plan-watch.py:144). Общей библиотеки у них нет, и смена схемы имён
правится в трёх местах на двух языках.

Приписку про разнобой записи и чтения проверка подтвердила симптомом, но не
причиной. Признака дочерней сессии не читает ни `set`, ни `show`. Оба идут через
planResolve. Свой план `show` печатает первым и только потом подмешивает планы
субагентов, на синтетике видно оба. Путь, на котором своих пунктов не видно,
другой. С непустой меткой `show` адресует один файл с меткой и в обход
субагентов не заходит вовсе (plan.go:296-311). Значит в том заходе
DEVKIT_PLAN_LABEL стояла в окружении на `show` и не стояла на `set`. Утечь она
могла тем же путём, каким в DK-852 текла CLAUDE_CODE_CHILD_SESSION, из
глобального окружения сервера tmux.

Черновик DK-1012 про вложенного субагента вычитки это тот же предмет с другого
захода, и он пойдёт припиской к строке.

## Чего хотим

## Границы

Схема имени файла плана не меняется. Маску `<sid>-sub-<метка>.json` разбирают
своим кодом три места, `agentctl`, дашборд и `hooks/plan-watch.py`, и смена
схемы правится в трёх местах на двух языках.

Выдача `plan show` с непустой меткой в объём не входит, у неё своя запись
DK-1040. Кольцо дашборда при метке тоже мимо, это DK-991.

## Развилки

- «кейсы»: какие пользовательские кейсы у DK-900? рекомендую сбалансированный: узкий молчит про (3), где страдает не set, а done, и про (4), где заход вложен в исполнителя, а широкий тянет внутрь (5) с чужим предметом наблюдаемости show и (6), который не кейс, а граница правки
  - решает: человек
  - рекомендация: сбалансированный состав, 4 кейса: (1) исполнитель ведёт план с меткой задачи, поднятый им ревьювер берёт ту же метку и кладёт свой план поверх (DK-888); (2) субагент без метки пишет по унаследованному sid и стирает план головы захода (DK-936); (3) субагент зовёт plan done без метки и закрывает чужой пункт в плане диспетчера (DK-467); (4) вложенный субагент вычитки внутри исполнителя берёт метку задачи и трёт план исполнителя (DK-1012)
  - вариант: узкий состав, 2 кейса: (1) ревьювер берёт метку задачи и кладёт план поверх плана исполнителя (DK-888); (2) субагент без метки пишет по унаследованному sid и стирает план головы захода (DK-936)
  - вариант: широкий состав, 6 кейсов: (1) ревьювер с меткой задачи поверх плана исполнителя (DK-888); (2) субагент без метки по унаследованному sid (DK-936); (3) plan done без метки закрывает чужой пункт (DK-467); (4) вложенная вычитка внутри исполнителя с меткой задачи (DK-1012); (5) метка утекла в окружение, и plan show с непустой меткой печатает только её файл, ни своих пунктов, ни соседских (приписка про разнобой); (6) кольцо дашборда и сторожок plan-watch разбирают маску имени своим кодом, и новая схема обязана остаться видимой обоим (DK-991)
  - источник: код, tools/agentctl/plan.go:296-311 и tools/dashboard/sessions.go:2729, оттуда кейсы (5) и (6) широкого состава
  - решено человеком 2026-09-17: сбалансированный состав, 4 кейса: (1) исполнитель ведёт план с меткой задачи, поднятый им ревьювер берёт ту же метку и кладёт свой план поверх (DK-888); (2) субагент без метки пишет по унаследованному sid и стирает план головы захода (DK-936); (3) субагент зовёт plan done без метки и закрывает чужой пункт в плане диспетчера (DK-467); (4) вложенный субагент вычитки внутри исполнителя берёт метку задачи и трёт план исполнителя (DK-1012)

### Приписка 2026-09-17: повтор на DK-1022

Субагент ревью с меткой DK-1022 затёр план исполнителя своим, и закрыть пункты перед возвратом исполнителю стало нечем.
- «механизм»: чем различать заходы внутри одной задачи? рекомендую пару: ворот ловит кейсы (1), (2) и (4), где план трут целиком, а кейс (3) с plan done ворот не видит, его закрывает только своя метка у захода
  - решает: человек
  - рекомендация: пара: ворот на plan set, когда новый набор пунктов не пересёкся со старым ни одним, плюс хвост захода в метке (роль или ключ вложенности) правилом в определениях агентов и в work-plan
  - вариант: только машинный ворот: plan set отказывает поверх непохожего плана и просит --label либо --force, правила и определения агентов не трогаем
  - вариант: только правило: метка обязана нести хвост захода, машинной защиты нет
  - вариант: субагент план не ведёт вовсе: план держит голова, а субагент сдаёт шаги итогом
  - источник: код, tools/agentctl/plan.go:78-104 и docs/tasks/archive/2026/DK-852.md:36-46, различителя головы и субагента в окружении нет, а метку DEVKIT_PLAN_LABEL в devkit никто не кладёт
  - решено человеком 2026-09-17: пара: ворот на plan set, когда новый набор пунктов не пересёкся со старым ни одним, плюс хвост захода в метке (роль или ключ вложенности) правилом в определениях агентов и в work-plan
- «правило метки»: что делать с фразой про DEVKIT_PLAN_LABEL в kit/skills/work-plan/SKILL.md:105 и tools/agentctl/README.md:1887?
  - решает: исполнитель
  - рекомендация: снять обещание «поднявший кладёт метку парой окружения»: переменная остаётся входом для оболочки и человека, а правило говорит, что метку называет себе сам заход флагом --label по своему определению
  - вариант: оставить фразу замыслом на будущее харнеса и дописать оговорку, что сегодня её никто не исполняет
- «ценность»: какая ценность у DK-900? остальные слагаемые беру фактами: серьёзность 25, обход дешёвый и очевидный; неопределённость 1, подход выбран ответом «механизм»; поправка на баг 5; рычаг 2, польза своя
  - решает: человек
  - рекомендация: 5: чинится заметное трение, но работа задачи от потери плана не встаёт
  - вариант: 7: план это канал наблюдаемости, по кольцу и панели человек понимает, чем занят заход, и потеря плана бьёт по главному способу вести разработку
  - вариант: 3: небольшое удобство, случай редкий и обход известен
  - решено человеком 2026-09-17: 7: план это канал наблюдаемости, по кольцу и панели человек понимает, чем занят заход, и потеря плана бьёт по главному способу вести разработку

## DoD

`plan set` отказывает, когда новый набор пунктов не совпал со старым ни одним
пунктом. В отказе названы оба выхода, своя метка и `--force`. Закрытие чужого
пункта ворот не ловит, и адресность даёт метка: вложенный заход берёт её с
хвостом роли, а велят это определения агентов и скилл `work-plan`.

Снаружи работа видна тремя признаками. Отказ приходит текстом с именем чужого
плана. Кольцо дашборда показывает два плана вместо одного затёртого. Тесты
`tools/agentctl/plan_test.go` закрывают все четыре кейса состава: ревьювер с
меткой задачи, субагент без метки по унаследованному sid, `plan done` без метки
и вложенная вычитка внутри исполнителя.

## Ранг

`25+7+1+5+2 = 40`, P2.

Серьёзность 25. Заход теряет записанный план, а обход дешёвый и очевидный:
звать план со своей меткой.

Ценность 7, названа человеком на разборе. План это канал наблюдаемости, по
кольцу и панели человек понимает, чем занят заход.

Неопределённость 1. Подход выбран развилкой «механизм», правка живёт в одном
процессе и одном языке.

Поправка на баг 5. Механика теряет то, что ей записали.

Рычаг 2. Польза своя: кольцо и сторожок плана перестанут показывать чужие
пункты.

## Ход работы

- Из черновика DK-900 «Субагент затирает план родителя общей меткой agentctl plan»: записан 2026-09-10, оформлен 2026-09-17.
- Разработка: субагент fable/medium по вердикту pick (маппинг opus, корректор: профицит week_all, week_max; квота: week_all 42% профицит, week_max 60% профицит, снимок 22м назад), 2026-09-17 22:51-23:16.
- Разбор кода: сторож `hooks/plan-watch.py` маску имени читает как прежде, а роль в хвосте метки в неё укладывается, правки сторожа не потребовалось.
- regcheck: тесты четырёх кейсов в `tools/agentctl/plan_test.go` красны на коде до правки и зелены после, прогон 2026-09-17.
- Ревью 2026-09-17: три замечания отработаны. Правило хвоста роли вписано в `kit/agents/exec-*.md`, `kit/agents/review-*.md` и `kit/skills/work-plan/SKILL.md`, обещание метки парой окружения снято. Предел ворот (одного совпавшего пункта хватает) назван в README agentctl. Изъятие для доделанного плана сужено до адреса из `DEVKIT_TMUX`, тест `TestPlanSetOverFinishedPlan` дописан и краснеет на широком изъятии по regcheck.
- Стенд: сценарий `47-review-label-tail` на паре core, ревьювер под новым определением берёт метку `OB-047-review` 3/3, под старым `OB-047` 0/3, вердикт «польза».
- Ревью, второй круг, замечание 4: в сценарий 30 дописана проверка метки у `plan step` и `plan done` по событиям Bash транскрипта, мутация «done без метки» краснеет. Прогон пары core на mini, k 3: новое определение 1/3, старое 0/3, вердикт «не хуже», зачтён. Красные клетки не про метку: на новом определении мини-модель дважды не положила план вовсе, на старом ни разу не закрыла пункт. Пометка исключения сужена до фразы про вложенный заход.

## Ревью

Уровень 2 до 0885080: неопределённость 1, ворота на plan set трогают путь наблюдаемости из DoD; tools/agentctl вне critical_paths

- [возврат: постановка, 2026-09-17] блокирует: правило метки из DoD не поставлено. Ворот сделан, второй половины пары нет. Определения kit/agents/exec-*.md:81 и kit/agents/review-*.md:67 по-прежнему велят субагенту брать --label <ID задачи>. Скилл kit/skills/work-plan/SKILL.md:103-106 обещает метку парой DEVKIT_PLAN_LABEL от поднявшего, и разбор задачи это обещание опроверг. Кейс DK-888 до правки определений воспроизводится так же. Кончается он теперь отказом вместо потери плана. Проверить грепом label по kit/agents. Правка промптов идёт со стендом prompt-test. Вынос хвоста роли в DK-881 сужает DoD, и сузить его должен человек словом.: исправлено, правило хвоста роли вписано в kit/agents/exec-*.md, kit/agents/review-*.md и kit/skills/work-plan/SKILL.md, обещание метки парой окружения снято; стенд: сценарий 47-review-label-tail, 3/3 против 0/3, польза, след в «Проверке»; коммиты 7f6eef7b, c30e05e6, f1391203
- [возврат: реализация, 2026-09-17] не блокирует: предел ворот не назван в README. Одного случайно совпавшего пункта хватает на проход чужого набора. На временном доме набор «разведка, чужой пункт один, чужой пункт два» лёг поверх плана «разведка, правка, коммит» молча и унёс два пункта. Критерий заказан DoD, вопрос тут только к доке. Раздел «Чужой план» в tools/agentctl/README.md называет предел step и done, а про этот молчит. Хватит строки рядом. Ворот держится на тексте пунктов и пропускает набор, совпавший с лежащим планом хоть одним пунктом.: исправлено, предел ворот назван в README agentctl, раздел «Чужой план»: одного совпавшего пункта хватает на перекладку; коммит 81cabd44
- [возврат: реализация, 2026-09-17] не блокирует: изъятие для доделанного плана шире своего повода. planForeign (tools/agentctl/plan.go:242) пропускает всякий закрытый целиком план. Комментарий и README называют поводом контур второй подписки, где адрес плана это имя tmux. Проверка на временном доме показала обратную сторону. Диспетчер закрыл оба пункта, следом безымянный plan set субагента лёг поверх, и запись о сделанном пропала вместе с кольцом. Изъятие стоит сузить до повода. planAddr помнит, откуда взят sid, и пропуск можно дать адресу из DEVKIT_TMUX.: исправлено, изъятие сужено до адреса из DEVKIT_TMUX (поле tmux у planAddr), комментарий и README переписаны, тест TestPlanSetOverFinishedPlan проверяет отказ поверх доделанного плана у адреса из ID сессии, regcheck красный на широком изъятии; коммит 81cabd44
- [возврат: правила, 2026-09-18] не блокирует: пометка исключения стенда шире правды. Правка kit/agents/exec-*.md не только повторяет правило ревьювера. Она заводит новое требование, метку в каждой команде плана, включая step и done. Это смена шага, то есть повод прогона по порогу скилла prompt-test. Стенд исполнителя поднимать умеет: сценарии 30-plan-close-on-return и 27-work-plan держат exec-*.md ключом «предмет». Непокрытым стендом остаётся только вложенный заход. Выход любой из двух. Дописать проверку метки у step и done в сценарий 30 и прогнать пару. Либо оставить пометку, назвав в ней ровно вложенный заход и признав требование метки непроверенным.: исправлено, в сценарий 30-plan-close-on-return дописана проверка метки у plan step и plan done по событиям Bash транскрипта, мутация без метки краснеет; прогон пары core на mini k 3: 1/3 против 0/3, не хуже, зачтён, след в «Проверке»; пометка исключения сужена до фразы про вложенный заход; коммит 1116db1b
- Вердикт: без замечаний. Круг 3 до 09158b5c: замечание 4 закрыто. Проверка метки в сценарии 30 меряет заявленное: на подставном транскрипте команда plan done без метки краснеет («команда плана без метки»), транскрипт без команд плана краснеет тоже, полный набор с метками зелёный. След стенда на пару core (mini, k 3, база старый, 1/3 против 0/3, «не хуже») лежит в «Проверке» и по правилу скилла prompt-test при базе «старый» это зачёт; зелёная клетка кандидата прошла новую проверку метки, отпечаток предмета держится, exec-*.md и work-plan после прогона не правились. Пометка исключения сужена до вложенного захода и теперь честная: требование метки у step и done меряет сценарий 30.

## Сценарий проверки

Шаги гоняются из корня чекаута, окружение это go на машине, сеть и дашборд не
нужны. Первый шаг воспроизводит поломку: на коде до правки тесты четырёх
кейсов краснеют, база берётся у родителя первого коммита задачи, тронувшего
plan.go. Дальше идут тесты и живая команда на временном доме. Каждый шаг это
одна строка.

```sh
regcheck -C tools/agentctl --base "$(git log --reverse --format=%H --grep='DK-900' -- tools/agentctl/plan.go | head -1)^" --tests tools/agentctl/plan_test.go -- env GOWORK=off go test -count=1 -run 'TestPlanSetRefusesReviewerOverExecutor|TestPlanSetRefusesUnlabelledOverHead|TestPlanDoneWithoutLabelHitsHeadPlan|TestPlanSetNestedProofreadTakesOwnLabel' .
```

Ожидание: строка «тест краснеет на ... и проходит на текущем коде, регрессия
закрыта».

```sh
GOWORK=off go -C tools/agentctl test -count=1 -run 'TestPlan' .
```

Ожидание: строка `ok`, вместе с четырьмя кейсами живы выход `--force` и
перекладка поверх доделанного плана.

```sh
env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan set --label DK-900 "разведка" >/dev/null && ! env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan set --label DK-900 "чтение диффа"
```

Ожидание: отказ с именем файла `dk900-sub-DK-900.json`, подсказкой метки
`DK-900-review` и ключом `--force`, а код выхода команды шага нулевой.

```sh
env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan set --label DK-900 "разведка" >/dev/null && env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan set --label DK-900-review "чтение диффа" >/dev/null && env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan show
```

Ожидание: два плана субагентов в выводе, `DK-900` с пунктом «разведка» и
`DK-900-review` с пунктом «чтение диффа».

```sh
env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan set "разведка" >/dev/null && env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan set --force "с нуля"
```

Ожидание: план сессии `dk900` из одного пункта «с нуля».

## Из черновика DK-1012 «Субагент вычитки с меткой задачи перезаписал план исполнителя» (записан 2026-09-15)

### Черновик

#### Ситуация

Исполнитель DK-974 вёл план с меткой DK-974. Субагент вычитки по скиллу proofread поднялся из его хода с той же меткой и положил свой план поверх. План исполнителя пропал, и он восстановил его повторным plan set после возврата вычитки. Метка одна на пачку субагентов, а вложенный субагент внутри исполнителя ей не отличим. Нужен свой хвост метки у вложенного захода либо отказ plan set, когда файл с меткой уже ведёт другой заход.

#### Осложнение

#### Вопрос

#### Гипотеза

## Выкат

- 2026-09-18 слито: 79c50cee, 75b3978a, 5b9807c9, 3073237e, 12bf2bc3, 913c56f4, bcbfade9, 0e231f84, ed7d911e, c67acb7d, 0e63a121, 2644cd83, 020ae185

## Проверка

- Стенд: 2026-09-17 23:59, дерево fd3fee1e, предмет 0ad9520a, база старый, ярус mini, k 3, сценарии 47-review-label-tail, зачтён.
- Исключение: стенд (kit/agents/exec-*.md, только фраза про вложенный заход с хвостом роли: подъём ревью или вычитки из хода исполнителя стенд субагентским концом не поднимает, и эта фраза стендом не проверена; требование метки у step и done у исполнителя меряет сценарий 30, след ниже)

```console
$ /private/tmp/claude-501/-Users-elenavnukova-projects-devkit/40a3cd86-5be9-4fdb-af69-18537ec607e1/scratchpad/obeycheck --task DK-900 --for kit/agents/review-medium.md --devkit /Users/elenavnukova/projects/devkit-dk-900 --end субагент --only 47-review-label-tail -k 3 --base старый --agent-def review-low /Users/elenavnukova/projects/rules-dk900-new /Users/elenavnukova/projects/rules-dk900-old
сценарий                                       rules-dk900-new  rules-dk900-old  вердикт
ревьювер ведёт план под меткой с хвостом роли              3/3              0/3  польза

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  47-review-label-tail / rules-dk900-old / повтор 1: проверка: exit status 1 (метка плана без хвоста роли: 34f48065-552e-4c7b-bf1f-da0170e9843c-sub-OB-047.json)
```

- Стенд: 2026-09-18 00:17, дерево 77570b67, предмет b05ae66c, база старый, ярус mini, k 3, сценарии 30-plan-close-on-return, зачтён.

```console
$ /private/tmp/claude-501/-Users-elenavnukova-projects-devkit/40a3cd86-5be9-4fdb-af69-18537ec607e1/scratchpad/obeycheck --task DK-900 --for kit/agents/exec-medium.md --devkit /Users/elenavnukova/projects/devkit-dk-900 --end субагент --only 30-plan-close-on-return -k 3 --base старый /Users/elenavnukova/projects/rules-dk900-new /Users/elenavnukova/projects/rules-dk900-old
сценарий                                       rules-dk900-new  rules-dk900-old  вердикт
закрыть план работ перед возвратом диспетчеру              1/3              0/3  не хуже

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  30-plan-close-on-return / rules-dk900-new / повтор 2: проверка: exit status 1 (плана работ субагент не положил)
  30-plan-close-on-return / rules-dk900-old / повтор 1: проверка: exit status 1 (в плане /private/var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/obeycheck-1422262616/30-plan-close-on-return-rules-dk900-old-1/home/.devkit/plans/84cbe65e-f220-4712-b32c-837f347fb8d5-sub-OB-030-review.json не закрыт ни один пункт: план положили и забыли)
```

- Обкатка: 2026-09-18 00:19, свежее дерево 1116db1bd237, сценарий 60220ddb, временный HOME, утилит дерева 11, шагов 5, все зелёные.

Шаг 1 (зелёный):

```console
$ regcheck -C tools/agentctl --base "$(git log --reverse --format=%H --grep='DK-900' -- tools/agentctl/plan.go | head -1)^" --tests tools/agentctl/plan_test.go -- env GOWORK=off go test -count=1 -run 'TestPlanSetRefusesReviewerOverExecutor|TestPlanSetRefusesUnlabelledOverHead|TestPlanDoneWithoutLabelHitsHeadPlan|TestPlanSetNestedProofreadTakesOwnLabel' .
тест краснеет на bd6242ba088a9d8631a3ba7c44c2ae03382f0908^ и проходит на текущем коде, регрессия закрыта (тесты: tools/agentctl/plan_test.go)
```

Шаг 2 (зелёный):

```console
$ GOWORK=off go -C tools/agentctl test -count=1 -run 'TestPlan' .
ok  	github.com/dronrider/devkit/tools/agentctl	0.556s
```

Шаг 3 (зелёный):

```console
$ env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan set --label DK-900 "разведка" >/dev/null && ! env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan set --label DK-900 "чтение диффа"
ошибка: в файле /var/folders/n1/kx99v5jn74l6hcg7n7x75mzc0000gn/T/taskctl-rehearse-3434778964/home/.devkit/plans/dk900-sub-DK-900.json уже лежит план с меткой DK-900 (пунктов 1, первый "разведка"), и новый набор не совпал с ним ни одним пунктом: это чужой заход. Свой план веди под своей меткой (--label DK-900-<роль>, скажем DK-900-review), а этот план перекладывай с --force
```

Шаг 4 (зелёный):

```console
$ env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan set --label DK-900 "разведка" >/dev/null && env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan set --label DK-900-review "чтение диффа" >/dev/null && env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan show
план сессии dk900: пунктов 0, закрыто 0
план субагента DK-900-review: пунктов 1, закрыто 0
  1 [ ] чтение диффа
план субагента DK-900: пунктов 1, закрыто 0
  1 [ ] разведка
плана нет, положи его командой agentctl plan set
```

Шаг 5 (зелёный):

```console
$ env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan set "разведка" >/dev/null && env CLAUDE_CODE_SESSION_ID=dk900 agentctl plan set --force "с нуля"
план сессии dk900: пунктов 1, закрыто 0
  1 [ ] с нуля
```
