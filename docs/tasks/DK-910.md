# DK-910: taskctl list и show печатают живой этап задачи и признак «брошена»

## Что происходит

Виток цели DK-909. Живой этап лежит в `~/.devkit/runs/<ID>` с 15 августа (DK-338), читает его `agentctl stage <ID>` и дашборд. `taskctl list` под строкой In progress печатает только «строка не двигалась N дней». 10 сентября по DK-860 в записи стояло «разработка с 09:54», сессия диспетчера молчала с 15:23, цикл слияния встал в 15:26 отказом ворот, и в списке это выглядело как идущая работа.

Ответ на вопрос, что делается по двум строкам, стоит трёх команд на каждый ID. Брошенная задача от идущей в списке не отличается, и очередь Check стоит до того, как кто-нибудь откроет ленту сессии. `taskctl elapsed` считает часы слияния разработкой и без повода выводит «сдавай хвост».

### Проверено грумингом 2026-09-13

Симптом на месте. Под строкой In progress `taskctl list` печатает только возраст правки строки. Пометки собирает `rowNotes` (`tools/taskctl/view.go:246`), подпись «строка не двигалась N дней» даёт `ageLabel` (`age.go:214`). Считается она по `git blame` доски, то есть по дате правки строки, а не по работе. `taskctl show` печатает те же пометки (`view.go:303`). Сегодня живой этап печатает только команда `agentctl stage <ID>` (`tools/agentctl/stage.go:78`).

Обход кода поправил гипотезу в трёх местах. Круга в записи нет. У структуры этапа четыре поля: вид, начало, заметка и сессия (`internal/stage/stage.go:200`). Номер захода считается по числу этапов одного вида в пакете. Признак живости сессии лежит не в `~/.devkit/agents`, как предполагала развилка цели, а в `~/.claude/sessions/<pid>.json`. Дашборд проверяет pid сигналом ноль (`tools/dashboard/peers.go:90`), свежесть записи рубежом двадцати минут (`board.go:281`) и занятость по транскрипту сессии (`sessions.go`, незакрытый вызов инструмента до тридцати минут). Эта логика лежит приватными методами сервера дашборда, в `internal/` она не вынесена. Общий пакет этапов `internal/stage` читают и taskctl, и agentctl, и дашборд. Гашение оборванного этапа на экране дашборда сделано (`tools/dashboard/stage.go:45`), в CLI его нет.

Ошибка `taskctl elapsed` не в том механизме, что назван в осложнении. Вид он как раз фильтрует и берёт начало последнего этапа «разработка» (`internal/stage/stage.go:230`), но не смотрит, жив ли этот этап. Открытые следом ревью и слияние отсчёт не останавливают, а в записи этапа нет конца вовсе. Порог захода 120 минут (`tools/taskctl/elapsed.go:14`).

Дублей на доске и в архиве нет. Родитель DK-338 закрыта 15 августа, признак оборванного этапа из её DoD сделан только в дашборде.

## Чего хотим

Под строкой задачи в `list` и `show` стоит строка «этап: ревью, круг 2, 12 минут, сессия жива» либо «этап: разработка, 5 часов, сессии нет, брошена». Возраст этапа считается от его начала. Смена статуса на него не влияет.

## Границы

- Кто пишет этапы и каким словарём это DK-911, здесь только чтение записи и печать. К ней нет ребра, решение человека 2026-09-13 развилкой «порядок».
- Номер работы субагента в записи и свод токенов это DK-912.
- Черновик DK-874 приписывается к этой строке, и починка `elapsed` входит в DoD.
- Условий, посильных только человеку, не нашёл. Доступов и секретов работа не требует, согласие на объём, охват секций и порог получено развилками «порядок», «секции» и «порог».

## Развилки

Как считать признак «брошена». DK-338 называла тот же случай, что `gone` у признака Run дашборда, живой сессии за записью нет. Брать логику дашборда общим модулем или дублировать в taskctl.

- «источник живости»: откуда taskctl берёт признак живой сессии?
  - решает: исполнитель
  - рекомендация: peer-реестр ~/.claude/sessions с проверкой pid сигналом ноль и рубежом свежести, вынесенный из дашборда в internal/; скан транскрипта в CLI не тащить, он читает четверть мегабайта на сессию и для частого обхода отвергнут решением 14 LLD DK-430
  - вариант: скан хвоста транскрипта, как у дашборда, ловит подвисшую сессию, но платит чтением на каждый вызов list
  - вариант: реестр ~/.devkit/agents, как предполагала развилка цели, дешёвый, но следит только за субагентами, и случай DK-860 без субагента им не ловится
  - источник: код, tools/dashboard/sessions.go:2457 скан хвоста в 256 КБ; дока, docs/lld/DK-430, решение 14; hooks/agent-watch.py:340 состояние lost считает сам хук изнутри живой сессии
  - решено исполнителем 2026-09-20: по рекомендации: реестр ~/.claude/sessions с проверкой pid сигналом ноль и рубежом свежести вынесен из дашборда в internal/peers. Скан транскрипта отвергнут, он читает четверть мегабайта на сессию на каждый list (решение 14 LLD DK-430). Реестр ~/.devkit/agents отвергнут, он видит только субагентов, а случай DK-860 шёл без субагента
- «две копии признака»: как не развести показание списка с бейджем дашборда, который сам зовёт taskctl list --json каждые пять секунд?
  - решает: исполнитель
  - рекомендация: дашборд перестаёт считать свой gone для строки и берёт готовое поле из вывода taskctl list --json, считает признак одна сторона; расчёт идёт только по строкам In progress, чтобы цена вызова не выросла на всей доске
  - вариант: оба считают своё. Дашборд остаётся на своём расчёте, а taskctl считает рядом, и показания расходятся
  - вариант: вынести расчёт в internal/ и звать его из обоих, дашборд платит им дважды за тик
  - источник: код, tools/dashboard/board.go:18 доска дашборда это подпроцесс taskctl list --json; bench_test.go:24 замер 50-270 мс; static/app.js:5766 опрос каждые пять секунд
  - решено исполнителем 2026-09-20: по рекомендации: дашборд перестал считать свой rowStage и берёт поля stage, stage_since, stage_round, stage_age и stage_session из taskctl list --json как есть. Считать в обоих отвергнуто, дашборд платил бы за расчёт дважды за тик. Расчёт идёт по всем секциям, а не только по In progress: печатать всюду велел человек развилкой «секции», а цена одна на вызов, реестры читаются раз и только когда у доски есть хоть один открытый этап
- «сессии строки»: живость считается по сессии, записанной в этапе, или по всем сессиям строки?
  - решает: исполнитель
  - рекомендация: по всем сессиям, ведущим строку, как решение 8 LLD DK-430. У строки бывает и чат поверх работы конвейера, а запись этапа хранит только ту сессию, что его открыла
  - вариант: по сессии из записи этапа, проще, но чат поверх работы читается как брошенная задача
  - источник: дока, docs/lld/DK-430, решение 8: состояние строки это свёртка по множеству сессий; код, internal/stage/stage.go:200 поле сессии одно
  - решено исполнителем 2026-09-20: по рекомендации: по всем рабочим сессиям строки из реестра чатов (sessions.WorksOn, решение 8 LLD DK-430) плюс сессия, открывшая живой этап. Одна сессия из записи этапа отвергнута, чат поверх работы конвейера читался бы брошенной задачей
- «круг»: откуда берётся номер круга, если поля круга в записи нет?
  - решает: исполнитель
  - рекомендация: круг считается по числу этапов того же вида в пакете записи. Отдельного поля у этапа нет, у этапа четыре поля: вид, начало, заметка и сессия
  - вариант: завести поле круга в записи этапа, но тогда его пишут все писатели этапов, а это предмет DK-911
  - источник: код, internal/stage/stage.go:200 структура этапа из четырёх полей
  - решено исполнителем 2026-09-20: по рекомендации: круг считается по числу этапов того же вида в пакете записи, первый круг не печатается. Поле круга в записи отвергнуто, его писали бы все писатели этапов, а это предмет DK-911
- «кейсы»: верен ли список кейсов записи: поправьте, снимите лишний, добавьте пропущенный?
  - решает: человек
  - рекомендация: 1 человек зовёт taskctl list и по строке In progress видит этап, его возраст и круг, не спрашивая отдельной командой по каждому ID; 2 задача, за записью которой нет живой сессии, помечена в списке «сессии нет, брошена», и это видно раньше, чем кто-нибудь откроет ленту сессии; 3 taskctl show по одной задаче печатает то же самое; 4 возраст считается от начала этапа, смена статуса строки его не обнуляет; 5 taskctl elapsed перестаёт считать часы ревью и слияния разработкой и без повода требовать сдать хвост, это приписанный черновик DK-874; 6 бейдж дашборда и текст списка говорят про строку одно и то же
  - решено человеком 2026-09-13: 1 человек зовёт taskctl list и по строке In progress видит этап, его возраст и круг, не спрашивая отдельной командой по каждому ID; 2 задача, за записью которой нет живой сессии, помечена в списке «сессии нет, брошена», и это видно раньше, чем кто-нибудь откроет ленту сессии; 3 taskctl show по одной задаче печатает то же самое; 4 возраст считается от начала этапа, смена статуса строки его не обнуляет; 5 taskctl elapsed перестаёт считать часы ревью и слияния разработкой и зря звать сдавать хвост, это приписанный черновик DK-874; 6 бейдж дашборда и текст списка говорят про строку одно и то же
- «порядок»: ставить ли ребро «после DK-911», раз этап «ревью» сегодня ложится только по флагу --record, который диспетчеры забывают?
  - решает: человек
  - рекомендация: ребра не ставить и делать сразу. Признак «брошена» и возраст этапа работают на сегодняшней записи, а пропуски этапа «ревью» закрывает сама DK-911, и переделки в списке это не потребует
  - вариант: поставить ребро «после DK-911», подождать, пока этапы начнёт писать инструмент, и печатать сразу достоверную картину, но пользы от списка до тех пор нет
  - вариант: без ребра, но круг до DK-911 не печатать. Вид, возраст и признак печатаются сейчас, круг добавляется после
  - решено человеком 2026-09-13: ребра не ставить и делать сразу: признак «брошена» и возраст этапа работают на сегодняшней записи, а пропуски этапа «ревью» лечит сама DK-911, и переделки в списке это не потребует
- «секции»: в каких секциях доски печатать строку этапа?
  - решает: человек
  - рекомендация: печатать всюду, где в записи есть открытый этап, а не только под In progress. На Check и Blocked строки тоже стоят на этапах, и их простой как раз надо видеть. Признак «брошена» ставить там, где этап требует живой сессии
  - вариант: только под строками In progress, как сказано в записи. Короче вывод, но простой Check и Blocked остаётся невидимым
  - решено человеком 2026-09-13: печатать всюду, где в записи есть открытый этап, а не только под In progress: на Check и Blocked строки тоже стоят на этапах, и их простой как раз надо видеть; признак «брошена» ставить там, где этап требует живой сессии
- «порог»: что печатать, когда сессия жива, но молчит дольше рубежа?
  - решает: человек
  - рекомендация: брошена это когда сессии за записью нет вовсе, процесс мёртв или записи о нём не осталось; живая, но молчащая дольше рубежа печатается своими словами «сессия молчит N минут», это два разных случая
  - вариант: одно слово на оба случая, молчание дольше рубежа тоже «брошена». Строка короче, но упавший процесс не отличить от думающего
  - вариант: молчание не считать вовсе, печатать только «сессия жива» и «сессии нет»: дешевле всего, подвисшая сессия читается как работающая
  - решено человеком 2026-09-13: брошена это когда сессии за записью нет вовсе, процесс мёртв или записи о нём не осталось; живая, но молчащая дольше рубежа печатается своими словами «сессия молчит N минут», это два разных случая
- «рубеж молчания»: какой рубеж молчания считать, раз живая молчащая сессия печатается отдельными словами?
  - решает: исполнитель
  - рекомендация: тот же рубеж, что у дашборда, двадцать минут свежести записи сессии (tools/dashboard/board.go:281), и вынести его в общий пакет одной константой, чтобы список и экран говорили об одном и том же
  - вариант: свой рубеж у CLI, короче или длиннее. Показания списка и экрана разъедутся на границе
  - решено исполнителем 2026-09-20: по рекомендации: тот же рубеж, что у дашборда, двадцать минут, одной константой peers.IdleAfter в internal/peers, дашборд читает её же через workIdleAfter. Свой рубеж у CLI отвергнут, список и экран разошлись бы на границе

## DoD

- `taskctl list` под строкой с открытым этапом печатает вид этапа, возраст от его начала и круг, а `taskctl show <ID>` печатает то же самое по одной задаче.
- У задачи, за записью которой не стоит живая сессия, в списке стоит «сессии нет, брошена». Живая, но молчащая дольше рубежа печатается словами «сессия молчит N минут». У этапа, которому живая сессия не нужна, признака нет.
- Строка этапа стоит во всех секциях, где в записи есть открытый этап, а не только под In progress.
- Признак считает одна сторона. Дашборд берёт готовое поле из `taskctl list --json` и своего расчёта по строке не ведёт, бейдж экрана и текст списка сходятся.
- `taskctl elapsed` отсчитывает от начала живого этапа, часы ревью и слияния в разработку не идут. Это предмет приписанного черновика DK-874.
- Прогон идёт на временном репозитории с синтетической доской. Запись этапа с мёртвым процессом даёт «брошена», запись живой сессии даёт вид, возраст и круг.

## Ранг

`50+8+3+0+4 = 65`, P1.

- Серьёзность 50: конвейер ведёт не туда. 10 сентября DK-860 пять часов стояла под словом «разработка» с молчащей сессией, по списку это читалось идущей работой, и очередь Check ждала, пока человек откроет ленту.
- Ценность 8: ответ на вопрос, что делается по строке, перестаёт стоить трёх команд на каждый ID, и в каждом витке цели работа видна со строки.
- Неопределённость 3: подход выбран, а в деталях шесть развилок, и главная из них про то, что живость сессии в общий пакет не вынесена и в списке считается заново.
- Поправка на баг 0: печать этапа это новая механика поверх записи DK-338.
- Рычаг 4: на признаке живости и возрасте этапа стоят `elapsed`, дашборд и выбор работы, и каждая следующая задача конвейера дешевле.

Цена L: живость сессии выносится из дашборда в общий пакет, читателей у неё двое, и рядом правка `elapsed`. В один контекст это не укладывается.

Вид приёмки agent. Вывод команды сверяется дословно на временном репозитории с синтетической доской, мёртвая сессия подделывается записью этапа с несуществующим процессом, и ни один из шести барьеров не назван.

## Ход работы

- Из черновика DK-910 «taskctl list и show печатают живой этап задачи и признак «брошена»»: записан 2026-09-10, оформлен 2026-09-13.
- Вычитка: 1 файл, 20 правок, 1 пометка, 2026-09-13.
- Разработка: субагент fable/high по вердикту pick (маппинг opus, корректор: профицит week_all, week_max; квота: week_all 55% профицит, week_max 40% профицит (панель разбивку не дала, цифра из кеша клиента возрастом 1ч 32м назад), снимок 43м назад), 2026-09-20 11:45-11:46.
- Вычитка: 4 файла, 12 правок, 0 пометок, 2026-09-20.
- Сторож прозы: docs/tasks/DK-910.md, двоеточие в середине фразы 17,5 при пороге 11, оставлено: двоеточия стоят в строках «Ожидание: ...» сценария проверки и в записях решений развилок, форму которых пишет taskctl decide.

## Сценарий проверки

Агентская часть, из основного чекаута после слияния и обновления бинарей
(`devkitctl update`). Стенд живёт во временном каталоге под `$TMPDIR`, свой
дом и своя доска, боевые записи `~/.devkit/runs` и реестр сессий не трогаются.
Шаги идут по одному в строке, каждая строка заново называет каталог стенда.

Шаг 1, доска. Четыре строки в трёх секциях, у них нет файлов задач.

```sh
S=${TMPDIR:-/tmp}/dk910 && rm -rf "$S" && mkdir -p "$S/home" "$S/proj/docs/tasks" && printf '%s\n' '# Стенд DK-910 (префикс XR)' '' '## In progress' '' '| ID | Задача | Тип | P | R | Цена | Ссылка |' '|---|---|---|---|---|---|---|' '| XR-001 | Брошенная | task | P2 | 30 (25+2+1+0+2) | S | - |' '| XR-002 | Живая | task | P2 | 30 (25+2+1+0+2) | S | - |' '' '## Check (готово, ждёт проверки пользователем)' '' '| ID | Задача | Тип | P | R | Цена | Ссылка |' '|---|---|---|---|---|---|---|' '| XR-003 | Ждёт снаружи | task | P2 | 30 (25+2+1+0+2) | S | - |' '' '## Backlog' '' '| ID | Задача | Тип | P | R | Цена | Ссылка |' '|---|---|---|---|---|---|---|' '' '## Blocked' '' '| ID | Задача | Тип | P | R | Цена | Ссылка |' '|---|---|---|---|---|---|---|' '| XR-004 | Молчащая | task | P2 | 30 (25+2+1+0+2) | S | - |' > "$S/proj/docs/TASKS.md" && printf '%s\n' '# Стенд DK-910: архив' '' '| ID | Задача | Тип | P | Закрыто | Ссылка |' '|---|---|---|---|---|---|' > "$S/proj/docs/TASKS-archive.md" && echo "стенд готов"
```

Ожидание: строка «стенд готов».

Шаг 2, реестр сессий клиента. Живая сессия это фоновый `sleep`, мёртвая это
pid только что кончившегося процесса, молчащая это тот же живой pid с
касанием реестра 25 минут назад.

```sh
S=${TMPDIR:-/tmp}/dk910 && mkdir -p "$S/home/.claude/sessions" && (sleep 600 & echo $! > "$S/live.pid") && DEAD=$(sh -c 'echo $$') && NOW=$(date +%s) && printf '{"pid":%s,"sessionId":"s-live","status":"busy","updatedAt":%s000}\n' "$(cat "$S/live.pid")" "$NOW" > "$S/home/.claude/sessions/live.json" && printf '{"pid":%s,"sessionId":"s-dead","status":"busy","updatedAt":%s000}\n' "$DEAD" "$NOW" > "$S/home/.claude/sessions/dead.json" && printf '{"pid":%s,"sessionId":"s-quiet","status":"busy","updatedAt":%s000}\n' "$(cat "$S/live.pid")" "$((NOW-1500))" > "$S/home/.claude/sessions/quiet.json" && echo "реестр сессий: живая, мёртвая, молчащая"
```

Ожидание: строка «реестр сессий: живая, мёртвая, молчащая».

Шаг 3, записи этапов. XR-001 открыла мёртвая сессия, XR-002 открыла живая, и
у неё второй круг ревью, XR-003 стоит на ожидании снаружи, XR-004 открыла
молчащая.

```sh
S=${TMPDIR:-/tmp}/dk910 && export HOME="$S/home" && CLAUDE_CODE_SESSION_ID=s-dead agentctl stage XR-001 разработка -C "$S/proj" >/dev/null && CLAUDE_CODE_SESSION_ID=s-live agentctl stage XR-002 разработка -C "$S/proj" >/dev/null && CLAUDE_CODE_SESSION_ID=s-live agentctl stage XR-002 ревью -C "$S/proj" >/dev/null && CLAUDE_CODE_SESSION_ID=s-live agentctl stage XR-002 ревью -C "$S/proj" >/dev/null && CLAUDE_CODE_SESSION_ID=s-dead agentctl stage XR-003 снаружи -C "$S/proj" >/dev/null && CLAUDE_CODE_SESSION_ID=s-quiet agentctl stage XR-004 разработка -C "$S/proj" >/dev/null && ls "$S/home/.devkit/runs" | wc -l | tr -d ' '
```

Ожидание: число `4`, по записи на строку.

Шаг 4, список.

```sh
S=${TMPDIR:-/tmp}/dk910 && HOME="$S/home" taskctl list -C "$S/proj"
```

Ожидание дословно:

```
In progress (2)
| XR-001 | Брошенная | task | P2 | 30 (25+2+1+0+2) | S | - |
  этап: разработка, меньше минуты, сессии нет, брошена
| XR-002 | Живая | task | P2 | 30 (25+2+1+0+2) | S | - |
  этап: ревью, круг 2, меньше минуты, сессия жива
Check (1)
| XR-003 | Ждёт снаружи | task | P2 | 30 (25+2+1+0+2) | S | - |
  вид agent
  этап: снаружи, меньше минуты
Backlog (0)
Нет.
Blocked (1)
| XR-004 | Молчащая | task | P2 | 30 (25+2+1+0+2) | S | - |
  этап: разработка, меньше минуты, сессия молчит 25 минут
```

Возраст стоит «меньше минуты» у всех четырёх, записи открыты шагом 3 секунды
назад. Строка этапа стоит и под Check, и под Blocked. У ожидания снаружи нет
хвоста о сессии. Стенд вне git, поэтому нет строки «строка не двигалась N дней».

Шаг 5, одна задача.

```sh
S=${TMPDIR:-/tmp}/dk910 && HOME="$S/home" taskctl show XR-001 -C "$S/proj"
```

Ожидание дословно:

```
XR-001 в in-progress
| XR-001 | Брошенная | task | P2 | 30 (25+2+1+0+2) | S | - |
  этап: разработка, меньше минуты, сессии нет, брошена
после: -
держит: -
файла задачи нет (создаст taskctl file XR-001)
```

Шаг 6, машинный вывод. Те же куски едут полями, дашборд берёт их отсюда.

```sh
S=${TMPDIR:-/tmp}/dk910 && HOME="$S/home" taskctl list --json -C "$S/proj" | python3 -c 'import json,sys; d=json.load(sys.stdin); print("\n".join(r["id"]+" "+str(r.get("stage"))+" "+str(r.get("stage_round"))+" "+r.get("stage_session","-") for s in d["sections"] for r in s["rows"]))'
```

Ожидание дословно:

```
XR-001 разработка 1 сессии нет, брошена
XR-002 ревью 2 сессия жива
XR-003 снаружи 1 -
XR-004 разработка 1 сессия молчит 25 минут
```

Шаг 7, `elapsed` от живого этапа. У XR-002 разработка открыта раньше ревью, и
счёт идёт от ревью. Число минут и отметка времени заменены подстановкой.

```sh
S=${TMPDIR:-/tmp}/dk910 && HOME="$S/home" taskctl elapsed XR-002 -C "$S/proj" | sed -E 's/[0-9]+ минут[а-я]* назад \(с [^)]*\)/N минут назад (с ...)/'
```

Ожидание: строка «этап ревью открыт N минут назад (с ...), лимит 120 минут: в
пределах».

Шаг 8, уборка. Фоновый `sleep` снимается, каталог стенда удаляется.

```sh
S=${TMPDIR:-/tmp}/dk910 && kill "$(cat "$S/live.pid")" && rm -rf "$S" && echo "стенд убран"
```

Ожидание: строка «стенд убран».

Дашборд проверяется своим сквозным прогоном (`dashboard smoke`, шаг «вид
деятельности в строке доски»). Поля этапа приходят из фикстуры taskctl и
доезжают до ручки доски нетронутыми, а у брошенной строки стоит «сессии нет,
брошена». Прогон гоняет `go test ./...` в `tools/dashboard`, здесь нет
отдельного шага.

## Из черновика DK-874 «taskctl elapsed молчит про этап, пока задача идёт в работе» (записан 2026-09-08)

### Черновик

#### Ситуация

Замечено 2026-09-08 на прогоне DK-571. Диспетчер отметил этап
`agentctl pick DK-571 --record`, перевёл строку в in-progress и отдал работу
исполнителю. На каждом переходе плана `taskctl elapsed DK-571` отвечает «этап
не открыт, лимит не проверить», и так же он отвечает у диспетчера через два
часа работы. Похоже, `taskctl move` уносит накопленный пакет этапов в «Ход
работы» файла задачи, и открытого этапа после этого не остаётся.

#### Осложнение

Скилл `board-task` держит на этой мерке жизнь одиночного захода, и мерить
оказалось нечем. Исполнитель идёт без предела и сдаёт хвост по своему
ощущению. На DK-571 заход шёл 52 минуты при плановом лимите 120 и порога не
увидел ни разу.

#### Вопрос

Открывать ли этап заново переводом в in-progress или считать время от метки
записи, пережившей перенос в файл задачи.

#### Гипотеза

`taskctl elapsed` у идущей задачи называет минуты и остаток лимита, каким бы
порядком ни шли pick и move.
