# DK-912: taskctl spend сводит токены задачи по этапам из транскриптов

## Что происходит

Виток цели DK-909. Запись этапа хранит снимок квоты в процентах недели («week_all 25%, снимок 47м назад, протух»). Транскрипты в `~/.claude/projects/<проект>/<сессия>/subagents/agent-<работа>.jsonl` несут поле usage на каждом ходу, вывод, свежий вход и чтение кэша. Номер работы тот же, что записывается в `~/.devkit/agents.log` на старте и конце субагента. Замер 10 сентября по DK-899: исполнитель 235 ходов и 59k вывода, ревью 74 хода и 14k, вычитка 9 ходов и 5k, вложенная вычитка от исполнителя 69 ходов и 26k. Вложенных субагентов исполнителя журнал агентов не видит.

Оптимизировать расход нечем. Процент недели не различает заход на четыре минуты, а без номера работы в записи этапа токены субагента к этапу не привязать. Вложенный субагент исполнителя весит как половина исполнителя и в учёте отсутствует.

### Проверено грумингом 2026-09-13

Актуальность на месте. Команды `spend` у `taskctl` нет. Запись `~/.devkit/runs/<ID>-<корень>.run` несёт id, корень и строки этапа с видом, временем, заметкой и UUID сессии, номера работы субагента в ней нет.

Связка этапа с транскриптом проверена на живом заходе. Строка `agents.log` «работа a78e0249f3bdbd68d» отвечает файлу `agent-a78e0249f3bdbd68d.jsonl` в каталоге своей сессии. `message.usage` лежит на каждом ходу вывода, свежего входа и чтения кэша. Номер работы в заметке этапа это третье применение готового приёма. `internal/stage/stage.go:66,113` так же вытаскивает исполнителя вердикта и прогонявшего сценарий, а заметка переживает архивацию дословно (`docs/tasks/archive/2026/DK-899.md:141`).

Вложенность собирается без хука. В транскриптах субагентов проекта 325 вызовов инструмента Agent, ответ несёт строку «agentId: <id>», и файл вложенной работы лежит в том же каталоге `subagents/`. Дыра, названная записью, закрывается чтением транскриптов, а кейс 11 задачи DK-911 расширять не придётся.

Готовый разбор usage лежит в python (`tools/devkitctl/context.py:58-99` и `sessions.py:18-45`, поля и дедуп по requestId). В go такого нет, а `taskctl` по правилу раскладки обязан быть go-инструментом. Пересчитать токены в проценты недельных бакетов не из чего. Расчёт лимитов серверный, снимок приходит готовым процентом (`tools/agentctl/usagecache.go:13-19`, `budget.go:36-116`).

Дубля нет. DK-209 (стоимость закрытых задач в двух валютах) назван в файле цели надстройкой над этим сводом, DK-913 берёт сквозные статьи, DK-646 меряет расход fable дельтой снимков квоты. У метода есть прецедент. Закрытая DK-615 мерила субагента токенами, но брала их из уведомлений харнеса, а не из транскриптов. Чувствительность транскриптов уже всплывала задачей DK-231, где из них вычищали утёкшие тела токенов.

## Чего хотим

Запись этапа несёт номер работы субагента. Команда `taskctl spend <ID>` печатает токены по этапам задачи, свод за период печатает те же числа по всем задачам с медианой и хвостом. Вложенных субагентов исполнителя ловит тот же хук спавна, что ставит этапы в DK-911.

## Границы

- Сквозные статьи «оркестрация», «постановка» и «фон» это DK-913. Здесь только этапы самой задачи.
- Номер работы в запись этапа кладётся по DK-911, одиннадцатым кейсом. Здесь только чтение записи, и ребро к ней ставится зависимостью.
- Токены в проценты недельных бакетов не пересчитываются, решено развилкой «валюта». Проценты из заметок этапов идут рядом справочной колонкой.
- Стоимость закрытых задач в двух валютах и курс размеров это DK-209, надстройка над сводом.
- Условий, посильных только человеку, не нашёл. Транскрипты лежат на машине, доступов и секретов работа не просит, наружу свод ничего не отправляет.

## Развилки

Оба вопроса записи сняты на уровне цели. Развилка «второй харнес» оставила источником транскрипты харнеса claude. Развилка «хранение свода» велела считать на лету, а в файл задачи класть одну строку итога при закрытии. Открытым остаётся, чем меряется этап, открытый головной сессией без номера работы, и как свод за период обходится без чтения всех транскриптов машины.

- «кейсы»: верен ли список кейсов свода: поправьте, снимите лишний, добавьте пропущенный?
  - решает: человек
  - рекомендация: 1 taskctl spend DK-899 печатает строку на каждый этап задачи: вид, часы, номер работы субагента, вывод, свежий вход, чтение кэша; 2 этап без номера работы (запись до DK-911 либо этап головной сессии) печатается со строкой «нет данных» и причиной, а не пропускается молча; 3 вычитка, поднятая исполнителем вложенным субагентом, ложится в свод того этапа, который её поднял; 4 расход диспетчера, груминга и фона в свод задачи не входит, под них статьи DK-913; 5 taskctl spend --from --to печатает те же числа по всем задачам периода с медианой и хвостом; 6 у закрытой и заархивированной задачи этапы берутся из строк «Хода работы», а при закрытии в файл задачи ложится одна строка итога; 7 заход второго харнеса печатается строкой «нет данных», а не молчит; 8 наружу идут только суммы, тела реплик транскриптов свод не печатает
  - вариант: без кейса 3: вложенные субагенты вне свода, отдельный след
  - вариант: без кейса 6: свод считает только живые задачи, закрытые оставляет DK-209
  - решено человеком 2026-09-13: 1 taskctl spend DK-899 печатает строку на каждый этап задачи: вид, часы, номер работы субагента, вывод, свежий вход, чтение кэша; 2 этап без номера работы (запись до DK-911 либо этап головной сессии) печатается со строкой «нет данных» и причиной, а не пропускается молча; 3 вычитка, поднятая исполнителем вложенным субагентом, ложится в свод того этапа, который её поднял; 4 расход диспетчера, груминга и фона в свод задачи не входит, под них статьи DK-913; 5 taskctl spend --from --to печатает те же числа по всем задачам периода с медианой и хвостом; 6 у закрытой и заархивированной задачи этапы берутся из строк «Хода работы», а при закрытии в файл задачи ложится одна строка итога; 7 заход второго харнеса печатается строкой «нет данных», а не молчит; 8 наружу идут только суммы, тела реплик транскриптов свод не печатает
- «язык»: чтение транскриптов для taskctl spend пишется на go заново или обёрткой над готовым разбором devkitctl?
  - решает: исполнитель
  - рекомендация: новый пакет в internal/, читающий usage по образцу tools/devkitctl/context.py:68-99 теми же полями и тем же дедупом по requestId: taskctl по правилу раскладки README.md обязан быть go-инструментом, а вызов python из команды в PATH ломает раскладку сильнее, чем повторение разбора
  - источник: код, README.md раздел «Раскладка» держит taskctl в go; tools/devkitctl/context.py:68-99 и sessions.py:18-45 уже разбирают usage с дедупом по requestId
- «безголовый»: как считаются токены этапа, открытого головной сессией без номера работы?
  - решает: исполнитель
  - рекомендация: резервная привязка по времени этапа к транскрипту самой сессии (projects/<слепок>/<sid>.jsonl): номера работы у головной сессии не будет по устройству хука, он пишет запись только на вызов Task; запись этапа UUID сессии уже несёт
  - источник: код, hooks/agent-watch.py:170-178 пишет строку только на вызов Task; internal/stage/stage.go:230-238 считает длительность этапа
- «объём»: как свод за период не разбирает все транскрипты машины на каждый запрос?
  - решает: исполнитель
  - рекомендация: свод за период читает файлы по номерам работ из записей и «Хода работы», а не обходит каталог целиком; по каталогу проекта сегодня 1715 файлов и 1.6G на машине, и чистки транскриптов в devkit нет
  - источник: код, tools/devkitctl/context.py:58-65 обходит subagents/*.jsonl рекурсивно по всему каталогу сессии
- «валюта»: чем меряется этап в своде, раз бюджеты целей стоят в процентах недельных бакетов?
  - решает: человек
  - рекомендация: печатать сырые токены и не мешать их с процентами бакета: пересчёта токенов в пункты недели у devkit нет и взяться ему неоткуда, лимиты считает сервер; проценты из заметок этапов печатаются рядом справочной колонкой
  - вариант: только токены, проценты бакета не печатать вовсе
  - вариант: рядом с токенами дельта снимков квоты по этапу, как мерит DK-646
  - источник: возражение, tools/agentctl/usagecache.go:13-19 и budget.go:36-116 держат проценты бакета серверного расчёта, пересчёта токенов в пункты недели нет
  - решено человеком 2026-09-13: печатать сырые токены и не мешать их с процентами бакета: пересчёта токенов в пункты недели у devkit нет и взяться ему неоткуда, лимиты считает сервер; проценты из заметок этапов печатаются рядом справочной колонкой
- «вложенность»: откуда свод берёт цепочку вложенных субагентов, раз журнал агентов её не видит?
  - решает: исполнитель
  - рекомендация: дерево вложенности свод собирает из самих транскриптов: вложенный субагент лежит в том же каталоге subagents/ сессии, а в транскрипте родителя его номер работы стоит строкой «agentId: <id>» в ответе на вызов инструмента Agent; хук и реестр ~/.devkit/agents для этого не нужны, и кейс 11 DK-911 расширять не придётся
  - источник: код, в транскриптах субагентов проекта 325 вызовов инструмента Agent, ответ несёт «agentId: <id>», и файл agent-<id>.jsonl лежит в том же каталоге subagents/

## DoD

- `taskctl spend DK-899` печатает строку на каждый этап задачи: вид, часы, номер работы субагента, вывод, свежий вход и чтение кэша. Вложенный субагент ложится в этап того, кто его поднял.
- Этап без номера работы и заход второго харнеса печатаются строкой «нет данных» с причиной, а не пропускаются молча.
- У закрытой и заархивированной задачи этапы берутся из строк «Хода работы», а при закрытии в файл задачи ложится одна строка итога.
- `taskctl spend --from --to` печатает те же числа по всем задачам периода с медианой и хвостом. Наружу идут суммы, свод не печатает тела реплик транскриптов.
- Команда описана в `tools/taskctl/README.md` разделом с первым прогоном и примером вывода.

## Ранг

`25+8+2+0+4 = 39`, P2.

`25+8+2+0+4 = 39`, а до ранга цели строку подтягивает состав DK-909.

- Серьёзность 25: заметное трение с дешёвым обходом. Расход виден процентом недели и разбирается руками по транскриптам, как 10 сентября. На каждый вопрос «куда ушла квота» это заход на полчаса.
- Ценность 8: свод по этапам это начало оптимизаций расхода, довод взят у цели. Диспетчер DK-860 выдал 109k вывода против 30k у исполнителя, и без свода такое не находится.
- Неопределённость 2: подход выбран и проверен на живых транскриптах, в деталях открыты четыре развилки исполнителя.
- Поправка на баг 0: новая механика, а не регресс.
- Рычаг 4: на своде стоят статьи DK-913 и стоимость задач DK-209, и с него начинается выбор, что делать дешевле.

## Ход работы

- Из черновика DK-912 «Токены сводятся по этапам задачи из транскриптов субагентов»: записан 2026-09-10, оформлен 2026-09-13.
- Вычитка: 1 файл, 12 правок, 3 пометки, 2026-09-13.
