# DK-914: Сессия без задачи получает правила плана и отзывчивости, указатель зовёт скилл

## Что происходит

Разговор 2026-09-10 в общем чате доски devkit, сессия без привязки к задаче. Хук старта `hooks/session-task.py` положил в контекст одну строку. «Порядок разговора с человеком по задаче лежит в скилле board-chat». Агент скилл не позвал и весь заход вёл по общим правилам. Поиск по доске шёл десятком грепов в одном ходе, харнес дважды напоминал, что человек давно ничего не слышал. План через `agentctl plan` не заводился, кольцо в панели стояло пустым. DK-880 закрыта в тот же день, и строка до чата дошла, потерялось поведение.

В коде ветка «сессия без задачи» отличается от двух соседних. Ветка с заказом задачи кладёт три строки, план, отзывчивость и указатель. Ветка после сжатия контекста кладёт те же три и просит «перечитай скилл board-chat». Ветка без задачи отдаёт один указатель (`run_hook`, строка около 395), правил плана и отзывчивости в ней нет. Сам указатель это справка. Слово «по задаче» в нём и в описании скилла не подходит к чату с пустой привязкой и к консольной сессии, а обе идут этой веткой.

Разбор 2026-09-10 добавил два факта. На поводе сжатия контекста ветка без
задачи отдаёт ту же одну строку, что на старте. Просьба «перечитай скилл»
есть только у ветки с задачей. DK-880 решила развилкой «состав», что план и
отзывчивость остаются у всех, кто ведёт работу, а ревью приняло ветку без
задачи как «CHAT_RULE без плана». Стенд DK-880 на ярусе base дал с одним
указателем четыре зачёта из десяти против нуля без него.

Человек в общем чате доски и в консоли смотрит в молчащее окно на каждом долгом деле и не видит плана. Стенд DK-614 мерил цену отсутствия скилла на ярусе base. Без него ответ уходил каналом SendMessage восемь раз из тринадцати. Правило «долгие дела субагенту» в такой чат не приезжает вовсе, оно лежит только в теле незваного скилла.

## Чего хотим

Сессия без задачи и без признака DEVKIT_HIDDEN получает на старте те же строки плана и отзывчивости, что и сессия с задачей. Указатель велит позвать скилл и обходится без слова «по задаче». Консольная сессия, поднятая руками, ведёт себя так же, как чат панели. Хук тот же, канал additionalContext тот же.
После сжатия контекста та же ветка просит перечитать скилл и повторяет
строки плана и отзывчивости, как ветка с задачей. Скрытая сессия по-прежнему
молчит.

## Границы

- Текст правил плана и отзывчивости не меняется, порог повода у плана это
  DK-881. Правится отбор строк по веткам хука и слово «по задаче» в указателе
  и в описании скилла.
- Тело скилла `board-chat` в хук не едет, размер резидента правил не растёт.
- Признак `DEVKIT_HIDDEN`, точки подъёма и реестр чатов не трогаются.
- Условий, посильных только человеку, не нашёл. Стенд гоняет исполнитель,
  доступов и шагов наружу нет.

## Развилки

Указатель или тело. DK-614 решила развилку в пользу фразы с оговоркой. Тело скилла идёт в ход, когда живой чат покажет потерю правил. Этот чат её показал. Хватит ли команды «позови скилл board-chat» вместе со строками плана и отзывчивости, или класть тело целиком, порядка сорока строк на каждый старт и каждое сжатие. Решает стенд obeycheck сценарием «чат без задачи, долгое дело, ответ человеку».

Решено 2026-09-10 (развилка «тело»): указатель-команда и строки плана и
отзывчивости, тело скилла только по проигрышу указателя на стенде и
отдельной развилкой с бюджетом контекста.

- «тело»: Хватит ли указателя-команды со строками плана и отзывчивости, или класть в контекст тело скилла на каждом старте и сжатии?
  - решает: человек
  - рекомендация: указатель-команда «позови скилл board-chat» плюс строки плана и отзывчивости, стенд сценарием «чат без задачи, долгое дело» сверяет против ветки без правил; тело скилла целиком идёт только по проигрышу указателя на стенде, и тогда отдельной развилкой с бюджетом контекста (DK-844: резидент правил перерос потолок)
  - вариант: тело скилла целиком, порядка сорока строк на каждый старт и каждое сжатие
  - вариант: исполнитель гоняет обе раскладки стендом и берёт ту, что выиграла, без отдельного вопроса про бюджет
  - решено человеком 2026-09-10: указатель-команда «позови скилл board-chat» плюс строки плана и отзывчивости, стенд сценарием «чат без задачи, долгое дело» сверяет против ветки без правил; тело скилла целиком идёт только по проигрышу указателя на стенде, и тогда отдельной развилкой с бюджетом контекста (DK-844: резидент правил перерос потолок)
- «консоль»: Консольная сессия, поднятая руками в любом проекте, получает те же строки плана и отзывчивости, что чат панели?
  - решает: человек
  - рекомендация: да, той же веткой хука: разделить консоль и чат панели нечем (DK-880 «полярность» метит отсутствие человека), а шум плана на разговор в одну реплику чинит DK-881 порогом повода
  - вариант: нет, план и отзывчивость получает только чат панели, консоль по-прежнему один указатель
  - решено человеком 2026-09-10: да, той же веткой хука: разделить консоль и чат панели нечем (DK-880 «полярность» метит отсутствие человека), а шум плана на разговор в одну реплику чинит DK-881 порогом повода
- «сжатие»: Что кладёт хук сессии без задачи после сжатия контекста?
  - решает: исполнитель
  - рекомендация: та же просьба «перечитай скилл board-chat» и те же строки плана и отзывчивости, что у ветки с задачей после сжатия: сейчас ветка без задачи на поводе compact отдаёт ту же одну строку, что на старте
- «стенд»: Каким сценарием стенд obeycheck меряет правку?
  - решает: исполнитель
  - рекомендация: новый сценарий рядом с 30-chat-no-task-human-reply: чат без задачи, реплика человека с долгим делом, проверка ждёт файл плана agentctl plan и ответ в ленту, а не каналом; судья смотрит, ушло ли долгое дело субагенту; прогон по скиллу prompt-test, база это ветка без правил

## DoD

- Сессия без задачи и без признака `DEVKIT_HIDDEN` получает на старте строки
  плана и отзывчивости и указатель на скилл `board-chat`. Это чат доски с
  пустой привязкой и консольная сессия, поднятая руками в любом проекте.
- Указатель велит позвать скилл и обходится без слова «по задаче». Описание
  скилла `board-chat` подходит к чату без привязки и к консоли.
- После сжатия контекста сессия без задачи получает просьбу перечитать скилл
  и те же строки плана и отзывчивости, как сессия с задачей.
- Сессия с признаком `DEVKIT_HIDDEN` без задачи молчит по-прежнему, сценарий
  DK-880 держится.
- Стенд obeycheck прогнан по скиллу `prompt-test` сценарием «чат без задачи,
  долгое дело, ответ человеку» против ветки без правил, след прогона лежит в
  файле задачи. Проигрыш указателя на стенде это не тело скилла в хуке, а
  отдельная развилка с бюджетом контекста.

## Ранг

`25+6+1+5+4 = 41`, P2.

- Серьёзность 25. В общем чате доски и в консоли план не заводится, кольцо
  стоит пустым, а долгие дела идут в одном ходе при молчащем окне. Обход
  дешёвый, человек первой репликой просит позвать скилл.
- Ценность 6. Замер DK-614 даёт со скиллом ноль уходов каналом из двадцати
  трёх, а замер DK-880 с одним указателем четыре зачёта из десяти. Касается
  чата доски и консоли, поток задач правила уже получает.
- Неопределённость 1. Обе человеческие развилки решены, ветка хука и канал
  готовы, исполнителю оставлены сжатие и состав сценария стенда.
- Поправка на баг 5. DK-880 решила, что план и отзывчивость остаются у всех,
  кто ведёт работу, а ветка без задачи их не получила.
- Рычаг 4. Каждый следующий разговор в чате доски и в консоли идёт по
  написанным правилам, а сценарий «долгое дело» стенда пригодится DK-881.

## Ход работы

- Из черновика DK-914 «Чат без задачи не получает правил разговора, указатель на скилл молчит»: записан 2026-09-10, оформлен 2026-09-10.
- Вычитка: 1 файл, 3 правки, 0 пометок, 2026-09-10.
