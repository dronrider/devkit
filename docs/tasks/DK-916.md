# DK-916: Интервью идёт вглубь по кейсам, а внутренности уходят исполнителю

## Что происходит

Разобрал 2026-09-10 все перечни «Развилок» машинной формы в docs/tasks: 147
развилок в 37 файлах, вложенных нет ни одной. В 29 файлах из 37 весь перечень
заведён одним коммитом, то есть одним раундом. Развилка после ответа человека
появилась в трёх файлах (DK-863, DK-864, DK-880), и ни одна из них не выросла
из ответа. Человеку адресовано 109 развилок из 147, исполнителю 38. Из
человеческих примерно треть про процесс и объём (ранг, бюджет, ярус, состав,
стыки с другими строками), треть про внутренности реализации (где живёт
функция, своё поле или производное, греп или индекс), треть про поведение,
видимое снаружи.

Форма развилки в TASKFORM.md не знает родителя, и команда taskctl decide --ask
ребёнка от соседа не отличает. Таблица «Что приносит вход» в скилле interview
велит грумингу собирать слагаемые ранга, предмет, конец работы и условия, а
постановке цели DoD, потолок по бакетам и ярус. Про сценарии использования
там ни слова. Правило останова кончает разговор, как только открытых
человеческих развилок нет.

Сверил ответы человека с рекомендациями по 107 решённым человеческим
развилкам. Ответ повторил рекомендацию в 98 случаях, разошёлся с ней в 9. Все
девять расхождений про процесс и объём: серьёзность в DK-396, состав в DK-837,
бюджет и ярус в DK-909, «одной задачей или отдельной» в DK-609, DK-861, DK-860
и DK-863. По поведению фичи человек не поправил агента ни разу. У 35 развилок
из 107 варианта ответа кроме рекомендации не было вовсе.

Проба на черновике DK-461 2026-09-10, перечень в его «Развилках». Первый
раунд назвал четыре пользовательских кейса и спросил только про них, без
хранения и без бюджета. Второй заход ушёл сразу по всем веткам, четыре
вопроса несли девять решений, и человек остановил разговор: такой объём не
принимается. Дальше раунд шёл по одной ветке до края, одна развилка на
вопрос, и три ветки закрылись за семь вопросов. Гипотеза черновика («агент
дополнения по ходу не берёт, спрашивает в конце») опровергнута вторым
ответом. Один вопрос человек вернул встречным: агент предлагал новый раздел
файла под след раскладки, а форма задачи уже отводит обоснованиям решений
«Развилки». Факт лежал в TASKFORM.md и не был снят до вопроса. Третья ветка
закрылась без вопроса: разницы для человека в ней не нашлось. Ответов «по
рекомендации» в пробе три из шести, два ответа расширили рекомендацию, один
её отверг.

Скилл спрашивает то, что ему велено, и останавливается на первом же пустом
крае. Постановка получает ответы про бюджет и состав, а поведение фичи в
пользовательских сценариях остаётся неспрошенным и всплывает уже на ревью
или на прогоне. Треть человеческих развилок по собственному критерию скилла
должна была уйти исполнителю с рекомендацией, машинной проверки этого шага
нет. Глубину разбора мерить нечем, потому что в форме её негде записать.

Раунд с четырьмя вопросами, где каждый ответ заранее известен, это не
интервью, а подтверждение галочками. Человек тратит внимание на вопросы, ответ
на которые агент уже написал рекомендацией, и по правилу шага 4 скилла такая
развилка исполнительская. Вопрос человеку нужен там, где рекомендация
неуверенная или ответ меняет видимое поведение, и таких в перечнях единицы.

## Чего хотим

Первый раунд интервью называет пользовательские кейсы записи и спрашивает
человека про них, а не про бюджет. Каждый следующий раунд идёт вглубь одной
ветки: ответ рождает детей, дети записаны под родителем, и --open печатает
дерево. Процессные развилки (ранг, состав, стыки) остаются, но идут после
предметных. Внутренности реализации уходят исполнителю с рекомендацией без
вопроса человеку, и это видно замером на стенде. Человеку уходят
только развилки, где рекомендация не одна или ответ меняет видимое поведение,
и доля ответов «по рекомендации» падает с девяти из десяти заметно ниже. Вопрос несёт одну
развилку, раунд идёт по одной ветке до её края, факт снимается до вопроса
даже про собственную форму записи, и ветка без разницы для человека
закрывается без вопроса, с решением агента в перечне.

## Границы

Родитель у развилки в эту строку не входит: подстрока формы, флаг `--after` у
`decide`, дерево в выводе `--open` и сторож формы заведены отдельной строкой.
Она садится в разбор перечня, где уже ждут находки DK-862 и DK-887, и правка
скилла её не ждёт.

## Развилки

- Как выглядит родитель в форме развилки и в команде: подстрока вроде
  «после: «имя»» и флаг у decide --ask, а --open печатает детей под
  родителем?
- Что считается пользовательским кейсом в записи и откуда скилл его берёт:
  из «Чего хотим», из сценария проверки, из вопроса человеку первым раундом?
- Чем раунд кончается по ветке: ответ человека без открытых детей, или у
  ветки есть потолок глубины?
- Как ворота отличают распределение «человек или исполнитель»: decide --ask
  без явного --who отказывает, стенд obeycheck меряет долю человеческих
  развилок про внутренности?
- Чем развилка заслуживает вопроса человеку: две рекомендации с равным
  доводом, ответ меняет DoD, ответ про кейс, о котором в записи ни слова?
  Развилка с одной уверенной рекомендацией уходит исполнителю без вопроса.
- Порядок веток: первой идёт та, чей ответ режет объём или меняет
  остальные, и агент называет причину выбора? Карта веток показывается один
  раз, дальше человек видит «ветка 2 из 4»?
- Размер раунда: пока следующий вопрос зависит от ответа, он один; в пачку
  до четырёх складываются только независимые вопросы одной ветки?

- «родитель»: как в форме развилки и в команде записан родитель?
  - решает: исполнитель
  - рекомендация: подстрока «после: «имя»» у ребёнка и флаг --after у --ask, --open печатает дерево отступом; неизвестная подстрока по TASKFORM это проза при развилке, форма не ломается
  - вариант: своя нумерация имён вида «кейсы.1»
- «край»: чем раунд кончается по ветке?
  - решает: исполнитель
  - рекомендация: ветка кончается, когда у её края нет открытых человеческих развилок; потолка глубины нет, глубину обрывает сам ответ
  - вариант: потолок глубины числом, дальше вопросы уходят исполнителю
- «порядок»: в каком порядке идут ветки и видит ли человек карту?
  - решает: исполнитель
  - рекомендация: первой идёт ветка, чей ответ режет объём или меняет остальные, причину выбора агент называет; карта веток печатается один раз, дальше в блоке стоит «ветка 2 из 4»
  - вариант: порядок записи перечня, карта не печатается
- «пачка»: какого размера раунд по ветке?
  - решает: исполнитель
  - рекомендация: зависимые вопросы идут по одному, в пачку до четырёх складываются только независимые развилки одной ветки; проба DK-461: человек оборвал разговор на четырёх вопросах с девятью решениями
  - вариант: потолок пачки остаётся четвёркой без оглядки на зависимость
- «кейсы»: откуда интервью берёт пользовательские кейсы первым раундом?
  - решает: человек
  - рекомендация: агент сам называет кейсы из «Что происходит» и «Чего хотим» записи и просит человека поправить список, потому что в записи они уже есть словами, а открытый вопрос перекладывает чтение на человека
  - вариант: человек называет кейсы сам, открытым вопросом первого раунда
  - вариант: кейсы берутся из сценария проверки, и до него первый раунд про них не спрашивает
  - решено человеком 2026-09-10: агент сам называет кейсы из «Что происходит» и «Чего хотим» записи и просит человека поправить список, потому что в записи они уже есть словами, а открытый вопрос перекладывает чтение на человека
- «право»: чем машина держит границу «человек или исполнитель»?
  - решает: человек
  - рекомендация: сценарий стенда obeycheck на исполнительский вопрос: запись с внутренней развилкой, проверка требует «решает: исполнитель» в перечне; команда --ask при этом никого не отбивает
  - вариант: decide --ask без явного --who отказывает, назначение называется всегда
  - вариант: ворота считают долю человеческих развилок в перечне и краснеют выше порога
  - решено человеком 2026-09-10: сценарий стенда obeycheck на исполнительский вопрос: запись с внутренней развилкой, проверка требует «решает: исполнитель» в перечне; команда --ask при этом никого не отбивает
- «мера»: чем доказываем, что интервью стало глубже и дешевле человеку?
  - решает: человек
  - рекомендация: стенд ловит правило, а живой замер ловит эффект: доля ответов «по рекомендации» и число вопросов до края на ближайших разборах против сегодняшних 98 из 107
  - вариант: только сценарии стенда obeycheck, живого замера не заводим
  - вариант: только живой замер на ближайших разборах, стенд не трогаем
  - решено человеком 2026-09-10: стенд ловит правило, а живой замер ловит эффект: доля ответов «по рекомендации» и число вопросов до края на ближайших разборах против сегодняшних 98 из 107
- «состав»: одной строкой или резать?
  - решает: человек
  - рекомендация: две строки: машинная вложенность (родитель в форме, --after у decide, дерево в --open, lint) отдельно, потому что садится в парсер перечня, где уже ждут черновики DK-862 и DK-887; правка скилла со сценарием стенда второй строкой, она даёт эффект и без дерева
  - вариант: одна задача целиком: форма, команда, скилл и стенд
  - вариант: сначала LLD: DK-916 пересматривает решения закрытой DK-552, дизайн раньше кода
  - решено человеком 2026-09-10: две строки: машинная вложенность (родитель в форме, --after у decide, дерево в --open, lint) отдельно, потому что садится в парсер перечня, где уже ждут черновики DK-862 и DK-887; правка скилла со сценарием стенда второй строкой, она даёт эффект и без дерева

## DoD

- Скилл `interview` первым раундом называет пользовательские кейсы записи и
  просит человека поправить список, а таблица «Что приносит вход» несёт кейсы
  темой у всех четырёх входов.
- Развилка уходит человеку, только когда рекомендация не одна либо ответ
  меняет видимое снаружи поведение. Остальные заводятся с `--who исполнитель`
  и рекомендацией, и человека ими не тревожат.
- Раунд идёт по одной ветке до её края: зависимый вопрос задаётся по одному, в
  пачку до четырёх складываются только независимые развилки одной ветки.
- Сценарий стенда obeycheck краснеет, когда агент отдал человеку развилку про
  внутренности с уверенной рекомендацией. Прогон `prompt-test` отмечен в файле
  задачи.
- На ближайших разборах записаны доля ответов «по рекомендации» и число
  вопросов до края ветки, и обе цифры сверены с сегодняшними 98 из 107.

## Ранг

`25+8+1+0+1 = 35`, P2.

- Серьёзность 25: постановка идёт, но человек отвечает на вопросы, ответ на
  которые уже написан рекомендацией. Обход дешёвый, одной строкой «по
  рекомендации», так и закрылись 98 развилок из 107.
- Ценность 8: вопрос человеку это остановка витка, и разбор без лишних
  вопросов делает каждый виток автономнее.
- Неопределённость 1: подход выбран ответами про кейсы, право, меру и состав,
  правка живёт в тексте скилла и в сценарии стенда, границ между процессами и
  языками нет.
- Поправка на баг 0: скилл работает так, как написан, и речь о новом
  поведении.
- Рычаг 1: правка текста скилла без новой механики, машинная часть уехала
  второй строкой.

## Приёмка

- вид: mixed
- барьер «событие»: долю ответов «по рекомендации» видно только на живых
  разборах, а они случаются в неизвестный момент
  - игрушечные часы: не годятся, событие тут не время, а разговор человека с
    агентом
  - синтетические входящие: не годятся, подставная запись даёт ответ агента, а
    меряется выбор человека
  - стенд с подменой события: годится для правила и стоит агентской половиной
    сценария, но доли ответов живого человека не даёт

## Ход работы

- Из черновика DK-916 «интервью идёт раундами вглубь по пользовательским кейсам»: записан 2026-09-10, оформлен 2026-09-10.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 34%, снимок 0м назад, сдвига нет), 2026-09-10 19:17-19:17.
- Вычитка: 3 файла, 8 правок, 0 пометок, 2026-09-10.

## Проверка

Сценарий стенда (`tools/obeycheck/scenarios/32-interview-exec-fork.md`) это
агентская половина. Черновик несёт уже открытую внутреннюю развилку на
`решает: человек`, и проверка краснеет, пока развилка не переведена
исполнителю. Пользовательская половина это живой замер на ближайших
разборах. Метрика (доля ответов «по рекомендации» и число вопросов до края
ветки против сегодняшних 98 из 107, `docs/tasks/DK-916.md`, раздел «Что
происходит») отдельной строкой не заводится, а идёт полем в «Ход работы»
по факту первых разборов с новым текстом.

- Стенд: 2026-09-10 20:45, дерево 4c5afb98, предмет 5624bdcf, база старый, ярус mini, k 3, сценарии 32-interview-exec-fork, зачтён.

```console
$ /tmp/obeycheck-check --devkit /Users/rider/projects/devkit-dk-916 --only 32-interview-exec-fork -k 3 --base старый --task DK-916 /Users/rider/projects/dk916-rules-with /Users/rider/projects/dk916-rules-old
сценарий                                        dk916-rules-with  dk916-rules-old  вердикт
не спрашивать человека про внутренности задачи               1/3              0/3  не хуже

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  32-interview-exec-fork / dk916-rules-with / повтор 1: проверка: exit status 1 (внутренняя развилка не переведена исполнителю: решает: исполнитель нет в docs/tasks/drafts/OB-032.md)
  32-interview-exec-fork / dk916-rules-old / повтор 1: проверка: exit status 1 (внутренняя развилка не переведена исполнителю: решает: исполнитель нет в docs/tasks/OB-032.md)
```
