# DK-920: Вход в Claude Design делается с плашки дашборда

## Что происходит

2026-09-10, разговор chat-29 в дашборде. Сессия открылась с отказом
подключения к серверу claude-design. api.anthropic.com отдаёт 403 на токен
обычного входа и просит `/design-login`. Из дашборда команду не подать.
Плашка входа заточена под одну команду `/login` (`tools/dashboard/clientlogin.go`,
строка 765). Сервер печатает её в одноразовую tmux-сессию, ловит ссылку и
принимает код. Слэш-команда из поля ввода уезжает агенту текстом, а с
префиксом `!` уходит в zsh как путь к файлу (`no such file or directory:
/design-login`). Человеку остался терминал на машине.

Дополнение 2026-09-22, чат DK-909. Диалог `/design-login` устроен как у
`/login`. Сначала печатается ссылка авторизации, под ней строка «Paste code
here if prompted», а после кода ответ «Design-system access authorized».
Разбор панели `loginAwaitLink` к нему подходит. Вход с телефона прошёл
руками соседней сессии. Она подала команду в tmux, склеила ссылку с экрана,
отдала её в свой чат, а код из чата занесла обратно в диалог. Через дашборд
такой ход не делается. Вторая беда рядом. Диалог со сбоем кода («Invalid
code. Press Enter to retry, or any other key to cancel») висит в сессии, и
реплики из панели уходят в него клавишами. Запертый диалог распознаётся при
подаче только по вопросу агента. Три реплики человека за сутки до
модели не дошли, а журнал дашборда на каждую записал «реплика подана».

Дашборд смотрят и с телефона, и вход в Claude Design без машины под рукой
невозможен вовсе. Работа с макетами встаёт до тех пор, пока человек не дойдёт
до терминала. Отказ виден только в шапке сессии, плашка о нём молчит, и
«сессия открыта, а макета нет» неотличимо от работы.

Разбор 2026-09-22 снял два вопроса из трёх. Диалог `/design-login` устроен как
у `/login`, и разбор `loginAwaitLink` к нему подходит, а расходится путь
отказа кода. Признак отказа нашёлся в том, что дашборд и так читает: клиент
пишет в транскрипт запись `attachment` с полем `failedMcpServers`, где стоят
имя `claude-design`, код `FIRST_PARTY_AUTH_REJECTED` и текст «Run
/design-login and retry». Прогон `claude mcp list` дороже и вида отказа не
показывает, а слова панели tmux заточены под экран `/login`. Третий вопрос,
вид плашки, остался открытым и держит старт.

## Чего хотим

На плашке разлогина дашборда есть вход в Claude Design тем же порядком, что
и обычный вход: кнопка, ссылка, код, исход словами. Сессия с отказом
claude-design показывает плашку сама, а свежий процесс после входа
подключается к серверу.

## Границы

Реплики, уходящие в запертый диалог входа и теряющиеся молча, здесь не
чинятся. Это предмет черновика DK-1021, где тот же дашборд подаёт реплику в
клиент, стоящий на экране входа.

## Развилки

- «отказ кода»: как разбирать неудачный код в диалоге design-login, если общие узнаватели читают повторяемый отказ как окончательный провал?
  - решает: исполнитель
  - рекомендация: завести узнаватели поля и отказа по виду входа, общие списки слов не править: диалог design-login по дополнению черновика печатает «Invalid code. Press Enter to retry», слов loginCodeWords в нём нет, loginWantsCode вернёт false, и loginSaysFailed по слову invalid объявит окончательный провал и снимет живую сессию (clientlogin.go:185, 215, 912-945)
  - источник: код, tools/dashboard/clientlogin.go:912-945
- «второй вход»: как развести обычный вход и вход в Claude Design внутри сервера: поле вида у одной сессии входа либо второй набор ручек?
  - решает: исполнитель
  - рекомендация: завести в loginRun поле вида входа и передавать вид в теле трёх существующих ручек, второго набора маршрутов не заводить: сейчас у сервера одно поле s.loginRun без признака вида, и подряд поднятые входы столкнутся (clientlogin.go:27-36, 648-671, server.go:309-311)
  - источник: код, tools/dashboard/clientlogin.go:27-36 и server.go:309-311
- «кейсы»: какие пользовательские кейсы у DK-920? рекомендую широкий: кейсы (4) и (5) это находки обхода кода, без них прямой перенос механики /login ломается на отказе кода и на втором входе подряд
  - решает: человек
  - рекомендация: широкий состав, 5 кейсов: (1) человек с телефона входит в Claude Design кнопкой на плашке дашборда; (2) тот же вход с машины из панели вместо терминала; (3) сессия с отказом claude-design поднимает плашку сама; (4) код не принят: человеку сказано словами и попытка повторяется без потери сессии входа; (5) обычный вход и вход в Claude Design подняты подряд и не мешают друг другу
  - вариант: сбалансированный состав, 3 кейса: (1) человек с телефона входит в Claude Design кнопкой на плашке дашборда; (2) тот же вход с машины из панели вместо терминала; (4) код не принят: человеку сказано словами и попытка повторяется без потери сессии входа
  - вариант: узкий состав, 2 кейса: (1) человек с телефона входит в Claude Design кнопкой на плашке дашборда; (2) тот же вход с машины из панели вместо терминала
  - решено человеком 2026-09-22: широкий состав, 5 кейсов: (1) человек с телефона входит в Claude Design кнопкой на плашке дашборда; (2) тот же вход с машины из панели вместо терминала; (3) сессия с отказом claude-design поднимает плашку сама; (4) код не принят: человеку сказано словами и попытка повторяется без потери сессии входа; (5) обычный вход и вход в Claude Design подняты подряд и не мешают друг другу
- «место кнопки»: как вход в Claude Design встаёт на плашке дашборда: вторая кнопка рядом с «Войти», выбор внутри одной кнопки либо своя плашка?
  - решает: человек
  - рекомендация: вторая кнопка рядом с «Войти» на той же плашке: плашка поднимается по признаку конкретного входа, и выбор из списка заставил бы человека самого понимать, какой из двух входов слетел
  - вариант: одна кнопка входа с выбором вида внутри плашки
  - вариант: своя плашка на вход в Claude Design, отдельно от плашки разлогина
  - решено человеком 2026-09-26: один блок в ленте с одной кнопкой, вид входа выбирает признак подъёма: отказ claude-design даёт «Войти в Claude Design», разлогин клиента даёт «Войти». Механика /login переносится целиком, расходятся только команда в tmux и признак. Две кнопки рядом не нужны: признаки приходят в разные моменты жизни разговора, и при обоих сразу заходы всё равно идут подряд
- «признак»: откуда дашборд узнаёт, что вход в Claude Design слетел: запись транскрипта, прогон claude mcp list или слова панели tmux?
  - решает: исполнитель
  - рекомендация: читать транскрипт, который дашборд и так читает: при отказе клиент пишет запись «type»: «attachment» с полем failedMcpServers, там имя claude-design, errorCode FIRST_PARTY_AUTH_REJECTED и текст «Run /design-login and retry» (проверено на 002159a1-90d2-4303-b719-84a8dae28d52.jsonl от 2026-08-2x); claude mcp list это внешний процесс на каждый тик и формат отказа у него не проверен, а слова панели tmux заточены под экран /login
  - источник: транскрипты клиента в ~/.claude/projects, запись attachment с полем failedMcpServers

## DoD

Сессия, где подключение к claude-design отказало, поднимает в панели плашку с
входом в Claude Design. Человек с телефона жмёт кнопку, получает ссылку,
заносит код и читает словами, чем вход кончился. Свежий процесс после этого
подключается к серверу, и отказ из шапки сессии уходит. Неверный код
повторяется в той же сессии входа, а обычный вход и вход в Claude Design,
поднятые подряд, друг другу не мешают.

## Ранг

`50+6+3+0+3 = 62`, P1.

- серьёзность 50: основной путь сломан, обход болезненный. С телефона вход не
  сделать вовсе, а руками он прошёл только через соседнюю сессию, которая
  склеила ссылку с экрана tmux и занесла код обратно.
- ценность 6: работа с макетами через дашборд стоит каждый раз, когда токен
  дизайна слетает, и ждёт человека у машины.
- неопределённость 3: подход известен, механика входа переиспользуется, но
  правка идёт через границу сервера и панели, а вид плашки остаётся открытой
  развилкой.
- поправка на баг 0: вход в Claude Design из дашборда не ломался, его там не
  было.
- рычаг 3: снимает привязку дизайнерской ветки к машине, на которой лежит
  терминал.

Цена M: механика входа готова, работа это поле вида в сессии входа, свои
узнаватели диалога, признак отказа из транскрипта и кнопка на панели.

Приёмка mixed, барьер «секрет». Полный путь входа требует учётной записи
человека и кода из браузера, а разбор диалога и подъём плашки агент закрывает
фикстурой панели и записью транскрипта.

## Приёмка

- вид: mixed
- барьер «секрет»: живой вход требует учётной записи человека и кода из браузера, у агента её нет и secretctl не заводит; фикстура панели и запись транскрипта закрывают разбор диалога и подъём плашки, но не сам вход

## Ход работы

- Из черновика DK-920 «Вход в Claude Design делается с плашки дашборда, как /login»: записан 2026-09-10, оформлен 2026-09-22.
- Вычитка: 1 файл, 6 правок, 0 пометок, 2026-09-22.
- Постановка: разбор черновика, сессия startup, 2026-09-22 13:13-10:49.
- Вычитка: субагент sonnet по определению proofread, работа a4ee089d5393f584c, минут 2, 2026-09-22 16:03-16:05.
- Ждёт человека: вопрос: «место кнопки»: как вход в Claude Design встаёт на плашке дашборда: вторая кнопка рядом с «Войти», выбор внутри одной кнопки либо своя плашка?, 2026-09-26 10:49-10:50.
- Ждёт человека: блок: вопрос: место кнопки входа в Claude Design на плашке, 2026-09-26 10:50-12:32.
