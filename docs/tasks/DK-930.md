# DK-930: agentctl wait ждёт слияния и закрытия строки, конца процесса и назначенного часа

Цель: [tasks/DK-902.md](tasks/DK-902.md)

## Что происходит

2026-09-11, нарезка цели DK-902 чтением кода. DK-899 завела команду
`agentctl wait` (tools/agentctl/wait.go), и оболочка task-run.py больше не
снимает голову, которая отметила ожидание. Видов условия у команды четыре:
`срок`, `есть <путь>`, `нет <путь>` и `чисто <дерево>`. Срок обязателен и не
длиннее двух часов. Отметка лежит в `~/.devkit/waits/<ID>.json`, читает её
только `wait_mark` в task-run.py.

Голова позвала слияние фоном и ждёт его конца. У команды нет условия «задача
слита». Голова подбирает путь к файлу, который появится после слияния,
или ставит голый срок и потом проверяет сама. С закрытием соседней строки и с
концом фоновой команды то же самое. Потолок в два часа прибит в коде, профиль
харнеса его не задаёт.

## Чего хотим

Голова называет условие прямо, и видов у `agentctl wait` становится больше.
Условие «слита» ждёт, пока код ветки задачи войдёт в main. Условие «закрыта»
ждёт строку в архиве. Условие «процесс» ждёт конца процесса по pid. Условие
«час» ждёт абсолютного времени вместо срока от текущей минуты. Оболочка
task-run.py проверяет новые условия так же, как старые, и просыпается по
событию раньше срока. Потолок срока берётся из профиля харнеса в kit/harness,
у claude-code и glm-code умолчание остаётся двухчасовым.

Правило в промптах исполнителя называет новые условия рядом со старыми.
Первым в нём идёт пример с фоновым слиянием.

## Границы

Ожидание дольше хода, парковка с разрядами и подъём ждущих идут своими
строками цели. Здесь только ход, который ждёт внутри себя. Сторожок и дашборд
отметок ожидания по-прежнему не читают.

## Развилки

- Признак «слита» закрыт решением 2 LLD DK-933. Признак лежит в internal,
  опорой ему служат запись «Выкат» в файле задачи и subject коммитов. Ветку
  после слияния shipctl удаляет, и `git merge-base --is-ancestor` по ней не
  работает. Правка только в доске и файлах задач слиянием не считается, правка
  в docs считается. Если DK-930 сливается раньше строки про смысл ребра
  «после», признак в internal кладёт она.
- Имя ключа потолка в профиле харнеса и его секция. Решено исполнителем
  2026-09-11. Ключ `wait_cap` лежит в секции `[head]`, значение пишется
  строкой срока, как у `--until` (`wait_cap = "2h"`). Потолок относится к живой
  голове, и секция `[head]` описывает её же. Секцию уже читают оболочка
  task-run.py и `internal/taskhead`. Новому ключу хватило строки в двух сводах
  типов. Отказ печатает потолок той же записью, какой он стоит в профиле.
  Отвергнуты три варианта. Своя секция `[wait]` заводилась бы под один ключ, и
  обоим разборщикам профиля понадобилась бы новая ветка. Число секунд
  разошлось бы с записью `--until`, и отказ называл бы секунды. Ключ в
  машинном слое `~/.devkit/harness.local` постановке не подходит, значение
  нужно в профиле. Потолок у харнеса один на все машины.

## DoD

- `agentctl wait` с условиями `слита DK-X`, `закрыта DK-X`, `процесс <pid>` и
  `час <время>` кладёт отметку. Незнакомое условие даёт отказ с перечнем видов.
- Оболочка task-run.py выходит из ожидания по каждому новому условию раньше
  срока. Тест идёт на синтетической доске и временном git: слияние ветки,
  закрытие строки, смерть процесса. Тест «слияние ветки» проверяет признак уже
  после удаления ветки.
- Потолок срока читается из профиля харнеса. Срок выше потолка даёт отказ, в
  котором названы потолок и профиль.
- Правило в промптах исполнителя называет новые условия, след стенда obeycheck
  лежит в файле задачи.

## Ранг

`25+8+3+0+4 = 40`, P2.

- Серьёзность 25: голова ждёт слияния сроком и перепроверкой. Обход дешёвый,
  но каждая голова собирает его заново.
- Ценность 8: голова конвейера ждёт соседнюю работу, не выходя из хода.
- Неопределённость 3: отметку пишет go-утилита, проверяет python-оболочка, а
  условие «слита» читает git.
- Поправка на баг 0: новые виды условия.
- Рычаг 4: на этих условиях стоят остальные строки цели и любая голова под
  оболочкой.

Цена M. Подкоманда с тестами, проверка в оболочке и строка правила ложатся в
одну сессию.

## Приёмка

- вид: agent
- барьер «событие» рассмотрен. Слияние, закрытие и смерть процесса делаются на
  временном репозитории и синтетической доске, час подделывается близким
  временем

## Ход работы

- Вычитка: 1 файл, 1 правка, 0 пометок, 2026-09-11.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 46%, week_max 32%, снимок 30м назад, сдвига нет), 2026-09-11 14:50-14:50.
- Вычитка: 3 файла, 4 правки, 1 пометка, 2026-09-11.
- Исключение: стенд (task-run.py это код оболочки, тесты task_run_test.py проверяют её поведение; правило в SKILL.md board-task прогнано стендом отдельно)
- Вычитка: 1 файл, 3 правки, 0 пометок, 2026-09-11.

## Ревью

Уровень 3 до f32f081: неопределённость 3 из ранга, признак "слита" в internal критичен для будущих taskctl/shipctl, дифф трогает tools/shipctl (critical_paths)

- Вердикт: без замечаний. живой сценарий из DoD прогнан бинарём (6 вызовов wait/--check совпали посимвольно с ожиданием), тесты internal/merged, internal/attime, agentctl TestWait+TestHarnessFixtures, harness_test.py, shipctl TestTaskCommitsAgreeWithMerged, task_run_test.py (28) зелёные; код internal/merged, attime, wait.go, task-run.py и перенос ownsSubject/isRevertSubject/boardOnly в internal/merged прочитан построчно; обеycheck 31-wait-mark 2/3 против 3/3 сверен с verdictOf: Фишер при k=3 не значим, «не хуже» законно; барьер «событие» ACCEPTANCE.md закрыт синтетикой; дока agentctl/README.md, internal/README.md, SKILL.md board-task соответствует коду

## Сценарий проверки

1. Признак «слита» и разбор часа на временном git. Ветка слитой задачи удалена,
   а признак видит работу по subject. Запись «Выкат» находит коммит без номера.
   Правка доски слиянием не считается, правка docs/lld считается. После отката
   признак ложен до нового слияния. Из дерева задачи видны запись и архив
   основного чекаута.

   ```sh
   cd internal && GOWORK=off go test -count=1 -v ./merged/ ./attime/ 2>&1 | grep -E '^(--- |ok|FAIL)'
   ```

   В выводе `--- PASS` у TestBranchMergedAfterDelete,
   TestRecordWithoutIDInSubject, TestBoardOnlyIsNotMerged,
   TestRevertHoldsUntilRemerge, TestSeenFromWorktree и TestParse. У обоих
   пакетов строка `ok`, строк `FAIL` нет.

2. Команда `agentctl wait` и профиль харнеса. Новые условия кладут отметку,
   пришедшее событие и строка без события дают отказ, потолок читается из
   `[head] wait_cap`. Секцию оба парсера профиля читают одинаково.

   ```sh
   cd tools/agentctl && GOWORK=off go test -count=1 -run 'TestWait|TestHarnessFixtures' . && cd ../.. && python3 tools/devkitctl/harness_test.py
   ```

   В выводе `ok  github.com/dronrider/devkit/tools/agentctl` и `OK`.

3. shipctl и признак находят работу задачи одним кодом.

   ```sh
   cd tools/shipctl && GOWORK=off go test -count=1 -run TestTaskCommitsAgreeWithMerged .
   ```

   В выводе `ok  github.com/dronrider/devkit/tools/shipctl`.

4. Оболочка выходит из ожидания по событию раньше срока. Событий четыре,
   слияние с удалённой веткой, закрытие строки, смерть процесса и час. Слияние
   и закрытие она спрашивает у настоящего agentctl, собранного из дерева.

   ```sh
   python3 kit/skills/board-task/task_run_test.py TestWaitKinds TestWaitEvents TestWaitMark
   ```

   В выводе `Ran 28 tests` и `OK`, пропусков нет.

5. Настоящий бинарь на синтетической доске. До слияния соседки событие не
   пришло, после слияния и удаления ветки пришло, и отметка на слитую задачу
   отбивается. Срок выше потолка называет потолок и профиль, незнакомое
   условие называет все виды.

   ```sh
   cd tools/agentctl && GOWORK=off go build -o "$HOME/agentctl-dk930" . && cd ../..
   A="$HOME/agentctl-dk930"; R="$HOME/board-dk930"; export DEVKIT_WAIT_DIR="$HOME/waits" DEVKIT_HOME="$PWD" DEVKIT_HARNESS=
   G="git -c user.name=t -c user.email=t@t -c commit.gpgsign=false"
   mkdir -p "$R/docs" && cd "$R" && git init -q -b main
   printf '# доска\n\n## In progress\n\n| ID | Задача | Тип | P | R | Цена | Ссылка |\n|---|---|---|---|---|---|---|\n| DK-1 | голова | task | P2 | 1 | S | - |\n| DK-2 | соседка | task | P2 | 1 | S | - |\n' > docs/TASKS.md
   git add -A && $G commit -q -m "docs(tasks): доска"
   git switch -q -c dk-2 && echo 'package x' > code.go && git add code.go && $G commit -q -m "feat: DK-2 код" && git switch -q main
   "$A" wait DK-1 слита DK-2 --check; echo "код $?"
   "$A" wait DK-1 закрыта DK-2 --until 10m; echo "код $?"
   $G merge -q --no-ff -m "Merge branch dk-2" dk-2 && git branch -q -D dk-2
   "$A" wait DK-1 слита DK-2 --check; echo "код $?"
   "$A" wait DK-1 слита DK-2 --until 10m; echo "код $?"
   "$A" wait DK-1 срок --until 3h; echo "код $?"
   "$A" wait DK-1 погода --until 10m; echo "код $?"
   ```

   В выводе по порядку:
   - `DK-2 не слита` и `код 1`;
   - `DK-1 ждёт: закрыта DK-2, срок до` и `код 0`;
   - `DK-2 слита:` с коммитом `feat: DK-2 код` и `код 0`;
   - `событие уже пришло, ждать нечего` и `код 1`;
   - `срок 3h больше потолка 2h (профиль`, путь `kit/harness/claude-code.toml`
     и `код 1`;
   - `условие "погода" неизвестно, виды: срок, есть, нет, чисто, слита,
     закрыта, процесс, час` и `код 1`.

## Проверка

- Стенд: 2026-09-11 15:46, дерево 924606e7, предмет d4a9e749, база старый, ярус mini, k 3, сценарии 33-wait-merge, зачтён.

```console
$ obeycheck --task DK-930 --only 33-wait-merge -k 3 --base старый /private/tmp/claude-501/-Users-rider-projects-devkit/1cee7798-9061-4d49-bb39-cae460a61245/scratchpad/rules-with /private/tmp/claude-501/-Users-rider-projects-devkit/1cee7798-9061-4d49-bb39-cae460a61245/scratchpad/rules-old
сценарий                                    rules-with  rules-old  вердикт
дождаться фонового слияния соседней задачи         3/3        0/3  польза

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  33-wait-merge / rules-old / повтор 1: проверка: exit status 1 (слияние не названо условием: вызова agentctl wait OB-002 слита OB-001 нет)
```

- Стенд: 2026-09-11 15:51, дерево 924606e7, предмет d4a9e749, база старый, ярус mini, k 3, сценарии 31-wait-mark, зачтён.

```console
$ obeycheck --task DK-930 --only 31-wait-mark -k 3 --base старый /private/tmp/claude-501/-Users-rider-projects-devkit/1cee7798-9061-4d49-bb39-cae460a61245/scratchpad/rules-with /private/tmp/claude-501/-Users-rider-projects-devkit/1cee7798-9061-4d49-bb39-cae460a61245/scratchpad/rules-old
сценарий                                      rules-with  rules-old  вердикт
отметить машинное ожидание перед концом хода         2/3        3/3  не хуже

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  31-wait-mark / rules-with / повтор 2: проверка: exit status 1 (отметки ожидания нет: оболочка сочтёт проход холостым)
```
