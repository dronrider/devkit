# DK-931: Команда taskctl run поднимает голову задачи лестницей носителей

Цель: [tasks/DK-902.md](tasks/DK-902.md)

## Что происходит

2026-09-11, нарезка цели DK-902 чтением кода. Голову задачи сейчас поднимает
только дашборд. Функция `startTaskSession` (tools/dashboard/runs.go:406)
заводит tmux-сессию `task-<ID>` с оболочкой task-run.py. К ней ведут четыре
дороги: кнопка экрана, `dashboard wake`, `dashboard check` и
`dashboard round`. Тик сторожка тоже зовёт дашборд, и без него голову никак
не поднять.

Своего замка у task-run.py нет. От двойного старта защищает только
совпадение имени tmux-сессии: при живой сессии runs.go отвечает отказом 409.
При живом окне `taskWake` снимает парковку и в окно ничего не подаёт.
Реплику в живое окно подаёт только `resume_failed` в watch.py, и только
сессии с непустым полем tmux в реестре чатов. Поле пишет хук
`hooks/session-task.py` из переменной `DEVKIT_TMUX`, которую ставит дашборд.
Окно, открытое руками, получает в реестре прочерк.

Команда клиента прибита в коде дважды, в task-run.py:274 и в `clientCommand`
(runs.go:359). В профилях kit/harness ключей под неё нет.

## Чего хотим

`taskctl run <ID>` поднимает голову задачи и перебирает носителей по порядку.
Первым идёт живое окно tmux по задаче из реестра чатов, туда уходит реплика
«продолжай». Вторым идёт новое окно tmux с оболочкой task-run.py. Без tmux
голова поднимается headless через ту же оболочку. Последним идёт громкий зов
человеку, в тексте которого лежит готовая команда.

Команда берёт замок на ID задачи в ~/.devkit. Занятый замок даёт отказ с
отдельным кодом выхода, второй головы нет. Адрес поднятой сессии в реестр
пишет сама команда. Хук старта остаётся для сессий, поднятых рукой, и пишет
адрес любого окна tmux из `$TMUX` и `$TMUX_PANE`.

Команду клиента, резюм и признак конца хода команда берёт из профиля харнеса.
Ключи заводятся у claude-code и glm-code. Код запуска ложится в общий
go-модуль internal, и дашборд переходит на него строкой DK-935.

## Границы

Перевод дашборда, `close`, `merge` и тика на команду делают другие строки
цели, DK-932 и DK-935. Здесь сама команда, замок, ключи профиля и адрес окна в
реестре. Профиль под Codex в цель не входит.

## Развилки

- «замок»: как команда подъёма и тик делят замок от двойного старта?
  - решает: исполнитель
  - рекомендация: замок на ID задачи в ~/.devkit, тот же, что держит task-run.py: команда подъёма и тик берут его до старта головы, занятый замок это молчаливый отказ
  - решено исполнителем 2026-09-11: каталог ~/.devkit/task-<ID>.lock с pid владельца, как у goal-run.py. Держит его task-run.py, пока жива голова. Первой замок берёт `taskctl run` и кладёт туда свой pid. Оболочка получает этот pid переменной DEVKIT_TASK_LOCK_FROM и вписывает свой, а команда ждёт подмены. Оболочка, поднятая мимо команды, берёт замок сама. При занятом замке команда даёт код 3 и строку в stderr. Тик DK-932 читает код 3 как молчаливый отказ. Отвергнут flock. Tmux не передаёт дескриптор в окно, а владельца по файлу не прочесть. Отвергнут замок на время старта. После выхода команды второй вызов поднял бы вторую голову. Отвергнуто имя tmux-сессии. Оно не покрывает headless и окна, открытые руками.
- «лестница»: в каком порядке команда подъёма перебирает носителей?
  - решает: исполнитель
  - рекомендация: живое окно tmux по задаче, иначе новое окно tmux, иначе headless через task-run.py, иначе громкий зов человеку с готовой командой; без tmux лестница начинается с headless
  - решено исполнителем 2026-09-11: по рекомендации. Сначала замок. Потом живое окно из реестра чатов: хозяин адреса сверен, панель жива, и в ней не голая оболочка. Потом новое окно tmux `task-<ID>` с task-run.py. Потом headless task-run.py отдельным процессом. Последним идёт громкий зов через notify.py с готовой командой. При провале ступени команда передаёт ход следующей и оставляет строку в выводе. Без клиента пропускаются обе ступени подъёма, без tmux пропускается новое окно. Отвергнут headless раньше нового окна. Печатной голове не подать реплику человека. Отвергнут зов человеку вместо headless на машине без tmux. Цель требует конвейера без человека.

## DoD

- `taskctl run <ID>` при живом окне задачи подаёт в него реплику и нового
  окна не заводит.
- Без живого окна команда заводит окно tmux с task-run.py. Без tmux она
  поднимает headless, а без клиента зовёт человека через notify.py строкой с
  готовой командой.
- Второй вызов при занятом замке кончается отказом, вторая голова не встаёт.
- Сессия, поднятая командой, лежит в реестре чатов с адресом окна. Окно,
  открытое руками внутри tmux, получает адрес через хук старта.
- Команда клиента и резюм берутся из профиля харнеса, в task-run.py прибитой
  строки клиента нет.
- Каждая ступень лестницы проверена тестом на подменных tmux и клиенте.

## Ранг

`50+8+3+0+4 = 65`, P1.

- Серьёзность 50: конвейер должен работать без человека, а мёртвую голову без
  дашборда поднимает человек руками. Сессию без поля tmux и headless тик не
  поднимает вовсе.
- Ценность 8: подъём нужен каждой ждущей задаче, ночью и в любой пачке.
- Неопределённость 3: команда пишется на go, оболочка на python, замок
  делится с тиком, лестница идёт через tmux.
- Поправка на баг 0: новая команда.
- Рычаг 4: на подъём опираются разряды парковки, взвод ребра и дашборд.

Цена M. Команда, замок, ключи профиля и адрес окна в хуке ложатся в одну
сессию, перенос дашборда вынесен в DK-935.

## Приёмка

- вид: agent
- барьер «событие» рассмотрен. Живое и мёртвое окно, отсутствие tmux и
  клиента делаются подменой на стенде

## Ход работы

- Вычитка: 1 файл, 4 правки, 0 пометок, 2026-09-11.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 43%, week_max 31%, снимок 1м назад, сдвига нет), 2026-09-11 12:49-12:50.
- Вычитка: 4 файла, 11 правок, 0 пометок, 2026-09-11.

## Сценарий проверки

1. Лестница на подменных tmux, клиенте и оболочке. Каждая ступень проверена
   своим тестом. Живое окно получает реплику, новое окно поднимает оболочку и
   пишет сессию в реестр с адресом. Без tmux голова идёт headless, без клиента
   зовётся человек. Второй вызов при занятом замке отказывает.

   ```sh
   cd internal && GOWORK=off go test -count=1 -v ./taskhead/ ./sessions/ 2>&1 | grep -E '^(--- |ok|FAIL)'
   ```

   В выводе `--- PASS` у TestLiveWindowGetsReply, TestNewWindowRaisesShell,
   TestNoTmuxGoesHeadless, TestNoClientCallsHuman,
   TestBusyLockRefusesSecondHead и TestParseLineReadsPane. У обоих пакетов
   строка `ok`, строк `FAIL` нет.

2. Обвязка команды. Харнес берётся из флага и переменной, а при занятом замке
   команда возвращает код 3.

   ```sh
   cd tools/taskctl && GOWORK=off go test -count=1 -run 'TestRun|TestCmdRun' .
   ```

   В выводе `ok  github.com/dronrider/devkit/tools/taskctl`.

3. Оболочка держит замок и берёт клиента из профиля.

   ```sh
   python3 kit/skills/board-task/task_run_test.py TestLock TestProfileClient
   ```

   В выводе `Ran 7 tests` и `OK`.

4. Хук старта пишет адрес панели у окна, открытого руками.

   ```sh
   python3 hooks/session_task_test.py TestRecord
   ```

   В выводе `OK`.

5. Секцию `[head]` оба парсера профиля читают одинаково.

   ```sh
   cd tools/agentctl && GOWORK=off go test -count=1 -run TestHarnessFixtures . && cd ../.. && python3 tools/devkitctl/harness_test.py
   ```

   В выводе `ok  github.com/dronrider/devkit/tools/agentctl` и `OK`.

6. Настоящий бинарь без клиента в PATH. Голова не поднимается, человек позван
   строкой с готовой командой, код выхода 1.

   ```sh
   cd tools/taskctl && GOWORK=off go build -o "$HOME/taskctl-dk931" . && cd ../.. && PATH=/usr/bin:/bin DEVKIT_NOTIFY_OFF=1 DEVKIT_HOME="$PWD" "$HOME/taskctl-dk931" run DK-931 -C "$PWD"; echo "код $?"
   ```

   В выводе строка `клиент claude не нашёлся в PATH`, строка с `Поднять
   руками: taskctl run DK-931 -C`, строка `ступень: зов человеку, код 1` и
   `код 1`.

## Проверка

- Обкатка: 2026-09-11 13:42, свежее дерево 1618ad153a35, сценарий 0a8ea1cc, временный HOME, утилит дерева 11, шагов 6, все зелёные.

Шаг 1 (зелёный):

```console
$ cd internal && GOWORK=off go test -count=1 -v ./taskhead/ ./sessions/ 2>&1 | grep -E '^(--- |ok|FAIL)'
--- PASS: TestTakeRefusesLiveOwner (0.00s)
--- PASS: TestTakeRetakesDeadOwner (0.00s)
--- PASS: TestTakeRefusesYoungEmptyLock (0.00s)
--- PASS: TestReleaseKeepsHandedLock (0.00s)
--- PASS: TestWaitHandoff (0.37s)
--- PASS: TestRealProfilesNameTheHead (0.00s)
--- PASS: TestReadHeadParsesStringsAndLists (0.00s)
--- PASS: TestReadHeadRefusals (0.01s)
--- PASS: TestLiveWindowGetsReply (0.54s)
--- PASS: TestLiveWindowByName (0.65s)
--- PASS: TestStaleWindowsAreSkipped (2.89s)
--- PASS: TestNewWindowRaisesShell (0.84s)
--- PASS: TestBusyLockRefusesSecondHead (0.62s)
--- PASS: TestNoTmuxGoesHeadless (0.48s)
--- PASS: TestWindowFailureFallsToHeadless (0.63s)
--- PASS: TestNoClientCallsHuman (0.47s)
--- PASS: TestShellWithoutLockEndsInCall (0.91s)
--- PASS: TestMissingHeadSectionIsSetupError (0.06s)
--- PASS: TestDevkitLookup (0.00s)
ok  	github.com/dronrider/devkit/internal/taskhead	8.910s
--- PASS: TestParseLineReadsPane (0.00s)
--- PASS: TestLineWritesPaneAndLastKeepsIt (0.00s)
--- PASS: TestPaneOwnerTakesTheFreshestClaim (0.00s)
--- PASS: TestParseLineKeepsSpacedPaths (0.00s)
--- PASS: TestParseLineReadsParent (0.00s)
--- PASS: TestParseSkipsForeignLines (0.00s)
--- PASS: TestLeadsTakesTheFreshestRecord (0.00s)
--- PASS: TestLeadsForgetsTheUnbound (0.00s)
--- PASS: TestLoadWithoutRegistry (0.00s)
--- PASS: TestParseKeepsTmuxAcrossWorkRecords (0.00s)
--- PASS: TestWorksStandsOnSourceNotWindow (0.00s)
--- PASS: TestWorksIgnoresOrderAndHand (0.00s)
--- PASS: TestWorksKeepsEveryTaskUntilRelease (0.00s)
--- PASS: TestTmuxOwnerSinceSkipsRecordsBeforeRaise (0.00s)
ok  	github.com/dronrider/devkit/internal/sessions	0.829s
```

Шаг 2 (зелёный):

```console
$ cd tools/taskctl && GOWORK=off go test -count=1 -run 'TestRun|TestCmdRun' .
ok  	github.com/dronrider/devkit/tools/taskctl	7.735s
```

Шаг 3 (зелёный):

```console
$ python3 kit/skills/board-task/task_run_test.py TestLock TestProfileClient
----------------------------------------------------------------------
Ran 7 tests in 2.771s

OK
```

Шаг 4 (зелёный):

```console
$ python3 hooks/session_task_test.py TestRecord
----------------------------------------------------------------------
Ran 17 tests in 0.082s

OK
```

Шаг 5 (зелёный):

```console
$ cd tools/agentctl && GOWORK=off go test -count=1 -run TestHarnessFixtures . && cd ../.. && python3 tools/devkitctl/harness_test.py
ok  	github.com/dronrider/devkit/tools/agentctl	0.547s
........
----------------------------------------------------------------------
Ran 8 tests in 12.645s

OK
```

Шаг 6 (зелёный):

```console
$ cd tools/taskctl && GOWORK=off go build -o "$HOME/taskctl-dk931" . && cd ../.. && PATH=/usr/bin:/bin DEVKIT_NOTIFY_OFF=1 DEVKIT_HOME="$PWD" "$HOME/taskctl-dk931" run DK-931 -C "$PWD"; echo "код $?"
tmux не нашёлся в PATH: живого и нового окна не будет, лестница начинается с headless
клиент claude не нашёлся в PATH: поднимать голову нечем
голову DK-931 поднять нечем: tmux не нашёлся в PATH: живого и нового окна не будет, лестница начинается с headless; клиент claude не нашёлся в PATH: поднимать голову нечем. Поднять руками: taskctl run DK-931 -C /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/taskctl-rehearse-3681758697/tree
уведомитель отработал, строка зова в ~/.devkit/notify.log
ступень: зов человеку, код 1
код 1
```
