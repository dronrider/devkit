# DK-932: Разряды «слияние:» и «закрытие:», close и merge поднимают припаркованных

Цель: [tasks/DK-902.md](tasks/DK-902.md)

## Что происходит

2026-09-11, нарезка цели DK-902 чтением кода. Машинных разрядов парковки у
taskctl четыре: «вопрос:», «окружение:», «автор:» и «спор:» (`parkPrefixes`,
tools/taskctl/ops.go:79). Незнакомый разряд проходит как проза. Сторожок
`devkitctl watch` различает только «вопрос:» и «автор:», это константы
`PARKED` и `REVIEW_PARKED` в watch.py. Вопрос он поднимает через
`dashboard wake`, автора через `dashboard round`. «Окружение:» и «спор:» не
будит никто.

Строке нечем ждать соседнюю задачу. DK-863 простояла в Blocked с причиной
«ждёт DK-888», когда DK-888 уже закрылась. `taskctl close` в конце снимает
маркер «после» и зовёт разлив поезда. `shipctl merge` в конце зовёт прогон
Check. Ни одна из двух команд не обходит ждущих.

## Чего хотим

Строка паркуется причиной «слияние: DK-X» или «закрытие: DK-X». Оба разряда
машинные, ID предпосылки разбирается из причины.

Обход ждущих это один хвост в taskctl. Его зовут `taskctl close` и
`shipctl merge` в конце своей работы, тот же хвост зовёт тик сторожка.
Совпавшая строка выходит из Blocked и поднимается командой `taskctl run`
(DK-931). Правило совпадения живёт в одном месте. Тик добирает событие,
которое close и merge пропустили.

Тик переводит на `taskctl run` подъём вопроса и подъём упавшей сессии из
`resume_failed`. Строка `claude --resume` в watch.py:1008 уходит в профиль
харнеса.

## Границы

Ребро «после» и его взвод идут строками DK-933 и DK-934. Проза в причине
парковки остаётся без подъёма. Разряды «окружение:» и «спор:» не меняются.

## Развилки

- «разряды»: хватит ли двух новых машинных разрядов парковки?
  - решает: исполнитель
  - рекомендация: к четырём разрядам парковки добавить «слияние:» и «закрытие:» с ID предпосылки; сторожок будит их наравне с «вопрос:», проза остаётся молчащей
  - решено исполнителем 2026-09-11: по рекомендации. К четырём разрядам добавлены «слияние: <ID>» и «закрытие: <ID>». ID предпосылки идёт первым словом, дальше можно писать прозу. Парковка отбивает разряд без ID, ожидание самой себя, ID, которого нет ни на доске, ни в архиве, и уже случившееся событие. Слияние сверяется признаком «слита» из internal/merged, закрытие строкой в архиве доски. Проза в причине остаётся без подъёма. Отвергнут один разряд «сосед: <ID>» с выбором события по статусу предпосылки. Поездное слияние оставляет строку предпосылки в In progress, и по статусу событие не различить. Отвергнута сверка слияния статусом строки, её отвергло решение 2 LLD DK-933.
- «обход»: кто обходит ждущих после close и merge?
  - решает: исполнитель
  - рекомендация: обход ждущих это один хвост в taskctl, его зовут close и shipctl merge в конце; тик сторожка зовёт тот же хвост, чтобы правило совпадения лежало в одном месте
  - решено исполнителем 2026-09-11: по рекомендации. Хвост это `cmdWake` в tools/taskctl/wake.go. Правило совпадения одно, `condMet`. Источники ждущих лежат перечнем `waiterSources`, и взвод DK-934 ляжет туда вторым источником. Close зовёт хвост после записи архива и до разлива поезда. `shipctl merge` зовёт `taskctl wake --quiet` в конце одиночного и поездного слияния. Тик зовёт ту же команду по корням, где на доске есть такие строки. Совпавшая строка проходит предполёт `taskhead.Preflight`, выходит в In progress коммитом доски и поднимается лестницей `taskctl run`. Отказ предполёта оставляет её в Blocked. Отвергнута своя копия обхода в shipctl, правило совпадения разошлось бы на первой правке. Отвергнут подъём без предполёта. Снятая впустую парковка оставила бы строку в работе без головы, и тик бы её не увидел.
- «дашборд в тике»: какие вызовы дашборда уходят из watch.py?
  - решает: исполнитель
  - рекомендация: подъём вопроса и упавшей сессии уходят на taskctl, второй круг ревью и подъём прогона сценария остаются на командах дашборда до переноса их правил
  - решено исполнителем 2026-09-11: по рекомендации. Вопрос тик поднимает командой `taskctl wake <ID>`. Упавшую сессию окна задачи поднимает `taskctl run <ID> --order продолжай`, окну без задачи реплика идёт в панель, как прежде. Команду продолжения в зове человеку тик берёт из профиля харнеса. Вызовы `dashboard round` и `dashboard check` остаются. У них свои правила отбора строк, ярус по роли ревью и проверка прав машинного контура, и живёт это в дашборде. Эти команды переводит на общий код запуска DK-935, а перенос их правил в taskctl это отдельная строка. Отвергнут перенос обоих подъёмов в этой задаче. Он тянет в taskctl выбор яруса и правила прогона сценария и в цену M не ложится.

## DoD

- `taskctl move <ID> blocked --reason "слияние: DK-X"` и
  `--reason "закрытие: DK-X"` проходят. Разряд без ID даёт отказ.
- `shipctl merge DK-X` в конце поднимает строку, припаркованную на
  «слияние: DK-X». `taskctl close DK-X` поднимает строку на «закрытие: DK-X».
- Тик сторожка поднимает такую строку, когда событие прошло мимо close и
  merge.
- Тик поднимает вопрос и упавшую сессию через `taskctl run`, вызовов
  `dashboard wake` в watch.py не остаётся. Второй круг ревью и подъём прогона
  сценария остаются на командах дашборда (развилка «дашборд в тике»).
- Подъём виден строкой в `.devkit/log` с разрядом и ID предпосылки.

## Ранг

`50+8+3+0+4 = 65`, P1.

- Серьёзность 50: строка, ждущая соседа, стоит до руки человека. Так стояла
  DK-863 при закрытой DK-888.
- Ценность 8: цепочка задач проходит стык без человека.
- Неопределённость 3: хвост пишется на go, зовут его taskctl, shipctl и
  python-сторожок.
- Поправка на баг 0: новые разряды и обход.
- Рычаг 4: на обход опираются взвод ребра «после» и стенд цели.

Цена M. Два разряда, хвост обхода и перевод тика ложатся в одну сессию.

## Приёмка

- вид: agent
- барьер «событие» рассмотрен. Слияние и закрытие делаются на временном
  репозитории, пропущенное событие подделывается правкой доски мимо close

## Ход работы

- Вычитка: 1 файл, 3 правки, 0 пометок, 2026-09-11.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 49%, week_max 32%, снимок 21м назад, сдвига нет), 2026-09-11 16:58-17:00.
- Вычитка: 6 файлов, 5 правок, 2 пометки, 2026-09-11.
- Вычитка: 1 файл, 2 правки, 0 пометок, 2026-09-11.

## Ревью

Уровень 3 до 51f105b: неопределённость 3 по вердикту pick, дифф трогает tools/taskctl и tools/shipctl из critical_paths

- блокирует: taskctl wake поднимает голову без яруса модели. raiseHead в tools/taskctl/wake.go:177-179 зовёт cmdRun с runOpts{hidden: o.hidden}. Model остаётся пустым. Head.Command (internal/taskhead/profile.go:126-135) при пустой модели флаг вовсе не подставляет, и клиент стартует на голом умолчании. У CLI-команды wake (tools/taskctl/main.go, case "wake") нет флага --model. watch.py вызывает её без модели что для вопроса (wake_raise, tid, --push), что для соседа (waiters, --quiet --push). До DK-932 подъём вопроса шёл через dashboard wake. Тот явно спрашивал ярус вердиктом (s.pickTier зовёт agentctl pick, tools/dashboard/wake.go:113-116) перед стартом сессии. DK-932 убрал dashboard wake из тика по DoD, а выбор яруса не перенёс. Теперь каждый агентский подъём стартует без вердикта pick, случайным умолчанием клиента вместо яруса задачи. Тесты модель не проверяют, ни wake_test.go в taskctl, ни watch_test.py. Чем проверить: taskctl wake DK-901 на строке в Blocked с ответом, в логе запуска клиента нет флага модели, ярус не совпадает с agentctl pick DK-901. Починка: raiseHead или cmdWake зовут agentctl pick, как звал pickTier, и передают ярус в runOpts.model; либо wake получает свой флаг --model.: исправлено, модель спрашивается у agentctl pick --record (с --goal по строке «Цель:» файла задачи) в taskhead.Raise перед стартом нового клиента через Request.Pick, его ставит runRequest, так что модель получают run, wake из close, merge и тика; явный --model сильнее вердикта, отказ или agentctl вне PATH дают старт без модели и строку в .devkit/log; e45666fa, ad65de6a, 33eb1858

## Сценарий проверки

Агентский. Шаги гоняются из корня дерева, в PATH стоит taskctl, собранный из
ветки. `taskctl rehearse DK-932` прогоняет их в свежем дереве.

1. Предполёт подъёма отказывает без клиента, оболочки и секции `[head]` и не
   трогает ни замка, ни tmux. Ждём `ok` пакета taskhead.

```sh
GOWORK=off go -C internal test -count=1 -run TestPreflight ./taskhead/
```

2. Разряды, подъём по close, подъём по слиянию на временном репозитории,
   пропущенное закрытие, отказ предполёта, вопрос по ID и строка цели. Ждём
   `ok` пакета taskctl.

```sh
GOWORK=off go -C tools/taskctl test -count=1 -run 'TestParkWaitClasses|TestCloseWakesParkedOnClose|TestCloseWithoutWaitersIsQuiet|TestWakeRaisesOnMerge|TestWakeCatchesMissedClose|TestWakePreflightKeepsRowParked|TestWakeNamedQuestion|TestWakeGoalRowOnlyUnparks|TestWakeQuietAndBrokenReason' ./...
```

3. `shipctl merge` зовёт обход после перевода в Check и несёт его слова в
   отчёт, отказ обхода слияние не роняет. Ждём `ok` пакета shipctl.

```sh
GOWORK=off go -C tools/shipctl test -count=1 -run 'TestMergeCallsWake|TestMergeReportsWake|TestMergeWakeFailureIsANote|TestMergeHappyPath' ./...
```

4. Тик зовёт обход по корню с ждущими, будит вопрос командой `taskctl wake` и
   поднимает упавшую сессию окна задачи через `taskctl run`. Ждём `OK`.

```sh
cd tools/devkitctl && python3 -m unittest watch_test.WaitersTest watch_test.WakeTest watch_test.ResumeTest
```

5. Собранный taskctl отбивает разряд без ID. Ждём строку «разряд «слияние:»
   ждёт ID предпосылки» и `код 1`.

```sh
d=$(mktemp -d) && cp -R tools/obeycheck/testdata/project/. "$d" && printf '# OB-002\n' > "$d/docs/tasks/OB-002.md" && DEVKIT_NOTIFY_OFF=1 taskctl -C "$d" move OB-002 blocked --reason "слияние: потом"; echo "код $?"
```

6. Пропущенное закрытие добирает `taskctl wake`. Строку предпосылки увозят в
   архив мимо close, дерева devkit нет, и предполёт оставляет ждущую в
   Blocked. Ждём строку «OB-002: ждала «закрытие: OB-001», OB-001 закрыта, а
   голову поднять нечем», итог «событие случилось у 1, разбужено 0» и `код 1`.

```sh
d=$(mktemp -d) && cp -R tools/obeycheck/testdata/project/. "$d" && printf '# OB-002\n' > "$d/docs/tasks/OB-002.md" && DEVKIT_NOTIFY_OFF=1 taskctl -C "$d" move OB-002 blocked --reason "закрытие: OB-001" >/dev/null && printf '| OB-001 | Сосед | bug | P2 | 2026-09-11 | - |\n' >> "$d/docs/TASKS-archive.md" && DEVKIT_HOME=/nonexistent DEVKIT_NOTIFY_OFF=1 taskctl -C "$d" wake --no-commit; echo "код $?"
```

7. Вызова `dashboard wake` в тике нет, бинарь дашборда ищут только второй круг
   ревью и подъём прогона сценария. Ждём две строки, в `review_round` и
   `check_run`.

```sh
grep -n 'devkit_bin("dashboard")' tools/devkitctl/watch.py
```

8. Подъём без `--model` через run и wake берёт модель вердиктом `agentctl
   pick`, явный `--model` сильнее, отказ вердикта даёт старт без модели и
   строку журнала. Ждём `ok` обоих пакетов.

```sh
GOWORK=off go -C internal test -count=1 -run TestPickOnlyForNewClient ./taskhead/ && GOWORK=off go -C tools/taskctl test -count=1 -run 'TestRunTakesModelFromPick|TestRunModelFlagBeatsPick|TestRunPickRefusalStartsWithoutModel|TestWakeRaisesWithPickedModel' ./...
```

## Проверка


- Стенд: 2026-09-11 18:08, дерево 9b39bbbe, предмет 7de70b54, база старый, ярус mini, k 3, сценарии 34-park-neighbor, зачтён.

```console
$ obeycheck --task DK-932 --for RULES.board.md --only 34-park-neighbor -k 3 --base старый --no-preflight --timeout 90s /private/tmp/claude-501/-Users-rider-projects-devkit/dce382b2-6674-4ab7-a0c3-909b2c0d9d43/scratchpad/rules-with /private/tmp/claude-501/-Users-rider-projects-devkit/dce382b2-6674-4ab7-a0c3-909b2c0d9d43/scratchpad/rules-old
сценарий                                    rules-with  rules-old  вердикт
припарковать задачу, ждущую слияния соседа         3/3        0/3  польза

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  34-park-neighbor / rules-old / повтор 1: проверка: exit status 1 (OB-002 не припаркована разрядом «слияние: OB-001»; | OB-002 | README называет утилиту старым именем | task | P2 | 35 (25+4+1+0+3, S+2) | S | - |)
```

- Обкатка: 2026-09-11 18:41, свежее дерево 44c257677a32, сценарий 165b0293, временный HOME, утилит дерева 11, шагов 8, все зелёные.

Шаг 1 (зелёный):

```console
$ GOWORK=off go -C internal test -count=1 -run TestPreflight ./taskhead/
ok  	github.com/dronrider/devkit/internal/taskhead	0.637s
```

Шаг 2 (зелёный):

```console
$ GOWORK=off go -C tools/taskctl test -count=1 -run 'TestParkWaitClasses|TestCloseWakesParkedOnClose|TestCloseWithoutWaitersIsQuiet|TestWakeRaisesOnMerge|TestWakeCatchesMissedClose|TestWakePreflightKeepsRowParked|TestWakeNamedQuestion|TestWakeGoalRowOnlyUnparks|TestWakeQuietAndBrokenReason' ./...
ok  	github.com/dronrider/devkit/tools/taskctl	2.485s
```

Шаг 3 (зелёный):

```console
$ GOWORK=off go -C tools/shipctl test -count=1 -run 'TestMergeCallsWake|TestMergeReportsWake|TestMergeWakeFailureIsANote|TestMergeHappyPath' ./...
ok  	github.com/dronrider/devkit/tools/shipctl	4.368s
```

Шаг 4 (зелёный):

```console
$ cd tools/devkitctl && python3 -m unittest watch_test.WaitersTest watch_test.WakeTest watch_test.ResumeTest
.........................................
----------------------------------------------------------------------
Ran 41 tests in 0.131s

OK
```

Шаг 5 (зелёный):

```console
$ d=$(mktemp -d) && cp -R tools/obeycheck/testdata/project/. "$d" && printf '# OB-002\n' > "$d/docs/tasks/OB-002.md" && DEVKIT_NOTIFY_OFF=1 taskctl -C "$d" move OB-002 blocked --reason "слияние: потом"; echo "код $?"
ошибка: разряд «слияние:» ждёт ID предпосылки первым словом («слияние: DK-123 ...»): по нему обход ждущих узнаёт событие
код 1
```

Шаг 6 (зелёный):

```console
$ d=$(mktemp -d) && cp -R tools/obeycheck/testdata/project/. "$d" && printf '# OB-002\n' > "$d/docs/tasks/OB-002.md" && DEVKIT_NOTIFY_OFF=1 taskctl -C "$d" move OB-002 blocked --reason "закрытие: OB-001" >/dev/null && printf '| OB-001 | Сосед | bug | P2 | 2026-09-11 | - |\n' >> "$d/docs/TASKS-archive.md" && DEVKIT_HOME=/nonexistent DEVKIT_NOTIFY_OFF=1 taskctl -C "$d" wake --no-commit; echo "код $?"
OB-002: ждала «закрытие: OB-001», OB-001 закрыта, а голову поднять нечем, строка стоит в blocked: дерево devkit с kit/skills/board-task/task-run.py не нашлось ни в DEVKIT_HOME, ни в .devkit/devkit проекта, ни рядом с ним, ни в ~/projects/devkit; указать: DEVKIT_HOME=<путь к devkit>
обход ждущих: ждут событием 1, событие случилось у 1, разбужено 0
код 1
```

Шаг 7 (зелёный):

```console
$ grep -n 'devkit_bin("dashboard")' tools/devkitctl/watch.py
1199:    bin = devkit_bin("dashboard") if dashboard is None else dashboard
1492:    bin = devkit_bin("dashboard") if dashboard is None else dashboard
```

Шаг 8 (зелёный):

```console
$ GOWORK=off go -C internal test -count=1 -run TestPickOnlyForNewClient ./taskhead/ && GOWORK=off go -C tools/taskctl test -count=1 -run 'TestRunTakesModelFromPick|TestRunModelFlagBeatsPick|TestRunPickRefusalStartsWithoutModel|TestWakeRaisesWithPickedModel' ./...
ok  	github.com/dronrider/devkit/internal/taskhead	3.550s
ok  	github.com/dronrider/devkit/tools/taskctl	4.079s
```
