# DK-933: LLD: взвод ребра «после», уровни цепочки и выстрел по слиянию кода

Цель: [tasks/DK-902.md](tasks/DK-902.md)

## Что происходит

2026-09-11, нарезка цели DK-902. Цель поглотила черновики DK-721 и DK-794
припиской к своему файлу, вопросы взвода лежат там целиком. Ребро «после»
ставит `taskctl dep add`, читают его `move`, `batch`, `slot` и lint.
`taskctl close` снимает маркер с зависимых строк циклом в
tools/taskctl/ops.go:930. Везде ребро значит «предпосылка в архиве».
Освобождённая строка остаётся в Backlog до руки человека. Взвода и автозапуска
нет.

DK-794 показала второй конец. Предпосылка стоит в Check со смешанной
приёмкой, её код слит и выкачен. Зависимая строка при этом ждёт взгляда
человека на чужую задачу. `trainTasks` в tools/shipctl/ops.go:667 уже считает
признак «код слит», но к рёбрам он не подключён. DK-538 ждёт часового условия
того же механизма.

## Чего хотим

LLD решает вопросы черновика DK-721 и выбирает лучший вариант сам,
отвергнутые названы с причиной. Решений пять. Где живёт взвод и чем
взведённая строка отличается от заглохшей. Чем стреляет ребро, слиянием кода
предпосылки или закрытием строки. Что гасит взвод: возврат предпосылки из
Check, откат на проде, удаление строки. Как уровни цепочки ложатся на рёбра,
одинаковый номер значит «вместе». Как часовое условие DK-538 встаёт вторым
видом того же взвода. Развилку DK-238 про порядок исполнения поверх ранга LLD
решает явно.

LLD пишет нарезку механики под DK-934. Второй и третий заезды DK-721 (скилл
цепочки и диалог дашборда) она заводит строками Backlog вне цели.

## Границы

Код идёт строкой DK-934. Обход ждущих и замок подъёма решают DK-932 и
DK-931, LLD опирается на них и их не переделывает.

## Развилки

- «взвод»: чем стреляет и чем гаснет взвод ребра «после»?
  - решает: исполнитель
  - рекомендация: ребро «после» стреляет по слиянию кода предпосылки, а не по закрытию строки; уровни цепочки это номер, одинаковый номер значит вместе; возврат предпосылки из Check и откат гасят взвод, и это первым решает LLD по DK-721
  - решено исполнителем 2026-09-11: решено в LLD docs/lld/DK-933-armed-edge.md: ребро снимается слиянием предпосылки (решение 2), взвод это суффикс [взвод] строки Backlog (решение 1), провал и откат держат взвод до нового слияния, а возврат из Check с замечаниями не держит (решение 3), уровни это синтаксис taskctl chain (решение 4)
- «кейсы»: какие пользовательские кейсы у взвода ребра «после»?
  - решает: исполнитель
  - рекомендация: цепочка вне цели идёт ночью без человека по просьбе «после DK-A сделай DK-B и DK-C, потом DK-D»; зависимая стартует, когда код предпосылки со смешанной приёмкой слит, не дожидаясь взгляда человека на чужую задачу; откат или провал предпосылки не пускают зависимую на откаченное основание; отложенный прогон стартует сам в льготный час; по доске видно, какая строка стартует сама, а какая ждёт руки человека; отказ подъёма приходит зовом с причиной, а не молчанием
  - решено исполнителем 2026-09-11: шесть кейсов рекомендации взяты из DK-721, DK-794 и DK-538, каждый закрыт решением LLD docs/lld/DK-933-armed-edge.md

## DoD

- Документ `docs/lld/DK-933-*.md` решает пять вопросов и развилку DK-238. У
  каждого решения названы отвергнутые варианты с причиной.
- Нарезка под DK-934 лежит в документе, второй и третий заезды заведены
  строками или черновиками.
- Решения документа попадают в карту проекта перегенерацией
  `devkitctl doctor --fix`.

## Ранг

`50+8+3+0+4 = 65`, P1.

- Серьёзность 50: цепочка задач вне цели ждёт человека на каждом стыке, ночью
  стык стоит до утра.
- Ценность 8: цепочка задач идёт ночью без человека.
- Неопределённость 3: подход рекомендован развилкой «взвод» в файле цели,
  открыты хранение взвода, гашение и уровни.
- Поправка на баг 0: проектирование.
- Рычаг 4: на взвод встанет DK-538 и любая цепочка на доске.

Цена M. Документ ложится в одну сессию, кода нет.

## Приёмка

- вид: agent
- барьер «согласие» рассмотрен. Развилку «взвод» человек оставил
  исполнителю на постановке цели, решения LLD принимает ревью дизайна в
  документе

## Ход работы

- Вычитка: 1 файл, 2 правки, 0 пометок, 2026-09-11.
- Разработка: субагент opus/xhigh по вердикту pick (квота: week_all 43%, week_max 31%, снимок 2м назад, сдвига нет), 2026-09-11 12:50-12:50.
- LLD записан: [docs/lld/DK-933-armed-edge.md](../lld/DK-933-armed-edge.md).
  Карту проекта перегенерировал `devkitctl map --write`. Доктор с `--fix`
  трогает службы launchd машины, и из дерева задачи его не зовут.

## Ревью

Уровень 3 до e935ecf: LLD, стартовый уровень ревью дизайна; неопределённость 3; решение меняет смысл ребра «после» у всех читателей доски: taskctl, shipctl, agentctl, дашборд

- Вердикт: без замечаний. Принято с правками ревьювера 9e59ba21. Сверил семь решений с кодом (ворота move, dep add, lintDeps, openDeps, codeCommits, вид приёмки), с RULES.board.md п. 6-7 раздела «Ветки, ревью и деплой», ACCEPTANCE.md и стыками DK-930, DK-931, DK-932, DK-934, DK-936, DK-538, DK-794. Отход от рекомендации цели в решении 3 держится на п. 7. В документе решил судьбу начатой зависимой при откате и возврате предпосылки, два слоя признака «слита», chain отдельной строкой вне цели и правку постановки DK-930. Карта сходится с map --write, шаги 1, 3 и 4 сценария прогнаны

## Сценарий проверки

Агентский. Шаги читают документ и карту в свежем дереве, прод они не трогают.

1. В документе семь решений, и у каждого свой список отвергнутого.

```sh
test "$(grep -c '^## Решение [1-7]\. ' docs/lld/DK-933-armed-edge.md)" -eq 7 && test "$(grep -c '^Отвергнутое\.$' docs/lld/DK-933-armed-edge.md)" -eq 7
```

Ожидание: код возврата 0.

2. Решения лежат в карте проекта, и карта совпадает с перегенерацией.

```sh
test "$(grep -c '^DK-933\.[1-7] ' docs/map.md)" -eq 7 && python3 tools/devkitctl/devkitctl.py map -C . --write && git diff --exit-code docs/map.md
```

Ожидание: код возврата 0, `git diff` пустой. Перегенерация идёт записью в
файл. Вывод `map` в stdout длиннее записанной карты на пустую строку в конце.

3. Нарезка называет новую строку, суженную DK-934, команду chain, оба заезда
   вне цели и правки DK-930 и DK-538.

```sh
test "$(grep -c '^### \(Ребро «после» снимается слиянием предпосылки\|DK-934: что меняется в постановке\|Команда chain строит цепочку уровнями\|Агент в чате взводит цепочку задач по просьбе человека\|Диалог цепочки на дашборде: список задач и номера уровней\|DK-930: что меняется в постановке\|DK-538: что меняется в постановке\)$' docs/lld/DK-933-armed-edge.md)" -eq 7
```

Ожидание: код возврата 0.

4. В документе и файле задачи только символы клавиатуры en/ru.

```sh
python3 hooks/check-symbols.py docs/lld/DK-933-armed-edge.md docs/tasks/DK-933.md
```

Ожидание: код возврата 0.

## Проверка

- Обкатка: 2026-09-11 14:07, свежее дерево 6a759083ada5, сценарий e44af337, временный HOME, утилит дерева 11, шагов 4, все зелёные.

Шаг 1 (зелёный):

```console
$ test "$(grep -c '^## Решение [1-7]\. ' docs/lld/DK-933-armed-edge.md)" -eq 7 && test "$(grep -c '^Отвергнутое\.$' docs/lld/DK-933-armed-edge.md)" -eq 7

```

Шаг 2 (зелёный):

```console
$ test "$(grep -c '^DK-933\.[1-7] ' docs/map.md)" -eq 7 && python3 tools/devkitctl/devkitctl.py map -C . --write && git diff --exit-code docs/map.md
Карта записана: /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/taskctl-rehearse-624827505/tree/docs/map.md
```

Шаг 3 (зелёный):

```console
$ test "$(grep -c '^### \(Ребро «после» снимается слиянием предпосылки\|DK-934: что меняется в постановке\|Агент в чате взводит цепочку задач по просьбе человека\|Диалог цепочки на дашборде: список задач и номера уровней\|DK-538: что меняется в постановке\)$' docs/lld/DK-933-armed-edge.md)" -eq 5

```

Шаг 4 (зелёный):

```console
$ python3 hooks/check-symbols.py docs/lld/DK-933-armed-edge.md docs/tasks/DK-933.md

```
