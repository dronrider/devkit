# DK-934: Взведённая строка стартует сама по слиянию предпосылки

Цель: [tasks/DK-902.md](tasks/DK-902.md)

## Что происходит

2026-09-11, нарезка цели DK-902. Ребро «после» снимается только закрытием
предпосылки, и освобождённая строка ждёт в Backlog руки человека. LLD DK-933
решила устройство взвода и сузила эту строку до него. Смысл ребра ушёл строкой
DK-943, команда цепочки ушла строкой DK-940 вне цели.

## Чего хотим

`taskctl` взводит строку на её предпосылки, и взведённая строка стартует сама.
Ребро снимается так, как задала строка DK-943. Строку поднимает `taskctl run`
из DK-931. Состав берётся из решений LLD DK-933.

- Суффикс `[взвод]`, команды `arm` и `arm --off`. `arm` отказывает строке с
  человеческой развилкой, с неопределённостью 4-5 или ценой XL и строке
  незакрытой цели (решение 1).
- Вид ждущих «взвод» в хвосте обхода DK-932 с воротами ёмкости и подъёмом
  через `taskctl run` (решение 5).
- Файл отказа `.devkit/arm/<ID>`, зов один раз на смену причины, строка
  попытки в `.devkit/log`, каталог в .gitignore.
- Три состояния в `list` и `show`, поле взвода в `dep list --json`.
- `move` из Backlog снимает `[взвод]`. Lint находит `[взвод]` вне Backlog и на
  строке незакрытой цели, хвост обхода такую строку пропускает.

Отказ подъёма из-за квоты или потолка деревьев приходит уведомлением.

## Границы

Команда `chain` идёт строкой DK-940 вне цели. Скилл цепочки и диалог
дашборда идут после неё строками DK-941 и DK-942. Часовое условие остаётся
строкой DK-538 и встаёт на механизм после этой.

## DoD

- Взведённая строка стартует сама после слияния кода всех предпосылок. Строка
  уровня N стартует после всех строк уровня N-1.
- Откат и провал предпосылки держат взведённую строку до нового слияния.
  Возврат предпосылки из Check с замечаниями её не держит (решение 3).
- Взведённая строка отличается от заглохшей в `taskctl list`, сторожок её
  брошенной не считает.
- Тест на временном репозитории. Цепочка из трёх строк на двух уровнях
  стартует по слияниям, уровни ставятся рёбрами `dep add` и взводом `arm`.

## Ранг

`50+8+3+0+4 = 65`, P1.

- Серьёзность 50: цепочка вне цели ждёт человека на каждом стыке.
- Ценность 8: машина работает ночью цепочкой без ручного подъёма.
- Неопределённость 3: правка идёт через taskctl, shipctl и python-сторожок,
  решения берутся из LLD.
- Поправка на баг 0: новая механика.
- Рычаг 4: на взвод встанут DK-538 и живой замер цели.

Цена M. Смысл ребра и команда цепочки ушли строками DK-943 и DK-940.

## Приёмка

- вид: agent
- барьер «событие» рассмотрен. Слияния предпосылок делаются на временном
  репозитории, откат подделывается записью провала

## Ход работы

- Вычитка: 1 файл, 3 правки, 0 пометок, 2026-09-11.
- Вычитка: 1 файл, 0 правок, 0 пометок, 2026-09-11.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 59%, week_max 35% профицит, снимок 43м назад, сдвига нет), 2026-09-12 12:33-12:33.
- Вычитка: 4 файла, 15 правок, 0 пометок, 2026-09-12.

## Ревью

Уровень 3 до 85919e5: неопределённость 3 из ранга, тронут tools/taskctl (критичный путь), дифф трогает старт строк по DoD DK-902

- Вердикт: без замечаний. устройство взвода сверено с LLD DK-933 (решения 1, 3, 5): суффикс [взвод] на своё место в splitTitle/joinTitle (6 значений, все вызовы обновлены), armReadiness и lintArmed повторяют ворота арма, снятие ребра решением 2 не тронуто. Прогнаны go test ./... в tools/taskctl (полный пакет), python3 -m unittest watch_test.WaitersTest, go test дашборда (TestNotificationsArmIsTaskEvent), живой сценарий из файла задачи на временной доске (взвод: ждёт -> взвод: готова по слиянию). Утечки в .gitignore и коммитах не нашёл

## Сценарий проверки

Агентская часть, из корня чекаута.

```sh
cd tools/taskctl && GOWORK=off go test -count=1 -run "TestArm|TestListTellsArmedFromIdle|TestMoveOutOfBacklogDisarms|TestLintFindsArmWhereItDoesNotBelong" ./...
```

Ожидается `ok github.com/dronrider/devkit/tools/taskctl`. Девять тестов взвода
идут на временном репозитории. Цепочка из трёх строк на двух уровнях стартует
по слияниям, откат и провал предпосылки держат взведённую строку, возврат
предпосылки из Check не держит, а ворота ёмкости зовут человека один раз на
причину.

```sh
cd tools/devkitctl && python3 -m unittest watch_test.WaitersTest
```

Ожидается `Ran 8 tests` и `OK`. Тик зовёт обход по доске со взведённой строкой
Backlog и молчит по доске, где взвода нет.

Живой круг на временной доске. Каждый шаг обкатка гонит своим шеллом. Путь
стенда шаг выводит у себя, а бинарь берёт из корня дерева.

```sh
cd tools/taskctl && GOWORK=off go build -o ../../taskctl-arm .
```

Ожидается код 0 и бинарь `taskctl-arm` в корне дерева. Его берут два следующих
шага.

```sh
S=${TMPDIR:-/tmp}/dk934-arm; set -e; rm -rf "$S"; mkdir -p "$S"; git -C "$S" init -q -b main .; ./taskctl-arm -C "$S" init --prefix XR --here; ./taskctl-arm -C "$S" add --title "Предпосылка" --type task --rank "25+2+1+0+2" --cost M --accept agent; ./taskctl-arm -C "$S" add --title "Зависимая" --type task --rank "25+2+1+0+2" --cost M --accept agent; ./taskctl-arm -C "$S" dep add XR-002 XR-001; ./taskctl-arm -C "$S" arm XR-002; git -C "$S" add -A; git -C "$S" -c user.email=t@t -c user.name=t commit -qm seed; ./taskctl-arm -C "$S" list backlog | grep "взвод: ждёт"
```

Последней строкой ожидается пометка `взвод: ждёт` с причиной,
`XR-001 не слита`. Суффикс `[взвод]` стоит в строке доски сразу за
`[после XR-001]`.

```sh
S=${TMPDIR:-/tmp}/dk934-arm; set -e; echo "package x" > "$S/code.go"; git -C "$S" add code.go; git -C "$S" -c user.email=t@t -c user.name=t commit -qm "feat(x): XR-001 правка"; ./taskctl-arm -C "$S" list backlog | grep "взвод: готова"; rm -rf "$S" ./taskctl-arm
```

Ожидается строка `взвод: готова`. Слияние кода предпосылки сняло ребро, и
строка ждёт ближайшего обхода. Последней командой шаг убирает стенд и бинарь.

Пользовательская часть. На ближайшей цепочке вне цели взвести вторую строку
командой `taskctl arm` и утром посмотреть, вышла ли она в In progress по
слиянию первой. След подъёма лежит строкой `wake <ID> взвод` в `.devkit/log`
проекта.

## Выкат

- 2026-09-12 слито: 880a2712, c38ba3a2, 12b004c1, 88c8830c, e1833939, 1f9e5be5, 45b042f2, 5f20393b, 48f14e88

## Проверка

- Исключение: стенд (в RULES.board.md правлен один пункт 4 «Трекинга задач».
  К разбору ребра «после» дописаны две фразы про команду `taskctl arm` и
  суффикс «[взвод]». Шаги агента прежние, ворота взвода стоят в утилите и
  меряются её тестами, а такая дописка не берёт порог повода скилла
  prompt-test)

- Обкатка: 2026-09-12 14:00, свежее дерево 0345d9688842, сценарий 4cccb5f9, временный HOME, утилит дерева 11, шагов 5, все зелёные.

Шаг 1 (зелёный):

```console
$ cd tools/taskctl && GOWORK=off go test -count=1 -run "TestArm|TestListTellsArmedFromIdle|TestMoveOutOfBacklogDisarms|TestLintFindsArmWhereItDoesNotBelong" ./...
ok  	github.com/dronrider/devkit/tools/taskctl	5.566s
```

Шаг 2 (зелёный):

```console
$ cd tools/devkitctl && python3 -m unittest watch_test.WaitersTest
........
----------------------------------------------------------------------
Ran 8 tests in 0.031s

OK
```

Шаг 3 (зелёный):

```console
$ cd tools/taskctl && GOWORK=off go build -o ../../taskctl-arm .

```

Шаг 4 (зелёный):

```console
$ S=${TMPDIR:-/tmp}/dk934-arm; set -e; rm -rf "$S"; mkdir -p "$S"; git -C "$S" init -q -b main .; ./taskctl-arm -C "$S" init --prefix XR --here; ./taskctl-arm -C "$S" add --title "Предпосылка" --type task --rank "25+2+1+0+2" --cost M --accept agent; ./taskctl-arm -C "$S" add --title "Зависимая" --type task --rank "25+2+1+0+2" --cost M --accept agent; ./taskctl-arm -C "$S" dep add XR-002 XR-001; ./taskctl-arm -C "$S" arm XR-002; git -C "$S" add -A; git -C "$S" -c user.email=t@t -c user.name=t commit -qm seed; ./taskctl-arm -C "$S" list backlog | grep "взвод: ждёт"
доска создана в /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk934-arm: docs/TASKS.md, docs/TASKS-archive.md, docs/tasks/
XR-001 заведена в backlog: P2, R=30
XR-002 заведена в backlog: P2, R=30
XR-002: после XR-001
XR-002: взвод стоит, ждёт признак «слита» у XR-001 не спрошен: в /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk934-arm не нашёл ветку main или master
  взвод: ждёт, XR-001 не слита: в main нет её коммитов кроме доски и файлов задач, строка не двигалась 0 дней
```

Шаг 5 (зелёный):

```console
$ S=${TMPDIR:-/tmp}/dk934-arm; set -e; echo "package x" > "$S/code.go"; git -C "$S" add code.go; git -C "$S" -c user.email=t@t -c user.name=t commit -qm "feat(x): XR-001 правка"; ./taskctl-arm -C "$S" list backlog | grep "взвод: готова"; rm -rf "$S" ./taskctl-arm
  взвод: готова, строка не двигалась 0 дней
```
