# DK-936: Стенд трёх ожиданий на временном репозитории

Цель: [tasks/DK-902.md](tasks/DK-902.md)

## Что происходит

2026-09-11, нарезка цели DK-902. Первый пункт DoD цели требует стенда на
временном репозитории, который показывает три ожидания. Голова с живым
ожиданием переживает воронку оболочки. `taskctl close` и `shipctl merge`
поднимают припаркованных с машинным разрядом и взведённые рёбра «после». Тик
сторожка добирает пропущенное событие. Каждая строка цели везёт свои тесты,
а сквозного прогона через все утилиты сразу нет.

## Чего хотим

Один скрипт заводит временный репозиторий с доской и тремя строками,
подменяет клиента харнеса и tmux и гоняет три ожидания подряд. На каждое
ожидание скрипт печатает строку исхода и кончается кодом возврата. Прогон DoD
цели зовёт его одной командой.

## Границы

Живой замер цели ночью стендом не проверяется, его подтверждает прогон DoD
по `.devkit/log`. Стенд не трогает живую доску, `~/.devkit` и службы машины.

## DoD

- Скрипт проходит три ожидания на временном репозитории и кончается кодом 0.
  Каждое ожидание печатает строку исхода.
- Скрипт кончается ненулевым кодом, когда любое ожидание не снялось.
  Проверяется откатом одного звена.
- Стенд подменяет HOME и не трогает службы launchd машины.
- Команда стенда названа в README утилиты, рядом с которой он лежит, и в
  разделе «DoD» цели.

## Ранг

`25+6+3+0+4 = 38`, P2.

- Серьёзность 25: без стенда поломку ожидания находит ночной стоп. Обход
  есть, это тесты каждой строки по отдельности.
- Ценность 6: регресс ожиданий ловится до выката.
- Неопределённость 3: стенд идёт через go-утилиты, python-оболочку, git и
  подменный tmux.
- Поправка на баг 0: новый стенд.
- Рычаг 4: стенд делает дешевле каждую следующую правку ожиданий.

Цена M.

## Приёмка

- вид: agent
- барьер «событие» рассмотрен. События делаются на временном репозитории, тик
  зовётся напрямую

## Ход работы

- Вычитка: 1 файл, 1 правка, 0 пометок, 2026-09-11.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 61%, week_max 37% профицит, снимок 8м назад, сдвига нет), 2026-09-12 14:14-14:14.

## Сценарий проверки

1. Стенд гоняется одной командой и проходит три ожидания подряд. В выводе три
   строки исхода («ожидание «живое ожидание против воронки»: ок» и соседние),
   последняя строка «три ожидания прошли», код возврата 0. Прогон занимает
   около сорока секунд, временный проект и дом стенд убирает за собой.
2. Откат любого звена красит стенд. Тесты откатывают три: голова кончает ход
   без отметки `agentctl wait`, строку не взводят, тик молчит. Каждый прогон
   обязан выйти кодом 1 и назвать сломанное ожидание. Четыре теста класса идут
   около двух минут, часть времени уходит на сборку бинарей копии devkit.

```sh
python3 tools/devkitctl/devkitctl.py waitcheck
```

```sh
cd tools/devkitctl && GOWORK=off python3 -m unittest waitcheck_test
```

Ожидаемый итог: обе команды кончаются нулём, стенд называет три снятых
ожидания, а тесты откатов проходят все четыре.

## Проверка

- Обкатка: 2026-09-12 14:57, свежее дерево c8c44a6d073f, сценарий 43f2e32c, временный HOME, утилит дерева 11, шагов 2, все зелёные.

Шаг 1 (зелёный):

```console
$ python3 tools/devkitctl/devkitctl.py waitcheck
стенд трёх ожиданий: временный проект в /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/devkit-waitcheck-2uq17m2x/proj
  проект: ок
  ожидание «живое ожидание против воронки»: ок (проходов 5, из них ожиданий 4, оболочка вышла нулём)
  ожидание «close и merge поднимают ждущих»: ок (слияние WT-002 подняло WT-004, закрытие подняло WT-003)
  ожидание «тик добирает пропущенное»: ок (тик добрал слияние WT-005 руками и поднял WT-006)
три ожидания прошли: конвейер снимается с каждого
```

Шаг 2 (зелёный):

```console
$ cd tools/devkitctl && GOWORK=off python3 -m unittest waitcheck_test
....
----------------------------------------------------------------------
Ran 4 tests in 117.747s

OK
```
