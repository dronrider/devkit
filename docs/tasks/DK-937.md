# DK-937: Интервью обходит проект и возражает до первого вопроса

## Что происходит

Разобрал 2026-09-11 заходы скилла interview на рабочих задачах. DK-916 к
этому дню слита. Кейсы идут первым раундом, раунд идёт ветками, внутренние
развилки уходят исполнителю. Текст скилла держит порядок раунда. Перечень,
край, пачка по четыре, правило останова. Развилки агент берёт из текста
записи и из файла цели. Шаг 3 «Раунда» велит достать факт из кода до
вопроса. Про самостоятельный поиск проблем вокруг записи в скилле ни слова.
Про чтение смежного кода и доки проекта тоже. Про возражение человеку тоже.
Ни один из заходов не принёс развилку, которой в записи не было. Ни один не
сказал человеку «так делать не надо» с доводом.

Скилл спрашивает то, что видно в тексте черновика. Человек отвечает на
вопросы, ответ на которые он же и написал. Специфика проекта целиком в
разговор не попадает. Это соседние утилиты, принятые в LLD решения, обход,
который уже есть. Всё это всплывает на ревью или на прогоне, когда правка
уже сделана. Информацию из человека скилл не выжимает. Нет встречного
вопроса «а что будет, если». Нет проверки идеи на прочность. Нет попытки
найти, чем идея ломает уже работающее. Постановка на выходе той же глубины,
что сам черновик.

### Что снял разбор

Нехватку сверил 2026-09-11 чтением `kit/skills/interview/SKILL.md`. Шаг 2
«Раунда» уже тянет развилки «из кода и с доски». Сама запись при этом
называет тему. Поиска рисков вокруг записи в тексте нет. Своего варианта
решения и возражения человеку тоже нет. Рядом живёт несогласный критик, шаг 11
скилла `board-groom`. Критик привлекает субагента со свежим контекстом,
который оспаривает решение. Зовётся он только на черновике, записанном
готовым решением, и при цене от M. Про перечень развилок в этом шаге ничего не
сказано, находки читает человек.
DK-916 стоит в Check. Живой замер доли ответов «по рекомендации» там ещё не
записан. Дублей на доске, в архиве и в накопителе не нашёл. DK-928, DK-892 и
DK-844 трогают interview с другой стороны.

## Чего хотим

Перечень развилок после интервью содержит развилки, которых в записи не было.
Их агент нашёл сам в коде, доке и на доске проекта. Человек в разговоре
слышит возражения с доводами и встречные вопросы про кейсы, о которых он не
написал. Он отвечает на них вместо пересказа своего черновика. Проблемы,
которые сегодня всплывают на ревью, всплывают на постановке.

## Границы

Нарезка цели и порядок LLD в работу не входят. Кейсы названы только для
груминга и постановки цели. Старт ждёт закрытия DK-916 с её живым замером.
Иначе две правки одного скилла смешаются в одной мере. DK-917 заводит в форме
развилки подстроку «после:», эта задача заводит «источник:». Форма у них
общая, и вторая из двух сверяется с первой.

Условий, посильных только человеку, кроме живых разборов для замера, не
нашёл. Искал в доступах, секретах, шагах наружу и в судье стенда. Проверка
стенда машинная по подстроке «источник», судье примеры от человека не нужны.

## Развилки

Как выглядит роль агента в разговоре, устроенном как архитектурное
собеседование. Собеседуемый тут агент, интервьюер человек. Что скилл требует
от агента до первого вопроса человеку. Обход кода и доки проекта по теме
записи, список найденных рисков и стыков, свой вариант решения с доводами
против записанного. Чем считать, что агент бросил вызов идее. Какой след
этого остаётся в перечне. Развилка, заведённая агентом из кода, возражение с
доводом. Как это меряется на стенде obeycheck и на живых разборах рядом с
мерой DK-916, долей ответов «по рекомендации».

- «кейсы»: какие пользовательские кейсы у DK-937? поправьте список, добавьте пропущенный, снимите лишний
  - решает: человек
  - рекомендация: четыре кейса: (1) до первого вопроса агент обходит код, доку и доску вокруг предмета и приносит развилки, которых в записи нет (соседние утилиты, решения LLD, готовый обход); (2) агент возражает записанной идее с доводом и кладёт свой вариант рядом; (3) агент задаёт встречный вопрос «а что будет, если» про кейс, о котором человек не написал; (4) проблема, которую сегодня находит ревью или прогон, всплывает на постановке, и в перечне видно, какую развилку агент нашёл сам
  - вариант: те же четыре, плюс пятый: всё это и на нарезке цели, и в порядке LLD, а не только на груминге и постановке цели
  - решено человеком 2026-09-11: четыре кейса: (1) до первого вопроса агент обходит код, доку и доску вокруг предмета и приносит развилки, которых в записи нет (соседние утилиты, решения LLD, готовый обход); (2) агент возражает записанной идее с доводом и кладёт свой вариант рядом; (3) агент задаёт встречный вопрос «а что будет, если» про кейс, о котором человек не написал; (4) проблема, которую сегодня находит ревью или прогон, всплывает на постановке, и в перечне видно, какую развилку агент нашёл сам
- «возражение»: кому уходит развилка-возражение агента против записанной идеи?
  - решает: исполнитель
  - рекомендация: возражение записанной идее всегда уходит человеку: ответ меняет саму запись, это уже покрыто критерием «Кто спрашивает» из DK-916, нового правила маршрутизации не нужно
- «стенд»: как стенд obeycheck ловит, что агент нашёл развилку сам и возразил?
  - решает: исполнитель
  - рекомендация: подготовка кладёт черновик, чья идея спорит с кодом стендового репозитория (функция уже есть или решение LLD говорит обратное); проверка ищет в перечне развилку, которой нет в тексте черновика и которая называет конфликт; секция «Судья» смотрит, есть ли в реплике возражение с доводом
  - решено агентом 2026-09-11: проверка машинная: подготовка кладёт черновик, чья идея спорит с кодом стендового репозитория, проверка ищет в перечне развилку с подстрокой «источник», называющую конфликт; секция «Судья» не нужна, подстрока грепается, а судье нужны примеры, размеченные человеком
- «критик»: где живут обход проекта и возражение: один механизм в interview или рядом с критиком груминга?
  - решает: человек
  - рекомендация: перенести несогласного критика из шага 11 board-groom в interview первым шагом до вопросов на груминге и постановке цели: субагент со свежим контекстом обходит код, доку и доску вокруг предмета, находки и возражения ложатся развилками в перечень, триггер «готовое решение и цена от M» снимается, шаг 11 ссылается на interview
  - вариант: отдельный шаг обхода в interview, критик шага 11 остаётся на своём триггере как есть
  - вариант: расширить только критика груминга: зовётся на каждом черновике, находки развилками, постановку цели не трогать
  - решено человеком 2026-09-11: перенести несогласного критика из шага 11 board-groom в interview первым шагом до вопросов на груминге и постановке цели: субагент со свежим контекстом обходит код, доку и доску вокруг предмета, находки и возражения ложатся развилками в перечень, триггер «готовое решение и цена от M» снимается, шаг 11 ссылается на interview
- «след»: какой след в перечне отличает развилку, найденную агентом, от развилки из текста записи?
  - решает: исполнитель
  - рекомендация: у развилки, заведённой агентом из обхода, подстрока «источник: код|дока|доска|возражение» с путём или ID; неизвестная подстрока по TASKFORM это проза при развилке, форма не ломается, живой замер считает такие развилки рядом с долей ответов «по рекомендации»
- «цена обхода»: сколько квоты отдать обходу: на каждом заходе, только на важных записях или без потолка?
  - решает: человек
  - рекомендация: обход на каждом заходе груминга и постановки цели, один субагент на запись, ярус как у ревьювера (agentctl pick --role review); повторный заход по записи без правок обход не повторяет, а берёт развилки с подстрокой «источник» из перечня
  - вариант: обход только у записей с уровнем разбора «высокий» и у целей, остальные разбираются без критика
  - вариант: без ограничений: обход на ярусе сессии на каждом заходе
  - решено человеком 2026-09-11: обход на каждом заходе груминга и постановки цели, один субагент на запись, ярус как у ревьювера (agentctl pick --role review); повторный заход по записи без правок обход не повторяет, а берёт развилки с подстрокой «источник» из перечня
- «после DK-916»: ждать ли закрытия DK-916 с её живым замером до старта DK-937?
  - решает: человек
  - рекомендация: ждать: taskctl dep add ставит DK-937 после DK-916, правка interview стартует, когда у DK-916 записан живой замер доли ответов «по рекомендации» и строка закрыта; иначе две правки одного скилла смешаются в одной мере
  - вариант: не ждать: DK-937 идёт сразу, а живой замер DK-916 пишется по заходам до слияния DK-937 и дальше меряет обе правки вместе
  - решено человеком 2026-09-11: ждать: taskctl dep add ставит DK-937 после DK-916, правка interview стартует, когда у DK-916 записан живой замер доли ответов «по рекомендации» и строка закрыта; иначе две правки одного скилла смешаются в одной мере

## DoD

- В скилле `interview` на груминге и постановке цели первым шагом, до
  первого вопроса, идёт обход. Субагент со свежим контекстом обходит код, доку
  и доску вокруг предмета записи. Ярус берётся из вердикта `agentctl pick
  --role review`.
- Находки обхода ложатся развилками в перечень. Это стыки, риски, готовый
  обход, возражение с доводом и встречный вопрос «а что будет, если». У
  каждой стоит подстрока «источник».
- Шаг 11 скилла `board-groom` больше не держит свой триггер и ссылается на шаг
  обхода в interview.
- Повторный заход по записи без правок не повторяет обход. Он берёт развилки
  с подстрокой «источник» из перечня.
- `TASKFORM.md` описывает подстроку «источник» у развилки.
- Подготовка кладёт черновик, который спорит с кодом стендового репозитория.
  Сценарий стенда obeycheck краснеет, если агент не завёл развилку с
  источником про этот конфликт. Прогон `prompt-test` отмечен в файле задачи.
- На ближайших разборах после слияния записаны число развилок с источником на
  запись и доля ответов «по рекомендации». Обе цифры сверены с мерой DK-916.

## Ранг

`50+8+1+0+4 = 63`, P1.

`50+8+1+0+4 = 63`, P1.

- Серьёзность 50. Постановка идёт, но стыки и готовый обход всплывают на
  ревью или на прогоне, когда правка уже сделана. У человека есть обход, и он
  болезненный. Ревью возвращает готовую правку на переделку.
- Ценность 8. Проблема, пойманная на постановке, не стоит захода разработки и
  захода ревью. Это прямой вклад в автономный конвейер задач.
- Неопределённость 1. Подход выбран ответами про кейсы, критика, цену обхода и
  порядок. Правка живёт в тексте двух скиллов, в форме развилки и в сценарии
  стенда. Границ между процессами и языками нет.
- Поправка на баг 0. Скилл работает так, как написан, речь о новом поведении.
- Рычаг 4. Постановка с найденными стыками удешевляет каждую следующую задачу.

## Приёмка

- вид: mixed
- барьер «событие»: число развилок с источником и долю ответов «по
  рекомендации» видно только на живых разборах, а они случаются в неизвестный
  момент. Стенд с подменой события ловит правило и стоит агентской половиной.
  Ответов живого человека на возражения он не даёт.

## Ход работы

- Из черновика DK-937 «Интервью исследует проект и спорит с постановкой, а не собирает галочки»: записан 2026-09-11, оформлен 2026-09-11.
- Вычитка: 1 файл, 8 правок, 0 пометок, 2026-09-11.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 44%, снимок 43м назад, сдвига нет), 2026-09-11 14:17-14:17.
- Вычитка правки промптов (`kit/skills/interview/SKILL.md`, `kit/skills/board-groom/SKILL.md`, `TASKFORM.md`): 3 файла, 3 правки, 0 пометок, 2026-09-11.

## Сценарий проверки

Агентская часть, из основного чекаута после слияния.

```sh
obeycheck --list | grep -q "^33-groom-walk-source" && echo "сценарий разобран"
```

Ожидание: строка «сценарий разобран». Файл
`tools/obeycheck/scenarios/33-groom-walk-source.md` находится стендом.
Привязка «предмет» сходится с деревом: `kit/skills/interview/SKILL.md`
«Обход до первого вопроса», `kit/skills/board-groom/SKILL.md` «Процедура».

```sh
grep -n "источник" TASKFORM.md kit/skills/interview/SKILL.md kit/skills/board-groom/SKILL.md
```

Ожидание: подстрока «источник» находится во всех трёх файлах. Форма стоит в
`TASKFORM.md` (таблица «Подстрок шесть видов» и абзац под ней). Шаг обхода
стоит в `interview` (раздел «Обход до первого вопроса»). Ссылка на него
стоит в `board-groom` (шаг 11).

Живой прогон стенда двумя раскладками (новый текст `interview` и
`board-groom` против прежней редакции) стоит реальных сессий. Он сделан один
раз прогоном `prompt-test`, след в разделе «Проверка» ниже. Рехёрсал его не
повторяет.

Пользовательская часть. На ближайших после слияния разборах (груминг и постановка цели) посчитать число развилок с подстрокой «источник» на запись и долю ответов «по рекомендации», сверить обе цифры с мерой из `docs/tasks/archive/2026/DK-916.md`.

Оба шага агентской части проверены руками в дереве `dk-937` 2026-09-11: обе
команды дали ожидаемый вывод. `taskctl rehearse` из основного чекаута звать
было нельзя, worktree его отбивает сам, обкатку из основного чекаута делает
диспетчер.

## Проверка

- Стенд: 2026-09-11 17:11, дерево 9e097ee5, предмет ca5fa979, база старый, ярус base, k 3, сценарии 33-groom-walk-source, зачтён.

```console
$ /tmp/obeycheck-dk937 --task DK-937 --no-preflight --tier base --only 33-groom-walk-source -k 3 --base старый /private/tmp/claude-501/-Users-rider-projects-devkit/ebf8083f-2fc7-450e-94ec-da74ccc13dcf/scratchpad/dk937-obey/rules-with /private/tmp/claude-501/-Users-rider-projects-devkit/ebf8083f-2fc7-450e-94ec-da74ccc13dcf/scratchpad/dk937-obey/rules-old
сценарий                                    rules-with  rules-old  вердикт
найти обходом готовую утилиту вместо новой         1/3        0/3  не хуже

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  33-groom-walk-source / rules-with / повтор 1: проверка: exit status 1 (развилки с подстрокой «источник: код|дока|доска|возражение» в разделе «Развилки» нет)
  33-groom-walk-source / rules-old / повтор 1: проверка: exit status 1 (развилки с подстрокой «источник: код|дока|доска|возражение» в разделе «Развилки» нет)
```
