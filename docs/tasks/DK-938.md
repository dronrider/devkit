# DK-938: Кнопка чата у цели открывает мёртвый груминг, реплика не доходит до «Входящих»

## Что происходит

2026-09-11, цель DK-902 поднята кнопкой «Выполнить» в дашборде. Оболочка
goal-run.py поднимает цикл с DEVKIT_HIDDEN=1 по решению DK-847 от 2026-09-07.
Хук старта пишет hidden: true в память диалога сессии 7eaa707c, её tmux
goal-DK-902. Панель чатов просит список ?all=1 без ключа hidden=1. Чат
цикла в список не попадает. Сервер с ключом hidden=1 отдаёт ему state: live.
Кнопка чата на форме задачи фильтрует видимые чаты по DK-902. Находится
один кандидат, вчерашняя сессия груминга ba1e0826 в tmux task-DK-902. У неё
заголовок «Разбор и груминг задачи DK-902» и state: dead.

Сверка с кодом при груминге, 2026-09-11. Фильтр скрытых стоит на сервере,
`tools/dashboard/chats.go:917`, и ключ `keep` его не обходит. Кнопка чата одна
на строку и форму, это `rowChatBtn` в `static/app.js:9486`. Форма передаёт ей
только ID задачи (`app.js:3896`). У строки цели пустое поле `run_chat`.
Работа из tmux цели собирается без сессии (`board.go:432-452`), goal-run.py
не ставит `DEVKIT_TMUX`, и имени окна в реестре нет. Поэтому выбор уходит в
поиск по ID среди видимых чатов (`app.js:7908-7914`). Даже адресный вход по
sid покажет живой чат мёртвым. В списке нет записи, и `chatWay` по умолчанию
берёт `dead` (`app.js:9643`).

Во «Входящие» цели панель сегодня не пишет вовсе. Ручка
`POST /goals/{id}/message` жива, но её вызов снят с фронтенда правкой DK-397
от 2026-08-19 (`3f6a27b2`). `goalMessageURL` с `makeOutbox` остались без
вызовов. README дашборда эту дорогу описывает («реплика уходит ручкой цели,
во «Входящие» её файла»). Это регресс. Реплика в живой чат цикла ушла бы
через `/say` и подняла бы резюм второго агента рядом с идущим витком.

Человек заходит в чат цели с формы и видит чужой заголовок. Над лентой стоит
красная плашка «разговор остановлен», а цикл в это время жив и пишет журнал.
Реплика из этого чата поднимет резюм мёртвой сессии груминга. Во «Входящие»
цели она не доедет, подхват chat-in.py её не увидит. Ответ на вопрос витка
теряется. У дашборда нет входа во «Входящие», а goal-run.py --say пишет
строку в журнал цикла.

## Чего хотим

Кнопка чата у строки и формы цели открывает живой чат цикла. У него свой
заголовок и зелёное состояние. Реплика из него доезжает во «Входящие».

## Границы

Решение DK-847 остаётся в силе, цикл цели из списка чатов не выходит. Условий,
посильных только человеку, не нашёл. Доступов и секретов правка не просит,
у неё нет выката наружу, правил другим строкам она не меняет.

## Развилки

Развилка из двух дорог. По первой адресный вход с формы задачи и со строки цели
не смотрит на признак hidden, а признак остаётся фильтром списка. По второй
цикл цели перестаёт прятаться вовсе, и решение DK-847 пересматривается для
целей. Первая дорога выглядит правильнее. Чистота списка сохраняется, а
адресный переход ведёт в живой чат.

- «кейсы»: Кейсы задачи такие, как в рекомендации. Поправьте, добавьте пропущенный или снимите лишний?
  - решает: человек
  - рекомендация: 1) цикл цели жив, человек жмёт кнопку чата у строки цели или на форме: открывается чат цикла со своим заголовком и зелёным состоянием, а не чужой груминг; 2) реплика из этого чата ложится во «Входящие» цели, идущий виток получает её подхватом chat-in.py, второй агент резюмом не поднимается; 3) цикл не идёт: реплика ждёт во «Входящих» следующего витка (дорога из README дашборда); 4) список чатов панели по-прежнему без скрытых сессий (решение DK-847)
  - решено человеком 2026-09-11: 1) цикл цели жив, человек жмёт кнопку чата у строки цели или на форме: открывается чат цикла со своим заголовком и зелёным состоянием, а не чужой груминг; 2) реплика из этого чата ложится во «Входящие» цели, идущий виток получает её подхватом chat-in.py, второй агент резюмом не поднимается; 3) цикл не идёт: реплика ждёт во «Входящих» следующего витка (дорога из README дашборда); 4) список чатов панели по-прежнему без скрытых сессий (решение DK-847)
- «дорога»: Кнопка открывает чат цикла адресом мимо фильтра hidden или цикл цели перестаёт прятаться?
  - решает: исполнитель
  - рекомендация: адресный вход мимо фильтра скрытых: сервер отдаёт скрытую запись, когда её просят по sid (chats.go:917), признак hidden остаётся фильтром списка; довод: человек принял кейс 4, список остаётся без скрытых, и решение DK-847 не пересматривается
  - решено исполнителем 2026-09-11: адресный вход мимо фильтра скрытых. Сервер отдаёт скрытую запись, когда её просят по sid ключом keep (chats.go). Признак hidden остаётся фильтром списка. Кейс 4 держит список без скрытых, и решение DK-847 не пересматривается. Пока такая запись открыта, панель держит её строкой в выпадающем списке. Ключ keep вводит запись в st.chats. Готовый pin(st.sid) кладёт открытый sid поверх фильтра `!c.hidden` (app.js:8396). Разбор нюанса лежит в README дашборда. Соседних скрытых записей этот путь не открывает.
- «адрес цикла»: Чем строка и форма цели находят sid живого цикла и куда уходит реплика из его чата?
  - решает: исполнитель
  - рекомендация: goal-run.py ставит DEVKIT_TMUX=goal-<ID> и DEVKIT_TASK при подъёме, board.go кладёт Session в работу из tmux цели, форма задачи передаёт кнопке всю строку с run_chat, а не только id; реплика с чата цели уходит ручкой POST /goals/<ID>/message, как описывает README дашборда
  - решено исполнителем 2026-09-11: от рекомендации отступил в одном месте. Строка цели берёт sid витка из файла session в замке .devkit/goal-<ID>.lock, пока жив pid оболочки из того же замка. Замок goal-run.py пишет до подъёма клиента, по нему же адресует реплики chat-in.py, а формат сторожит TestGoalTurnContractWithShell. Виток идёт через claude -p без терминала. Хук старта по DK-673 такое имя окна отбрасывает, и с DEVKIT_TMUX сессии в board.go не оказалось бы. С DEVKIT_TASK хук добавил бы витку контекст задачи. Остальное идёт по рекомендации. Строка кладёт sid в run_chat, форма отдаёт кнопке всю строку, реплика из чата цели уходит ручкой POST /goals/<ID>/message.

## DoD

- Пока цикл цели жив, кнопка чата у строки цели и на форме цели открывает чат
  цикла с его заголовком и зелёным состоянием. Это видно браузерным тестом на
  песочнице с синтетической целью.
- Реплика из чата цели ложится строкой во «Входящие» файла цели ручкой цели, и
  резюмом второй агент не поднимается. Идущий виток получает её подхватом
  chat-in.py, а при стоящем цикле строка ждёт следующего витка.
- Список чатов панели по-прежнему не показывает скрытых сессий, тесты
  `chathidden_test.go` зелёные.
- Тест каждого кейса падает на старом коде, это сверено прогоном `regcheck`.
- README дашборда описывает адресный вход в скрытый чат и дорогу реплики цели
  так, как они работают после правки.

## Ранг

`50+7+3+5+2 = 67`, P1.

- Серьёзность 50. Ответ на вопрос витка из дашборда теряется, а у панели нет
  входа во «Входящие». Конвейер встаёт на человека там, где обещана
  автономия.
- Ценность 7. Вопросы человеку цель задаёт каждый день, и ответ снова доходит
  с экрана, без правки файла цели руками.
- Неопределённость 3. Дорогу выбрал кейс 4, но правка проходит через оболочку
  цикла и хук на python, сервер на Go и фронтенд, и развилки возникают на
  стыках.
- Поправка на баг +5. Дорогу во «Входящие» сняла правка DK-397, а README её
  по-прежнему описывает. Кнопку цели сломало скрытие цикла по DK-847.
- Рычаг 2. Ускоряет свой поток по одной теме, циклу цели.
- Цена M. Четыре-пять мест правки, тесты рядом с каждым, одна сессия.
- Приёмка агентская. Браузерный тест и песочница с синтетической целью
  закрывают глаза, прочих барьеров нет.

## Ход работы

- Из черновика DK-938 «Кнопка чата у цели открывает мёртвый чат груминга вместо чата цикла»: записан 2026-09-11, оформлен 2026-09-11.
- Вычитка: 1 файл, 9 правок, 0 пометок, 2026-09-11.
- Разработка: субагент opus/high по вердикту pick (квота: week_all 44%, week_max 31%, снимок 14м назад, сдвига нет), 2026-09-11 13:48-13:49.
- Вычитка: 1 файл, 5 правок, 0 пометок, 2026-09-11.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 46%, week_max 32%, снимок 17м назад, сдвига нет), 2026-09-11 14:37-14:57.
- Ревью: ревью провёл sonnet, ходов 81, минут 18, 2026-09-11 14:57-15:10.

## Ревью

Уровень 3 до db67ab5: неопределённость 3 из вердикта pick, bug P1, критичный путь дашборда (чат цели, hooks/chat-in.py); dt=db67ab5

- [возврат: постановка, 2026-09-11] не блокирует: кейс 4 в тексте задачи и «дорога» не называют одну вещь. Открытая по адресу скрытая запись (ключ keep) попадает в st.chats. Готовый pin(st.sid) в switcher (app.js:8396) кладёт её в выпадающий список панели, пока чат открыт. Нюанс описан только в README (tools/dashboard/README.md). Кейс 4 читатель поймёт иначе, как «список без keep никогда не покажет hidden». Сверка кодом: chats.go:926 (win.keep обходит фильтр hidden), app.js:8387-8397 (pin выше фильтра !c.hidden). Поведение корректно, TestChatKeepOpensHiddenGoalTurn держит серверную границу. Нюанс стоило назвать рядом с текстом кейса 4 в «Развилках».: исправлено, мелочь: строка в развилке «дорога» дописана ревью самим, файл уже был в диффе
- Вердикт: без замечаний. путь по DoD пройден по звеньям: строка цели -> rowChatBtn -> run_chat из board.go/goalTurn -> chats.go keep обходит фильтр hidden -> POST /goals/<ID>/message -> Входящие -> chat-in.py. Кейсы 1-2 сверены TestBrowserGoalChatOpensLoop и TestGoalTurnNamesRowChat/RegistryGoal и TestStaticGoalChatReplyToInbox. Кейс 3 сверен той же веткой стенда (idle-цикл, /say не зовётся), кейс 4 сверен TestChatKeepOpensHiddenGoalTurn и chathidden_test.go. Полный go test ./tools/dashboard (GOWORK=off) зелёный 163с, включая настоящий браузерный TestBrowserGoalChatOpensLoop. regcheck --base main подтвердил красноту новых тестов кейсов на main и зелень на текущем коде. Развилка «адрес цикла» (лок-файл вместо DEVKIT_TMUX) сверена с DK-673 (session-task.py: window_name пуст без терминала клиента) и с goal-run.py (sessfile пишется до подъёма клиента). README описывает адресный вход и дорогу реплики так, как работает код. Одна мелочь по кейсу 4 (нюанс pin в выпадающем списке) поправлена ревью коммитом 2d213141.

## Сценарий проверки

Вид агентский. Каждый шаг идёт в свежем дереве с временным `HOME`, цель XR-100
и её виток синтетические, их заводят сами тесты. Вердикт шага это код возврата
команды.

1. Исходный баг на синтетической цели. Цель XR-100 идёт циклом в tmux
   `goal-XR-100`, её виток живой и скрыт из списка чатов, рядом лежит
   вчерашний груминг той же задачи. Браузерный стенд поднимает сервер
   дашборда и жмёт кнопку чата в строке доски и на форме цели. До правки обе
   кнопки открывали «Разбор и груминг задачи XR-100» с красной плашкой
   «разговор остановлен». В выводе ждём `--- PASS: TestBrowserGoalChatOpensLoop`
   и две строки `Title:Цикл цели XR-100: третий виток Busy:true Stop:false
   Green:true`. Без браузера тест печатает `--- SKIP`, и шаг краснеет. Браузер
   стенд находит сам, это chrome-headless-shell из кеша playwright настоящего
   дома. Переменная `DASHBOARD_CHROME` нужна, только когда он лежит в другом
   месте. Полный Google Chrome ей не годится. В таком прогоне он поднимает
   профиль и не укладывается в срок теста, так упал первый прогон после
   выката.

```sh
cd tools/dashboard && GOWORK=off go test -count=1 -run '^TestBrowserGoalChatOpensLoop$' -v . 2>&1 | tee /dev/stderr | grep -c 'Title:Цикл цели XR-100: третий виток Busy:true Stop:false Green:true' | grep -qx 2
```

2. Реплика из чата цели. Стенд над настоящим `app.js` открывает живой виток по
   sid и адрес цели при стоящем цикле. В обоих случаях реплика уходит ручкой
   `/goals/XR-100/message`, `/say` не зовётся, а живой чат человека о цели
   остаётся своим разговором. В выводе ждём строку `poc_goalchat: ok`.

```sh
cd tools/dashboard && GOWORK=off go test -count=1 -run '^TestStaticGoalChatReplyToInbox$' -v . 2>&1 | grep 'poc_goalchat: ok'
```

3. Ручка цели кладёт строку во «Входящие» файла цели и называет стоящий цикл
   словами. В выводе ждём два `--- PASS` и `ok`.

```sh
cd tools/dashboard && GOWORK=off go test -count=1 -run '^(TestMessageLandsInInbox|TestMessageAtIdleCycleSaysSo)$' -v .
```

4. Идущий виток получает строку «Входящих» подхватом `hooks/chat-in.py`.
   В конце вывода ждём `OK`.

```sh
python3 -m unittest discover -s hooks -p 'chat_in_test.py'
```

5. Адрес витка у строки цели, адресный вход в скрытую запись и список чатов
   без скрытых (DK-847). Все тесты печатают `--- PASS`, в конце `ok`.

```sh
cd tools/dashboard && GOWORK=off go test -count=1 -run '^(TestGoalTurnNamesRowChat|TestGoalTurnNamesRegistryGoal|TestChatKeepOpensHiddenGoalTurn|TestGoalTurnContractWithShell|TestChatListHidesHiddenSession|TestChatSearchFindsHiddenSession|TestChatListKeepsOrdinarySession)$' -v .
```

## Выкат

- 2026-09-11 слито: 206f1f17, 88847f6c, 752b552d, 492870f1, 1279e699, 4fdfd287, 95ebdafc, c7cab464, 66fe22dc

## Проверка


- Обкатка: 2026-09-11 15:09, свежее дерево b59e422c9b78, сценарий 892ac9a9, временный HOME, утилит дерева 11, шагов 5, все зелёные.

Шаг 1 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test -count=1 -run '^TestBrowserGoalChatOpensLoop$' -v . 2>&1 | tee /dev/stderr | grep -c 'Title:Цикл цели XR-100: третий виток Busy:true Stop:false Green:true' | grep -qx 2
=== RUN   TestBrowserGoalChatOpensLoop
    goalbrowser_test.go:79: кнопка в строке доски: {Title:Цикл цели XR-100: третий виток Busy:true Stop:false Green:true Note:Сообщение уйдёт агенту. Идущий виток получит его за минуты, на ближайшем законченном ходе, а развернуть работу может и позже, на границе шага.Поднять виток}
    goalbrowser_test.go:79: кнопка на форме цели: {Title:Цикл цели XR-100: третий виток Busy:true Stop:false Green:true Note:Сообщение уйдёт агенту. Идущий виток получит его за минуты, на ближайшем законченном ходе, а развернуть работу может и позже, на границе шага.Поднять виток}
--- PASS: TestBrowserGoalChatOpensLoop (1.75s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	2.458s
```

Шаг 2 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test -count=1 -run '^TestStaticGoalChatReplyToInbox$' -v . 2>&1 | grep 'poc_goalchat: ok'
    goalchat_test.go:179: poc_goalchat: ok
```

Шаг 3 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test -count=1 -run '^(TestMessageLandsInInbox|TestMessageAtIdleCycleSaysSo)$' -v .
=== RUN   TestMessageLandsInInbox
--- PASS: TestMessageLandsInInbox (0.09s)
=== RUN   TestMessageAtIdleCycleSaysSo
--- PASS: TestMessageAtIdleCycleSaysSo (0.12s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	0.643s
```

Шаг 4 (зелёный):

```console
$ python3 -m unittest discover -s hooks -p 'chat_in_test.py'
....................................
----------------------------------------------------------------------
Ran 36 tests in 6.297s

OK
```

Шаг 5 (зелёный):

```console
$ cd tools/dashboard && GOWORK=off go test -count=1 -run '^(TestGoalTurnNamesRowChat|TestGoalTurnNamesRegistryGoal|TestChatKeepOpensHiddenGoalTurn|TestGoalTurnContractWithShell|TestChatListHidesHiddenSession|TestChatSearchFindsHiddenSession|TestChatListKeepsOrdinarySession)$' -v .
=== RUN   TestChatListHidesHiddenSession
--- PASS: TestChatListHidesHiddenSession (0.06s)
=== RUN   TestChatSearchFindsHiddenSession
--- PASS: TestChatSearchFindsHiddenSession (0.05s)
=== RUN   TestChatListKeepsOrdinarySession
--- PASS: TestChatListKeepsOrdinarySession (0.03s)
=== RUN   TestGoalTurnNamesRowChat
--- PASS: TestGoalTurnNamesRowChat (0.07s)
=== RUN   TestGoalTurnNamesRegistryGoal
--- PASS: TestGoalTurnNamesRegistryGoal (0.05s)
=== RUN   TestChatKeepOpensHiddenGoalTurn
--- PASS: TestChatKeepOpensHiddenGoalTurn (0.04s)
=== RUN   TestGoalTurnContractWithShell
--- PASS: TestGoalTurnContractWithShell (0.00s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	0.739s
```

### Прогон после выката, sonnet, 2026-09-11

Независимый прогон чужими руками поверх слитого main (b59e422c9b78 предок
HEAD 87ecfe80, дерево чистое). Каждый шаг во временном HOME, GOWORK=off, шаг 1
с DASHBOARD_CHROME на системный Google Chrome.

Шаг 1 (красный, две попытки):

```console
$ go -C tools/dashboard test -count=1 -run '^TestBrowserGoalChatOpensLoop$' -v .
    goalbrowser_test.go:69: chrome на http://127.0.0.1:.../#demo: signal: killed
--- FAIL: TestBrowserGoalChatOpensLoop (120.06s)
FAIL
```

Обе попытки упали одинаково. Chrome не успевает завершить `--dump-dom` до
среза контекста в 120с (goalbrowser_test.go:160). Виртуальный бюджет теста
всего 40с. В выводе перед падением видны реальные процессы `GoogleUpdater`
(`--wake`, `--wake-all`, `--crash-handler`). Они из настоящего
`/Users/rider/Library/Application Support/Google/GoogleUpdater`, не из
временного HOME. Ещё одна строка выделяется отдельно: `handshake failed; returned -1, SSL
error code 1, net_error -3`.

Догадка: chrome на macOS путь Library берёт не из переменной HOME.
NSHomeDirectory смотрит на реальную учётку. Свежий временный HOME его не
обманывает, и chrome всё равно будит настоящий GoogleUpdater. Апдейтер
стучится в сеть, ловит ошибку TLS и не укладывается в 120с вместе с
тестовой частью. На обкатке 15:09 тот же chrome прошёл за 1.75с: в тот раз
будильник апдейтера не сработал. Похоже на помеху окружения, сетевой
профиль сессии вместе с системным апдейтером chrome. На регресс кода не
похоже. Остальные четыре шага зелёные, включая серверную границу кейса 1
(TestGoalTurnNamesRowChat, TestGoalTurnContractWithShell).

Исход разбора. Причина в браузере, а не в коде. Без переменной `findChrome`
берёт chrome-headless-shell из кеша playwright, на нём прошла обкатка за
1.75с. Полный Google Chrome пришёл сюда из вводной проверяющему, и комментарий
у `findChrome` про такой прогон предупреждает. Шаг 1 сценария теперь называет
браузер явно, прогон повторяется чужими руками без `DASHBOARD_CHROME`.

Шаг 2 (зелёный):

```console
$ go -C tools/dashboard test -count=1 -run '^TestStaticGoalChatReplyToInbox$' -v .
    goalchat_test.go:179: poc_goalchat: ok
--- PASS: TestStaticGoalChatReplyToInbox (0.19s)
PASS
```

Шаг 3 (зелёный):

```console
$ go -C tools/dashboard test -count=1 -run '^(TestMessageLandsInInbox|TestMessageAtIdleCycleSaysSo)$' -v .
--- PASS: TestMessageLandsInInbox (0.09s)
--- PASS: TestMessageAtIdleCycleSaysSo (0.11s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	0.611s
```

Шаг 4 (зелёный):

```console
$ python3 -m unittest discover -s hooks -p 'chat_in_test.py'
Ran 36 tests in 6.226s
OK
```

Шаг 5 (зелёный):

```console
$ go -C tools/dashboard test -count=1 -run '^(TestGoalTurnNamesRowChat|TestGoalTurnNamesRegistryGoal|TestChatKeepOpensHiddenGoalTurn|TestGoalTurnContractWithShell|TestChatListHidesHiddenSession|TestChatSearchFindsHiddenSession|TestChatListKeepsOrdinarySession)$' -v .
--- PASS: TestChatListHidesHiddenSession (0.05s)
--- PASS: TestChatSearchFindsHiddenSession (0.04s)
--- PASS: TestChatListKeepsOrdinarySession (0.04s)
--- PASS: TestGoalTurnNamesRowChat (0.08s)
--- PASS: TestGoalTurnNamesRegistryGoal (0.05s)
--- PASS: TestChatKeepOpensHiddenGoalTurn (0.04s)
--- PASS: TestGoalTurnContractWithShell (0.00s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	0.706s
```

Итог: 4 из 5 шагов зелёные, шаг 1 красный по помехе окружения (chrome и
системный GoogleUpdater), не по коду. Строку не закрываю.
