# DK-942: Диалог цепочки на дашборде, список задач и номера уровней

## Что происходит

2026-09-11, нарезка LLD DK-933, третий заезд черновика DK-721 вне цели DK-902.
Карточка зависимостей на дашборде принимает один ID в поле ввода. Взвода она
не показывает, и цепочку с уровнями на ней не собрать.

## Чего хотим

В карточке вместо поля ввода список задач с поиском и переключатель
автозапуска, который зовёт `arm` и `arm --off`. Из списка доски открывается
диалог цепочки. Строки раскладываются по номерам уровней, предпросмотр идёт
через `chain --dry-run`, запись через `chain`. Три состояния взвода видны на
строке списка и в карточке, приходят они из `dep list --json`.

## DoD

- Цепочка из трёх строк на двух уровнях собирается диалогом и ложится на
  доску теми же рёбрами, что даёт `chain`.
- Переключатель ставит и снимает `[взвод]`, отказ `arm` виден в карточке с
  причиной.
- Проверка идёт тестом сервера дашборда на синтетической доске.

## Ранг

`25+5+3+0+2 = 35`, P2.

- Серьёзность 25: обход через чат или терминал дешёвый.
- Ценность 5: цепочку видно на доске и собирают глазами.
- Неопределённость 3: сервер на go, экран и стык с taskctl через JSON, три
  процесса.
- Поправка на баг 0: новая работа.
- Рычаг 2: небольшая своя польза.

Цена M. После DK-941. Правило DK-721 не даёт следующему заезду начаться до
закрытия предыдущего.

## Приёмка

- вид: agent

## Ход работы

- Вычитка: 1 файл, 0 правок, 0 пометок, 2026-09-11.
- Вычитка: 1 файл, 8 правок, 0 пометок, 2026-09-13.

## Ревью

Уровень 3 до 520689b: до 520689bb: неопределённость 3 из ранга задачи, тронуты tools/taskctl и tools/dashboard

- [возврат: реализация, 2026-09-13] блокирует: tools/dashboard/chain.go:119, handleChain берёт subject коммита из необработанного body.Levels[0][0], а не из значения, которое chainIDs очистило (пробелы, верхний регистр) и увело в подпроцесс taskctl. При запросе с ID не в каноническом виде (лишний пробел, нижний регистр) рёбра на доске лягут верно, а subject коммита получит сырой ID и разойдётся с ним, ломая розыск коммитов задачи по ID (RULES.board.core.md). Починить: взять голову из уже очищенного списка, например через chainIDs(body.Levels[0])[0], а не из тела запроса напрямую.: исправлено, subject берётся из очищенного списка chainArgs, тест на неканоничный номер в голове уровня краснеет на HEAD (regcheck)
- Вердикт: без замечаний. круг 2 до 317e8f31: fixed 1 сверено диффом a46eeae9 и прогоном regcheck --tests tools/dashboard/chain_test.go (красное на main, зелёное на HEAD). Прогнано: go test -count=1 ./... в tools/dashboard (186.7с), gofmt, go vet, check-symbols по тронутым файлам чисто
