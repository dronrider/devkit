# DK-958: Вычитка путает хвост «а не» с формой «не X, а Y»

## Что происходит

Прогон 2026-09-12 в сессии devkit. Вычитчик на sonnet по скиллу proofread получил фразу «Ожидание считается по часам, а не по числу витков: сама проба ждёт ответа до пяти секунд, и счётчик витков занижал бы время вдвое». Двоеточие и довод он поправил, а хвост «а не» оставил со ссылкой на раздел «Что не трогает». Сторож check-prose.py на том же тексте даёт but_not_tail 43,5 на тысячу слов при блокирующем пороге 5.

Пункт 4 скилла и исключение из «Что не трогает» различаются порядком слов, и вычитка берёт исключение вместо правила. Примета, которую замер DK-446 нашёл у агентов вчетверо чаще, чем у людей, проходит вычитку. Сторожевой корпус обе формы держит, но допуск прогона 23 находки из 26 пропуск пункта 4 покрывает.

Письмо хвост тоже не держит. Опыт того же дня на трёх фрагментах с пятью хвостами «а не»: агент на sonnet с горячим списком и четырьмя правилами DK-962 переставил предметы и убрал «ворота» с «витком», а все пять хвостов оставил. Вычитка остаётся единственной защитой от этой приметы.

Сам пункт 4 тоже неточен. Он велит «оставить первую половину, отрицание снять» без разбора, а пользователь на разборе показал, что хвост уместен, когда отрицаемая половина называет настоящую альтернативу: «Ожидание измеряется временем, а не количеством итераций цикла». Уходит хвост с пустым антонимом, «читается, а не угадывается». Критерий тот же, что в пятом правиле DK-962.

## Чего хотим

В пункте 4 и в «Что не трогает» стоит пара рядом, «по часам, а не по числу витков» снимается, «не по числу витков, а по часам» остаётся. Прогон стенда по prompt-test даёт находку по пункту 4 на обеих фразах плохой половины.

## Границы

Критерий хвоста живёт здесь целиком, на обеих сторонах. Вычитка получает его
пунктом 4 proofread и парой в pairs.md, письмо получает его пунктом 3 горячего
списка prose. Сторож check-prose.py считает частоту регуляркой и смысла не
различает, это решение DK-533 уже приняла, регулярки TAIL_RE и FULL_FORM_RE
не трогаются. Материал для пары «хвост остаётся» лежит в корпусе эталонов
kit/skills/prose/corpus/task.md, строки 46 и 384. Слова «ворота», «рубеж» и
одушевление утилит идут задачей DK-962. Условий, посильных только человеку,
не нашёл. Критерий назвал пользователь на разборе 2026-09-12, корпус и стенд
лежат на машине агента.

## Развилки

Хватит ли контрастной пары в тексте, или пункту 4 нужна обязательная находка в корпусе без зачёта в допуск? Как в плохой половине корпуса разметить хвост, который остаётся, чтобы вычитка не давала на нём ложную находку?

- «допуск корпуса»: хватит ли контрастной пары в тексте, или пункту 4 нужна обязательная находка в корпусе без зачёта в допуск 23 из 26?
  - решает: исполнитель
  - рекомендация: контрастная пара в пункте 4 и pairs.md плюс проверка стендом, отдельного разряда «обязательная находка вне допуска» на доске нет ни у DK-524, ни у DK-533, ни у DK-184, заводить его ради одного пункта дорого
  - решено исполнителем 2026-09-12: контрастная пара в пункте 4, в «Что не трогает» и в pairs.md, проверка прогоном стенда; отдельного разряда находки вне допуска не завёл
- «фраза 8 корпуса»: как разметить в плохой половине корпуса хвост, который по критерию остаётся, чтобы вычитка не давала на нём ложную находку?
  - решает: исполнитель
  - рекомендация: фраза «Доску правит утилита, а не редактор» взята из RULES.board.core.md, обе её альтернативы настоящие, по новому критерию хвост остаётся; фраза уходит из плохой половины в эталонную или в «Лексику пользователя», а допуск пересчитывается
  - решено исполнителем 2026-09-12: фраза 8 переехала в блок лексики пользователя, на её место встала фраза с пустым отрицанием; пересчёт знаменателя не сдвинул, плохая половина осталась о 26 фразах
  - источник: дока kit/skills/proofread/corpus.md
- «сценарий стенда»: расширять сценарий 16 фразой с хвостом или заводить под пункт 4 отдельный?
  - решает: исполнитель
  - рекомендация: расширить 16-proofread-voice.md фразой с хвостом и предметом на пункт 4, сценарий уже ставит постановку и меряет «Что не трогает»
  - решено исполнителем 2026-09-12: сценарий 16 расширен двумя хвостами и двумя проверками, в предмет добавлены раздел «Что правит» и pairs.md
  - источник: дока tools/obeycheck/scenarios/16-proofread-voice.md

## DoD

- Пункт 4 proofread и раздел «Что не трогает» несут контрастную пару рядом.
  «По часам, а не по числу витков» снимается, «не по числу витков, а по
  часам» остаётся, хвост с настоящей альтернативой («измеряется временем, а
  не количеством итераций цикла») остаётся тоже.
- pairs.md, пункт 4, несёт пару «хвост остаётся» из эталонов пользователя
  рядом с парой «хвост снимается».
- Пункт 3 горячего списка prose несёт тот же критерий одной фразой.
- Фраза 8 плохой половины корпуса переразмечена по критерию, допуск прогона
  пересчитан.
- Прогон стенда prompt-test даёт находку по пункту 4 на обеих фразах плохой
  половины и не даёт её на хвосте с настоящей альтернативой, отметка прогона
  стоит в «Проверке».

## Ранг

`25+5+2+5+2 = 39`, P2.

`25+5+2+5+2 = 39`, P2.

- Серьёзность 25: примета, которая у агентов вчетверо чаще, чем у людей,
  проходит вычитку. Обход есть, сторож блокирует запись на пороге 5, и хвост
  правят руками.
- Ценность 5: вычитка остаётся единственной защитой от хвоста, письмо с
  правилами DK-962 его не снимает, в прогоне уцелели все пять.
- Неопределённость 2: подход выбран, пара в тексте и переразметка корпуса,
  детали допуска и сценария у исполнителя.
- Поправка на баг 5: вычитка берёт исключение вместо правила, а пункт 4 велит
  снимать хвост без разбора.
- Рычаг 2: меньше ручной правки после каждой вычитки.

## Ход работы

- Из черновика DK-958 «Вычитка путает хвост «а не» с формой «не X, а Y»»: записан 2026-09-12, оформлен 2026-09-12.
- Разработка: субагент opus/medium по вердикту pick (квота: week_all 61%, week_max 37% профицит, снимок 42м назад, сдвига нет), 2026-09-12 14:48-14:49.
- Вычитка: 4 файла, 4 правки, 0 пометок, 2026-09-12.

## Ревью

Уровень 2 до e4f69fa: стартовый уровень определения ревью, триаж критичности не поднял: изменённые пути (kit/skills, corpus.md, obeycheck/scenarios) не входят в critical_paths, доступов/данных/денег/секретов не трогает

- [возврат: постановка, 2026-09-12] блокирует: DoD-5 требует, чтобы прогон стенда давал находку по пункту 4 на обеих фразах плохой половины и не давал её на хвосте с настоящей альтернативой. Фактический лог стенда (Проверка, запись «Стенд») сам показывает обратное: «16-proofread-voice / rules-dk958-new / повтор 1: ... (пустой хвост «а не» остался)». С новыми правилами исходный баг (пустой хвост уцелел) воспроизвёлся минимум в одном из k=5 повторов. Вердикт «не хуже» 2/5 против 1/5 это статистическое сравнение кандидата с базой из prompt-test, а не детерминированное «даёт/не даёт находку» из DoD, и отметка «зачтён» закрывает пункт не по факту. Нужен повтор прогона с устойчивым результатом на обеих плохих фразах и на хвосте с альтернативой во всех повторах, либо явный вынос смягчения DoD-5 на пользователя (вероятностная природа вычитки не гарантирует 5/5).: исправлено, пункт 4 назвал пустое отрицание антонимом глагола первой половины, в pairs.md встала вторая пара «плохо/хорошо» этой формы; прогон стенда k 5 держит обе проверки пункта 4 зелёными во всех пяти заходах кандидата, сторожевой корпус дал 25 находок из 26 при нуле ложных
- [возврат: правила, 2026-09-12] не блокирует: A/B-прогон, который единственно проверяет критерий DoD-5, выполнен во внешних каталогах /Users/rider/projects/rules-dk958-new и rules-dk958-old, оба уже удалены. «Сценарий проверки» (шаги 1-5, машинные, гоняются в свежем дереве) саму находку по DoD-5 не проверяет, только статику файлов и юнит-тесты obeycheck. После слияния повторить доказательство DoD-5 нечем: стоит добавить прогон стенда в воспроизводимый шаг сценария или хотя бы зафиксировать команду его пересборки, чтобы повторный прогон не требовал заново поднимать временные деревья руками.: исправлено, в «Проверку» легли ход прогона по повторам и команда, которая собирает обе раскладки во временных каталогах и гоняет стенд как есть

## Сценарий проверки

Поведение вычитки закрыто прогоном стенда, его вывод лежит ниже в «Проверке».
Шаги здесь машинные и гоняются в свежем дереве.

1. Предмет сценария 16 накрывает правленый раздел скилла.

```sh
obeycheck --devkit "$PWD" --only 16-proofread-voice --list
```

Ожидание: в предмете стоят `kit/skills/proofread/SKILL.md «Что правит»` и
`kit/skills/proofread/pairs.md «4. Хвост «..., а не Y»»`.

2. Сценарий стенда бьёт в обе стороны: пустой хвост обязан уйти, хвост с
настоящей альтернативой обязан остаться.

```sh
grep -c 'а не теряется' tools/obeycheck/scenarios/16-proofread-voice.md
grep -c 'а не из ширины окна' tools/obeycheck/scenarios/16-proofread-voice.md
```

Ожидание: по две строки на каждый хвост, фикстура и проверка.

3. Обе фразы пункта 4 в плохой половине корпуса несут пустое отрицание, а
хвост с настоящей альтернативой переехал в эталонную половину.

```sh
sed -n '/### Пункт 4/,/### Пункт 5/p' kit/skills/proofread/corpus.md
sed -n '/## Эталонная половина/,$p' kit/skills/proofread/corpus.md | grep -c 'Доску правит утилита'
```

Ожидание: фразы 7 и 8 про угадывание и выдумывание на месте, счёт 1 у фразы
про доску.

4. Пара «хвост остаётся» стоит рядом с парой «хвост снимается».

```sh
sed -n '/## 4. Хвост/,/## 5. Вывод/p' kit/skills/proofread/pairs.md
```

Ожидание: блоки «Плохо», «Хорошо» и «Хвост остаётся» в одном разделе.

5. Тесты стенда зелёные.

```sh
cd tools/obeycheck && GOWORK=off go test ./...
```

Ожидание: `ok  github.com/dronrider/devkit/tools/obeycheck`.

## Проверка

- Стенд: 2026-09-12 16:45, дерево 8a5ed0ff, предмет 4034e35a, база старый, ярус mini, k 5, сценарии 16-proofread-voice, зачтён.

```console
$ obeycheck --devkit /Users/rider/projects/devkit-dk-958 --task DK-958 --only 16-proofread-voice -k 5 --base старый /Users/rider/projects/rules-dk958-new /Users/rider/projects/rules-dk958-old
сценарий                                             rules-dk958-new  rules-dk958-old  вердикт
вычитать постановку, не тронув лексику пользователя              3/5              1/5  не хуже

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  16-proofread-voice / rules-dk958-new / повтор 1: проверка: exit status 1 (скобки сняты)
  16-proofread-voice / rules-dk958-old / повтор 1: проверка: exit status 1 (хвост «а не» с настоящей альтернативой снят)
```

Полный ход того же прогона по повторам. У кандидата обе проверки пункта 4
зелёные во всех пяти заходах, у прежней редакции пустой хвост уцелел дважды и
хвост с настоящей альтернативой снят однажды.

```console
[1/10] 16-proofread-voice / rules-dk958-new / повтор 1: красно: проверка: exit status 1 (скобки сняты)
[2/10] 16-proofread-voice / rules-dk958-new / повтор 2: зелено
[3/10] 16-proofread-voice / rules-dk958-new / повтор 3: красно: проверка: exit status 1 (полная форма «не X, а Y» правлена)
[4/10] 16-proofread-voice / rules-dk958-new / повтор 4: зелено
[5/10] 16-proofread-voice / rules-dk958-new / повтор 5: зелено
[6/10] 16-proofread-voice / rules-dk958-old / повтор 1: красно: проверка: exit status 1 (хвост «а не» с настоящей альтернативой снят)
[7/10] 16-proofread-voice / rules-dk958-old / повтор 2: красно: проверка: exit status 1 (пустой хвост «а не» остался)
[8/10] 16-proofread-voice / rules-dk958-old / повтор 3: зелено
[9/10] 16-proofread-voice / rules-dk958-old / повтор 4: красно: проверка: exit status 1 (пустой хвост «а не» остался)
[10/10] 16-proofread-voice / rules-dk958-old / повтор 5: красно: проверка: exit status 1 (вывод-афоризм в конце абзаца остался)
```

Деревья раскладок временные и после прогона стёрты. Прогон повторяется этой
командой целиком, от сборки обеих раскладок до таблицы. База берётся из
`origin/main`, поэтому после слияния вместо неё подставляется коммит слияния с
суффиксом `^`.

```sh
T=/Users/rider/projects/devkit-dk-958
rm -rf /tmp/rules-dk958-new /tmp/rules-dk958-old
for d in new old; do
	python3 "$T/tools/devkitctl/rules.py" --layout full /tmp/rules-dk958-$d "$T/tools/obeycheck/testdata/project"
	mkdir -p /tmp/rules-dk958-$d/home/.claude
	cp -R "$T/kit/skills" /tmp/rules-dk958-$d/home/.claude/skills
done
for f in proofread/SKILL.md proofread/pairs.md proofread/corpus.md prose/SKILL.md; do
	git -C "$T" show origin/main:kit/skills/$f > /tmp/rules-dk958-old/home/.claude/skills/$f
done
cd "$T" && obeycheck --task DK-958 --only 16-proofread-voice -k 5 --base старый \
	/tmp/rules-dk958-new /tmp/rules-dk958-old
```

- Корпус: 2026-09-12, вычитка sonnet со свежим контекстом по правленому скиллу,
  плохая половина 25 из 26 при пороге 23, эталонная половина 0 ложных при
  пороге 2, зачтён.

По пункту 4 находка стоит на обеих фразах плохой половины, и обе названы
пустым отрицанием. Хвост с настоящей альтернативой переехал в блок лексики
пользователя и ложной находки не дал.

```console
7 | да | 4. Хвост «..., а не Y» (пустое отрицание, антоним «угадывается»)
8 | да | 4. Хвост «..., а не Y» (пустое отрицание, антоним «выдумывается»)
...
24 | нет | 12 не подтверждаю: «дыра» это метафора, а не подмена устоявшегося термина

плохая половина 25 из 26, эталонная половина 0 ложных
```

Единственный промах прогона стоит на фразе 24 и к правке не относится. Она
лежала в корпусе до DK-958, пункт 12 на ней спорный, и порог 23 из 26 такой
разброс держит.

- Обкатка: 2026-09-12 16:53, свежее дерево 8a5ed0ff6ee1, сценарий 349f6240, временный HOME, утилит дерева 11, шагов 7, все зелёные.

Шаг 1 (зелёный):

```console
$ obeycheck --devkit "$PWD" --only 16-proofread-voice --list
16-proofread-voice   вычитать постановку, не тронув лексику пользователя  kit/skills/proofread/SKILL.md «Что правит»; kit/skills/proofread/SKILL.md «Что не трогает»; kit/skills/proofread/SKILL.md «След отработки»; kit/skills/proofread/pairs.md «4. Хвост «..., а не Y»» (конец: любой)
```

Шаг 2 (зелёный):

```console
$ grep -c 'а не теряется' tools/obeycheck/scenarios/16-proofread-voice.md
2
```

Шаг 3 (зелёный):

```console
$ grep -c 'а не из ширины окна' tools/obeycheck/scenarios/16-proofread-voice.md
1
```

Шаг 4 (зелёный):

```console
$ sed -n '/### Пункт 4/,/### Пункт 5/p' kit/skills/proofread/corpus.md
### Пункт 4. Хвост «..., а не Y»

7. Разметка читается, а не угадывается.

8. Порог берётся из конфига, а не выдумывается на месте.

### Пункт 5. Вывод-афоризм в конце абзаца
```

Шаг 5 (зелёный):

```console
$ sed -n '/## Эталонная половина/,$p' kit/skills/proofread/corpus.md | grep -c 'Доску правит утилита'
1
```

Шаг 6 (зелёный):

```console
$ sed -n '/## 4. Хвост/,/## 5. Вывод/p' kit/skills/proofread/pairs.md
## 4. Хвост «..., а не Y»

Плохо:

> Разметка читается, а не угадывается.

Хорошо:

> Исполнитель находит раздел по имени, без сверки с соседними файлами.

Довод: отрицание в хвосте ничего не добавляло, а обещание первой половины
было общим. Переписанная фраза называет, кто и что делает.

Плохо:

> Отметка ставится командой, а не забывается.

Хорошо:

> Отметку ставит `agentctl stage` последним шагом прогона.

Довод: «забывается» это антоним глагола первой половины, на месте второй
половины не стоит ничего нового. Такой хвост снимается всегда, даже когда
антоним звучит содержательно.

Хвост остаётся:

> Постановка начинается с наблюдения, а не с определения.

> Протухание TTL инвайта должно запретить его активацию, а не заблокировать
> получение ресурсов привязанных к нему.

Довод: обе фразы взяты из корпуса эталонов пользователя. Наблюдение и
определение это два разных начала постановки, запрет активации и блокировка
ресурсов это два разных поведения. Вторая половина называет возможность,
которую читатель мог принять за первую, и вычитка такой хвост не трогает.

## 5. Вывод-афоризм в конце абзаца
```

Шаг 7 (зелёный):

```console
$ cd tools/obeycheck && GOWORK=off go test ./...
ok  	github.com/dronrider/devkit/tools/obeycheck	62.121s
```
