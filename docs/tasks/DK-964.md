# DK-964: Галочки вариантов груминга встают только после перезагрузки

## Что происходит

Замечено пользователем 2026-09-12 в чате груминга DK-959 (сессия ba0dc89a, tmux task-DK-959). Агент напечатал блок вопроса в 14:54, ответ «кейсы 1» ушёл в 15:36, и галочки при строках вариантов появились только после перезагрузки страницы. Признак ожидания за это время лежал, серверная ручка GET /ask отдаёт его из файла, дашборд с 14:12 не перезапускался: после перезагрузки тот же ответ сервера галочки поставил.

Галочки вешает askPickWire по признаку pick.on, а признак ставит опрос tick в watchClientAsk (tools/dashboard/static/app.js:11833) раз в ASK_POLL. Следующий шаг опроса планируется после await api(...) без try/catch, и первое же исключение (fetch отвергнут при обрыве связи, сне ноутбука, через внешний вход) обрывает цикл до конца жизни панели: tick().catch(console.error) пишет строку в консоль и больше не зовётся. Соседние опросы это переживают: индикатор занятости (app.js:10396) и догон ленты (app.js:5637) ловят ошибку и планируют следующий шаг. Лента поэтому продолжает ехать, реплика с блоком приходит текстом, а отметок при ней нет. Перезагрузка и уход с панели с возвратом (kept.arm) поднимают опрос заново.

Обрыв в самом случае DK-959 не доказан: следа в журнале дашборда нет, увидеть его можно только в консоли браузера строкой TypeError: Failed to fetch.

Человек отвечает на развилки рукой, набирая «имя номер» по памяти формата, или перезагружает страницу на каждом вопросе груминга. Тот же опрос кормит плашки вопроса клиента (доверие каталогу, вход) и помощника пароля, и после обрыва они тоже молчат до перезагрузки.

## Чего хотим

Галочки при вариантах встают в открытой панели без перезагрузки и после обрыва связи, и у стенда poc_askpick появляется случай с отвергнутым fetch посреди опроса.

## Развилки

Хватит ли try/catch с перепланированием шага по образцу индикатора занятости, или опрос вопроса заодно переезжает на событие online и visibilitychange, как догон ленты?

## DoD

- Опрос вопроса в панели чата (watchClientAsk в tools/dashboard/static/app.js) переживает отвергнутый fetch: шаг перепланируется после ошибки, как у индикатора занятости, и галочки при вариантах встают в открытой панели без перезагрузки страницы.
- Стенд поверх app.js (testdata, по образцу poc_askpick.mjs) держит случай с отвергнутым fetch посреди опроса: тест падает на старом коде и проходит на исправленном, прогон regcheck это подтверждает.
- В README дашборда в описании опроса вопроса стоит фраза про обрыв связи.

## Ранг

`25+4+1+5+2 = 37`, P2.

- Серьёзность 25: заметное трение, обход дешёвый и очевидный, перезагрузка страницы.
- Ценность 4: ощутимое улучшение, о котором попросил пользователь.
- Неопределённость 1: правка в одном месте клиента по образцу соседнего опроса.
- Поправка на баг 5: галочки работали и перестали держаться после обрыва связи.
- Рычаг 2: тот же опрос кормит плашки вопроса клиента и помощника пароля.

## Ход работы

- Из черновика DK-964 «Галочки вариантов в чате груминга встают только после перезагрузки»: записан 2026-09-12, оформлен 2026-09-12.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 64%, снимок 34м назад, сдвига нет), 2026-09-12 16:12-16:12.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 66%, снимок 24м назад, сдвига нет), 2026-09-12 16:48-17:33.

## Ревью

Уровень 2 до 4aa409f: неопределённость 1, триаж да: watchClientAsk общий опрос, от него зависят плашки клиента и помощника пароля

- Вердикт: без замечаний. прогнан regcheck TestStaticAskPollSurvivesFetchFailure (краснеет на main, проходит на ветке), прогнаны стенды TestStaticAsk/TestChatAsk/TestPulseOwnAsk целиком (ok), сверен путь 401 в api() (редирект на /login до throw, не глохнет), сверен stop-флаг (опрос не оживает после остановки панели), сверена симметрия с индикатором занятости (app.js:10396), проверены символы и gofmt, дифф в объёме DoD без расширения

## Сценарий проверки

Стенд поверх `static/app.js` (`testdata/poc_askwake.mjs`, го-обёртка
`TestStaticAskPollSurvivesFetchFailure`) подменяет `fetch` отказом на первом
шаге опроса `watchClientAsk`. Стенд проверяет, что следующий шаг планируется
сам и вешает галочки при вариантах, дождавшись его. Базой regcheck стоит
коммит перед фиксом a02aa88e, а не main: после слияния main уже несёт правку,
и дифф тестов относительно него пуст.

```sh
GOWORK=off regcheck -C tools/dashboard --base a02aa88e^ -- go test -run TestStaticAskPollSurvivesFetchFailure -v ./...
```

Ожидание: строка «тест краснеет на main и проходит на текущем коде, регрессия
закрыта».

```sh
GOWORK=off go test -C tools/dashboard ./...
```

Ожидание: `ok` по пакету `tools/dashboard`, соседние стенды не задеты.

## Выкат

- 2026-09-12 слито: 1051c3e4, 3433643f, 8b3bbb1a, 88058d65, a02aa88e
- smoke прогнан, 2026-09-12

## Проверка

- Агентская проверка: 2026-09-12 17:45, haiku, шагов 2, все зелёные.

- Обкатка: 2026-09-12 17:33, свежее дерево 28dc26c45f10, сценарий c516fc7b, временный HOME, утилит дерева 11, шагов 2, все зелёные.

Шаг 1 (зелёный):

```console
$ GOWORK=off regcheck -C tools/dashboard --base a02aa88e^ -- go test -run TestStaticAskPollSurvivesFetchFailure -v ./...
тест краснеет на a02aa88e^ и проходит на текущем коде, регрессия закрыта (тесты: tools/dashboard/agentask_test.go, tools/dashboard/testdata/poc_askwake.mjs, tools/taskctl/chain_test.go)
```

Шаг 2 (зелёный):

```console
$ GOWORK=off go test -C tools/dashboard ./...
ok  	github.com/dronrider/devkit/tools/dashboard	175.690s
```
