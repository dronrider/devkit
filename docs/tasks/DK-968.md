# DK-968: Упавший прогон оставляет временное дерево, и ёмкость занята

Цель: [tasks/DK-902.md](tasks/DK-902.md)

## Что происходит

Живой замер цели DK-902 двенадцатого сентября, второй затор подряд. Первый
сняла задача DK-967, и ворота пачки пропустили взведённую строку. Дальше
отказ пришёл от соседних ворот: «потолок деревьев 17 из 5».

Деревьев в списке восемнадцать, и одиннадцать из них временные. Семь оставил
`shipctl merge`, три `regcheck`, все лежат в каталоге временных файлов. Каталоги
на месте, git их prunable не считает, `git worktree prune` такую запись не
трогает.

Прогоны эти падали. Только за сегодняшнюю сессию тест со стенным потолком
времени (DK-764) трижды отбил слияние, и каждое падение оставило дерево. Рабочих
деревьев задач при этом пять, и четыре из них тоже брошены: dk-369, dk-466,
dk-470 и dk-605 стоят на строках Backlog.

Ворота `armGates` считают деревья через `liveTrees` (tools/taskctl/arm.go).
Счёт идёт по списку git без разбора, чьё дерево и живо ли оно.

## Чего хотим

Прогон убирает своё дерево и на падении. Слияние падает часто, это штатный
исход, и мусор после него копиться не должен.

Порядок разбора такой. Временное дерево прогона снимается тем, кто его завёл,
в том числе на аварийном выходе. Уже накопленный мусор снимает `devkitctl
doctor --fix` наравне с прочими находками. Дерево брошенной задачи, чья строка
лежит в Backlog, ёмкости не занимает. Тот же разбор действовал для окон tmux в
DK-967.

## DoD

- Упавшее слияние и упавший regcheck не оставляют дерева в списке git.
- Уборка снимает накопленные временные деревья и называет их числом.
- Ворота ёмкости не считают деревом задачу, чья строка лежит в Backlog или в
  архиве.
- Тест на временном репозитории: прогон, оборванный на середине, дерева не
  оставляет.

## Ранг

`50+7+2+5+3 = 67`, P1.

- Серьёзность 50: взведённая строка не стартует, конвейер стоит до руки
  человека. Обход ручной, снимать деревья через git worktree remove.
- Ценность 7: ночная цепочка не встаёт на воротах ёмкости.
- Неопределённость 2: уборка на аварийном выходе и место вызова.
- Поправка на баг +5: прогон должен убирать за собой и не убирает.
- Рычаг 3: тем же счётом деревьев живут слот и пачка.

Цена S.

## Приёмка

- вид: agent

## Ход работы

- Вычитка: 1 файл, 6 правок, 0 пометок, 2026-09-12.
- Разработка: субагент opus/medium по вердикту pick (квота: week_all 70%, week_max 39% профицит, снимок 25м назад, сдвига нет), 2026-09-12 19:07-19:07.
- Вычитка: 9 файлов, 22 правки, 0 пометок, 2026-09-12.

## Ревью

Уровень 3 до ced5b58: неопределённость строки 2, тронут tools/taskctl (critical_paths), путь DoD цели DK-902 задет

- [возврат: реализация, 2026-09-12] не блокирует: pid_alive в tools/devkitctl/runtrees.py (os.kill(pid, 0)) даёт «жив», если номер процесса переиспользован на машине НЕ связанным с прогоном процессом; такое дерево останется висеть в git worktree list бессрочно, хотя DoD ждёт регулярную уборку накопленного. Стоило бы сверять не только номер, а и что процесс с этим pid это действительно тот же прогон (например, командную строку через ps), либо дать возрасту owner-файла тоже верхнюю границу.: исправлено, верхняя граница возраста метки: сутки
- [возврат: правила, 2026-09-12] не блокирует: TestMakeDropsTreeOnSignal (internal/freshtree/freshtree_test.go) проверяет только что дерево пропало, а не что процесс-помощник фактически ушёл тем же сигналом TERM. Само свойство код называет важным (комментарий к trap: «Позвавший читает код как причину смерти»), но тест его не защищает. Проверил вручную: собрал бинарь тестов, послал TERM помощнику из TestSignalHelper, код возврата 143 (128+SIGTERM). Сейчас работает верно, но будущая правка, которая заменит re-raise на os.Exit(1), эту дыру не поймает. Стоило бы добавить assert на cmd.ProcessState через syscall.WaitStatus.Signaled()/Signal().: исправлено
- [возврат: реализация, 2026-09-12] не блокирует: PREFIXES в tools/devkitctl/runtrees.py («shipctl-merge-», «regcheck-», «taskctl-rehearse-») дублирует строки, зашитые отдельно в tools/shipctl/ops.go, tools/regcheck/check.go и tools/taskctl/rehearse.go как аргумент freshtree.Make/Start. Каталог с чужим префиксом abandoned() пропускает целиком, даже по возрасту, так что новый вызов freshtree.Make с непереписанным в PREFIXES префиксом останется невидимым уборке навсегда. Стоило бы завести список префиксов один раз, например экспортом из internal/freshtree, и читать его оттуда в runtrees.py через общий текстовый файл или дублировать с явной сверкой в тесте.: исправлено

## Сценарий проверки

Агентский. Шаги гоняются из корня дерева, `taskctl rehearse DK-968` прогоняет
их в свежем дереве. Первый шаг воспроизводит поломку. Прогон, оборванный
сигналом, оставлял запись о дереве в списке git насовсем.

1. Оборванный прогон дерева за собой не оставляет. Помощник выкладывает дерево,
   получает TERM, уходит тем же сигналом, и в списке git остаётся один основной
   чекаут. Тем же шагом сторож сверяет, что зовущие берут префикс прогона из
   перечня, а не пишут строкой. Ждём `ok` пакета freshtree.

```sh
GOWORK=off go -C internal test -count=1 -run 'TestMakeDropsTreeOnSignal|TestMakeCheckoutsCommitWithoutWorkTree|TestCallersTakePrefixFromList' ./freshtree/
```

2. Ворота ёмкости считают деревом взятую задачу. Дерево упавшего слияния стоит
   на отцепленной голове, дерево брошенного захода держит строку Backlog, и оба
   в счёт не идут. Дерево строки из In progress идёт в счёт и держит потолок.
   Ждём `ok` пакета taskctl.

```sh
GOWORK=off go -C tools/taskctl test -count=1 -run 'TestArmGatesCountOnlyTakenTaskTrees|TestArmGatesSkipDeadWindows|TestArmCapacityRefusalCallsOnce' ./...
```

3. Уборка снимает брошенное, не трогает идущий прогон и чужие каталоги. Метка
   старше суток живой процесс уже не удостоверяет, каталог с меткой виден под
   любым именем, а список префиксов сверяется с go-перечнем. Ждём `OK` восьми
   тестов.

```sh
python3 -m unittest discover -s tools/devkitctl -p runtrees_test.py -v 2>&1 | tail -8
```

4. Живая уборка на синтетическом репозитории. Дерево брошенного прогона видно в
   списке git, находка называет его числом, после уборки в списке остаётся один
   основной чекаут.

```sh
d=$(mktemp -d) && p=$(sh -c 'echo $$') && git init -q -b main "$d/repo" && git -C "$d/repo" -c user.email=t@t -c user.name=t commit -q --allow-empty -m seed && mkdir -p "$d/T/shipctl-merge-1" && git -C "$d/repo" worktree add -q --detach "$d/T/shipctl-merge-1/tree" main && printf 'pid = %s\nroot = %s\ntree = %s\n' "$p" "$d/repo" "$d/T/shipctl-merge-1/tree" > "$d/T/shipctl-merge-1/owner" && git -C "$d/repo" worktree list && python3 -c "import sys; sys.path.insert(0, 'tools/devkitctl'); import runtrees; print(runtrees.check(False, root='$d/T')[0][0]); print(runtrees.check(True, root='$d/T')[1][0])" && git -C "$d/repo" worktree list
```

## Проверка

- Обкатка: 2026-09-12 20:05, свежее дерево bb27e67e8969, сценарий aec18aff, временный HOME, утилит дерева 11, шагов 4, все зелёные.

Шаг 1 (зелёный):

```console
$ GOWORK=off go -C internal test -count=1 -run 'TestMakeDropsTreeOnSignal|TestMakeCheckoutsCommitWithoutWorkTree' ./freshtree/
ok  	github.com/dronrider/devkit/internal/freshtree	1.011s
```

Шаг 2 (зелёный):

```console
$ GOWORK=off go -C tools/taskctl test -count=1 -run 'TestArmGatesCountOnlyTakenTaskTrees|TestArmGatesSkipDeadWindows|TestArmCapacityRefusalCallsOnce' ./...
ok  	github.com/dronrider/devkit/tools/taskctl	1.863s
```

Шаг 3 (зелёный):

```console
$ python3 -m unittest discover -s tools/devkitctl -p runtrees_test.py -v 2>&1 | tail -8
test_live_owner_is_left_alone (runtrees_test.RunTreesCase) ... ok
test_stale_record_is_pruned (runtrees_test.RunTreesCase)
Каталог без метки унёс с собой и запись. Из каталога не видно, ... ok

----------------------------------------------------------------------
Ran 5 tests in 0.657s

OK
```

Шаг 4 (зелёный):

```console
$ d=$(mktemp -d) && p=$(sh -c 'echo $$') && git init -q -b main "$d/repo" && git -C "$d/repo" -c user.email=t@t -c user.name=t commit -q --allow-empty -m seed && mkdir -p "$d/T/shipctl-merge-1" && git -C "$d/repo" worktree add -q --detach "$d/T/shipctl-merge-1/tree" main && printf 'pid = %s\nroot = %s\ntree = %s\n' "$p" "$d/repo" "$d/T/shipctl-merge-1/tree" > "$d/T/shipctl-merge-1/owner" && git -C "$d/repo" worktree list && python3 -c "import sys; sys.path.insert(0, 'tools/devkitctl'); import runtrees; print(runtrees.check(False, root='$d/T')[0][0]); print(runtrees.check(True, root='$d/T')[1][0])" && git -C "$d/repo" worktree list
/private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/tmp.cGZ3LuIuIA/repo                    d038294 [main]
/private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/tmp.cGZ3LuIuIA/T/shipctl-merge-1/tree  d038294 (detached HEAD)
временных деревьев от упавших прогонов: 1 дерево; висят в `git worktree list`, держат старые коммиты от сборки мусора и занимают диск; убрать: devkitctl doctor --fix
снято временных деревьев прогона: 1 дерево
/private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/tmp.cGZ3LuIuIA/repo  d038294 [main]
```
