# DK-978: Чат без плана ловит сторож на втором ходе, а кольцо показывает ракету

## Что происходит

2026-09-13, чат доски chat-56 с пустой привязкой. Человек заметил, что разбор
по его просьбе шёл несколькими ходами, а плана сессия не положила, и
`agentctl plan show` печатал «плана нет». Хук старта при этом положил в
контекст фразу про `agentctl plan` и просьбу позвать board-chat, правило 4
board-chat и скилл work-plan тоже на месте.

Реестр сессий за 12-13 сентября держит семь чатов devkit с пустой привязкой
(chat-52 тремя заходами, chat-53, chat-54, chat-55, chat-56). Свой план
положили три. У chat-54 файл плана есть только у субагента.

Сценарий стенда `32-chat-no-task-long-task.md` проверяет файл плана у чата без
задачи и проходит. Там просьба приходит одним куском, и работа сразу выглядит
долгой. В живом чате разговор начинается с вопроса, разрастается в работу, и
повода вернуться к плану не возникает. У сессии по задаче план зовут шаги
board-task, у чата такого шага нет.

Ни один живой контур не смотрит, положен ли план. Хуки Stop это notify,
agent-watch и turn-mark, каталог планов они не читают. Дашборд при пустом
плане рисует пустое кольцо, и человек не отличает сессию без плана от
вставшей. Правило плана держится только на решении самой сессии, что работа
«длиннее одного хода», и в чате это решение ничем не подпирается.

## Чего хотим

Сессия без плана второй ход подряд зовёт инструменты. Сторож плана держит этот
ход решением block, и сессия кладёт план сама. Пока плана нет, кольцо в шапке
разговора бежит, а в середине вместо числа стоит иконка ракеты. Человек читает
её как быстрое действие на один ход и не путает с вставшей сессией.

## Границы

- Текст правила плана и порог повода не трогаются, они у DK-881.
- Сторож расхождения плана с делом заводит DK-609. Здесь к нему дописывается
  один признак, своего хука у задачи нет.
- Иконка ракеты стоит у всякой живой сессии без плана. Дашборд файлов сторожа
  не читает.
- Поддержка второй реплики человека в стенде obeycheck входит в задачу, пока
  укладывается в правку раннера. Выросшая в свою работу режется отдельной
  строкой.

## Развилки

Где стоит проверка, в хуке Stop по числу вызовов инструментов за ход (и по
наличию субагента) или в дашборде по длине хода без файла плана? Что она
делает, пометку в контекст следующего хода, строку в ленте, надпись «плана
нет» вместо пустого кольца или всё вместе? Как стыкуется с DK-881? Порог
повода в правиле и порог проверки должны совпадать. При разных порогах
проверка шумит на один ответ.

- «кейсы»: Верен ли список пользовательских кейсов записи, чего в нём не хватает или что лишнее?
  - решает: человек
  - рекомендация: пять кейсов из черновика: 1) чат без задачи начинается вопросом и за несколько ходов разрастается в работу, плана нет, в дашборде пустое кольцо, и его не отличить от вставшей сессии; 2) один вопрос с одним ответом, пусть и с чтением трёх файлов, плана не просит, и проверка молчит; 3) просьба сразу долгая (сценарий 32), план ложится и сейчас; 4) план положил только субагент под своей меткой, у родителя плана нет (chat-54); 5) сессия, получившая сигнал, кладёт план сама, человек репликой её не подгоняет
  - вариант: список верный, идём дальше
  - вариант: список поправить, ответ словами
  - решено человеком 2026-09-13: пять кейсов из черновика: 1) чат без задачи начинается вопросом и за несколько ходов разрастается в работу, плана нет, в дашборде пустое кольцо, и его не отличить от вставшей сессии; 2) один вопрос с одним ответом, пусть и с чтением трёх файлов, плана не просит, и проверка молчит; 3) просьба сразу долгая (сценарий 32), план ложится и сейчас; 4) план положил только субагент под своей меткой, у родителя плана нет (chat-54); 5) сессия, получившая сигнал, кладёт план сама, человек репликой её не подгоняет
- «счёт ходов»: Откуда сторож берёт счёт ходов сессии без задачи?
  - решает: исполнитель
  - рекомендация: ходы чата без задачи считать по журналу ~/.devkit/turns.log хука turn-mark: реестр ~/.devkit/runs открывается только по ID задачи (internal/stage/stage.go:304), и у чата записи там нет
  - источник: код hooks/turn-mark.py:63-68, internal/stage/stage.go:304
- «сценарий стенда»: Каким сценарием стенд показывает живой случай, где план требуется со второго хода?
  - решает: исполнитель
  - рекомендация: новый сценарий obeycheck с двумя репликами человека: первая вопрос, вторая разворачивает его в работу; проверка ищет файл плана по ID сессии, а не любой файл в каталоге планов, как делает сценарий 32; ни один из сценариев стенда сегодня не несёт больше одного блока cross-session-message, и стенду, возможно, нужна поддержка второй реплики
  - источник: дока tools/obeycheck/scenarios/32-chat-no-task-long-task.md
- «признак»: Чем сторож машинно считает работу длиннее одного хода у чата без задачи?
  - решает: человек
  - рекомендация: второй ход подряд той же сессии с вызовами инструментов между отметками журнала turn-mark: это дословно порог DK-881 «длиннее одного хода», и разбор на один ответ с чтением трёх файлов сторож не трогает; число вызовов за один ход и наличие субагента признаками не считаются, субагент ради одного факта в каркасе обычное дело
  - источник: возражение docs/tasks/DK-881.md:45, hooks/agent-watch.py:60-62, hooks/turn-mark.py:63-68
  - вариант: число вызовов инструментов за один ход выше порога из конфига
  - вариант: правка файла (Edit, Write) в ходе, как у матчера session-task --touch
  - вариант: второй ход с инструментами либо субагент, оба признака
  - решено человеком 2026-09-13: второй ход подряд той же сессии с вызовами инструментов между отметками журнала turn-mark: это дословно порог DK-881 «длиннее одного хода», и разбор на один ответ с чтением трёх файлов сторож не трогает; число вызовов за один ход и наличие субагента признаками не считаются, субагент ради одного факта в каркасе обычное дело
- «стык с DK-609»: Как задача стыкуется со сторожем расхождения плана из DK-609?
  - решает: человек
  - рекомендация: отдельная строка с ребром после DK-609: сторож плана на конце хода заводит DK-609 с решением block, а эта задача дописывает в него признак «плана нет при втором ходе с работой»; свой хук рядом дал бы два сторожа на один каталог планов, а приписка к DK-609 тянет его из S в M и смешивает два предмета, у него план есть и разошёлся с делом, здесь плана нет вовсе
  - источник: доска docs/tasks/DK-609.md, развилки «место сторожа» и «голос сторожа»
  - вариант: приписать черновик к DK-609 четвёртым признаком тем же заходом
  - вариант: свой хук на Stop без ребра, DK-609 потом сливается с ним
  - вариант: проверка в дашборде без хука, сессию сигнал не получает
  - решено человеком 2026-09-13: отдельная строка с ребром после DK-609: сторож плана на конце хода заводит DK-609 с решением block, а эта задача дописывает в него признак «плана нет при втором ходе с работой»; свой хук рядом дал бы два сторожа на один каталог планов, а приписка к DK-609 тянет его из S в M и смешивает два предмета, у него план есть и разошёлся с делом, здесь плана нет вовсе
- «кольцо»: Что дашборд показывает человеку у чата без плана, когда сторож нашёл нехватку?
  - решает: человек
  - рекомендация: надпись «плана нет» в кольце только по находке сторожа: сторож кладёт метку рядом с файлом плана, дашборд по ней рисует надпись вместо пустой дорожки, а у сессии на один ответ кольцо остаётся пустым, и решение кода про заглушку (app.js:4831) не снимается; надпись нужна на остаток, когда block не помог: повторный Stop с признаком stop_hook_active ход уже не держит
  - источник: код tools/dashboard/static/app.js:4831-4834 и 9399-9415, tools/dashboard/pulse.go:184-186
  - вариант: дашборд не трогать, хватает решения block в ходе
  - вариант: надпись у всякой живой сессии без плана, решение про заглушку снимается
  - решено человеком 2026-09-14: надпись «плана нет» и цифра 1 в середине кольца одинаково глупы; у живой сессии без плана кольцо бежит, а в середине стоит иконка ракеты: быстрое действие на один ход; метки сторожа для этого не нужно, а сторож по второму ходу с работой держит ход решением block, и план ложится сам

## DoD

- Сторож плана на конце хода (DK-609) смотрит журнал turn-mark. У сессии без
  своего файла плана он находит второй ход подряд с вызовами инструментов и
  держит ход решением block с просьбой положить план. Файл субагента с меткой
  за план сессии не считается. На один ответ с чтением файлов сторож молчит.
- У живой сессии без плана кольцо в шапке разговора дашборда бежит. В середине
  вместо числа работающих стоит иконка ракеты. С планом кольцо рисует деления,
  как прежде. Метки сторожа дашборд не читает.
- Сценарий стенда несёт две реплики человека, вопрос и следом работу, и
  проверяет файл плана по ID сессии.

## Ранг

`25+5+2+0+2 = 34`, P2.

- Серьёзность 25. Конвейер не стоит, чат без плана работает. Обход дешёвый,
  человек открывает панель и читает ленту. Трение в том, что пустое кольцо
  неотличимо от вставшей сессии.
- Ценность 5. Чат доски идёт каждый день. Из семи чатов за два дня плана не
  положили четыре.
- Неопределённость 2. Место и признак выбраны человеком. У исполнителя
  остались счёт ходов по журналу и вторая реплика в сценарии стенда.
- Поправка на баг 0. Правило плана есть, механики под него не было. Это новое
  поведение.
- Рычаг 2. План у чата без задачи возвращает дашборду кольцо с делениями и
  снимает ручной вопрос «жива ли сессия». Польза своему потоку по одной теме.

Приёмка агентская. Ни один из шести барьеров не встал. Признак сторожа
проверяется прямым вызовом hooks/plan-watch.py на синтетическом транскрипте и
подменённом событии конца хода, ракета в кольце дашборда прогоном
tools/dashboard/testdata/poc_ring.mjs (тест по разметке и стилям, барьер
«глаза» снят этим обходом). Живой прогон нового сценария стенда обкатан
отдельно и записан ниже, ход работы держит агентские шаги без него.

## Ход работы

- Из черновика DK-978 «Чат без задачи идёт без плана, и снаружи этого не видно»: записан 2026-09-13, оформлен 2026-09-14.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 6%, снимок 22м назад, сдвига нет), 2026-09-14 18:40-18:40.
- Живой прогон сценария стенда `41-chat-no-task-second-turn` вскрыл границу
  живого поведения. Вторая реплика через `--resume` метит весь ход признаком
  `CLAUDE_CODE_CHILD_SESSION=1`, тем же, что у субагента. `--input-format
  stream-json` метит его тем же признаком. Другого способа отдать сессии
  вторую реплику headless-ходом у харнеса нет. Признак «плана нет» на такой
  ход не срабатывает по замыслу: сессия, чьё окружение сама требует метку,
  из-под него выведена. Сценарий стенда поэтому проверяет только
  добровольное поведение модели по правилу work-plan, а принуждение сторожа
  им не проверить. Прогон -k 3 на двух раскладках дал 3/6, план положен в трёх
  ходах из шести. Цифра близка к живым семи чатам находки, «свой план положили
  три». `taskctl rehearse` этот сценарий не гоняет: отметка «Проверки» не
  должна зависеть от настроения модели.
- В том же прогоне нашёлся отдельный баг, уже не про эту границу. У прочих
  инструментов результат харнес кладёт блоком `tool_result`. У инструмента
  Skill результат идёт блоком `text`, тем же, что у живой реплики человека.
  Один ответ с вызовом скилла внутри читался сторожем как два хода. Тест на
  баг и фикс, признак `isMeta`, лежат в hooks/plan-watch.py и
  hooks/plan_watch_test.py.

## Сценарий проверки

Команды гоняются из корня чекаута devkit.

1. Сессия без своего файла плана держит второй ход подряд с вызовами
   инструментов между отметками журнала. Сторож держит такой ход решением
   block.

```sh
rm -rf /tmp/dk978scenario && mkdir -p /tmp/dk978scenario/plans
printf '%s\n' '{"type":"user","message":{"role":"user","content":"первый вопрос"}}' '{"type":"assistant","message":{"role":"assistant","content":[{"type":"tool_use","name":"Bash","input":{}}]}}' '{"type":"user","message":{"role":"user","content":[{"type":"tool_result","content":"ок"}]}}' '{"type":"assistant","message":{"role":"assistant","content":[{"type":"text","text":"первый ответ"}]}}' '{"type":"user","message":{"role":"user","content":"вторая реплика, разворачивает в работу"}}' '{"type":"assistant","message":{"role":"assistant","content":[{"type":"tool_use","name":"Bash","input":{}}]}}' '{"type":"user","message":{"role":"user","content":[{"type":"tool_result","content":"ок"}]}}' '{"type":"assistant","message":{"role":"assistant","content":[{"type":"text","text":"второй ответ"}]}}' > /tmp/dk978scenario/transcript.jsonl
sed 's/"session_id":"[^"]*"/"session_id":"dk978probe"/; s#"transcript_path":"[^"]*"#"transcript_path":"/tmp/dk978scenario/transcript.jsonl"#' hooks/testdata/claude-code/turn-done.json | env -u CLAUDE_CODE_CHILD_SESSION DEVKIT_PLAN_DIR=/tmp/dk978scenario/plans DEVKIT_PLAN_WATCH_LOG=/tmp/dk978scenario/watch.log python3 hooks/plan-watch.py --hook claude-code
```

Ожидание: на выходе JSON с `"decision": "block"`, текст называет, что план
работ сессия не положила, и советует команду `agentctl plan set`.

2. Сессия, чьё окружение само требует метку у плана (субагент, окно
   конвейера, стенд obeycheck), этот признак не трогает.

```sh
sed 's/"session_id":"[^"]*"/"session_id":"dk978probe"/; s#"transcript_path":"[^"]*"#"transcript_path":"/tmp/dk978scenario/transcript.jsonl"#' hooks/testdata/claude-code/turn-done.json | CLAUDE_CODE_CHILD_SESSION=1 DEVKIT_PLAN_DIR=/tmp/dk978scenario/plans DEVKIT_PLAN_WATCH_LOG=/tmp/dk978scenario/watch.log python3 hooks/plan-watch.py --hook claude-code
```

Ожидание: вывод хука пуст, решения `block` нет.

3. Кольцо дашборда у живой работающей сессии без своего плана ставит в
   середину ракету вместо числа работающих. С планом кольцо рисует дробь, как
   и прежде.

```sh
node tools/dashboard/testdata/poc_ring.mjs tools/dashboard/static/app.js
```

Ожидание: команда завершается кодом 0 и печатает одну строку с перечнем
проверенных случаев кольца, включая деления по плану сессии и контур без
плана.

## Проверка


- Обкатка: 2026-09-14 20:35, свежее дерево 09ad7f954d9e, сценарий e037a9a3, временный HOME, утилит дерева 11, шагов 5, все зелёные.

Шаг 1 (зелёный):

```console
$ rm -rf /tmp/dk978scenario && mkdir -p /tmp/dk978scenario/plans

```

Шаг 2 (зелёный):

```console
$ printf '%s\n' '{"type":"user","message":{"role":"user","content":"первый вопрос"}}' '{"type":"assistant","message":{"role":"assistant","content":[{"type":"tool_use","name":"Bash","input":{}}]}}' '{"type":"user","message":{"role":"user","content":[{"type":"tool_result","content":"ок"}]}}' '{"type":"assistant","message":{"role":"assistant","content":[{"type":"text","text":"первый ответ"}]}}' '{"type":"user","message":{"role":"user","content":"вторая реплика, разворачивает в работу"}}' '{"type":"assistant","message":{"role":"assistant","content":[{"type":"tool_use","name":"Bash","input":{}}]}}' '{"type":"user","message":{"role":"user","content":[{"type":"tool_result","content":"ок"}]}}' '{"type":"assistant","message":{"role":"assistant","content":[{"type":"text","text":"второй ответ"}]}}' > /tmp/dk978scenario/transcript.jsonl

```

Шаг 3 (зелёный):

```console
$ sed 's/"session_id":"[^"]*"/"session_id":"dk978probe"/; s#"transcript_path":"[^"]*"#"transcript_path":"/tmp/dk978scenario/transcript.jsonl"#' hooks/testdata/claude-code/turn-done.json | env -u CLAUDE_CODE_CHILD_SESSION DEVKIT_PLAN_DIR=/tmp/dk978scenario/plans DEVKIT_PLAN_WATCH_LOG=/tmp/dk978scenario/watch.log python3 hooks/plan-watch.py --hook claude-code
{"decision": "block", "reason": "Сторож плана devkit: второй ход подряд идёт с вызовами инструментов, а план работ сессия не положила.\nРабота явно не на один ход, порядок в скилле work-plan: положи план командой agentctl plan set, шаг начинай agentctl plan step и закрывай agentctl plan done."}
```

Шаг 4 (зелёный):

```console
$ sed 's/"session_id":"[^"]*"/"session_id":"dk978probe"/; s#"transcript_path":"[^"]*"#"transcript_path":"/tmp/dk978scenario/transcript.jsonl"#' hooks/testdata/claude-code/turn-done.json | CLAUDE_CODE_CHILD_SESSION=1 DEVKIT_PLAN_DIR=/tmp/dk978scenario/plans DEVKIT_PLAN_WATCH_LOG=/tmp/dk978scenario/watch.log python3 hooks/plan-watch.py --hook claude-code

```

Шаг 5 (зелёный):

```console
$ node tools/dashboard/testdata/poc_ring.mjs tools/dashboard/static/app.js
кольцо агентов: место в шапке, четыре состояния, деления по плану сессии, простой с планом и контур без плана, список агентов с дорогой в разговор, давность словами, список закрывается вторым нажатием, кликом мимо и Escape, как и выбор подписки
```
