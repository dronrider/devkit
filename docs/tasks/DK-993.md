# DK-993: Промпты devkit без слов про дашборд, панель и кнопки, рубеж в стороже скиллов

## Что происходит

2026-09-14, находка пользователя в чате доски. Правило записано ходом DK-941 в
`RULES.core.md` («Мимикрия») и в `RULES.md` («Код и тексты»): промпт devkit не
поминает конкретную обвязку, в тексте стоит механика, оболочка и команда. Замер
той же задачи дал 0 из 5 у отсылки, начатой словами про кнопку на экране,
против 5 из 5 у текста без привязки. Печатная сессия прочитала её как «не про
меня».

Тексты правилу не отвечают. Счёт на 2026-09-14 по промптам, то есть по
скиллам, определениям агентов и страницам-контрактам:

- `kit/skills/*/SKILL.md`: 15 упоминаний в восьми скиллах. `board-task` и
  `goal-loop` по три, `work-plan` два, `board-batch`, `board-chat`,
  `board-draft`, `board-ship` и `interview` по одному;
- `kit/agents/*.md`: 12 упоминаний во всех восьми определениях, у exec-* по
  два, у review-* по одному. Это копии двух абзацев: про `taskctl decide
  --chat` «в сессии, поднятой панелью дашборда» и про план, который «читает
  дашборд»;
- `kit/harness/glm-code.toml`: одно, в комментарии.

DK-976 держит чистку попутным пунктом, и постановка там называет восемь мест в
скиллах, а определений агентов не видит. Рубежа на новые упоминания нет:
сторож `kit/skills/check-skills.py` знает слово только в своих комментариях, и
чистка зарастёт снова, как заросла за сутки после DK-941 копиями абзаца про
план.

Хуки чистые: их упоминания лежат в докстрингах и комментариях python, в
контекст сессии этот текст не едет. Текст самого правила в `RULES.md` и
примеры в `RANKING.md` и `ACCEPTANCE.md` остаются: там дашборд назван как
предмет, а не как вход.

## Чего хотим

Скиллы, определения агентов и страницы-контракты описывают вход абстрактно:
оболочкой (`task-run.py`, `claude -p`), командой (`agentctl plan`, `taskctl
decide --chat`), хуком и файлом, который читает наблюдатель снаружи. Кто
именно рисует кольцо или жмёт кнопку, тексту безразлично.

Сторож скиллов отбивает слова «дашборд», «панель», «кнопка» и латинское
`dashboard` в `kit/skills/*/SKILL.md`, `kit/agents/*.md`, `kit/harness/*.toml`
и на страницах-контрактах (`TASKFORM.md`, `RANKING.md`, `ACCEPTANCE.md`,
`CONNECT.md`, `RULES*.md`). Исключения перечислены в самом стороже с причиной:
текст правила и примеры, где дашборд стоит предметом.

Правленые абзацы про вопрос человеку и про план проходят стенд по скиллу
`prompt-test`: на них есть сценарии `12-ask-human` и `27-work-plan`.

## DoD

- Поиск слов «дашборд» и `dashboard` по `kit/skills/*/SKILL.md`,
  `kit/agents/*.md` и `kit/harness/*.toml` пуст, механика описана оболочкой и
  командами.
- `check-skills.py` краснеет на новом упоминании в промпте и зелен на
  исключениях, тест держит оба исхода.
- Прогон стенда по сценариям правленых абзацев не хуже прежней редакции, след
  в файле задачи.
- Из DK-976 попутный пункт и строка DoD про чистку сняты со ссылкой сюда.

## Ранг

- Серьёзность 25. Правило в ядре есть, а сессия печатного входа читает
  привязку к экрану как «не про себя», замер DK-941 дал 0 из 5. Обход есть:
  правило резидентно, и сессия с окном его выполняет.
- Ценность 5. Послушание правилам одинаково у всех входов, а не только у
  сессии с окном.
- Неопределённость 1. Места посчитаны, замена известна из правила.
- Баг 0. Новой поломки нет, тексты отстали от правила.
- Рычаг 4. Сторож делает каждую следующую правку промпта дешевле: слово не
  просочится, и повторный счёт не нужен.

Цена S: около тридцати мест по образцу, рубеж в существующем стороже и один
прогон стенда.

## Ход работы

## Сценарий проверки

Агентский. Шаги гоняются из корня дерева после слияния в main.

```sh
sh -c 'grep -r -i -l -E "дашборд|dashboard" kit/skills/*/SKILL.md kit/agents/*.md kit/harness/*.toml; test $? -eq 1'
python3 kit/skills/check-skills.py
```

Ожидание: первый шаг не печатает файлов и кончается кодом 0, второй зелёный с
кодом 0. Третьим шагом во временной копии дерева дописать в
`kit/agents/exec-low.md` фразу «план читает дашборд» и повторить сторож: он
кончается кодом 1 и называет файл в stderr.
