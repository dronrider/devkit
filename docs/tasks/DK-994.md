# DK-994: Снятый черновик оставляет свой ID фантомом в составе цели

## Что происходит

2026-09-14, экран цели DK-909 в дашборде. В списке задач цели висит строка
«DK-874 elapsed молчит про этап» с пометкой «ни на доске, ни в архиве: строкой
задача ещё не заведена». Черновик DK-874 был приписан к DK-910 командой
`taskctl draft attach` (коммит 7e57685e). Его текст уехал разделом в файл
DK-910, а файл черновика удалён. При этом ID остался в трёх местах. В разделе
«Задачи цели» файла DK-909 он стоит в хвосте пункта DK-910 («Черновик DK-874
(elapsed молчит про этап) приписывается к ней»). Ещё он назван в разделе
«Соседи» файла DK-911 и в теле DK-910. Разбор состава в дашборде
(`goalTasksFromDoc`) берёт ID со скобкой как члена цели. `fillGoalTasks` не
находит его нигде и подписывает фразой про незаведённую строку.

В скилле `board-groom` (раздел «Исходы разбора») исходы «приписан» (`draft
attach`) и «удалён как протухший» (`draft drop`) описывают только судьбу
файла черновика. Про упоминания его ID в чужих текстах ни слова. Скилл
`goal-cut` обратную половину закрывает, при материализации кандидата номер
меняется на выданный ID. Половина рубежа в утилите уже стоит: у `draft drop` есть вызов
`mentionsNote`, и места упоминаний снятого ID уходят хвостом в вывод (DK-397).
У приписки такого вызова нет. `cmdDraftAttach` правит файл задачи-приёмника и
строку доски, а прочие упоминания остаются без печати.

Фантомная запись в составе цели считается задачей «впереди», и прогресс цели
занижен на единицу. Человек читает пометку как поломку состава и идёт
разбираться, откуда взялась задача, которой нет. Повторится на каждой цели,
где нарезка назвала черновики вместе с кандидатами, а потом их приписали
или выбросили. Это штатный путь по goal-cut («черновики нарезаны вместе с
целью, первый виток оформляет их через груминг»).

## Чего хотим

После `draft attach` и `draft drop` в доске и файлах задач не остаётся
упоминаний исчезнувшего ID. Исходы 2 и 4 скилла board-groom велят найти их и
переписать на новый адрес. В составе цели упоминание черновика снимается или
уходит в текст задачи-приёмника без скобки. Команды печатают перечень файлов
с оставшимися упоминаниями. Правка скилла едет через стенд prompt-test.

## Границы

- Упоминания в `docs/TASKS-archive.md` и в `docs/tasks/archive/` не трогаются:
  так решено в DK-397, и решение держит тест `TestDraftDropWarnsAboutMentions`.
- Заголовок «Из черновика <ID>» в файле задачи-приёмника остаётся и за
  упоминание не считается: его пишет сама приписка.
- Раздел «Задачи цели» автоматика не переписывает. Его разбирают
  `tools/dashboard/goal.go`, `tools/agentctl/goal.go` и `tools/taskctl/rank.go`,
  а через `holdEdges` он тянет ранг задачи-приёмника, поэтому текст правит
  грумер.
- Печать мест у `taskctl close` и отсутствие команды снятия строки доски сюда
  не входят, под них заведена отдельная строка с ребром после DK-994.

## Развилки

Как держать след машинно? Первый вариант, `draft attach` и `draft drop`
печатают хвостом список файлов в `docs/TASKS.md` и `docs/tasks/`, где ID ещё
упоминается. Второй, команды отказывают, пока упоминания стоят. Печать не
встаёт поперёк дороги и ложится на правило «фичу видно снаружи». Отказ
надёжнее, но требует переписать чужие файлы до команды. Нужна ли находка
`devkitctl doctor` на осиротевший ID в «Задачах цели» как второй рубеж?

- «кейсы»: Вот кейсы, которые я вычитал из черновика. Поправьте список: что лишнее, чего не хватает, что назвали бы своими словами?
  - решает: человек
  - рекомендация: 1 человек открывает экран цели и видит в составе задачу, которой нет: подпись говорит «строкой задача ещё не заведена», и он идёт разбираться, откуда она взялась; 2 прогресс цели занижен, осиротевший ID считается задачей впереди, и кольцо шапки врёт; 3 грумер приписывает черновик к стоящей строке и не узнаёт, что на снятый ID ещё ссылаются, потому что attach об этом молчит, а drop уже печатает; 4 человек читает файл задачи или цели, встречает ID и не находит по нему ни записи, ни строки; 5 нарезка назвала черновики вместе с кандидатами, первый виток их приписал, и фантом заводится на каждой такой цели
  - решено человеком 2026-09-15: 1 человек открывает экран цели и видит в составе задачу, которой нет: подпись говорит «строкой задача ещё не заведена», и он идёт разбираться, откуда она взялась; 2 прогресс цели занижен, осиротевший ID считается задачей впереди, и кольцо шапки врёт; 3 грумер приписывает черновик к стоящей строке и не узнаёт, что на снятый ID ещё ссылаются, потому что attach об этом молчит, а drop уже печатает; 4 человек читает файл задачи или цели, встречает ID и не находит по нему ни записи, ни строки; 5 нарезка назвала черновики вместе с кандидатами, первый виток их приписал, и фантом заводится на каждой такой цели
- «архив»: Считать ли упоминания снятого ID в архиве и в docs/tasks/archive?
  - решает: исполнитель
  - рекомендация: не трогаем: решение DK-397 держит тест TestDraftDropWarnsAboutMentions, закрытую работу править никто не пойдёт, и инвариант «упоминаний не остаётся вовсе» сужается до живых файлов
  - источник: код tools/taskctl/groom.go, mentionsOf и тест groom_test.go:889
- «самоупоминание»: Что делать с заголовком «Из черновика <ID>», который attach дописывает в файл задачи-приёмника?
  - решает: исполнитель
  - рекомендация: остаётся и за упоминание не считается: attach сам пишет этот заголовок, и правило без исключения стирало бы то, что команда только что создала
  - источник: код tools/taskctl/groom.go:434, draftSection
- «ретроспектива»: Чинить ли упоминания DK-874 в DK-909, DK-910 и DK-911, которые стоят сейчас?
  - решает: исполнитель
  - рекомендация: чинить этой же задачей: фантом DK-874 в составе DK-909 это единственный способ показать снаружи, что правка отработала
  - источник: доска, docs/tasks/DK-909.md:97, DK-910.md:27, DK-911.md:24
- «парсеры»: Переписывает ли команда раздел «Задачи цели» сама?
  - решает: исполнитель
  - рекомендация: не переписывает: раздел читают три разбора, и у rank.go через holdEdges он тянет ранг приёмника, поэтому текст правит грумер, а команда только называет места
  - источник: код tools/taskctl/rank.go:170 и tools/agentctl/goal.go, разбор того же раздела
- «инвариант»: Чем кончается след снятого ID: печатью мест или запретом упоминаний?
  - решает: человек
  - рекомендация: печать остаётся и едет в attach, а переписывает живые упоминания грумер руками по скиллу: решение DK-397 не пересматривается, инвариант сужается до «состав цели не держит осиротевший ID»
  - вариант: отказ: attach и drop не выполняются, пока в живых файлах есть упоминания, и текст переписывается до команды
  - вариант: команда сама переписывает упоминания на новый адрес, как просит гипотеза черновика
  - решено человеком 2026-09-15: печать остаётся и едет в attach, а переписывает живые упоминания грумер руками по скиллу: решение DK-397 не пересматривается, инвариант сужается до «состав цели не держит осиротевший ID»
- «предмет»: Входит ли в предмет снятие стоящей задачи, или он остаётся на исчезнувшем черновике?
  - решает: человек
  - рекомендация: предмет остаётся на исчезнувшем черновике (attach и drop), а печать мест для close и отсутствие команды снятия строки уходят отдельной строкой с ребром после DK-994: цена DK-994 держится на S, а снятие строки это своя механика со своими воротами
  - вариант: расширить предмет до любой снятой записи: mentionsNote едет ещё и в close, цена растёт до M, команда снятия строки всё равно остаётся отдельной задачей
  - вариант: расширить до конца: DK-994 заводит и команду снятия строки доски, и печать мест у неё и у close
  - решено человеком 2026-09-15: предмет остаётся на исчезнувшем черновике (attach и drop), а печать мест для close и отсутствие команды снятия строки уходят отдельной строкой с ребром после DK-994: цена DK-994 держится на S, а снятие строки это своя механика со своими воротами
- «рубеж»: Нужен ли второй рубеж на осиротевший ID и где он стоит?
  - решает: человек
  - рекомендация: находка devkitctl doctor на осиротевший ID в «Задачах цели»: печать видна только в ленте той сессии, что звала команду, а грумер её проглядит, и фантом останется жить; паттерн готов в tools/devkitctl/board.py, named_inputs уже грепает прозу файла задачи и сверяет ID с живыми строками
  - вариант: рубеж на стороне дашборда: осиротевший ID не считается членом состава и прогресс цели не занижается, текст при этом остаётся битым и ошибку не видно
  - вариант: второго рубежа нет: хватает печати у attach и drop и правила в скилле
  - решено человеком 2026-09-15: находка devkitctl doctor на осиротевший ID в «Задачах цели»: печать видна только в ленте той сессии, что звала команду, а грумер её проглядит, и фантом останется жить; паттерн готов в tools/devkitctl/board.py, named_inputs уже грепает прозу файла задачи и сверяет ID с живыми строками
- «разбор состава»: Чьим разбором доктор берёт состав цели, если своих разборов уже три?
  - решает: исполнитель
  - рекомендация: доктор разбирает раздел своими регулярками, как соседняя проверка входов, но правило «член состава это ID со скобкой» выносится в одно место на стороне go, чтобы дашборд, ранг и журнал витка не разъехались: board.py от бинаря не зависит сознательно, и пятой копии быть не должно
  - источник: код tools/devkitctl/board.py:59, named_inputs и вызов devkitctl.py:2689

## DoD

- `taskctl draft attach` печатает хвостом места, где снятый ID ещё помянут, тем
  же `mentionsNote`, что стоит у `draft drop`.
- `devkitctl doctor` печатает находку на ID из раздела «Задачи цели», под
  которым нет ни строки доски, ни строки архива, ни файла черновика.
- Скилл `board-groom` в исходах 2 и 4 велит найти живые упоминания снятого ID и
  переписать их на новый адрес, а правка скилла прогнана стендом `prompt-test`.
- Упоминания DK-874 в DK-909, DK-910 и DK-911 переписаны, и на экране цели
  DK-909 фантомной задачи больше нет.
- На печать у приписки и на находку доктора есть тесты, падающие на старом
  коде, краснота сверена прогоном `regcheck`.

## Ранг

`25+5+3+5+2 = 40`, P2.

- Серьёзность 25: в составе цели DK-909 висит задача, которой нет, и прогресс
  занижен на единицу. Обход дешёвый, человек переписывает пункт руками, когда
  разберётся, откуда взялся фантом.
- Ценность 5: экран цели и кольцо шапки перестают врать про состав, а грумер
  после приписки видит, куда ещё ведут ссылки снятого ID.
- Неопределённость 3: подход выбран человеком, но правка идёт через две
  границы языка, go у `taskctl` и python у `devkitctl`, и исполнителю остаётся
  развилка про то, чьим разбором доктор берёт состав цели.
- Поправка на баг 5: состав цели показывает несуществующую строку, это дефект
  вывода, а не новое поведение.
- Рычаг 2: польза своему потоку на каждой цели, где нарезка назвала черновики
  вместе с кандидатами.

Сумма 40.

## Ход работы

- Из черновика DK-994 «ID черновика после attach и drop остаётся в чужих текстах»: записан 2026-09-14, оформлен 2026-09-15.
