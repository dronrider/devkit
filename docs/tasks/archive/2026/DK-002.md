# DK-002: devkitctl: сводка по журналу запусков .devkit/log (частота команд, доля ошибок)

## Ход работы

- Исполнение: субагент haiku по вердикту pick, 2026-07-29.
- Добавлена субкоманда `stats [-C dir]`, которая читает журнал .devkit/log, группирует запуски по парам (утилита, команда), и выводит таблицу с числом запусков каждой команды и долей ошибочных запусков. Отсортировано по частоте убыванием, в конце итоговая строка. Битые строки (не 4 поля табами или нечисловой код) молча пропускаются. Обновлены README.md и docstring модуля. Добавлены тесты.

## Сценарий проверки

Агентский сценарий.

Из корня devkit запустить:
```
python3 devkitctl/devkitctl.py stats
```

Ожидаемо:
- код выхода 0
- в выводе таблица со строками taskctl и shipctl с числами запусков
- в конце итоговая строка с суммой по журналу

Затем:
```
python3 devkitctl/devkitctl.py stats -C /tmp
```

Ожидаемо:
- код выхода 2
- в stderr сообщение про отсутствие журнала

## Ревью

- тест сортировки не ловит её пропажу: частая команда первой в фикстуре, порядок вставки совпадает с ожидаемым: исправлено
- нечисловой код выхода не покрыт тестом: битая строка трёхпольная, до int() не доходит: исправлено
- мёртвая ветка 'if count > 0 else 0': count всегда >= 1: исправлено
- журнал из одних битых строк даёт код 0 и пустой вывод, байтово пустой даёт 2: свести к одному исходу: исправлено
- транслит 'bitaya строка' в комментарии test.sh: исправлено
- запись модели голой строкой 'model: haiku' вместо принятого формата в разделе 'Ход работы': исправлено

## Проверка после выката

Сценарий прогнан агентом 2026-07-29 на живом журнале devkit:
`python3 devkitctl/devkitctl.py stats` вернул код 0, в сводке строки по
taskctl, shipctl, agentctl и regcheck (итого 112 запусков, ошибок 13);
`python3 devkitctl/devkitctl.py stats -C /tmp` вернул «журнал запусков не
найден» и код 2.
