# DK-1181: Доктор ставит находки на docs/ARCHITECTURE.md и docs/STYLEGUIDE.md, кладёт скелеты по --fix, карта несёт ссылки шапкой

Цель: [tasks/DK-1178.md](../../tasks/DK-1178.md)

## Что происходит

Якорных документов у проектов нет, и никто не говорит, что их нет. Доктор
`devkitctl doctor` про `docs/ARCHITECTURE.md` и `docs/STYLEGUIDE.md` молчит,
хотя сторож полноты доки (`tools/devkitctl/describe.py`) первый из них уже
читает. Карта `docs/map.md` едет в каждую сессию импортом, а ссылок на якоря
не несёт, так что даже существующий файл сессии не виден. Корень разобран в
файле цели DK-1178.

## Чего хотим

По решениям LLD DK-1179.

- Доктор ставит по находке на отсутствующий файл в проекте с кодом,
  `doctor --fix` кладёт скелеты с разделами из LLD, повторный `doctor` молчит.
  Перечень находок в `tools/devkitctl/README.md` пополняется.
- Шапка `docs/map.md` (`render_map()` в `codemap.py`) несёт ссылки на оба
  файла, свежесть карты (`check_map_freshness()`) это переживает.
- У самого devkit `docs/ARCHITECTURE.md` короткий, ссылкой на раздел
  «Раскладка» README, `docs/STYLEGUIDE.md` по скелету; прогон доктора на
  devkit чистый.
- Тесты рядом с модулями (`<модуль>_test.py`), прогон по канону
  `tools/devkitctl/parallel.py`.

## Границы

Тексты правил, ревью и определений идут в DK-1180. Находка про язык без
линтера в DK-1182. Проект с рукописным `ARCHITECTURE.md` не трогается, форма
сверх разделов скелета не навязывается.

## DoD

- В проекте с кодом и без якорей `devkitctl doctor` ставит по находке на
  файл, `doctor --fix` кладёт скелеты с разделами, повторный `doctor` молчит,
  шапка `docs/map.md` несёт ссылки на оба файла. Проверено тестами devkitctl.
- Прогон на самом devkit. Оба файла лежат, доктор чист, карта перегенерирована.

## Ранг

`50+7+1+0+3 = 61`, P1.

- Серьёзность 50. Без скелетов и находки якоря не появятся ни в одном
  проекте, и ревью из DK-1180 сверять правку нечем.
- Ценность 7. Каждая сессия видит якоря шапкой карты без загрузки в контекст.
- Неопределённость 1. Разделы и место решены в LLD.
- Поправка на баг 0. Новая работа.
- Рычаг 3. Канал накопления правил появляется у всех проектов devkit.

## Ход работы

- Разработка: субагент sonnet/high по определению exec-high, работа ac7ec8d5313f0e2c8, минут 43, 2026-09-26 15:23-16:06.
- Ревью: субагент sonnet/high по определению review-high, работа a2d38d2643024ebf7, ходов 49, минут 13, 2026-09-26 16:12-16:25.
- Слияние: shipctl merge, 2026-09-26 16:25-16:25.
- Слияние: shipctl merge, 2026-09-26 16:25-16:28.
- Слияние: shipctl merge, 2026-09-26 16:29-16:40.
- Ждёт человека: проверка после выката, 2026-09-26 16:40-16:41.
- Проверка: сценарий прогнал opus, 2026-09-26 16:41-16:42.
- Токены: ходов 221, вывод 24k, свежий вход 1.3k, чтение кэша 27.1M, этапов со счётом 2, статей 1, 2026-09-26.

## Ревью

Уровень 2 до 5c10359: второй уровень по вердикту pick, effort high; критичных путей (tools/shipctl, tools/taskctl, hooks) в диффе нет

- Вердикт: без замечаний до 5c10359. Сверено с LLD DK-1179, решения 1 и 3: скелеты из kit/templates, три раздела у каждого, доктор судит только существование, шапка карты ссылкой или словами, порядок якоря раньше свежести карты. Прогон GOWORK=off python3 -m unittest codemap_test describe_test devkitctl_test weigh_test layout_test из tools/devkitctl дал 285 тестов OK. devkitctl doctor на дереве чист по ARCHITECTURE и STYLEGUIDE, прочие 6 находок про машину к диффу не относятся. Сценарий проверки задачи прогнан руками, все три шага сошлись. README.md и docs/map.md обновлены тем же коммитом. Символы раскладок en/ru чисты по check-symbols.py

## Сценарий проверки

```sh
python3 tools/devkitctl/devkitctl.py doctor 2>&1 | grep -c 'ARCHITECTURE\|STYLEGUIDE' || true
test -f docs/ARCHITECTURE.md && test -f docs/STYLEGUIDE.md && echo ok
grep -c 'ARCHITECTURE.md\|STYLEGUIDE.md' docs/map.md
```

Ожидание: первый шаг печатает 0, второй `ok`, третий не меньше 2.

## Выкат

- 2026-09-26 слито: 6d26f59a9, e57e5c327, 5c10359cf, 4b0b28849
- smoke прогнан, 2026-09-26

## Проверка

- Обкатка: 2026-09-26 16:25, свежее дерево e57e5c327b08, сценарий 06df78f2, временный HOME, утилит дерева 11, шагов 3, все зелёные.

Шаг 1 (зелёный):

```console
$ python3 tools/devkitctl/devkitctl.py doctor 2>&1 | grep -c 'ARCHITECTURE\|STYLEGUIDE' || true
0
```

Шаг 2 (зелёный):

```console
$ test -f docs/ARCHITECTURE.md && test -f docs/STYLEGUIDE.md && echo ok
ok
```

Шаг 3 (зелёный):

```console
$ grep -c 'ARCHITECTURE.md\|STYLEGUIDE.md' docs/map.md
2
```

- Прогон на выкаченном коде: 2026-09-26 16:41, дерево main на aaf45cc26, шагов 3,
  все зелёные. Прогонял opus, разработку вёл sonnet.

Шаг 1 (зелёный), находок про якоря у доктора нет:

```console
$ python3 tools/devkitctl/devkitctl.py doctor 2>&1 | grep -c 'ARCHITECTURE\|STYLEGUIDE' || true
0
```

Шаг 2 (зелёный), оба файла на месте:

```console
$ test -f docs/ARCHITECTURE.md && test -f docs/STYLEGUIDE.md && echo ok
ok
```

Шаг 3 (зелёный), шапка карты несёт обе ссылки:

```console
$ grep -c 'ARCHITECTURE.md\|STYLEGUIDE.md' docs/map.md
2
```
