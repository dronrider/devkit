# DK-143: остаток sh по правилу выбора языка

Задача цели [DK-135](../../DK-135.md). Правило языка принято в
[DK-139](DK-139.md): граница sh это сотая строка или первая своя
функция, всё, что за ней, переезжает на python.

## Задача

Привести к правилу остаток sh, который не берут DK-141 и DK-142. По счёту
DK-139 это `kit/skills/test.sh` (111 строк, две своих функции),
`kit/skills/goal-loop/test.sh` (279) и `kit/skills/goal-loop/goal-run.sh`
(230). Правило их относит к сложному, значит они переезжают на python;
`hooks/quota-refresh.sh`, три точки входа git (`pre-commit`, `commit-msg`,
`pre-push`) и фикстура `tools/obeycheck/testdata/fake-agent.sh` остаются, и на
каждый оставшийся файл в доке лежит причина.

Счёт DK-139 снят до переезда, поэтому перед работой пересчитать корпус самому
и сказать в файле задачи, сошлось ли: правило машинное, и проверять его надо
машиной.

`goal-run.sh` это оболочка цикла цели, её зовут путём из чекаута
(`kit/skills/goal-loop/goal-run.sh`), и путь этот назван в самом скилле
`goal-loop`. Переезд на python меняет имя файла, значит правится и текст
скилла, и его тест. Оболочка при этом рабочая: цикл цели поднимается ею прямо
сейчас, так что поведение (замок с pid, журнал, коды возврата 0/1/2/3, разбор
маркера последней строкой) обязано сохраниться один в один, и тест это
показывает.

Прогоны, которые звали переехавшие файлы, правятся тем же коммитом: `.github/
workflows/ci.yml`, перечень локальных прогонов в конце `README.md`, команды
внутри скиллов. Гитигнорнутый `.devkit/deploy.local` правит диспетчер, туда не
лезть.

## Сценарий проверки

Агентский.

1. Пересчёт корпуса sh: каждый оставшийся файл проходит границу правила
   (меньше сотни строк и без своих функций) либо стоит в исключениях правила с
   причиной в доке.
2. Поведение оболочки цели сохранено: тест гоняет замок с чужим pid, воронку
   из трёх витков без записи, коды возврата 0/1/2/3 и разбор маркера последней
   строкой.
3. Прогоны CI зелёные, включая новые python-тесты скиллов и оболочки.
4. Текст скилла `goal-loop` и перечень локальных прогонов `README.md` называют
   новые пути, мёртвых команд не осталось.

## Ход работы

- Исполнение: субагент sonnet/high по вердикту pick (квота: week_all 29%, снимок 0м назад, сдвига нет), 2026-08-05.
- Переезд: `kit/skills/test.sh` (111) -> `kit/skills/check-skills.py` + `kit/skills/check_skills_test.py`
  (юниты на синтетическом каталоге скиллов, 24 теста); `kit/skills/goal-loop/test.sh` (279) и
  `kit/skills/goal-loop/goal-run.sh` (230) -> `kit/skills/goal-loop/goal-run.py` (оболочка, портирована
  метод в метод) + `kit/skills/goal-loop/goal_run_test.py` (14 тестов, полный сценарий бывшего test.sh:
  цикл до маркера done, каждый терминальный маркер, воронка и её сброс, снимок квоты не в счёт
  продвижения, замок с живым и брошенным владельцем, авария без маркера/в ограде/с падением клиента,
  четыре отказа на старте, предполётная проверка прав целиком и наполовину, запуск без --foreground в
  tmux и отказ на уже поднятую сессию). Красноту теста оболочки проверил регрессией: подменил
  `idle >= FUNNEL_LIMIT` на `idle > FUNNEL_LIMIT` (сдвиг порога воронки на виток) - тест упал на двух
  сценариях (`test_funnel_stops_on_third_silent_turn`, `test_quota_snapshot_does_not_count_as_progress`),
  вернул код на место - зелено.
- Находка по ходу: `python3 -m unittest discover -p '*_test.py'`, которым LLD советовал гонять тесты
  директории инструмента, у файла с дефисом в имени (`goal-run_test.py`, `check-skills_test.py`, как
  тесты назывались изначально) находит ноль тестов и падает кодом 5 - discover строит имя модуля из
  пути и дефис не даёт его разобрать. Первым проходом обошёл это прямым вызовом `python3 <файл>`, минуя
  discover вовсе. Параллельная DK-142 упёрлась в ту же дыру у хуков (`check-symbols_test.py` тоже давал
  зелёный прогон без единой проверки) и закрыла иначе: имя теста через подчёркивание
  (`check_symbols_test.py`), discover остаётся прогоном. По уточнению диспетчера свёл своё решение к
  этому же виду: `check-skills_test.py` -> `check_skills_test.py`, `goal-run_test.py` ->
  `goal_run_test.py` (`git mv`), CI и перечень локальных прогонов вернулись на `unittest discover` для
  обеих директорий (`kit/skills/`, `kit/skills/goal-loop/`). Дополнил, а не задвоил, абзац README
  «Раскладка», который для этого уже завела DK-142 (пример `check-symbols.py`/`check_symbols_test.py`):
  дописал случай точки входа с дефисом, которую зовут путём из чекаута и её имя названо в тексте
  скилла (`goal-run.py`) - такой файл дефис не отпускает и discover его всё равно не находит, поэтому
  `kit/skills/goal-loop/goal_run_test.py` гонит его отдельным процессом (`subprocess.run`), а не
  импортом; сам `goal-run.py` остался с дефисом, самопроверка `check-skills.py` (не тест, зовётся
  напрямую в CI) - тоже.
- `goal-run.py`: путь до `notify.py`/`perms.py` остался той же глубиной (`../../../hooks/...`,
  `../../../tools/devkitctl/...`), поведение портировано метод в метод (замок-директория с `pid`,
  `kill(pid, 0)` для живости, снятие брошенного замка, журнал с меткой времени, разбор «Журнала» файла
  цели тем же проходом, что и awk-версия, включая пропуск строк снимка квоты). Текст скилла
  `kit/skills/goal-loop/SKILL.md` и README (упоминание оболочки, перечень локальных прогонов) правлены
  на новое имя файла.
- Пересчёт корпуса sh (машиной, не на глаз; свежий скрипт разбора см. ниже, тот же приём, что у
  `kit/skills/check-skills.py`): из зоны DK-143 остаётся `hooks/quota-refresh.sh` (44 строки, 0 функций),
  три точки входа git `hooks/pre-commit` (43), `hooks/commit-msg` (23), `hooks/pre-push` (22) - все под
  сотней строк и без своих функций, граница соблюдена без исключения. Фикстура
  `tools/obeycheck/testdata/fake-agent.sh` (35 строк) и семь сценариев `docs/tasks/*-check.sh`
  (55..141 строк, у DK-066 и DK-067 свои функции, у DK-070 длина за сотню) остаются исключениями по
  причине в README «Раскладка» (фикстура чужой программы; сценарий задачи одноразовый и не проходит
  границу вовсе). `tools/devkitctl/test.sh` (2616) и `hooks/test.sh` (956) вне зоны DK-143 (DK-141,
  DK-142), не тронуты (проверено `git diff main...dk-143 --stat -- hooks tools/devkitctl` - пусто).
  Итог: диспозиция corpus'а (что уезжает, что остаётся и почему) сошлась с решением LLD DK-139 один в
  один; числа местами разошлись на строку-две (`tools/devkitctl/test.sh` подрос с 2615 до 2616 не
  в этой задаче) - это дрейф между задачами, а не расхождение правила.
- Сценарий проверки прогнан целиком, вывод:

  ```
  $ python3 kit/skills/check-skills.py
  ok: скиллы, проверено 8

  $ cd kit/skills && python3 -m unittest discover -p '*_test.py'
  ----------------------------------------------------------------------
  Ran 24 tests in 0.029s

  OK

  $ cd kit/skills/goal-loop && python3 -m unittest discover -p '*_test.py'
  ----------------------------------------------------------------------
  Ran 14 tests in 8.643s

  OK

  $ git ls-files -z | xargs -0 python3 hooks/check-symbols.py
  (пусто, находок нет)

  $ python3 hooks/check-exec-bit.py
  (пусто, находок нет)
  ```

  `go test ./...` по утилитам, `hooks/test.sh` и `tools/devkitctl/test.sh` не гонялись этой задачей:
  код там не менялся (см. пустой diff выше), только путь `kit/skills/`, который они читают как данные
  (копия скиллов на синтетический `HOME`), а этот случай уже покрыт их собственным тестом на «в
  `kit/skills/` бывает проза не скиллом» (`kit/skills/README.md` в стенде `devkitctl/test.sh`).
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 29%, снимок 35м назад, сдвига нет), 2026-08-05.
- Замечание 1 закрыто: в `kit/skills/goal-loop/goal_run_test.py` добавлены четыре теста на формы,
  которые постановка отдельно называет негодными маркером (`## Маркеры выхода` в SKILL.md: «строка
  идёт голой... и после неё в ответе не остаётся ничего, даже пустой строки»), помимо уже бывшей ограды
  (fenced): `test_marker_with_trailing_text_is_not_a_marker` (текст после маркера на той же строке -
  ровно случай из замечания), `test_marker_with_leading_text_is_not_a_marker` (текст перед маркером),
  `test_marker_with_trailing_dot_is_not_a_marker` (точка в конце), `test_marker_followed_by_more_text_after_blank_line_is_not_a_marker`
  (маркер, пустая строка, а за ней ещё строка ответа - проверяет, что разбор берёт истинную последнюю
  строку, а не застревает на маркере раньше срока). Стаб клиента (`CLAUDE_STUB`) научился четырём новым
  словам сценария: `trailing`, `leading`, `dotted`, `blankreal`.
  Красноту проверил той же мутацией, что нашло ревью: `last == "marker: %s" % m` ->
  `last.startswith("marker: %s" % m)`. Упали ровно `test_marker_with_trailing_text_is_not_a_marker` и
  `test_marker_with_trailing_dot_is_not_a_marker` (оба начинаются с `marker: continue`, startswith их
  пропускает) - "трейлинг" и "точка" ловят именно эту мутацию. `leading` под той же мутацией зелёный
  и должен быть зелёным: строка «готово, marker: continue» с `marker:` не начинается, так что и `==`,
  и `startswith` её одинаково отклоняют - это другая форма из той же постановки, а не тест на эту
  мутацию. `blankreal` тоже остался зелёным: он проверяет не сравнение маркера, а то, что разбор
  последней строки не хватает строку раньше срока - защита от другого класса ошибки, не от startswith.
  Вернул `==` на место, все 18 тестов `kit/skills/goal-loop/goal_run_test.py` зелёные.
  Свежий прогон сценария:

  ```
  $ python3 kit/skills/check-skills.py
  ok: скиллы, проверено 8

  $ cd kit/skills && python3 -m unittest discover -p '*_test.py'
  Ran 24 tests in 0.034s
  OK

  $ cd kit/skills/goal-loop && python3 -m unittest discover -p '*_test.py'
  Ran 18 tests in 11.241s
  OK
  ```

## Ревью

- goal_run_test.py не проверяет случай «маркер с текстом после» из постановки. Мутация в run_turn (goal-run.py): заменил сравнение last == "marker: %s" % m на last.startswith("marker: %s" % m), и все 14 тестов прошли зелёными. Тест на ограду (fenced) есть, а на «marker: continue и что-то после» на той же строке нет, хотя постановка отдельно называет оба случая: «маркер в ограде или с текстом после не считается»: исправлено, добавлены test_marker_with_trailing_text_is_not_a_marker и три соседние формы (текст перед маркером, точка в конце, текст после пустой строки); мутация last == -> last.startswith поймана двумя из них (trailing, dotted), красноту проверил и вернул назад

## Выкат

- 2026-08-05 слито: 5212feb, bf541e5, e1e1453, afbe71f, 1d46a66, 7d80aa5, ed19cea, a148498, 94b3e3e, fb9d422

## Проверка после выката

Диспетчер, 2026-08-06, на main после выката поезда (DK-141, DK-142, DK-143).

```
sh в дереве: docs/tasks/*-check.sh (7 одноразовых сценариев закрытых задач),
hooks/quota-refresh.sh, tools/obeycheck/testdata/fake-agent.sh,
плюс три точки входа git без расширения. Раннеров test.sh не осталось ни одного.

python3 -m unittest discover -p '*_test.py'
  hooks                 OK
  tools/devkitctl       OK
  kit/skills            OK
  kit/skills/goal-loop  OK
python3 kit/skills/check-skills.py   ok: скиллы, проверено 8
python3 hooks/check-exec-bit.py      находок нет
тесты на merge и выкат поезда зелёные, бинари пересобраны
```
