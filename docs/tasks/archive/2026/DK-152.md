# DK-152: doctor сверяет версии всех устанавливаемых бинарей, а не mtime четырёх

Задача цели [DK-138](../../DK-138.md), последняя в её нарезке. Решение принято в
[DK-149](DK-149.md), документ `docs/lld/DK-149-binary-delivery.md`:
дизайн сделан, второго круга задача не требует.

## Задача

Сегодня `check_binaries` сравнивает mtime бинаря с mtime `*.go` и знает про
четыре утилиты из шести: `trackctl` и `obeycheck` не проверяет никто.
[DK-150](DK-150.md) научил бинари называть свою версию,
[DK-151](DK-151.md) поставил их из релиза, и доктор остаётся
последним, кто судит по времени файла. Состав правки назван в разделе
«Что делает DK-152» документа DK-149:

- список проверяемых выводится из `tools/*/go.mod`, константа `BINARIES`
  уходит; `trackctl` и `obeycheck` попадают под проверку тем, что они есть;
- `check_binaries` спрашивает у бинаря `--version` и сравнивает коммит с
  `git rev-parse HEAD` основного чекаута; бинарь, не понимающий `--version`,
  это находка «собран до релизной схемы» с той же починкой;
- хвост про грязное дерево: при несохранённых правках `*.go` доктор
  досравнивает mtime и говорит про несобранные правки;
- `--fix`: чекаут на теге и релиз есть, значит скачать; go в PATH, значит
  собрать через `devkitctl build`; ни того, ни другого, значит находка остаётся
  с причиной, как сегодняшнее «собирать нечем»;
- проверка `path_winner` остаётся: чужая копия в PATH и каталог мимо PATH это
  по-прежнему находки;
- доктор называет режим чекаута («на релизе v0.3.0» или «на ветке main,
  впереди релиза»), иначе расхождение версий читается как поломка;
- две находки про релизы: точная, сравнением тега HEAD с новейшим тегом в
  клоне («стоит v0.2.0, вышел v0.3.0»), и косвенная, по mtime `FETCH_HEAD`
  основного чекаута старше 14 дней либо отсутствию файла. В сеть доктор не
  ходит ни в той, ни в другой.

Тесты по разделу «Тесты» того же документа: подставные бинари с заданной
строкой версии на три случая (совпало, разошлось, флага нет), грязное дерево с
несобранными правками, worktree ветки задачи (сравнение идёт с HEAD основного
чекаута и находок не даёт), полнота списка (утилита, добавленная в фикстуру,
попадает под проверку сама), новейший тег в клоне впереди тега HEAD и вровень с
ним, возраст `FETCH_HEAD` по обе стороны порога и его отсутствие. Отдельным
тестом смыкание цикла: после `fetch` с новым тегом косвенная находка гаснет, а
точная зажигается.

Дока едет той же задачей: `tools/devkitctl/README.md` про то, что проверяет
доктор (новые находки про версии и режим чекаута), и снятая оговорка про
DK-152 в разделе «Версии и доставка» `README.md`, если она там осталась.

Границы:

- установка и обновление (`devkitctl update`) уже сделаны DK-151, их не
  трогать; доктор пользуется тем, что есть;
- в сеть доктор не ходит ни одной из проверок, обе находки про релизы считаются
  по локальному клону.

## Сценарий проверки

Агентский.

1. На машине разработчика: `devkitctl doctor` называет режим чекаута и не
   печатает находок про версии, когда бинари собраны из текущего HEAD.
2. Подставной бинарь со старым коммитом даёт находку про расхождение с
   названным режимом чекаута, `--fix` её чинит доступным способом (сборкой на
   машине с go), и повторный прогон молчит.
3. Полнота списка: `trackctl` и `obeycheck` попадают под проверку, а
   константы `BINARIES` в коде не осталось.
4. Находки про релизы: на чекауте, где тег HEAD старше новейшего тега в клоне,
   доктор говорит «стоит X, вышел Y»; свежий `FETCH_HEAD` косвенную находку
   гасит.
5. Тесты: пирамида задачи зелёная, полный набор тестов проекта зелёный,
   `python3 hooks/check-symbols.py` на изменённых файлах молчит.

## Ход работы

- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 40%, снимок 49м назад, протух, сдвига нет), 2026-08-06.

## Ревью

- докстринг devkitctl.py (текст --help) не обновлён: строки про doctor всё ещё говорят «бинари в PATH и не старее исходников» и «--fix... сборка бинарей», хотя сверка теперь идёт по коммиту с названием режима чекаута, а --fix на теге ставит бинари релиза, а не собирает; про находки о вышедшем релизе там не сказано вовсе: исправлено
- две находки check_release («стоит X, вышел Y» и «не ходили N дней») проверены только юнит-тестами update.check_release() в update_test.py; ни один SandboxCase-тест в devkitctl_test.py не заводит на песочнице второй тег впереди HEAD или состаренный FETCH_HEAD и не проверяет, что devkitctl.py doctor реально печатает эти находки, а стык check_machine с update.check_release остаётся без интеграционного теста, хотя это ровно пункт 4 сценария проверки задачи: исправлено

## Выкат

- 2026-08-06 слито: f043a0f, 2a61619, dd48856, a58e857, 343abcf
- 2026-08-06 слито: 32e746b

## Ход работы

- Исполнение: субагент opus/medium по вердикту pick, 2026-08-06.
- Ревью: субагент sonnet/high по вердикту pick, 2026-08-06, два замечания,
  оба исправлены.
- Первый прогон сценария на живой машине задачу завалил: доктор сверял коммит
  бинаря с HEAD, а HEAD двигает и коммит доски, кода не касающийся, так что
  после каждого штатного слияния горели шесть находок. Форвард-фикс считает
  свежесть по последнему коммиту, тронувшему `*.go`, `go.mod` и `go.sum`
  утилиты, и проверяет её через `merge-base --is-ancestor`; ревью правки
  отдельно разобрало границу ослабления (revert, разошедшаяся ветка, чужая
  машина, общий код) и замечаний не нашло.
- По ходу задачи починен дефект сборки: `go build -o` отказывается писать
  поверх чужого файла, а на linux пересборка работающего бинаря дала бы
  `ETXTBSY`; теперь сборка идёт в `<имя>.new` и переезжает `os.replace`.
- Сценарий прогнан заново 2026-08-06 на main, все пять пунктов зелёные.

```
== 1. машина разработчика после слияния и коммита доски
чекаут devkit: на ветке main, впереди релиза v0.3.0
taskctl --version: taskctl v0.3.0-18-g9edb1d8 (9edb1d8926ae)
git rev-parse --short=12 HEAD: af4e81be3f86
находок про версии нет, режим чекаута назван
после doctor --fix (скилл и обёртка): обвязка в порядке

== 2. подставные бинари во временном каталоге назначения
машина: obeycheck не отвечает на --version: собран до релизной схемы версий; ... build
машина: trackctl собран из коммита deadbeef1234 (версия v0.0.1), этого коммита
        в клоне devkit нет; чекаут devkit на ветке main, впереди релиза v0.3.0; ... build
--fix:
починено: obeycheck в <bin>: v0.3.0-19-gaf4e81b (af4e81be3f86)
починено: trackctl в <bin>: v0.3.0-19-gaf4e81b (af4e81be3f86)
повторный прогон: находок про эти две утилиты 0

== 3. полнота списка
grep BINARIES по tools/: пусто, константы не осталось
trackctl и obeycheck проверяются наравне с остальными (см. пункт 2)

== 4. находки про релизы (клон, чекаут отвязан на v9.0.0, в клоне есть v9.1.0)
чекаут devkit: на релизе v9.0.0
машина: на машине стоит devkit v9.0.0, а вышел v9.1.0: обновиться одной
        командой, devkitctl update
машина: за тегами devkit не ходили ни разу (нет .git/FETCH_HEAD): про вышедший
        релиз узнать тут неоткуда, спросить: devkitctl update --check
после git fetch --tags: косвенная погасла, точная осталась

== 5. тесты
полный набор из .devkit/deploy.local прогнан слиянием, зелёный;
tools/devkitctl: 266 тестов
```
