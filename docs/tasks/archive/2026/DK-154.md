# DK-154: shipctl: вставший deploy проваливается по пределу, ожидание фона через Monitor

Задача цели DK-156 «автономный цикл не стоит молча и переживает обрывы»,
закрывает второй пункт её DoD. Чужой очереди не держит и ничьей не ждёт: правка
живёт в конвейере shipctl и от сторожка с перезапуском не зависит.

## Что происходит

Половина инцидента DK-153 (2026-08-06, цель XR-266 в xr-proxy) лежит в самом
конвейере. `shipctl merge` и `ship` гоняют команду выката через `runShell`
(`tools/shipctl/ops.go`) без предела времени: deploy-команда xr-proxy зовёт
`cross`, тому нужен Docker, и с неподнятым Docker Desktop сборка ждала демона
вечно, молча и без вывода. Правило «молчание неотличимо от штатной работы»
конвейер нарушает сам.

Вторая половина той же дыры в процедуре скиллов: единственный не молчащий
способ ждать, Monitor с таймаутом, в скиллах не назван, и сессия городила
самодельные until-циклы на командах, которые сами виснут (`docker info` без
предела). Все сторожа зависли вместе с выкатом, уведомления не пришло ни
одного.

## Чего хотим

- Шаг deploy в `shipctl merge` и `ship` ограничен пределом времени: вставшая
  команда убивается, выкат кончается громким провалом с внятным текстом (что
  висело, сколько ждали, хвост вывода), а не вечным молчанием. Предел
  настраивается в `.devkit/deploy.local`, у умолчания запас к штатной
  кросс-сборке.
- Скиллы, где ждут фон (board-ship, goal-loop), говорят процедуру ожидания:
  Monitor с таймаутом, перевзвод после срабатывания снова Monitor, самодельный
  until на команде, которая может виснуть, под запретом.

## DoD

Синтетический `.devkit/deploy.local` с вечной командой: `shipctl ship`
кончается провалом по пределу за настроенное время, текст называет вставшую
команду. Про Monitor в обоих скиллах есть строки, грепом по SKILL.md.

## Сценарий проверки

Агентский. Гоняется на стенде (временный репозиторий с доской и своим
`.devkit`), настоящий выкат devkit не трогается. На стенде должна лежать хотя бы
одна слитая, но не выкаченная задача, иначе `ship` откажет раньше, чем дойдёт до
команды выката.

1. Предел срабатывает. В `.devkit/deploy.local` команда выката `sleep 600`,
   предел выставлен в несколько секунд. Прогон `shipctl ship`. Ожидание: код
   возврата не 0, время работы близко к пределу и много меньше 600 секунд, а в
   выводе есть слова про предел, текст вставшей команды (`sleep 600`) и время
   ожидания.
2. Штатный выкат не задет. Та же настройка, команда выката `true`. Прогон
   `shipctl ship`. Ожидание: код возврата 0, слов про предел в выводе нет.
3. Умолчание есть. Предел из `.devkit/deploy.local` убран, шаг 1 повторяется с
   умолчанием: `shipctl ship` всё равно кончается провалом, а не висит; величина
   умолчания названа в `tools/shipctl/README.md`, грепом.
4. Процедура ожидания в скиллах. Греп по `kit/skills/board-ship/SKILL.md` и
   `kit/skills/goal-loop/SKILL.md` находит слова про Monitor с таймаутом и про
   запрет самодельного until на команде, которая может виснуть.

## Прогон сценария

Стенд: временный репозиторий во временной директории с доской (XR-001 в In
progress), своим `.devkit/deploy.local`, bare-origin рядом и задачей, слитой
`shipctl merge XR-001 --test true --train` (поезд не выкачен). Гонялся бинарь,
собранный из ветки dk-154; выкат devkit не трогался.

Шаг 1, предел срабатывает (`deploy = sleep 600`, `deploy_timeout = 5s`):

```
ошибка: выкат поезда встал и убит по пределу 5s: команда `sleep 600` не кончилась за 5s; предел двигается ключом deploy_timeout в .devkit/deploy.local, задачи остаются в In progress:

уведомление пропущено: корень ... лежит под /private/tmp, звать некого
код возврата: 1, ждали 6s
```

Уведомитель позван тем же порядком, что на прочих провалах выката, и сам
объяснил, почему на стенде промолчал (корень в песочнице).

Шаг 2, штатный выкат не задет (`deploy = true`, предел тот же):

```
код запушен
поезд выкачен (XR-001)
доска: XR-001 в Check, коммит 4c44cf9
доска запушена
код возврата: 0, ждали 1s
```

Слов про предел в выводе нет.

Шаг 3, умолчание. Величина названа в `tools/shipctl/README.md` («умолчание в 30
минут», строка 323) и стоит константой `defaultDeployTimeout = 30 * time.Minute`
(`tools/shipctl/config.go:35`); что она доезжает до плана выката и без ключа, и
без файла конфига, держит `TestDeployTimeoutConfig` (PASS). На стенде прогон с
убранным ключом и `deploy = sleep 600` через 30 секунд ещё ждал (мгновенного
провала нет, предел не сбился в ноль) и был оборван снаружи: полчаса на стенде
не выстаивались, ветка кода у умолчания и у ключа одна, различается только
величина.

Шаг 4, процедура ожидания в скиллах:

```
kit/skills/board-ship/SKILL.md:86:- Ждать только через `Monitor` с таймаутом: он будит сессию сам, и когда работа
kit/skills/board-ship/SKILL.md:89:- Самодельный `until` (или `while`) на команде, которая может виснуть, под
kit/skills/goal-loop/SKILL.md:197:`Monitor` с таймаутом: он будит сессию и по концу работы, и по концу времени.
kit/skills/goal-loop/SKILL.md:202:Самодельный `until` на команде, которая может виснуть (`docker info`, `ssh`,
```

Тесты проекта командой из ключа `test`: зелено (shipctl, taskctl, agentctl,
trackctl, regcheck, obeycheck, хуки, devkitctl, скиллы, `doctor --layout`).
Краснота новых тестов проверена `regcheck`: «тест краснеет на HEAD и проходит на
текущем коде, регрессия закрыта (тесты: tools/shipctl/timeout_test.go)».

## Ход работы

- Исполнение: субагент opus/medium по вердикту pick (квота: week_all 54%, снимок 1м назад, сдвига нет), 2026-08-07.
- Правка: предел выката в `runShellLimit` с убийством всей группы процессов
  (потомки сборки и ssh переживали бы смерть оболочки), ключ `deploy_timeout` в
  `.devkit/deploy.local` с умолчанием 30 минут, громкий провал с командой,
  временем ожидания и уведомителем в merge, ship и revert; процедура ожидания
  фона в скиллах board-ship и goal-loop.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 54%, снимок 19м назад, сдвига нет), 2026-08-07.

## Ревью

- гонка в runShellLimit при limit<=0 (tools/shipctl/ops.go:63): `return buf.String(), false, <-done`, а по спеке Go операнды return вычисляются лексически слева направо, значит buf.String() читает буфер ДО получения из done, то есть до возврата cmd.Wait() и до завершения внутренних горутин exec.Cmd, копирующих stdout/stderr в тот же buf. go test ./... -race -run TestShipDeployWithinTimeout валит WARNING: DATA RACE ровно на этой строке (запись из os/exec.(*Cmd).Start.func2 против чтения в runShellLimit). Путь limit<=0 это runShell(), которым гоняются тесты в cmdMerge (ops.go:672) и cmdRevert (ops.go:1199), то есть гонка бьёт на каждом merge/revert с командой теста, не только в редком крае. Правка: дождаться <-done в отдельной переменной и только потом звать buf.String(), как уже сделано в ветке limit>0.: исправлено, Wait дожидается в отдельной переменной, буфер читается после него; go test ./... -race в tools/shipctl чист
- gofmt ломается на tools/shipctl/config.go: импорт "fmt" воткнут после "path/filepath" вместо алфавитного места перед "os" (gofmt -l tools/shipctl/config.go его находит; на main файл был чист). Поправить goimports/gofmt.: исправлено

## Выкат

- 2026-08-07 слито: 1074aa1, d89c507, 02f9d5b, bdd19cf, 8b0afe9, ce69149, 5d62702

## Проверка после выката

Прогон диспетчером 2026-08-07 на стенде в scratchpad (свой репозиторий с
доской, bare origin, слитая невыкаченная XR-001), бинари после выката поезда.

1. `deploy = sleep 600`, `deploy_timeout = 5s`: `shipctl ship` кончился кодом 1
   за 5 секунд, текст «выкат поезда встал и убит по пределу 5s: команда
   `sleep 600` не кончилась за 5s; предел двигается ключом deploy_timeout в
   .devkit/deploy.local». Уведомитель позван и сам сказал, что молчит про
   песочницу под /private/tmp.
2. `deploy = true`, предел тот же: код 0, «поезд выкачен (XR-001)», слов про
   предел нет.
3. Умолчание: `defaultDeployTimeout = 30 * time.Minute` в config.go, README
   называет «умолчание в 30 минут» (строка 323). Полчаса не выстаивались,
   ветка кода та же, что в шаге 1, отступление то же, что у исполнителя.
4. Грепы скиллов: board-ship строки 86-89 (Monitor с таймаутом, перевзвод,
   запрет самодельного until), goal-loop строки 189 и 195 (то же в разделе
   ожидания витка).
