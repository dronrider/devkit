# DK-158: devkitctl selfcheck: живость связки после установки проверяется одной командой

## Что происходит

Установка devkit на машину заканчивается строкой «проверить devkit можно
командой devkitctl doctor», и доктор честно проверяет раскладку: файлы на
местах, бинари одной версии с чекаутом, каталог назначения в PATH, пакеты,
обвязка проекта. Чего он не проверяет, так это того, что связка живая: доска
правится, ранг считается, вердикт `pick` выдаётся, ветка задачи заводится в
своём дереве, слияние проходит.

Проверка эта сегодня ручная и на пять команд, с уборкой ещё на три:

```
devkitctl new -C ~/projects/myproj --prefix MP
devkitctl doctor -C ~/projects/myproj
taskctl -C ~/projects/myproj add --title "проверка связки" --type task --rank "25+5+1+0+0" --cost S
shipctl -C ~/projects/myproj status && agentctl pick MP-001
shipctl -C ~/projects/myproj start MP-001
git worktree remove ~/projects/myproj-mp-001 && git branch -D mp-001 && taskctl close MP-001
```

Отменить `start` нечем, отдельной команды у `shipctl` нет, так что за пробой
человек убирает руками и на своём же проекте. Помнить этот порядок неоткуда: в
доке его нет, он собирается по README трёх утилит.

Повод пришёл от пользователя после установки devkit на вторую машину: вопрос
был ровно «как убедиться, что devkit работает на моём проекте».

## Чего хотим

Команду `devkitctl selfcheck`, которая гоняет тот же круг сама и за собой
убирает. Круг: временный проект во временном каталоге, подключение, строка на
доске, вердикт `pick`, ветка задачи в отдельном дереве, слияние с зелёными
тестами. Отчёт по шагам, каждый со своим исходом, и внятный конец: связка жива
либо шаг такой-то не прошёл, вот вывод.

Настоящие проекты и HOME при этом не трогаются. Стенд самопроверки devkitctl
это уже умеет (`tools/devkitctl/testenv.py`): временный HOME, подставной PATH,
синтетический проект, уборка в любом исходе.

Открытые развилки, которые снимает исполнитель, а не постановка:

- граница с тестами devkit. Тесты гоняются на стенде из исходников и до
  установки, selfcheck на живой машине и после неё, поверх установленных
  бинарей: он проверяет не код, а собранное и разложенное. Из этого следует,
  какие куски стенда переиспользуются, а какие нет;
- берётся ли проект пользователя или всегда временный. Временный безопаснее и
  повторяем, но не отвечает на вопрос «работает ли на моём проекте»;
- что selfcheck делает с находками `doctor` по машине: считает ли их своим
  провалом, называет ли отдельно, гонит ли доктора вообще;
- гонять ли слияние с настоящим выкатом. У временного проекта команда выката
  своя, подставная, и тогда круг замыкается без риска для чего-либо снаружи.

## DoD

1. `devkitctl selfcheck` на машине, где devkit только что поставлен, проходит
   круг целиком и печатает отчёт по шагам, из которого видно, что живо, а что
   нет.
2. Сломанная связка ловится: подпорченное окружение (бинарь не той версии,
   утилита не в PATH, доска не заводится) даёт провал с названным шагом и
   выводом, а не молчание.
3. После прогона на машине не остаётся ничего: ни временного проекта, ни веток,
   ни worktree, ни правок в HOME. Проверяется слепком до и после.
4. Настоящие проекты и настоящий HOME в прогоне не участвуют вовсе.
5. Команда названа в доке установки как способ проверки: `CONNECT.md` и
   `tools/devkitctl/README.md`.
6. Своя пирамида тестов, полный набор тестов проекта зелёный.

## Ход работы

Исполнение: подпроцесс glm-5.3/high по вердикту pick (квота: объявления нет,
корректор выключен), 2026-08-15.

Развилки сняты так:

- тесты гоняются на стенде из исходников с собранными бинарями (образец
  FreshConnectTest), selfcheck зовёт утилиты по имени из PATH и работает
  поверх установленных бинарей; из стенда переиспользуется только Sandbox
  его тестами, сам круг стендовых заглушек не видит;
- проект всегда временный: «работает ли на моём проекте» закрывает
  `devkitctl doctor -C <проект>`, круг проверяет конвейер, а не конкретную
  доску;
- доктор отдельный шаг круга: код 1 (находки раскладки) не провал, их отчёт
  печатается в конце; код 2 (ошибка запуска) останавливает круг;
- выкат подставной: команда пишет метку в свой временный каталог, тесты
  пустой командой на python3 (без внешних утилит: прогон идёт через sh -c в
  том же PATH), autonomous=true, чтобы слияние само довело задачу до Check.

По ходу вскрылись детали конвейера, без которых круг не замыкается, и круг их
закрывает сам: временный проект получает собственный голый origin
(`autonomous=true` поднимает пуш в start и merge, без origin круг умирал на
переводе доски с «No configured push destination»), в файл задачи пишется
сценарий проверки (ворота слияния без него ветку не пускают) и раздел
«Проверка» (ворота закрытия агентского вида требуют непустой), факт выката
сверяется меткой, а не строкой «ок» в отчёте слияния.

Стенду не хватало `sh` в подставном PATH: утилиты гоняют тесты и выкат через
`sh -c`, и без него краснел сам конвейер, а не проверяемая раскладка. `sh`
добавлен в SYS_TOOLS testenv.py.
- Снаружи: проверка после выката, 2026-08-16 00:07-00:09.

## Сценарий проверки

Агентский вид: шаги прогоняются после выката на самой машине.

1. `devkitctl selfcheck` из чекаута devkit.
2. Ожидание: каждый шаг круга отвечает «ок» (доктор допускает находки
   раскладки с кодом 1, они перечисляются в конце), последняя строка
   «связка жива: круг прошёл целиком», выход 0.
3. Уборка за кругом: команда сама печатает «за кругом осталось», если в
   репозитории временного проекта остались ветки задачи (слияние обязано унести
   их с собой) или место круга после него не пусто; отсутствие строки и есть
   чистота. Настоящий дом не трогается: сабпроцессы круга получают временный
   HOME (утилиты devkit пишут в ~/.devkit и без ведома вызвавшего), автор
   коммитов идёт переменными `GIT_AUTHOR_*` и `GIT_COMMITTER_*`, не
   ~/.gitconfig.
4. Подпорченное окружение: без taskctl в PATH круг падает на шаге «строка на
   доске» с выводом «taskctl: нет такой команды в PATH», без утилит devkit
   вовсе на шаге «доктор». Оба провала покрыты тестами selfcheck_test.py.

## Проверка

Сценарий прогнан на самой машине после выката, 2026-08-16.

Шаг 1 и 2, `python3 tools/devkitctl/devkitctl.py selfcheck` из чекаута devkit:

```text
самопроверка связки devkit: временный проект в /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/devkit-selfcheck-ldhgdan9/proj
  доктор: не прошёл
  подключение: ок
  строка на доске: ок
  вердикт pick: ок
  ветка задачи: ок
  сценарий задачи: ок
  слияние с выкатом: ок
  закрытие задачи: ок
доктор назвал находки по машине, круг при этом прошёл:
  находок: 18
  чекаут devkit: на ветке main, впереди релиза v0.12.0
  не git-репозиторий: <путь>/proj; звать из подключённого проекта либо из чекаута devkit
  <и ещё 16 находок раскладки временного дома: почтальон, хуки квоты и проверок,
  права, deny-правила, агенты, скиллы, сторожок, дашборд, обёртка и PATH, CLAUDE.md,
  снимок квоты>
связка жива: круг прошёл целиком
```

Выход 0. Шаг «доктор: не прошёл» это код 1 с находками раскладки временного
дома (хуки не подключены, нет обёртки и PATH, нет CLAUDE.md), круг его провалом
не считает и перечисляет находки в конце; все семь остальных шагов «ок», строка
«за кругом осталось» не вышла.

Шаг 3, уборка: каталог места круга снесён сам (ls отвечает «No such file or
directory»), в ~/.devkit/runs записей SC- нет, каталогов devkit-selfcheck в $TMPDIR
нет, строк SC-001 в настоящем ~/.devkit/notify.log после живого прогона не
прибавилось (три старых записи от прогонов и саботажей до правки HOME остаются).

Шаг 4, подпорченное окружение: покрыт тестами selfcheck_test.py
(test_missing_utility_fails_named_step, test_broken_path_fails_first_step_with_output),
в живом прогоне не повторялся, чтобы не портить PATH машины.

Шаг 4 сценария про подпорченное окружение дополнительно проверен саботажем
между кругами ревью: живой прогон без taskctl в PATH падал на шаге «строка на
доске» с выводом «taskctl: нет такой команды в PATH».

## Ревью

- selfcheck.py:59 Нарушен DoD 4: в боевом прогоне круг пишет в настоящий HOME. run() наследует os.environ, а перевод доски в Check пишет строку в ~/.devkit/notify.log (живой прогон оставил запись «повод task_check ... задача SC-001 проект proj»), и провал шага «закрытие задачи» или непоставленная метка выката оставляет запись ~/.devkit/runs/SC-001-<путь>.run (проверено саботажем close: после его сбоя осталась SC-001-private-tmp-dk158-sabotage-proj.run). Тесты этого не видят: их cmd_run подставляет home стенда. Чинить подстановкой HOME временного каталога в боевом run для сабпроцессов круга, и привести утверждения selfcheck.py:13, tools/devkitctl/README.md:279, CONNECT.md:64, docs/tasks/DK-158.md:112 к реальному поведению; исправлено: боевой main() подменяет дом каталогом круга (run(home=...)), проверки до/после по mtime настоящего notify.log чисты, все четыре утверждения переписаны
- selfcheck.py:236 Нарушен DoD 3 «После прогона на машине не остаётся ничего»: слепок snapshot() меряет только каталог where и ветки проекта, а запись ~/.devkit/runs из замечания 1 остаётся навсегда при провале close (доказано живым саботажем), строка notify.log не убирается вовсе. Слепок обязан покрывать и настоящий HOME, раз круг в него пишет, либо круг обязан в него не писать; исправлено: круг в настоящий дом больше не пишет (замечание 1), слепок судит ветки задачи до сноса проекта и пустоту места после
- selfcheck.py:61 run() тащит в круг переменные родителя целиком (dict(os.environ)), включая PYTHONPATH, PYTHONDONTWRITEBYTECODE и прочее сеансовое: на машине с PYTHONPATH, указывающим в другой чекаут, devkitctl-обёртка круга подхватит чужой selfcheck/модули. У круга по доке «тот PATH, что достанется человеку», окружение должно собираться так же явно, как PATH: чистая база, PATH и GIT_*: отклонено, прямой подмены модулей нет: каталог скрипта в sys.path стоит раньше PYTHONPATH, проверено живьём; ядро замены окружения уже покрыто замечанием 1
- selfcheck_test.py:38 не покрыт главный провал круга, обман слияния, который ловит stamp_result (selfcheck.py:236): слияние отвечает «ок», а метки выката нет. Это единственное место, где честность отчёта зависит от перепроверки, и мутация «выкат молчит» обязана ронять шаг «слияние с выкатом» с текстом «выкат не оставил метку». Сейчас три теста покрывают только живой круг и отсутствующие утилиты; исправлено: добавлен test_silent_deploy_fails_merge_step, подменяет deploy на пустую команду и требует провал шага с названной причиной
- selfcheck.py:41,45, selfcheck_test.py:51 «barebone-окружение» это англицизм, в репозитории он не встречается ни разу вне этих трёх мест; живой русский: «минимальное окружение», «голое окружение». Заодно в selfcheck_test.py:51 опечатка «всегда назвает» вместо «называет»; исправлено: переписано на «минимальное окружение», опечатка убрана
- selfcheck.py:41,45 двойной комментарий про sh -c и touch(1)/true(1) повторяет одну мысль дважды (комментарий DEPLOY_CMD и комментарий TEST_CMD почти дословно); правило: комментарии только там, где «почему» неочевидно. Один раз у констант достаточно, второй лишь повторяет его; исправлено: сведено в один комментарий над парой констант
- selfcheck.py:82 проверка веток в слепке мертва: snapshot() смотрит (where/.git), а репозиторий лежит в where/proj/.git, так что for-each-ref не выполняется никогда, и «ветки: -» в обоих слепках не результат проверки. Плюс сам каталог проекта сносится rmtree до слепка «после», и остаток ветки sc-001 в репозитории проекта неотличим от чистоты по построению. DoD 3 обещает слепком «ни веток», поэтому проверку надо делать до rmtree по proj/.git либо честно убрать её из слепка и доки; исправлено: слепок заменён на leftover_branches(), спрашивает proj/.git до сноса и исключает основную ветку
- selfcheck.py:84 (вызов на :281) проверка остаточных веток не закреплена ни одним тестом: мутация «leftover_branches всегда возвращает пустой список» оставляла все четыре теста зелёными, то есть смерть проверки уборки не видна сюите. Чинить тестом-саботажем в духе test_silent_deploy: обёртка над cmd_run, оставляющая лишнюю ветку в репозитории проекта, с требованием «за кругом осталась ветка задачи» в отчёте; исправлено: добавлен test_leftover_branch_reported, саботаж подкладывает ветку sc-002 в репозиторий проекта на перехвате вызова слияния, до его прогона, и требует её в отчёте; мутация «пустой список на любом входе» прогнана повторно, тест роняет
- selfcheck_test.py:135 комментарий саботажа переворачивает момент подкладки: «когда слияние уже прошло и репозиторий проекта ещё жив», а обёртка зовёт саботаж до прогона перехваченной команды, ветка ложится до shipctl merge и обязана пережить его; тест timing-чувствительный, и читатель с таким комментарием ищет подкладку после слияния и не находит; исправлено: переписан на «на перехвате вызова слияния, до его прогона: раньше репозитория проекта ещё нет, позже круг снесёт проект»
- доска не доехала до origin: пять из шести коммитов ветки трогают docs/TASKS.md или docs/tasks/, а ветки dk-158 на origin нет (ls-remote), правило доски требует пушить такие коммиты сразу и без просьбы, отставший remote при параллельных сессиях оборачивается конфликтами; пуш из этой сессии проверен и проходит, чинится одним push ветки, в него же уходит коммит этого замечания; исправлено: вручную смешанный пуш не проходит (рубеж pre-push пускает доску без утилит только чистым диффом, новая ветка не проходит вовсе), коммиты едут в origin пушем слияния автономного режима, который идёт следом за этой правкой; сам пуш откладывался правилом «слияние после чистого ревью», и четвёртый круг его требовал раньше слияния, что без утилиты и не исполнить

## Выкат

- 2026-08-16 слито: 7af98d3b, db207218, 1b509ca6, 9f5484e6, 07876313, 395a8f0c, d1dfa321, f22c305a
