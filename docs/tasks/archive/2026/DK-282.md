# DK-282: dashboard: флеш-уведомление нечем закрыть

записан 2026-08-12, оформлен 2026-08-12

## Что происходит

У всплывающего уведомления единственное действие это переход в ленту:
`showFlash` в `tools/dashboard/static/app.js` вешает на карточку один
обработчик, который убирает её и уводит на экран ленты. Крестика нет,
смахивание не ловится, живёт карточка восемь секунд, копится до трёх. На
телефоне три карточки закрывают экран целиком, и единственный способ их
убрать уводит человека с того экрана, где он работал. Поймано на приёмке цели
DK-112 2026-08-12.

## Чего хотим

Уведомление закрывается на месте: крестик на карточке и смахивание пальцем, а
на узком экране очередь держится в одну карточку.

## DoD

Нажатие на крестик убирает карточку и оставляет открытый экран нетронутым,
смахивание делает то же самое, на узком экране одновременно висит не больше
одной карточки. Держит это тест раскладки флеша на узкой ширине.

## Сценарий проверки (агентский)

1. `cd tools/dashboard && GOWORK=off go test . -run 'TestStaticFlash' -v`:
   проходят проверки крестика со `stopPropagation`, порога смахивания
   (`flashSwiped` прогоняется в node на границах 47 и 48 пикселей) и правила
   узкого экрана, где видна одна карточка; код 0.
2. `cd tools/dashboard && GOWORK=off go test .`: пакет целиком зелёный, код 0.

Итог проверяется кодами возврата обеих команд, не на глаз. Живое касание
пальцем тестами не воспроизводится: смахивание на телефоне пользователь
увидит на дашборде после выката, очередь выката это не держит.

## Ранг

`25+5+1+0+0 = 31`, P2.

- Серьёзность 25 (Major). На телефоне уведомления закрывают рабочий экран, а
  единственный способ их убрать уводит с него; обход есть, подождать восемь
  секунд.
- Ценность 5. Заход с телефона перестаёт спотыкаться о собственную ленту.
- Неопределённость 1. Правка целиком в клиенте, поведение названо.
- Поправка на баг 0. Карточка задумывалась гаснущей сама, это доработка.
- Рычаг 0. Дальше ни на что не влияет.

## Ход работы

- Исполнение: sonnet/high по вердикту pick, 2026-08-12.
- Вычитка: 5 файлов, 1 правка, 0 пометок, 2026-08-12.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 37%, снимок 53м назад, протух, сдвига нет), 2026-08-12.

## Выкат

- 2026-08-12 слито: 3f4808e2, a0f5d8f5, 28f26f51, 085de113

### Прогон после выката (2026-08-13)

`cmdout sh -c 'cd tools/dashboard && GOWORK=off go test . -run "TestStaticFlash" -v'`
на выкаченном main:

```
=== RUN   TestStaticFlashWorthy
--- PASS: TestStaticFlashWorthy (0.34s)
=== RUN   TestStaticFlashNotice
--- PASS: TestStaticFlashNotice (0.00s)
=== RUN   TestStaticFlashSwiped
--- PASS: TestStaticFlashSwiped (0.26s)
=== RUN   TestStaticFlashNarrowQueue
--- PASS: TestStaticFlashNarrowQueue (0.00s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	4.210s
```

Итог: PASS. Живое касание пальцем остаётся пользователю на дашборде, очередь
выката оно не держит.
