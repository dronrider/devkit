# DK-299: Критерии вида приёмки в ACCEPTANCE.md и вопрос про вид в груминге, нарезке цели, работе и ревью

## Ход работы

- Исполнение: подпроцесс glm-5.2/medium по вердикту pick (квота: объявления нет, корректор выключен), 2026-08-13.
- Вычитка: 1 файл (ACCEPTANCE.md), 3 правки, 1 пометка, 2026-08-13. Пометка:
  «след» в значении «запись/место пересмотра» как кандидат в словарь (в
  документе употребляется последовательно, в общем техническом русском такого
  термина нет) -&gt; оставлена пользователю.
- Источник дизайна: `docs/lld/DK-292-check-kind.md`, разделы «Решение 1» (шесть
  барьеров), «Приёмка» (форма ответа), «Прогон критериев по архиву» (примеры),
  «Решение 5» (общий документ рядом с RANKING.md), «Затрагиваемая
  документация».
- Сделано:
  - `ACCEPTANCE.md` в корне: три вида и умолчание, таблица шести барьеров с
    обходами (глаза 3, доступ 4, необратимость 4, секрет 3, согласие 1,
    событие 3), форма ответа, два правила поверх списка, развилка «обход
    неисполним vs шаг не написан», особые случаи «согласие» (LLD по умолчанию
    user, без повышения) и «глаза» (эталон макета, два вида ссылки, проверка
    `git show`), пересмотр по ходу работы, примеры из архива (DK-064, DK-232,
    DK-272, DK-223, DK-284, LLD/DK-216), места назначения вида.
  - `RULES.board.md`, п. 6 «Трекинг задач»: три фразы про вид при заведении
    строки, умолчание и ссылку на `ACCEPTANCE.md`.
  - `README.md`, раздел «Раскладка»: `ACCEPTANCE.md` в перечне корневых
    файлов.
  - `tools/devkitctl/layout.py`: `ACCEPTANCE.md` в белом списке `TOP_LEVEL`;
    `layout_test.py`: порог 14 -> 15 и переименование теста.
  - `kit/skills/board-groom`: шаг 12 назначения вида по `ACCEPTANCE.md` перед
    оформлением строки, флаги `--accept`/`--barrier` в `taskctl add`.
  - `kit/skills/board-task`: сценарий пишется по виду из `ACCEPTANCE.md`,
    раздел «Приёмка» с перебором, самодостаточность агентской части,
    пересмотр `set --accept`.
  - `kit/skills/board-ship`: ревью судит вид по содержанию сценария и
    перебору обходов, не по пометке; ненаписанный шаг под исполнимый обход
    это замечание сценария, а не повод поднять вид.
  - `kit/skills/goal-loop`: нарезка назначает вид по `ACCEPTANCE.md`;
    пользовательский и смешанный вид уходят в хвост цикла.
  - `kit/skills/goal-start`: в диалоге про DoD критерии вида живут в
    `ACCEPTANCE.md`.
  - `kit/skills/board-batch`: проверка по сценарию читает вид из строки и
    зовёт человека для mixed/user.
  - `kit/agents/review-{low,medium,high,xhigh}.md`: проход ревью судит вид по
    `ACCEPTANCE.md` и перебору обходов.
- Ревью: подпроцесс glm-5.2/high по вердикту pick (квота: объявления нет, корректор выключен), 2026-08-13.

## Сценарий проверки

Часть проверок агентские (машина гоняет сама), часть глазная (формулировки
примеров обходов из LLD выбраны на месте по материалу раздела «Прогон по
архиву», и их убедительность судит ревью).

### Агентское

1. `ACCEPTANCE.md` лежит в корне и проходит `devkitctl doctor --layout`
   без находки про лишний файл:

   ```sh
   python3 tools/devkitctl/devkitctl.py doctor --layout
   ```

   Ожидание: «раскладка в порядке».

2. Белый список раскладки и перечень README совпадают, сторож `readme_gaps`
   молчит:

   ```sh
   cd tools/devkitctl && python3 -m unittest layout_test -v
   ```

   Ожидание: 20 тестов ок, включая `test_all_fifteen_entries_are_named` и
   `test_lists_match`.

3. В `ACCEPTANCE.md` ровно шесть барьеров и у каждого блока обходов, а
   распределение числа обходов (3/4/4/3/1/3) совпадает с LLD «Решение 1»:

   ```sh
   grep -c '^| глаза\|^| доступ\|^| необратимость\|^| секрет\|^| согласие\|^| событие' ACCEPTANCE.md
   ```

   Ожидание: 6 (строка заголовка таблицы). Число обходов сверяется по тексту
   под таблицей и по примерам перебора (DK-064 глаза 3, DK-232 необратимость
   4, DK-272 событие 3, DK-223 доступ 4).

4. Названные ссылки из правил и скиллов реально ведут на `ACCEPTANCE.md`, и
   файл лежит по тем же путям, что и `RANKING.md`:

   ```sh
   grep -l 'ACCEPTANCE.md' RULES.board.md README.md \
     kit/skills/board-groom/SKILL.md kit/skills/board-task/SKILL.md \
     kit/skills/board-ship/SKILL.md kit/skills/goal-loop/SKILL.md \
     kit/skills/goal-start/SKILL.md kit/skills/board-batch/SKILL.md \
     kit/agents/review-low.md kit/agents/review-medium.md \
     kit/agents/review-high.md kit/agents/review-xhigh.md
   test -f ACCEPTANCE.md && echo ok
   ```

   Ожидание: 12 файлов в выдаче `grep -l` и `ok`.

5. Символы чистые по всему изменённому набору:

   ```sh
   python3 ~/projects/devkit/hooks/check-symbols.py \
     ACCEPTANCE.md RULES.board.md README.md \
     kit/skills/board-groom/SKILL.md kit/skills/board-task/SKILL.md \
     kit/skills/board-ship/SKILL.md kit/skills/goal-loop/SKILL.md \
     kit/skills/goal-start/SKILL.md kit/skills/board-batch/SKILL.md \
     kit/agents/review-low.md kit/agents/review-medium.md \
     kit/agents/review-high.md kit/agents/review-xhigh.md \
     tools/devkitctl/layout.py tools/devkitctl/layout_test.py
   ```

   Ожидание: exit 0.

6. Самопроверка скиллов зелёная (тела скиллов под правилами):

   ```sh
   python3 kit/skills/check-skills.py
   ```

   Ожидание: «ok: скиллы, проверено 10».

7. Прогон критериев по выборке даёт те же виды, что в LLD «Прогон по архиву».
   Берутся строки архива с суффиксом `[приёмка: ...]` (заведённые после DK-298)
   и файлы задач с разделом «Приёмка»; для каждой считается вид по шести
   барьерам `ACCEPTANCE.md` и сверяется с записанным. На момент DK-299 архив
   ещё не накопил задач с суффиксом (DK-298 вкодился одновременно), поэтому
   счёт пока пуст, и шаг этот проверяет готовность процедуры, а не число. Когда
   появятся строки `[приёмка: ...]`, та же команда даст наполнение:

   ```sh
   grep -c '\[приёмка:' docs/TASKS-archive.md || true
   ```

   Ожидание на момент работы: 0 (архив ещё не накопил). Контрольный счёт
   отложен до наполнения архива, как и сказано в LLD «Решение 6».

### Глазное

8. Формулировки примеров обходов в `ACCEPTANCE.md` (DK-064, DK-232, DK-272,
   DK-223, DK-284) сверяются с разбором тех же задач в LLD «Прогон критериев
   по архиву»: исходы и причины те же, число строк перебора равно числу
   обходов барьера. Судит ревью.

## Ревью

- board-groom/SKILL.md: после вставки шага 12 «Вид приёмки» нумерация хвоста не сдвинулась. «Переписать файл разбором» (стр. 87) и «Собрать вопросы» (стр. 91) оба под номером 13, а «Сверка после ответов» (стр. 98) и «По ответам довести» (стр. 105) остались 14 и 15, хотя после вставки должны идти 15 и 16. Перенумеровать шаги с бывшего 13 до конца списка.: исправлено, шаги Собрать/Сверка/По ответам перенумерованы в 14/15/16, ссылка (шаг 12) -> (шаг 13)
- goal-loop/SKILL.md:57-58: при правке шага 3 перенос разорвал фразу посередине. На одной строке «Строки заводит `taskctl add --link`», на следующей «со ссылкой на файл цели,», а в исходнике это была одна логическая строка, и перенос встал не на границе мысли. Собрать фразу обратно или перенести разрыв на границу смысла.: исправлено, фраза taskctl add --link и «со ссылкой на файл цели» собрана в одну строку

## Выкат

- 2026-08-14 слито: 6184d850, fc680ff2, 002e3530, 6d8e3a9e

## Проверка

- 2026-08-14 агентский сценарий пройден (диспетчер):
  1. `doctor --layout`: раскладка в порядке
  2. `layout_test`: 20 тестов ok (`test_lists_match`, `test_named_files_exist`)
  3. шесть барьеров в `ACCEPTANCE.md`: 6
  4. ссылки на `ACCEPTANCE.md`: 12 файлов, файл на месте
  5. `check-symbols` по изменённому набору: exit 0
  6. `check-skills`: ok, проверено 10
  7. суффиксов `[приёмка:` в архиве: 0 (архив ещё не накопил, как и сказано в сценарии)
  глазное п. 8 судит ревью, замечаний по `ACCEPTANCE.md` не было
