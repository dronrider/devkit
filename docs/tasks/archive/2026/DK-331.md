# DK-331: Раскладка скиллов везёт один SKILL.md, вычитка на машине без пар и словаря

записан 2026-08-14

## Черновик

Раскладка скилла proofread теряет pairs, dictionary и corpus.

## Что происходит

`check_skills` (`tools/devkitctl/devkitctl.py`, строки 1085-1099) обходит
`kit/skills/*/SKILL.md` и копирует ровно один файл на скилл. У скилла
`proofread` рядом со `SKILL.md` лежат три спутника (`pairs.md` 6 Кб,
`dictionary.md` 1,8 Кб, `corpus.md` 8,7 Кб), и на машину они не уезжают.
Проверено 2026-08-16: в `~/.claude/skills/proofread/` лежит один `SKILL.md`,
в `kit/skills/proofread/` четыре файла.

По `SKILL.md` (строка 35) субагент до прохода читает `pairs.md` и
`dictionary.md`, а `corpus.md` держит сторожевой прогон по 16 плохим фразам.
Пары дают примеры по восьми пунктам типологии, словарь называет термины devkit,
которые вычитка принимает как свои. Без словаря субагент правит «регкоммит» или
«вердикт pick» как выдуманный термин, без пар работает по одним названиям
пунктов.

В самом devkit потери не видно: сессия сидит в чекауте и читает
`kit/skills/proofread/pairs.md` по относительному пути. Дефект вылезает в чужом
проекте, где скилл берётся из `[skills] dir` харнеса, а чекаута devkit под
рукой нет.

Раскладка поступает так по решению, а не по недосмотру:
в `tools/devkitctl/README.md` (строки 796-802) прямо написано «соседняя проза
в `kit/skills/` на машину не уезжает». Решение писалось под DK-054, когда каталог
скилла состоял из одного файла, а спутники пришли позже с DK-184, и стык двух
задач не проверили. Значит правится и код, и это место README.

## Чего хотим

Скилл доезжает на машину целиком, а не заголовком. Развилка открыта: везти весь
каталог скилла либо только файлы, названные внутри `SKILL.md`; сверять спутники
на расхождение с devkit так же, как сверяется `SKILL.md`, либо ограничиться
раскладкой; убирать ли с машины файл, которого в devkit больше нет.

Смежное, решить заодно или назвать вслух: `kit/skills/goal-loop/` держит
`goal-run.py` с тестом, и эта оболочка зовётся путём в чекауте
(`python3 <чекаут>/kit/skills/goal-loop/goal-run.py`), а не с машины. Тест
скилла `check-skills.py` лежит там же, и на машину его тоже не кладут.

## Развилки

- Что везти: каталог скилла целиком, а не файлы, названные внутри `SKILL.md`.
  Имена спутников стоят в теле скилла прозой, и парсер прозы отставал бы от
  каждой правки, причём отставание видно только в чужом проекте, где чекаута
  devkit нет. Каталог как единица раскладки проверяется тем, что файл в нём
  лежит. Исключение одно и машинное: `__pycache__` и точечные файлы заводит
  прогон тестов и файловый менеджер, на машине они горели бы расхождением.
- Сверка спутников: та же, что у `SKILL.md`, побайтовая. Недостающий или
  правленный руками спутник даёт находку «разошёлся скилл» с рубежом основного
  чекаута, а скилл, у которого на машине нет и `SKILL.md`, остаётся находкой
  «не разложен». Находка по-прежнему одна на скилл, а не на файл: строка на
  каждый спутник вернула бы простыню, из-за которой в DK-157 всё сворачивали.
- Лишний файл на машине: не удаляем и находкой не считаем. Свежий `SKILL.md`
  на пропавшего спутника не ссылается, и никто его не читает, а чистить чужой
  `~/.claude` автоматике не по чину.
- Оболочка `goal-loop/goal-run.py` доезжает на машину вместе с каталогом, но
  вторым входом не работает: цикл цели зовётся путём в чекауте, потому что
  правится вместе с остальным кодом devkit. Самопроверка `check-skills.py`
  лежит прямо в `kit/skills/`, без своего каталога, и под раскладку не
  попадает вовсе. Оба решения записаны в `tools/devkitctl/README.md`.

## DoD

- `devkitctl doctor --fix` кладёт в `[skills] dir` каталог скилла целиком:
  после прогона в `~/.claude/skills/proofread/` лежат `pairs.md`,
  `dictionary.md` и `corpus.md`;
- недостающий или разошедшийся спутник это находка `doctor` с тем же текстом
  про эталон основного чекаута, что у самого `SKILL.md`;
- решение переписано в `tools/devkitctl/README.md` на месте прежнего «соседняя
  проза на машину не уезжает», и там же названо, что делать с оболочкой
  `goal-loop` и с самопроверкой скиллов;
- тест в `tools/devkitctl/devkitctl_test.py` гоняет раскладку в песочнице с
  подменённым HOME и падает на старом коде.

## Ход работы

- Вычитка: 1 файл, 4 правки, 0 пометок, 2026-08-16.
- Вычитка: 2 файла, 6 правок, 0 пометок, 2026-08-25.
- regcheck --base main: тест краснеет на main, на HEAD ветки проходит, 2026-08-25.
- Разработка: субагент opus/medium по вердикту pick (квота: week_all 9%, week_max 0%, снимок 7м назад, сдвига нет), 2026-08-25 01:09-01:25.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 10%, week_max 0%, снимок 3м назад, сдвига нет), 2026-08-25 01:25-01:50.
- Снаружи: проверка после выката, 2026-08-25 01:50-01:53.

## Ревью

- Сценарий проверки: первый шаг это прогон юнит-тестов ветки (MachineContourTest), а не воспроизведение бага на живом состоянии; шаги 2-3 гоняют doctor на реальной машине, но не фиксируют явно состояние «до фикса» (нет ls ~/.claude/skills/proofread перед --fix, показывающего только SKILL.md). Живая машина сейчас реально в состоянии бага (проверено: в ~/.claude/skills/proofread лежит только SKILL.md), это и нужно поймать первым шагом по образцу DK-463/DK-514, где явно расписано «до фикса»/«после фикса», юнит-тест ветки же дополнение, а не замена: исправлено
- Ход работы: запись «regcheck: тест краснеет на HEAD и проходит на текущем коде» читается задом наперёд и путает, ведь HEAD это и есть текущий (исправленный) код ветки, на нём тест обязан быть зелёным, а не красным. Прогон regcheck --base main подтверждает обратное тому, что написано: тест краснеет на main (базе до правки) и проходит на HEAD ветки. Переписать запись с явным указанием базы (main), чтобы не читалось как «тест ломается на исправленном коде»: исправлено

## Сценарий проверки

Агентский: первый шаг ловит живое состояние машины, где скилл разложен одним
заголовком, а не показывает правку.

1. `ls ~/.claude/skills/proofread` до фикса. Ожидание: один `SKILL.md`, пар,
   словаря и корпуса рядом нет, хотя в `kit/skills/proofread/` лежат четыре
   файла.
2. `python3 tools/devkitctl/devkitctl.py doctor` из дерева ветки. Ожидание:
   находка «машина: разошлось 2 скилла в ~/.claude/skills: goal-loop,
   proofread» с рубежом основного чекаута и командой починки оттуда, файлы на
   машине не тронуты (шаг 1 повторяется тем же выводом).
3. После слияния `python3 tools/devkitctl/devkitctl.py doctor --fix` из
   основного чекаута, дальше снова `ls ~/.claude/skills/proofread`. Ожидание:
   в отчёте «починено: обновлено 2 скилла в ~/.claude/skills», рядом со
   `SKILL.md` легли `pairs.md`, `dictionary.md` и `corpus.md`, а повторный
   `doctor --fix` про скиллы молчит.
4. Дополнением юнит-тест ветки:
   `cd tools/devkitctl && GOWORK=off python3 -m unittest devkitctl_test.MachineContourTest`
   -> OK, среди прогонов `test_04b_missing_skill_companion`.

## Проверка

Прогон 2026-08-25 на выкаченном main, все четыре шага сценария зелёные:

- шаг 1, `ls ~/.claude/skills/proofread` до фикса -> один `SKILL.md`, при том
  что в `kit/skills/proofread/` лежат четыре файла;
- шаг 2 прогнан ревьювером из дерева ветки до слияния -> находка «машина:
  разошлось 2 скилла в ~/.claude/skills: goal-loop, proofread» с рубежом
  основного чекаута, файлы на машине не тронуты;
- шаг 3, `doctor --fix` из основного чекаута -> «починено: обновлено 2 скилла
  в /Users/rider/.claude/skills, devkit ушёл вперёд: goal-loop, proofread»,
  той же строкой починен контур `~/.devkit/claude-glm/skills`; после прогона
  рядом со `SKILL.md` лежат `pairs.md`, `dictionary.md` и `corpus.md`,
  повторный `doctor --fix` про скиллы молчит;
- шаг 4, `GOWORK=off python3 -m unittest devkitctl_test.MachineContourTest` ->
  Ran 23 tests OK за 54.6s, среди прогонов `test_04b_missing_skill_companion`.

## Выкат

- 2026-08-25 слито: a6ca7357, aeec4a4c, 22e563cd
- smoke прогнан, 2026-08-25
