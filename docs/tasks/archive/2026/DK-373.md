# DK-373: Живой вопрос грумера: вход разговора, draft ask, ручка реплики, блок пачки, скилл [после DK-372]

Постановка: [lld/DK-354-draft-screen.md](../../../lld/DK-354-draft-screen.md)

## DoD

## Расхождение с решениями DK-430

2026-08-18, нарезка цели DK-397: постановка LLD DK-354 писала черновику свой
вход `.devkit/draft-<ID>.mail` с отметками доставки, а разговор он взял общий.
Разбор расхождения живёт в самих документах, своей копии тут нет: свод решения 1
в `docs/lld/DK-354-draft-screen.md` и решение 2 в
`docs/lld/DK-430-task-chat.md`. К этой строке вход, признак ожидания и
инструмент ожидания уже написаны, так что работы в ней меньше, а не больше:
остались блок пачки в ленте и правка скилла.

## Проверка

2026-08-28, ветка POC `poc-chat`. К этому дню от строки осталась одна дыра в
канале. Ответ живой сессии панель шлёт в сокет клиента. Вопрос ждёт процесс
`taskctl ask` внутри хода Bash, и сокета он не слышит. Ответ ложился в очередь
следующего хода, а ожидание тем временем добирало свой срок. Правка развела две
дороги по признаку ожидания, коммит `129197d2`. Скилл `board-groom` научился
спрашивать посреди захода, коммит `66097195`.

Краснота стендов `tools/dashboard/asksay_test.go`. Файлы ручки взяты из
`129197d2^`.

```text
--- FAIL: TestChatSayGoesToInputWhileAskWaits (0.77s)
    asksay_test.go:68: реплика ушла в сокет клиента мимо ждущего инструмента: ["{\"from\":\"uds:/tmp/cc-socks/51070.sock\",\"message\":{\"content\":\"\\u003ccross-session-message from=... \\u003e\\nрежем, две половины по шву\\n\\u003c/cross-session-message\\u003e\",\"role\":\"user\"},\"msgV\":1,\"priority\":\"next\",\"type\":\"user\"}\n"]
FAIL
FAIL	github.com/dronrider/devkit/tools/dashboard	2.848s
```

Те же три стенда на исправленном коде.

```text
=== RUN   TestChatSayGoesToInputWhileAskWaits
--- PASS: TestChatSayGoesToInputWhileAskWaits (0.40s)
=== RUN   TestChatSayIgnoresStaleAsk
--- PASS: TestChatSayIgnoresStaleAsk (0.72s)
=== RUN   TestChatSayIgnoresAskOfOtherSession
--- PASS: TestChatSayIgnoresAskOfOtherSession (0.75s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	2.312s
```

Пакет дашборда целиком, `GOWORK=off go test ./`, кончился строкой `ok
github.com/dronrider/devkit/tools/dashboard 279.417s`.

Живая проверка шла на экземпляре POC, порт 7131. Дашборд пересобран из ветки и
выкачен под launchd, корень отвечает 302.

1. Заведена запись накопителя DK-551, разбор поднят ручкой дашборда.

```text
{"id":"DK-551","kind":"task","message":"грумминг DK-551 поднят в tmux-сессии task-DK-551: разбор доведёт черновик до строки Backlog либо снимет его с причиной, ярус base (sonnet)","model":"sonnet","prompt":"Проведи груминг DK-551. Вопросы задавай в этом же разговоре, командой `taskctl ask DK-551 --question \"...\" --wait 480`, и жди ответа в ней: вопросом заход не кончай, а не дождавшись, отложи запись с причиной","session":"task-DK-551","tier":"base"}
```

2. Грумер спросил сам и заход не кончил. Признак ожидания
   `.devkit/chat/task-DK-551.ask` основного чекаута появился через 18 секунд.

```text
2026-08-28T14:56:27
сессия aa74cd11-4453-4edf-be63-0d074edd9477
задача DK-551
{"questions":[{"text":"Груминг DK-551: код не подтверждает расхождение слова prio ... Подтверждаете снятие черновика как протухшего (drop), без строки на доске?"}]}
```

3. Ответ послан ручкой живого разговора, `POST /api/projects/devkit/chats/{sid}/say`.

```text
{"chat":"task-DK-551","message":"ответ лёг во вход разговора task-DK-551: его ждёт инструмент ожидания и заберёт в тот же ход","task":"DK-551","until":1787918187,"way":"ask"}
```

   Журнал дашборда той же секундой.

```text
2026-08-28T14:48:38 ответ человека сессии aa74cd11-4453-4edf-be63-0d074edd9477 ушёл во вход разговора task-DK-551: сессия стоит на вопросе до 14:56:27
```

4. Ожидание забрало строку меньше чем за три секунды. Вход опустел, заход
   продолжился тем же ходом и по сроку не кончился. Хвост панели грумера.

```text
Задал вопрос через taskctl ask DK-551 и получил ваш ответ: это была проверка
канала живого вопроса, запись снимается. Черновик удалён командой taskctl
draft drop DK-551 --reason "проверка живого вопроса"
```

Стенд послушания по новому сценарию `13-groom-ask` не гонялся. Он стоит живых
сессий на двух раскладках. Сверх того стенд кладёт во временный дом только
`kit/agents`. Скиллов там нет, и проверил бы он указатель раскладки правил.
Находка про стенд заведена отдельно.
