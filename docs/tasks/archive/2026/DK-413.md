# DK-413: trackctl: адаптер Jira ходит только в API v3, на v2 не работает

записан 2026-08-17

## Что происходит

Базовый путь Jira зашит в адаптере константой `jiraAPI = "/rest/api/3"`
(`tools/trackctl/jira.go:19`). Выбрать версию нечем: ключа в контуре нет
(`tools/trackctl/contour.go:38`), флага у команд нет тоже. На Jira Server и
Data Center третьей версии REST нет вовсе, там живёт `/rest/api/2`. На такой
инсталляции все оси адаптера отказывают сразу: `show`, переход статуса,
исполнитель, ворклог, комментарий уходят в 404.

Контур на v2 у нас есть сегодня, и работа с тикетами там идёт руками через веб:
trackctl туда не ходит, а вся обвязка вокруг него (факты работы, ранг, сводка)
на этом контуре недоступна.

Симптом живого контура сверен прогоном (2026-08-18, Jira Server 10.3.18):
отсутствующий путь `/rest/api/3` отдаёт не 404, а 302 на веб-логин
(`login.jsp?permissionViolation`), и адаптер умирает не статусом, а разбором
HTML-страницы как JSON: Go-клиент превращает POST в GET на перенаправлении.
Внятный отказ по чужому формату ответа входит в объём наравне с текстом про
неизвестную версию.

Подмены цифры в константе мало, версия тянет за собой формат тел.

- Комментарий в v3 уезжает телом Atlassian Document Format
  (`jira.go:282`), в v2 то же поле принимает строку wiki-разметки.
- Исполнитель в v3 Cloud ставится по `accountId` (`jira.go:248`), а на
  Server и DC такого поля нет, там `name`. Без этого куска `take` разваливается
  на первом же вызове, то есть половинчатая поддержка версии не считается
  работой.
- Тесты адаптера прибиты к путям `/rest/api/3` строками стенда
  (`jira_test.go:154` и дальше), а таблица операций README описывает ручки той
  же цифрой (раздел «Адаптер jira»); примечание про `/rest/api/2` на Server и
  DC в README уже есть, но ключа контура за ним нет.

Аутентификация в объём не входит сознательно. Токен уезжает заголовком
`Bearer` (`jira.go:104`), и персональные токены в таком виде принимают все Jira
от 8.14. Инсталляция старше упрётся в это отдельным симптомом: 401 с текстом
трекера, который адаптер передаёт без изменений, и это лечится отдельной
строкой.

## Чего хотим

Версия API называется контуром, а не кодом: один ключ верхнего уровня в
`~/.devkit/tracker/<имя>.local` рядом с `base_url`. Умолчание оставляет
сегодняшнее поведение v3, чтобы стоящие контуры не требовали правки. Адаптер по
этому ключу собирает путь и выбирает формат тел, а команды остаются прежними:
снаружи разницы между контурами не видно.

Задача не предрешает, как раскладывается ветвление по коду: годится и
параметризация одного адаптера, и вторая запись в таблице адаптеров. Выбор
делает исполнитель, и он же отвечает за то, чтобы обе версии были покрыты
тестами, а не одна.

## DoD

- Версия Jira задаётся ключом контура, отсутствие ключа означает v3 и
  ничего не ломает у стоящих контуров.
- На контуре с v2 работают все оси адаптера: чтение тикета, переход статуса,
  исполнитель, ворклог, комментарий, ранг.
- Комментарий на v2 уходит строкой, на v3 остаётся телом ADF; исполнитель на
  v2 ставится по `name`, на v3 по `accountId`.
- Неизвестное значение ключа отбивается на чтении контура с внятным текстом, а
  не превращается в 404 посреди работы; ответ не-JSON (302 на веб-логин
  живого контура) тоже отдаётся внятным отказом, а не паникой разбора.
- Тесты гоняют обе версии: стенд отвечает по путям своей версии, тела
  сверяются по формату.
- README trackctl описывает ключ версии и разницу тел, а не одну третью версию.

## Ранг

Считается по `RANKING.md`, 50+7+3+0+2 = 62, это P1.

- Серьёзность 50: основной сценарий утилиты на контуре с v2 не работает вовсе,
  обход у пользователя есть, но болезненный, вести тикеты руками через веб.
- Ценность 7: закрытие возвращает контуру всю обвязку trackctl, ради которой
  утилита и заводилась.
- Неопределённость 3: подход понятен, но правка идёт через чужой API, чьи
  расхождения видны на живом прогоне, а не в исходниках.
- Поправка на баг 0: v2 никогда не работала, это новое поведение, а не регресс.
- Рычаг 2: адаптер становится годен и для чужих контуров, но чужую работу
  сейчас не поддерживает.

## Проверка

Прогнано агентом после слияния (main a0540968, доска в Check), 2026-08-18.

1. Тесты на слитом main: `cd tools/trackctl && go test -count=1 .` ->
   `ok github.com/dronrider/devkit/tools/trackctl 6.561s`.
2. Живой контур corp (Server 10.3.18, только чтение), бинарь пересобран
   `go install` из слитого main, проект ls-local (привязка corp/CAP):
   `secretctl exec TRACKER_TOKEN -- trackctl issue CAP-2766` ->
   «CAP-2766 Сводный LLD обмена GLS-LLS, статус: В работе, тип: Задача,
   оценка: 32ч». До правки шаг падал разбором HTML страницы логина; успех
   чтения на Server доказывает v2, третьей версии там нет и сработай
   адаптер по ней, ответ был бы 302.
3. Неизвестная версия: временный HOME, копия контура corp с
   api_version = "4", синтетический проект с доской-заглушкой ->
   «corp.local: api_version "4" не понимаю, жду 2 или 3: у Cloud это 3,
   у Server и Data Center 2». Отказ до первого запроса в сеть.

Попутная находка вне задачи: статус «В работе» живого контура не расписан
в секциях [status] corp.local, утилита честно зовёт вписать его руками.

## Приёмка

- вид: agent

Шесть барьеров пройдены, ни один не устоял. «Глаза», «согласие» и «событие» к
предмету не относятся: итог не экран, не согласие с направлением и не ждёт
внешнего момента. «Доступ» и «секрет» снимаются обходами, стенд с ответами v2
поднимается на машине агента (он уже есть в тестах адаптера), а токен живого
контура агент получает через `secretctl exec`, не владея значением.
«Необратимость» снимается тем, что приёмка на живом контуре читает, а пишет на
стенд.

## Решения

- Ключ контура назван `api_version`, значения `2` и `3`, ведущее `v`
  срезается; пустой ключ это `3`, стоящие контуры Cloud не требуют правки.
- Значение проверяется на сборке адаптера, а не в `loadContour`: контур
  адаптеро-независим, и что значит версия, знает адаптер. Отказ называет путь
  контура, само значение и допустимые, похода в сеть нет.
- Перенаправления не проходят вовсе (`CheckRedirect` -> `ErrUseLastResponse`):
  любой 3xx отдаётся отказом с адресом перенаправления и ключом `api_version`.
  Тихое следование за 302 на веб-логин превращало запись в молчаливый успех.
- Тела ветвятся только там, где версии расходятся: `assign` шлёт `name` на v2
  и `accountId` на v3, `comment` строку на v2 и документ ADF на v3. Остальные
  оси совпадают телами и меняют только префикс пути.

## Тесты

- `TestJiraAPIVersionFromContour`: неизвестные значения отбиваются на сборке с
  текстом про `2 или 3`, без похода в сеть.
- `TestJiraV2Axes`: все семь осей на контуре v2 ходят путями второй версии,
  исполнитель уезжает `name` без `accountId`, комментарий строкой.
- `TestJiraV3DefaultBodies`: умолчание без ключа остаётся на ADF и `accountId`.
- `TestJiraRedirectRefused`: 302 на веб-логин отдаётся отказом с адресом и
  `api_version`, второго запроса адаптер не делает.
- Краснота регрессии проверена regcheck против main: «тест краснеет на main и
  проходит на текущем коде». Пять типовых мутаций (ключ assign, тело
  комментария, умолчание версии, снятый запрет перенаправлений, молчаливая
  неизвестная версия), и каждая краснеет своим тестом.

## Сценарий проверки

Задача без выката (deploy в `.devkit/deploy.local` пуст), агентские шаги
гоняются после слияния в main.

1. Стенд: `cd tools/trackctl && go test .`, ожидание `ok` на слитом main.
   Ожидание: `ok ... tools/trackctl`.
2. Живой контур corp (Server 10.3, только чтение): в проекте с привязкой
   `contour = corp` (например `ls-local`, ключ CAP) выполнить
   `trackctl show CAP-2766`. Ожидание: тикет назван (статус, тип, заголовок)
   без отказа; до правки этот шаг падал разбором HTML страницы логина, а путь
   `/rest/api/2` доказывается самим успехом чтения: на Server нет v3, и
   сработай адаптер по ней, ответ был бы 302.
3. Неизвестная версия: во временном `HOME` копия контура corp с
   `api_version = "4"` и та же привязка, `trackctl show CAP-2766` там.
   Ожидание: отказ с «api_version ... жду 2 или 3», без похода в сеть.

## Ход работы

- Вычитка: 1 файл, 8 правок, пометок нет, 2026-08-17.
- Проверка на живом контуре (только чтение): Jira Server 10.3.18, Bearer
  принят, чтение v2 работает; 302-симптом и правка README внесены в текст,
  2026-08-18.
- Исполнение в дереве dk-413: адаптер, контур, тесты, README; машинный контур
  corp.local переведён на api_version = 2, 2026-08-18.
- Разработка: субагент pro/high по вердикту pick (квота: объявления нет, корректор выключен), 2026-08-18 12:31-14:29.
- Ревью: субагент base/high по вердикту pick (квота: объявления нет, корректор выключен), 2026-08-18 14:29-14:44.
- Снаружи: проверка после выката, 2026-08-18 14:44-14:47.

## Ревью

- jira.go:157 диагноз 3xx единственный (нет пути API), а перенаправление бывает не про версию: смягчить формулировку: исправлено, текст переформулирован, диагноз версии как условие
- api_version = 2 целым без кавычек молча даёт умолчание v3: отвергать не-строковое значение при чтении контура: исправлено, loadContour отвергает числовой api_version, тест TestContourAPIVersionQuoted
- jira_test.go проверка got.Key тавтологична: усилить полем из образца: исправлено, проверка по Status и Title из образца
- README:152 англицизм named в русском тексте: исправлено, rewritten без англицизма
- DK-413.md:135 ожидание шага 2 ссылается на путь, которого нет в выводе show: переформулировать: исправлено, ожидание через успех чтения и объяснение, почему это доказывает v2
- contour.go:116 текст отказа заявляет «записан числом» для любого не-строкового вида: называть настоящий вид: исправлено, отказ называет настоящий вид значения из tomlKindNames, тест гоняет три вида

## Выкат

- 2026-08-18 слито: 09dcca41, 5e1f10cc, 8de3e20f, b3e174d1
