# DK-431: Реестр чатов задачи: журнал сессий, хук старта, подпись источника

Цель: [DK-397.md](DK-397.md)
Дизайн: [lld/DK-430-task-chat.md](../../../lld/DK-430-task-chat.md), решение 1

## Что происходит

Дашборд угадывает, какую задачу ведёт сессия: `sessionTask`
(`tools/dashboard/sessions.go`) пробует хвост каталога бокового дерева, ID в
первой реплике и имя ветки. Отсюда две находки пользователя. Разговор про
задачу («а что происходит с DK-194?») ловится той же регуляркой, что заказ
работы, и горит на карточке задачи её живой работой, конкурируя с настоящим
исполнителем (2026-08-15). Сессия, не попавшая ни под один из трёх способов,
даёт бесполезную карточку: ни чьё это окно, ни чем занято, а зайти внутрь
нельзя, потому что экран открывается по ID задачи (2026-08-13, повторно).
Конвейерную сессию дашборд к тому же поднимает в главном дереве, так что
дерево её не выдаёт, и узнаётся она только по первой реплике.

## Чего хотим

Запись «сессия ведёт задачу» появляется в машинном журнале
`~/.devkit/sessions.log` рядом с журналом уведомителя, кладут её SessionStart-хук
и ручка привязки рукой, а угадывание остаётся запасным разрядом. Привязка
делится на два разряда: «ведёт» по записи (только она делает сессию работой
задачи) и «говорит о» по угадыванию (сессия стоит в списке чатов и
открывается, но работой не считается). Источник виден в подписи, а рядом едет
машинное поле, по которому экран отличает разряд, не разбирая слов.

Устройство целиком расписано решением 1 документа
[DK-430-task-chat.md](../../../lld/DK-430-task-chat.md): формат строки журнала с полями
сессии, задачи, проекта, дерева, транскрипта, источника и повода; хук
`hooks/session-task.py` в раскладке доктора; переменная `DEVKIT_TASK` в начале
команды сессии дашборда (`sessionCommand` в `tools/dashboard/runs.go`); ручка
`POST /api/projects/{p}/sessions/{sid}/task` с пустым значением на отвязку.

## Границы

- Правило адресности реплики, выбор дерева и ручка реплики задаче едут строкой
  DK-438: строка разрезана по шву решением 1 документа, и половина «разговор»
  идёт следом.
- Список чатов задачи на экране и подъём нового чата это DK-436, здесь только
  данные, на которых список стоит.
- Инструмент ожидания и состояние «ждёт человека» это DK-432 и DK-433, обе
  читают реестр, но заводит его эта строка.

## Развилки

- Имя tmux-сессии полем журнала. Ревью дизайна оставило пометку: мера
  «кончившегося разговора» из решения 2 опирается на то, жива ли tmux-сессия с
  именем разговора, а вывести имя `chat-<ID>-<n>` из записи нечем. Имя есть у
  дашборда в момент подъёма, и дешевле всего положить его полем тем же
  способом, что путь транскрипта. Решено: поле в журнале есть, зовётся `tmux`,
  стоит последним, и значение приезжает хуку переменной `DEVKIT_TMUX` от того,
  кто поднял сессию, ровно как задача приезжает переменной `DEVKIT_TASK`.
  Писать его самому дашборду нечем: ID сессии в момент подъёма ещё не
  существует, и всё, что у него есть, это имя tmux-сессии, поэтому пара
  переменных в начале команды это единственная дорога от поднявшего к записи.
  Спросить имя у tmux из самого хука отвергнуто: в окружении лежит `$TMUX` с
  индексом, а не с именем, за именем пришлось бы звать `tmux display-message`
  подпроцессом на каждом старте сессии на машине. Вывести имя из полей записи
  отвергнуто той же пометкой ревью: номер `<n>` знает только поднявший. Пустое
  поле стоит дефисом, и это честная пустота: у сессии, поднятой рукой из
  терминала, имени tmux может не быть вовсе.

## DoD

- Строка журнала пишется при старте сессии и читается: сессия, поднятая
  дашбордом, сессия в боковом дереве задачи и сессия без задачи дают по записи
  со своим источником, а перепривязка и отвязка ложатся новыми строками, и
  выигрывает последняя.
- Дашборд считает работой задачи только разряд «ведёт»: разговор о задаче из
  главного дерева на карточке живой работой не горит, а в списке чатов стоит.
- Нераспознанная сессия открывается и привязывается рукой ручкой привязки,
  подпись источника называет способ словами.
- Хук подключается раскладкой доктора, а не правкой файлов руками; сессия вне
  git-дерева хук не роняет.
- Тесты по стандарту devkit, regcheck зелёный.

## Ранг

`50+8+1+0+3 = 62`, P1.

- Серьёзность 50: главная функция дашборда, разговор с агентом задачи, стоит на
  привязке, и сегодня она угадывается неверно в обе стороны.
- Ценность 8: на реестре стоят четыре строки цели, и каждая пачка после него
  дешевеет.
- Неопределённость 1: устройство закрыто решением 1 документа, открыта одна
  развилка про имя сессии.
- Рычаг 3: реестр держит DK-432, DK-433, DK-436 и DK-438.

## Ход работы

- Разработка: opus/high по вердикту pick, 2026-08-18.
- regcheck: три прогона, по одному на писателя и читателя реестра
  (`hooks/session_task_test.py`, `tools/dashboard/registry_test.go`,
  `tools/devkitctl/devkitctl_test.py` с `testenv.py`), каждый краснеет на HEAD
  и проходит на текущем коде.
- Вычитка: 5 файлов, 15 правок, 0 пометок, 2026-08-18.
- Разработка: субагент opus/medium по вердикту pick (квота: week_all 16%, week_max 16%, снимок 21м назад, сдвига нет), 2026-08-18 21:44-22:26.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 16%, week_max 16%, снимок 1ч 3м назад, протух, сдвига нет), 2026-08-18 22:26-23:07.
- Снаружи: проверка после выката, 2026-08-18 23:07-23:17.

## Сценарий проверки

1. `cd hooks && python3 -m unittest discover -s . -p '*_test.py'`: прогон
   зелёный, среди тестов есть `session_task_test`.
2. `cd tools/dashboard && go test .`: прогон зелёный.
3. `cd tools/devkitctl && python3 -m unittest devkitctl_test`: прогон зелёный,
   находка про неподключённый `session-task.py` есть у доктора и уходит после
   раскладки.
4. `python3 tools/devkitctl/devkitctl.py doctor -C .`: среди находок названа
   строка «машина: SessionStart-хук session-task.py не подключён»; после
   `doctor --fix` (уже на слитом коде) находка уходит, а в
   `~/.claude/settings.json` появляется команда хука на событии SessionStart.
5. Живая проверка после раскладки: поднять любую сессию и прочитать
   `tail -1 ~/.devkit/sessions.log`. В строке стоит ID сессии, путь дерева,
   путь транскрипта и повод `startup`; у сессии в боковом дереве задачи стоит
   её ID со словом источника `дерево`, у сессии главного чекаута задача
   дефисом.
6. Запуск задачи с доски дашборда: в команде tmux-сессии стоят
   `DEVKIT_TASK=<ID> DEVKIT_TMUX=task-<ID>`, а в реестре появляется строка со
   словом источника `заказ`.
7. `curl -X POST .../api/projects/<p>/sessions/<sid>/task -d '{"task":"DK-431"}'`:
   ответ отдаёт привязку словами, `GET .../sessions/<sid>` отдаёт
   `"taskNote":"привязана рукой"` и `"bound":"lead"`; тот же POST с пустым
   значением снимает привязку и отдаёт `"bound"` пустым.

## Выкат

- 2026-08-18 слито: 8493a5ef, 07f6bb1f, fae13910, b4983334
- smoke прогнан, 2026-08-18

## Проверка

Прогон 2026-08-18 на слитом и выкаченном коде, из главного чекаута.

1. `cd hooks && python3 -m unittest discover -s . -p '*_test.py'`: «Ran 457
   tests in 49.080s / OK», среди них `session_task_test`.
2. `cd tools/dashboard && GOWORK=off go test .`: «ok
   github.com/dronrider/devkit/tools/dashboard 119.538s». Без `GOWORK=off`
   прогон из главного чекаута падает на `go work use .`, это известная поломка
   DK-367, к задаче отношения не имеет.
3. `cd tools/devkitctl && python3 -m unittest devkitctl_test`: «Ran 117 tests
   in 134.010s / OK».
4. `python3 tools/devkitctl/devkitctl.py doctor -C .`: находок 2, обе про
   неподключённый хук, по одной на харнес: «машина: SessionStart-хук
   session-task.py не подключён в /Users/rider/.claude/settings.json: реестр
   чатов пуст, и дашборд возвращается к угадыванию задачи по транскрипту».
   После `doctor --fix` печатается «починено: включён хук харнеса ...
   session-task.py на SessionStart» по обоим харнесам, повторный `doctor`
   находок про хук не даёт.
5. Живая проверка после раскладки. Сессия главного чекаута:
   `2026-08-18T23:13:34 сессия 812a52bc-... задача - проект devkit дерево
   /Users/rider/projects/devkit транскрипт /Users/rider/.claude/projects/...
   источник - повод startup tmux -`. Сессия бокового дерева задачи:
   `2026-08-18T23:13:43 сессия 9d9885fa-... задача DK-371 проект devkit дерево
   /Users/rider/projects/devkit-dk-371 транскрипт ... источник дерево повод
   startup tmux -`. Обе строки легли сами, руками журнал не трогали.
6. Переменные заказа проверены на слитом коде тестами команды сессии
   (`runs_test.go`, строки с `DEVKIT_TASK='XR-002' DEVKIT_TMUX='task-XR-002'`);
   живой подъём задачи с доски не гонялся, чтобы не поднимать настоящего
   исполнителя ради одной строки журнала.
7. Ручка привязки живая на выкаченном дашборде: `POST` по
   `/api/projects/devkit/sessions/<sid>/task` отдаёт 401 (просит входа), а
   несуществующий маршрут рядом отдаёт 404, то есть выкат перезапустил дашборд
   с новым маршрутом. Поведение привязки и отвязки закрыто тестами
   `TestSessionTaskPostBinds`, `TestSessionTaskPostUnbinds`,
   `TestSessionTaskPostKeepsTmuxName` и `TestSessionTaskPostRefusals` из
   прогона шага 2. Отдельный экземпляр дашборда с чужим домом для этой
   проверки не годится: транскрипты ищутся от дома, и ручка честно отвечает
   «транскрипта нет среди сессий проекта».
