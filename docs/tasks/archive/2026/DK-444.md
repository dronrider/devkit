# DK-444: Форма файла задачи и черновика: страница формы, болванка и сторож первой строки

## Что происходит

Файл задачи это основа работы: с него начинается черновик, по нему исполнитель
входит в задачу, по нему считают ворота и утилиты. При этом ни для черновика,
ни для файла задачи нет описания формы: какие разделы, в каком порядке, что в
каждом лежит и кто из них читает машина. Замечено пользователем 2026-08-18 на
разборе накопителя.

У черновика механика описана в трёх местах
(подсказка `taskctl draft`, README taskctl, п. 2 board-task), и все три
описывают, куда записать, а не как. Что первая строка это заголовок, написано
только в README утилиты, и потому виток цели пишет черновик одним абзацем на
девятьсот символов: он становится и заголовком, и телом. `taskctl draft list`
печатает простыню, а `cmdDraft`
(`tools/taskctl/draft.go:306`) не смотрит длину первой строки и не
показывает заголовок на выходе. Так записаны DK-330, DK-439 и DK-442 (у него
заголовок на 1800 символов), тогда как DK-188 и DK-195 короткие человеческие;
кто как понял, так и записал.

У файла задачи описание есть, но по тому же образцу: раздел «Файл задачи» в
board-task и п. 3 «Трекинга задач» в RULES.board.md перечисляют содержимое
(разбор корня, DoD, ход работы, ревью, сценарий, выкат), а нет порядка
разделов, их имён и того, что в каждый идёт. Пересчёт по живым файлам на
2026-08-18: в 80
открытых 65 раз «DoD», 37 «Что происходит», 35 «Чего хотим», 21 «Приёмка», 16
«Ранг», а дальше хвост из тридцати с лишним заголовков по одному-два файла
(«Корень», «Разбор корня», «Что чинить», «Что решать», «Развилки», «Границы»,
«Смежное», «Дока»); «Грумминг» и «Грумминг» живут в двух написаниях, а у
DK-421 «Приёмка» стоит дважды. Среди шести последних закрытых задач шесть
разных наборов и порядков. Читатель угадывает разметку по соседям.

Часть разделов при этом уже контракт: `taskctl` считает строки «Приёмки» по
барьеру, `shipctl` пишет и читает «Выкат», `review` ведёт «Ревью», ворота
`move check` ищут сценарий проверки, `move blocked` пишет причину, смена
статуса уносит этапы в «Ход работы», «Проверка» читается по префиксу
заголовка. В одном месте нет перечня контрактных разделов с их форматом,
каждый живёт в доке своей утилиты, а машина вовсе не читает «Ранг», «Что
происходит», «Чего хотим».

## На чём стоит форма

Прежде чем рисовать разделы, сверили устоявшиеся
методики: шаблоны тикетов (GitHub issue forms, GitLab, Jira, Linear),
истории и приёмку (user story, INVEST, Gherkin, DoR и DoD по Scrum Guide),
канбан Андерсона и GTD, документы решений (RFC, ADR, дизайн-док Google,
Shape Up pitch, PR/FAQ), баг-репорты (Mozilla, Спольски, минимальный
воспроизводимый пример) и правило первой строки коммита. Все они сходятся к
трём слоям: одна строка, по которой запись узнаётся в списке; три-пять
обязательных разделов про «что не так, чего хотим, чем проверим»; хвост
контекста. Чем ближе формат к быстрой записи, тем короче обязательный слой
(Linear держит обязательными только заголовок и статус, GTD одну строку).
Чем ближе к решению с последствиями (RFC, ADR), тем важнее мотивация,
альтернативы и открытые вопросы. Из этого берём то, что ложится на наши
особенности: разделы это `##`-заголовки в markdown, метаданные живут в строке
доски. Читатель файла часто агент со свежим контекстом, а черновик пишется
на бегу и разбирается отдельным заходом.

- Правило первой строки, как у коммита и BLUF: заголовок до 72 символов,
  пустая строка, тело. У бага заголовок это наблюдаемая поломка, а не
  починка (Mozilla), у задачи это исход, а не занятие: «логи ротируются раз
  в сутки» закрывается, «разобраться с логами» нет. Это ровно та дыра, из-за
  которой DK-442 стал простынёй.
- Черновик дешёвый, разбор отдельно (GTD «захвати быстро, проясни потом»,
  Linear triage, DEEP: верх бэклога подробно, низ грубо). Обязательное в
  черновике только заголовок; тело абзацами отвечает на три вопроса шаблона
  фичи GitHub и контекста ADR: что происходит, что предлагается, откуда
  взялось. В черновике нет ранга и DoD, они появляются на грумминге. Три
  ворот board-groom (актуальность, предмет, конец работы) это наш Definition
  of Ready, и страница формы на них ссылается, а не заводит вторых.
- Не-цели и открытые вопросы явными разделами (дизайн-док Google, Shape Up
  «rabbit holes», RFC «unresolved questions»). В файлах они уже стихийно есть
  как «Границы», «Смежное», «Развилки», «Что решать»; в форме у них одно
  имя.
- Баг из трёх полей по Спольски: как воспроизвести, что ожидалось, что
  вышло. У задачи типа bug эти три вопроса это содержимое «Что происходит».
  Тяжесть последствий и очередь исправления разведены (severity против
  priority): у нас это серьёзность в формуле RANKING и ранг строки, и в
  форме они не смешаны.
- Обязательность разделов проверяет машина, как required-поля в issue forms:
  `add --id` уже отказывает без «## DoD», тот же сторож смотрит первую строку
  черновика и порядок контрактных разделов.
- Общий DoD один на доску, частный в файле только сверх общего (Scrum Guide:
  Definition of Done это состояние инкремента, а не список на карточке).
  Тесты, дока и доводка до пользователя записаны в RULES.core, файл задачи
  их не переписывает, а в нём названо только то, что видно снаружи именно у
  этой задачи.
- Политики на доске, а не в карточке (канбан): тип, приоритет, ранг, цена,
  зависимости и вид приёмки остаются в строке, файл их не дублирует; причина
  блокировки это строка от `move blocked`, у карточки Андерсона это тот же
  флажок с причиной.

Что отвергли и почему. Формулу user story «как роль я хочу, чтобы»: у доски
один пользователь и он же исполнитель, формула станет ритуалом, и Linear её
для внутренних задач называет антипаттерном. Given/When/Then для сценария
проверки: наш сценарий это команда и ожидаемый вывод, ему хватает пары
«прогон, ожидание». Отказ Shape Up от бэклога: накопитель черновиков нам
нужен, но с обязательным исходом разбора и причиной откладывания, что
`draft defer` уже делает. Аппетит Shape Up как поле файла: он уже есть в
строке ценой S/M/L/XL и в бюджете цели.

## Чего хотим

Тремя кусками.

1. Одна страница формы рядом с RANKING.md и ACCEPTANCE.md (оба лежат в корне
   репозитория, значит и она туда): разделы черновика и файла задачи по
   порядку, что в каждом лежит, какие читает машина и чем. Черновик это
   заголовок одной строкой до 72 символов, шапка (`записан`, метка
   `приоритет`) и раздел «Черновик» абзацами: что происходит, что
   предлагается, откуда взялось; раздел «Грумминг» дописывает утилита. Файл
   задачи идёт от человека к машине: «Что происходит» (у бага: как
   воспроизвести, что ожидалось, что вышло), «Чего хотим» с не-целями внутри
   или отдельными «Границами», «Развилки» для открытых вопросов, «DoD» сверх
   общего, «Ранг» с причинами по слагаемым RANKING, дальше контрактные
   разделы в порядке жизни задачи: «Приёмка», «Ход работы», «Ревью», «Сценарий
   проверки», «Проверка», «Выкат», с форматом строк каждого и ссылкой на
   утилиту, которая его ведёт. Одно написание «Грумминг». Файл цели со
   своей формой (goal-start) и LLD-документы страница формы называет со
   ссылкой, а не переписывает.
2. Болванка от `taskctl add` и `taskctl file` кладёт заголовки формы сразу, а
   не один `## DoD`; `taskctl draft` отбивает первую строку длиннее заголовка
   и в отказе указывает на форму. Многострочный текст удобнее отдать на
   stdin; `draft list` печатает заголовок и не печатает тело.
3. Скиллы board-task (п. 2 и раздел «Файл задачи»), board-groom (шаг 13),
   live-core (п. 7) и goal-loop ссылаются на страницу формы, а не пересказывают
   её каждый по-своему; RULES.board.md п. 3 и README taskctl отсылают туда же.

## Границы

Живые файлы задач под новую форму не переписываются: форма действует вперёд,
как стиль по DK-173. Форма файла цели и LLD не трогается. Редактор черновика
на дашборде (DK-369) и подсказка `add` (DK-330) остаются своими задачами; на
дашборде правило первой строки появится своей строкой, когда страница формы
будет готова. Поле заголовка и подсказка про первую строку у формы записи дашборда
ждут своей строки доски: здесь только текст отказа `taskctl draft` без совета
про stdin тому, кто пришёл со stdin.

## Развилки

Развилки закрыты диспетчером до старта. Страница называется `TASKFORM.md` и
лежит в корне по образцу `RANKING.md` и `ACCEPTANCE.md`. Не-цели идут отдельным
разделом «Границы», он уже прижился в живых файлах. Сторож порядка контрактных
разделов живёт в `taskctl lint`, а `add --id` продолжает отказывать черновику
без «## DoD». Порог первой строки черновика 72 символа, как у subject коммита.

Открытой развилкой по ходу работы осталась одна пара разделов, и решена она не
так, как названа в постановке. «Выкат» в форме стоит перед «Проверкой», а не
после: раздел выката пишет `shipctl merge` в момент слияния, а вывод прогона
вкладывается уже после выката, и обратный порядок давал бы находку сторожу на
каждой проверенной задаче. По архиву так и лежит: 61 файл против 34.

Болванка кладёт пустой «Ход работы» не для читателя: пакет этапов от смены
статуса иначе уезжал в хвост файла и вставал после разделов, которые по форме
идут позже. Со второго круга то же место выбирает и сам писатель: порядок формы
вынесен в общий пакет `internal/taskform`, и `stage.InsertIntoSection` с
`appendToSection` shipctl заводят отсутствующий раздел по форме, так что
болванка теперь удобство, а не защита.

Пять живых файлов задач с переставленными разделами (DK-315, DK-355, DK-356,
DK-395, DK-430) приведены к порядку формы первым кругом: сторож иначе краснел
бы на исправной доске с первого дня. Правка только перестановка блоков, текст
в них не тронут.

Второй круг по замечаниям ревью. Подразделы черновика на русском («Ситуация»,
«Осложнение», «Вопрос», «Гипотеза») под «## Черновик» заголовками третьего
уровня: шапка и разделы грумера («## DoD», «## Грумминг») читаются отдельно от
тела идеи, разбор шапки в `parseDraftFile` не меняется. `add --id` собирает
файл задачи по форме, а не переносит черновик как есть: H1 из `--title`,
ситуация и осложнение в «Что происходит», гипотеза в «Чего хотим», вопрос в
«Развилки», чужой раздел грумера после «Ранга». След грумминга это одна строка
в «Ходе работы» с заголовком черновика и датами, под ней пометки «Грумминга»
списком, так история разбора не теряется, а заголовок черновика не пропадает,
даже когда `--title` другой. Приписка `draft attach` несёт заголовок черновика
в заголовке раздела «Из черновика», потому что первая строка в теле черновика
больше не дублируется. Формула ранга первой строкой «Ранга» узаконена: так
лежат живые файлы, и причины читаются рядом с числами. Тест границы порога
вынесен своим файлом `guard_test.go`, чтобы `form_test.go` собирался на базе
и regcheck сравнивал тесты, а не сборку.

## DoD

В корне лежит страница формы, и с неё читаются разделы черновика и файла
задачи по порядку с указанием, какие ведёт машина и какой утилитой.
`taskctl draft "строка длиннее порога"` отказывает и в отказе называет
страницу формы, тот же текст со stdin с короткой первой строкой ложится
черновиком; `taskctl draft list` печатает заголовки без тела. Болванка
`taskctl add` и `taskctl file` у `task` и `bug` содержит заголовки формы, у
LLD и цели прежняя. Четыре скилла, RULES.board.md и README taskctl ссылаются
на страницу и не пересказывают форму. Тесты на отказ `draft`, на болванку и
на `draft list` зелёные, regcheck на отказе `draft` красный до фикса.

## Ранг

`50+8+2+0+4 = 64`, P1.

- Серьёзность 50. Конвейер ведёт не туда и встаёт на человека там, где
  обещана автономия: виток цели сам пишет черновики-простыни (DK-330, DK-439,
  DK-442), разгребает их человек на грумминге, а исполнитель со свежим
  контекстом угадывает разметку файла задачи по соседям.
- Ценность 8. Каждый виток дешевле по квоте: черновик и файл задачи пишутся и
  читаются каждый день, форма снимает чтение простыни в `draft list` и
  угадывание разметки на каждом входе в задачу.
- Неопределённость 2. Подход выбран, открыты детали из «Развилок»: имя
  страницы, место сторожа, порог длины.
- Поправка на баг 0. Новая работа.
- Рычаг 4. Ускоряет работу агента на каждом входе в задачу и на каждом
  грумминге: разметка читается, а не угадывается.

Цена M: страница, правки в двух местах `taskctl` с тестами, четыре скилла и
две доки, одна сессия.

## Приёмка

- вид: mixed
- барьер «согласие»: страница формы меняет, как пишутся черновики и файлы
  задач самим пользователем
  - обхода нет: критик со свежим контекстом даёт находки по тексту, и ревью их
    дало, но согласие с формой записи даёт человек

Агентская часть самодостаточна: болванка, порог первой строки, `draft list`,
сторож порядка разделов и тесты проверяются с машины агента.

## Ход работы

- Вычитка: 1 файл, 11 правок, 0 пометок, 2026-08-18.
- Разработка: opus/medium по вердикту pick, 2026-08-18 14:01-14:01.
- Вычитка: 4 файла, 10 правок, 1 пометка, 2026-08-18.
- Разбор ревью: перебор обхода у барьера «согласие» переписан подсписком, и
  `parseAcceptance` на этом тексте видит один обход. Сценарий разделён на
  прогон из ветки и прогон после слияния по собранному бинарю. Раздел «Ревью»
  сам встал в хвост файла, и это была дыра в фиче: `taskctl review add` теперь
  кладёт его на место по форме, тест `TestReviewSectionGoesByForm` держит
  рубеж. В DK-395 восстановлены пустые строки внутри примера на python,
  схлопнутые перестановкой разделов.
- Пять живых файлов задач приведены к порядку формы, перечень и причина в
  «Развилках».
- regcheck: `regcheck --base 714eb6d5^ --tests
  tools/taskctl/draft_test.go,tools/taskctl/form_test.go` с GOWORK=off краснеет
  на базе и проходит на правке (прогнан ревьювером первого круга).
- Тест дашборда `TestDraftTextLimit` писал черновик в 1151 символ одной
  строкой и после порога краснел; текст в нём разбит на заголовок и тело,
  предел по телу запроса проверяется тем же местом.
- `TASKFORM.md` добавлен в белый список верхнего уровня `devkitctl` и в раздел
  «Раскладка» README, иначе новый корневой файл был бы находкой `devkitctl doctor`.
- Скилл `goal-loop` стоял в 15 989 символах при пороге 16 000, и ссылка на
  форму его перебрала; абзац про найденное по ходу сжат, тело 15 998.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 11%, week_max 8%, снимок 30м назад, сдвига нет), 2026-08-18 14:31-14:59.
- Снаружи: проверка после выката, 2026-08-18 14:59-15:54.
- Второй круг: fable/medium по вердикту диспетчера, 2026-08-18. Замечания 3-14
  закрыты, пакет `internal/taskform` (`internal`), правки `shipctl`, `taskctl`,
  теста дашборда, страница формы, скилл `board-draft`, отсылки в скиллах,
  правилах и определениях агентов.
- Замечания 15-17: подразделы черновика встают до собственных «## » разделов
  текста, копия заголовка старого черновика в «Что происходит» не едет,
  «Ранг» от `add --id` открывается формулой и бакетом, строка «Приёмка» и
  пример черновика на странице уточнены, «Гипотеза» переопределена исходом
  (тесты `TestDraftBodyKeepsOwnSections`, обновлённые
  `TestDraftPromotedByForm` и `TestDraftWithoutSubsectionsGoesToSituation`).
- regcheck второго круга: `GOWORK=off regcheck --base c9611da3 --tests
  tools/taskctl/form_test.go -- go test -C tools/taskctl -run
  'TestDraftPromotedByForm|TestMoveStagesGoByForm|TestFileAddsStagesByForm|TestDraftWithoutSubsectionsGoesToSituation' .`
  и то же с `tools/shipctl/record_test.go` на `TestAppendToSectionGoesByForm`:
  краснеют на базе, проходят на правке. Тест `TestDraftListJSONKeepsFullTitle`
  на базе зелёный: JSON заголовок и до этого отдавал целиком, тест держит
  границу, а не чинит регрессию.
- Вычитка: 6 файлов, 9 правок, 2 пометки, 2026-08-18.
- Снаружи: проверка после выката, 2026-08-18 17:13-21:41.

## Ревью

- раздел «Приёмка» самого DK-444 не по форме ACCEPTANCE.md: у барьера «согласие» нужна ровно одна строка перебора вида «  - обхода нет: причина» (двухпробельный подсписок), а в файле она вписана продолжением строки барьера без «  - ». parseAcceptance/gate.go считают её строки регексом ^  -  и находят 0 при требуемой 1 (проверено прогоном parseAcceptance на этом тексте): taskctl move check DK-444 откажет с «у барьера «согласие» обходов 1, а перебор... имеет строк 0». Рядом в этом же диффе DK-430 показывает правильную форму той же связки «- барьер «согласие»: ...\n  - обхода нет: ...».: исправлено
- «Сценарий проверки» состоит целиком из шагов, которые целиком повторимы до слияния на песочной доске (шаги 1-4 буквально дублируют TestDraftLongFirstLineRefused/TestAddPutsFormSkeleton/TestLintFindsFormOrder, шаг 5 гоняет taskctl lint на живом дереве, но тоже пригоден к прогону из ветки без выката): ни один шаг не помечен «после слияния/выката» и не бьёт по реально переустановленному бинарю taskctl/devkitctl, как это сделано в DK-440 (шаг 6 сценария и отдельный «Прогон ... после слияния и выката, из основного чекаута»). Задача правит tools/taskctl, tools/dashboard, tools/devkitctl - код, который должен переехать в PATH через devkitctl update, и агентская часть mixed-сценария обязана держать хотя бы один такой шаг, иначе краснота taskctl fail после выката неотличима от штатной работы.: исправлено
- Порядок формы держит только болванка add/file, а машинные писатели разделов формы не знают: flushStages (stage.InsertIntoSection) и shipctl appendToSection при отсутствии заголовка дописывают «Ход работы» и «Выкат» в хвост файла. Файл, оформленный из черновика (add --id, основной путь board-groom: ensureTaskFile видит готовый файл и болванку не кладёт; проверено в песочнице), файл LLD и любой файл, заведённый до этой задачи (78 из 80 живых без «## Ход работы», у DK-114 и DK-241 «Сценарий проверки» уже стоит), на первом move check получит «Ход работы» после «Сценария проверки», и taskctl lint (а с ним devkitctl doctor и ворота board-batch) станет красным; так лежат 123 из 290 файлов архива, ещё 12 с «Выкат» после «Проверки», написанной до слияния. Чинить: порядок формы вынести в internal (общий пакет для taskctl, shipctl и stage), чтобы InsertIntoSection и appendToSection вставляли отсутствующий раздел по форме, либо add --id и file дополняли файл недостающими заголовками контрактной части; на путь черновик -> add --id -> сценарий -> move check нужен тест, сейчас его нет.: исправлено
- Тело черновика без структуры: TASKFORM.md называет три вопроса раздела «Черновик» абзацами, болванка taskctl draft подразделов не кладёт, и на груминге автор снова разбирает простыню. Нужна структура SCQA: ситуация, осложнение, вопрос, гипотеза (вместо ответа), подразделами в болванке черновика и на странице формы, а add --id переносит их в «Что происходит» (ситуация и осложнение), «Чего хотим» (гипотеза) и «Развилки» (вопрос), а не двигает файл как есть.: исправлено
- Процедуры написания черновика нет: TASKFORM.md остаётся контрактом (что и где лежит), а как писать заголовок-исход, ситуацию, осложнение, вопрос и гипотезу, чем проверить себя перед записью, описать негде, board-task п. 2 и live-core п. 7 только отсылают к форме. Нужен отдельный скилл board-draft с процедурой письма, а страница формы на него ссылается; заводить своей строкой доски или вторым кругом решает диспетчер.: исправлено
- Порог первой строки доехал до дашборда без доводки: ручка POST /drafts (tools/dashboard/newtask.go) отдаёт текст на stdin taskctl draft, и мысль с телефона одним абзацем теперь отбивается, а пользователь видит ошибку про stdin и heredoc taskctl draft <<'EOF' (draftTitleGuard пишет её для терминала). Поля заголовка или подсказки про первую строку у формы записи нет, теста на путь отказа в дашборде нет (TestDraftTextLimit только обошёл порог, разбив свой текст), строки доски про правило первой строки на дашборде тоже нет (DK-369 про правку текста, а «Границы» обещали свою строку, когда страница формы будет). Нужно: своя строка доски на поле заголовка или подсказку и человеческий отказ, а до неё текст отказа taskctl draft не должен советовать stdin тому, кто уже пришёл через stdin (проверять os.Stdin или дашборд переводит ошибку сам), плюс тест дашборда на отказ.: исправлено
- В tools/taskctl/README.md строка 198 ушла с мусором кавычек оболочки: «taskctl draft <<'"'"'EOF'"'"' ... EOF» вместо «taskctl draft <<'EOF' ... EOF» (артефакт sed/heredoc при правке); это пользовательская дока, читатель скопирует нерабочую команду.: исправлено
- TASKFORM.md расходится с тем, что делают утилиты и сам файл DK-444, по нескольким пунктам, и каждый править на странице или в коде: (а) пример черновика показывает раздел «Черновик» без повторённого заголовка, а taskctl draft кладёт в раздел весь текст вместе с первой строкой (проверено: «## Черновик» начинается с копии заголовка); (б) строка «Ранг» говорит, что файл повторяет сумму «только заголовком раздела», а «Ранг» самого DK-444 и DK-430 открывается формулой и P (решить, что верно, и привести в согласие); (в) образец строки «Ход работы» «- Разработка: 2026-08-18 14:01-15:20.» не совпадает с настоящим пакетом «- Разработка: opus/medium по вердикту pick, ...»; (г) абзац «форма действует вперёд, старые файлы не переписываются, находка на старом файле чинится когда дошли руки» не описывает реальность: lint судит все живые строки (пять файлов переставлены этой же задачей), а архив не смотрит вовсе, и так и надо написать; (д) абзац про LLD и цель («в обоих случаях форма ограничивается ссылкой») читается как отказ от формы целиком, тогда как контрактная часть («Ход работы», «Ревью», «Сценарий проверки», «Выкат») и lint к файлу LLD-задачи применяются; (е) у файла задачи, в отличие от черновика, нет примера, только таблицы, а RULES.core требует вход для новичка примером раньше устройства; (ж) шапка обещает переносимость и независимость от трекера, а таблица контрактных разделов вся из утилит devkit, ACCEPTANCE.md для этого помечает специфику словами «у devkit это», здесь такой пометки нет.: исправлено
- Переход черновика в файл задачи на странице формы не описан, а машина его делает по-своему: add --id переносит файл как есть (git mv, снимается только строка приоритета), так что «записан <дата>», заголовок «## Черновик» с копией первой строки и «## Грумминг» переезжают в docs/tasks/<ID>.md, если грумер их не убрал руками (board-groom шаг 13 про это молчит), а H1 остаётся заголовком черновика, а не --title строки; раздел «Из черновика <ID> (записан ...)», который пишет draft attach, в форме не назван вовсе. Дописать на странице, что из шапки и разделов черновика переживает оформление и приписку, что снимает утилита, а что грумер, и согласовать с шагом 13 board-groom; вместе с переносом SCQA-подразделов это одна правка.: исправлено
- Написание «Грумминг» не унифицировано там, где DoD это обещал: в TASKFORM.md заголовок раздела «Грумминг», а в прозе трижды «на груминге», то же в комментарии form.go и в тексте самого DK-444; дашборд (tools/dashboard/drafts.go, кнопка «Провести груминг» и её журнал) целиком на одном «м». Привести страницу, form.go и файл задачи к одному написанию; дашборд либо тем же кругом, либо своей строкой.: исправлено
- RULES.board.md п. 3 правлен наполовину: в том же абзаце осталось «taskctl file <ID> (со ссылкой в строке и заголовком DoD по типу)», хотя file теперь кладёт заголовки формы; п. 5 (черновики) по-прежнему описывает механику draft и машинную опору заведения (отказ add --id без DoD) без слова про порог первой строки и без отсылки к TASKFORM.md, а это второй машинный сторож той же формы.: исправлено
- Читатель, ради которого страница написана (агент со свежим контекстом), к ней не отослан из своей точки входа: определения kit/agents/exec-*.md и review-*.md говорят «постановка лежит в docs/tasks/<ID>.md», «там же раздел «Ревью»» и ничего про форму разделов; ревьювер к тому же судит «Приёмку» и «Сценарий проверки» по ACCEPTANCE.md, а к порядку и составу разделов у него ссылки нет. Добавить в определения одну отсылку к TASKFORM.md рядом с ACCEPTANCE.md.: исправлено
- Файл DK-444 сам не по своей форме и не по своему DoD: вывод прогона шагов 6-8 после выката вложен в конец «Сценария проверки», а не в «Проверку», которую таблица формы отводит именно под реальный вывод прогона агентской части (у mixed ворота close его не спрашивают, но страница обещает раздел); DoD требует «regcheck на отказе draft красный до фикса», а в «Ходе работы» следа прогона нет (прогнан ревьювером: regcheck --base 714eb6d5^ --tests tools/taskctl/draft_test.go,tools/taskctl/form_test.go с GOWORK=off краснеет на базе и проходит, записать в файл); «Ход работы» набит обоснованиями решений (пустой «Ход работы» в болванке, порядок «Выкат/Проверка», перестановка пяти файлов), которым по той же форме место в «Развилках», а не среди этапов.: исправлено
- Тесты не держат границы порога и путь оформления: TestDraftLongFirstLineRefused проверяет 101 символ против 30, а не 72 против 73 (мутация <= на < проходит незамеченной), clipTitle не проверен на JSON-выдаче draft list (там заголовок должен оставаться целым, тест этого не фиксирует), и нет теста на файл, оформленный через add --id, где болванки нет, а «Приёмка» и позже «Ход работы» встают по остаточному принципу (см. замечание про хвост файла).: исправлено
- draftBodySection кладёт пустые подразделы после уровня-2 заголовка из тела: текст без «###», но с «## DoD» (или любым «## ») внутри, целиком уходит под «### Ситуация», а «### Осложнение», «### Вопрос», «### Гипотеза» встают уже после «## DoD» и структурно принадлежат ему; add --id потом кладёт эти три пустых заголовка в раздел «DoD» файла задачи (воспроизведено на бинаре ветки: printf 'третий черновик\n\nтекст.\n\n## DoD\n\nадрес.\n' | taskctl draft, затем add --id, в «## DoD» файла лежат «### Осложнение / ### Вопрос / ### Гипотеза»). Резать rest по первому «## » вне ограждений: в «Ситуацию» только то, что до него, хвост с уровнем 2 после четырёх подразделов; тест на этот вход. Тем же местом: у 50 черновиков накопителя, записанных до подразделов, «## Черновик» начинается с копии заголовка, и renderTaskFromDraft открывает ими «Что происходит» повтором H1; первую строку тела, равную заголовку черновика, опускать.: исправлено
- Файл, собранный add --id, расходится с двумя строками таблиц TASKFORM.md, и оба места нашёл свежий контекст на прогоне по странице: «Ранг» после оформления пуст, хотя таблица требует первой строкой формулу и бакет (add --id их знает: писать «`25+4+2+5+2 = 38`, P2.» первой строкой, а причины грумера из «## Ранг» черновика под ней); строка «Приёмка» контрактной таблицы читается как «раздел кладётся всегда», а у вида agent его нет, и что это по замыслу, читатель восстанавливает по фразе из «Что читает машина»: дописать в строку таблицы «у неагентского вида». Там же в примере черновика показать, куда грумер дописывает «## DoD» (после подразделов, до «Грумминга»).: исправлено
- «Гипотеза» определена как механизм («как это, по мнению автора, решается»), а едет в «Чего хотим», куда по той же странице «развилки реализации не идут»: свежий контекст по этим определениям честно написал в «Чего хотим» техническое решение (замена слэша на дефис), а не то, что будет видно снаружи. Раз перенос гипотеза -> «Чего хотим» решён, определение подраздела в TASKFORM.md и в board-draft (шаг 5) переписать под него: гипотеза это догадка автора о желаемом исходе, каким оно станет снаружи, а способ, если он есть, идёт в «Вопрос» развилкой или остаётся исполнителю.: исправлено

## Сценарий проверки

Вид приёмки mixed. Шаги 1-5 идут из ветки на песочной доске (`taskctl init
--prefix XR` во временном каталоге), шаги 6-8 после слияния и выката, по
переустановленному бинарю из основного чекаута, шаг 9 пользовательский.

1. Порог первой строки черновика.

```
$ taskctl draft "уведомитель шумит из песочницы, потому что хук старта берёт адрес из окружения, а не из реестра чатов"
ошибка: первая строка черновика длиннее 72 символов (101): она идёт заголовком, по ней черновик узнают в накопителе.
  подробности пишутся абзацем ниже через пустую строку, форма черновика в TASKFORM.md
  многострочный текст удобнее отдать на stdin: taskctl draft <<'EOF' ... EOF
код возврата: 1
```

2. Тот же текст с заголовком в первой строке идёт на stdin и ложится
   черновиком, а накопитель печатает заголовок без тела.

```
$ printf 'уведомитель шумит из песочницы\n\nхук старта берёт адрес из окружения, а не из реестра чатов, и вторая сессия перебивает первую\n' | taskctl draft
XR-001: черновик в docs/tasks/drafts/XR-001.md, оформить: taskctl add --id XR-001 --title "..." --type ... --rank "..."
$ taskctl draft list
XR-001 (сегодня): уведомитель шумит из песочницы
```

3. Болванка новой строки несёт заголовки формы, а «Приёмка» неагентского вида
   встаёт выше «Хода работы».

```
$ taskctl add --title "болванка новой строки" --type bug --rank "25+5+1+5+1" --accept agent
$ cat docs/tasks/XR-002.md
# XR-002: болванка новой строки

## Что происходит

## Чего хотим

## DoD

## Ранг

## Ход работы
```

4. Переставленный раздел это находка `lint`.

```
$ taskctl lint
docs/tasks/XR-002.md:9: раздел «Чего хотим» стоит после «Выкат», порядок разделов в TASKFORM.md
находок: 1
код возврата: 1
```

5. На живой доске devkit `taskctl lint` из ветки печатает «доска и архив в
   порядке»: пять файлов с переставленными разделами приведены к форме той же
   веткой.

Дальше прогон после слияния и выката, из основного чекаута. Бинари берутся
сборкой, а не `devkitctl update`: обновление переводит клон на новейший
релизный тег, и правка этой задачи доедет до него только следующим релизом.

6. `devkitctl build` кладёт собранные бинари в каталог назначения из PATH, и
   `taskctl version` печатает версию с коммитом сборки, а не прежнюю.

7. Установленный `taskctl` отбивает длинную первую строку и называет форму:

```
$ taskctl draft "уведомитель шумит из песочницы, потому что хук старта берёт адрес из окружения, а не из реестра чатов"
ошибка: первая строка черновика длиннее 72 символов (101): ...форма черновика в TASKFORM.md
```

   Черновик на живой доске от этого не заводится, отказ стоит до записи файла.

8. Во временной доске (`taskctl -C <tmp> init --prefix XR`) установленный
   бинарь кладёт болванку по форме и краснеет на переставленном разделе:
   `taskctl add --type bug` даёт файл с «Что происходит», «Чего хотим», «DoD»,
   «Ранг» и «Ход работы», а `taskctl lint` после перестановки «Выката» выше
   «Чего хотим» печатает строку с отсылкой к `TASKFORM.md`. На живой доске
   devkit тот же `taskctl lint` печатает «доска и архив в порядке».

9. Пользовательская часть, барьер «согласие»: прочитать `TASKFORM.md` и
   сказать, той ли формой дальше пишутся черновики и файлы задач. Обхода у
   барьера нет, критик со свежим контекстом даёт находки по тексту, но не
   согласие.

Второй круг добавляет шаги 10-12, они тоже идут после слияния и выката по
переустановленному бинарю; из ветки те же шаги обкатаны сборкой из дерева
(запись ниже, в «Проверке»).

10. Черновик с подразделами оформляется по форме. Во временной доске:

```
$ taskctl draft <<'EOF'
уведомитель шумит из песочницы

### Ситуация

хук берёт адрес из окружения.

### Осложнение

вторая сессия перебивает первую.

### Гипотеза

адрес берётся из реестра чатов.
EOF
XR-001: черновик в docs/tasks/drafts/XR-001.md, оформить: ...
$ printf '\n## DoD\n\nшум ушёл.\n' >> docs/tasks/drafts/XR-001.md
$ taskctl add --id XR-001 --title "Тихий уведомитель" --type bug --rank "25+4+2+5+2" --accept agent
$ cat docs/tasks/XR-001.md
# XR-001: Тихий уведомитель

## Что происходит

хук берёт адрес из окружения.

вторая сессия перебивает первую.

## Чего хотим

адрес берётся из реестра чатов.

## DoD

шум ушёл.

## Ранг

`25+4+2+5+2 = 38`, P2.

## Ход работы

- Из черновика XR-001 «уведомитель шумит из песочницы»: записан <дата>, оформлен <дата>.
```

11. Отсутствующий раздел встаёт по форме, а не в хвост. В том же файле
    дописать в конец «## Сценарий проверки» с шагом, затем
    `taskctl move XR-001 in-progress` и `taskctl move XR-001 check`: «Ход
    работы» остаётся выше «Сценария проверки», а `taskctl lint` печатает
    «доска и архив в порядке». Файлу без «Хода работы» (`# XR-002` c одним
    «## DoD» и «## Сценарий проверки») `taskctl file XR-002` печатает «раздел
    «Ход работы» дописан по форме», и раздел стоит между ними.

12. Отказ `taskctl draft` со stdin без совета про stdin:

```
$ printf 'уведомитель шумит из песочницы, потому что хук старта берёт адрес из окружения, а не из реестра чатов\n' | taskctl draft
ошибка: первая строка черновика длиннее 72 символов (101): она идёт заголовком, по ней черновик узнают в накопителе.
  подробности пишутся строками ниже через пустую строку, форма черновика в TASKFORM.md
код возврата: 1
```

    Тот же текст аргументом даёт третью строку про stdin и heredoc.

## Выкат

- 2026-08-18 слито: dc18999d, 5967d800, d8ec6430, dccc48b5, 32d27cec, b15bdb4c, 74797ea7, 0c8da36b
- smoke прогнан, 2026-08-18
- 2026-08-18 слито: 682bec9c, a03161a6, d2455bd1, 3f82c335, 7e5409cf, 1cfccf9e, 0a0911e1, ecb01dc3, 8cceda2b, 171f2259, d63b7c40
- smoke прогнан, 2026-08-18

## Проверка

Прогон шагов 6-8 после выката 2026-08-18, основной чекаут, бинарь от
`shipctl ship` (deploy = `devkitctl build`):

```
$ taskctl --version
taskctl v0.12.0-2267-g3529b060 (3529b060ed0a)
$ git log --oneline -1
1b313d86 docs(tasks): DK-444 в Check поездом
$ taskctl draft "уведомитель шумит из песочницы, потому что хук старта берёт адрес из окружения, а не из реестра чатов"
ошибка: первая строка черновика длиннее 72 символов (101): она идёт заголовком, по ней черновик узнают в накопителе.
  подробности пишутся абзацем ниже через пустую строку, форма черновика в TASKFORM.md
  многострочный текст удобнее отдать на stdin: taskctl draft <<'EOF' ... EOF
код возврата: 1
$ cd <tmp> && git init -q . && taskctl init --prefix XR && taskctl add --title "болванка новой строки" --type bug --rank "25+5+1+5+1" --accept agent
XR-001 заведена в backlog: P2, R=37
$ grep '^## ' docs/tasks/XR-001.md
Шаг 9, пользователь, 2026-08-18: TASKFORM.md и board-draft прочитаны, форма
принята с правками по ходу приёмки (концепция формы, разделы автора, утилиты
вместо «машины», скелет примера, чистка от мотивации и названий ради
названий), правки слиты в main. Уроки приёмки записаны черновиком DK-450,
проверка поведения стендом заведена строками DK-448 и DK-449.

## Что происходит
## Чего хотим
## DoD
## Ранг
## Ход работы
$ (вставлен «## Выкат» выше «Чего хотим») taskctl lint
docs/tasks/XR-001.md:9: раздел «Чего хотим» стоит после «Выкат», порядок разделов в TASKFORM.md
находок: 1
$ cd ~/projects/devkit && taskctl lint
доска и архив в порядке
```

Черновика на живой доске от шага 7 не появилось (`git status docs/tasks/drafts`
пуст). Шаг 9 за пользователем.

Обкатка шагов 10-12 из ветки второго круга, 2026-08-18, бинарь собран из
дерева (`go build` в `tools/taskctl`), временная доска XR:

```
$ taskctl add --id XR-001 --title "Тихий уведомитель" --type bug --rank "25+4+2+5+2" --accept agent
XR-001 заведена в backlog: P2, R=38, черновик перенесён в docs/tasks/XR-001.md
$ grep '^## ' docs/tasks/XR-001.md
## Что происходит
## Чего хотим
## DoD
## Ранг
## Ход работы
$ (дописан «## Сценарий проверки») taskctl move XR-001 in-progress && taskctl move XR-001 check && taskctl lint
XR-001: backlog -> in-progress
XR-001: in-progress -> check
доска и архив в порядке
$ taskctl file XR-002
раздел «Ход работы» дописан по форме
$ grep '^## ' docs/tasks/XR-002.md
## DoD
## Ход работы
## Сценарий проверки
$ printf 'уведомитель шумит из песочницы, потому что хук старта ...\n' | taskctl draft
ошибка: первая строка черновика длиннее 72 символов (101): она идёт заголовком, по ней черновик узнают в накопителе.
  подробности пишутся строками ниже через пустую строку, форма черновика в TASKFORM.md
код возврата: 1
```

Тот же текст аргументом печатает третьей строкой совет про stdin. После
замечаний 15-17 повторён шаг 10 с текстом без «###», но с «## DoD» внутри:
подразделы встали до «## DoD», файл открывает «Ранг» строкой
«`25+4+2+5+2 = 38`, P2.». Прогон шагов 10-12 по переустановленному бинарю
после выката второго круга ниже.

Прогон шагов 6-8 и 10-12 после выката второго круга 2026-08-18, основной
чекаут, бинарь от `shipctl merge`:

```
$ taskctl --version
taskctl v0.12.0-2286-g28347455 (283474557cca)
$ git log --oneline -1
b2db086a docs(tasks): DK-444 в Check
$ taskctl draft "уведомитель шумит из песочницы, потому что хук старта берёт адрес из окружения, а не из реестра чатов"
ошибка: первая строка черновика длиннее 72 символов (101): она идёт заголовком, по ней черновик узнают в накопителе.
  подробности пишутся строками ниже через пустую строку, форма черновика в TASKFORM.md
  многострочный текст удобнее отдать на stdin: taskctl draft <<'EOF' ... EOF
код возврата: 1
$ printf '<тот же текст>\n' | taskctl draft
ошибка: первая строка черновика длиннее 72 символов (101): она идёт заголовком, по ней черновик узнают в накопителе.
  подробности пишутся строками ниже через пустую строку, форма черновика в TASKFORM.md
код возврата: 1
$ cd <tmp> && git init -q . && taskctl init --prefix XR
$ taskctl draft <<'EOF' (заголовок, ### Ситуация, ### Осложнение, ### Гипотеза)
XR-001: черновик в docs/tasks/drafts/XR-001.md, оформить: taskctl add --id XR-001 ...
$ printf '\n## DoD\n\nшум ушёл.\n' >> docs/tasks/drafts/XR-001.md
$ taskctl add --id XR-001 --title "Тихий уведомитель" --type bug --rank "25+4+2+5+2" --accept agent
XR-001 заведена в backlog: P2, R=38, черновик перенесён в docs/tasks/XR-001.md
$ cat docs/tasks/XR-001.md
# XR-001: Тихий уведомитель
## Что происходит: обе строки ситуации и осложнения
## Чего хотим: адрес берётся из реестра чатов.
## DoD: шум ушёл.
## Ранг: `25+4+2+5+2 = 38`, P2.
## Ход работы: - Из черновика XR-001 «уведомитель шумит из песочницы»: записан 2026-08-18, оформлен 2026-08-18.
$ printf '\n## Сценарий проверки\n\n1. шаг.\n' >> docs/tasks/XR-001.md
$ taskctl move XR-001 in-progress && taskctl move XR-001 check && grep '^## ' docs/tasks/XR-001.md
## Что происходит / Чего хотим / DoD / Ранг / Ход работы / Сценарий проверки
$ taskctl lint
доска и архив в порядке
$ (файл XR-002 с одними «## DoD» и «## Сценарий проверки») taskctl file XR-002
раздел «Ход работы» дописан по форме
## DoD / Ход работы / Сценарий проверки
$ taskctl add --title "болванка" --type bug --rank "25+5+1+5+1" --accept agent && grep '^## ' docs/tasks/XR-003.md
## Что происходит / Чего хотим / DoD / Ранг / Ход работы
$ (вставлен «## Выкат» выше «Чего хотим») taskctl lint
docs/tasks/XR-003.md:9: раздел «Чего хотим» стоит после «Выкат», порядок разделов в TASKFORM.md
находок: 1
$ cd ~/projects/devkit && taskctl lint
доска и архив в порядке
```

Черновика на живой доске от шагов 7 и 12 не появилось. Шаг 9 за
пользователем.

Повторный smoke после шести коммитов доки на main мимо merge
(4978610e..422fd4c8), основной чекаут, 2026-08-18:

```
$ devkitctl build
taskctl v0.12.0-2303-g42f93276 (42f932760baa) -> /Users/vea/go/bin/taskctl
trackctl v0.12.0-2303-g42f93276 (42f932760baa) -> /Users/vea/go/bin/trackctl
сборка в порядке
$ taskctl --version
taskctl v0.12.0-2303-g42f93276 (42f932760baa)
$ taskctl draft "уведомитель шумит из песочницы, потому что хук старта берёт адрес из окружения, а не из реестра чатов"
ошибка: первая строка черновика длиннее 72 символов (101): она идёт заголовком, по ней черновик узнают в накопителе.
  подробности пишутся строками ниже через пустую строку, форма черновика в TASKFORM.md
  многострочный текст удобнее отдать на stdin: taskctl draft <<'EOF' ... EOF
код возврата: 1
$ (временная доска XR) taskctl add --title "болванка" --type bug --rank "25+5+1+5+1" --accept agent >/dev/null && grep "^## " docs/tasks/XR-001.md
## Что происходит
## Чего хотим
## DoD
## Ранг
## Ход работы
$ (выкат переставлен выше «Чего хотим») taskctl -C <tmp> lint
docs/tasks/XR-001.md:9: раздел «Чего хотим» стоит после «Выкат», порядок разделов в TASKFORM.md
находок: 1
$ cd ~/projects/devkit && taskctl lint
доска и архив в порядке
```

Шаг 6 сверён по версии бинаря с HEAD main (42f93276), черновика на живой
доске от шага 7 не появилось. Шаг 9 за пользователем.
