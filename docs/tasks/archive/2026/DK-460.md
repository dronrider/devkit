# DK-460: LLD: в корп-контуре ветка доходит до пуша только через ревью

## Что происходит

2026-08-20, разбор по сообщению коллеги: перед корп-MR агент локальное
ревью не гонял, пока его не попросили явно. По правилам оно обязано идти:
RULES.board.md применяется в корп-контуре целиком, кроме слияния и выката,
а LLD DK-074 (раздел про отказ merge) прямо говорит, что проверку
«замечания ревью закрыты, тесты зелёные» берёт локальное ревью и от
контура оно не зависит. Но держится это только текстом: ворота ревью
живут в shipctl merge, который в корп-контуре отказывает, а в скилле
board-ship корп-ветки сценария нет вовсе (греп по «корп», «MR», «gitlab»
пуст).

Путь агента в корп-репозитории кончается локальным коммитом, шага merge
нет, и триггер ревью не наступает: код уезжает в MR без локального ревью,
если пользователь не вспомнил попросить. Несостоявшееся ревью ничем не
видно, молчание неотличимо от штатной работы; дыра всплыла только жалобой
коллеги. Усиливают её два обстоятельства: в свежем клоне без обвязки
(CLAUDE.md с импортом RULES.local.md) сессия правил флоу не видит вовсе,
а изъятие для мелочи даёт легальный повод ревью не звать.

### Второй случай, LS-007 (CAP-2767)

Разобрано 2026-08-20 по той же сессии (чат «CAP-2783 проверка гипотезы
сертификатов», дерево задачи `../ls-cap-2767`). Дыра повторилась, и на этот раз
её никто не поймал: сессия отчиталась локальными коммитами, вопроса про ревью
в чате не прозвучало, и на момент разбора строка LS-007 стоит в In progress с
пустым ревью и готовым к MR кодом.

Ход задачи целиком: скилл `board-task` на «взять в работу», `shipctl start`,
код пакета `pkg/sdk/exchange`, `make lint`, мутация на свежем тесте, скилл
`proofread`, три локальных коммита. Ревью в этом списке нет вовсе:
`agentctl pick --role review` не звался, ревьювер не запускался, в
`docs/tasks/LS-007.md` раздела «Ревью» нет ни строки, а «Ход работы»
заканчивается словами «Пуш и MR за пользователем». Для сравнения, у соседних
LS-004 и LS-005 в архиве разделы «Ревью» на два и семь замечаний с исходами:
там ревью звали руками, и оба раза после вопроса пользователя.

Путь до пропуска ровно тот, что описан выше. Сессия позвала `board-task`, а он
про ревью говорит одной отсылкой («дальше код: ветка, ревью и выкат идут по
`board-ship`», SKILL.md:87). `board-ship` не звался, потому что звать его было
не на что: слияния в корп-контуре нет. При этом `shipctl start` в начале работы
отработал штатно, то есть в корп-пути `shipctl` присутствует и ворота вешать
есть на что, не хватает их именно на выходе.

Цена случая выше обычной. В дифф попала правка `crypt.Verify`, который сверял
подпись с вложенным в неё же сертификатом и пропускал чужой самоподписанный, и
новый публичный пакет SDK, который ждёт смежник на CAP-3000. Единственной
перекрёстной проверкой был прогон мутации, который автор устроил сам себе в том
же контексте.

### Ворота merge и сейчас не про наличие ревью

Тут черновик надо поправить. Ворота, которые он называет живущими в
`shipctl merge`, ловят не пропуск ревью, а незакрытые замечания:
`openReviewNotes` (`tools/shipctl/board.go:172-186`) собирает элементы раздела
«Ревью» без исхода, и слияние падает только на них. Пустой раздел и отсутствие
раздела дают пустой список, слияние проходит молча. Значит «ревью не было»
неотличимо от «ревью прошло без замечаний» и в обычном контуре тоже, а
корп-контур убирает последнее место, где процедура хотя бы проговаривается.
Ворота на пуш должны проверять запись о состоявшемся ревью, иначе они повторят
ту же дыру на новом месте.

### Шаг держал живой контекст, а не процедура

Между двумя пропусками был обратный случай. На LS-005, в том же корп-контуре и
так же без слияния, сессия позвала ревьювера сама («отправляю код на ревью,
правка большая и трогает публичный SDK»), часа через четыре после того, как её
поправили на LS-004. Наутро прошёл `/compact`, и в сводку поправка попала
списком коротких реплик пользователя («Ты делал ревью?», «Делай ревью») в
разделе про историю общения, а не пунктом процедуры. Следующая же задача ревью
не позвала.

То есть корп-контур объясняет не всё: шаг держался вниманием в живом контексте
и ушёл вместе с ним при сжатии. Довод в пользу механических ворот против ещё
одного абзаца в скилле, и заодно повод посмотреть, что из процедуры доски
переживает `/compact`.

## Чего хотим

В корп-контуре ветка доходит до пуша только через шаг агентского ревью:
процедура зовёт его сама после коммита, а пропущенное ревью видно снаружи
отказом ворот или явной пометкой, а не тишиной.

## Развилки

Где ставить ворота: команда готовности к пушу в shipctl (аналог ворот
merge для корп-контура) или корп-раздел процедуры в board-ship, который
зовёт ревью после коммита? И как переформулировать изъятие для мелочи,
чтобы им не накрывался полноценный MR.

## DoD

LLD-документ в `docs/lld/` с принятыми решениями по трём развилкам: где живут
ворота ревью на выходе корп-пути (команда готовности к пушу в `shipctl`
против корп-раздела `board-ship`), как проверяется наличие состоявшегося
ревью (а не только закрытость замечаний, и что с той же дырой в воротах
merge обычного контура), как переформулируется изъятие для мелочи, чтобы им
не накрывался полноценный MR. Документ утверждён пользователем, задачи
реализации нарезаны строками с зависимостями от LLD.

## Ранг

`50+8+3+0+2 = 63`, P1.

- Серьёзность 50: обязательный шаг флоу молча выпадает на всём корп-контуре,
  обход держится на памяти человека и дважды не сработал (жалоба коллеги,
  LS-007 с дефектом `crypt.Verify` в готовом к MR коде).
- Ценность 8: гарантия ревью перед выходом кода наружу это прямой вклад в
  главную ценность devkit, корп-контур это рабочий контур пользователя.
- Неопределённость 3: подход назван гипотезой (механические ворота на
  выходе), в деталях развилки из раздела «Вопрос».
- Поправка на баг 0: ревью в корп-контуре само не звалось никогда, ломаться
  было нечему.
- Рычаг 2: своя польза процедуре доски, чужую работу строка не разблокирует.

## Грумминг: сверка фактов (2026-08-20)

Оба машинных факта черновика перепроверены на текущем дереве и держатся:
греп по «корп», «MR», «gitlab» в скилле `board-ship` пуст (единственное
совпадение это строка про pre-push), а `openReviewNotes`
(`tools/shipctl/board.go:171-186`) на отсутствующем файле и пустом разделе
«Ревью» возвращает пустой список, слияние молчит. Соседние строки предмет не
закрывают: DK-259 чинит файловый доступ к боковой директории, DK-183 даёт
запись чистого вердикта в `taskctl review` («прошло чисто» отличимо от «не
гонялось»), и на эту запись воротам наличия ревью есть смысл опереться, но
самих ворот на выходе корп-пути она не ставит.

## Приёмка

- вид: user
- барьер «согласие»: предмет решений это процедура доски и правила ревью, и
  согласие на них даёт тот, чью работу они меняют
  - обхода нет: субагент-критик со свежим контекстом даёт находки по документу,
    но выбор между воротами в утилите и абзацем в скилле это решение
    пользователя, машине его выдать нечем
- 2026-08-22, развилка решения 2 закрыта пользователем: `merge` на третьем
  разряде отказывает, то есть выбор документа подтверждён. Читающие команды
  (`ready`, `status`, хук) остаются на предупреждении.

## Ход работы

- Из черновика DK-460 «в корп-контуре агентское ревью само не запускается»: записан 2026-08-20, оформлен 2026-08-20.
- Разработка: субагент pro/xhigh по вердикту pick (квота: claude-code/week_all 63% профицит, claude-code/week_max в снимке нет, снимок 1м назад, сдвига нет), 2026-08-20 23:26-23:27.
- Дизайн: `docs/lld/DK-460-review-gate.md`, четыре решения, 2026-08-20.
- Вычитка: 2 файла, 7 правок, 0 пометок, 2026-08-20.
- Правки по ревью: шесть замечаний исправлено, документ переписан в решениях 1,
  2 и 4 и в разделе про строки, 2026-08-21.
- Вычитка: 1 файл, 6 правок, 0 пометок, 2026-08-21.
- Правки по второму кругу ревью: три замечания исправлено, у ready описан
  контракт исходов, третий разряд ворота у merge стал отказом, 2026-08-21.
- Вычитка: 1 файл, 6 правок, 0 пометок, 2026-08-21.
- Правки по третьему кругу ревью: три замечания исправлено, вердикт готовности
  сведён с журналом прогонов, план переставлен под запись вердикта, развилка
  человеку переписана симметрично, 2026-08-22.
- Вычитка: 1 файл, 11 правок, 0 пометок, 2026-08-22.
- Правки по четвёртому кругу ревью: два замечания исправлено, план разрезан по
  рамке DK-183 на команду вердикта и процедуру записи, 2026-08-22.
- Вычитка: 1 файл, 5 правок, 0 пометок, 2026-08-22.
- Правки по пятому кругу ревью: два замечания исправлено, очередь с DK-185
  названа у обеих текстовых строк, перечень подкоманд уехал в строку 1,
  2026-08-22.
- Снаружи: проверка после выката, 2026-08-22 15:39-15:39.

## Ревью

- Решение 2: источники 2 и 3 доказывают назначение ревью, а не то, что оно состоялось. Этап «ревью» открывает agentctl pick --role review --record в момент выбора ревьювера (tools/agentctl/pick.go:468 -> internal/stage/stage.go:149), поэтому ворот проходит и задача, где ревьювера назвали, но не спавнили, и та, где ревью оборвалось на середине. В «Живом материале» это названо точно, «след назначенного ревью», а в решении 2 то же самое превратилось в «запись состоявшегося ревью», и цена подмены не названа, хотя DoD спрашивает именно про состоявшееся ревью.: исправлено, решение 2 переписано: следы разведены на запись ревьювера и отметку назначения (pick.go:460, recordStage), ворот собран из трёх разрядов (отказ без следа, тихий проход по записи, проход с предупреждением по отметке), цена третьего разряда названа прямо, и сказано, что состоятельность ревью машина не доказывает вовсе
- Строка 4 плана (pre-push в корп-клоне) не сходится ни со своей зависимостью, ни с рамкой документа: чем хук узнаёт про запись ревью, не сказано. Если зовёт shipctl ready, строка идёт после строки 2, а не «после строки 1, со строками 2 и 3 не связана»; если проверяет сам (тесты в строке названы python-овыми), это третья реализация критерия, которую запрещают рамка документа и решение 1. Вдобавок в решении 4 находка описана только по разделу «Ревью» файла задачи, без источников 2 и 3 из решения 2, то есть критерий хука разойдётся с воротом заведомо.: исправлено, хук своего критерия не заводит: находит задачу по ветке и печатает вердикт shipctl ready, при отсутствии утилиты или строки молчит нулём; строка 4 плана переставлена после строки 2 с этим основанием, разряды у хука и ворот теперь одни
- Решение 1: шестая проверка ready (чистое дерево, непустой дифф) обоснована как «механика до спавна ревьювера» по фактуре DK-183 п. 7, но единственный названный момент вызова ready стоит после ревью: строка 3 плана описывает корп-порядок как коммиты, ревью по вердикту pick, потом ready. В таком порядке проверка приезжает, когда ревьювер уже оплачен, и того, ради чего взята, не делает. Нужен либо второй момент вызова в процедуре (перед спавном ревьювера), либо другое обоснование проверки.: исправлено, у ready названы два момента вызова (до спавна ревьювера и после ревью), седьмая проверка получила двойное основание: ближнее про пуш (грязное дерево и пустой дифф на выходе) и дальнее про экономию спавна; корп-порядок в строке 3 плана переписан под два вызова
- Решение 4 не проиграно на LS-007, на том самом случае, которым документ обосновывает задачу: там сессия не звала ни status, ни merge, и из трёх мест сработало бы только третье, печать хука в момент ручного пуша человека, без отбоя. Отсюда следует, что в корп-контуре ни одно из решений выход кода не останавливает, а только сообщает, и это главное, что утверждает человек; в документе прямо это нигде не сказано, а отвергнутый вариант объясняет цену отбоя, но не цену находки, которую можно пролистать.: исправлено, в решении 4 появился подраздел «Что из этого сработало бы на LS-007»: merge и status там не звались, скилл не открывался, срабатывает только печать хука при ручном пуше, и отсюда вывод вслух, что в корп-контуре решения сообщают, а не останавливают; тот же вывод вынесен в рамку и в DoD, цена находки названа рядом с ценой отбоя
- Решение 1: сказано «Проверок у ready шесть», а перечислено семь (пять функций merge, запись ревью из решения 2 и «шестая своя» про чистое дерево и непустой дифф). По этому абзацу диспетчер режет строку 2, и состав проверок в ней должен быть однозначным.: исправлено, проверок семь: шесть общих с merge (пять сегодняшних плюс след ревью) и седьмая своя про чистое дерево и непустой дифф; состав перечислен одним списком, в строке 2 плана та же нумерация
- Решение 1: «Ревью с пустым диффом и ревью незакоммиченной реализации там встречались в трёх проектах» фактурой DK-183 не подпирается. Пункт 7 называет два случая одной доски (IRC-094 и IRC-065), а третий пример там про другой симптом, незакоммиченную строку вердикта pick (DK-120).: исправлено, формулировка исправлена по источнику: два случая одной доски, IRC-094 с пустым диффом и IRC-065 с незакоммиченной реализацией, про три проекта больше не сказано
- Трёхразрядность ворота не доведена до контракта команды и до её машинного читателя. Решение 1 оставляет у ready два исхода («готово» либо отказ с перечнем недостающего), строка 2 плана обещает «код возврата и короткую строку», не говоря, сколько у них значений, а решение 4 велит хуку печатать вывод ready, не говоря, печатает ли он на проходе с предупреждением. При двузначном выходе разряд 3 сливается с разрядом 2 у каждого машинного читателя, и предупреждение пропадает ровно там, где в корп-пути у него единственный читатель, в терминале человека в момент пуша.: исправлено, у ready описан контракт исходов: коды 0 (готово), 3 (готово с предупреждением) и 1 (не готово), код 2 оставлен за неверным употреблением по main.go:149, первая строка вывода повторяет код словами; хук читает именно коды и печатает вывод на 1 и 3, молчит на 0 и на всём остальном; в строках 2 и 4 плана контракт назван вместо прежней «короткой строки»
- Разряд 3 у merge приходит постфактум, и от этого пункт DoD «дыра обычного контура закрыта тем же воротом» завышен. Предупреждения merge собираются до ребейза, а печатаются в отчёте уже слитой задачи (tools/shipctl/ops.go:786 -> :870), когда код в main и едет на прод, поэтому фраза решения 2 «строка отчёта видна диспетчеру в тот момент, когда позвать ревьювера ещё дёшево» верна для ready и неверна для merge. По архиву новый ворот жёстко ловит 3 задачи из 236 (DK-089, DK-117, DK-121), а остальные 63 из 66 переводит в предупреждение после слияния: это надо сказать числом там же, где стоит 66.: исправлено, третий разряд разведён по командам: merge на нём отказывает (предупреждение там приходит после ff и выката, ops.go:786 и :870), читающие команды предупреждают; цена выписана числами (3 из 236 без следа вовсе, 63 с одной отметкой назначения, это 27% слияний, каждое чинится строкой записи), пункт DoD переписан под эти числа, а выбор между отказом и предупреждением вынесен подразделом «Строка, которую решает человек» с обоими последствиями
- Два места не переехали на новый словарь решения 2 и потому спорят с ним. Строка 319 (решение 3): «есть ветка задачи с коммитами сверх main, значит нужна запись ревью», хотя ворот теперь берёт и отметку назначения, а «запись ревьювера» это отдельный сорт следа. Строка 467: заголовок строки 1 плана «Ворот наличия ревью» диспетчер перенесёт в доску как есть, а ворот следа ревью и ворот наличия записи это разные обещания.: исправлено, оба места переведены на словарь решения 2: в решении 3 «значит нужен след ревью по решению 2», заголовок строки 1 плана стал «Ворот следа ревью», в её составе названы все три разряда с разной строгостью у merge и читающих команд
- Контракт исходов не сведён с журналом прогонов. shipctl пишет в .devkit/log код возврата (tools/shipctl/runlog.go:17, logRun(1) из fail и logRun(0) на успехе), а сводка журнала считает ошибкой любой ненулевой код (tools/devkitctl/devkitctl.py:2220 и :2238, «доля ошибок»). При контракте решения 1 в ошибки уедут и код 3 («готово с предупреждением»), и код 1 первого вызова, который документ сам называет штатным состоянием, а хук зовёт ready на каждом пуше. Надо сказать, что ready пишет в журнал, иначе сводка перестанет показывать то, ради чего заведена.: исправлено, контракт сведён с журналом: вердикт готовности это ответ команды, а не её падение, поэтому в .devkit/log идёт ноль на всех трёх исходах (включая код 3 и штатный код 1 первого вызова), а ненулевой код журнал получает только когда команда ответить не смогла; сказано правило «код отвечает вызывающему про задачу, журнал про команду», названа причина (хук зовёт ready на каждом пуше, доля ошибок у devkitctl.py:2220 и :2238 иначе теряет смысл) и оговорка реализации: путь «не готово» не идёт через fail (main.go:111), который пишет единицу сам
- Строка 1 плана меняет поведение merge раньше, чем процедура учится писать запись, а порядок относительно строки 5 объявлен свободным («5 в любой момент», при том что сама строка 5 обещает увести третий разряд в редкий случай). В составе строки 1 названы RULES.board.md п. 2 и tools/shipctl/README.md, но не скилл board-ship, а именно его читает диспетчер в момент ревью: в разделе «Ревью» пп. 5-6 (SKILL.md:91-95) сказано только «строка на замечание, суть и исход», про вердикт без замечаний нет ничего. В промежутке между строками 1 и 5 отказ merge будет приходить на честной работе, а где брать запись, не написано нигде.: исправлено, план переставлен: штатная запись вердикта вместе с правкой скилла board-ship (раздел «Ревью», пп. 5-6, SKILL.md:91-95) и промтов review-* стала строкой 1 и стоит перед воротом, который теперь строка 2; порядок объявлен не техническим, а выкатным, с причиной «пока писать вердикт нечем и в скилле про это ничего нет, отказ приходил бы на честную работу», остальные строки перенумерованы и ссылки на них поправлены; DK-469 разобрана отдельным абзацем: разряды решения 2 она не ломает (ворот считает элементы, а не исходы, это сказано и в решении), но форму записи вердикта задаёт, поэтому строка 1 идёт после неё или вместе с ней, а ворот следа от неё технически не зависит. Исход вписан рукой: taskctl review resolve на этом замечании отказывает, считая его закрытым чистым вердиктом, это и есть баг DK-469 на живом тексте.
- Подраздел «Строка, которую решает человек» описывает ветки неровно, а решать человек будет по нему. У ветки «Предупреждение» названа слабость, у ветки «Отказ» только цена в 63 задачи и «чинится одной строкой», хотя круг назад документ говорил про этот же вариант обратное («строгость покупается ложными отказами на честной работе, а рубеж, который врёт, отключают»), и этот довод из документа исчез вместе с прежним отвергнутым вариантом, вместо того чтобы переехать сюда. Не названо и то, что самый дешёвый выход из отказа это написать запись, а у того, кто зовёт merge позже сессии ревью (поезд, автономный режим), отчёта ревьювера на руках может не быть. Фраза «второй вариант дешевле и слабее» это вердикт автора в разделе, отданном человеку.: исправлено, подраздел переписан симметрично: у каждой ветки четыре пункта (что даёт, чего стоит, где слаба, чем смягчается); у отказа названа слабость, снятая кругом раньше (выход из отказа это написать запись, а у зовущего merge позже сессии ревью, в поезде и автономном режиме, отчёта на руках может не быть, и рубеж закрывается формальной строкой, после чего его перестают принимать всерьёз), у предупреждения названо его место во времени (печатается после ff и выката); авторский вердикт из подраздела убран, выбор документа остался в решении, а довод против отказа переехал в новый отвергнутый вариант «оставить merge на предупреждении»
- Перенумерация оставила хвост, который спорит с новым порядком: в решении 2, отвергнутый вариант «опереть ворот на цель DK-183», строка 393 по-прежнему говорит «Строка про команду вердикта вытаскивается из цели отдельно (раздел про строки), ворота её не ждут, и порядок между ними любой». Теперь ворот ждёт её прямо: строка 2 плана стоит после строки 1, а пункт DoD объясняет, зачем («чтобы отказ не пришёл раньше, чем в процедуре появится команда для записи вердикта»). Читатель решения 2 сделает вывод, что ворот можно катить первым, то есть ровно то, из-за чего порядок и переставляли.: исправлено, хвост убран: отвергнутый вариант теперь говорит «опереть ворот на цель DK-183 целиком» и кончается тем, что нужный пункт вытащен двумя первыми строками плана, а ворот идёт после них по порядку выката, а не по технике
- Строка 1 плана склеивает команду вердикта с правками текстов, а цель DK-183 в рамках нарезки просит обратного: «чистый вердикт в taskctl review идёт отдельной задачей от правок текстов» (docs/tasks/DK-183.md, раздел «Задачи цели»). Там же названа соседка DK-185, которая правит те же kit/agents/review-* и board-ship, и сказано координироваться, а не дублировать. Документ вытаскивает пункт из DoD цели вслух, а расхождение с её рамкой нарезки и соседнюю строку не называет вовсе. Склейка может быть сознательной, порядок выката её оправдывает, но сказать это надо в самой строке, иначе диспетчер заведёт строку поверх чужой.: исправлено, выбран развод, а не склейка: прежняя строка 1 разрезана по шву рамки DK-183 на строку 1 (только команда вердикта в taskctl review: код, тесты, справка, текстов не трогает) и строку 2 (процедура записи вердикта: раздел «Ревью» board-ship и промты review-*); в строке 2 названа судьба DK-185 (не поглощается и не сужается, предмет у неё другой, общий только файл) и единственное требование к очереди: параллельно не ехать, идущая второй подхватывает формулировки первой; план перенумерован на семь строк, ссылки по тексту и пункт DoD поправлены
- Оговорка про очередь с чужой DK-185 не сведена с составом строки 5. Сама строка 5 пишет «оговорка про параллель та же, что у строки 2, файл board-ship общий», а сводные фразы говорят другое: итог порядка это «только строка 2 просит очереди с чужой DK-185», и пункт DoD называет пересечение тоже одной строкой 2. Диспетчер строит зависимости по сводным фразам, пустит DK-185 параллельно строке 5, и две руки сойдутся в одном файле скилла.: исправлено, сводные фразы сведены с составом: в итоге порядка сказано, что очереди с DK-185 просят обе текстовые строки, 2 и 5, потому что board-ship правят обе и DK-185 правит его же, и пункт DoD переписан тем же порядком
- Ни одна строка плана не правит RULES.board.md п. 3, где перечислены подкоманды taskctl review (add, resolve, show, stats): после новой подкоманды вердикта перечень становится неполным, а по RULES.core.md дока едет вместе с правкой. Строка 1 объявляет себя «только код» и по рамке нарезки DK-183 процедурных текстов не берёт, но перечень подкоманд это не процедура, а справочный факт про утилиту, того же сорта, что README, который в строку 1 уже включён. Строка 3 правит п. 2, строка 5 правит п. 1 и п. 2, п. 3 не берёт никто, и второе место с тем же перечнем (board-ship SKILL.md:92) закрыто строкой 2, а это только подчёркивает пропуск.: исправлено, перечень подкоманд taskctl review из RULES.board.md п. 3 вошёл в состав строки 1 с обоснованием: это справочный факт про утилиту того же сорта, что README, а не шаг процедуры; второй такой же перечень в board-ship SKILL.md:92 закрыт строкой 2, и это сказано там же

## Сценарий проверки

Вид приёмки user, барьер «согласие»: предмет задачи это решения, меняющие
процедуру доски, поэтому машинная часть проверяет только целость и полноту
документа, а согласие с самими решениями даёт человек.

Машинная часть, прогнана в дереве задачи (числа после пяти кругов ревью,
2026-08-22).

```text
$ grep -c "^## Решение" docs/lld/DK-460-review-gate.md
4
$ grep -c "^Отвергнуто:" docs/lld/DK-460-review-gate.md
15
$ taskctl review show DK-460 | grep -c "\[исправлено\]"
16
$ python3 tools/devkitctl/devkitctl.py doctor --fix
починено: перегенерирована карта: /Users/vea/projects/devkit-dk-460/docs/map.md
$ grep -n "^DK-460" docs/map.md
98:DK-460.1 Ворота стоят командой `shipctl ready`, а не абзацем скилла
99:DK-460.2 Ворот различает запись ревьювера и отметку назначения
100:DK-460.3 Изъятие для мелочи привязано к маршруту, а не к размеру
101:DK-460.4 Снаружи видно тремя местами: status, отказ merge и находка при пуше
$ python3 hooks/check-symbols.py docs/lld/DK-460-review-gate.md docs/tasks/DK-460.md docs/map.md; echo rc=$?
rc=0
```

Повторный `doctor` после `--fix` расхождения карты уже не показывает. Две
оставшиеся находки к задаче не относятся: порядок разделов в
`docs/tasks/DK-397.md` (чужая задача) и отсутствие `.devkit/deploy.local` в
свежем дереве задачи (болванку, заведённую `--fix`, я убрал, выкат берёт конфиг
основного чекаута).

Человеческая часть: прочитать решения 1-4 и либо сказать «согласен», либо
назвать, что переделать. Спорных мест три, и они названы прямо в документе.

Первое и главное решается одной строкой ответа: что делает `merge` на третьем
разряде, когда ревьювера назначили, а записи ревьювера нет. Обе ветки, отказ и
предупреждение, стоят с ценой и слабостью в подразделе «Строка, которую решает
человек» решения 2; документ выбрал отказ, довод против него лежит там же
отвергнутым вариантом.

Второе: у ворота ревью нет пометки-исключения, он единственный такой из четырёх
(решение 2). Третье: в корп-контуре ни одно из мест выход кода не
останавливает, сильнейшее из них печатает находку при ручном пуше человека
(решение 4, разбор LS-007).

## Выкат

- 2026-08-22 слито: 1d3c3bff, 77c7ff33, 34aaa189, 1e05159e, 7b3ddf4b, 4509baa3, 92f306e0, bbe430d1, c29e02f3, e9bf3a3f, cb87eab9, 4a0119b0
