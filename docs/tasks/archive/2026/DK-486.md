# DK-486: Интерактивный вопрос клиента виден в чате дашборда и отвечается оттуда

## Что происходит

Замечание пользователя 2026-08-22 по POC DK-397. Дашборд набирает проекты
сканом корня из конфига (root = ~/projects, всё с docs/TASKS.md становится
проектом само). Так в списке оказался it-road-course, каталог которого claude
на машине ни разу не подтверждал. Первый же чат в нём встал на невидимом
tmux-вопросе «Yes, I trust this folder». Сессия не родилась, реплика висела
«не доставлено», повторы наплодили трёх застрявших клиентов.

### Проверено разбором 2026-08-25

Предмет на месте: проекты дашборд по-прежнему набирает сканом корня
(`scanProjects` в `tools/dashboard/projects.go`, ключ `root` в конфиге,
`tools/dashboard/config.go`), а проверки доверия каталогу нет ни в одном файле
дашборда. Опора гипотезы на готовую механику держит наполовину. Снимок
tmux-панели сервер уже берёт (`tools/dashboard/tmux.go`, `capture-pane` для
карточки хода), но детектора интерактивного вопроса у него нет. Клавиши он не
шлёт вовсе, `send-keys` живёт только в `tools/agentctl/usage.go`. Значит показ
вопроса клиента в чате это не приписка к стоящей механике, а своя, с обратным
ходом. Дублей нет ни на доске, ни в накопителе. Соседние DK-479, DK-480,
DK-483 и DK-485 про чат, DK-465 про чужой харнес, предмета подключения проекта
не трогает ни один.

Найденное сканом выглядит подключением. Проект виден и предлагает чаты, но не
рабочий. Это видно не в момент подключения, а минутой тишины посреди первого
разговора. Пользователь ждал разбора задачи, получил зависший чат.

### Разбор 2026-08-26

Ответы автора на вопросы груминга получены. Общий вопрос лучше решить сразу, но
разбить тоже можно. Обхода через терминал нет: работа идёт в дашборде, где
терминала не бывает. Про четвёрку вопрос не понят.

Четвёрка это проекты, которые скан находит сейчас: devkit, goonies,
it-road-course и xr-proxy, боковые деревья задач `scanProjects` отсеивает сам.
Речь о том, теряются ли они при переходе на явный список.

Предложенный разрез, до подтверждения автором не оформлен. Работы две, и стоят
они не в куче, а в порядке: канал интерактивных вопросов это предпосылка
подключения. Без канала проект не подключить вовсе, доверие подтвердить негде,
а автозаписи в обход диалога в этой гипотезе нет. Сам же канал доезжает до
пользователя и без подключения. Он делает видимым и отвечаемым вопрос любого
застрявшего чата.

- Первой строкой канал: интерактивный вопрос клиента виден в чате дашборда и
  отвечается оттуда. Детектор вопроса в снимке панели, показ структурным блоком
  с кнопками по механике решения DK-354.4, ответ уезжает клавишами в tmux.
  Проверяется на доверии каталогу и на вопросе другого рода (меню или y/n).
  Оценка task, M, R=63 (50+7+2+0+4), приёмка mixed по барьеру «глаза».
  Серьёзность 50. Чат с новым проектом встаёт намертво, а дорога до машины с
  терминалом это не выход, а болезненный обход. До 75 оценка не идёт:
  подключённые проекты работают.
- Второй строкой подключение: проект появляется в дашборде явным действием,
  скан лишь предлагает кандидатов, при подключении гоняется готовность
  (доверие, читаемость доски, бинари). Оценка task, M, R=35 (25+6+2+0+2),
  приёмка mixed. После первой строки, связь через `taskctl dep add`.

Открытым остался перевод четвёрки: подключается она молча или ждёт
подтверждения по каждому проекту.

### Ответы автора 2026-08-26

Разрез утверждён. Серьёзность 50 принята. Терминала в дашборде не бывает, и
обхода у застрявшего чата нет.

Про состав проектов ответ шире вопроса. Проекты подключаются раздельно и явно,
и у дашборда должно быть обратное действие. Проект, по которому работ пока
нет, закрывается и уходит из списка. Отсюда вопрос про пополнение состава.
Реестра такого у devkit сейчас нет: единственный источник это ключ `root` в
`~/.devkit/dashboard.local`, каталоги под ним становятся проектами сами
(`scanProjects`). Реестр заводится второй строкой вместе с подключением.

## Чего хотим

Проект появляется в дашборде только явным действием «подключить проект»;
скан корня лишь предлагает кандидатов. Подключение сразу делает проект рабочим. Прогоняется готовность (доверие
каталогу, читаемость доски, бинари), и вопрос доверия задаётся тут же
структурным блоком в чате с кнопками «Доверяю»/«Не доверяю» (механика решения
DK-354.4: детектор уже читает tmux-панель, ответ уезжает клавишами в tmux). Та
же механика показывает в чате любой интерактивный вопрос клиента и после
подключения. Автозаписи доверия в ~/.claude.json в обход диалога нет. Решение
всегда за человеком. Полуподключённых состояний нет. Подключён значит чаты и
конвейер работают.

## Границы

Задача берёт только канал вопросов. Явное подключение проекта, реестр
подключённых и закрытие проекта уехали второй строкой, и до неё дашборд
набирает проекты прежним сканом.

## Развилки

Судьба нынешнего скана: остаётся ли он как «найдено, не подключено» с
предложением подключить, или корень уходит из конфига целиком в пользу списка.
Как переводить текущую четвёрку проектов без молчаливой потери.

## DoD

- Чат дашборда показывает интерактивный вопрос клиента структурным блоком:
  вопрос найден в снимке панели, варианты стоят кнопками.
- Нажатая кнопка уезжает клавишами в tmux-сессию разговора, и разговор идёт
  дальше без терминала.
- Дорога проверена на вопросе доверия каталогу («Yes, I trust this folder») в
  проекте, который claude на машине не подтверждал.
- Той же дорогой проходит вопрос другого рода: выбор из меню либо y/n.
- Пока вопрос висит, чат показывает блок. Тишины, за которой не видно
  спрашивающего клиента, не остаётся.
- Тесты: разбор вопроса на снимках панели, отправка клавиш на стенде tmux.
- Дока: раздел README дашборда про интерактивные вопросы клиента.

## Ранг

`50+7+2+0+4 = 63`, P1.

- Серьёзность 50. Разговор с проектом, которого claude не подтверждал, встаёт
  намертво. Сессия не рождается, реплика висит «не доставлено». Обхода из
  дашборда нет, терминала там не бывает (ответ автора 2026-08-26).
- Ценность 7. Виснет не только первый разговор в новом проекте, а любой.
- Неопределённость 2. Снимок панели сервер уже берёт (`tools/dashboard/tmux.go`),
  образец отправки клавиш стоит в `tools/agentctl/usage.go`. Открыты признак, по
  которому вопрос отличается от обычного вывода клиента, и разбор вариантов.
- Поправка на баг 0. Механики не было вовсе, это новое поведение.
- Рычаг 4. Без канала проект не подключить, и второй вопрос клиента пойдёт этой
  же дорогой.

## Приёмка

- вид: mixed
- барьер «глаза»:

## Ход работы

- Из черновика DK-486 «Проект подключается явно и подключённый работает сразу»: записан 2026-08-22, оформлен 2026-08-26.
- 2026-08-26, отложен: разрез предложен и лежит в разделе «Разбор»: канал интерактивных вопросов первой строкой (task, M, R=63), явное подключение проекта второй (task, M, R=35); ждём подтверждения формулировок и ответа, подключается ли нынешняя четвёрка проектов молча или по подтверждению
- Вычитка: 2 файла, 16 правок, 0 пометок, 2026-08-26.
- Уточнение: вопрос: Груминг DK-486. Проверил по коду: скан на месте (scanProjects по root из конфига, projects.go), проверки доверия каталогу нет нигде, а снимок tmux-панели дашборд уже берёт (tmux.go, capture-pane для карточки хода), но детектора интерактивного вопроса у него нет и клавиши он не шлёт вовсе (send-keys живёт только в agentctl usage). Дублей по доске и накопителю нет. Осталось три вопроса. 1. Объём. Показ интерактивного вопроса клиента в чате с кнопками это своя механика, а не приписка к готовой: детектор панели, разбор вариантов, обратный ход клавишами. Задача берёт её целиком (любой вопрос клиента виден в чате и отвечается оттуда) или ограничивается доверием каталогу при подключении, а общий канал уходит отдельной строкой после неё? От ответа цена M против L. 2. Обход и серьёзность. Когда чат встал на невидимом вопросе, обход у тебя такой: открыть каталог в терминале, подтвердить доверие руками и вернуться в дашборд. Он дешёвый и очевидный (серьёзность 25) или болезненный, с застрявшими клиентами и потерянным разговором (серьёзность 50)? 3. Перевод нынешней четвёрки. Проекты, которые скан уже нашёл, при первом запуске новой версии подключаются сами и молча, или дашборд ставит их кандидатами и ждёт подтверждения по каждому?, 2026-08-26 00:00-14:55.
- Уточнение: вопрос: Спасибо, отвечаю и свожу к разрезу. 1. Разбить, и вот почему. Ты прав, что доступа к одной директории мало: клиент спрашивает и про меню выбора, и про y/n, и про доверие. Но эти две работы стоят в порядке, а не в куче: канал интерактивных вопросов это предпосылка подключения, а не его часть. Без канала подключить проект нельзя вовсе (доверие подтвердить негде, автозаписи в обход диалога мы не хотим), а канал сам по себе доезжает до пользователя и без подключения: любой вопрос застрявшего чата становится видимым и отвечаемым. Поэтому первой строкой идёт общий канал, второй подключение поверх него, связь через dep. 2. Принял, обхода из дашборда нет. Ставлю серьёзность 50: сценарий чата с новым проектом встаёт намертво, а дойти до машины и открыть терминал это болезненный обход, а не выход. До 75 не поднимаю: подключённые проекты работают, продукт целиком не встаёт. 3. Четвёрка это фраза из твоего же черновика, я её просто повторил. Проверил: скан сейчас находит devkit, goonies, it-road-course и xr-proxy (боковые деревья задач он отсеивает сам). Вопрос был про них: после перехода на явный список они подключаются молча или ждут подтверждения по каждому. Сверяю формулировку до оформления, скажи да или поправь: DK-486 (task, M, R=63: 50+7+2+0+4, приёмка mixed по барьеру «глаза»). Интерактивный вопрос клиента виден в чате дашборда и отвечается оттуда. Детектор вопроса в снимке панели, показ структурным блоком с кнопками по механике DK-354.4, ответ уезжает клавишами в tmux. Проверяется на доверии каталогу и на втором вопросе другого рода (меню или y/n). Вторая строка (task, M, R=35: 25+6+2+0+2, приёмка mixed). Проект появляется в дашборде явным подключением, скан лишь предлагает кандидатов, при подключении гоняется готовность (доверие, читаемость доски, бинари). После DK-486. Осталось решить по третьему: нынешняя четвёрка подключается сама или по подтверждению?, 2026-08-26 14:55-17:35.

## Сценарий проверки

1. Поднять клиента в каталоге, которого он не знает. Посмотреть, что видит
   разбор панели. У свежего клиента вопрос доверия идёт без номеров.
2. Ответить ходом курсора. Клиент должен подняться.
3. Прогнать пакет дашборда. Новые стенды обязаны краснеть на прежнем коде.
4. Глазами. Открыть дашборд и найти на строке задачи чип «клиент ждёт ответа»
   у разговора, стоящего на вопросе. Панель чата при этом закрыта.

## Проверка

Живой вопрос доверия снят с панели 2026-08-28, сессия `dk486-probe` в каталоге
`/private/tmp/dk486-untrusted`. Номеров у вариантов нет вовсе. Курсор клиент
держит на «No, exit».

```
 Security guide

 > No, exit
   Yes, I trust this folder

 Enter to confirm . Esc to cancel
```

Знак курсора и точку-разделитель тут пришлось перерисовать клавиатурными.
В самой панели стоят чужие знаки клиента, в файл задачи они не едут.

Ответ ходом курсора на живой сессии, `Down` и следом `Enter`. Клиент поднялся,
каталог записался доверенным.

```
Claude Code v2.1.250, Fable 5 with high effort, Claude Max
/private/tmp/dk486-untrusted

hasTrustDialogAccepted: True
```

Краснота новых стендов на прежнем коде (`tmux.go` с HEAD). Виден и промах
разбора, и чем кончался прежний ответ. Номер «2» в виджет без номеров не
попадает никуда, а Enter подтверждает «No, exit» и снимает сессию.

```
--- FAIL: TestParseTmuxAskBareTrust (0.00s)
    tmux_test.go:820: вопрос доверия без номеров разобран в 0 вариантов: []
--- FAIL: TestTmuxAnswerBareTrustKeys (0.73s)
    --- FAIL: TestTmuxAnswerBareTrustKeys/доверяю (0.41s)
        tmux_test.go:891: клавиши поданы не те: ["2" "Enter"], жду ["Down" "Enter"]
    --- FAIL: TestTmuxAnswerBareTrustKeys/не_доверяю (0.32s)
        tmux_test.go:891: клавиши поданы не те: ["1" "Enter"], жду ["Enter"]
```

Краснота стендов показа без открытой панели (ветка виджета в списке чатов и в
строке доски отключена):

```
--- FAIL: TestChatEntryAskFromPaneWithoutJournal (0.96s)
    chat_test.go:2396: вопрос с панели не узнан: stuck="", ждал "ждёт ответа в терминале"
--- FAIL: TestWidgetWaitSeenWithoutPanel (0.25s)
    waiting_test.go:461: клиент стоит на вопросе, а строка доски молчит
```

Весь пакет дашборда на исправленном коде зелёный:

```
ok  	github.com/dronrider/devkit/tools/dashboard	296.094s
```

Выкат POC и живость сервера:

```
GOWORK=off go build -o ~/.devkit/dashboard-poc/dashboard .
launchctl kickstart -k gui/501/com.devkit.dashboard-poc
curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:7131/
302
```
