# DK-523: Скилл prose подмешивает эталоны в момент письма, зовут четыре точки

Цель: [tasks/DK-446.md](../../tasks/DK-446.md)

## Что происходит

К моменту письма модель хуже держит стилевую инструкцию из начала контекста, и
разбор цели собрал на это ссылки («Style Amnesia», «Lost in Multi-Turn
Conversation»). Проба DK-173 показала то же самое. Три задачи переписывались с
полным списком замечаний в контексте, но шаблоны воспроизводились всё равно.
Примеры держат форму сильнее правил, а после 2-10 образцов улучшение почти
прекращается. Сейчас в devkit нет скилла, который приезжает в контекст в
момент написания текста.

## Чего хотим

Скилл `prose` в `kit/skills/`. В контекст письма попадает случайный набор
эталонных фрагментов из корпуса DK-522 и короткий горячий список из пяти
примет замера. Ядро правил при этом не растёт, решение DK-173.2 остаётся в
силе. Зовут скилл четыре точки, где пишется долгоживущий текст: оформление
черновика в `board-groom`, файл задачи в `board-task`, LLD в `exec-xhigh`,
правка README.
Набор фрагментов случайный, чтобы тексты не выходили однородными.

## DoD

1. `kit/skills/prose/SKILL.md` есть, в нём горячий список из пяти примет и
   правило брать эталоны того же жанра.
2. `grep -l prose kit/skills/board-groom/SKILL.md kit/skills/board-task/SKILL.md
   kit/agents/exec-xhigh.md` печатает три пути.
3. Два запуска скилла дают разные наборы фрагментов, это показывает тест.
4. Правка README как точка вызова названа в самом скилле.
5. Прогон `prompt-test` по скиллу записан в «Ход работы».

## Ранг

`25+8+2+0+2 = 37`, P2.

- Серьёзность 25: трение с дешёвым обходом.
- Ценность 8: ради этого цель и затевается, первый черновик пишется живой
  прозой.
- Неопределённость 2: форма скилла ясна, открыты объём выборки и место
  горячего списка.
- Рычаг 2: ускоряет свой поток по одной теме.

## Ход работы

- Вычитка: 1 файл, 5 правок, 1 пометка, 2026-08-25.
- Вычитка: 7 файлов, 3 правки, 2 пометки, 2026-08-25.
- Разработка: субагент opus/medium по вердикту pick (квота: week_all 20%, week_max 2%, снимок 1м назад, сдвига нет), 2026-08-25 17:01-17:01.
- Тесты: `python3 tools/devkitctl/parallel.py`, 17 из 17 компонентов, `OK`,
  5м10с. Новая проверка `check_prose` по чекауту main даёт четыре находки, по
  ветке ноль.
- Прогон `prompt-test` не состоялся. Сценарий `14-prose-sample` написан и
  разбирается, `obeycheck --list` печатает его четырнадцатой строкой. Две
  раскладки собраны в `/tmp/dk523-with` и `/tmp/dk523-without`, скиллы лежат в
  `home/.claude/skills`. Пробная сессия падает на затравке HOME. Токен в
  `~/.devkit/obey-home` отозван 2026-08-04, `claude -p` отвечает `401 OAuth
  access token has been revoked`. Заход не может пересобрать затравку
  из связки ключей, дамп секрета режется правами сессии. Команда ждёт живой
  затравки:

      obeycheck --scenarios tools/obeycheck/scenarios --only 14-prose-sample \
        -k 1 --tier mini --home-seed ~/.devkit/obey-home \
        /tmp/dk523-with /tmp/dk523-without
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 20%, week_max 2%, снимок 1м назад, сдвига нет), 2026-08-25 17:21-17:45.

## Ревью

- Сценарий проверки, шаг 4, проверяет не то. Критерий «черновик проходит пороги check-prose.py» опирается на код возврата. В файловом режиме check-prose.py возвращает 0 при любых находках, пока kit/prose.toml держит mode=warn: worst=1 ставится только при conf.mode=="block" (см. run_files). Формально проходит даже чистый шаблон. Шаг стоит переписать на проверку конкретного признака в выводе, например отсутствия строки «порог блокировки».: исправлено, шаг 4 требует пустого грепа по «порог блокировки». Причина названа там же, при mode=warn код возврата всегда 0. Прогон вложен в шаг: на kit/skills/board-task/SKILL.md греп даёт 2, на docs/tasks/DK-523.md 0
- Board-groom/SKILL.md получил новый шаг 13, старые шаги 13-16 стали 14-17. Внешние ссылки на номер шага этого не учли. docs/lld/DK-354-draft-screen.md:353 и docs/tasks/drafts/DK-396.md:23 называют шаг 14 board-groom. Раньше это был шаг «Собрать вопросы», теперь там «Дописать в черновик». Ссылки стоит поправить на новый номер или заменить на имя шага.: исправлено, номера заменены именами шагов в трёх местах. Это docs/lld/DK-354-draft-screen.md строки 46 и 353, там же нашлась третья ссылка «шаги 14-15», и docs/tasks/drafts/DK-396.md строка 23. Имя шага переживёт следующую вставку
- kit/skills/prose/prose_test.py, тест test_два_запуска_без_seed_дают_разные_наборы. Комментарий называет пул «15 с лишним» фрагментов. Фактический пул по всем жанрам 33 фрагмента: lld 9, readme 5, skill 7, task 12. Число в комментарии стоит поправить на действительное.: исправлено, в комментарии теперь «двенадцать фрагментов из 33 (lld 9, readme 5, skill 7, task 12)», числа пересчитаны по корпусу

## Сценарий проверки

1. `grep -l prose` по трём путям из DoD печатает три пути.
2. Тест скилла на двух запусках печатает разные наборы фрагментов:
   `cd kit/skills/prose && python3 -m unittest prose_test`, в выводе `OK` и 30
   тестов.
3. `python3 kit/skills/check-skills.py` печатает `ok: скиллы`, а тот же
   `check_prose` по чекауту main печатает четыре находки: пропавший горячий
   список и три точки без выборки.
4. `python3 hooks/check-prose.py <файл текста, написанного со скиллом>`
   печатает шесть метрик. Ни в одной строке нет пометки «порог блокировки».
   Ответ читается из вывода. Код возврата тут ничего не говорит, при
   `mode = "warn"` в `kit/prose.toml` файловый режим отдаёт 0 при любых
   находках. Красный и зелёный прогон:

       $ python3 hooks/check-prose.py kit/skills/board-task/SKILL.md | grep -c "порог блокировки"
       2
       $ python3 hooks/check-prose.py docs/tasks/DK-523.md | grep -c "порог блокировки"
       0

## Выкат

- 2026-08-25 слито: 81441f44, c7d6018c, ff886aae, 022c7015, 3e2dfc84, bb228607, d0d90fd1, fac3474e
- smoke прогнан, 2026-08-25

## Проверка

2026-08-25, сценарий прогнан на выкаченном main витком цели DK-446. Все четыре
шага сошлись, вывод ниже.

Шаг 1, три точки вызова:

```
$ grep -l prose kit/skills/board-groom/SKILL.md kit/skills/board-task/SKILL.md kit/agents/exec-xhigh.md
kit/skills/board-task/SKILL.md
kit/agents/exec-xhigh.md
kit/skills/board-groom/SKILL.md
```

Шаг 2, тест выборки:

```
$ cd kit/skills/prose && python3 -m unittest prose_test
Ran 30 tests in 0.018s
OK
```

Шаг 3, сторож скиллов:

```
$ python3 kit/skills/check-skills.py
ok: скиллы, проверено 14
```

Четырёх находок `check_prose` шаг ждёт от чекаута без задачи. Сняли их в ветке
до слияния, вывод лежит в «Ходе работы». На выкаченном main находок нет, и это
тот же ответ с другой стороны.

Шаг 4, метрики прозы:

```
$ python3 hooks/check-prose.py docs/tasks/DK-523.md
слов 772, предложений 52, абзацев 9
  средняя длина предложения: 14,8 слов
  предложений длиннее 25 слов: 13,5 %
  довод в той же фразе: 25,0 %
  двоеточие в середине фразы: 15,5 на 1000 слов  порог предупреждения 15 перейдён
  хвост «..., а не Y»: 0,0 на 1000 слов
  абзац кончается обобщением: 0,0 %
$ python3 hooks/check-prose.py kit/skills/board-task/SKILL.md | grep -c "порог блокировки"
2
$ python3 hooks/check-prose.py docs/tasks/DK-523.md | grep -c "порог блокировки"
0
```

Шесть метрик напечатаны, пометки «порог блокировки» нет ни в одной строке.

DoD 5 остаётся открытым. Прогон `prompt-test` не идёт, затравка HOME стенда
`obeycheck` держит отозванный 4 августа токен. Пересобрать её дампом связки
ключей не дают права сессии ни исполнителю, ни диспетчеру. Сценарий стенда
`14-prose-sample` написан и разбирается, команда прогона лежит в «Ходе работы».
Починка стенда ушла черновиком DK-530. Пока прогона нет, задача стоит в Check.
