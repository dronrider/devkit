# DK-524: proofread перекалиброван по замеру: пять пунктов остатка, старые убраны

Цель: [tasks/DK-446.md](../../tasks/DK-446.md)

## Что происходит

Типология `proofread` из восьми пунктов написана в DK-173 до замера. Замер цели
её проверил числами и развёл две группы. Полная форма «не X, а Y», копула «X
это Y», слово «живой», глагол «едет», триады и скобки стоят у людей не реже,
чем у агентов. Это лексика пользователя. Правка их портит текст. Довод в той
же фразе, двоеточие-довод, хвост «..., а не Y», афоризм в конце абзаца,
метафорный глагол при абстрактном подлежащем, усилители и моральная лексика про
код у людей отсутствуют. В типологии их пока нет.

## Чего хотим

Чек-лист `proofread` совпадает с чек-листом сторожа DK-521. Пункты, которых
человеческий ориентир не подтвердил, убраны с записью причины. Пять пунктов
остатка добавлены с парами «было, стало» и корпусом. Пары берутся из раздела
«Что показал замер» файла цели и из корпуса DK-522.

## Развилки

Что убрано из типологии и почему. Ни одна из шести форм не стояла отдельным
пунктом первого списка, они сидели внутри пунктов подсказкой или примером,
поэтому убраны они не строкой, а разбором. Все шесть названы поимённо в
разделе «Что не трогает», и правка любой из них считается ложной находкой на
сторожевом прогоне.

- Полная форма «не X, а Y» (1,5 на тысячу слов у агентов против 2,3 у людей).
  Пунктом не стояла, но её считал доводом сторож DK-521 (`NOT_Y_RE` внутри
  метрики `argued`). Счёт снят.
- Копула «X это Y» (4,1 против 4,1). Стояла в старом пункте 1 подсказкой,
  чем заменяют снятое тире. Подсказка осталась, а сама копула теперь названа
  нетрогаемой прямо.
- Слово «живой» (стоит в словаре пользователя, 5 реплик из 179 кандидатов
  замера DK-522). Правилось как выдуманный термин по старому пункту 4.
- Глагол «едет» («дока едет вместе с правкой»). Попадал под старый пункт 3
  об одушевлении, хотя это оборот из самих правил.
- Триады и скобки (у агентов как у людей). Скобки старый пункт 1 сам
  предлагал как замену тире, и одновременно они попадали под правку
  синтаксиса.

Что добавлено. Замер назвал семь примет остатка, в чек-лист они сложились
пятью новыми пунктами: довод в той же фразе, хвост «..., а не Y»,
вывод-афоризм в конце абзаца, метафорный глагол при абстрактном подлежащем,
усилители вместе с моральной лексикой про код. Усилители и мораль про код
слиты в один пункт: правка у них одна, слово вычёркивается без замены.
Двоеточие-довод отдельным пунктом уже стояло (старый пункт 1), его
переписали на язык замера. Старый пункт об одушевлении кода поглощён
метафорным глаголом и моральной лексикой. Итого двенадцать пунктов, у
каждого пара «было, стало» в `pairs.md`.

Решение по сторожу. Чек-лист и метрики сведены механически: ключ метрики
(`sentence_len`, `argued`, `colon_mid`, `but_not_tail`, `aphorism`) выписан
рядом с пунктом скилла, а совпадение списков держит тест
`check_prose_test.py`. Отвергнут вариант с одним общим файлом-источником,
из которого генерятся оба текста. У сторожа числа и регулярки, у скилла
проза с примерами, общего у них только имя приметы. Генератор ради шести
строк дороже теста.

Решение по корпусу. `corpus.md` вырос до 24 фраз, по две на пункт, а порог
пересчитан долей от первого прогона DK-184: 14 из 16 это 21 из 24. Заодно
сторож прозы перестал мерить сам корпус (каталоги `corpus` и файл
`corpus.md` рядом с `testdata`). Плохие фразы лежат там нарочно, и
предупреждение на каждой записи файла было бы шумом.

## DoD

1. Неподтверждённые пункты убраны, причина каждого удаления записана в файл
   задачи.
2. Пять пунктов остатка добавлены, у каждого пара «было, стало».
3. `corpus.md` скилла обновлён, сторожевой прогон по нему проходит свои пороги.
4. Чек-лист скилла и метрики `check-prose.py` называют одни и те же приметы.
5. Прогон `prompt-test` по скиллу записан в «Ход работы».

## Ранг

`25+6+2+0+1 = 34`, P2.

- Серьёзность 25: трение с дешёвым обходом.
- Ценность 6: вычитка идёт на каждом долгоживущем тексте, сейчас она правит не
  то.
- Неопределённость 2: чек-лист готов замером, открыты пары и пороги
  сторожевого прогона.
- Рычаг 1: правка текста скилла без новой механики.

## Ход работы

- Вычитка: 1 файл, 2 правки, 0 пометок, 2026-08-25.
- Разработка: opus/medium по вердикту pick, 2026-08-25 18:25.
- Ревью: sonnet/high по вердикту pick, 2026-08-25 18:25-18:26.
- Вычитка: 3 файла (SKILL.md, pairs.md, corpus.md), 29 правок, 2 пометки,
  2026-08-25. Пометки: «Ревьювер у веточного текста уже свежий контекст»
  (фраза без глагола, ни под один пункт типологии не попадает) и «свалка
  смысла» в разделе «Что не трогает» (кандидат в словарь).
- Сторожевой прогон по обновлённому `corpus.md`, субагент sonnet со свежим
  контекстом, 2026-08-25 18:44. Находок 23 из 24 при пороге 21, ложных 1 при
  пороге 2. Пропущена фраза 20 («Скилл командной работы над одним проектом»),
  ложная одна: длинное предложение во втором абзаце эталона DK-089. Все шесть
  форм пользователя из блока «Лексика пользователя» прогон не тронул.
- `regcheck -C hooks --base HEAD --tests hooks/check_prose_test.py --
  python3 -m unittest check_prose_test`: тест краснеет на HEAD и проходит на
  текущем коде.
- prompt-test: сценарий стенда написан, живой прогон не выполнен. Затравка
  `~/.devkit/obey-home` держит токен, отозванный 4 августа, пересобрать её не
  хватило прав сессии. Это известное препятствие, черновик DK-530, обходов
  запретов песочницы не искал. Команда прогона после починки стенда стоит
  пунктом 4 «Сценария проверки».
- Вычитка: 1 файл (DK-524.md), 4 правки, 1 пометка, 2026-08-25. Пометка:
  глагол «везёт» в шаге 4 «Сценария проверки» стоит рядом с нетрогаемым
  «едет», и пункт 6 типологии на него не натянут.
- Полный локальный прогон `python3 tools/devkitctl/parallel.py -j 6`:
  17 компонентов, OK, 4м56с.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 23%, week_max 3%, снимок 9м назад, сдвига нет), 2026-08-25 19:01-19:16.
- Сценарий проверки после выката, 2026-08-25 витком цели DK-446. Шаг 1,
  `python3 -m unittest check_prose_test` из `hooks`, дал `Ran 31 tests` и `OK`.
  Шаг 2, `python3 hooks/check-prose.py kit/skills/proofread/SKILL.md`, дал
  среднюю длину 12,1 слова, длинных 4,2 %, довод 9,5 %, двоеточие 6,1 на 1000,
  `but_not_tail` 1,7 на 1000. Обобщение в конце абзаца вышло 5,0 % и подняло
  предупреждение. Порогов блокировки не перейдено, ожидание шага сошлось.
  Шаг 3, сторожевой прогон свежим субагентом sonnet, дал 22 находки из 24 на
  плохой половине при пороге 21. Ложных на эталонной половине ноль при пороге
  два, на блоке «Лексика пользователя» тоже ноль. Шаг 4 не прогнан, стенд не
  поднимается (DK-530).
- Находка сторожевого прогона. Пункты 2 и 3 типологии не отделяют избыточный
  довод и двоеточие-вместо-тире от содержательных. Вычитка по букве правила
  тянется к местам, где причина несёт факт. Уточнение формулировок ушло
  черновиком DK-532.
- Снаружи: проверка после выката, 2026-08-27 14:37-23:18.

## Ревью

- без замечаний

## Сценарий проверки

1. `cd hooks && python3 -m unittest check_prose_test`: 31 тест, `OK`. Среди
   них `test_full_form_not_x_but_y_is_not_argued` (полная форма доводом не
   считается) и `test_skill_names_every_metric_key` (чек-лист скилла называет
   все ключи метрик).
2. `python3 hooks/check-prose.py kit/skills/proofread/SKILL.md`: ни один порог
   блокировки не перейдён.
3. Сторожевой прогон: субагент sonnet со свежим контекстом читает `SKILL.md`,
   `pairs.md` и оба словаря, потом перечисляет находки по `corpus.md`.
   Ожидание: не меньше 21 находки из 24 на плохой половине, не больше двух
   ложных на эталонной, и ни одной находки на блоке «Лексика пользователя».
4. Стенд послушания, сценарий `16-proofread-voice`. Скиллы едут в сессию
   чекаутом devkit (`--devkit`), раскладка правил их не везёт, поэтому обе
   раскладки тут одинаковые. Сравниваются два прогона между собой.

   ```
   python3 tools/devkitctl/rules.py --layout core ../dk524-rules-a \
     tools/obeycheck/testdata/project
   python3 tools/devkitctl/rules.py --layout core ../dk524-rules-b \
     tools/obeycheck/testdata/project
   obeycheck --only 16-proofread-voice -k 2 --devkit ../devkit-before-dk524 \
     ../dk524-rules-a ../dk524-rules-b
   obeycheck --only 16-proofread-voice -k 2 --devkit . \
     ../dk524-rules-a ../dk524-rules-b
   ```

   Ожидание: на чекауте до правки хотя бы одна клетка красная (вычитка правит
   копулу, скобки или полную форму «не X, а Y», либо оставляет афоризм), на
   этой ветке все четыре клетки зелёные. Столбец «вердикт» тут ничего не
   значит. Раскладки одинаковые, и сравнивать столбцы между собой нечего.

## Выкат

- 2026-08-25 слито: 67298583, 3c72cbec, 50d4dfa8, 2aacdaef, 4ea3cc66, b6abcbd1
- smoke прогнан, 2026-08-25

## Проверка

2026-08-27, прогон на выкаченном main вместе с приёмкой у пользователя.

Шаг 1, тесты сторожа:

```
$ cd hooks && python3 -m unittest check_prose_test
Ran 36 tests in 1.069s
OK
```

Шаг 2, сам скилл под сторожем:

```
$ python3 hooks/check-prose.py kit/skills/proofread/SKILL.md
слов 1191, предложений 98, абзацев 20
  средняя длина предложения: 12,2 слов
  предложений длиннее 25 слов: 4,1 %
  довод в той же фразе: 10,2 %
  двоеточие в середине фразы: 5,9 на 1000 слов
  хвост «..., а не Y»: 0,8 на 1000 слов
  абзац кончается обобщением: 5,0 %  порог предупреждения 2 перейдён
```

Блокирующих порогов нет. Обобщения в конце абзацев держатся выше порога
предупреждения, это остаток прежней редакции скилла.

Шаг 3, сторожевой прогон свежим субагентом (sonnet). Находок 23 из 24 на
плохой половине при пороге 21, ложных на эталонной ноль при пороге 2, на блоке
«Лексика пользователя» ноль. Пропущена одна фраза пункта 10, заголовок-загадка.
На свежий взгляд она читается обычными словами. Отдельно субагент разобрал два
спорных места эталонной половины и оставил их без замечания. Поясняющее
двоеточие вводит объяснение и тире собой не подменяет, а связка «поэтому»
несёт суть абзаца.

Приёмка у пользователя 2026-08-27. Пять пунктов остатка прочитаны, замечание
одно и закрыто правкой. Пункт 3 говорил про подменённое тире и умалчивал, что
менять знак на знак бесполезно. Дописано, что перестраивается сама фраза
(коммит 93211bb6).

Шаг 4, стенд послушания, прогнан 2026-08-27 на выкаченном main:

```
прогон: сценариев 1, раскладки 2, повторов 1, всего 2 сессий (конец: сессия)
[1/2] 16-proofread-voice / rules-a / повтор 1: зелено
[2/2] 16-proofread-voice / rules-b / повтор 1: зелено
сценарий                                             rules-a  rules-b  вердикт
вычитать постановку, не тронув лексику пользователя      1/1      1/1  держится
```

Живая сессия зовёт вычитку, снимает афоризм и усилители, а копулу, полную
форму «не X, а Y», слово «живой» и скобки оставляет на месте.

Дорога до этой клетки шла через пять починок стенда и скилла, каждая своей
задачей цели: DK-545, DK-530, DK-546, DK-547 и DK-548. До них шаг 4 прогнать
было нечем.
