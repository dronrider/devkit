# DK-546: Стенд кладёт скиллы в дом прогона, сценарии про них перестают молчать

Цель: [tasks/DK-446.md](../../tasks/DK-446.md)

## Что происходит

Ситуация: сценарии стенда проверяют, как правила меняют поведение сессии.
Часть из них меряет работу через скилл, например вычитку прозы.
Осложнение: скиллов в доме прогона нет. Раскладка кладёт определения агентов из
kit/agents и на этом останавливается, каталога skills она не заводит вовсе, и
это видно прямо в коде (tools/obeycheck/env.go, около строки 276). Сессия
поднимается, читает правила, а звать скилл ей нечем. Прогон 2026-08-27 на
сценарии 16-proofread-voice дал две красные клетки с текстом «вычитка не
звалась», и выглядело это находкой про правила. Проверка дома сохранённого
прогона показала другое. Каталоги agents и projects на месте, skills нет, и
красная клетка тут про раскладку.
Вопрос: какие скиллы кладутся в дом прогона и откуда.
Гипотеза: раскладка кладёт скиллы из kit/skills тем же порядком, что и
определения агентов, и так же уступает готовому дому из --home-seed. Проверять
это должен тест, который смотрит на дом после раскладки, иначе дыра вернётся
тихо.

## Чего хотим

## DoD

1. Дом прогона держит каталог скиллов, разложенный из `kit/skills`, рядом с
   определениями агентов.
2. Готовый дом из `--home-seed` раскладку перекрывает, как перекрывает её для
   агентов.
3. Тест смотрит на дом после раскладки и краснеет, когда каталога скиллов в
   нём нет.
4. Прогон сценария 16-proofread-voice даёт клетки, которые отражают поведение
   агента. Вычитку он либо зовёт, либо нет, и свалить на пустой дом уже
   нечего.

## Ранг

`50+6+1+5+3 = 65`, P1.

`50+6+1+5+3 = 65`, P1.

- Серьёзность 50. Стенд ведёт не туда. Три сценария выглядят работающими и
  дают красноту, а меряют пустой дом.
- Ценность 6. Работает на каждом прогоне стенда, а стенд остаётся
  единственной проверкой правок промптов.
- Неопределённость 1. Место в коде названо, порядок раскладки берётся у
  агентов.
- Поправка на баг 5. Раскладка неполная с рождения стенда, и заметили это
  через месяц после того, как первый сценарий про скиллы был написан.
- Рычаг 3. Без него не едут DK-449, DK-497 и четвёртый шаг сценария DK-524.

## Ход работы

- Из черновика DK-546 «Стенд не кладёт скиллы в дом прогона, сценарии про них молчат»: записан 2026-08-27, оформлен 2026-08-27.
- Раскладка. `tools/obeycheck/env.go` кладёт `.claude/skills` из `kit/skills`
  сразу следом за `.claude/agents` из `kit/agents`, тем же порядком. Копия
  идёт, только если каталога `.claude/skills` ещё нет. Готовый дом из
  `--home-seed` кладётся раньше и такой каталог уже приносит, поэтому
  раскладка ему уступает целиком, как и для агентов. Самопроверка
  `kit/skills/check-skills.py` с её тестом лежит в `kit/skills/` не
  подкаталогом, скиллом не является. Реальная раскладка машины
  (`tools/devkitctl/devkitctl.py`, `check_skills`) её тоже не копирует. В дом
  прогона она не едет.
- Тесты. `TestRunEnvironment` (tools/obeycheck/run_test.go) проверяет теперь и
  `.claude/skills/proofread/SKILL.md` после обычной раскладки. Новый
  `TestHomeSeedOverridesSkills` кладёт в затравку свой `.claude/skills` с
  проверенным вручную скиллом и смотрит, что раскладка его не трогает и не
  докладывает рядом скиллы devkit. `regcheck -C tools/regcheck ... -C
  tools/obeycheck --tests run_test.go` подтвердил красноту на HEAD и зелень на
  правке.
- Прогоны. `GOWORK=off go test -count=1 .` в tools/obeycheck зелёный, 17.2s.
  `GOWORK=off python3 tools/devkitctl/parallel.py` зелёный, 17 из 17
  компонентов за 5m21s, среди них `go:obeycheck`, `skills`, `check-skills`.
- Третья вещь по пункту 3 не нашлась. У профиля харнеса (`kit/harness/*.toml`)
  два каталога, которые devkit кладёт машине из своего kit: определения
  субагентов (`[delegate] agents_dir`) и скиллы (`[skills] dir`). Оба уже
  разложены. Остальное содержимое kit, `kit/harness/*.toml`, `kit/templates/`,
  `kit/prose.toml`, сессия читает по симлинку на чекаут devkit внутри проекта
  (DK-193). Отдельного каталога в HOME под него раскладка не заводит.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 44%, снимок 4м назад, сдвига нет), 2026-08-27 19:43-19:57.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 44%, снимок 8м назад, сдвига нет), 2026-08-27 19:57-20:27.
- Снаружи: проверка после выката, 2026-08-27 20:27-22:18.

## Ревью

- Раздела «Сценарий проверки» в файле задачи нет вовсе. TASKFORM.md держит его обязательным до Check и до слияния. DoD-4 просит живой прогон сценария 16-proofread-voice: клетки должны показать настоящее поведение агента, раз пустой дом больше не мешает. В «Ходе работы» есть только go test и parallel.py, живого прогона нет и следа. Вид приёмки agent стоит по умолчанию, а у него сценарий обязан нести хотя бы один шаг после выката поверх тестов ветки. Проверить это пока нечем.: исправлено
- tools/obeycheck/README.md, раздел «Как устроен прогон», шаг 1 всё ещё называет только определения субагентов из kit/agents/. Про скиллы из kit/skills/ там ни слова, хотя раскладка теперь кладёт их тем же порядком. Дока про устройство прогона это вход для новичка. Поведение стенда в ней должно ехать той же задачей.: исправлено
- Список исключений в env.go (check-skills.py, check_skills_test.py) уже расходится с боевой раскладкой. `skill_files()` в tools/devkitctl/devkitctl.py явно выбрасывает `__pycache__` и точечные файлы у каждого скилла, copyTree такого фильтра не держит. Проверено прогоном: `kit/skills/__pycache__` (мусор локального прогона теста самопроверки) уезжает в `.claude/skills/__pycache__` дома прогона. Та же дыра у `goal-loop/__pycache__` и `prose/__pycache__`, их заводят тесты внутри самих скиллов. Нужен фильтр по признаку пути, тем же порядком, что и в `skill_files()`.: исправлено

## Сценарий проверки

1. `cd tools/regcheck && GOWORK=off go run . -C ../obeycheck --base b1c821a8
   --tests tools/obeycheck/run_test.go -- go test -count=1 -run
   'TestRunEnvironment|TestHomeSeedOverridesSkills' .`, вердикт «тест краснеет
   на b1c821a8 и проходит на текущем коде». На b1c821a8 раскладка ещё не
   заводит `.claude/skills`. Это исходная поломка. На текущем коде раскладка
   кладёт скилл, а готовый дом из `--home-seed` её перекрывает.
2. `cd tools/regcheck && GOWORK=off go run . -C ../obeycheck --base 06c67519
   --tests tools/obeycheck/env_test.go -- go test -count=1 -run
   'TestSkillLayoutSkipsNoise' .`, тот же вердикт про 06c67519. На нём
   раскладка скиллов уже есть, а фильтра мусора ещё нет: `__pycache__` и
   точечные файлы уезжают в дом прогона. На текущем коде фильтр их снимает тем
   же порядком, что `skill_files()` в tools/devkitctl/devkitctl.py.
3. `cd tools/obeycheck && GOWORK=off go test -count=1 .`, пакет проходит
   целиком.
4. `GOWORK=off python3 tools/devkitctl/parallel.py`, полный прогон зелёный, 17
   из 17 компонентов.
5. Живой прогон сценария 16-proofread-voice идёт после выката задачи,
   четвёртым шагом сценария DK-524. Клетки должны отражать поведение агента:
   вычитку он либо зовёт, либо нет. Пустой дом стенда больше не мешает.

## Выкат

- 2026-08-27 слито: 70b2db5d, 5d2e6e36, e35fb942, 26e5f9e7, 06c67519

## Проверка

2026-08-27, прогон на выкаченном main.

Шаг 1, раскладка скиллов:

```
тест краснеет на b1c821a8 и проходит на текущем коде, регрессия закрыта
(тесты: tools/obeycheck/run_test.go)
```

Шаг 2, фильтр мусора:

```
тест краснеет на 06c67519 и проходит на текущем коде, регрессия закрыта
(тесты: tools/obeycheck/env_test.go)
```

Шаг 3, пакет целиком:

```
ok  	github.com/dronrider/devkit/tools/obeycheck	19.208s
```

Шаги 4 и 5 прогоняло слияние командой из `.devkit/deploy.local`.

Шаг 6, живой прогон стенда, сделан в тот же день соседней задачей DK-548.
Скиллы в доме прогона появились, сессии стали звать вычитку, и мера дошла до
шести заходов из шести. До правки было два из четырёх.
