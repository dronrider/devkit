# DK-649: Полный прогон тестов dashboard виснет на TestChatStopDropEndsLiveSession

Цель: [tasks/DK-161.md](../../tasks/DK-161.md)

## Что происходит

Полный прогон `GOWORK=off go test .` в tools/dashboard на main падает по
таймауту: `panic: test timed out after 3m0s` (и после 10m в прогоне merge),
последним запускается `TestChatStopDropEndsLiveSession`, горутина висит в
`net/http roundTrip` внутри `doReq`. Одиночный прогон любого из этих тестов
зелёный за секунды, значит виснет не сам тест, а состояние, оставшееся от
предыдущих тестов `chatEnv`. Слияние любой ветки из-за этого отбивается:
`shipctl merge` гоняет полный набор, и dashboard красный на каждом прогоне.
Тест пришёл с веткой POC DK-397 (коммиты dc28d603 и соседние).

Вечного зависания в пакете нет. Прогон с запасом по времени проходит за 356
секунд, а процессор при этом загружен на четверть ядра, и почти всё время
прогон чего-то ждёт. Ждёт он запуска фикстур. Каждый стенд пишет себе свежие
исполняемые файлы утилит (taskctl, tmux, agentctl). Первый запуск свежего
исполняемого файла macOS отдаёт проверке подписи, и стоит она от десятых долей
секунды до нескольких секунд, а повторный запуск того же файла идёт за пять
миллисекунд. Семьсот тестов со своими фикстурами складывают эту проверку
сотнями. Отсюда и разнобой в дампах. Виснет тот тест, на котором кончился срок,
и в каждом прогоне он свой.

## Чего хотим

Полный прогон tools/dashboard зелёный и укладывается в обычное время. Причина
зависания найдена и закрыта кодом с регрессионным тестом, краснота
подтверждена прогоном regcheck.

## DoD

1. `GOWORK=off go test . -count=1` в tools/dashboard зелёный полным набором,
   три прогона подряд без таймаута.
2. Причина названа в файле задачи, фикс закрыт тестом, краснота подтверждена
   прогоном regcheck.
3. `shipctl merge` перестаёт отбиваться на пакете dashboard.

## Ранг

- Серьёзность 75: красный билд, слить нельзя ни одну ветку.
- Ценность 0: чистый ремонт.
- Неопределённость 1: место известно, причина гонки ищется.
- Баг +5: регресс от слитого POC DK-397.
- Рычаг 4: разблокирует слияния всех задач цели и соседних сессий.

## Ход работы

- Причину дала проверка подписи macOS на первом запуске свежего исполняемого
  файла фикстуры. Исполняемым остался один общий переходник
  (`exec /bin/sh "$0.sh"`) в постоянном каталоге временных файлов. `writeScript`
  кладёт в каталог стенда ссылку на переходник и тело фикстуры отдельным
  неисполняемым файлом. Полный прогон пакета сократился с 356 до 106 секунд,
  загрузка процессора выросла с 24 до 76 процентов. Уговор сторожит структурой
  регрессионный тест `TestScriptFixturesShareOneExecutable` в
  `tools/dashboard/fixture_test.go`.
- Разработка: субагент opus/medium по вердикту pick (квота: week_all 2%, week_max 37% протух (панель разбивку не дала, цифра из кеша клиента возрастом 4ч 32м назад), снимок 6м назад, сдвига нет), 2026-08-31 15:09-15:12.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 2%, week_max 37% протух (панель разбивку не дала, цифра из кеша клиента возрастом 4ч 42м назад), снимок 35м назад, сдвига нет), 2026-08-31 15:48-20:53.
- Снаружи: проверка после выката, 2026-08-31 20:53-21:12.

## Ревью

- сценарий агентского вида целиком повтор тестов ветки: три прогона go test в tools/dashboard и regcheck зелены ещё до слияния и ничего не доказывают про выкаченное (ACCEPTANCE.md, «Самодостаточность агентской части»). Нужен шаг после слияния в main, отсутствующий сейчас: реальный вызов shipctl merge (или прогон дашборд-пакета на смерженном main) как подтверждение DoD п.3, что гейт слияния больше не бьётся таймаутом: исправлено
- шаг «regcheck подтверждает красноту» не воспроизводится буквально: обычный вызов regcheck --base main -- go test . в tools/dashboard проходит зелёным и на main тоже, потому что сам фикс (scriptShim и новый writeScript) лежит в server_test.go, а автопоиск переносит на базу все изменённые *_test.go файлы вместе с фиксом. Красноту даёт только regcheck -C tools/dashboard --base main --tests tools/dashboard/fixture_test.go (путь от корня репозитория, server_test.go и quota_test.go нарочно не в списке). Вписать в сценарий проверки точную команду с этими флагами, иначе шаг не повторить: исправлено

## Сценарий проверки

Агентский. Предусловие ветки, до слияния:

1. `GOWORK=off go test . -count=1 -timeout 280s` в tools/dashboard ветки
   печатает `ok` три раза подряд.
2. Из корня репозитория
   `regcheck -C tools/dashboard --base main --tests tools/dashboard/fixture_test.go -- env GOWORK=off go test . -count=1 -timeout 120s -run TestScriptFixturesShareOneExecutable`
   говорит, что тест краснеет на базе. Флаг `--tests` тут обязателен. Фикс
   лежит в `server_test.go`, автопоиск уносит этот файл на базу вместе с
   фиксом, и наивный вызов зеленеет на обеих сторонах.

Проверка выкаченного, после слияния:

3. `GOWORK=off go test . -count=1 -timeout 280s` в tools/dashboard основного
   чекаута на смерженном main печатает `ok`.
4. `shipctl merge` очередной ветки проходит и пакетом dashboard не отбивается.
   Шаг закрывает диспетчер фактом слияния следующих задач.

## Выкат

- 2026-08-31 слито: 0162db7d, fb54804e, d8039a3a, 9d961563
- smoke прогнан, 2026-08-31

## Проверка

Сценарий прогнал диспетчер цели DK-161 (fable), не автор правки (opus),
2026-08-31 21:00. Шаги 1-2 гонялись в ветке исполнителем и ревьювером. Шаг 3
на слитом main из основного чекаута:

```text
$ GOWORK=off go test . -count=1 -timeout 280s   # tools/dashboard
ok  	github.com/dronrider/devkit/tools/dashboard	105.772s
```

Шаг 4 (merge очередной ветки не отбивается пакетом dashboard) закрывается
фактом слияния следующих задач цели.
