# DK-677: Тесты notify в hooks падают в чистом окружении прогона merge

Цель: [tasks/DK-161.md](../../tasks/DK-161.md)

## Что происходит

После DK-641 прогон тестов слияния идёт в свежем дереве с временным HOME и
урезанным PATH. Первый же merge (DK-643) упал компонентом hooks. Три теста
notify_test.TestArgumentMode получают пустой sent(), уведомление до
подставного бэкенда не доходит. В прогретом окружении те же тесты зелёные
(hooks 586 ok в прогонах исполнителей). Тесты полагаются на живое окружение
сессии, ровно класс 98b43e7, который должен ловить прогон в чистом дереве.
Пока компонент красный, слияние отбивает любую ветку.

### Причина

Метод `HookCase.cli()` в `hooks/notify_test.py` не задавал подпроцессу свой
`cwd`. Подпроцесс наследовал рабочую директорию самого раннера тестов.

Аргументный режим `notify.py` сам зовёт `sandbox_reason(os.getcwd())`. Он
читает корень под TMPDIR песочницей, вроде синтетической доски из обкатки
сценария (DK-069). Баннер из такого корня ложный, и `deliver()` тогда не
зовётся.

В прогретом чекауте раннер стоит в обычном дереве. Песочница не срабатывает.
В свежем дереве прогона merge (`freshTestTree`, DK-641/643) весь чекаут лежит
под TMPDIR. Унаследованный `os.getcwd()` подпроцесса тоже оказывается под
TMPDIR. `sandbox_reason` читает это песочницей, `deliver()` не вызывается, `sent()`
у теста пустеет.

Так падают ровно три теста `TestArgumentMode`, что проверяют `sent()` без
явного `cwd`. Тесты с явным `cwd` (`self.tree(...)`, `self.tmp`) не задеты.
Не задеты и тесты, что проверяют только журнал. Пропуск в него пишется
строкой, и поля там остаются точными. Пропуск не теряется. Он виден и на
выходе, и в `~/.devkit/notify.log`. Это штатное поведение DK-069,
продукционный код тут без дефекта.

Правка живёт в `hooks/notify_test.py`. `HookCase.cli()` теперь задаёт
подпроцессу собственный `cwd`: `CLI_CWD`, каталог интерпретатора `python3`,
заведомо вне `TMP_ROOTS`. Раннер больше не отдаёт подпроцессу свою рабочую
директорию. Регрессионный тест
`test_reaches_the_backend_even_when_the_suite_itself_runs_from_tmp` явно
переходит в `self.tmp` перед вызовом `cli()`. Так он воспроизводит случай,
когда сам раннер стоит под TMPDIR. Тест отмечен маркерами
`regcheck:test-begin/end`. Правка и тест живут в одном файле.

Прогон `regcheck -C hooks --inline hooks/notify_test.py -- python3 -m
unittest
notify_test.TestArgumentMode.test_reaches_the_backend_even_when_the_suite_itself_runs_from_tmp`
подтверждает разбор. Тест краснеет на HEAD без правки и проходит на текущем
коде.

### Второй компонент: watch_test.QuotaTick (DK-633)

Тот же класс дефекта нашёлся в `tools/devkitctl` следующим чистым прогоном.
`watch.run()` внутри тика зовёт `quota_snap(call)` без параметра `agentctl`.
`quota_snap` находит бинарь сама. Она ищет его живым PATH и каталогами
релиза (`devkit_bin`). Тесты `QuotaTick` подставляли только `call` (`Fake`).
Бинарь `agentctl` они брали из живого PATH сессии, обычно `~/go/bin`.

В окружении прогона merge каталоги под домом из PATH срезаны. `devkit_bin`
ничего не находит. `quota_snap` сразу возвращает строку «бинаря agentctl
нет...», `Fake` не зовётся. Красных тестов пять. Четыре из них ждут строки
вида «снято 1», «кодом 1», «оставлено 1».
`test_fresh_snapshots_stay_out_of_the_journal` падает наоборот, он находит в
журнале лишнюю строку про отсутствие бинаря.

Правка меняет `watch.run()`. Функция принимает параметр `agentctl` и
пробрасывает его в `quota_snap()`, приёмом, каким уже пробрасываются
`taskctl` и `shipctl`. Тесты подставляют фейковый путь `AGENTCTL =
"/bin/подставной-agentctl"`. `Fake` его не исполняет, только записывает
`argv`, реальный бинарь по этому пути не нужен.

Добавлен тест `test_missing_agentctl_reaches_the_journal_through_the_tick`.
Это штатный случай отсутствия бинаря через тик целиком. `call` не зовётся,
причина доезжает и до отчёта, и до журнала. Прогон `regcheck -C
tools/devkitctl --base main -- python3 -m unittest watch_test.QuotaTick`
подтверждает разбор. Тест краснеет на main и проходит на текущем коде.

### Третий компонент: tools/dashboard, два случая

Третий чистый прогон уронил `go:dashboard`. Причины у двух случаев разные,
хоть класс общий. Тесты полагались на условия, которых прогон слияния не
даёт.

**Смок-цепочка** (`TestSmokePassesTheChain`,
`TestSmokeLeavesNothingBehind`). Шаг 15 ждёт уведомление о стопе в ленте.
`smokeBase()` уже избегает песочницы `sandbox_reason` (DK-069, DK-196).
Комментарий у функции называет это прямо. Рабочий каталог прогона лежит в
`os.UserCacheDir()` (кеш пользователя), не в системном temp.
`os.UserCacheDir()` строит путь от `$HOME` процесса. Прогон слияния
подменяет `HOME` на временный каталог (DK-641/643), и тот сам лежит под
TMPDIR. Обход песочницы уезжает туда же, куда должен был не пускать.
Уведомление молчит той же строкой журнала, что видел координатор: «на этой
машине уведомитель ещё ни разу не срабатывал».

Правка меняет источник дома у `smokeBase()`. Новая функция `realHomeDir()`
берёт его не из окружения процесса, а у системной базы учётных записей,
поверх `os/user.Current()`. Она называет настоящий дом машины даже при
подменённой переменной `HOME`. Проверено прямо в этом дереве.
`user.Current().HomeDir` вернул `/Users/rider` при `HOME=/tmp/fakehome-test`.
Кеш от настоящего дома не лежит под TMPDIR ни при какой подмене `HOME`
окружения. `HOME` процесса подменяется лишь на время самого вызова
`os.UserCacheDir()` и сразу восстанавливается. Правка не читает живого
состояния машины. Рабочий каталог смок-прогона всё равно свой, временный,
`os.MkdirTemp` под этим кешем.

**Тесты клиент-логина** (`TestClientLoginHashAfterLinkNotGlued`,
`TestClientLoginLinkRefusalSaysFrame`). Деадлайн ожидания ссылки у обоих
задан в 300ms (`fastLoginWait(t, 300*time.Millisecond)`). У всех соседних
тестов входа в этом же файле тот же деадлайн стоит в 2 секунды. За 300ms
цепочка фикстуры tmux обязана дойти до стадии со ссылкой:
`capture-pane`, несколько `send-keys`, опрос каждые 3ms. Каждый шаг это
отдельный подпроцесс. Под живой нагрузкой прогона слияния (несколько
компонентов разом) заминка в один подпроцесс уже съедает весь бюджет.
Деадлайн истекает раньше, чем цепочка доходит до стадии `url`. Разбор
ссылки называет общую причину «клиент не напечатал ссылку авторизации»
вместо разобранной. Причина подтверждена и без общей нагрузки машины.
Добавленный тест `TestClientLoginLinkRefusalSurvivesASlowTmuxCall` через
хелпер `slowDownTmuxOnce` тормозит ровно один `send-keys` фикстуры на
500ms. Это заминка одного внешнего процесса, не постоянно медленная
машина. При деадлайне 300ms тест красный. При 2 секундах (значение
остальных тестов входа) зелёный. Оба значения проверены вручную в этом
дереве.

Правка поднимает деадлайн у обоих тестов до 2 секунд, вровень с остальными
тестами входа этого файла. `regcheck` тут не подходит буквально. Правка вся
в тестовом файле, `clientlogin.go` не тронут. Стандартный перенос текущих
тестов на прод-код базы ничего не различил бы. `--inline` тоже не подходит.
Он умеет дописывать в конец базового файла новый регион, не переставлять
константу внутри уже существующего теста (то же ограничение уже отмечено в
ревью DK-649). Красную и зелёную стороны показал ручной прогон
`TestClientLoginLinkRefusalSurvivesASlowTmuxCall` с обоими значениями
деадлайна. Он и остаётся регрессионным тестом.

## Чего хотим

Тесты hooks строят своё окружение сами и зелёные в чистом прогоне merge.
Причина молчания sent() названа в файле задачи и закрыта правкой тестов или
notify.py (если тот молча глотает отсутствие бэкенда там, где должен звать
подставной). Краснота регрессии подтверждена прогоном regcheck.

Той же веткой дорезан второй случай того же класса. Пять тестов
`watch_test.QuotaTick` в `tools/devkitctl` полагались на живой `agentctl` из
PATH сессии. Они падали в том же чистом прогоне (раздел «Второй компонент»
выше).

Третьей находкой дорезан и `tools/dashboard`. Смок-цепочка теряла обход
песочницы уведомителя из-за подменённого `HOME`. Два теста клиент-логина
несли деадлайн, слишком тесный для живой нагрузки прогона слияния (раздел
«Третий компонент» выше).

## DoD

1. Компонент hooks зелёный в окружении прогона merge (временный HOME,
   урезанный PATH), три прогона подряд.
2. Причина записана в файле задачи, у фикса регрессионный тест, краснота
   подтверждена regcheck.
3. shipctl merge очередной ветки не отбивается компонентом hooks.
4. Компонент tools/devkitctl (в частности `watch_test.QuotaTick`) зелёный в
   том же окружении, у фикса регрессионный тест, краснота подтверждена
   regcheck.
5. Пакет tools/dashboard зелёный в свежем дереве и том же окружении, три
   прогона подряд. У фикса клиент-логина регрессионный тест. Краснота
   подтверждена ручным прогоном. Regcheck тут базу и текущий код не
   различает, причина в разделе «Третий компонент».

## Ранг

- Серьёзность 75: красный компонент в прогоне слияния, слить нельзя ни одну
  ветку.
- Ценность 0: чистый ремонт.
- Неопределённость 1: тесты и трейсы на руках, причина ищется.
- Баг +5: дефект тестов, вскрытый сменой окружения прогона.
- Рычаг 4: разблокирует слияния всем.

## Ход работы

- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 18%, снимок 14м назад, сдвига нет), 2026-08-31 23:09-23:09.
- Вычитка: 1 файл, 8 правок, 0 пометок, 2026-08-31.
- Вычитка: 1 файл, 2 правки, 0 пометок, 2026-09-01.
- Вычитка: 1 файл, 10 правок, 1 пометка, 2026-09-01.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 18%, снимок 43м назад, сдвига нет), 2026-08-31 23:38-01:38.
- Снаружи: проверка после выката, 2026-09-01 01:38-02:24.
- Проверка: сценарий прогнал fable, 2026-09-01 02:24-02:24.

## Сценарий проверки

Агентский. У `parallel.py` нет флага `--only`, компонент hooks гоняется
напрямую его же командой из `components()`.

```sh
cd hooks && python3 -m unittest discover -p '*_test.py'
cd tools/devkitctl && python3 suite.py
cd tools/dashboard && GOWORK=off go test . -count=1
```

Ожидание: все три компонента зелёные. Дальше слияние очередной ветки проходит
их без отбоя, диспетчер закрывает это фактом merge DK-643.

## Выкат

- 2026-09-01 слито: 5849ca5b, 6337e581, 7877598f, cfe13b27
- smoke прогнан, 2026-09-01

## Проверка

Сценарий прогнал диспетчер цели DK-161 (fable), исполнитель разработки sonnet,
2026-09-01 02:30.

```text
$ cd hooks && python3 -m unittest discover -p '*_test.py'
OK (587 tests)

$ cd tools/devkitctl && python3 suite.py
Ran 675 tests in 2m27s across 126 classes
OK
```

Шаг про слияние закрыт фактом: merge DK-643 прошёл полный набор в свежем
дереве на d37bc425352e, компоненты hooks и devkitctl зелёные.
