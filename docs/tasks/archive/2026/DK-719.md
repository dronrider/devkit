# DK-719: Открытая форма черновика после груминга не становится формой задачи

## Что происходит

Сообщение пользователя 2026-09-02. В панели открыта форма черновика, и по
нему в этот момент идёт груминг. Груминг заводит строку доски, файл
переезжает из `docs/tasks/drafts/` в `docs/tasks/`. Ожидалось, что экран
сам перейдёт на форму заведённой задачи. Вышло иначе. Экран остаётся формой
черновика, поля пустеют, а в подписи стоит отказ «черновика DK-NNN в devkit
нет: файла не видно, грумминг мог уже завести по нему задачу». До формы
задачи человек добирается руками, через доску или правкой адреса.

Механика в асимметрии двух ручек. Ручка задачи знает про черновик. Когда
строки на доске нет, а файл в `drafts/` есть, сервер кладёт в отказ поле
`draft`, и экран задачи по нему уходит на форму черновика. Ручка черновика
(`handleDraft` в `tools/dashboard/drafts.go`) читает только файл в `drafts/`
и при его отсутствии отдаёт голый 404 с текстом. Доску она не спрашивает,
подсказки для перехода не даёт. Экран черновика (`renderDraft` в
`static/app.js`) отказ не разбирает и показывает текст как есть. Опрос
груминга держится, пока жива tmux-сессия груминга, и гаснет вместе с ней.
Проверка «файла черновика нет, а строка на доске есть» у сервера уже
написана. Ею закрывает разговор груминга `groomDone`, но ручке черновика
она не досталась.

## Чего хотим

Форма черновика, чей ID за время просмотра стал строкой доски, сама
переходит на форму этой задачи, как экран задачи уходит на черновик в
обратную сторону. Ручка черновика на пропавший файл сверяется с доской и
в отказе называет задачу. Экран черновика по этому слову меняет адрес и
перерисовывается формой задачи. Отказ с текстом остаётся только у ID,
которого нет ни в черновиках, ни на доске.

## DoD

Тест ручки. Черновика в `drafts/` нет, строка с тем же ID стоит на доске,
ответ несёт поле с ID задачи, симметрично `TestTaskOfDraftIDNamesDraft`.
Стенд экрана. Открытая форма черновика при таком ответе меняет адрес на
адрес задачи и рисует форму задачи, подписи с отказом нет. Тесты красные
на коде до правки и зелёные после, краснота проверена regcheck. Поведение
перехода вписано в `tools/dashboard/README.md` рядом с обратным переходом.

## Ранг

`25+4+1+5+2 = 37`, P2.

- Серьёзность 25. Экран показывает неверный отказ и теряет открытую запись.
  Обход дешёвый, задачу открывают с доски руками.
- Ценность 4. Груминг из формы черновика идёт каждый день, в накопителе
  больше сотни записей, и каждое оформление при открытой форме
  кончается этим отказом.
- Неопределённость 1. Причина названа, обратный переход уже написан и
  служит образцом.
- Поправка на баг 5. Сломанный переход, а не новое удобство.
- Рычаг 2. Своя польза по одной теме. Чинится стык экранов дашборда.

## Ход работы

- Сообщение пользователя 2026-09-02, строка заведена сразу, без черновика.
  Разбор механики вёл субагент, причина подтверждена чтением кода.
  Ручка черновика не сверяется с доской, экран черновика отказ не
  разбирает.
- Вычитка: 1 файл, 10 правок, 1 пометка, 2026-09-02.
- Разработка: субагент opus/medium по вердикту pick (маппинг sonnet, корректор: профицит week_all, week_max; квота: week_all 28% профицит, week_max 44% профицит, снимок 16м назад), 2026-09-17 00:27-05:18.
- Правка: ручка черновика на пропавший файл сверяется с доской (`rowHere` в
  `tools/dashboard/drafts.go`) и несёт в отказе поле `task`, экран по нему
  подменяет адрес и рисует форму задачи (`goTaskInstead` в `static/app.js`).
  Тем же приёмом теперь пользуется `groomDone`, свой разбор доски у него снят.
- Тесты: `TestDraftOfTaskIDNamesTask` на ручку и стенд экрана
  `testdata/poc_drafttask.mjs` под `TestStaticDraftGoesToTask`. Прогон
  `regcheck --base ece80282`: краснеют на коде до правки, проходят после.
  Пакет дашборда прогнан целиком, 462 секунды, зелёный.
- Препятствие devkit: node, на машине его нет ни в PATH, ни в brew, и стенды
  экрана дашборда молча пропускаются (`t.Skip`). Прогнала стендом через node
  из поставки Cursor, `/Applications/Cursor.app/Contents/Resources/app/resources/helpers/node`.
- Находка мимо предмета задачи: стенд `testdata/poc_draftlink.mjs` не
  вызывается ни одним go-тестом, обратный переход проверяется только тестом
  ручки.
- Вычитка: 2 файла, 8 правок, 0 пометок, 2026-09-17.
- Ревью: субагент sonnet/high по вердикту pick (маппинг sonnet, корректор: профицит week_all, week_max; квота: week_all 36% профицит, week_max 50% профицит, снимок 13м назад), 2026-09-17 05:41-14:26.
- Снаружи: проверка после выката, 2026-09-17 14:26-14:26.
- Проверка: сценарий прогнал fable, 2026-09-17 14:26-14:26.

## Ревью

Уровень 2 до 92bd50b: неопределённость 1, дифф трогает общий путь груминга (handleDraft, groomDone) и общий static/app.js, который параллельно правит dk-970

- [возврат: правила, 2026-09-17] блокирует: README молчит про несохранённую правку. Код в static/app.js (renderDraft, переход через goTaskInstead) сам сбрасывает taskDraft.dirty и taskDraft.edit. Обычный сторож правки строкой ниже так не делает. Человек в этот момент теряет введённый текст без предупреждения. В README рядом с переходом об этом ни слова. Нужен абзац: правка в открытой форме черновика при этом переходе теряется, файла для неё уже нет.: исправлено, В README рядом с переходом дописан абзац: набранный в поле текст пропадает, сторож dirty у этого перехода снят, писать текст некуда, файл записи унесён разбором и PUT .../drafts/{id} по нему отвечает 404
- Вердикт: без замечаний. Второй круг: дифф 92bd50b..d8197e81 только доковый, тронуты docs/tasks/DK-719.md и tools/dashboard/README.md, код app.js не менялся. Абзац README сверен с renderDraft, goTaskInstead и ответом PUT .../drafts/{id}, описание совпадает с кодом.

## Сценарий проверки

Стенд экрана поднимает статику в node. Без node в PATH шаг пропускается, и
проверка тогда сделана только наполовину.

1. Тест ручки и стенд экрана краснеют на коде до правки и проходят на нём же
   после. Вывод кончается словами «регрессия закрыта».

```sh
PATH=/usr/local/go/bin:$PATH regcheck -C tools/dashboard --base ece80282 --tests tools/dashboard/drafts_test.go,tools/dashboard/testdata/poc_drafttask.mjs -- go test -run 'TestDraftOfTaskIDNamesTask|TestStaticDraftGoesToTask' .
```

2. Оба теста зелёные в рабочем дереве, стенд печатает `poc_drafttask: ok`.

```sh
cd tools/dashboard && PATH=/usr/local/go/bin:$PATH go test -v -run 'TestDraftOfTaskIDNamesTask|TestStaticDraftGoesToTask' .
```

## Выкат

- 2026-09-17 слито: de9b8f9d, 3a8eb61c, 953ba486, ddffc2ae, 5f0c572c

## Проверка

- Обкатка: 2026-09-17 13:43, свежее дерево 777343098066, сценарий 35eb55ab, временный HOME, утилит дерева 11, шагов 2, все зелёные.
- Исключение: обкатка (отметка зелёная стоит на коммите 777343098066, после него в main приехали только файлы задач той же пачки, кода не было; ворота сверки с HEAD не рассчитаны на поезд, черновик DK-1039)

Шаг 1 (зелёный):

```console
$ PATH=/usr/local/go/bin:$PATH regcheck -C tools/dashboard --base ece80282 --tests tools/dashboard/drafts_test.go,tools/dashboard/testdata/poc_drafttask.mjs -- go test -run 'TestDraftOfTaskIDNamesTask|TestStaticDraftGoesToTask' .
тест краснеет на ece80282 и проходит на текущем коде, регрессия закрыта (тесты: tools/dashboard/drafts_test.go, tools/dashboard/testdata/poc_drafttask.mjs)
```

Шаг 2 (зелёный):

```console
$ cd tools/dashboard && PATH=/usr/local/go/bin:$PATH go test -v -run 'TestDraftOfTaskIDNamesTask|TestStaticDraftGoesToTask' .
=== RUN   TestDraftOfTaskIDNamesTask
--- PASS: TestDraftOfTaskIDNamesTask (1.40s)
=== RUN   TestStaticDraftGoesToTask
    drafts_test.go:1450: poc_drafttask: ok
--- PASS: TestStaticDraftGoesToTask (0.08s)
PASS
ok  	github.com/dronrider/devkit/tools/dashboard	2.153s
```
