# DK-739: Автоматический прогон сценария встаёт на разрешениях и паркует задачу

Цель: [tasks/DK-713.md](tasks/DK-713.md)

## Что происходит

После слияния shipctl зовёт dashboard check, тот поднимает headless-проход claude -p в tmux, и прогон упирается в «This command requires approval». Подтвердить некому: окна нет, зрителя нет. Сессия паркует задачу вопросом к человеку и замолкает. На DK-727 так встала цель DK-713 на четыре с половиной часа.

Корней два, и по отдельности ни один не лечится.

Первый в данных прав. MACHINE_ALLOW в tools/devkitctl/perms.py не знает ни node, ни dashboard, а сценарии дашборда сплошь гоняют стенды node testdata/*.mjs и сквозной прогон dashboard smoke. Список правится кодом и разъезжается по машинам через devkitctl doctor --fix, поэтому дыра в нём общая, а не местная.

Второй в подъёме. clientCommand в tools/dashboard/runs.go для своей подписки отдаёт голое «claude --model <ярус>», без --permission-mode и без файла настроек: расчёт на то, что права уже разложены. Проверки этого расчёта перед подъёмом нет, в отличие от витка цели, где goal-run.py гоняет perms.py и громко отказывается стартовать. Показательно, что сама task-run.py в своём умолчании ставит --permission-mode auto, но дашборд это умолчание всегда перебивает своей строкой клиента.

Чинить надо оба конца: дополнить список прав и не поднимать headless-проход вслепую, а либо ставить ему режим разрешений, либо проверять права заранее и говорить об этом отчётом.

## Чего хотим

Перечень прав машинного контура знает команды, на которых держатся сценарии
проверки этого репозитория: `dashboard check`, `dashboard smoke`, `devkitctl` и `node`. Перечень закрыт
сторожем. Команда, выпавшая из него, обнаруживается тестом раньше, чем
остановит прогон на живой машине.

Подъём прогона после выката перестаёт быть слепым. Клиент своей подписки
поднимается в том же режиме разрешений, что и клиент чужой. Перед подъёмом
`dashboard check` спрашивает права тем же перечнем, каким их спрашивает виток
цели. Прав нет. Сессия не поднимается вовсе, а отчёт слияния называет нехватку
словами перечня вместе с командой починки.

## Развилки

Второй конец лечится двумя путями, и выбраны оба, каждый на своём месте.

Режим разрешений своей подписке (`--permission-mode auto` в `clientCommand`)
это то, чем закрыта сама поломка. Голая строка клиента была не решением, а
недосмотром. Чужой подписке режим ставится с DK-328, оболочка проходов ставит
его себе умолчанием, и перебивала это умолчание только строка дашборда. Риск
дать автономной сессии больше прав, чем человек ожидает, тут невелик. Режим
`auto` это не `bypassPermissions`, а классификатор поверх разложенных правил.
Deny-правила на секреты остаются в силе, и этим же режимом уже поднимаются
виток цели и клиент второй подписки.

Предполётная проверка прав это то, чем закрыта молчаливая парковка. Одного
режима мало. Команда, которой нет ни в перечне, ни в списке безопасных у
классификатора, всё равно упрётся в вопрос. Список прав ещё не раз придётся
править кодом `perms.py`. Проверка стоит в `checkRun`, где отказ записывается
словами в отчёт зовущего.

Отвергнуто: только предполётная проверка, без режима. Она сказала бы про
разложенные права и промолчала бы про всё остальное. С умолчанием клиента
каждый вызов вне перечня остаётся тихим отказом, а строка дашборда продолжала
бы перебивать умолчание оболочки. Отвергнут и `--dangerously-skip-permissions`:
он и правда снимает вопросы все разом, но вместе с ними снимает и запрет на
чтение секретов.

Проверка поставлена не кнопке экрана, а подъёму после выката. Кнопку жмёт
человек, панель сессии у него перед глазами, и запрос разрешения он видит и
одобряет сам.

## DoD

Задача с агентским сценарием, слитая при autonomous = true, доезжает до Check и закрытия без человека. Прогон гоняет go test, node testdata/*.mjs и dashboard smoke, не спрашивая подтверждения. Проверяется прогоном на живой машине после выката. Случай нехватки прав перестаёт быть молчаливой парковкой. Подъём либо не начинается с внятным отказом в отчёте, либо проходит.

## Ранг

`50+9+2+5+4 = 70`, P1.

## Ход работы

- Из черновика DK-739 «Автоматический прогон сценария встаёт на разрешениях и паркует задачу»: записан 2026-09-03, оформлен 2026-09-03.
- Краснота regcheck: `regcheck -C tools/dashboard --tests tools/dashboard/checkrun_test.go,tools/dashboard/runs_test.go -- go test ./... -run 'TestCheckRunRefusesWithoutPerms|TestCheckRunAsksPermsUnderUserHome|TestClientCommandOwnHarnessGetsPermissionMode'` и `regcheck -C tools/devkitctl --tests tools/devkitctl/perms_test.py -- python3 -m unittest perms_test.MachinePermsTest.test_scenario_commands_are_granted`, оба сказали «тест краснеет на HEAD и проходит на текущем коде».
- Вычитка: 3 файла, 22 правки, 0 пометок, 2026-09-03.
- Разработка: субагент opus/medium по вердикту pick (квота: week_all 20%, week_max 7% (панель разбивку не дала, цифра из кеша клиента возрастом 15ч 26м назад), снимок 23м назад, сдвига нет), 2026-09-03 09:04-09:33.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 21%, week_max 7% (панель разбивку не дала, цифра из кеша клиента возрастом 16ч 11м назад), снимок 7м назад, сдвига нет), 2026-09-03 09:33-12:17.

## Ревью

Уровень 3 до 61221b7: правка трогает права автономных сессий, MACHINE_ALLOW это критичный путь по конфигу, и просьба диспетчера была смотреть за лишними правами

- блокирует: Bash(dashboard:*) в MACHINE_ALLOW (tools/devkitctl/perms.py) шире, чем нужно сценарию. У бинаря dashboard четыре подкоманды (tools/dashboard/main.go): serve, secret, check, smoke. Сценариям DoD нужны только check и smoke. Подкоманда dashboard secret [--rotate] печатает живой токен входа (tools/dashboard/auth.go, main.go:81-90). Это тот же класс секрета, ради которого исполнитель нарочно не взял secretctl. Тут его прикрывает только ширина wildcard. SECRET_DENY режет лишь инструмент Read. hooks/check-read-secret.py грепает буквальные пути файлов в команде, а dashboard secret ни один такой путь не упоминает. Автономная сессия без подтверждения печатает себе токен в контекст. Она же может выполнить dashboard secret --rotate и разлогинить разом все сессии панели, включая человека. Это тихий self-DoS без единого запроса разрешения. В архиве (docs/tasks/archive) dashboard secret и dashboard serve встречаются только в шагах человека руками, ни разу в агентском автономном шаге. Сузить можно без потери покрытия DoD. Предлагаю заменить строку на Bash(dashboard check:*) и Bash(dashboard smoke:*), тем же приёмом префикса, что уже стоит в перечне (Bash(env -u DEVKIT_HOME:*), docs/tasks/DK-498.md). Заодно поправить perms_test.py:test_scenario_commands_are_granted и оба README: сейчас они проверяют и называют ровно Bash(dashboard:*): исправлено, в перечне Bash(dashboard check:*) и Bash(dashboard smoke:*), сторож perms_test.py ловит маску, дотянувшуюся до dashboard secret, оба README переписаны; Bash(devkitctl:*) оставлено, подкоманды, печатающей секрет, у него нет

## Сценарий проверки

1. Перечень прав знает команды сценариев этого репозитория.

```sh
python3 -c "import sys; sys.path.insert(0, 'tools/devkitctl'); import perms; print([r for r in perms.MACHINE_ALLOW if 'node' in r or 'dashboard' in r or 'devkitctl' in r])"
```

Ждём четыре правила списком: `['Bash(dashboard check:*)', 'Bash(dashboard smoke:*)',
'Bash(devkitctl:*)', 'Bash(node:*)']`. Дашборд стоит подкомандами, целого бинаря в
перечне быть не должно.

2. Право на `node` работает на живой машине. Стенд дашборда гоняется без вопроса
разрешения.

```sh
cd tools/dashboard && node testdata/board_tiers.mjs static/app.js
```

Ждём строку «доска: очередь без ждущих, два яруса Blocked, чип держащей задачи
ведёт на неё» и код возврата 0.

3. Права машинного контура разложены на машине, где идёт прогон.

```sh
python3 tools/devkitctl/perms.py; echo "код $?"
```

Ждём «права машинного контура на месте» и «код 0».

- Исключение: обкатка (перечень читает настройки харнеса под домом машины, а
  каждый шаг обкатки идёт под временным домом, где их нет вовсе; там перечень
  отвечает нехваткой прав, и ожидание шага сверяется на машине прогона).

4. Подъём прогона говорит словами. Прогоняющий сам сидит в поднятой сессии,
так что повторный подъём не нужен, и команда обязана сказать почему.

```sh
dashboard check DK-739
```

Ждём одну строку отчёта, начинающуюся с «DK-739: подъём не нужен», дальше
причина: работа уже идёт в tmux-сессии `task-DK-739` либо отметка smoke стоит.

- Исключение: обкатка (обвязка выката лежит вне git, и в свежем дереве обкатки
  команда отвечает «выкат за пользователем»; ожидание шага сверяется на машине
  прогона).

## Выкат

- 2026-09-03 слито: 0237af88, e8218efd, 67082713, 4af81ed4, 2ee6fabc, c023effa, 40e6b6fc, 58f83572, b7dab7d7, 47c419e5, b19f88eb
- smoke прогнан, 2026-09-03

## Проверка


- Обкатка: 2026-09-03 09:54, свежее дерево 898faa5f3a8f, сценарий 10e7503a, временный HOME, утилит дерева 10, шагов 4, все зелёные.
- Прогон: 2026-09-03 (проверка), выкаченный код, вывод ниже.

Шаг 1 (зелёный):

```console
$ python3 -c "import sys; sys.path.insert(0, 'tools/devkitctl'); import perms; print([r for r in perms.MACHINE_ALLOW if 'node' in r or 'dashboard' in r or 'devkitctl' in r])"
['Bash(dashboard check:*)', 'Bash(dashboard smoke:*)', 'Bash(devkitctl:*)', 'Bash(node:*)']
```

Шаг 2 (зелёный):

```console
$ cd tools/dashboard && node testdata/board_tiers.mjs static/app.js
доска: очередь без ждущих, два яруса Blocked, чип держащей задачи ведёт на неё
```

Шаг 3 (зелёный):

```console
$ python3 tools/devkitctl/perms.py; echo "код $?"
в /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/taskctl-rehearse-4082335669/home/.claude/settings.json не хватает прав машинного контура, 39 из 39 (Bash(taskctl:*), Bash(shipctl:*), Bash(agentctl:*), Bash(regcheck:*), Bash(obeycheck:*), Bash(dashboard check:*), Bash(dashboard smoke:*), Bash(devkitctl:*) и ещё 31): одобрять запросы харнеса в сессии без человека некому, и виток цели молча не сделает ничего; разложить: python3 /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/taskctl-rehearse-4082335669/tree/tools/devkitctl/devkitctl.py doctor --fix
в /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/taskctl-rehearse-4082335669/home/.claude/settings.json не хватает 4 deny-правила на чтение секрета в deny (Read(~/.claude/access.local.md), Read(~/.ssh/**), Read(~/.devkit/secrets/**), Read(./local-docs/**)): файл доступов, приватные ключи, хранилище secretctl и local-docs читаются инструментом Read, и значение уезжает в контекст модели; рубится deny, значение берётся через secretctl (цель DK-207); разложить: python3 /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/taskctl-rehearse-4082335669/tree/tools/devkitctl/devkitctl.py doctor --fix
код 1
```

Шаг 4 (зелёный):

```console
$ dashboard check DK-739
DK-739: подъём не нужен, выкат за пользователем (autonomous = false в .devkit/deploy.local): проверяющего поднимает человек
```

Шаг 1 (зелёный):

```console
$ python3 -c "import sys; sys.path.insert(0, 'tools/devkitctl'); import perms; print([r for r in perms.MACHINE_ALLOW if 'node' in r or 'dashboard' in r or 'devkitctl' in r])"
['Bash(dashboard check:*)', 'Bash(dashboard smoke:*)', 'Bash(devkitctl:*)', 'Bash(node:*)']
```

Шаг 2 (зелёный):

```console
$ cd tools/dashboard && node testdata/board_tiers.mjs static/app.js
доска: очередь без ждущих, два яруса Blocked, чип держащей задачи ведёт на неё
код 0
```

Шаг 3 (зелёный):

```console
$ python3 tools/devkitctl/perms.py; echo "код $?"
права машинного контура на месте
код 0
```

Шаг 4 (зелёный):

```console
$ dashboard check DK-739
DK-739: подъём не нужен, работа уже идёт в tmux-сессии task-DK-739
код 0
```
