# DK-804: Ворота старта отказывают при открытой блокирующей развилке

Цель: [tasks/DK-565.md](../../tasks/DK-565.md)

## Что происходит

Раздел «Развилки» у файла задачи есть, и стоит он в форме
(`internal/taskform/taskform.go:26`). Пишется он прозой. Состояния у развилки
нет, автора решения нет, признака «держит старт» тоже нет.

Ветка заводится в обход этого текста. `cmdStart` смотрит на PATH taskctl, на
основной чекаут, на замок, на секцию доски и на корп-привязку
(`tools/shipctl/worktree.go:291`). Файл задачи он не открывает вовсе.
Исполнитель садится за код с нерешённой развилкой и решает её сам, а
расхождение всплывает на ревью.

Формат машинного перечня развилок даёт LLD DK-552, эта задача садится на его
решение.

## Чего хотим

`shipctl start` читает перечень развилок и отказывает, когда открыта
блокирующая. Отказ называет саму развилку прямо в строке. Закрытая
развилка ворот не держит, и ветка заводится обычным порядком. Дашборд
показывает открытые развилки рядом со статусом задачи.

## DoD

- `shipctl start DK-X` на задаче с открытой блокирующей развилкой печатает
  отказ с её именем.
- После закрытия развилки та же команда заводит ветку.
- Развилка, оставленная исполнителю, старта не держит.
- `taskctl close DK-X` при открытой блокирующей развилке отказывает тем же
  чтением перечня, что и старт, по решению 2 DK-552.
- Дашборд показывает открытые развилки задачи рядом со статусом.
- Тесты по стандарту, разбор в README shipctl.

## Ранг

`50+8+1+0+3 = 62`, P1.

- Серьёзность 50: старт по нерешённой развилке даёт переделку, обход это
  память исполнителя.
- Ценность 8: ворота старта это второй из четырёх механизмов цели DK-565.
- Неопределённость 1: формат перечня приходит готовым из DK-552, осталось
  сделать.
- Поправка на баг 0.
- Рычаг 3: на тот же перечень садятся груминг, цикл цели и дашборд.

## Ход работы

- Вычитка: 0 правок, 0 пометок, 2026-09-05.
- Вычитка: 1 правка, 0 пометок, 2026-09-06.
- Разработка: субагент opus/medium по вердикту pick (маппинг sonnet, корректор: профицит week_all, week_max; квота: week_all 43% профицит, week_max 37% профицит (панель разбивку не дала, цифра из кеша клиента возрастом 4ч 34м назад), снимок 11м назад), 2026-09-06 13:57-13:57.
- Вычитка: 3 файла, 6 правок, 0 пометок, 2026-09-06 (четыре правки вычитки в чужом абзаце про merge отменены, они вне предмета задачи).
- Ревью: субагент sonnet/high по вердикту pick (маппинг sonnet, корректор: профицит week_all, week_max; квота: week_all 43% профицит, week_max 37% профицит (панель разбивку не дала, цифра из кеша клиента возрастом 4ч 34м назад), снимок 39м назад), 2026-09-06 14:25-17:04.
- Снаружи: проверка после выката, 2026-09-06 17:04-17:10.
- Проверка: сценарий прогнал sonnet, 2026-09-06 17:10-17:13.

## Ревью

Уровень 3 до e3bd057: критичный путь: тронуты tools/shipctl и tools/taskctl (critical_paths), правка меняет ворота старта и закрытия любой задачи доски

- Вердикт: без замечаний. прочитаны все 14 файлов диффа; отбор HoldingForks сверен по коду и тестам на сочетаниях полей (открыта, решена, оставлена исполнителю, повторное решено, пустой и отсутствующий файл); порядок вызова forkGate/closeForkGate в cmdStart и cmdClose прочитан построчно - обе ворота стоят раньше первой записи (board move, lock, worktree add); закрытие без раздела «Развилки» и старые прозаические файлы проверены через forkSection/forkHeadRe - молчат, не отказывают; текст ForkGateNote проверен на имя, вопрос, рекомендацию и обе команды снятия; дашборд - поле forks и чип, openForks возвращает nil без файла задачи; go test ./... (GOWORK=off) зелёный в internal/taskform, tools/shipctl, tools/taskctl, tools/dashboard; gofmt и check-symbols.py по тронутым файлам чисты

## Сценарий проверки

- Исключение: обкатка (поезд из трёх задач: отметка каждой устаревает от
  коммита с отметкой соседа, и разом ворота они не проходят. Сценарий прогнан
  зелёным на b13999267337, вывод ниже в «Проверке»; дефект ворот заведён
  черновиком DK-838)

Шаги гоняются из корня чекаута. Обкатка исполняет каждую строку блока своим
процессом, поэтому строки тут самодостаточны. Путь до корня подставляется в
каждую команду, а стенд третьего, четвёртого и пятого шагов собирается и
убирается внутри одной строки.

1. Общий отбор и слова отказа. Открытая человеческая развилка держит ворота,
   решённая и оставленная исполнителю не держат, а отказ несёт имя, вопрос,
   рекомендацию и обе команды снятия.

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/internal" test ./taskform/... -count=1 -run "TestHoldingForks|TestForkGateNote" -v
   ```

   Ожидание: строка `PASS`, оба теста в списке `--- PASS`.

2. Ворота в трёх утилитах. Отказ старта, отказ закрытия и поле открытых
   развилок в строке задачи на дашборде.

   ```sh
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/shipctl" test . -count=1 -run "TestStartHeldByFork|TestStartPassesExecutorFork"
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" test . -count=1 -run "TestCloseHeldByFork|TestCloseAnsweredForkPasses"
   GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/dashboard" test . -count=1 -run "TestTaskForksInRow"
   ```

   Ожидание: три строки `ok` с именами пакетов `shipctl`, `taskctl` и
   `dashboard`.

3. Живой отказ обоих ворот. Строка поднимает свой проект с доской из двух
   строк, заводит каждой человеческую развилку и зовёт обе команды.

   ```sh
   w="${TMPDIR:-/tmp}/dk804-stand-a"; rm -rf "$w"; mkdir -p "$w"; git -C "$w" init -q -b main; git -C "$w" config user.email t@t; git -C "$w" config user.name t; taskctl -C "$w" init --prefix XR --name проба; taskctl -C "$w" add --id XR-001 --title "Старт под развилкой" --rank 25+0+0+0+0 --accept user --barrier глаза; taskctl -C "$w" add --id XR-002 --title "Закрытие под развилкой" --rank 25+0+0+0+0 --accept user --barrier глаза; git -C "$w" add -A; git -C "$w" commit -qm стенд; taskctl -C "$w" decide XR-001 --ask доступ --hint "считать приватным" "токен положен в secretctl?"; taskctl -C "$w" decide XR-002 --ask публичность "релизы видны без токена?"; shipctl -C "$w" start XR-001; taskctl -C "$w" close XR-002; git -C "$w" status --short docs/TASKS.md; rm -rf "$w"; echo "стенд убран"
   ```

   Ожидание: `shipctl start` отказывает и называет «доступ» с вопросом про
   токен, `taskctl close` отказывает и называет «публичность», оба отказа несут
   команды `decide ... --by человек` и `decide ... --leave`. Доска стенда не
   тронута, `git status` по ней печатает пустую строку, последняя строка вывода
   это «стенд убран».

4. Ответ отпускает оба прохода. Тот же стенд, но развилки закрываются ответом
   до вызова команд.

   ```sh
   w="${TMPDIR:-/tmp}/dk804-stand-b"; rm -rf "$w" "$w"-xr-001; mkdir -p "$w"; git -C "$w" init -q -b main; git -C "$w" config user.email t@t; git -C "$w" config user.name t; taskctl -C "$w" init --prefix XR --name проба; taskctl -C "$w" add --id XR-001 --title "Старт под развилкой" --rank 25+0+0+0+0 --accept user --barrier глаза; taskctl -C "$w" add --id XR-002 --title "Закрытие под развилкой" --rank 25+0+0+0+0 --accept user --barrier глаза; git -C "$w" add -A; git -C "$w" commit -qm стенд; taskctl -C "$w" decide XR-001 --ask доступ "токен положен в secretctl?"; taskctl -C "$w" decide XR-002 --ask публичность "релизы видны без токена?"; taskctl -C "$w" decide XR-001 «доступ» --by человек "положил, имя xr-token"; taskctl -C "$w" decide XR-002 «публичность» --by человек "да, релизы публичные"; shipctl -C "$w" start XR-001; taskctl -C "$w" close XR-002; rm -rf "$w" "$w"-xr-001; echo "стенд убран"
   ```

   Ожидание: печать `decide` показывает у обеих развилок состояние «решена» со
   штампом автора и даты, `start` заводит ветку `xr-001` и переводит строку в
   In progress, `close` уносит XR-002 в архив. Предупреждение про разлив
   очереди у `close` законно, доска стенда после закрытия не закоммичена.

5. Развилка исполнителя и задача без перечня. Ни та, ни другая ворот не
   держат.

   ```sh
   w="${TMPDIR:-/tmp}/dk804-stand-c"; rm -rf "$w" "$w"-xr-003; mkdir -p "$w"; git -C "$w" init -q -b main; git -C "$w" config user.email t@t; git -C "$w" config user.name t; taskctl -C "$w" init --prefix XR --name проба; taskctl -C "$w" add --id XR-003 --title "Развилка исполнителя" --rank 25+0+0+0+0 --accept user --barrier глаза; taskctl -C "$w" add --id XR-004 --title "Задача без перечня" --rank 25+0+0+0+0 --accept user --barrier глаза; git -C "$w" add -A; git -C "$w" commit -qm стенд; taskctl -C "$w" decide XR-003 --ask поле --who исполнитель --hint "«решает»" "как звать поле?"; shipctl -C "$w" start XR-003; taskctl -C "$w" close XR-004; rm -rf "$w" "$w"-xr-003; echo "стенд убран"
   ```

   Ожидание: `decide` называет «поле» оставленной исполнителю, `start` заводит
   ветку `xr-003` при открытой развилке, `close` уносит XR-004 без перечня в
   архив, последняя строка вывода это «стенд убран».

6. Живой заход на файле этой задачи и дока. Развилка заводится, печатается
   состоянием, файл возвращается копией, снятой в той же строке. Копия тут
   надёжнее `git checkout`: он вернул бы файл к коммиту и унёс с ним
   незакоммиченное, в том числе запись самой обкатки.

   ```sh
   cp "$(git rev-parse --show-toplevel)/docs/tasks/DK-804.md" "${TMPDIR:-/tmp}/dk804-task.bak"; taskctl -C "$(git rev-parse --show-toplevel)" decide DK-804 --ask проба --hint "проба" "проба сценария?"; taskctl -C "$(git rev-parse --show-toplevel)" decide DK-804 --open; cp "${TMPDIR:-/tmp}/dk804-task.bak" "$(git rev-parse --show-toplevel)/docs/tasks/DK-804.md"; rm -f "${TMPDIR:-/tmp}/dk804-task.bak"; git -C "$(git rev-parse --show-toplevel)" diff --stat -- docs/tasks/DK-804.md; echo "файл возвращён"
   grep -n "Развилка держит старт" "$(git rev-parse --show-toplevel)/tools/shipctl/README.md"
   grep -n "Ворота старта и закрытия" "$(git rev-parse --show-toplevel)/tools/taskctl/README.md"
   ```

   Ожидание: `decide` заводит «пробу» и печатает её открытой, держащей старт,
   последняя строка первой команды это «файл возвращён», а `git diff --stat` по
   файлу печатает перед ней то же, что печатал до шага (на чистом дереве
   ничего). Оба `grep` печатают по строке с заголовком раздела README.

## Выкат

- 2026-09-06 слито: c6c8caa5a, 132bfa711, d08e63250, de3363db5, 52cc51622, 1b6bbce18, b4b912b02, 21717ba88, de05c72bc
- smoke прогнан, 2026-09-06

## Проверка

- Прогон: 2026-09-06 17:09, выкаченный код на коммите 57239944e, основной
  чекаут, все 6 шагов зелёные.

Шаг 1 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/internal" test ./taskform/... -count=1 -run "TestHoldingForks|TestForkGateNote" -v
=== RUN   TestHoldingForks
--- PASS: TestHoldingForks (0.00s)
=== RUN   TestForkGateNote
--- PASS: TestForkGateNote (0.00s)
PASS
ok  	github.com/dronrider/devkit/internal/taskform	0.447s
```

Шаг 2 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/shipctl" test . -count=1 -run "TestStartHeldByFork|TestStartPassesExecutorFork"
ok  	github.com/dronrider/devkit/tools/shipctl	2.302s
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" test . -count=1 -run "TestCloseHeldByFork|TestCloseAnsweredForkPasses"
ok  	github.com/dronrider/devkit/tools/taskctl	0.852s
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/dashboard" test . -count=1 -run "TestTaskForksInRow"
ok  	github.com/dronrider/devkit/tools/dashboard	1.900s
```

Шаг 3 (зелёный):

```console
$ w="${TMPDIR:-/tmp}/dk804-stand-a"; rm -rf "$w"; mkdir -p "$w"; git -C "$w" init -q -b main; git -C "$w" config user.email t@t; git -C "$w" config user.name t; taskctl -C "$w" init --prefix XR --name проба; taskctl -C "$w" add --id XR-001 --title "Старт под развилкой" --rank 25+0+0+0+0 --accept user --barrier глаза; taskctl -C "$w" add --id XR-002 --title "Закрытие под развилкой" --rank 25+0+0+0+0 --accept user --barrier глаза; git -C "$w" add -A; git -C "$w" commit -qm стенд; taskctl -C "$w" decide XR-001 --ask доступ --hint "считать приватным" "токен положен в secretctl?"; taskctl -C "$w" decide XR-002 --ask публичность "релизы видны без токена?"; shipctl -C "$w" start XR-001; taskctl -C "$w" close XR-002; git -C "$w" status --short docs/TASKS.md; rm -rf "$w"; echo "стенд убран"
доска создана в /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-a: docs/TASKS.md, docs/TASKS-archive.md, docs/tasks/
XR-001 заведена в backlog: P2, R=25
XR-002 заведена в backlog: P2, R=25
XR-001 (файл записи): развилка «доступ» заведена, решает человек, держит старт задачи
- «доступ» (открыта, держит старт): токен положен в secretctl?
  рекомендация: считать приватным
XR-002 (файл записи): развилка «публичность» заведена, решает человек, держит старт задачи
- «публичность» (открыта, держит старт): релизы видны без токена?
ошибка: XR-001: открытых развилок 1, решает человек, и до ответа старт задачи не идёт:
- «доступ»: токен положен в secretctl?
  рекомендация: считать приватным
ответ снимает развилку: taskctl decide XR-001 «доступ» --by человек "ответ и довод"
вопрос, на который отвечает исполнитель, передаётся ему: taskctl decide XR-001 «доступ» --leave
ошибка: XR-002: открытых развилок 1, решает человек, и до ответа закрытие задачи не идёт:
- «публичность»: релизы видны без токена?
ответ снимает развилку: taskctl decide XR-002 «публичность» --by человек "ответ и довод"
вопрос, на который отвечает исполнитель, передаётся ему: taskctl decide XR-002 «публичность» --leave
стенд убран
```

Шаг 4 (зелёный):

```console
$ w="${TMPDIR:-/tmp}/dk804-stand-b"; rm -rf "$w" "$w"-xr-001; mkdir -p "$w"; git -C "$w" init -q -b main; git -C "$w" config user.email t@t; git -C "$w" config user.name t; taskctl -C "$w" init --prefix XR --name проба; taskctl -C "$w" add --id XR-001 --title "Старт под развилкой" --rank 25+0+0+0+0 --accept user --barrier глаза; taskctl -C "$w" add --id XR-002 --title "Закрытие под развилкой" --rank 25+0+0+0+0 --accept user --barrier глаза; git -C "$w" add -A; git -C "$w" commit -qm стенд; taskctl -C "$w" decide XR-001 --ask доступ "токен положен в secretctl?"; taskctl -C "$w" decide XR-002 --ask публичность "релизы видны без токена?"; taskctl -C "$w" decide XR-001 «доступ» --by человек "положил, имя xr-token"; taskctl -C "$w" decide XR-002 «публичность» --by человек "да, релизы публичные"; shipctl -C "$w" start XR-001; taskctl -C "$w" close XR-002; rm -rf "$w" "$w"-xr-001; echo "стенд убран"
доска создана в /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-b: docs/TASKS.md, docs/TASKS-archive.md, docs/tasks/
XR-001 заведена в backlog: P2, R=25
XR-002 заведена в backlog: P2, R=25
XR-001 (файл записи): развилка «доступ» заведена, решает человек, держит старт задачи
- «доступ» (открыта, держит старт): токен положен в secretctl?
XR-002 (файл записи): развилка «публичность» заведена, решает человек, держит старт задачи
- «публичность» (открыта, держит старт): релизы видны без токена?
XR-001 (файл записи): развилка «доступ» решена
- «доступ» (решена): токен положен в secretctl?
  решено человеком 2026-09-06: положил, имя xr-token
XR-002 (файл записи): развилка «публичность» решена
- «публичность» (решена): релизы видны без токена?
  решено человеком 2026-09-06: да, релизы публичные
ветка xr-001 в worktree /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-b-xr-001
доска: XR-001: backlog -> in-progress, коммит b0dc35b
следующий шаг: код в дереве задачи (shipctl start XR-001), правка вместе с тестами, дальше ревью и слияние (shipctl merge XR-001); сдача это Check, а не «код написан». Слияние за пользователем: в .devkit/deploy.local стоит autonomous = false, агент останавливается на локальном коммите и ждёт команды
работать в /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-b-xr-001, по готовности: shipctl merge XR-001 (оттуда же или из основного чекаута)
XR-002 закрыта 2026-09-06, строка в архиве, файл задачи в tasks/archive/2026/XR-002.md
предупреждение: разлив упал: exit status 1 (ошибка: в рабочем дереве незакоммиченное, сначала закоммить:
M docs/TASKS-archive.md
 M docs/TASKS.md
 M docs/tasks/XR-001.md
RM docs/tasks/XR-002.md -> docs/tasks/archive/2026/XR-002.md
path: /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-b/.devkit/cmdout/20260906T170855-git-status/out)
следующий шаг: очередь выката свободна, следующая задача берётся с доски (taskctl list, дальше taskctl move <ID> in-progress)
стенд убран
```

Шаг 5 (зелёный):

```console
$ w="${TMPDIR:-/tmp}/dk804-stand-c"; rm -rf "$w" "$w"-xr-003; mkdir -p "$w"; git -C "$w" init -q -b main; git -C "$w" config user.email t@t; git -C "$w" config user.name t; taskctl -C "$w" init --prefix XR --name проба; taskctl -C "$w" add --id XR-003 --title "Развилка исполнителя" --rank 25+0+0+0+0 --accept user --barrier глаза; taskctl -C "$w" add --id XR-004 --title "Задача без перечня" --rank 25+0+0+0+0 --accept user --barrier глаза; git -C "$w" add -A; git -C "$w" commit -qm стенд; taskctl -C "$w" decide XR-003 --ask поле --who исполнитель --hint "«решает»" "как звать поле?"; shipctl -C "$w" start XR-003; taskctl -C "$w" close XR-004; rm -rf "$w" "$w"-xr-003; echo "стенд убран"
доска создана в /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-c: docs/TASKS.md, docs/TASKS-archive.md, docs/tasks/
XR-003 заведена в backlog: P2, R=25
XR-004 заведена в backlog: P2, R=25
XR-003 (файл записи): развилка «поле» заведена, решает исполнитель, старта не держит
- «поле» (оставлена исполнителю): как звать поле?
  рекомендация: «решает»
ветка xr-003 в worktree /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-c-xr-003
доска: XR-003: backlog -> in-progress, коммит 9cbe036
следующий шаг: код в дереве задачи (shipctl start XR-003), правка вместе с тестами, дальше ревью и слияние (shipctl merge XR-003); сдача это Check, а не «код написан». Слияние за пользователем: в .devkit/deploy.local стоит autonomous = false, агент останавливается на локальном коммите и ждёт команды
работать в /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-c-xr-003, по готовности: shipctl merge XR-003 (оттуда же или из основного чекаута)
XR-004 закрыта 2026-09-06, строка в архиве, файл задачи в tasks/archive/2026/XR-004.md
предупреждение: разлив упал: exit status 1 (ошибка: в рабочем дереве незакоммиченное, сначала закоммить:
M docs/TASKS-archive.md
 M docs/TASKS.md
 M docs/tasks/XR-003.md
R  docs/tasks/XR-004.md -> docs/tasks/archive/2026/XR-004.md
path: /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-c/.devkit/cmdout/20260906T170902-git-status/out)
следующий шаг: очередь выката свободна, следующая задача берётся с доски (taskctl list, дальше taskctl move <ID> in-progress)
стенд убран
```

Шаг 6 (зелёный):

```console
$ cp "$(git rev-parse --show-toplevel)/docs/tasks/DK-804.md" "${TMPDIR:-/tmp}/dk804-task.bak"; taskctl -C "$(git rev-parse --show-toplevel)" decide DK-804 --ask проба --hint "проба" "проба сценария?"; taskctl -C "$(git rev-parse --show-toplevel)" decide DK-804 --open; cp "${TMPDIR:-/tmp}/dk804-task.bak" "$(git rev-parse --show-toplevel)/docs/tasks/DK-804.md"; rm -f "${TMPDIR:-/tmp}/dk804-task.bak"; git -C "$(git rev-parse --show-toplevel)" diff --stat -- docs/tasks/DK-804.md; echo "файл возвращён"
DK-804 (файл записи): развилка «проба» заведена, решает человек, держит старт задачи
- «проба» (открыта, держит старт): проба сценария?
  рекомендация: проба
- «проба» (открыта, держит старт): проба сценария?
  рекомендация: проба
файл возвращён
$ grep -n "Развилка держит старт" "$(git rev-parse --show-toplevel)/tools/shipctl/README.md"
157:### Развилка держит старт
$ grep -n "Ворота старта и закрытия" "$(git rev-parse --show-toplevel)/tools/taskctl/README.md"
1240:### Ворота старта и закрытия
```

- Обкатка: 2026-09-06 17:02, свежее дерево b13999267337, сценарий e1360c2f, временный HOME, утилит дерева 11, шагов 10, все зелёные.

Шаг 1 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/internal" test ./taskform/... -count=1 -run "TestHoldingForks|TestForkGateNote" -v
=== RUN   TestHoldingForks
--- PASS: TestHoldingForks (0.00s)
=== RUN   TestForkGateNote
--- PASS: TestForkGateNote (0.00s)
PASS
ok  	github.com/dronrider/devkit/internal/taskform	0.477s
```

Шаг 2 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/shipctl" test . -count=1 -run "TestStartHeldByFork|TestStartPassesExecutorFork"
ok  	github.com/dronrider/devkit/tools/shipctl	2.419s
```

Шаг 3 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/taskctl" test . -count=1 -run "TestCloseHeldByFork|TestCloseAnsweredForkPasses"
ok  	github.com/dronrider/devkit/tools/taskctl	0.994s
```

Шаг 4 (зелёный):

```console
$ GOWORK=off go -C "$(git rev-parse --show-toplevel)/tools/dashboard" test . -count=1 -run "TestTaskForksInRow"
ok  	github.com/dronrider/devkit/tools/dashboard	2.020s
```

Шаг 5 (зелёный):

```console
$ w="${TMPDIR:-/tmp}/dk804-stand-a"; rm -rf "$w"; mkdir -p "$w"; git -C "$w" init -q -b main; git -C "$w" config user.email t@t; git -C "$w" config user.name t; taskctl -C "$w" init --prefix XR --name проба; taskctl -C "$w" add --id XR-001 --title "Старт под развилкой" --rank 25+0+0+0+0 --accept user --barrier глаза; taskctl -C "$w" add --id XR-002 --title "Закрытие под развилкой" --rank 25+0+0+0+0 --accept user --barrier глаза; git -C "$w" add -A; git -C "$w" commit -qm стенд; taskctl -C "$w" decide XR-001 --ask доступ --hint "считать приватным" "токен положен в secretctl?"; taskctl -C "$w" decide XR-002 --ask публичность "релизы видны без токена?"; shipctl -C "$w" start XR-001; taskctl -C "$w" close XR-002; git -C "$w" status --short docs/TASKS.md; rm -rf "$w"; echo "стенд убран"
доска создана в /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-a: docs/TASKS.md, docs/TASKS-archive.md, docs/tasks/
XR-001 заведена в backlog: P2, R=25
XR-002 заведена в backlog: P2, R=25
XR-001 (файл записи): развилка «доступ» заведена, решает человек, держит старт задачи
- «доступ» (открыта, держит старт): токен положен в secretctl?
  рекомендация: считать приватным
XR-002 (файл записи): развилка «публичность» заведена, решает человек, держит старт задачи
- «публичность» (открыта, держит старт): релизы видны без токена?
ошибка: XR-001: открытых развилок 1, решает человек, и до ответа старт задачи не идёт:
- «доступ»: токен положен в secretctl?
  рекомендация: считать приватным
ответ снимает развилку: taskctl decide XR-001 «доступ» --by человек "ответ и довод"
вопрос, на который отвечает исполнитель, передаётся ему: taskctl decide XR-001 «доступ» --leave
ошибка: XR-002: открытых развилок 1, решает человек, и до ответа закрытие задачи не идёт:
- «публичность»: релизы видны без токена?
ответ снимает развилку: taskctl decide XR-002 «публичность» --by человек "ответ и довод"
вопрос, на который отвечает исполнитель, передаётся ему: taskctl decide XR-002 «публичность» --leave
стенд убран
```

Шаг 6 (зелёный):

```console
$ w="${TMPDIR:-/tmp}/dk804-stand-b"; rm -rf "$w" "$w"-xr-001; mkdir -p "$w"; git -C "$w" init -q -b main; git -C "$w" config user.email t@t; git -C "$w" config user.name t; taskctl -C "$w" init --prefix XR --name проба; taskctl -C "$w" add --id XR-001 --title "Старт под развилкой" --rank 25+0+0+0+0 --accept user --barrier глаза; taskctl -C "$w" add --id XR-002 --title "Закрытие под развилкой" --rank 25+0+0+0+0 --accept user --barrier глаза; git -C "$w" add -A; git -C "$w" commit -qm стенд; taskctl -C "$w" decide XR-001 --ask доступ "токен положен в secretctl?"; taskctl -C "$w" decide XR-002 --ask публичность "релизы видны без токена?"; taskctl -C "$w" decide XR-001 «доступ» --by человек "положил, имя xr-token"; taskctl -C "$w" decide XR-002 «публичность» --by человек "да, релизы публичные"; shipctl -C "$w" start XR-001; taskctl -C "$w" close XR-002; rm -rf "$w" "$w"-xr-001; echo "стенд убран"
доска создана в /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-b: docs/TASKS.md, docs/TASKS-archive.md, docs/tasks/
XR-001 заведена в backlog: P2, R=25
XR-002 заведена в backlog: P2, R=25
XR-001 (файл записи): развилка «доступ» заведена, решает человек, держит старт задачи
- «доступ» (открыта, держит старт): токен положен в secretctl?
XR-002 (файл записи): развилка «публичность» заведена, решает человек, держит старт задачи
- «публичность» (открыта, держит старт): релизы видны без токена?
XR-001 (файл записи): развилка «доступ» решена
- «доступ» (решена): токен положен в secretctl?
  решено человеком 2026-09-06: положил, имя xr-token
XR-002 (файл записи): развилка «публичность» решена
- «публичность» (решена): релизы видны без токена?
  решено человеком 2026-09-06: да, релизы публичные
ветка xr-001 в worktree /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-b-xr-001
доска: XR-001: backlog -> in-progress, коммит da513f2
следующий шаг: код в дереве задачи (shipctl start XR-001), правка вместе с тестами, дальше ревью и слияние (shipctl merge XR-001); сдача это Check, а не «код написан». Слияние за пользователем: в .devkit/deploy.local стоит autonomous = false, агент останавливается на локальном коммите и ждёт команды
работать в /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-b-xr-001, по готовности: shipctl merge XR-001 (оттуда же или из основного чекаута)
XR-002 закрыта 2026-09-06, строка в архиве, файл задачи в tasks/archive/2026/XR-002.md
предупреждение: разлив упал: exit status 1 (ошибка: в рабочем дереве незакоммиченное, сначала закоммить:
M docs/TASKS-archive.md
 M docs/TASKS.md
 M docs/tasks/XR-001.md
RM docs/tasks/XR-002.md -> docs/tasks/archive/2026/XR-002.md
path: /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-b/.devkit/cmdout/20260906T170239-git-status/out)
следующий шаг: очередь выката свободна, следующая задача берётся с доски (taskctl list, дальше taskctl move <ID> in-progress)
стенд убран
```

Шаг 7 (зелёный):

```console
$ w="${TMPDIR:-/tmp}/dk804-stand-c"; rm -rf "$w" "$w"-xr-003; mkdir -p "$w"; git -C "$w" init -q -b main; git -C "$w" config user.email t@t; git -C "$w" config user.name t; taskctl -C "$w" init --prefix XR --name проба; taskctl -C "$w" add --id XR-003 --title "Развилка исполнителя" --rank 25+0+0+0+0 --accept user --barrier глаза; taskctl -C "$w" add --id XR-004 --title "Задача без перечня" --rank 25+0+0+0+0 --accept user --barrier глаза; git -C "$w" add -A; git -C "$w" commit -qm стенд; taskctl -C "$w" decide XR-003 --ask поле --who исполнитель --hint "«решает»" "как звать поле?"; shipctl -C "$w" start XR-003; taskctl -C "$w" close XR-004; rm -rf "$w" "$w"-xr-003; echo "стенд убран"
доска создана в /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-c: docs/TASKS.md, docs/TASKS-archive.md, docs/tasks/
XR-003 заведена в backlog: P2, R=25
XR-004 заведена в backlog: P2, R=25
XR-003 (файл записи): развилка «поле» заведена, решает исполнитель, старта не держит
- «поле» (оставлена исполнителю): как звать поле?
  рекомендация: «решает»
ветка xr-003 в worktree /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-c-xr-003
доска: XR-003: backlog -> in-progress, коммит 7b7563e
следующий шаг: код в дереве задачи (shipctl start XR-003), правка вместе с тестами, дальше ревью и слияние (shipctl merge XR-003); сдача это Check, а не «код написан». Слияние за пользователем: в .devkit/deploy.local стоит autonomous = false, агент останавливается на локальном коммите и ждёт команды
работать в /var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-c-xr-003, по готовности: shipctl merge XR-003 (оттуда же или из основного чекаута)
XR-004 закрыта 2026-09-06, строка в архиве, файл задачи в tasks/archive/2026/XR-004.md
предупреждение: разлив упал: exit status 1 (ошибка: в рабочем дереве незакоммиченное, сначала закоммить:
M docs/TASKS-archive.md
 M docs/TASKS.md
 M docs/tasks/XR-003.md
R  docs/tasks/XR-004.md -> docs/tasks/archive/2026/XR-004.md
path: /private/var/folders/x1/vsj11crd03dcghk7d4p8ppjm0000gn/T/dk804-stand-c/.devkit/cmdout/20260906T170240-git-status/out)
следующий шаг: очередь выката свободна, следующая задача берётся с доски (taskctl list, дальше taskctl move <ID> in-progress)
стенд убран
```

Шаг 8 (зелёный):

```console
$ cp "$(git rev-parse --show-toplevel)/docs/tasks/DK-804.md" "${TMPDIR:-/tmp}/dk804-task.bak"; taskctl -C "$(git rev-parse --show-toplevel)" decide DK-804 --ask проба --hint "проба" "проба сценария?"; taskctl -C "$(git rev-parse --show-toplevel)" decide DK-804 --open; cp "${TMPDIR:-/tmp}/dk804-task.bak" "$(git rev-parse --show-toplevel)/docs/tasks/DK-804.md"; rm -f "${TMPDIR:-/tmp}/dk804-task.bak"; git -C "$(git rev-parse --show-toplevel)" diff --stat -- docs/tasks/DK-804.md; echo "файл возвращён"
DK-804 (файл записи): развилка «проба» заведена, решает человек, держит старт задачи
- «проба» (открыта, держит старт): проба сценария?
  рекомендация: проба
- «проба» (открыта, держит старт): проба сценария?
  рекомендация: проба
файл возвращён
```

Шаг 9 (зелёный):

```console
$ grep -n "Развилка держит старт" "$(git rev-parse --show-toplevel)/tools/shipctl/README.md"
157:### Развилка держит старт
```

Шаг 10 (зелёный):

```console
$ grep -n "Ворота старта и закрытия" "$(git rev-parse --show-toplevel)/tools/taskctl/README.md"
1240:### Ворота старта и закрытия
```
