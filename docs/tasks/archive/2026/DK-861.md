# DK-861: Кольцо закрытой задачи не сходится из-за брошенных планов субагентов

## Что происходит

2026-09-07 в чате «План работ DK-851» кольцо показывает 26 из 32 при закрытой задаче. План диспетчера закрыт целиком, 8 из 8. Кольцо складывает его с планами субагентов из файлов ~/.devkit/plans/<sid>-sub-<метка>.json (subPlans в tools/dashboard/sessions.go). Ревьювер rev851 отметил один пункт из четырёх, проверяльщик chk851 оставил пункт в in_progress, исполнитель dk851 не закрыл последний. Работы тех же субагентов в журнале стоят закрытыми. Планов субагентов на машине 73, с незакрытыми пунктами 12, затронуто 10 сессий.

Человек читает кольцо как «задача недоделана» и идёт искать хвост, которого нет. Кольцо законченной задачи никогда не доходит до полного. Скилл work-plan закрыть сделанное перед возвратом велит, определения exec-* на скилл ссылаются, а у четырёх review-* блока про план нет вовсе, и субагент уходит с открытыми пунктами. Диспетчер этого не чинит, содержания чужого плана он не знает.

## Чего хотим

Кольцо задачи, у которой закрыты план диспетчера и работы субагентов, показывает полный круг. Пока работа субагента идёт, её план стоит в кольце своими пунктами; работа закрылась, и пункты сворачиваются в один закрытый пункт с заказом. Той же строкой определения exec-* и review-* получают требование закрыть план перед возвратом, а правка промптов проходит стенд prompt-test.

## Границы

- Текст самого правила плана в чате и синонимы состояний это DK-551 и DK-580,
  отменённая работа это DK-609.
- Ширина панели этапов при длинном плане это DK-742.

## Развилки

Считать ли пункты субагента выполненными по закрытой работе в журнале или сворачивать его план в один пункт «работа субагента»? Нужна ли фраза в определениях субагентов про закрытие плана перед отчётом, и как её проверять стендом prompt-test?

- «сведение»: чем кольцо считает пункты плана субагента, когда его работа в журнале уже закрыта
  - решает: человек
  - рекомендация: пока работа идёт, план субагента развёрнут пунктами, а закрылась - сворачивается в один закрытый пункт
  - решено человеком 2026-09-07: пока работа идёт, план субагента стоит своими пунктами; закрылась - сворачивается в один закрытый пункт
- «промпты»: правятся ли определения exec-* и review-* этой же строкой или отдельной
  - решает: человек
  - рекомендация: дашборд отдельной строкой сейчас, фраза в восьми определениях и прогон prompt-test отдельной строкой после
  - решено человеком 2026-09-07: одной строкой: дашборд, восемь определений exec-* и review-*, прогон стендом prompt-test

## DoD

Кольцо задачи, у которой закрыты план диспетчера и работы субагентов, доходит
до полного круга. Пока работа субагента идёт, её план стоит в кольце своими
пунктами и видно, на каком он шаге; работа закрылась, и пункты сворачиваются в
один закрытый пункт с заказом субагента. Уже лежащие двенадцать файлов чинятся
тем же счётом, без ручной уборки каталога планов.

Восемь определений (`exec-*` и `review-*`) требуют закрыть план перед
возвратом диспетчеру, причём у `review-*` блок про план заводится с нуля.
Правка промптов прогоняется стендом `prompt-test` со сценарием, где субагент
кончает работу с незакрытым пунктом.

Счёт кольца проверяется тестом по фикстурам планов и журналов, правка
промптов прогоном стенда.

## Ранг

`25+6+2+5+2 = 40`, P2.

- Серьёзность 25: кольцо законченной задачи врёт, человек идёт искать хвост,
  которого нет; обход дешёвый, состояние задачи видно на доске.
- Ценность 6: кольцо читается каждым заходом, и врёт оно на каждой задаче с
  субагентами.
- Неопределённость 2: подход выбран ответами человека, открыты детали
  сворачивания и формулировка блока у ревьюверов.
- Поправка на баг +5: счёт кольца сломан.
- Рычаг 2: своя польза, чтение хода работы снаружи.

## Разбор

Проверено чтением кода 2026-09-07. Пункты `subPlans` дописываются в кольцо
после `withSubWorks` (`tools/dashboard/sessions.go:3321`), поэтому закрытая
работа субагента их состояния не касается вовсе: сведение с журналом умеет
поднимать пункт в `in_progress` и не умеет его закрывать. Файлов планов
субагентов на машине 75, с незакрытыми пунктами 12, и у пяти из них закрыто
ноль пунктов: план положили и больше не трогали.

Черновик промахнулся насчёт скилла: `work-plan` требование несёт, пункт 5
ритма говорит закрыть сделанное перед возвратом. Определения `exec-*` про план
говорят и на скилл ссылаются, а у всех четырёх `review-*` блока про план нет
ни строчки, при том что ревьювер `rev851` план вёл и бросил на одном пункте из
четырёх. Правка промптов упирается в заведение блока у ревьюверов.

Дублей нет: на доске строк про кольцо и планы не стоит, ближайшая родня
закрыта (DK-527 про перетирание планов пачки, DK-613 про скилл и утилиту). В
накопителе рядом лежат DK-609, DK-551 и DK-580, все трое про текст правила
плана.

Условий, посильных только человеку, не нашёл: доступов, секретов и невозвратных
шагов наружу задача не требует, обе развилки закрыты ответами человека.

## Ход работы

- Из черновика DK-861 «Кольцо чата закрытой задачи остаётся неполным из-за планов субагентов»: записан 2026-09-07, оформлен 2026-09-07.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 6%, week_max 8% (панель разбивку не дала, цифра из кеша клиента возрастом 3ч 47м назад), снимок 36м назад, сдвига нет), 2026-09-08 09:53-10:09.
- Ревью: ревью провёл sonnet, ходов 51, минут 15, 2026-09-08 10:09-10:27.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 7%, week_max 8% (панель разбивку не дала, цифра из кеша клиента возрастом 4ч 32м назад), снимок 25м назад, сдвига нет), 2026-09-08 10:27-10:35.
- Ревью: ревью провёл sonnet, ходов 35, минут 12, 2026-09-08 10:35-10:49.
- Снаружи: проверка после выката, 2026-09-08 10:49-10:55.
- Проверка: сценарий прогнал sonnet, 2026-09-08 10:55-10:55.

## Ревью

Уровень 3 до 6a91bc8: неопределённость 2 из вердикта, триаж: правка меняет счёт кольца сессий tools/dashboard/sessions.go - путь, от которого зависит каждый показ плана на панели (DoD фичи чтения хода работы), critical_paths не задет

- [возврат: реализация, 2026-09-08] блокирует: subPlanOwner (tools/dashboard/sessions.go:2764) выбирает хозяина плана субагента правилом «начатая последней», не проверяя, чей план это на самом деле. Работ в одном окне бывает несколько (комментарий кода это признаёт), и когда более ранняя работа пачки уже вернулась, а более поздняя ещё идёт, план вернувшейся отдаётся более поздней (живой) работе: owner.item.State=in_progress делает live=true, и брошенный пункт вернувшейся работы остаётся в кольце вечно - тот же симптом DK-861, другой триггер. Эмпирически: subPlanOwner([]subWork{A вернулась (start -20m, last -5m), B идёт (start -15m, last сейчас)}, at=-6m) вернул owner=B вместо A. Такие сессии не редкость: на машине сейчас 16 сессий с несколькими планами субагентов сразу. TestPlanFoldsClosedSubagentPlan этот случай не покрывает: обе тестовые работы не пересекаются окнами. Нужен тест на пачку из двух работ, где ранняя вернулась, а поздняя ещё идёт, и матчинг, который не путает их местами (например: среди кандидатов в окне предпочитать того, чьё at ближе к last, а не того, кто стартовал позже).: исправлено, выбор хозяина плана переписан: среди работ, чьё окно накрывает время плана, берётся та, чей край last ближе к нему, ноль last (работа без журнала) уступает любой другой, равный зазор разводится поздним стартом; регрессия закрыта тестами TestPlanOwnerSplitsOverlappingSubWorks (пачка из двух пересекающихся работ через planOf) и TestSubPlanOwnerEdges (краевые случаи), regcheck на HEAD ветки краснеет
- [возврат: правила, 2026-09-08] не блокирует: обкатка стендом прогнана только с --agent-def review-medium, а «предмет» сценария 30-plan-close-on-return.md называет все восемь определений плюс SKILL.md. Текст exec-* и трёх других review-* идентичен review-medium дословно, так что поведенческий риск в основном закрыт, но сама зачтённая отметка формально покрывает и exec-*, которых прогон не касался. Стоило бы добавить хотя бы один прогон на exec-medium (либо явно пояснить в «Проверке», почему один агент-деф репрезентативен для всех восьми).: исправлено, добавлен второй прогон стенда на умолчании (--agent-def exec-medium, ярус mini, k 3): 1 из 3 против 0 из 3 у базы, вердикт «не хуже», зачтён; след и приписка о двух семействах определений лежат в «Проверке»
- Вердикт: без замечаний. круг 2 до b331a6f5b: fixed 1-2 сверены. Дельта 63412559b..b331a6f5b: subPlanOwner переписан на выбор по ближнему краю last, старый код (6a91bc865) воспроизведён во временном worktree - оба новых теста (TestPlanOwnerSplitsOverlappingSubWorks, TestSubPlanOwnerEdges) красные на нём по существу (владение планом путается), зелёные на HEAD; TestPlanFoldsClosedSubagentPlan не задет. go test ./tools/dashboard/... -count=1 зелёный целиком. Второй обеycheck-прогон (--agent-def exec-medium) закрывает пункт 2, след в «Проверке» соответствует исправленному коду (дерево 820468f53, обкатка перегнана на нём же в b331a6f5b). Строка исправлено стоит в той же строке, что маркер списка.

## Сценарий проверки

Шаги гоняются по порядку и держат общий стенд в `/tmp/dk861-stand`. Там
временный дом, синтетический проект и сессия с закрытым планом диспетчера.
Работа ревьювера вернулась два часа назад и бросила свой план на открытом
пункте, работа проверяльщика идёт. Кольцо спрашивается у поднятого дашборда
ручкой сессии, а не читается глазами. В ответе
`/api/projects/demo/sessions/dk861-ring` лежит поле `plan` с теми же пунктами,
по которым панель считает круг. Живого дашборда машины шаги не трогают. У
стенда свой дом, свой порт и своя копия бинаря. Каждый шаг это одна строка.

```sh
python3 -c 'import json,os,re,shutil,socket,time,pathlib as P;w=P.Path("/tmp/dk861-stand");shutil.rmtree(w,ignore_errors=True);now=time.time();S=lambda d:time.strftime("%Y-%m-%dT%H:%M:%S.000Z",time.gmtime(now+d));W=lambda p,t,d:(P.Path(p).write_text(t),os.utime(p,(now+d,now+d)));sid="dk861-ring";proj=w/"root/demo";sess=w/"home/.claude/projects"/re.sub("[^A-Za-z0-9-]","-",str(proj));subs=sess/sid/"subagents";plans=w/"home/.devkit/plans";[P.Path(d).mkdir(parents=True) for d in (proj/"docs",subs,plans)];side=lambda t,d:json.dumps({"type":"assistant","isSidechain":True,"timestamp":S(d),"message":{"role":"assistant","content":[{"type":"text","text":t}]}})+"\n";job=lambda n,about,born,quiet:(W(subs/("agent-%s.meta.json"%n),json.dumps({"agentType":"claude","description":about,"toolUseId":"tool-"+n}),0),W(subs/("agent-%s.jsonl"%n),side("отчёт субагента",born),quiet));plan=lambda n,items,d:W(plans/n,json.dumps(items,ensure_ascii=False),d);W(proj/"docs/TASKS.md","# Доска стенда (префикс XR)\n\n## In progress\n\n## Check\n\n## Backlog\n\n## Blocked\n",0);W(sess/(sid+".jsonl"),json.dumps({"type":"user","timestamp":S(-5*3600),"gitBranch":"main","message":{"role":"user","content":"возьми задачу в работу"}})+"\n",-3600);plan(sid+".json",[{"text":"раздать работы","state":"completed"},{"text":"собрать отчёт","state":"completed"}],-600);job("rev851","Ревью правки",-4*3600,-2*3600);plan(sid+"-sub-rev851.json",[{"text":"прочитать дифф","state":"completed"},{"text":"написать замечания","state":"in_progress"}],-2*3600);job("chk851","Прогон сценария",-1800,-30);plan(sid+"-sub-chk851.json",[{"text":"поднять стенд","state":"completed"},{"text":"прогнать шаги","state":"in_progress"}],-30);s=socket.socket();s.bind(("127.0.0.1",0));port=s.getsockname()[1];s.close();(w/"port").write_text(str(port));c=w/"home/.devkit/dashboard.local";c.write_text("root = %s\naddr = 127.0.0.1\nport = %d\ntoken = dk861-token\n"%(w/"root",port));c.chmod(0o600);print("стенд /tmp/dk861-stand готов: план диспетчера закрыт, ревьювер вернулся с брошенным планом, проверяльщик работает; порт",port)'
```

Ждём строку «стенд /tmp/dk861-stand готов» с номером порта.

Второй шаг воспроизводит баг на коде до правки. Дашборд собирается из родителя
того коммита, которым правка приехала в ветку.

```sh
d=/tmp/dk861-stand; mkdir -p "$d/old" && git archive "$(git log --format=%H -S 'func subPlanOwner' -- tools/dashboard/sessions.go | tail -1)^" | tar -x -C "$d/old" && (cd "$d/old/tools/dashboard" && GOWORK=off CGO_ENABLED=0 go build -o "$d/dashboard-old" .) && { HOME=$d/home "$d/dashboard-old" serve >"$d/old.log" 2>&1 & } && trap 'pkill -f "$d/dashboard-old"' EXIT && port=$(cat "$d/port") && for i in $(seq 1 60); do curl -sf "http://127.0.0.1:$port/healthz" >/dev/null && break; sleep 1; done && curl -sf -c "$d/jar" -H 'Content-Type: application/json' -d '{"token":"dk861-token"}' "http://127.0.0.1:$port/api/login" >/dev/null && curl -sf -b "$d/jar" "http://127.0.0.1:$port/api/projects/demo/sessions/dk861-ring" | python3 -c 'import json,sys;p=json.load(sys.stdin)["plan"];[print(" ",i["state"],i["text"]) for i in p];l=[i for i in p if i["state"]!="completed" and i["text"].startswith("rev851: ")];print("кольцо: закрыто %d из %d, открытых пунктов от вернувшегося ревьювера %d"%(sum(1 for i in p if i["state"]=="completed"),len(p),len(l)));sys.exit(None if l else "баг не воспроизвёлся: брошенный план вернувшейся работы в кольце не висит")'
```

Ожидание: в кольце висят пункты `rev851: ...`, а счёт говорит «закрыто 5 из 8,
открытых пунктов от вернувшегося ревьювера 1».

Третий шаг спрашивает то же кольцо у выкаченного дашборда. `dashboard` берётся
из PATH. На обкатке это сборка ветки, после выката бинарь от
`python3 tools/devkitctl/devkitctl.py build`.

```sh
d=/tmp/dk861-stand; cp "$(command -v dashboard)" "$d/dashboard-new" && { HOME=$d/home "$d/dashboard-new" serve >"$d/new.log" 2>&1 & } && trap 'pkill -f "$d/dashboard-new"' EXIT && port=$(cat "$d/port") && for i in $(seq 1 60); do curl -sf "http://127.0.0.1:$port/healthz" >/dev/null && break; sleep 1; done && curl -sf -c "$d/jar" -H 'Content-Type: application/json' -d '{"token":"dk861-token"}' "http://127.0.0.1:$port/api/login" >/dev/null && curl -sf -b "$d/jar" "http://127.0.0.1:$port/api/projects/demo/sessions/dk861-ring" | python3 -c 'import json,sys;p=json.load(sys.stdin)["plan"];[print(" ",i["state"],i["text"]) for i in p];has=lambda t,s:[i for i in p if i["text"]==t and i["state"]==s];bad=[m for m,c in (("план вернувшегося ревьювера остался в кольце пунктами",[i for i in p if i["text"].startswith("rev851: ")]),("вернувшаяся работа не стоит в кольце закрытым пунктом",not has("Ревью правки","completed")),("план идущей работы не развёрнут пунктами: на каком шаге субагент, не видно",not has("chk851: прогнать шаги","in_progress"))) if c];print("кольцо: закрыто %d из %d"%(sum(1 for i in p if i["state"]=="completed"),len(p)));sys.exit(bad[0] if bad else None)'
```

Ожидание: пунктов `rev851: ...` в кольце нет вовсе, вернувшаяся работа стоит
закрытым пунктом «Ревью правки», план идущей работы развёрнут своими пунктами,
и «chk851: прогнать шаги» в них открыт.

Четвёртый шаг доводит стенд до закрытой задачи. Работа проверяльщика тоже
вернулась и тоже бросила план на открытом пункте.

```sh
d=/tmp/dk861-stand; cp "$(command -v dashboard)" "$d/dashboard-new" && python3 -c 'import json,os,time,pathlib as P;w=P.Path("/tmp/dk861-stand");now=time.time();subs=next((w/"home/.claude/projects").glob("*/dk861-ring/subagents"));log=subs/"agent-chk851.jsonl";log.write_text(json.dumps({"type":"assistant","isSidechain":True,"timestamp":time.strftime("%Y-%m-%dT%H:%M:%S.000Z",time.gmtime(now-4*3600)),"message":{"role":"assistant","content":[{"type":"text","text":"отчёт субагента"}]}})+"\n");[os.utime(f,(now-3*3600,now-3*3600)) for f in (log,w/"home/.devkit/plans/dk861-ring-sub-chk851.json")];print("проверяльщик вернулся три часа назад, его план тоже брошен на открытом пункте")' && { HOME=$d/home "$d/dashboard-new" serve >"$d/done.log" 2>&1 & } && trap 'pkill -f "$d/dashboard-new"' EXIT && port=$(cat "$d/port") && for i in $(seq 1 60); do curl -sf "http://127.0.0.1:$port/healthz" >/dev/null && break; sleep 1; done && curl -sf -c "$d/jar" -H 'Content-Type: application/json' -d '{"token":"dk861-token"}' "http://127.0.0.1:$port/api/login" >/dev/null && curl -sf -b "$d/jar" "http://127.0.0.1:$port/api/projects/demo/sessions/dk861-ring" | python3 -c 'import json,sys;p=json.load(sys.stdin)["plan"];[print(" ",i["state"],i["text"]) for i in p];print("кольцо: закрыто %d из %d"%(sum(1 for i in p if i["state"]=="completed"),len(p)));sys.exit("кольцо опустело" if not p else ("у закрытой задачи в кольце висит открытый пункт" if [i for i in p if i["state"]!="completed"] else None))'
```

Ждём счёт «закрыто 4 из 4» и полный круг.

Вторую половину DoD, требование закрыть план в восьми определениях `exec-*` и
`review-*`, закрыл прогон стенда `prompt-test`. Его след лежит ниже, в
«Проверке», и шагом сценария не повторяется.

## Выкат

- 2026-09-08 слито: 18ceacbcb, 10af25af6, 348c69294, a7d7155c4, d15ff3ed7, 89eea1364, 34b7af4a5, 3a41ad288, c8d5d0738, de4d34389, aed9cb8f0
- smoke прогнан, 2026-09-08

## Проверка

- Стенд: 2026-09-08 09:32, дерево b48a30ca5, предмет 0b82d161, база старый, ярус mini, k 3, сценарии 30-plan-close-on-return, зачтён.

```console
$ obeycheck --devkit /Users/rider/projects/devkit-dk-861 --only 30-plan-close-on-return -k 3 --tier mini --end субагент --agent-def review-medium --base старый --keep --no-preflight --task DK-861 --work /private/tmp/obey861/runs4 /private/tmp/obey861/cand /private/tmp/obey861/base
сценарий                                       cand  base  вердикт
закрыть план работ перед возвратом диспетчеру   2/3   0/3  не хуже

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  30-plan-close-on-return / cand / повтор 3: проверка: exit status 1 (плана работ субагент не положил)
  30-plan-close-on-return / base / повтор 1: проверка: exit status 1 (плана работ субагент не положил)
```

- Стенд: 2026-09-08 10:24, дерево 6a91bc865, предмет 0b82d161, база старый, ярус mini, k 3, сценарии 30-plan-close-on-return, зачтён.

```console
$ obeycheck --devkit /Users/rider/projects/devkit-dk-861 --only 30-plan-close-on-return -k 3 --tier mini --end субагент --agent-def exec-medium --base старый --keep --no-preflight --task DK-861 --work /private/tmp/obey861/runs6 /private/tmp/obey861/cand /private/tmp/obey861/base
сценарий                                       cand  base  вердикт
закрыть план работ перед возвратом диспетчеру   1/3   0/3  не хуже

итог: сценариев 1, все зачтены, база старый

первый красный прогон каждого угла:
  30-plan-close-on-return / cand / повтор 1: проверка: exit status 1 (плана работ субагент не положил)
  30-plan-close-on-return / base / повтор 1: проверка: exit status 1 (плана работ субагент не положил)
```

Прогонов два, по одному на каждое семейство определений: `review-medium` и
`exec-medium`. Остальные шесть текстом дословно совпадают со своим семейством,
и отдельного прогона им не заводили.

- Обкатка: 2026-09-08 10:49, свежее дерево adadf807b8d2, сценарий b79894d3, временный HOME, утилит дерева 11, шагов 4, все зелёные.

Шаг 1 (зелёный):

```console
$ python3 -c 'import json,os,re,shutil,socket,time,pathlib as P;w=P.Path("/tmp/dk861-stand");shutil.rmtree(w,ignore_errors=True);now=time.time();S=lambda d:time.strftime("%Y-%m-%dT%H:%M:%S.000Z",time.gmtime(now+d));W=lambda p,t,d:(P.Path(p).write_text(t),os.utime(p,(now+d,now+d)));sid="dk861-ring";proj=w/"root/demo";sess=w/"home/.claude/projects"/re.sub("[^A-Za-z0-9-]","-",str(proj));subs=sess/sid/"subagents";plans=w/"home/.devkit/plans";[P.Path(d).mkdir(parents=True) for d in (proj/"docs",subs,plans)];side=lambda t,d:json.dumps({"type":"assistant","isSidechain":True,"timestamp":S(d),"message":{"role":"assistant","content":[{"type":"text","text":t}]}})+"\n";job=lambda n,about,born,quiet:(W(subs/("agent-%s.meta.json"%n),json.dumps({"agentType":"claude","description":about,"toolUseId":"tool-"+n}),0),W(subs/("agent-%s.jsonl"%n),side("отчёт субагента",born),quiet));plan=lambda n,items,d:W(plans/n,json.dumps(items,ensure_ascii=False),d);W(proj/"docs/TASKS.md","# Доска стенда (префикс XR)\n\n## In progress\n\n## Check\n\n## Backlog\n\n## Blocked\n",0);W(sess/(sid+".jsonl"),json.dumps({"type":"user","timestamp":S(-5*3600),"gitBranch":"main","message":{"role":"user","content":"возьми задачу в работу"}})+"\n",-3600);plan(sid+".json",[{"text":"раздать работы","state":"completed"},{"text":"собрать отчёт","state":"completed"}],-600);job("rev851","Ревью правки",-4*3600,-2*3600);plan(sid+"-sub-rev851.json",[{"text":"прочитать дифф","state":"completed"},{"text":"написать замечания","state":"in_progress"}],-2*3600);job("chk851","Прогон сценария",-1800,-30);plan(sid+"-sub-chk851.json",[{"text":"поднять стенд","state":"completed"},{"text":"прогнать шаги","state":"in_progress"}],-30);s=socket.socket();s.bind(("127.0.0.1",0));port=s.getsockname()[1];s.close();(w/"port").write_text(str(port));c=w/"home/.devkit/dashboard.local";c.write_text("root = %s\naddr = 127.0.0.1\nport = %d\ntoken = dk861-token\n"%(w/"root",port));c.chmod(0o600);print("стенд /tmp/dk861-stand готов: план диспетчера закрыт, ревьювер вернулся с брошенным планом, проверяльщик работает; порт",port)'
стенд /tmp/dk861-stand готов: план диспетчера закрыт, ревьювер вернулся с брошенным планом, проверяльщик работает; порт 61926
```

Шаг 2 (зелёный):

```console
$ d=/tmp/dk861-stand; mkdir -p "$d/old" && git archive "$(git log --format=%H -S 'func subPlanOwner' -- tools/dashboard/sessions.go | tail -1)^" | tar -x -C "$d/old" && (cd "$d/old/tools/dashboard" && GOWORK=off CGO_ENABLED=0 go build -o "$d/dashboard-old" .) && { HOME=$d/home "$d/dashboard-old" serve >"$d/old.log" 2>&1 & } && trap 'pkill -f "$d/dashboard-old"' EXIT && port=$(cat "$d/port") && for i in $(seq 1 60); do curl -sf "http://127.0.0.1:$port/healthz" >/dev/null && break; sleep 1; done && curl -sf -c "$d/jar" -H 'Content-Type: application/json' -d '{"token":"dk861-token"}' "http://127.0.0.1:$port/api/login" >/dev/null && curl -sf -b "$d/jar" "http://127.0.0.1:$port/api/projects/demo/sessions/dk861-ring" | python3 -c 'import json,sys;p=json.load(sys.stdin)["plan"];[print(" ",i["state"],i["text"]) for i in p];l=[i for i in p if i["state"]!="completed" and i["text"].startswith("rev851: ")];print("кольцо: закрыто %d из %d, открытых пунктов от вернувшегося ревьювера %d"%(sum(1 for i in p if i["state"]=="completed"),len(p),len(l)));sys.exit(None if l else "баг не воспроизвёлся: брошенный план вернувшейся работы в кольце не висит")'
  completed раздать работы
  completed собрать отчёт
  completed Ревью правки
  completed chk851: поднять стенд
  completed rev851: прочитать дифф
  in_progress Прогон сценария
  in_progress chk851: прогнать шаги
  in_progress rev851: написать замечания
кольцо: закрыто 5 из 8, открытых пунктов от вернувшегося ревьювера 1
```

Шаг 3 (зелёный):

```console
$ d=/tmp/dk861-stand; cp "$(command -v dashboard)" "$d/dashboard-new" && { HOME=$d/home "$d/dashboard-new" serve >"$d/new.log" 2>&1 & } && trap 'pkill -f "$d/dashboard-new"' EXIT && port=$(cat "$d/port") && for i in $(seq 1 60); do curl -sf "http://127.0.0.1:$port/healthz" >/dev/null && break; sleep 1; done && curl -sf -c "$d/jar" -H 'Content-Type: application/json' -d '{"token":"dk861-token"}' "http://127.0.0.1:$port/api/login" >/dev/null && curl -sf -b "$d/jar" "http://127.0.0.1:$port/api/projects/demo/sessions/dk861-ring" | python3 -c 'import json,sys;p=json.load(sys.stdin)["plan"];[print(" ",i["state"],i["text"]) for i in p];has=lambda t,s:[i for i in p if i["text"]==t and i["state"]==s];bad=[m for m,c in (("план вернувшегося ревьювера остался в кольце пунктами",[i for i in p if i["text"].startswith("rev851: ")]),("вернувшаяся работа не стоит в кольце закрытым пунктом",not has("Ревью правки","completed")),("план идущей работы не развёрнут пунктами: на каком шаге субагент, не видно",not has("chk851: прогнать шаги","in_progress"))) if c];print("кольцо: закрыто %d из %d"%(sum(1 for i in p if i["state"]=="completed"),len(p)));sys.exit(bad[0] if bad else None)'
  completed раздать работы
  completed собрать отчёт
  completed Ревью правки
  completed chk851: поднять стенд
  in_progress Прогон сценария
  in_progress chk851: прогнать шаги
кольцо: закрыто 4 из 6
```

Шаг 4 (зелёный):

```console
$ d=/tmp/dk861-stand; cp "$(command -v dashboard)" "$d/dashboard-new" && python3 -c 'import json,os,time,pathlib as P;w=P.Path("/tmp/dk861-stand");now=time.time();subs=next((w/"home/.claude/projects").glob("*/dk861-ring/subagents"));log=subs/"agent-chk851.jsonl";log.write_text(json.dumps({"type":"assistant","isSidechain":True,"timestamp":time.strftime("%Y-%m-%dT%H:%M:%S.000Z",time.gmtime(now-4*3600)),"message":{"role":"assistant","content":[{"type":"text","text":"отчёт субагента"}]}})+"\n");[os.utime(f,(now-3*3600,now-3*3600)) for f in (log,w/"home/.devkit/plans/dk861-ring-sub-chk851.json")];print("проверяльщик вернулся три часа назад, его план тоже брошен на открытом пункте")' && { HOME=$d/home "$d/dashboard-new" serve >"$d/done.log" 2>&1 & } && trap 'pkill -f "$d/dashboard-new"' EXIT && port=$(cat "$d/port") && for i in $(seq 1 60); do curl -sf "http://127.0.0.1:$port/healthz" >/dev/null && break; sleep 1; done && curl -sf -c "$d/jar" -H 'Content-Type: application/json' -d '{"token":"dk861-token"}' "http://127.0.0.1:$port/api/login" >/dev/null && curl -sf -b "$d/jar" "http://127.0.0.1:$port/api/projects/demo/sessions/dk861-ring" | python3 -c 'import json,sys;p=json.load(sys.stdin)["plan"];[print(" ",i["state"],i["text"]) for i in p];print("кольцо: закрыто %d из %d"%(sum(1 for i in p if i["state"]=="completed"),len(p)));sys.exit("кольцо опустело" if not p else ("у закрытой задачи в кольце висит открытый пункт" if [i for i in p if i["state"]!="completed"] else None))'
проверяльщик вернулся три часа назад, его план тоже брошен на открытом пункте
  completed раздать работы
  completed собрать отчёт
  completed Ревью правки
  completed Прогон сценария
кольцо: закрыто 4 из 4
```

- Check: 2026-09-08 10:51, дерево adadf807b8d2. Бинарь взят из PATH:
  `dashboard v0.12.0-5595-gadadf807b (adadf807b8d2)`, тот же коммит, что HEAD.
  Собран выкатом (`python3 tools/devkitctl/devkitctl.py build`), не сборкой из
  дерева. Временный HOME, свой порт, своя копия бинаря. Шагов 4, все зелёные.
  Живой дашборд машины и настоящий `~/.devkit` не тронуты.

Шаг 1 (зелёный):

```console
$ python3 -c 'import json,os,re,shutil,socket,time,pathlib as P;w=P.Path("/tmp/dk861-stand");shutil.rmtree(w,ignore_errors=True);now=time.time();S=lambda d:time.strftime("%Y-%m-%dT%H:%M:%S.000Z",time.gmtime(now+d));W=lambda p,t,d:(P.Path(p).write_text(t),os.utime(p,(now+d,now+d)));sid="dk861-ring";proj=w/"root/demo";sess=w/"home/.claude/projects"/re.sub("[^A-Za-z0-9-]","-",str(proj));subs=sess/sid/"subagents";plans=w/"home/.devkit/plans";[P.Path(d).mkdir(parents=True) for d in (proj/"docs",subs,plans)];side=lambda t,d:json.dumps({"type":"assistant","isSidechain":True,"timestamp":S(d),"message":{"role":"assistant","content":[{"type":"text","text":t}]}})+"\n";job=lambda n,about,born,quiet:(W(subs/("agent-%s.meta.json"%n),json.dumps({"agentType":"claude","description":about,"toolUseId":"tool-"+n}),0),W(subs/("agent-%s.jsonl"%n),side("отчёт субагента",born),quiet));plan=lambda n,items,d:W(plans/n,json.dumps(items,ensure_ascii=False),d);W(proj/"docs/TASKS.md","# Доска стенда (префикс XR)\n\n## In progress\n\n## Check\n\n## Backlog\n\n## Blocked\n",0);W(sess/(sid+".jsonl"),json.dumps({"type":"user","timestamp":S(-5*3600),"gitBranch":"main","message":{"role":"user","content":"возьми задачу в работу"}})+"\n",-3600);plan(sid+".json",[{"text":"раздать работы","state":"completed"},{"text":"собрать отчёт","state":"completed"}],-600);job("rev851","Ревью правки",-4*3600,-2*3600);plan(sid+"-sub-rev851.json",[{"text":"прочитать дифф","state":"completed"},{"text":"написать замечания","state":"in_progress"}],-2*3600);job("chk851","Прогон сценария",-1800,-30);plan(sid+"-sub-chk851.json",[{"text":"поднять стенд","state":"completed"},{"text":"прогнать шаги","state":"in_progress"}],-30);s=socket.socket();s.bind(("127.0.0.1",0));port=s.getsockname()[1];s.close();(w/"port").write_text(str(port));c=w/"home/.devkit/dashboard.local";c.write_text("root = %s\naddr = 127.0.0.1\nport = %d\ntoken = dk861-token\n"%(w/"root",port));c.chmod(0o600);print("стенд /tmp/dk861-stand готов: план диспетчера закрыт, ревьювер вернулся с брошенным планом, проверяльщик работает; порт",port)'
стенд /tmp/dk861-stand готов: план диспетчера закрыт, ревьювер вернулся с брошенным планом, проверяльщик работает; порт 61974
```

Шаг 2 (зелёный, старый код до 6a91bc865, воспроизводит баг). Рубеж DK-770 не
пускает `cd` в связке с редиректом, поэтому сборка старого дерева гонялась
двумя вызовами вместо одного subshell из сценария, команды и итог те же:

```console
$ d=/tmp/dk861-stand; mkdir -p "$d/old" && git archive "$(git log --format=%H -S 'func subPlanOwner' -- tools/dashboard/sessions.go | tail -1)^" | tar -x -C "$d/old"
$ cd /tmp/dk861-stand/old/tools/dashboard && GOWORK=off CGO_ENABLED=0 go build -o /tmp/dk861-stand/dashboard-old .
$ d=/tmp/dk861-stand; { HOME=$d/home "$d/dashboard-old" serve >"$d/old.log" 2>&1 & } && trap 'pkill -f "$d/dashboard-old"' EXIT && port=$(cat "$d/port") && for i in $(seq 1 60); do curl -sf "http://127.0.0.1:$port/healthz" >/dev/null && break; sleep 1; done && curl -sf -c "$d/jar" -H 'Content-Type: application/json' -d '{"token":"dk861-token"}' "http://127.0.0.1:$port/api/login" >/dev/null && curl -sf -b "$d/jar" "http://127.0.0.1:$port/api/projects/demo/sessions/dk861-ring" | python3 -c 'import json,sys;p=json.load(sys.stdin)["plan"];[print(" ",i["state"],i["text"]) for i in p];l=[i for i in p if i["state"]!="completed" and i["text"].startswith("rev851: ")];print("кольцо: закрыто %d из %d, открытых пунктов от вернувшегося ревьювера %d"%(sum(1 for i in p if i["state"]=="completed"),len(p),len(l)));sys.exit(None if l else "баг не воспроизвёлся: брошенный план вернувшейся работы в кольце не висит")'
  completed раздать работы
  completed собрать отчёт
  completed Ревью правки
  completed chk851: поднять стенд
  completed rev851: прочитать дифф
  in_progress Прогон сценария
  in_progress chk851: прогнать шаги
  in_progress rev851: написать замечания
кольцо: закрыто 5 из 8, открытых пунктов от вернувшегося ревьювера 1
```

Шаг 3 (зелёный, выкаченный бинарь):

```console
$ d=/tmp/dk861-stand; cp "$(command -v dashboard)" "$d/dashboard-new" && { HOME=$d/home "$d/dashboard-new" serve >"$d/new.log" 2>&1 & } && trap 'pkill -f "$d/dashboard-new"' EXIT && port=$(cat "$d/port") && for i in $(seq 1 60); do curl -sf "http://127.0.0.1:$port/healthz" >/dev/null && break; sleep 1; done && curl -sf -c "$d/jar" -H 'Content-Type: application/json' -d '{"token":"dk861-token"}' "http://127.0.0.1:$port/api/login" >/dev/null && curl -sf -b "$d/jar" "http://127.0.0.1:$port/api/projects/demo/sessions/dk861-ring" | python3 -c 'import json,sys;p=json.load(sys.stdin)["plan"];[print(" ",i["state"],i["text"]) for i in p];has=lambda t,s:[i for i in p if i["text"]==t and i["state"]==s];bad=[m for m,c in (("план вернувшегося ревьювера остался в кольце пунктами",[i for i in p if i["text"].startswith("rev851: ")]),("вернувшаяся работа не стоит в кольце закрытым пунктом",not has("Ревью правки","completed")),("план идущей работы не развёрнут пунктами: на каком шаге субагент, не видно",not has("chk851: прогнать шаги","in_progress"))) if c];print("кольцо: закрыто %d из %d"%(sum(1 for i in p if i["state"]=="completed"),len(p)));sys.exit(bad[0] if bad else None)'
  completed раздать работы
  completed собрать отчёт
  completed Ревью правки
  completed chk851: поднять стенд
  in_progress Прогон сценария
  in_progress chk851: прогнать шаги
кольцо: закрыто 4 из 6
```

Шаг 4 (зелёный, выкаченный бинарь):

```console
$ d=/tmp/dk861-stand; cp "$(command -v dashboard)" "$d/dashboard-new" && python3 -c 'import json,os,time,pathlib as P;w=P.Path("/tmp/dk861-stand");now=time.time();subs=next((w/"home/.claude/projects").glob("*/dk861-ring/subagents"));log=subs/"agent-chk851.jsonl";log.write_text(json.dumps({"type":"assistant","isSidechain":True,"timestamp":time.strftime("%Y-%m-%dT%H:%M:%S.000Z",time.gmtime(now-4*3600)),"message":{"role":"assistant","content":[{"type":"text","text":"отчёт субагента"}]}})+"\n");[os.utime(f,(now-3*3600,now-3*3600)) for f in (log,w/"home/.devkit/plans/dk861-ring-sub-chk851.json")];print("проверяльщик вернулся три часа назад, его план тоже брошен на открытом пункте")' && { HOME=$d/home "$d/dashboard-new" serve >"$d/done.log" 2>&1 & } && trap 'pkill -f "$d/dashboard-new"' EXIT && port=$(cat "$d/port") && for i in $(seq 1 60); do curl -sf "http://127.0.0.1:$port/healthz" >/dev/null && break; sleep 1; done && curl -sf -c "$d/jar" -H 'Content-Type: application/json' -d '{"token":"dk861-token"}' "http://127.0.0.1:$port/api/login" >/dev/null && curl -sf -b "$d/jar" "http://127.0.0.1:$port/api/projects/demo/sessions/dk861-ring" | python3 -c 'import json,sys;p=json.load(sys.stdin)["plan"];[print(" ",i["state"],i["text"]) for i in p];print("кольцо: закрыто %d из %d"%(sum(1 for i in p if i["state"]=="completed"),len(p)));sys.exit("кольцо опустело" if not p else ("у закрытой задачи в кольце висит открытый пункт" if [i for i in p if i["state"]!="completed"] else None))'
проверяльщик вернулся три часа назад, его план тоже брошен на открытом пункте
  completed раздать работы
  completed собрать отчёт
  completed Ревью правки
  completed Прогон сценария
кольцо: закрыто 4 из 4
```

Все четыре шага сошлись с ожиданием. Расхождений между сценарием и
выкаченным кодом не нашлось. Стенд `/tmp/dk861-stand` и процессы
`dashboard-old`/`dashboard-new` убраны по завершении.
