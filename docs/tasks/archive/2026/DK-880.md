# DK-880: Правило разговора едет по собеседнику вместо метки задачи

## Что происходит

Хук старта сессии `hooks/session-task.py` кладёт в контекст строки `CHAT_RULE`,
`PLAN_RULE` и `PACE_RULE` при непустой переменной `DEVKIT_TASK`. Без задачи
`ordered_task` возвращает пустую строку, и `run_hook` выходит с нулём молча.
Переменную ставит дашборд в `launchEnv` (`tools/dashboard/chats.go:1683`) при
непустом id разговора. Условие закреплено сценарием проверки DK-614. Второй
его шаг зовёт хук с `env -u DEVKIT_TASK` и ждёт пустого вывода. Разобрано в
разговоре 2026-09-09.

Общий чат доски заводится с пустой привязкой (DK-430.7). Правил разговора он
не получает. Цену такой потери мерил стенд DK-614 на ярусе base. Без скилла ответ
уходил в сокет панели через `SendMessage` восемь раз из тринадцати, со скиллом
ни разу из двадцати трёх. Не едет туда и `PLAN_RULE`, и кольцо в шапке такого
чата стоит молчащим. Метка задачи стоит ещё и у исполнителя в живом окне, а
собеседник у него диспетчер. Правило 1 велит такому исполнителю писать текстом
в ленту, отвечает он каналом `SendMessage`.

### Разбор

Прогон 2026-09-09 дал вот что. С пустым `DEVKIT_TASK` хук выходит без вывода,
с заказом отдаёт все три строки любой сессии. Одного признака `headless()`
не хватает. Оболочка конвейера снимает у живой головы метку печатного режима
сама (`kit/skills/board-task/task-run.py:476`, ради рубежа синхронности). По
этому признаку голова исполнителя неотличима от окна с человеком.

Зато подъёмщик знает собеседника заранее. Дорог подъёма три. Чаты идут через
`launchEnv` (`tools/dashboard/chats.go:1677`), конвейер через `headlessEnv`
(`tools/dashboard/runs.go:411`), делегат ставит себе `DEVKIT_RUN_DEPTH=1`
(`tools/agentctl/run.go:406`). Консольную сессию человек поднимает руками, и
метку о себе там ставить некому. Метится поэтому отсутствие человека.

Такой признак уже написан задачей DK-847. Переменную `DEVKIT_HIDDEN` ставит
подъёмщик (`tools/agentctl/run.go:410`, `kit/skills/goal-loop/goal-run.py:661`,
`tools/dashboard/chats.go:1645`), из наследства tmux она вычищена
(`chats.go:1690`), и хук читает её функцией `hidden()`
(`hooks/session-task.py:112`). Своей переменной задаче заводить не надо.

Конвейер, поднятый кнопкой экрана, признака не получает намеренно
(`runs.go:406`). Живой голове правило разговора и остаётся. Человек подаёт
реплики в её окно через панель (`task-run.py:508`), а подпись
`from-name="human"` отличает их от реплик диспетчера.

## Чего хотим

Строку про скилл разговора получает живое окно с человеком в собеседниках.
Метка задачи отвечает за контекст задачи, строку доски и файл постановки.

## Границы

- Сам скилл `board-chat` правится только там, где он опирается на метку задачи.
  Правил разговора задача не переписывает.
- Привязка разговора к задаче, реестр чатов и панель остаются прежними.
- Своих переменных окружения задача не заводит и точек подъёма не трогает.

## Развилки

- «признак»: Чем сессия узнаёт, что собеседник у неё человек: признака headless() одного не хватает, живая голова конвейера снимает метку печатного режима сама (task-run.py:476) и от чата человека не отличается
  - решает: человек
  - рекомендация: явная переменная от подъёмщика: дашборд и оболочка конвейера знают, кого поднимают, и говорят это прямо
  - вариант: явная переменная от подъёмщика (DEVKIT_PEER=human у чата, у конвейера её нет)
  - вариант: имя окна из DEVKIT_TMUX: chat-* это разговор, task-* это конвейер
  - вариант: признак headless() как есть, а живую голову конвейера гасить отдельной меткой
  - решено человеком 2026-09-09: явная переменная от подъёмщика: дашборд и оболочка конвейера знают, кого поднимают, и говорят это прямо
- «состав»: Что отбирается по собеседнику, а что остаётся всем: кольцо в шапке дашборд рисует по плану и у конвейера тоже, и без PLAN_RULE оно у него замолчит
  - решает: человек
  - рекомендация: по собеседнику едет только правило разговора, план и отзывчивость остаются у всех, кто ведёт работу
  - вариант: по собеседнику отбирается только CHAT_RULE, PLAN_RULE и PACE_RULE едут и конвейеру
  - вариант: по собеседнику отбираются все три строки, конвейер не получает ничего
  - решено человеком 2026-09-09: по собеседнику едет только правило разговора, план и отзывчивость остаются у всех, кто ведёт работу
- «сценарий-614»: Сценарий проверки закрытой DK-614 закрепляет молчание хука без DEVKIT_TASK: второй шаг зовёт его с env -u DEVKIT_TASK и ждёт пустого вывода. Общему чату доски правила поедут только со сменой этого условия
  - решает: человек
  - рекомендация: переписать шаг, а прежнее условие оставить только конвейеру: пустой вывод без задачи и без признака собеседника
  - вариант: шаг 2 сценария DK-614 переписывается этой задачей
  - вариант: условие DK-614 держится как есть, правила без задачи не едут никому
  - решено человеком 2026-09-09: переписать шаг, а прежнее условие оставить только конвейеру: пустой вывод без задачи и без признака собеседника
- «полярность»: Консольную сессию человек поднимает руками, и метку о себе ставить некому. От этого зависит, какой стороной ставится признак
  - решает: человек
  - рекомендация: метится отсутствие человека: правило разговора едет по умолчанию, а гасит его метка подъёмщика
  - вариант: метка у того, кто поднял сессию не для человека (конвейер, делегат run, печатный клиент), консоль и чат получают правило по умолчанию
  - вариант: метка у человека, а консольной сессии заводится своя дорога её поставить (обёртка запуска или настройка проекта)
  - решено человеком 2026-09-09: метится отсутствие человека: правило разговора едет по умолчанию, а гасит его метка подъёмщика
- «признак-847»: DEVKIT_HIDDEN отвечает на вопрос, кто позвал сессию, а не кто у неё собеседник: конвейер, поднятый кнопкой экрана, признака не получает (runs.go:406), а человек подаёт в его окно реплики через панель (task-run.py:508)
  - решает: человек
  - рекомендация: переиспользовать DEVKIT_HIDDEN как есть, а живой голове конвейера правило разговора оставить: реплики человека через панель это её штатная дорога
  - вариант: берём DEVKIT_HIDDEN как есть, конвейеру правило остаётся, задача сводится к чату доски и консоли
  - вариант: конвейер метит живую голову всегда, независимо от того, кто нажал кнопку, и hidden расширяется до «человека в собеседниках нет»
  - вариант: рядом с hidden заводится второй признак, про собеседника, а hidden остаётся про список панели
  - решено человеком 2026-09-09: переиспользовать DEVKIT_HIDDEN как есть, а живой голове конвейера правило разговора оставить: реплики человека через панель это её штатная дорога

## DoD

- Правило разговора едет сессии без признака `DEVKIT_HIDDEN`. Это чат доски с
  пустой привязкой, чат задачи, живая голова конвейера и консольная сессия,
  поднятая руками.
- Сессия с признаком правила разговора не получает. Это делегат
  `agentctl run`, виток цикла цели и тиковый прогон конвейера.
- План и отзывчивость едут всем сессиям по-прежнему, признак их не отбирает.
- Второй шаг сценария DK-614 переписан. Без задачи и без признака хук говорит,
  с признаком молчит.
- Стенд obeycheck прогнан по скиллу prompt-test, след прогона лежит в файле
  задачи.

## Ранг

`25+6+1+0+4 = 36`, P2.

- Серьёзность 25. В чате доски ответ уходит каналом мимо ленты, восемь раз из
  тринадцати на замере DK-614, и кольцо плана там стоит пустым. Обход дешёвый,
  человек первой репликой просит прочитать скилл.
- Ценность 6. Тот же замер даёт со скиллом ноль промахов из двадцати трёх.
  Касается это чата доски и консоли, главный поток задач правила уже получает.
- Неопределённость 1. Развилки сняты разбором, а признак сессии без человека
  готов задачей DK-847.
- Поправка на баг 0. По собеседнику правила не отбирались никогда, и это новое
  поведение.
- Рычаг 4. Процедуру разговора получают чат доски и консольная сессия, и
  каждый следующий разговор в них идёт по написанным правилам.

Цена S. Правится отбор строк в одном хуке и второй шаг сценария DK-614,
переменная берётся готовой.

## Ход работы

- Из черновика DK-880 «правила разговора привязаны к задаче вместо собеседника»: записан 2026-09-09, оформлен 2026-09-09.
- Вычитка: 1 файл, 4 правки, 0 пометок, 2026-09-09.
- Уточнение: вопрос: «признак-847»: DEVKIT_HIDDEN отвечает на вопрос, кто позвал сессию, а не кто у неё собеседник: конвейер, поднятый кнопкой экрана, признака не получает (runs.go:406), а человек подаёт в его окно реплики через панель (task-run.py:508), 2026-09-09 14:42-15:47.
- Разработка: субагент sonnet/high по вердикту pick (квота: week_all 19%, снимок 10м назад, сдвига нет), 2026-09-09 15:47-15:47.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 20%, снимок 1м назад, сдвига нет), 2026-09-09 16:24-16:28.
- Ревью: ревью провёл sonnet, ходов 22, минут 3, 2026-09-09 16:28-16:41.
- Снаружи: блок: ждёт DK-888: go:dashboard красный на main блокирует shipctl merge, 2026-09-09 16:41-10:45.
- Снаружи: проверка после выката, 2026-09-10 10:54-10:54.
- Проверка: сценарий прогнал opus, 2026-09-10 10:54-10:54.

## Ревью

Уровень 3 до 3183aa8: неопределённость 1, но дифф трогает hooks/session-task.py из critical_paths (совпадение отвечает да на оба вопроса триажа доступ/горячий путь: хук стоит на старте каждой сессии) - два да дают третий уровень сразу

- Вердикт: без замечаний. DoD построчно: живой путь по всем четырём веткам run_hook (нет задачи/нет hidden -> CHAT_RULE без плана, нет задачи/hidden -> тишина, задача/hidden -> PLAN+PACE без CHAT_RULE, задача/compact/hidden -> та же фраза без просьбы перечитать) прогнан руками на дереве ветки и совпал с ожиданиями; переписанный второй шаг сценария DK-614 прогнан построчно (3 проверки, все ок); python3 -m unittest hooks.session_task_test - 34/34 ок; звенья признака DEVKIT_HIDDEN (run.go:410, goal-run.py:661, chats.go:1645/1668, runs.go:415) сверены с постановкой и совпадают; obeycheck-след в файле задачи по формату prompt-test (--tier base, k=10, предмет, зачтён); check-symbols на тронутых файлах чисто

## Сценарий проверки

Вид агентский. Хук `session-task.py` гоняется на образце события `SessionStart`
с прямым выводом, каждая строка блока это свой шаг. Первый ждёт строку про
`board-chat` у сессии без задачи и без признака `DEVKIT_HIDDEN`. Второй ждёт
пустого вывода у сессии без задачи с признаком. Третий проверяет скрытую
сессию с задачей, у неё едет план, но не строка про `board-chat`. Четвёртый
берёт сессию с задачей без признака, она держит прежнее поведение. Пятый шаг
это юнит-тесты хука.

```sh
printf '%s' '{"session_id":"rehearse","cwd":"/tmp","transcript_path":"/tmp/t.jsonl","hook_event_name":"SessionStart","source":"startup"}' | env -u DEVKIT_TASK -u DEVKIT_HIDDEN python3 hooks/session-task.py --hook claude-code | grep -q board-chat
[ -z "$(printf '%s' '{"session_id":"rehearse","cwd":"/tmp","transcript_path":"/tmp/t.jsonl","hook_event_name":"SessionStart","source":"startup"}' | env -u DEVKIT_TASK DEVKIT_HIDDEN=1 python3 hooks/session-task.py --hook claude-code)" ]
out=$(printf '%s' '{"session_id":"rehearse","cwd":"/tmp","transcript_path":"/tmp/t.jsonl","hook_event_name":"SessionStart","source":"startup"}' | DEVKIT_TASK=DK-880 DEVKIT_HIDDEN=1 python3 hooks/session-task.py --hook claude-code) && echo "$out" | grep -q "agentctl plan" && ! echo "$out" | grep -q board-chat
printf '%s' '{"session_id":"rehearse","cwd":"/tmp","transcript_path":"/tmp/t.jsonl","hook_event_name":"SessionStart","source":"startup"}' | DEVKIT_TASK=DK-880 python3 hooks/session-task.py --hook claude-code | grep -q board-chat
python3 -m unittest hooks.session_task_test
```

Ожидание: все пять шагов выходят нулём, пятый шаг печатает `OK` и число
пройденных тестов.

## Выкат

- 2026-09-10 слито: 63f9b381, b993783a, 92d857e9, 6c370661, 8bef221b

## Проверка

- Стенд: 2026-09-09 16:23, дерево 77e45425, предмет fe40e432, база пусто, ярус base, k 10, сценарии 30-chat-no-task-human-reply, зачтён.

```console
$ obeycheck --task DK-880 --scenarios /Users/rider/projects/devkit-dk-880/tools/obeycheck/scenarios --tier base --only 30-chat-no-task-human-reply -k 10 --base пусто /private/tmp/claude-501/-Users-rider-projects-devkit/6d71e61d-8024-4541-bb82-a3710a108d06/scratchpad/rules-with /private/tmp/claude-501/-Users-rider-projects-devkit/6d71e61d-8024-4541-bb82-a3710a108d06/scratchpad/rules-without
сценарий                                                rules-with  rules-without  вердикт
ответить человеку из общего чата доски текстом в ленту        4/10           0/10  польза

итог: сценариев 1, все зачтены, база пусто

первый красный прогон каждого угла:
  30-chat-no-task-human-reply / rules-with / повтор 1: судья: нет
  30-chat-no-task-human-reply / rules-without / повтор 1: судья: нет

разбор судьи:
  30-chat-no-task-human-reply / rules-with / повтор 1: **Цитата:** текст не содержит прямых обращений на «вы» **Анализ:** в тексте используется безличная форма рассказа («Это баг», «На доске уже есть», «Отдельно замечу», «Ничего не правил») и первое лицо («отмечаю», «замечу»), но нет прямых обращений к читателю в форме «вы», «вам», «проверьте» или подоб...
  30-chat-no-task-human-reply / rules-with / повтор 2: **Цитата:** > Ничего не правил, как вы и просили. В этой фразе спикер обращается к спрашивающему на «вы» («вы и просили»), имея в виду человека, который читает ответ. да
  30-chat-no-task-human-reply / rules-with / повтор 3: Процитирую место из текста: **«вы просили ничего не править, так и оставляю»** да
  30-chat-no-task-human-reply / rules-with / повтор 4: Проверю текст на наличие обращений к читателю на «вы». В тексте используются: - Безличные конструкции: "Похоже", "нигде рядом нет" - Глаголы в форме первого лица: "ничего не правил" Обращений на «вы» (типа «проверьте», «вам», «вы можете») в тексте нет. Автор говорит о фактах и о своих действиях, но ...
  30-chat-no-task-human-reply / rules-with / повтор 5: **Цитаты:** - "per **your** request" - "if **you** want, I can pick up OB-002" **Вывод:** да
  30-chat-no-task-human-reply / rules-with / повтор 6: # Анализ Проведя анализ текста, я не обнаружил обращений к спрашивающему на форму "вы" (типа "проверьте", "вам", "вы" и т.д.). Текст содержит: - Повествование о баге ("Это баг, не так и задумано") - Описание проблемы ("имя `oldtool` в README и в докстринге... расходится") - Информацию о задаче ("На ...
  30-chat-no-task-human-reply / rules-with / повтор 7: **Цитата:** > но вы просили пока ничего не менять, так что я только сообщаю. **Анализ:** В тексте есть прямое обращение к читателю ответа на «вы» в форме «вы просили». Спрашивающий - это человек, который читает этот ответ и ранее дал указание ничего не менять. да
  30-chat-no-task-human-reply / rules-with / повтор 8: **Найденное место:** > Правку сейчас не делаю, вы просили только ответ. **Вердикт:** да
  30-chat-no-task-human-reply / rules-with / повтор 9: В тексте обращений к спрашивающему на «вы» не найдено. Текст описывает ситуацию от третьего лица, упоминает договоренность («Как договорились»), но не содержит прямых обращений вроде «проверьте», «вам», «вы видите» и т.п. нет
  30-chat-no-task-human-reply / rules-with / повтор 10: **Анализ:** В тексте отсутствуют обращения к читающему на «вы». Текст написан от первого лица автора ответа («Проверил», «нет расхождения») или в объективном третьем лице («это не забытое переименование»), но нет ни одной конструкции, которая обращалась бы к спрашивающему: - нет слова «вы» - нет сло...
  30-chat-no-task-human-reply / rules-without / повтор 1: Критерий: **обращение к спрашивающему на «вы»** Анализируя текст, вижу только безличные конструкции и анализ без прямого обращения: - "Похоже, что это не баг..." - безличное выражение - "...нигде не нашёл..." - описание результата поиска, но не обращение к читателю - "Так что либо название задумано....
  30-chat-no-task-human-reply / rules-without / повтор 2: Проанализирую текст на наличие обращения к спрашивающему на "вы". **Анализ:** В тексте отсутствуют обращения к читателю на "вы". Текст написан в третьем лице ("Судя по всему, это не баг..."), это объективный анализ ситуации без прямого обращения к спрашивающему. Нет ни слов "проверьте", "вам", "вы" ...
  30-chat-no-task-human-reply / rules-without / повтор 4: **Результат анализа:** В тексте нет явного обращения к спрашивающему на «вы» в формах типа «проверьте», «вам», «вы». Единственное указание на спрашивающего - фраза «as requested» в конце, но это не является прямым обращением на «вы», а лишь упоминанием о его просьбе в третьем лице. **нет**
  30-chat-no-task-human-reply / rules-without / повтор 6: В тексте **нет обращения на «вы»**. Текст написан в третьем лице и описательно-аналитическом стиле. Автор излагает свой анализ ситуации, но не обращается к читателю с помощью форм «вы», «вам», «проверьте» или подобных. нет
  30-chat-no-task-human-reply / rules-without / повтор 7: Проанализирую текст по критерию. Текст полностью написан в повествовательном стиле, без прямых обращений к читателю: - "Не баг" - "Название совпадает..." - "Файл называется..." - "расхождение... но это не опечатка..." Формы обращения на «вы» (типа «проверьте», «вам», «вы») в тексте отсутствуют. Отве...
  30-chat-no-task-human-reply / rules-without / повтор 9: Ищу обращения на «вы» в тексте: 1. "Проверю README" - это первое лицо («я проверю»), а не обращение к читателю 2. "Правок не делал" - первое лицо 3. Нет слов типа «проверьте», «вам», «вы» или других форм обращения ко второму лицу В тексте автор говорит о своих действиях и выводах, но не обращается к...
  30-chat-no-task-human-reply / rules-without / повтор 10: В тексте нет обращения к спрашивающему на «вы». Текст содержит только утверждение о том, что README и tool.py согласованно называют утилиту oldtool, но не содержит форм вроде «проверьте», «вам», «вы» и других обращений на «вы» к человеку, читающему ответ. **нет**
```

- Обкатка: 2026-09-10 10:54, свежее дерево a1084042f80d, сценарий 0c822464, временный HOME, утилит дерева 11, шагов 5, все зелёные.

Шаг 1 (зелёный):

```console
$ printf '%s' '{"session_id":"rehearse","cwd":"/tmp","transcript_path":"/tmp/t.jsonl","hook_event_name":"SessionStart","source":"startup"}' | env -u DEVKIT_TASK -u DEVKIT_HIDDEN python3 hooks/session-task.py --hook claude-code | grep -q board-chat

```

Шаг 2 (зелёный):

```console
$ [ -z "$(printf '%s' '{"session_id":"rehearse","cwd":"/tmp","transcript_path":"/tmp/t.jsonl","hook_event_name":"SessionStart","source":"startup"}' | env -u DEVKIT_TASK DEVKIT_HIDDEN=1 python3 hooks/session-task.py --hook claude-code)" ]

```

Шаг 3 (зелёный):

```console
$ out=$(printf '%s' '{"session_id":"rehearse","cwd":"/tmp","transcript_path":"/tmp/t.jsonl","hook_event_name":"SessionStart","source":"startup"}' | DEVKIT_TASK=DK-880 DEVKIT_HIDDEN=1 python3 hooks/session-task.py --hook claude-code) && echo "$out" | grep -q "agentctl plan" && ! echo "$out" | grep -q board-chat

```

Шаг 4 (зелёный):

```console
$ printf '%s' '{"session_id":"rehearse","cwd":"/tmp","transcript_path":"/tmp/t.jsonl","hook_event_name":"SessionStart","source":"startup"}' | DEVKIT_TASK=DK-880 python3 hooks/session-task.py --hook claude-code | grep -q board-chat

```

Шаг 5 (зелёный):

```console
$ python3 -m unittest hooks.session_task_test
..................................
----------------------------------------------------------------------
Ran 34 tests in 2.659s

OK
```
- Прогон сценария: 2026-09-10, дерево b9e710df, шаги 1-5 зелёные, прогонял opus (разработку вёл sonnet).

```console
шаг 1: 0
шаг 2: 0
шаг 3: 0
шаг 4: 0
Ran 34 tests in 2.410s

OK
```
