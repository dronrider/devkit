# DK-966: Воронка снимает голову по фантомным проходам от лишних отметок конца

## Что происходит

2026-09-12, конвейер DK-962 живой головой из дашборда, окно task-DK-962, сессия acdf6a28. В 16:57:53 оболочка task-run.py насчитала три холостых прохода подряд и сняла окно, строки .devkit/log с 89950 по 90358. Из трёх проходов честным был один: проход 2 в 15:50 за 15 с, короткий отчёт головы о работе исполнителя. Проходы 5 (16:41:20) и 6 (16:57:52) кончились за 0 с: оболочка съела как конец прохода отметку «кончен», оставшуюся от прошлого хода. В turns.log на один «ход начат» лежат два «ход кончен» (16:10:53 и 16:11:10, 16:11:25; 16:41:20 и 16:41:40, 16:41:49). Вторая отметка появляется так: стоп-хук сторожа фоновых работ (hooks/agent-watch.py) жалуется на «работа кончилась, а сессия про это не узнала», голова отвечает второй репликой, и ход кончается ещё раз. Хвост пачки отметок wait_turn кладёт в self.pending, и следующий проход закрывается им мгновенно. Между проходами 2 и 5 стояли три честных ожидания по DK-899 (проходы 3, 4, 5 с отметками agentctl wait), но ветка `if waited: holds += 1` счётчик idle не сбрасывает, и единица от 15:50 дожила до 16:57.

Жалоба стоп-хука сама ложная. Запись субагента-исполнителя adf949cffb285bede в ~/.devkit/agents/acdf6a28.json перезаписана его же фоновой командой стенда: тот же ключ, type exec-high, job shell, command obeycheck. Метод busy() оболочки считает живыми только записи с job subagent, поэтому и оболочка, и хук видели «субагентов нет» при живом исполнителе. Обход «проход кончился при живых субагентах, заказ не подаётся» не сработал.

Сверка по коду 2026-09-12 держит все три механизма живыми. В `wait_turn`
(kit/skills/board-task/task-run.py, строки 1042-1051) проход закрывает первое же
слово конца из очереди `pending`. Парного «начат» рядом никто не ищет. Ветка
`if waited: holds += 1` (строка 1176) оставляет счётчик холостых нетронутым, ноль
ему ставит только соседняя ветка. В `launched` (hooks/agent-watch.py, строка 255)
запись ложится по ключу события. Ключ у фоновой команды субагента тот же, что у
самого субагента. В `hooks/hookio.py` (строки 271-276) `agent_id` события старше
собственного номера фоновой работы, и образец
`hooks/testdata/claude-code/tool-done-subagent.json` показывает этот ключ у события
внутри субагента. Тестов ни на один из трёх случаев нет. Стаб клиента
(kit/skills/board-task/task_run_test.py, строка 330) пишет одну отметку конца на
заказ, а запись реестра в хелпере `watch` ложится мимо `launched`.

Окно снято через две секунды после того, как голова начала настоящий ход по коммиту исполнителя (ход начат 16:57:51, коммит d07b6931 в 16:57:40). Коммиты в ветке dk-962 уцелели, замечание ревью снято, а хвост головы (второй круг ревью, отметки стенда, слияние) потерян, строка осталась в in-progress без сессии, как в DK-953. Тот же рисунок, что у DK-899 и черновика DK-840, но другим входом: воронка срабатывает не на коротких честных ходах, а на отметках, которых ход не делал. Любая задача, где исполнитель пускает фоновую команду, а хук на неё жалуется, накапливает фантомные проходы и умирает на третьем.

## Чего хотим

Проход оболочки закрывается только концом того хода, который она заказала, и после честного ожидания счёт холостых начинается заново. Фоновая команда субагента не затирает его запись в реестре, живой исполнитель виден оболочке и стоп-хуку, и голова с работающим исполнителем воронкой не снимается.

## Границы

Правка трогает воронку холостых проходов и ключ записи в реестре сторожа. Видимость
ожидания в панели это DK-955, ожидание чужого замка конвейера DK-840, подъём строки
после снятия окна DK-953. Ни один из трёх предметов сюда не едет. Строка идёт вне
цели DK-902 по решению развилки «цель».

Условий, посильных только человеку, не нашёл. Доступов, секретов, чужих команд и
шагов наружу правка не просит. Все три случая воспроизводятся на стенде временного
репозитория.

## Развилки

Считать ли конец прохода только отметкой, пришедшей после заказа этого прохода (по времени или по паре начат/кончен), и сбрасывать ли счётчик холостых после честного ожидания. Должна ли запись субагента в реестре сторожа переживать его фоновую команду: отдельный ключ у команды, либо вложенная запись под субагентом.

- «кейсы»: кейсы записи названы верно и полно?
  - решает: человек
  - рекомендация: четыре кейса: (1) исполнитель пускает фоновую команду, сторож теряет его запись в реестре и жалуется стоп-хуком; (2) голова отвечает на жалобу второй репликой, и на один заказ приходит две отметки «кончен»; (3) лишняя отметка закрывает следующий проход за 0 с, и он считается холостым; (4) честное машинное ожидание счётчик холостых не сбрасывает, и единица живёт часами до снятия окна
  - решено человеком 2026-09-12: четыре кейса: (1) исполнитель пускает фоновую команду, сторож теряет его запись в реестре и жалуется стоп-хуком; (2) голова отвечает на жалобу второй репликой, и на один заказ приходит две отметки «кончен»; (3) лишняя отметка закрывает следующий проход за 0 с, и он считается холостым; (4) честное машинное ожидание счётчик холостых не сбрасывает, и единица живёт часами до снятия окна
- «проход»: чем отличить лишнюю отметку «кончен» от честного конца чужого хода?
  - решает: исполнитель
  - рекомендация: закрывать проход только отметкой, перед которой в той же очереди лежал «начат», пришедший после заказа: ход человека тоже даёт пару, а лишняя реплика на жалобу сторожа парного «начат» не имеет
  - вариант: сверять время отметки со временем заказа, пару не разбирать
  - источник: код, kit/skills/board-task/task-run.py:1042-1051, возражение обхода: ход человека оболочка тоже не заказывала (task_run_test.py:765)
- «реестр»: как фоновой команде субагента не затирать его запись в реестре?
  - решает: исполнитель
  - рекомендация: команда субагента ложится своим ключом (ID субагента плюс ID работы харнеса), разряд записи остаётся один на запись: тогда job_of, busy, lost_of и shell_ends читаются без правки
  - вариант: вложенная запись команды под записью субагента
  - источник: код, hooks/agent-watch.py:255 и hooks/hookio.py:271, образец hooks/testdata/claude-code/tool-done-subagent.json
- «состав»: три механизма чинятся одной строкой или режутся?
  - решает: человек
  - рекомендация: одна строка на три механизма: они бьют одним рисунком, тесты у них общие, а починка одного корня оставляет два живых входа в ту же воронку
  - вариант: строка на реестр сторожа, вторая на воронку оболочки
  - вариант: только реестр сторожа, воронку оставить как есть
  - источник: возражение обхода: тестов нет ни на один из трёх механизмов, стаб пишет одну отметку на заказ (task_run_test.py:330)
  - решено человеком 2026-09-12: одна строка на три механизма: они бьют одним рисунком, тесты у них общие, а починка одного корня оставляет два живых входа в ту же воронку
- «счётчик»: сбрасывать ли счётчик холостых после честного машинного ожидания?
  - решает: человек
  - рекомендация: честное ожидание счёт холостых обнуляет: ожидание значит, что работа шла, а воронка ловит подряд идущие пустые проходы
  - вариант: счёт не обнулять, иначе чередование ожидания и пустого прохода держит окно вечно
  - решено человеком 2026-09-12: честное ожидание счёт холостых обнуляет: ожидание значит, что работа шла, а воронка ловит подряд идущие пустые проходы
- «цель»: куда садится строка: в цель DK-902 или вне её?
  - решает: человек
  - рекомендация: строка вне цели: виток DK-902 идёт по нарезке, а баг чинится на горячем следе и ждать витка ему незачем
  - вариант: наследная строка цели DK-902
  - источник: доска, DK-902 и её первая задача DK-899, поставившая ветку holds
  - решено человеком 2026-09-12: строка вне цели: виток DK-902 идёт по нарезке, а баг чинится на горячем следе и ждать витка ему незачем

## DoD

Проход конвейера закрывает та отметка конца, у которой в очереди лежит парный
«начат» этого заказа. Лишняя реплика на жалобу сторожа даёт отметку без пары.
Такая отметка проход не кончает. Ход, начатый репликой человека, пару имеет, и
проход от него кончается по-прежнему.

Счёт холостых проходов после честного машинного ожидания начинается заново.

Фоновая команда субагента ложится в реестр сторожа своим ключом. Запись самого
субагента остаётся на месте. Метод `busy()` оболочки видит живого исполнителя, и
жалобы стоп-хука на пропажу больше нет.

Тестами закрыты три случая. Две отметки конца на один «начат» окна не снимают.
Чередование ожидания и пустого прохода счёт не копит. Фоновая команда из субагента
оставляет в реестре обе записи. Краснота каждого теста проверена `regcheck` на коде
до правки.

Снаружи работа видна строкой журнала конвейера. Непарная отметка названа вслух с
номером прохода, молча её не отбрасывают.

## Ранг

`75+8+2+5+2 = 92`, P0.

- Серьёзность 75. Конвейер стоит. Голову сняли при живом исполнителе, хвост работы
  по DK-962 потерян, и строка до сих пор висит в In progress без сессии.
- Ценность 8. Фантомные проходы копит любая задача, где исполнитель пускает фоновую
  команду. Рисунок этот встречается на каждом прогоне стенда и слияния.
- Неопределённость 2. Три механизма названы строками кода. Открытыми остались
  детали, обе оставлены исполнителю с рекомендацией.
- Поправка на баг +5. Сломалось то, что работало. Затёртая запись обносит обход
  DK-870 про живых субагентов.
- Рычаг 2. Дешевле становится свой поток конвейера, чужие задачи правка не ускоряет.

## Ход работы

- Из черновика DK-966 «Воронка снимает голову по фантомным проходам от лишних отметок «кончен»»: записан 2026-09-12, оформлен 2026-09-12.
- Исключение: стенд (task-run.py это код оболочки, его поведение проверяют
  тесты task_run_test.py. В SKILL.md board-task, раздел «Конвейер задачи с
  экрана», добавлены два пункта: чем оболочка кончает проход и когда
  начинает счёт холостых заново. Шаги сессии от них не меняются.)
- Разработка: субагент opus/medium по вердикту pick (квота: week_all 80%, week_max 45% профицит, снимок 22м назад, сдвига нет), 2026-09-13 20:21-20:21.
- Вычитка: 1 файл, 3 правки, 0 пометок, 2026-09-13.
- Ребейз на main с DK-979, 2026-09-13. Конфликт был один, в стабе живой головы:
  обе ветки дописали свой шаг сценария. Оба шага оставлены, у каждого свой хвост
  отметок. Механизмы соседние и не спорят: DK-979 придерживает проход по живой
  фоновой работе до воронки, DK-966 кончает проход парной отметкой и начинает
  счёт холостых заново. Коммиты после ребейза сменили sha, и записи ревью
  называют старые.
- Сверх трёх решённых механизмов правка меняет четвёртое. В `wait_turn` сброс
  придержания зова человека стоит теперь на втором ходе прохода. Ход, которым
  проход открылся, придержания не снимает. Разбор пары «начат»/«кончен»
  потребовал честной отметки «начат» на первый заказ и в стабе живой головы. С
  нею вскрылся срез. Вопрос своего же хода оболочка читает запасным каналом
  раньше отметок, и «начат» из той же пачки снимал придержание. Человека звали
  дважды про один вопрос. Срез держит `test_one_question_calls_the_human_once`.
  Краснота его на коде до правки проверена `regcheck` с базой на родителе
  первого коммита правки.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 81%, week_max 46% профицит, снимок 13м назад, сдвига нет), 2026-09-13 21:45-22:07.
- Ревью: ревью провёл sonnet, ходов 60, минут 21, 2026-09-13 22:07-22:19.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 81%, week_max 46% профицит, снимок 47м назад, протух, сдвига нет), 2026-09-13 22:19-22:24.
- Ревью: ревью провёл sonnet, ходов 28, минут 4, 2026-09-13 22:24-23:25.
- Снаружи: проверка после выката, 2026-09-13 23:25-23:30.
- Проверка: сценарий прогнал sonnet, 2026-09-13 23:30-23:30.

## Ревью

Уровень 3 до 50acc6e: круг 2, оба замечания первого круга сверены с диффом, критичный путь hooks в этой дельте не тронут

- [возврат: реализация, 2026-09-13] не блокирует: в wait_turn (kit/skills/board-task/task-run.py) сброс self.said_stuck на TURN_STARTED теперь обусловлен begun (строки ~1073-1075). Это четвёртое поведенческое изменение сверх трёх решённых развилкой пунктов (спорная отметка, сброс holds, ключ реестра). Логика обоснована в комментарии (первый «начат» прохода не должен гасить придержание, второй внутри прохода должен), полный прогон task_run_test.py::TestLiveHead зелёный (24/24), включая test_one_question_calls_the_human_once и test_question_after_the_turn_resumed_calls_again, но отдельного теста на сценарий «двойное уведомление человека при первом начат» нет. Прошу явно назвать этот дрейф в «Ход работы» (расширение сверх решённых трёх механизмов) либо добавить тест на срез, который он чинит: исправлено, дрейф назван в «Ходе работы», а срез держит test_one_question_calls_the_human_once: краснота его на коде до правки проверена regcheck --base b44a9278^
- [возврат: реализация, 2026-09-13] блокирует: kit/skills/board-task/task-run.py:1320-1321, run_passes(). Тот же счётчик холостых, что чинили в run_live (строка 1223), тут остался нетронутым: "if self.held(n, after): holds += 1" не сбрасывает idle. DoD задачи сформулирован без оговорки про живую голову ("счёт холостых проходов после честного машинного ожидания начинается заново"), а run_passes это второй, действующий путь оболочки (печатная череда без tmux, класс TestPasses). Единица холостых от старого прохода так же доживёт через несколько честных ожиданий до трёх и снимет окно вхолостую, хотя работа шла. Чем проверить: в task-run.py одно место со сбросом (строка 1223) и одно без (строка 1321), тестов на путь run_passes для этого случая нет ни одного (в классе TestPasses нет теста вроде test_wait_starts_the_idle_count_over). Сделать: тот же сброс idle в run_passes и тест по образцу test_wait_starts_the_idle_count_over в TestPasses, либо назвать явно в файле задачи, почему run_passes сюда не входит.: исправлено, сброс idle стоит и в run_passes, печатная череда; тест TestPasses.test_wait_starts_the_idle_count_over, краснота regcheck на HEAD
- Вердикт: без замечаний. Круг 2: оба замечания круга 1 сверены с диффом 1f85ced..50acc6ee. Блокирующее (run_passes без сброса idle) закрыто симметричным сбросом holds,idle=holds+1,0 и тестом TestPasses.test_wait_starts_the_idle_count_over, краснота на b44a9278^ и зелень на HEAD проверены regcheck лично. Неблокирующее (дрейф said_stuck) закрыто припиской в «Ход работы», срез держит test_one_question_calls_the_human_once, краснота на b44a9278^ проверена regcheck лично. Сценарий проверки шаг 3 включает новый тест, обкатка перегнана после фикса (50acc6ee после 32815a3a), SKILL.md строка про сброс холостых после ожидания верна для обоих путей

## Сценарий проверки

1. Баг воспроизводится на коде до правки. Временное дерево встаёт на родителе
   коммита правки, и сторож получает два события подряд: запуск фонового
   субагента и уход в фон команды, поданной изнутри этого субагента. Оба
   события собраны из живых образцов `hooks/testdata/claude-code`.

   ```sh
   tmp=$(mktemp -d); base=$(git log --format=%H --grep=DK-966 -- hooks/agent-watch.py | tail -1); git worktree add -q --detach "$tmp/old" "$base^" && python3 -c 'import json,sys;d="hooks/testdata/claude-code/";t=sys.argv[1];[json.dump(dict(json.load(open(d+f)),**e),open(t+"/"+n,"w")) for f,e,n in [("tool-done-agent-launch.json",{"session_id":"dk966"},"launch.json"),("tool-done-bash-background.json",{"session_id":"dk966","agent_id":"ac08b50abbc64e119","agent_type":"general-purpose"},"shell.json")]]' "$tmp" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 "$tmp/old/hooks/agent-watch.py" --hook claude-code < "$tmp/launch.json" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 "$tmp/old/hooks/agent-watch.py" --hook claude-code < "$tmp/shell.json" && python3 -c 'import json,sys;d=json.load(open(sys.argv[1]));print(sorted((k,v["job"]) for k,v in d["agents"].items()))' "$tmp/agents/dk966.json" && git worktree remove --force "$tmp/old"
   ```

   Ожидаемый итог: в реестре стоит одна запись, `[('ac08b50abbc64e119',
   'shell')]`. Команда легла ключом субагента и затёрла его запись, живого
   исполнителя в реестре не осталось.

2. Та же пара событий на выкаченном коде.

   ```sh
   tmp=$(mktemp -d); python3 -c 'import json,sys;d="hooks/testdata/claude-code/";t=sys.argv[1];[json.dump(dict(json.load(open(d+f)),**e),open(t+"/"+n,"w")) for f,e,n in [("tool-done-agent-launch.json",{"session_id":"dk966"},"launch.json"),("tool-done-bash-background.json",{"session_id":"dk966","agent_id":"ac08b50abbc64e119","agent_type":"general-purpose"},"shell.json")]]' "$tmp" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 hooks/agent-watch.py --hook claude-code < "$tmp/launch.json" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 hooks/agent-watch.py --hook claude-code < "$tmp/shell.json" && python3 -c 'import json,sys;d=json.load(open(sys.argv[1]));print(sorted((k,v["job"]) for k,v in d["agents"].items()))' "$tmp/agents/dk966.json" && grep "запущено субагентом" "$tmp/agents/agents.log"
   ```

   Ожидаемый итог: записей две, `[('ac08b50abbc64e119', 'subagent'),
   ('bpmzinhpl', 'shell')]`. Запись исполнителя на месте, и в журнале сдач
   стоит строка «запущено субагентом ac08b50abbc64e119».

3. Краснота тестов оболочки на коде до правки.

   ```sh
   base=$(git log --format=%H --grep=DK-966 -- kit/skills/board-task/task-run.py | tail -1); regcheck -C kit/skills/board-task --base "$base^" --tests kit/skills/board-task/task_run_test.py -- python3 -m unittest task_run_test.TestLiveHead.test_extra_turn_end_does_not_close_a_pass task_run_test.TestLiveHead.test_wait_starts_the_idle_count_over task_run_test.TestPasses.test_wait_starts_the_idle_count_over
   ```

   Ожидаемый итог: строка «тест краснеет на `<коммит>^` и проходит на текущем
   коде, регрессия закрыта». Первый тест проверяет проход при двух отметках
   конца на один «начат». Второй проверяет чередование ожидания и пустого
   прохода у живой головы, третий то же у печатной череды.

4. Краснота теста реестра сторожа на коде до правки.

   ```sh
   base=$(git log --format=%H --grep=DK-966 -- hooks/agent-watch.py | tail -1); regcheck -C hooks --base "$base^" --tests hooks/agent_watch_test.py -- python3 -m unittest agent_watch_test.Watch.test_command_from_a_subagent_keeps_both_records
   ```

   Ожидаемый итог: строка «тест краснеет на `<коммит>^` и проходит на текущем
   коде, регрессия закрыта». Тест проверяет, что фоновая команда субагента
   ложится своим ключом и не затирает запись исполнителя.

## Выкат

- 2026-09-13 слито: e79a035a, 526062dd, a1b577fe, a107ebec, 6a0b93e2, a68fb3ed, e1072eb5, bddf83ba, a1aeba5d, 8e3fcdde, 822bd3ea, 2f707415, 5553dfa7, 4dd0b5fb, 5e38317e
- smoke прогнан, 2026-09-13

## Проверка

- Ревью: шаг 4 (регистр сторожа) прогнан отдельно 2026-09-13, зелёный.

Шаг 1 (зелёный):

```console
$ tmp=$(mktemp -d); base=$(git log --format=%H --grep=DK-966 -- hooks/agent-watch.py | tail -1); git worktree add -q --detach "$tmp/old" "$base^" && python3 -c 'import json,sys;d="hooks/testdata/claude-code/";t=sys.argv[1];[json.dump(dict(json.load(open(d+f)),**e),open(t+"/"+n,"w")) for f,e,n in [("tool-done-agent-launch.json",{"session_id":"dk966"},"launch.json"),("tool-done-bash-background.json",{"session_id":"dk966","agent_id":"ac08b50abbc64e119","agent_type":"general-purpose"},"shell.json")]]' "$tmp" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 "$tmp/old/hooks/agent-watch.py" --hook claude-code < "$tmp/launch.json" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 "$tmp/old/hooks/agent-watch.py" --hook claude-code < "$tmp/shell.json" && python3 -c 'import json,sys;d=json.load(open(sys.argv[1]));print(sorted((k,v["job"]) for k,v in d["agents"].items()))' "$tmp/agents/dk966.json" && git worktree remove --force "$tmp/old"
[('ac08b50abbc64e119', 'shell')]
```

Шаг 2 (зелёный):

```console
$ tmp=$(mktemp -d); python3 -c 'import json,sys;d="hooks/testdata/claude-code/";t=sys.argv[1];[json.dump(dict(json.load(open(d+f)),**e),open(t+"/"+n,"w")) for f,e,n in [("tool-done-agent-launch.json",{"session_id":"dk966"},"launch.json"),("tool-done-bash-background.json",{"session_id":"dk966","agent_id":"ac08b50abbc64e119","agent_type":"general-purpose"},"shell.json")]]' "$tmp" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 hooks/agent-watch.py --hook claude-code < "$tmp/launch.json" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 hooks/agent-watch.py --hook claude-code < "$tmp/shell.json" && python3 -c 'import json,sys;d=json.load(open(sys.argv[1]));print(sorted((k,v["job"]) for k,v in d["agents"].items()))' "$tmp/agents/dk966.json" && grep "запущено субагентом" "$tmp/agents/agents.log"
[('ac08b50abbc64e119', 'subagent'), ('bpmzinhpl', 'shell')]
2026-09-13T21:30:30 сессия dk966 разряд shell работа bpmzinhpl событие запуск текст «sleep 8; echo готово, запущено субагентом ac08b50abbc64e119»
```

Шаг 3 (зелёный):

```console
$ base=$(git log --format=%H --grep=DK-966 -- kit/skills/board-task/task-run.py | tail -1); regcheck -C kit/skills/board-task --base "$base^" --tests kit/skills/board-task/task_run_test.py -- python3 -m unittest task_run_test.TestLiveHead.test_extra_turn_end_does_not_close_a_pass task_run_test.TestLiveHead.test_wait_starts_the_idle_count_over
тест краснеет на b44a92780e2efa3064049a478a06448960fddd6e^ и проходит на текущем коде, регрессия закрыта (тесты: kit/skills/board-task/task_run_test.py)
```

Шаг 4 (зелёный, прогнан ревью 2026-09-13):

```console
$ regcheck -C hooks --base 80e4fca1^ --tests hooks/agent_watch_test.py -- python3 -m unittest agent_watch_test.Watch.test_command_from_a_subagent_keeps_both_records
тест краснеет на 80e4fca1^ и проходит на текущем коде, регрессия закрыта (тесты: hooks/agent_watch_test.py)
```

- Обкатка: 2026-09-13 22:58, свежее дерево 526062dd64a4, сценарий cac37cfc, временный HOME, утилит дерева 11, шагов 4, все зелёные.

Шаг 1 (зелёный):

```console
$ tmp=$(mktemp -d); base=$(git log --format=%H --grep=DK-966 -- hooks/agent-watch.py | tail -1); git worktree add -q --detach "$tmp/old" "$base^" && python3 -c 'import json,sys;d="hooks/testdata/claude-code/";t=sys.argv[1];[json.dump(dict(json.load(open(d+f)),**e),open(t+"/"+n,"w")) for f,e,n in [("tool-done-agent-launch.json",{"session_id":"dk966"},"launch.json"),("tool-done-bash-background.json",{"session_id":"dk966","agent_id":"ac08b50abbc64e119","agent_type":"general-purpose"},"shell.json")]]' "$tmp" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 "$tmp/old/hooks/agent-watch.py" --hook claude-code < "$tmp/launch.json" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 "$tmp/old/hooks/agent-watch.py" --hook claude-code < "$tmp/shell.json" && python3 -c 'import json,sys;d=json.load(open(sys.argv[1]));print(sorted((k,v["job"]) for k,v in d["agents"].items()))' "$tmp/agents/dk966.json" && git worktree remove --force "$tmp/old"
[('ac08b50abbc64e119', 'shell')]
```

Шаг 2 (зелёный):

```console
$ tmp=$(mktemp -d); python3 -c 'import json,sys;d="hooks/testdata/claude-code/";t=sys.argv[1];[json.dump(dict(json.load(open(d+f)),**e),open(t+"/"+n,"w")) for f,e,n in [("tool-done-agent-launch.json",{"session_id":"dk966"},"launch.json"),("tool-done-bash-background.json",{"session_id":"dk966","agent_id":"ac08b50abbc64e119","agent_type":"general-purpose"},"shell.json")]]' "$tmp" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 hooks/agent-watch.py --hook claude-code < "$tmp/launch.json" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 hooks/agent-watch.py --hook claude-code < "$tmp/shell.json" && python3 -c 'import json,sys;d=json.load(open(sys.argv[1]));print(sorted((k,v["job"]) for k,v in d["agents"].items()))' "$tmp/agents/dk966.json" && grep "запущено субагентом" "$tmp/agents/agents.log"
[('ac08b50abbc64e119', 'subagent'), ('bpmzinhpl', 'shell')]
2026-09-13T22:58:14 сессия dk966 разряд shell работа bpmzinhpl событие запуск текст «sleep 8; echo готово, запущено субагентом ac08b50abbc64e119»
```

Шаг 3 (зелёный):

```console
$ base=$(git log --format=%H --grep=DK-966 -- kit/skills/board-task/task-run.py | tail -1); regcheck -C kit/skills/board-task --base "$base^" --tests kit/skills/board-task/task_run_test.py -- python3 -m unittest task_run_test.TestLiveHead.test_extra_turn_end_does_not_close_a_pass task_run_test.TestLiveHead.test_wait_starts_the_idle_count_over task_run_test.TestPasses.test_wait_starts_the_idle_count_over
тест краснеет на 4dd0b5fb6b84d3ffef2ab3c35b5e126b96f1f6b2^ и проходит на текущем коде, регрессия закрыта (тесты: kit/skills/board-task/task_run_test.py)
```

Шаг 4 (зелёный):

```console
$ base=$(git log --format=%H --grep=DK-966 -- hooks/agent-watch.py | tail -1); regcheck -C hooks --base "$base^" --tests hooks/agent_watch_test.py -- python3 -m unittest agent_watch_test.Watch.test_command_from_a_subagent_keeps_both_records
тест краснеет на 5e38317e822114c4b8a33b3832b117494697c750^ и проходит на текущем коде, регрессия закрыта (тесты: hooks/agent_watch_test.py)
```

- Проверка: 2026-09-13 23:27, основной чекаут /Users/rider/projects/devkit, ветки и worktree задачи уже нет, шагов 4, все зелёные, вывод дословно совпал с ожидаемым.

Шаг 1 (зелёный):

```console
$ tmp=$(mktemp -d); base=$(git log --format=%H --grep=DK-966 -- hooks/agent-watch.py | tail -1); git worktree add -q --detach "$tmp/old" "$base^" && python3 -c 'import json,sys;d="hooks/testdata/claude-code/";t=sys.argv[1];[json.dump(dict(json.load(open(d+f)),**e),open(t+"/"+n,"w")) for f,e,n in [("tool-done-agent-launch.json",{"session_id":"dk966"},"launch.json"),("tool-done-bash-background.json",{"session_id":"dk966","agent_id":"ac08b50abbc64e119","agent_type":"general-purpose"},"shell.json")]]' "$tmp" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 "$tmp/old/hooks/agent-watch.py" --hook claude-code < "$tmp/launch.json" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 "$tmp/old/hooks/agent-watch.py" --hook claude-code < "$tmp/shell.json" && python3 -c 'import json,sys;d=json.load(open(sys.argv[1]));print(sorted((k,v["job"]) for k,v in d["agents"].items()))' "$tmp/agents/dk966.json" && git worktree remove --force "$tmp/old"
[('ac08b50abbc64e119', 'shell')]
```

Шаг 2 (зелёный):

```console
$ tmp=$(mktemp -d); python3 -c 'import json,sys;d="hooks/testdata/claude-code/";t=sys.argv[1];[json.dump(dict(json.load(open(d+f)),**e),open(t+"/"+n,"w")) for f,e,n in [("tool-done-agent-launch.json",{"session_id":"dk966"},"launch.json"),("tool-done-bash-background.json",{"session_id":"dk966","agent_id":"ac08b50abbc64e119","agent_type":"general-purpose"},"shell.json")]]' "$tmp" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 hooks/agent-watch.py --hook claude-code < "$tmp/launch.json" && DEVKIT_AGENT_WATCH_DIR="$tmp/agents" python3 hooks/agent-watch.py --hook claude-code < "$tmp/shell.json" && python3 -c 'import json,sys;d=json.load(open(sys.argv[1]));print(sorted((k,v["job"]) for k,v in d["agents"].items()))' "$tmp/agents/dk966.json" && grep "запущено субагентом" "$tmp/agents/agents.log"
[('ac08b50abbc64e119', 'subagent'), ('bpmzinhpl', 'shell')]
2026-09-13T23:26:52 сессия dk966 разряд shell работа bpmzinhpl событие запуск текст «sleep 8; echo готово, запущено субагентом ac08b50abbc64e119»
```

Записей в реестре две, запись исполнителя `ac08b50abbc64e119` осталась разрядом `subagent`, в журнале сдач стоит строка «запущено субагентом ac08b50abbc64e119».

Шаг 3 (зелёный):

```console
$ base=$(git log --format=%H --grep=DK-966 -- kit/skills/board-task/task-run.py | tail -1); regcheck -C kit/skills/board-task --base "$base^" --tests kit/skills/board-task/task_run_test.py -- python3 -m unittest task_run_test.TestLiveHead.test_extra_turn_end_does_not_close_a_pass task_run_test.TestLiveHead.test_wait_starts_the_idle_count_over task_run_test.TestPasses.test_wait_starts_the_idle_count_over
тест краснеет на 4dd0b5fb6b84d3ffef2ab3c35b5e126b96f1f6b2^ и проходит на текущем коде, регрессия закрыта (тесты: kit/skills/board-task/task_run_test.py)
```

Шаг 4 (зелёный):

```console
$ base=$(git log --format=%H --grep=DK-966 -- hooks/agent-watch.py | tail -1); regcheck -C hooks --base "$base^" --tests hooks/agent_watch_test.py -- python3 -m unittest agent_watch_test.Watch.test_command_from_a_subagent_keeps_both_records
тест краснеет на 5e38317e822114c4b8a33b3832b117494697c750^ и проходит на текущем коде, регрессия закрыта (тесты: hooks/agent_watch_test.py)
```
