# DK-1000: тесты хука квоты и цикла цели красны под нагрузкой прогона слияния

записан 2026-09-15
приоритет: средний

## Черновик

### Ситуация

2026-09-15, два подряд прогона `shipctl merge DK-996` с ключом test по умолчанию (parallel.py, восемь воркеров). Первый упал на hooks: `quota_refresh_test.TestQuotaRefresh.test_a_real_refusal_still_reaches_the_journal`, в журнале только отметка времени. Второй на goal-loop: `goal_run_test.GoalRunTests.test_wait_mark_holds_the_loop_instead_of_empty_turns`, строки «виток 1 кончился ожиданием» в журнале нет. По одному оба теста зелёные пять раз из пяти, а слияние прошло на `-j 3`.

### Осложнение

Слияние правки, которая хуков и цикла не трогает, стоит два лишних прогона по пять минут, а сессия сама решает, сколько раз повторять. Соседняя строка DK-999 держит другой флейк той же природы.

### Вопрос

Общая ли причина у трёх флейков (ожидание записи журнала по времени) и чинится ли она в тестах одним приёмом?

### Гипотеза

Прогон слияния на восьми воркерах зелёный на чистом дереве без повторов.
