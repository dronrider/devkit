# DK-116: taskctl: файл черновика всплыл в рабочем дереве после оформления задачи. DK-113 оформили из черновика коммитом 6171fef (файл из docs/tasks/drafts/ при этом удалён из гита), но docs/tasks/drafts/DK-113.md остался на диске неотслеживаемым, с временем правки позже коммита оформления, и висел в git status всю работу над задачей. Содержимое совпадает с исходным черновиком. Разобрать, кто его вернул: сам add --id (копирует вместо переноса и оставляет исходник), draft list, либо посторонняя сессия. Проверять на синтетической доске: оформить черновик и убедиться, что в drafts/ ничего не осталось.

записан 2026-08-04

## Черновик

taskctl: файл черновика всплыл в рабочем дереве после оформления задачи. DK-113 оформили из черновика коммитом 6171fef (файл из docs/tasks/drafts/ при этом удалён из гита), но docs/tasks/drafts/DK-113.md остался на диске неотслеживаемым, с временем правки позже коммита оформления, и висел в git status всю работу над задачей. Содержимое совпадает с исходным черновиком. Разобрать, кто его вернул: сам add --id (копирует вместо переноса и оставляет исходник), draft list, либо посторонняя сессия. Проверять на синтетической доске: оформить черновик и убедиться, что в drafts/ ничего не осталось.

## Грумминг

- 2026-08-05, отложен: две версии из трёх сняты разбором DK-129 (прогон на синтетике и чтение кода: cmdDraftList в накопитель не пишет), осталась посторонняя сессия, ждём повторного случая с git status в момент появления файла
