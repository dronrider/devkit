# DK-122: shipctl: замок .devkit/ship.lock пуст и брошенный от живого не отличить. При заходе на shipctl start (2026-08-05, DK-121) команда отказала «конвейер занят», а файл замка был нулевого размера и с временем правки трёхдневной давности: живой merge в соседней сессии шёл, но узнать это удалось только через pgrep по shipctl. Разобрать, писать ли в замок владельца (pid, ID задачи, время старта) и снимать ли протухший самому: тогда отказ говорит, кто держит и с какого времени, а брошенный после падения замок не требует ручного rm.

## Черновик

shipctl: замок .devkit/ship.lock пуст и брошенный от живого не отличить. При заходе на shipctl start (2026-08-05, DK-121) команда отказала «конвейер занят», а файл замка был нулевого размера и с временем правки трёхдневной давности: живой merge в соседней сессии шёл, но узнать это удалось только через pgrep по shipctl. Разобрать, писать ли в замок владельца (pid, ID задачи, время старта) и снимать ли протухший самому: тогда отказ говорит, кто держит и с какого времени, а брошенный после падения замок не требует ручного rm.
