# DK-136: Двусторонний чат с идущей сессией агента: писать в живой процесс и читать его ответ. Витки поднимаются claude -p (goal-run.sh), у неинтерактивного процесса входа нет, вывод буферизуется до конца сессии, а tmux-сессия принадлежит оболочке goal-run, не агенту, так что send-keys шлёт команды оболочке. Отложено на грумминге цели DK-112 (2026-08-05): переписка там решена через состояние на диске, которое подхватывает следующий виток, и для wait-human этого хватает. Живой чат нужен там, где ответ требуется прямо в идущей работе, без стопа витка. Разбирать надо механику: интерактивный клиент в своей tmux-сессии с чтением панели и send-keys (хрупко на перерисовке TUI и промптах разрешений, трогает сердце автономного режима), headless с потоковым вводом, либо что-то ещё. Смежно с DK-117 (след хода витка по ходу, а не по концу).

записан 2026-08-05

## Грумминг 2026-08-11: проверка актуальности

Нехватка на месте, но ландшафт с записи сдвинулся в трёх местах.

- Оболочка теперь goal-run.py (kit/skills/goal-loop/), не goal-run.sh.
  Механика та же: виток поднимает claude -p со stdout в PIPE, входа у
  процесса нет, вывод буферизуется до конца витка (комментарий в run_turn
  говорит это прямо), tmux-сессия принадлежит оболочке.
- Обход «состояние на диске» оброс продуктом: ручка message дашборда кладёт
  сообщение в раздел «Входящие» файла цели, и код прямо отказывается писать
  в идущий процесс, сообщение уходит следующему витку
  (tools/dashboard/messages.go). Асинхронная половина переписки закрыта
  вместе с UI.
- DK-117 закрыта 2026-08-09: след хода витка виден по ходу, читающая
  половина частично есть. Открытым остаётся ровно вход в живой процесс и
  ответ без стопа витка.

Дублей нет: DK-256 (экран «Агенты», переход в чат) ведёт к переписке через
«Входящие», а не к живому каналу; совпадения с DK-269 и DK-202 случайные,
по слову «чат» в другом смысле.

## Черновик

Двусторонний чат с идущей сессией агента: писать в живой процесс и читать его ответ. Витки поднимаются claude -p (goal-run.sh), у неинтерактивного процесса входа нет, вывод буферизуется до конца сессии, а tmux-сессия принадлежит оболочке goal-run, не агенту, так что send-keys шлёт команды оболочке. Отложено на грумминге цели DK-112 (2026-08-05): переписка там решена через состояние на диске, которое подхватывает следующий виток, и для wait-human этого хватает. Живой чат нужен там, где ответ требуется прямо в идущей работе, без стопа витка. Разбирать надо механику: интерактивный клиент в своей tmux-сессии с чтением панели и send-keys (хрупко на перерисовке TUI и промптах разрешений, трогает сердце автономного режима), headless с потоковым вводом, либо что-то ещё. Смежно с DK-117 (след хода витка по ходу, а не по концу).
