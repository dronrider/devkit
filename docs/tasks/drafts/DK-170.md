# DK-170: taskctl add падает на коммите, когда черновик не был закоммичен: draft без -m оставляет docs/tasks/drafts/<ID>.md вне индекса, а add -m --push кладёт старый путь в pathspec коммита и git отвечает did not match any file(s) known to git. Транзакция рвётся посередине: перенос файла и строка доски уже сделаны, коммита и пуша нет, докатывать приходится руками. Либо draft коммитит файл всегда (как остальные изменяющие команды), либо add не ставит в pathspec путь, которого нет в git. Воспроизведено 2026-08-07 на it-road-course (IRC-099).

записан 2026-08-07

## Черновик

taskctl add падает на коммите, когда черновик не был закоммичен: draft без -m оставляет docs/tasks/drafts/<ID>.md вне индекса, а add -m --push кладёт старый путь в pathspec коммита и git отвечает did not match any file(s) known to git. Транзакция рвётся посередине: перенос файла и строка доски уже сделаны, коммита и пуша нет, докатывать приходится руками. Либо draft коммитит файл всегда (как остальные изменяющие команды), либо add не ставит в pathspec путь, которого нет в git. Воспроизведено 2026-08-07 на it-road-course (IRC-099).
