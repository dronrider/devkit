# DK-175: Цель: запускатор отдельного инстанса vscode с конфигом glm: команда ставит CLAUDE_CONFIG_DIR на каталог с настройками z.ai (base URL и токен в секции env его settings.json), берёт worktree задачи через shipctl start и открывает в нём code -n. Сессии двух окон не пересекаются ни конфигом, ни рабочим деревом: в одном окне claude ходит в подписку Anthropic, в другом в endpoint z.ai. У запускатора dry-run, который печатает окружение (CLAUDE_CONFIG_DIR, base URL) без запуска редактора. Сейчас упомянут в составе цели DK-171 (DoD пункт 2); при оформлении решить, выносится ли оттуда отдельной целью, и тогда DK-171 сузить.

записан 2026-08-07

## Черновик

Цель: запускатор отдельного инстанса vscode с конфигом glm: команда ставит CLAUDE_CONFIG_DIR на каталог с настройками z.ai (base URL и токен в секции env его settings.json), берёт worktree задачи через shipctl start и открывает в нём code -n. Сессии двух окон не пересекаются ни конфигом, ни рабочим деревом: в одном окне claude ходит в подписку Anthropic, в другом в endpoint z.ai. У запускатора dry-run, который печатает окружение (CLAUDE_CONFIG_DIR, base URL) без запуска редактора. Сейчас упомянут в составе цели DK-171 (DoD пункт 2); при оформлении решить, выносится ли оттуда отдельной целью, и тогда DK-171 сузить.
