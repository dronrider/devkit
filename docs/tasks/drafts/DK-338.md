# DK-338: Редизайн доски: решения по ходу задач. Живое состояние этапа (activity, since) в gitignored-файле ~/.devkit/runs/<ID>, пишет конвейер при запуске этапа, читает дашборд; в файл задачи история едет одним пакетом при смене статуса, отдельных коммитов на переход нет. В строке доски кружок статуса у начала (работает/снаружи/очередь) и отдельная колонка хода цветным словом: разработка зелёным, ревью жёлтым, снаружи серым, уточнение вторым рядом. Экран задачи несёт карту «Ход работы»: идёт сейчас из живого файла, прошедшие этапы из журнала дашборда. Макеты устояли в Claude Design, проект «Дашборд devkit», экраны 01 и 02.

записан 2026-08-15

## Черновик

Редизайн доски: решения по ходу задач. Живое состояние этапа (activity, since) в gitignored-файле ~/.devkit/runs/<ID>, пишет конвейер при запуске этапа, читает дашборд; в файл задачи история едет одним пакетом при смене статуса, отдельных коммитов на переход нет. В строке доски кружок статуса у начала (работает/снаружи/очередь) и отдельная колонка хода цветным словом: разработка зелёным, ревью жёлтым, снаружи серым, уточнение вторым рядом. Экран задачи несёт карту «Ход работы»: идёт сейчас из живого файла, прошедшие этапы из журнала дашборда. Макеты устояли в Claude Design, проект «Дашборд devkit», экраны 01 и 02.
