# DK-365: Цикл цели в одном окне, исполнители на любом операторе

записан 2026-08-15

## Черновик

Цикл цели в одном окне, исполнители на любом операторе

Сегодня цикл цели живёт во втором окне на glm-worktree, и пользователь дежурит
в двух окнах. Механизм делегирования уже есть (agentctl run поднимает уехавшую
ступень подпроцессом claude -p с окружением второй подписки), но до жизни в
одном окне ему не хватает трёх вещей: модель субагенту в glm-сессии передаётся
только опусканием model, иначе расход утекает в квоту claude-code (черновик
DK-307); headless-запуск убивает фоновых субагентов через 10 минут после конца
хода, длинный конвейер в делегате гибнет; диспетчер расходует квоту того
харнеса, в котором сидит, поэтому цикл и уводили целиком в glm-окно.

Нужно довести делегирование до состояния, где диспетчер сидит в одном окне,
а исполнители и ревьюверы уходят ступенью вердикта на любой оператор, и второе
окно с ручным дежурством не нужно. Зонтик над DK-307, при грумминге решить,
сливать их или резать цепочкой.
