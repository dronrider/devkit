# DK-368: Параллельный прогон suite.py разваливает временные стенды devkitctl: на merge DK-338 упало 16 тестов с пустым отпечатком копии devkit, пропавшими файлами стенда (devkitctl.py, tools/taskctl) и FileNotFoundError 'git' в PATH дочернего процесса, точечные прогоны тех же тестов зелёные. Похоже на взаимовлияние классов через общие ресурсы; повторный merge прошёл, флак может отбить любой следующий.

записан 2026-08-15

## Черновик

Параллельный прогон suite.py разваливает временные стенды devkitctl: на merge DK-338 упало 16 тестов с пустым отпечатком копии devkit, пропавшими файлами стенда (devkitctl.py, tools/taskctl) и FileNotFoundError 'git' в PATH дочернего процесса, точечные прогоны тех же тестов зелёные. Похоже на взаимовлияние классов через общие ресурсы; повторный merge прошёл, флак может отбить любой следующий.
