# DK-416: Тест hooks.notify_test.TestHookFocusProcess.test_session_window_in_focus_keeps_quiet недетерминирован: в полном прогоне parallel.py упал с уведомлением от чужой сессии ('devkit-dk-034: ход закончен|первая строка'), при запуске тем же деревом по одному зелёный. Поймано 2026-08-17 на merge DK-288, слияние встало на ровном месте и потребовало повторного захода. Похоже, тест видит состояние окон или почтовый ящик соседних сессий вместо своего временного окружения.

записан 2026-08-17
приоритет: средний

## Черновик

Тест hooks.notify_test.TestHookFocusProcess.test_session_window_in_focus_keeps_quiet недетерминирован: в полном прогоне parallel.py упал с уведомлением от чужой сессии ('devkit-dk-034: ход закончен|первая строка'), при запуске тем же деревом по одному зелёный. Поймано 2026-08-17 на merge DK-288, слияние встало на ровном месте и потребовало повторного захода. Похоже, тест видит состояние окон или почтовый ящик соседних сессий вместо своего временного окружения.
