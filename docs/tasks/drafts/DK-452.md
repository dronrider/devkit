# DK-452: Текст замечания ревью уезжает в bash и исполняется подстановкой

записан 2026-08-19
приоритет: высокий

## Черновик

### Ситуация

Ревьювер DK-438 позвал taskctl review add с текстом в двойных кавычках, а внутри
стояли обратные кавычки вокруг имени команды: bash разобрал их как подстановку и
запустил devkitctl update. Тот дошёл до отказа «чекаут стоит на ветке main, а не
на релизном теге» и мутации не сделал, но повезло: тот же приём с другой командой
внутри имени файла или утилиты сработал бы молча, а замечания ревью пишутся про
команды постоянно. Замечено 2026-08-19 на ревью DK-438.

Кандидаты: научить агентов давать такие тексты через stdin или heredoc, а не
аргументом (правило в определениях ревьюверов и в скилле board-ship), либо
принимать текст замечания из stdin по умолчанию, как это уже сделано у
taskctl draft без аргумента. Та же ловушка живёт у draft, add --title и
move --reason.

### Осложнение

### Вопрос

### Гипотеза

## Грумминг

- 2026-08-25, отложен: Прогон 2026-08-25 ловушку подтвердил: bash съедает подстановку в двойных кавычках до вызова утилиты, review add требует ровно два позиционных аргумента и stdin не читает (needArgs 2,2, чтение stdin заведено только у draft), правила писать такие тексты через heredoc нет ни в определениях ревьюверов, ни в скилле board-ship. Дублей по доске, архиву и накопителю нет. Ранг собирается на 25+4+3+5+2 = 39 (P2, цена S) при узком предмете, но два вопроса груминга ушли без ответа двумя заходами taskctl ask: чем работа кончается, правилом агентам с приёмом текста со stdin у текстовых команд taskctl или машинным рубежом хуком на подстановку в вызовах утилит devkit (второй путь тянет цену на M и накрывает заодно shipctl с agentctl), и считать ли серьёзность Major, раз обход дешёвый, или Critical, раз чужая команда один раз уже выполнилась молча. Вернуться, когда на них ответят: без ответов DoD и полоса приоритета ставятся наугад.
