# DK-484: Пикер элементов в чате: клик по месту экрана едет агенту описателем

записан 2026-08-22
приоритет: низкий

## Черновик

### Ситуация

Замечание пользователя 2026-08-22 по POC DK-397. Правки интерфейса заказываются
словами («кнопка в строке, где карандаш»), и адрес элемента каждый раз
пересказывается. Предложение: кнопка в чате включает режим выбора, клики
собирают элементы страницы в набор A+B+C фишками над строкой отправки, реплика
уходит агенту вместе с ними.

### Осложнение

Словесный адрес элемента неоднозначен и дорог: пользователь описывает место,
агент переспрашивает или промахивается, круг повторяется на каждой правке
интерфейса.

### Вопрос

Состав описателя: хватает ли тега, классов, id, data-атрибутов, обрезанного
текста, цепочки родителей на пару уровней и экрана из адреса, или нужны ещё
координаты и размер под снимок. Включать ли панель чата в зону выбора.

### Гипотеза

В чате есть режим выбора: клик подсвечивает и добавляет элемент, Esc выходит,
фишки снимаются крестиком. Реплика уносит структурные блоки описателей тем же
путём, что нынешний selection. Живой DOM-узел не хранится: описатель снимается
в момент клика и не протухает от перерисовок; у строк задач в него входит ID
из dataset. Классы el("класс") говорящие, исполнитель находит место в app.js
грепом по ним.

### Что снял разбор

Проверка актуальности 2026-08-25 прошла по коду дашборда. Нехватка на месте.
Панель чата собирает `chatPanel` (`tools/dashboard/static/app.js`). Реплика
уходит чистым текстом, `sendMessage` зовёт `out.send(text)`. Режима выбора в
файле нет, прикрепления к реплике тоже. `getSelection` не встречается ни разу,
а `selection` живёт в сохранении курсора текстового поля (строки 100-117).
Фраза черновика про «нынешний selection» описывает будущий путь. Транспорт
задача несёт свой. Описатель поедет через `.devkit/chat` и `hooks/chat-in.py`,
где реплика сегодня строка, и вид записи придётся выбрать.

Дублей нет ни по доске, ни по архиву, ни по накопителю. Соседние строки про
дашборд правят перерисовку (DK-412), панель (DK-435) и ленту (DK-372).

Границы правки видны. Их три: браузер (`app.js`, `style.css`), письмо реплики,
разбор описателя на стороне агента. `RANKING.md` держит неопределённость таких
правок не ниже 3.

## Грумминг

- 2026-08-26, отложен: нет ответа на пачку груминга: цена словесного адреса (серьёзность), зона выбора и нужен ли снимок элемента; вопросы лежат в .devkit/draft-DK-484.question
