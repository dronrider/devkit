# DK-554: Скиллы только для человека не платят за описание в реестре каждой сессии

записан 2026-08-28
приоритет: средний

## Черновик

### Ситуация

Все 14 скиллов devkit сидят в реестре с описаниями по три-четыре строки,
реестр грузится в каждую сессию, и по нему модель решает, что позвать.
Часть скиллов зовёт только человек командой: goal-start, board-team,
live-core по замыслу не должны включаться сами. У харнеса есть флаг,
который убирает скилл из модельного вызова, и такой скилл не тратит место в
реестре.

### Осложнение

Описания всех скиллов лежат в контексте каждой сессии и субагента
независимо от того, нужен ли скилл в этой работе, а weigh считает вес ядра
и не видит вес реестра. Заодно длинное описание это единственное, по чему
модель выбирает скилл, и в нём смешаны условие запуска и пересказ
содержимого.

### Вопрос

Какие скиллы честно человеческие, а какие диспетчер зовёт из цикла цели по
имени, и не сломает ли флаг вызов из goal-loop. Как замерить вес реестра:
через devkitctl weigh или отдельной цифрой в stats.

### Гипотеза

У каждого скилла помечено, кто его зовёт; человеческие убраны из реестра
флагом, у остальных описание сведено к условию запуска в одну-две строки, а
что внутри, читается уже в самом скилле. devkitctl weigh показывает вес
реестра отдельной строкой, и doctor держит порог.
