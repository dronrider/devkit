# DK-613: Скилл work-plan: план работ сессии ведёт одно правило и утилита

записан 2026-08-30
приоритет: высокий

## Черновик

### Ситуация

Правило «веди план работ файлом ~/.devkit/plans/<ID сессии>.json» живёт сегодня в пяти копиях: в четырёх kit/agents/exec-*.md и в приписке заказа дашборда (planRule в POC-дереве, tools/dashboard/chats.go). Читает файлы только кольцо прогресса дашборда, в самом devkit читателей нет. Разбор дашборда терпит три формата (массив, объекты с полями stages/steps/items, состояние done наравне с completed), потому что агенты пишут файл кто как. Замена на TodoWrite проверена 30.08 и не подходит: у сессий на Fable инструмента в наборе нет вовсе (1 транскрипт из 588 за неделю), а у GLM есть; файлов планов за ту же неделю 153.

### Осложнение

Правило есть, а текста, который его задаёт, в правилах devkit нет: человек, читающий RULES, о плане не узнаёт, а агент получает его приписками, которые панель потом вырезает из ленты (cutOrderRules) и корпус прозы отсеивает (TAIL_MARKS в prose.py). Пять копий расходятся, формат плывёт, запасной адрес chat-N.json при пустом CLAUDE_CODE_SESSION_ID подпирает GLM-контур.

### Вопрос

Утилита это подкоманда taskctl plan или своя по правилу раскладки README «Раскладка»? Чистка планов кончившихся сессий: по возрасту доктором или при снятии сессии?

### Гипотеза

Есть скилл work-plan: один формат файла, адрес по ID сессии из окружения или события хука, у субагента <ID>-sub.json, чужой не перезаписывать, ритм ведения. Утилита пишет и показывает план (show печатает план живой сессии в терминале), формат держат её тесты. Вход в скилл одной строкой в RULES.core.md, exec-* и board-chat отсылают к нему, копий текста нет. Дашборд остаётся одним из читателей, а разбор трёх форматов у него уходит.
