# DK-616: Реплика панели в tmux-сессию идёт через send-keys, сокет вне tmux

записан 2026-08-30
приоритет: средний

## Черновик

### Ситуация

Доставка реплики из панели дашборда (POC-дерево, chats.go около 2040) идёт тремя дорогами. Первым пробуется сокет клиента (рамка «сообщение от другой сессии», from-name=dashboard), запасным - tmux send-keys, а при ожидании taskctl ask используется вход .devkit/chat. Из-за рамки заказу понадобилось channelRule («это человек, отвечай на вы текстом в ленту»). Грумер DK-509 и чат 93828026 всё равно отвечали человеку в третьем лице. По документации (проверено 30.08) рамку сокета сменить нельзя. Без рамки и с побудкой сессии текст приходит только через tty (send-keys) или Remote Control.

### Осложнение

Все сессии, которые поднимает дашборд, живут в tmux. Для них рамки можно не иметь вовсе, а правило послушания подпирает выбранный порядок дорог. Сессии не из tmux (vscode, терминал человека) без сокета не слышны, и там рамка остаётся.

### Вопрос

Как в момент доставки отличить клиент на диалоге разрешений от замороженного терминала, где send-keys глотается без эха? Панель метит дорогу в chatSayDone, и реплика не должна теряться молча.

### Гипотеза

Для tmux-сессий реплика человека приходит как его собственный ввод, без рамки и без правила. Для остальных сессий сокет несёт подпись человека, не имени приложения. Так devkit говорит соглашением в board-chat. Стенд проверяет реплику в спящую сессию, в сессию на диалоге разрешений и в замороженный терминал.

## Ход работы

- Вычитка: 1 файл, 4 правки, 0 пометок, 2026-08-30.
