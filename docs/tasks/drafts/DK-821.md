# DK-821: Ворот тестов shipctl merge узнаёт стенды testdata/*.mjs дашборда

записан 2026-09-05
приоритет: средний

## Черновик

### Ситуация

2026-09-05, слияние DK-817: shipctl merge отбил ветку словами «в диффе ветки против main нет тестовых файлов». Правка дашборда шла вместе с переведённым стендом tools/dashboard/testdata/poc_draftlock.mjs, и regcheck подтвердил его красноту на main. Предикат isTestFile в tools/shipctl/advice.go знает суффиксы _test.go, _test.py, _test.sh, _test.rs, префикс test_ и каталоги tests/ и src/test. Каталог testdata со стендами .mjs под go-обёртками он не знает.

### Осложнение

У дашборда экранная часть проверяется стендами testdata/poc_*.mjs, go-обёртка при этом не меняется. Каждая такая правка отбивается воротом, и исполнитель либо гасит ворот пометкой «Исключение: тесты», либо правит обёртку ради ворот. Пометка врёт по смыслу, тест у правки есть.

### Вопрос

Считать тестом любой файл в testdata, или только стенды .mjs, у которых есть go-обёртка в том же пакете?

### Гипотеза

Правка с изменённым стендом testdata/*.mjs проходит ворот тестов без пометки-исключения.
