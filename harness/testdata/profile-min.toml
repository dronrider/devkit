# Инструмент, у которого из осей есть только правила: изнутри не детектится,
# делегировать нечем, хуков нет, снять остаток нечем.
[detect]

[rules]
mode = "embed"

[delegate]
mode = "none"

[hooks]

[quota]
