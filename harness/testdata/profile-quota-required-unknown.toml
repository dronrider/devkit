[detect]

[rules]
mode = "embed"

[delegate]
mode = "none"

[hooks]

[quota]
snap = "usage-pane"
buckets = ["week_all"]
required = "week_max"
spend_mini = ["week_all"]
spend_base = ["week_all"]
spend_pro = ["week_all"]
spend_max = ["week_all"]
