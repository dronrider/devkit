# Скиллы инструменту доступны, но сам он их не подхватывает: читаются по
# указателю из правил, и раскладывать их некуда, поэтому dir не нужен.
[detect]

[rules]
mode = "embed"

[delegate]
mode = "none"

[hooks]

[quota]

[skills]
discovery = "manual"
