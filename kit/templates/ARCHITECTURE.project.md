<!-- Шаблон docs/ARCHITECTURE.md: заменить подстановки в угловых скобках,
лишние разделы и этот комментарий убрать. Пустой скелет доктора не пугает:
находку про якорь снимает уже он сам, наполнение приносит ревью по ходу
работы (DK-1179). Правило записывается одной строкой с адресом на образец:
`- <правило одной фразой>; образец <путь>, решено <ID> <дата>`. Карту
компонентов документ не дублирует, она в docs/map.md. -->
# Архитектура <название проекта>

## Границы

<Что за что отвечает и куда какой стороне ходить нельзя: слои, модули,
направление зависимости, места, где данные пересекают границу.>

## Инварианты

<Утверждения, которые правка не ломает: один источник правды для сущности,
порядок записи, схема имён снаружи, обратная совместимость ключа конфига.>

## Куда кладётся новое

<Правило места для нового файла, модуля, команды или таблицы.>
