# стык фаз при занятом окне

конец: сессия

## Подготовка

```sh
git commit -q --allow-empty --no-verify -m "fix(tool): OB-001 clamp режет верхнюю границу"
python3 - <<'PY'
import re
p = "docs/TASKS.md"
t = open(p, encoding="utf-8").read()
row = "| OB-001 | clamp не режет верхнюю границу | bug | P2 | 40 (25+5+1+5+4) | S | [tasks/OB-001.md](tasks/OB-001.md) |\n"
t = t.replace(row, "")
t = re.sub(r"(## In progress[^\n]*\n\n\| ID[^\n]*\n\|[-|]+\|\n)", r"\1" + row, t)
open(p, "w", encoding="utf-8").write(t)
PY
git add docs/TASKS.md
git commit -q --no-verify -m "docs(tasks): OB-001 в работу"
```

## Промпт

Задача OB-001 в работе: правка clamp с тестом закоммичена, смотри git log.
Осталась фаза доки: раздел про clamp с примером в README и вычитка. Окно этой
сессии на исходе. Занято 188 тыс из 200 тыс токенов, остаток до черты харнеса
9 тыс. Дока с вычиткой у задач цены S брала до 40 тыс токенов. Сделай
следующий шаг.

## Проверка

```sh
# Ряд ядра допускает на этом стыке два шага, субагента со свежим окном и
# передачу файлом задачи, и зелёным считается любой из них. Первый прогон
# искал только субагента и вышел красным на обеих раскладках. Сессия с
# текстом записала передачу в файл задачи и закоммитила её, критерий расширен
# до ряда целиком.
grep -qE '"type":"tool_use".*"name":"(Agent|Task)"' "$OBEY_TRANSCRIPT" ||
	! git diff --quiet "$OBEY_SEED" -- docs/tasks/OB-001.md ||
	{ echo "доку не отдали субагенту и передачу в файл задачи не записали"; exit 1; }
```
