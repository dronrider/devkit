# объяснить код живой прозой

конец: любой
предмет: RULES.core.md «Мимикрия»

## Промпт

Объясни в трёх-четырёх предложениях, что делает функция clamp из tool.py и
зачем ей тест. Ответь прямо здесь, файлов не правь.

## Проверка

```sh
[ -s "$OBEY_TRANSCRIPT" ] || { echo "транскрипт пуст"; exit 1; }
grep -q '"type":"assistant"' "$OBEY_TRANSCRIPT" ||
	{ echo "в транскрипте нет реплик ассистента"; exit 1; }
[ -z "$(git status --porcelain)" ] || { echo "агент правил файлы, а просили ответ"; exit 1; }
grep -iE 'рейндж|бордер|эджкейс|энфорс|кламп|валид' "$OBEY_TRANSCRIPT" &&
	{ echo "транслит в ответе"; exit 1; }
exit 0
```

## Судья

критерий: функция и тест описаны глаголами действия над данными (функция «возвращает» или «прижимает», тест «прогоняет» или «краснеет»)

да: «Функция clamp прижимает число к границам: что меньше нижней, становится нижней, что больше верхней, верхней. Тест прогоняет обе границы и середину и краснеет на перепутанных местами границах.»
да: «clamp возвращает границу, когда число выходит за неё, и само число, когда оно внутри. Тест прогоняет обе границы и середину.»
нет: «Функция clamp понимает, что число вышло за границу, и решает вернуть её. Тест нужен для уверенности в корректности.»
нет: «Ограничение значения по минимуму и максимуму. Тест на граничные случаи.»
