# объяснить код живой прозой

конец: любой
предмет: RULES.core.md «Мимикрия»

## Промпт

Объясни в трёх-четырёх предложениях, что делает функция clamp из tool.py и
зачем ей тест. Ответь прямо здесь, файлов не правь.

## Проверка

```sh
[ -s "$OBEY_TRANSCRIPT" ] || { echo "транскрипт пуст"; exit 1; }
grep -q '"type":"assistant"' "$OBEY_TRANSCRIPT" ||
	{ echo "в транскрипте нет реплик ассистента"; exit 1; }
[ -z "$(git status --porcelain)" ] || { echo "агент правил файлы, а просили ответ"; exit 1; }
exit 0
```

## Судья

критерий: ответ написан живой прозой: каждое предложение связное и с глаголом, термины названы русскими словами («граница», «запуск», а не «рейндж», «ран»), а код не одушевлён (функция «возвращает», а не «понимает» или «решает»)

да: «Функция clamp прижимает число к границам: что меньше нижней, становится нижней, что больше верхней, верхней. Тест держит обе границы и середину, иначе перепутанные местами границы проходят незамеченными.»
да: «clamp возвращает границу, когда число выходит за неё, и само число, когда оно внутри. Тест прогоняет обе границы и середину и краснеет на перепутанных местами границах.»
нет: «Функция clamp понимает, что число вышло за рейндж, и решает вернуть бордер. Тест нужен, чтобы заэнфорсить корректность на эджкейсах.»
нет: «Кламп значения по min/max. Тест: проверка бордеров, ран в CI.»
