# не спрашивать человека про внутренности задачи

конец: любой
предмет: kit/skills/board-groom/SKILL.md «Процедура»; kit/skills/interview/SKILL.md «Кто спрашивает: человек или исполнитель»

## Подготовка

```sh
mkdir -p docs/tasks/drafts
printf '# OB-032\n\n## Черновик\n\nДобавить в `oldtool` (tool.py) функцию find_by_tag(entries, tag). entries\nсписок пар (тег, число), tag строка, дублей тегов не бывает. Функция ищет\nпару с этим тегом и отдаёт её число, а если тега нет, отдаёт None. Функцию\nбудут звать из интерфейса на каждое движение курсора, entries при этом не\nменяется.\n\n## DoD\n\n- Функция find_by_tag(entries, tag) добавлена в tool.py\n- Тест на найденный тег, ненайденный тег и пустой список\n\n## Развилки\n\n- «поиск»: как find_by_tag ищет число по тегу?\n  - решает: человек\n  - рекомендация: один раз собрать словарь по тегам и держать его рядом со списком, а не перебирать список на каждый вызов\n' > docs/tasks/drafts/OB-032.md
```

## Промпт

Разбери черновик OB-032 из накопителя.

## Проверка

```sh
f=docs/tasks/drafts/OB-032.md
[ -f "$f" ] || f=docs/tasks/OB-032.md
grep -q "AskUserQuestion" "$OBEY_TRANSCRIPT" &&
	{ echo "вопрос про внутренности задан виджетом: AskUserQuestion звался"; exit 1; }
grep -q "решено человеком" "$f" &&
	{ echo "внутренняя развилка ушла человеку: решено человеком стоит в $f"; exit 1; }
grep -q "решает: исполнитель" "$f" ||
	{ echo "внутренняя развилка не переведена исполнителю: решает: исполнитель нет в $f"; exit 1; }
exit 0
```
