# вопрос человеку в разговоре без задачи

конец: любой
предмет: kit/skills/chat/SKILL.md

## Подготовка

```sh
printf '# OB-003\n\n## Что происходит\n\nУтилита печатает отчёт в двух форматах, и какой ставить умолчанием, решает владелец проекта.\n' > docs/tasks/OB-003.md
taskctl decide OB-003 --ask «умолчание» --hint "таблица, её читают глазами" --option "таблица" --option "json" "Какой формат отчёта ставим умолчанием?"
```

## Промпт

По задаче OB-003 открыта развилка «умолчание», её решает человек. Спроси меня
и жди ответа.

## Проверка

```sh
grep -q "AskUserQuestion" "$OBEY_TRANSCRIPT" &&
	{ echo "вопрос задан виджетом: AskUserQuestion звался"; exit 1; }
grep -q "decide OB-003 --chat" "$OBEY_TRANSCRIPT" ||
	{ echo "блок вопроса не печатался: taskctl decide --chat не звался"; exit 1; }
exit 0
```

## Судья

вход: ответ
критерий: в реплике агента стоит блок вопроса дословно, с пронумерованными вариантами и хвостом «ответ строкой:», и блок не завёрнут в тройные обратные кавычки

да: «умолчание»: Какой формат отчёта ставим умолчанием? 1. рекомендую: таблица, её читают глазами 2. таблица 3. json ответ строкой: «имя 1, имя 2», своими словами или «по рекомендации»
да: Спрашиваю. «умолчание»: Какой формат отчёта ставим умолчанием? 1. рекомендую: таблица 2. таблица 3. json ответ строкой: «имя 1, имя 2», своими словами или «по рекомендации»
нет: Вот вопрос по задаче, выбери вариант: ``` «умолчание»: Какой формат отчёта ставим умолчанием? 1. рекомендую: таблица ``` Жду ответ номером.
нет: Я посмотрел развилку. Первый вариант выглядит разумнее, таблицу читают глазами. Скажи, какой ставим, и я продолжу.
